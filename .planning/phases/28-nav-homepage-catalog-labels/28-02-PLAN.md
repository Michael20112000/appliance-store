---
phase: 28-nav-homepage-catalog-labels
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - src/app/globals.css
autonomous: true
requirements:
  - HOME-04
must_haves:
  truths:
    - "Clicking hero link /#kategorii on storefront smoothly scrolls to category section"
    - "Section heading is not hidden under sticky header after scroll"
    - "Users with prefers-reduced-motion get instant scroll, not animated"
    - "Admin routes do not inherit storefront smooth scroll"
  artifacts:
    - path: src/app/globals.css
      provides: Storefront-scoped scroll-behavior and #kategorii scroll-margin-top
      contains: "html:has(#main-content)"
  key_links:
    - from: src/components/home/hero-section.tsx
      to: "#kategorii"
      via: href="/#kategorii"
      pattern: 'href="/#kategorii"'
    - from: src/app/(storefront)/layout.tsx
      to: src/app/globals.css
      via: main id main-content enables :has selector
      pattern: 'id="main-content"'
---

<objective>
Enable CSS-only smooth scrolling for all storefront in-page anchors and offset `#kategorii` below the sticky header, respecting reduced motion.

Purpose: HOME-04 — hero CTA and other hash links feel intentional, not abrupt.
Output: Scoped rules in globals.css; no JS scroll hijack.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/28-nav-homepage-catalog-labels/28-CONTEXT.md
@.planning/phases/28-nav-homepage-catalog-labels/28-RESEARCH.md
@.planning/phases/28-nav-homepage-catalog-labels/28-UI-SPEC.md

<interfaces>
From src/app/(storefront)/layout.tsx:
- main element: id="main-content" className="flex-1"

From src/components/home/hero-section.tsx:
- CTA Link href="/#kategorii" — do not change

From src/components/home/category-grid.tsx:
- section id="kategorii" — scroll-margin applied via CSS selector #kategorii

From src/components/layout/store-header.tsx:
- header: sticky top-0 z-40 — offset target 4.5rem per UI-SPEC

Admin layout: no #main-content — smooth scroll must not apply there
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add storefront-scoped smooth scroll CSS</name>
  <files>src/app/globals.css</files>
  <read_first>
    src/app/globals.css
    src/app/(storefront)/layout.tsx
    src/app/(admin)/admin/layout.tsx
    .planning/phases/28-nav-homepage-catalog-labels/28-CONTEXT.md (D-07, D-08, D-10)
    .planning/phases/28-nav-homepage-catalog-labels/28-UI-SPEC.md (Smooth scroll)
  </read_first>
  <acceptance_criteria>
    - globals.css contains rule html:has(#main-content) { scroll-behavior: smooth; }
    - Under @media (prefers-reduced-motion: reduce), same selector sets scroll-behavior: auto
    - No scroll-behavior: smooth on bare html without :has guard
    - No new npm packages or client JS scroll listeners added
  </acceptance_criteria>
  <action>
    Append after the @layer base block in globals.css (per PATTERNS — no existing scroll rules). Add html:has(#main-content) { scroll-behavior: smooth; } per D-07 and D-08. Add prefers-reduced-motion reduce block setting scroll-behavior auto on the same selector per D-10. Do not modify hero-section or category-grid TSX for scroll behavior.
  </action>
  <verify>
    <automated>grep -c 'html:has(#main-content)' src/app/globals.css | awk '{exit ($1>=1)?0:1}'</automated>
  </verify>
  <done>Storefront pages with main-content get smooth hash navigation; reduced motion respected.</done>
</task>

<task type="auto">
  <name>Task 2: Add #kategorii scroll-margin for sticky header</name>
  <files>src/app/globals.css</files>
  <read_first>
    src/components/layout/store-header.tsx
    src/components/home/category-grid.tsx
    .planning/phases/28-nav-homepage-catalog-labels/28-UI-SPEC.md (scroll-margin 4.5rem)
  </read_first>
  <acceptance_criteria>
    - globals.css contains #kategorii { scroll-margin-top: 4.5rem; } or scroll-margin-top: var(--store-header-offset, 4.5rem)
    - Optional :root --store-header-offset: 4.5rem only if using CSS variable form
    - category-grid.tsx section id kategorii unchanged
    - scroll-margin-top value is at least 4rem and at most 5rem unless manual UAT proves otherwise
  </acceptance_criteria>
  <action>
    In globals.css add #kategorii scroll-margin-top per D-09 and UI-SPEC default 4.5rem (72px). Prefer literal 4.5rem unless discretion uses --store-header-offset on :root for future tuning. Do not add inline scroll-mt classes to category-grid unless CSS file approach blocked.
  </action>
  <verify>
    <automated>grep -c '#kategorii' src/app/globals.css | awk '{exit ($1>=1)?0:1}'</automated>
  </verify>
  <done>Hash navigation lands with category section title visible below sticky header.</done>
</task>

<task type="auto">
  <name>Task 3: Verify admin exclusion and build</name>
  <files>src/app/(admin)/admin/layout.tsx</files>
  <read_first>
    src/app/(admin)/admin/layout.tsx
    .planning/phases/28-nav-homepage-catalog-labels/28-VALIDATION.md (28-02 manual rows)
  </read_first>
  <acceptance_criteria>
    - Admin layout source does not define id main-content (grep confirms)
    - npm run build completes with exit code 0
    - Document in SUMMARY: manual steps for hero CTA scroll and reduced-motion check (HOME-04 UAT)
  </acceptance_criteria>
  <action>
    Confirm admin layout has no main-content id so :has selector excludes admin per RESEARCH Pitfall 2. Run full build. No code changes unless admin accidentally shares main-content id.
  </action>
  <verify>
    <automated>! grep -q 'main-content' src/app/\(admin\)/admin/layout.tsx && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>HOME-04 CSS deliverable complete; admin not scoped; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser CSS | Scroll behavior is presentation-only; no server input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04 | Denial of service | globals.css scroll rules | accept | Smooth scroll is UX-only; no security impact |
| T-28-05 | Tampering | CSS injection | accept | Static file in repo; no user-supplied CSS |
| T-28-SC | Tampering | npm installs | accept | No packages added |
</threat_model>

<verification>
- Automated: grep html:has(#main-content) and #kategorii in globals.css
- npm run build
- Manual: Homepage hero «Переглянути категорії» → #kategorii visible below header
- Manual: OS reduce motion → instant jump to #kategorii
- Manual: /admin route — anchor jumps not globally smoothed (no main-content)
</verification>

<success_criteria>
- HOME-04: storefront hash links scroll smoothly with header offset on #kategorii
- prefers-reduced-motion disables animation
</success_criteria>

<output>
Create `.planning/phases/28-nav-homepage-catalog-labels/28-02-SUMMARY.md` when done
</output>
