---
phase: 28-nav-homepage-catalog-labels
plan: 04
type: execute
wave: 4
depends_on: []
files_modified:
  - src/lib/catalog/catalog-labels.ts
  - src/lib/catalog/catalog-labels.test.ts
  - src/components/catalog/catalog-toolbar.tsx
autonomous: true
requirements:
  - CAT-02
must_haves:
  truths:
    - "Catalog sort select trigger shows Ukrainian labels from central map"
    - "Dropdown items show same labels as trigger — no duplicate hardcoded strings"
    - "Sort URL keys novi, cina-asc, cina-desc unchanged"
    - "Labels read Найновіші, Дорожче, Дешевше per CONTEXT D-16"
  artifacts:
    - path: src/lib/catalog/catalog-labels.ts
      provides: Updated CATALOG_SORT_LABELS
      contains: "Найновіші"
    - path: src/components/catalog/catalog-toolbar.tsx
      provides: SelectItem children from catalogSortLabel only
      pattern: catalogSortLabel
  key_links:
    - from: src/components/catalog/catalog-toolbar.tsx
      to: src/lib/catalog/catalog-labels.ts
      via: catalogSortLabel for SelectValue and SelectItem
      pattern: catalogSortLabel\(
---

<objective>
Centralize catalog sort Ukrainian copy and remove hardcoded duplicate labels in the toolbar select.

Purpose: CAT-02 — shorter, clearer sort options on /katalog.
Output: Updated CATALOG_SORT_LABELS, deduped catalog-toolbar SelectItem rendering, passing catalog-labels tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-nav-homepage-catalog-labels/28-CONTEXT.md
@.planning/phases/28-nav-homepage-catalog-labels/28-RESEARCH.md
@.planning/phases/28-nav-homepage-catalog-labels/28-UI-SPEC.md

<interfaces>
From src/lib/catalog/catalog-labels.ts (current → target per D-16):
- novi: Новіші → Найновіші
- cina-asc: Ціна ↑ → Дешевше
- cina-desc: Ціна ↓ → Дорожче
- catalogSortLabel(sort: CatalogSortValue): string

From src/components/catalog/catalog-toolbar.tsx (bug):
- SelectValue already uses catalogSortLabel(params.sort)
- SelectItem lines 87-89 hardcode Новіші, Ціна ↑, Ціна ↓ — replace with map over const array

From src/lib/catalog/search-params.ts:
- sort enum keys unchanged (D-17) — do not edit in this plan unless accidentally required
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update CATALOG_SORT_LABELS and tests</name>
  <read_first>
    src/lib/catalog/catalog-labels.ts
    src/lib/catalog/catalog-labels.test.ts
    .planning/phases/28-nav-homepage-catalog-labels/28-CONTEXT.md (D-16, D-17, D-18, D-20)
    .planning/REQUIREMENTS.md (CAT-02 — implement CONTEXT «Найновіші» not REQUIREMENTS «Новіше»)
  </read_first>
  <behavior>
    - catalogSortLabel("novi") === "Найновіші"
    - catalogSortLabel("cina-asc") === "Дешевше"
    - catalogSortLabel("cina-desc") === "Дорожче"
  </behavior>
  <acceptance_criteria>
    - CATALOG_SORT_LABELS values exactly: novi Найновіші, cina-asc Дешевше, cina-desc Дорожче per D-16
    - catalog-labels.test.ts asserts all three keys (not only novi and cina-asc)
    - ALL_BRANDS_VALUE and brand helpers untouched
    - No changes to search-params sort parser keys
  </acceptance_criteria>
  <action>
    Update catalog-labels.ts map per locked discussion D-16 (overrides REQUIREMENTS «Новіше» wording). Update catalog-labels.test.ts expectations for all three sort values before or with map change. Do not modify brand-related exports. Optional non-blocking: sync REQUIREMENTS.md CAT-02 line to «Найновіші» in a comment in SUMMARY only — do not block on doc edit per deferred ideas.
  </action>
  <verify>
    <automated>npm test -- --run src/lib/catalog/catalog-labels.test.ts</automated>
  </verify>
  <done>Single source of truth for sort labels matches locked UA copy.</done>
</task>

<task type="auto">
  <name>Task 2: Dedupe catalog-toolbar SelectItem labels</name>
  <read_first>
    src/components/catalog/catalog-toolbar.tsx
    src/lib/catalog/catalog-labels.ts
    .planning/phases/28-nav-homepage-catalog-labels/28-RESEARCH.md (Pitfall 1)
    .planning/phases/28-nav-homepage-catalog-labels/28-UI-SPEC.md (Catalog sort labels)
  </read_first>
  <acceptance_criteria>
    - SelectContent renders three SelectItem entries via iteration over novi, cina-asc, cina-desc const tuple
    - Each SelectItem child text is catalogSortLabel(value) only — no hardcoded Новіші or Ціна strings remain in file
    - SelectValue and onValueChange sort typing unchanged
    - SelectTrigger keeps aria-label Сортування and className w-36 (bump to w-40 only if manual check shows truncation)
  </acceptance_criteria>
  <action>
    Fix known research bug per D-18: replace three hardcoded SelectItem children (lines ~87-89) with map over ["novi","cina-asc","cina-desc"] as const calling catalogSortLabel for display. Keep value={value} on each SelectItem. Grep file after edit to ensure zero occurrences of Ціна ↑ or Новіші as literal UI strings.
  </action>
  <verify>
    <automated>! grep -E 'Ціна ↑|Ціна ↓|Новіші' src/components/catalog/catalog-toolbar.tsx</automated>
  </verify>
  <done>Toolbar trigger and dropdown show identical labels from catalogSortLabel.</done>
</task>

<task type="auto">
  <name>Task 3: Phase gate tests and build</name>
  <read_first>
    .planning/phases/28-nav-homepage-catalog-labels/28-VALIDATION.md
  </read_first>
  <acceptance_criteria>
    - npm test exits 0
    - npm run build exits 0
    - catalog-toolbar.tsx imports catalogSortLabel and CatalogSortValue from catalog-labels
  </acceptance_criteria>
  <action>
    Run full npm test and npm run build per D-21. Document manual CAT-02 check: open /katalog, sort select shows Найновіші, Дорожче, Дешевше; changing sort updates URL with novi/cina-asc/cina-desc only.
  </action>
  <verify>
    <automated>npm test && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>CAT-02 complete; no duplicate sort strings; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client toolbar → URL state | sort param via nuqs; values are enum keys not labels |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-08 | Tampering | Sort label strings | mitigate | Static UA map in TypeScript; labels not derived from URL or user input |
| T-28-09 | Spoofing | Sort URL keys | accept | Keys unchanged; server catalog query already validates sort enum |
| T-28-SC | Tampering | npm installs | accept | No packages added |
</threat_model>

<verification>
- npm test -- --run src/lib/catalog/catalog-labels.test.ts
- npm test && npm run build
- Manual: /katalog sort dropdown — Найновіші, Дорожче, Дешевше; URL still ?sort=novi etc.
</verification>

<success_criteria>
- CAT-02: sort select shows locked short Ukrainian labels from single map
- Known toolbar duplication bug fixed
</success_criteria>

<output>
Create `.planning/phases/28-nav-homepage-catalog-labels/28-04-SUMMARY.md` when done
</output>
