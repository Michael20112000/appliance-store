---
phase: 28-nav-homepage-catalog-labels
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/storefront-auth-links.tsx
  - src/components/layout/store-header-auth.tsx
  - src/components/layout/store-mobile-nav.tsx
  - src/components/layout/store-header.tsx
  - src/components/layout/store-mobile-nav.test.tsx
autonomous: true
requirements:
  - NAV-01
must_haves:
  truths:
    - "D-01: Drawer order categories → separator → CallbackRequestForm → separator → auth block"
    - "D-02: Guest drawer shows Увійти + Реєстрація (same targets as StoreHeaderAuth)"
    - "D-03: Signed-in drawer shows Кабінет + Вийти (same sign-out as header)"
    - "D-04: StorefrontAuthLinks shared by StoreHeaderAuth and StoreMobileNav"
    - "D-05: Session passed from StoreHeader into StoreMobileNav"
    - "D-06: Desktop header auth unchanged; drawer trigger stays md:hidden"
  artifacts:
    - path: src/components/layout/storefront-auth-links.tsx
      provides: Shared auth link markup for header and drawer
      min_lines: 40
    - path: src/components/layout/store-mobile-nav.tsx
      provides: Session prop and auth block after second Separator
      contains: StorefrontAuthLinks
    - path: src/components/layout/store-header.tsx
      provides: Passes session into StoreMobileNav
      pattern: "session={session}"
  key_links:
    - from: src/components/layout/store-header.tsx
      to: src/components/layout/store-mobile-nav.tsx
      via: session prop from auth.api.getSession
      pattern: "StoreMobileNav.*session"
    - from: src/components/layout/store-mobile-nav.tsx
      to: src/components/layout/storefront-auth-links.tsx
      via: render after CallbackRequestForm
      pattern: "StorefrontAuthLinks"
---

<objective>
Add mobile drawer auth parity with the storefront header by extracting shared `StorefrontAuthLinks` and passing the server session snapshot into `StoreMobileNav`.

Purpose: NAV-01 — buyers can sign in or register from the menu without hunting header-only controls.
Output: Shared auth component, drawer auth block, extended Vitest coverage for guest and signed-in states.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-nav-homepage-catalog-labels/28-CONTEXT.md
@.planning/phases/28-nav-homepage-catalog-labels/28-RESEARCH.md
@.planning/phases/28-nav-homepage-catalog-labels/28-PATTERNS.md
@.planning/phases/28-nav-homepage-catalog-labels/28-UI-SPEC.md

<interfaces>
From src/components/layout/store-header-auth.tsx (extract source):
- linkClass: inline-flex min-h-11 min-w-11 items-center justify-center rounded-md px-2 text-sm font-medium hover:bg-muted
- Guest: Link /uviity «Увійти»; Link /reiestratsiia «Реєстрація» with bg-primary text-primary-foreground hover:bg-primary/90
- Signed-in: Link /kabinet «Кабінет»; Button outline sm «Вийти» → authClient.signOut(); router.push("/"); router.refresh()
- Session type: { user: { name: string; email: string } } | null

From src/components/layout/store-mobile-nav.tsx (current):
- Props: categories: MobileNavCategory[] only — extend with session
- Sheet trigger: md:hidden; closes on pathname change via useEffect
- Order: categories ul → Separator my-6 → CallbackRequestForm compact idPrefix drawer → (add) Separator → StorefrontAuthLinks

From src/components/layout/store-header.tsx:
- session = await auth.api.getSession({ headers: await headers() })
- StoreMobileNav currently: categories={availableCategories} only
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract StorefrontAuthLinks and thin StoreHeaderAuth</name>
  <files>src/components/layout/storefront-auth-links.tsx, src/components/layout/store-header-auth.tsx</files>
  <read_first>
    src/components/layout/store-header-auth.tsx
    .planning/phases/28-nav-homepage-catalog-labels/28-CONTEXT.md (D-02, D-03, D-04)
    .planning/phases/28-nav-homepage-catalog-labels/28-UI-SPEC.md (Mobile drawer auth)
  </read_first>
  <acceptance_criteria>
    - New file src/components/layout/storefront-auth-links.tsx exports StorefrontAuthLinks with session prop type matching current StoreHeaderAuth
    - Guest branch renders links to /uviity and /reiestratsiia with identical linkClass and primary registration classes as header today
    - Signed-in branch renders /kabinet link and outline Button «Вийти» calling authClient.signOut then router.push("/") and router.refresh()
    - store-header-auth.tsx becomes a thin wrapper that only renders StorefrontAuthLinks with no duplicated markup
    - No new routes or env vars introduced
  </acceptance_criteria>
  <action>
    Create storefront-auth-links.tsx as a client component per D-04: move linkClass, guest block, and signed-in block from store-header-auth.tsx unchanged (paths and Ukrainian copy locked). Export type StorefrontAuthSession or reuse inline session type. Refactor StoreHeaderAuth to import and return StorefrontAuthLinks only — preserve export name StoreHeaderAuth for store-header.tsx. Do not add variant prop unless drawer layout requires it; default horizontal flex items-center gap-1 matches header (D-06: header unchanged).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>StoreHeaderAuth behavior unchanged on desktop/mobile header; shared component exists and compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Wire session and auth block into StoreMobileNav</name>
  <files>src/components/layout/store-mobile-nav.tsx, src/components/layout/store-header.tsx</files>
  <read_first>
    src/components/layout/store-mobile-nav.tsx
    src/components/layout/store-header.tsx
    src/components/layout/storefront-auth-links.tsx
    .planning/phases/28-nav-homepage-catalog-labels/28-CONTEXT.md (D-01, D-05, D-06)
  </read_first>
  <acceptance_criteria>
    - StoreHeader passes session={session} into StoreMobileNav alongside categories
    - StoreMobileNav accepts session prop with same type as StorefrontAuthLinks
    - After CallbackRequestForm block: Separator className my-6, then div px-4 pb-4 containing StorefrontAuthLinks session={session}
    - Category list and first Separator + callback form order unchanged (D-01)
    - Sheet trigger remains md:hidden; StoreHeaderAuth still rendered in header (D-06)
  </acceptance_criteria>
  <action>
    Update store-header.tsx StoreMobileNav call to pass session from existing getSession result per D-05. Extend store-mobile-nav.tsx props and JSX: keep categories map and badges as-is; after existing callback div add second Separator my-6 and auth wrapper with StorefrontAuthLinks. Import StorefrontAuthLinks from storefront-auth-links.tsx. Do not remove or relocate CallbackRequestForm.
  </action>
  <verify>
    <automated>grep -n 'session={session}' src/components/layout/store-header.tsx && grep -n 'StorefrontAuthLinks' src/components/layout/store-mobile-nav.tsx</automated>
  </verify>
  <done>Drawer structure matches locked order; session flows server → client drawer.</done>
</task>

<task type="auto">
  <name>Task 3: Extend store-mobile-nav tests for auth states</name>
  <files>src/components/layout/store-mobile-nav.test.tsx</files>
  <read_first>
    src/components/layout/store-mobile-nav.test.tsx
    src/components/layout/storefront-auth-links.tsx
    .planning/phases/28-nav-homepage-catalog-labels/28-VALIDATION.md (28-01-01, 28-01-02)
  </read_first>
  <acceptance_criteria>
    - Existing test still passes (category badges + callback heading)
    - New case: session null → after opening menu, link roles named Увійти and Реєстрація present
    - New case: session with user → links named Кабінет and button named Вийти present
    - All StoreMobileNav renders pass session prop (null or mock) — no TypeScript errors
  </acceptance_criteria>
  <action>
    Extend store-mobile-nav.test.tsx: add vi.mock for @/lib/auth-client if sign-out is triggered (optional — guest test does not need it). Test 1: render with session={null}, open Меню, assert getByRole link Увійти and Реєстрація. Test 2: render with session={{ user: { name: "Test", email: "t@t.com" } }}, open menu, assert Кабінет link and Вийти button. Keep existing categories fixture and callback assertion.
  </action>
  <verify>
    <automated>npm test -- --run src/components/layout/store-mobile-nav.test.tsx</automated>
  </verify>
  <done>NAV-01 guest and signed-in drawer auth covered by automated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server RSC → client drawer | Session snapshot passed as props; not re-fetched in drawer |
| Client → Better Auth | signOut via authClient only on fixed post-logout navigation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Spoofing | StorefrontAuthLinks hrefs | mitigate | Only hardcoded paths /uviity, /reiestratsiia, /kabinet — no user-controlled redirect targets |
| T-28-02 | Information disclosure | session prop in drawer | accept | Same session object already exposed in header; snapshot is non-sensitive display fields |
| T-28-03 | Tampering | signOut handler | mitigate | Reuse authClient.signOut from header; no alternate logout endpoint |
| T-28-SC | Tampering | npm/pip/cargo installs | accept | No new packages this plan |
</threat_model>

<verification>
- npm test -- --run src/components/layout/store-mobile-nav.test.tsx
- npm run build
- Manual (D-21): mobile viewport, guest drawer → auth below callback; signed-in → Кабінет/Вийти; header auth still visible
</verification>

<success_criteria>
- NAV-01 satisfied: drawer shows login/register for guests and cabinet/sign-out for signed-in users below callback form
- No regression to Phase 26 drawer category counts or callback placement
</success_criteria>

<output>
Create `.planning/phases/28-nav-homepage-catalog-labels/28-01-SUMMARY.md` when done
</output>
