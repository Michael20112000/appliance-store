---
phase: 28-nav-homepage-catalog-labels
plan: 03
type: execute
wave: 3
depends_on: []
files_modified:
  - src/lib/catalog/format.ts
  - src/lib/catalog/format.test.ts
  - src/components/home/category-grid.tsx
autonomous: true
requirements:
  - HOME-05
must_haves:
  truths:
    - "Each homepage category card shows availability count beside the category name"
    - "Count of 1 displays badge text 1 товар"
    - "Count 2 or more displays digits only in the badge"
    - "Categories with zero available products are not rendered on homepage"
  artifacts:
    - path: src/lib/catalog/format.ts
      provides: formatCategoryCountBadge helper
      exports: ["formatCategoryCountBadge"]
    - path: src/components/home/category-grid.tsx
      provides: CardTitle row with Badge using productCount
      contains: formatCategoryCountBadge
  key_links:
    - from: src/components/home/category-grid.tsx
      to: src/server/services/catalog.service.ts
      via: listCategoriesWithProductCounts
      pattern: listCategoriesWithProductCounts
    - from: src/components/home/category-grid.tsx
      to: src/lib/catalog/categories.ts
      via: categoriesWithAvailableProducts filter
      pattern: categoriesWithAvailableProducts
---

<objective>
Show per-category availability counts on homepage cards using the existing catalog count pipeline and a testable UA badge formatter.

Purpose: HOME-05 — buyers see stock signal before opening a category.
Output: formatCategoryCountBadge, Vitest coverage, CategoryGrid badge row per D-12–D-15.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-nav-homepage-catalog-labels/28-CONTEXT.md
@.planning/phases/28-nav-homepage-catalog-labels/28-PATTERNS.md
@.planning/phases/28-nav-homepage-catalog-labels/28-UI-SPEC.md

<interfaces>
From src/components/home/category-grid.tsx:
- listCategoriesWithProductCounts() then categoriesWithAvailableProducts()
- section id="kategorii"
- CardTitle + CardDescription «Переглянути» — keep description

From src/lib/catalog/format.ts:
- pluralResultsUa exists — do NOT use for badges (D-13 hybrid rule differs)

From store-mobile-nav.tsx (badge styling reference):
- Badge variant="secondary" className="shrink-0 tabular-nums text-muted-foreground" for numeric counts
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add formatCategoryCountBadge and unit tests</name>
  <read_first>
    src/lib/catalog/format.ts
    src/lib/catalog/catalog-labels.test.ts
    .planning/phases/28-nav-homepage-catalog-labels/28-CONTEXT.md (D-13, D-20)
  </read_first>
  <behavior>
    - formatCategoryCountBadge(1) returns "1 товар"
    - formatCategoryCountBadge(2) returns "2"
    - formatCategoryCountBadge(12) returns "12"
  </behavior>
  <acceptance_criteria>
    - formatCategoryCountBadge exported from src/lib/catalog/format.ts
    - count === 1 returns exact string 1 товар
    - count >= 2 returns String(count) with no товар suffix
    - src/lib/catalog/format.test.ts exists with vitest describe block covering at least cases 1 and 12
    - pluralResultsUa unchanged and not used by new helper
  </acceptance_criteria>
  <action>
    Per D-13 and D-20: add formatCategoryCountBadge(count: number) to format.ts — if count === 1 return 1 товар else return String(count). Create format.test.ts mirroring catalog-labels.test.ts structure. Write tests first (RED), implement helper (GREEN). Do not use pluralResultsUa for badge text.
  </action>
  <verify>
    <automated>npm test -- --run src/lib/catalog/format.test.ts</automated>
  </verify>
  <done>HOME-05 count copy rules locked in tested pure function.</done>
</task>

<task type="auto">
  <name>Task 2: Add Badge row to CategoryGrid cards</name>
  <read_first>
    src/components/home/category-grid.tsx
    src/components/layout/store-mobile-nav.tsx
    .planning/phases/28-nav-homepage-catalog-labels/28-UI-SPEC.md (Homepage category counts)
    .planning/phases/28-nav-homepage-catalog-labels/28-CONTEXT.md (D-11, D-12, D-14, D-15)
  </read_first>
  <acceptance_criteria>
    - CardHeader contains flex row: CardTitle truncate text-base + Badge after title with gap-2 min-w-0
    - Badge shows formatCategoryCountBadge(category.productCount)
    - Badge uses variant secondary; count >= 2 includes tabular-nums on Badge className
    - CardDescription still reads Переглянути
    - categoriesWithAvailableProducts filter unchanged — zero-count categories not in map
    - section id kategorii unchanged
  </acceptance_criteria>
  <action>
    Import Badge from @/components/ui/badge and formatCategoryCountBadge from @/lib/catalog/format. Replace lone CardTitle with flex min-w-0 items-center gap-2 row per D-12 and UI-SPEC. Apply tabular-nums text-muted-foreground shrink-0 on Badge when productCount >= 2 per D-21 discretion. Do not alter listCategoriesWithProductCounts or filter logic (D-11, D-14). Keep empty grid return null.
  </action>
  <verify>
    <automated>grep -n 'formatCategoryCountBadge' src/components/home/category-grid.tsx && grep -n 'categoriesWithAvailableProducts' src/components/home/category-grid.tsx</automated>
  </verify>
  <done>Homepage cards display counts per locked singular/plural display rules.</done>
</task>

<task type="auto">
  <name>Task 3: Regression tests and build</name>
  <read_first>
    src/lib/catalog/categories.test.ts
    .planning/phases/28-nav-homepage-catalog-labels/28-VALIDATION.md
  </read_first>
  <acceptance_criteria>
    - npm test -- --run src/lib/catalog/categories.test.ts passes
    - npm test -- --run src/lib/catalog/format.test.ts passes
    - npm run build exits 0
  </acceptance_criteria>
  <action>
    Run categories.test.ts to confirm zero-count filter still holds (HOME-05 partial). Run format.test.ts and full build. Fix any import or type errors only within phase files.
  </action>
  <verify>
    <automated>npm test -- --run src/lib/catalog/categories.test.ts src/lib/catalog/format.test.ts && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>HOME-05 implementation verified; Phase 25 empty-category behavior preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → RSC grid | productCount from catalog service; not user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-06 | Tampering | formatCategoryCountBadge output | accept | Display-only; count sourced server-side |
| T-28-07 | Information disclosure | product counts | accept | Public catalog availability signal by design |
| T-28-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
- npm test -- --run src/lib/catalog/format.test.ts src/lib/catalog/categories.test.ts
- npm run build
- Manual: homepage card with 1 product shows «1 товар»; multi-product shows digits only; no zero-count cards
</verification>

<success_criteria>
- HOME-05: visible counts on cards; zero-count categories hidden
- «Переглянути» description retained
</success_criteria>

<output>
Create `.planning/phases/28-nav-homepage-catalog-labels/28-03-SUMMARY.md` when done
</output>
