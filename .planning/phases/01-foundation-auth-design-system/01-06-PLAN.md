---
phase: 01-foundation-auth-design-system
plan: 06
type: execute
wave: 5
depends_on:
  - 01-04
  - 01-05
files_modified:
  - src/lib/permissions.ts
  - src/app/(admin)/admin/layout.tsx
  - src/app/(admin)/admin/page.tsx
  - src/proxy.ts
  - src/components/media/optimized-image.tsx
  - src/lib/demo-assets.ts
  - src/components/home/hero-section.tsx
  - e2e/admin-rbac.spec.ts
autonomous: true
requirements:
  - PERF-01
user_setup:
  - service: cloudinary
    why: "Delivery-only hero images (D-14, D-15)"
    env_vars:
      - name: NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME
        source: "Cloudinary Console → Dashboard"

must_haves:
  truths:
    - "Buyer не бачить /admin UI — redirect /uviity"
    - "Seeded admin відкриває /admin stub"
    - "Hero використовує Cloudinary f_auto q_auto через OptimizedImage"
    - "CLOUDINARY_API_SECRET не в NEXT_PUBLIC_* (D-16)"
  artifacts:
    - path: src/lib/permissions.ts
      provides: "requireAdmin()"
    - path: src/app/(admin)/admin/layout.tsx
      provides: "Server-side RBAC"
    - path: src/components/media/optimized-image.tsx
      provides: "CldImage wrapper PERF-01"
    - path: src/proxy.ts
      provides: "Cookie redirect auxiliary"
  key_links:
    - from: admin/layout.tsx
      to: requireAdmin
      via: call
      pattern: "requireAdmin"
    - from: hero-section.tsx
      to: optimized-image.tsx
      via: import
      pattern: "OptimizedImage"
---

## Phase Goal

**As a** адміністратор магазину, **I want to** захищений /admin shell і оптимізовані зображення на головній, **so that** інфраструктура готова до Phase 4 CRUD і швидкого завантаження (S6).

<objective>
Server-side admin RBAC (D-13), proxy cookie redirect, OptimizedImage + hero integration (D-14, D-15, PERF-01).

Purpose: Security pitfall #2 closure + media pipeline.
Output: Admin shell, Cloudinary delivery component on hero.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-auth-design-system/01-RESEARCH.md — Middleware, Cloudinary, requireAdmin
@.planning/research/PITFALLS.md — Admin RBAC, Cloudinary secrets
@.planning/phases/01-foundation-auth-design-system/01-UI-SPEC.md — Admin shell, OptimizedImage contract

npm install next-cloudinary@6.17.5.
demo public_ids in src/lib/demo-assets.ts (e.g. samples/ecommerce/leather-bag-gray).
No CldUploadWidget Phase 1 (D-16).
</context>

<tasks>

<task type="auto">
  <name>Task 1: requireAdmin + admin layout + proxy.ts</name>
  <read_first>
    - 01-RESEARCH.md — proxy.ts, permissions.ts, admin layout
    - 01-CONTEXT.md — D-13
    - PITFALLS.md #2 admin RBAC
  </read_first>
  <action>
    src/lib/permissions.ts: requireAdmin() — auth.api.getSession headers; if !session or session.user.role !== "admin" redirect /uviity; return session.
    src/app/(admin)/admin/layout.tsx: await requireAdmin() at top; minimal sidebar/top «Адмін-панель»; metadata robots noindex; bg-muted admin chrome per UI-SPEC.
    src/app/(admin)/admin/page.tsx: body «Панель керування — Phase 4» UA.
    src/proxy.ts (Next 16): matcher /admin/:path*; getSessionCookie — if missing redirect /uviity; **не** перевіряти role тут (auxiliary only).
    Route group (admin) separate from storefront (D-10).
  </action>
  <acceptance_criteria>
    - Buyer session: GET /admin → redirect /uviity (layout guard)
    - Admin seed: GET /admin → 200 with «Адмін-панель»
    - proxy.ts існує; layout викликає requireAdmin
  </acceptance_criteria>
  <verify>
    <automated>npm run test:e2e -- e2e/admin-rbac.spec.ts</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: OptimizedImage wrapper + demo assets</name>
  <read_first>
    - 01-UI-SPEC.md — Cloudinary OptimizedImage section
    - 01-CONTEXT.md — D-14, D-15, D-16
  </read_first>
  <action>
    npm install next-cloudinary@6.17.5 (if not installed).
    src/components/media/optimized-image.tsx: client component CldImage wrapper; defaults format auto, quality auto; required alt UA; sizes prop.
    src/lib/demo-assets.ts: export HERO_PUBLIC_ID constant(s) from Cloudinary media library demo.
    Env: NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME only on client; **не** додавати CLOUDINARY_API_SECRET до NEXT_PUBLIC (D-16).
    Unit smoke: component file exports OptimizedImage.
  </action>
  <acceptance_criteria>
    - OptimizedImage enforces f_auto/q_auto via CldImage props
    - grep client bundle config: no NEXT_PUBLIC.*SECRET
    - .env.example documents cloud name only for Phase 1 delivery
  </acceptance_criteria>
  <verify>
    <automated>npm run build && ! grep -r 'NEXT_PUBLIC_CLOUDINARY_API_SECRET\|NEXT_PUBLIC.*SECRET' src/ 2>/dev/null; test -f src/components/media/optimized-image.tsx</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Wire hero + PERF verification</name>
  <read_first>
    - 01-UI-SPEC.md — Hero image sizes priority
    - ROADMAP Phase 1 success criterion #4
  </read_first>
  <action>
    Оновити HeroSection: замінити placeholder на OptimizedImage з demo-assets; priority true; sizes (max-width: 1024px) 100vw, 50vw; alt UA descriptive.
    e2e/admin-rbac.spec.ts: login buyer → /admin blocked; login admin seed → /admin ok.
    Manual/automated check: rendered img src or response contains res.cloudinary.com with f_auto/q_auto transformation segment.
    Optional vitest: demo-assets exports non-empty public_id.
  </action>
  <acceptance_criteria>
    - Home hero loads Cloudinary URL in network/DOM
    - PERF-01: transformation params present (auto format/quality)
    - Admin RBAC e2e green
  </acceptance_criteria>
  <verify>
    <automated>npm run test:e2e -- e2e/admin-rbac.spec.ts && npm run build</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Buyer → /admin | Privilege escalation attempt |
| Browser → Cloudinary CDN | Public delivery only |
| Client bundle → env | Secret leakage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06-01 | Elevation | /admin | mitigate | requireAdmin in layout; proxy cookie-only auxiliary |
| T-01-06-02 | Information Disclosure | CLOUDINARY_API_SECRET | mitigate | Server-only env; no upload widget Phase 1 |
| T-01-06-03 | Spoofing | getSessionCookie in proxy | accept | Layout does real RBAC; proxy redirects unauthenticated only |
| T-01-06-04 | Tampering | unsigned upload | accept | Deferred Phase 4 (D-16) |

</threat_model>

<verification>
npm run test:e2e -- e2e/admin-rbac.spec.ts
Buyer denied admin; admin allowed
Hero Cloudinary URL check
</verification>

<success_criteria>
PERF-01 delivered; admin shell secured server-side per D-13
</success_criteria>

<output>
Create `.planning/phases/01-foundation-auth-design-system/01-06-SUMMARY.md` when done
</output>
