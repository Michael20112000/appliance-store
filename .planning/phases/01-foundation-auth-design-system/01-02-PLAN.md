---
phase: 01-foundation-auth-design-system
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - prisma/schema.prisma
  - prisma.config.ts
  - prisma/seed.ts
  - src/lib/db.ts
  - package.json
  - .env.example
autonomous: true
requirements: [] # data backbone for AUTH-01 home grid + admin seed (D-19); traced via 01-04/01-05/01-06
context_decisions: [D-07, D-17, D-18, D-19]
user_setup:
  - service: neon
    why: "EU Postgres dev branch — sole local DB path (no Docker per D-18)"
    env_vars:
      - name: DATABASE_URL
        source: "Neon → dev branch → Pooled connection"
      - name: DIRECT_URL
        source: "Neon → dev branch → Direct connection (migrations)"
      - name: ADMIN_EMAIL
        source: "Team secret — seed only, not production Vercel unless bootstrap"
      - name: ADMIN_PASSWORD
        source: "Team secret — min 12 chars"

must_haves:
  truths:
    - "Prisma підключається до Neon dev branch через adapter"
    - "У БД 8 категорій з українськими назвами та slug"
    - "Seed створює одного admin — публічна реєстрація не може стати admin (D-07)"
  artifacts:
    - path: prisma/schema.prisma
      provides: "Category + Better Auth models"
      contains: model Category
    - path: prisma/seed.ts
      provides: "8 categories + admin user"
    - path: src/lib/db.ts
      provides: "PrismaNeon singleton"
  key_links:
    - from: prisma/seed.ts
      to: src/lib/auth.ts
      via: "signUpEmail or documented hash — admin role admin"
      pattern: "role.*admin"
    - from: prisma.config.ts
      to: env DIRECT_URL
      via: env()
      pattern: "DIRECT_URL"
---

## Phase Goal

**As a** розробник, **I want to** мати Neon БД з категоріями та admin seed, **so that** публічна головна і auth можуть читати/писати реальні дані (S2).

<objective>
Підключити Neon EU dev branch, Prisma 7 + Neon adapter, схему Category + Better Auth tables, migrate, seed 8 категорій і admin (D-17, D-19, D-07).

Purpose: Data backbone для home grid і auth.
Output: Migrated schema, seeded DB, `prisma db seed` idempotent.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-auth-design-system/01-01-SUMMARY.md
@.planning/phases/01-foundation-auth-design-system/01-RESEARCH.md — DB Schema Minimum, Prisma 7 + Neon, Seed admin

**Category seed (UA / slug):** Пральні машини/pralni-mashyny, Холодильники/kholodylnyky, Морозильні камери/morozylni-kamery, Телевізори/televizory, Плити/plyty, Духові шафи/dukhovi-shafy, Варильні поверхні/varylni-poverkhni, Сушарки для одягу/susharky-dlia-odyahu.

**BLOCKING:** після будь-якої зміни schema — `npx prisma migrate dev` з DIRECT_URL (Neon direct), не Docker.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Neon dev branch + Prisma 7 wiring</name>
  <read_first>
    - 01-RESEARCH.md — Prisma 7 + Neon wiring, prisma.config.ts example
    - 01-CONTEXT.md — D-17, D-18 (Neon branch, NOT Docker)
  </read_first>
  <action>
    Створити Neon project region EU (eu-central-1 або найближчий). Створити **dev branch**; скопіювати pooled DATABASE_URL і direct DIRECT_URL у локальний .env і Vercel preview env.
    npm install prisma@7.8.0 @prisma/client@7.8.0 @prisma/adapter-neon@7.8.0 dotenv better-auth@1.6.11 zod@4.4.3 slugify@1.6.6.
    npx prisma init — output src/generated/prisma (або стандартний шлях з CLI).
    Створити prisma.config.ts: schema prisma/schema.prisma, datasource url env("DIRECT_URL"), import dotenv/config.
    Створити src/lib/db.ts: PrismaNeon adapter з process.env.DATABASE_URL, global singleton dev hot-reload.
    Не додавати datasource url у schema.prisma (Prisma 7 rule).
  </action>
  <acceptance_criteria>
    - npx prisma validate exit 0
    - prisma.config.ts існує і читає DIRECT_URL
    - src/lib/db.ts експортує prisma без throw при наявних env
  </acceptance_criteria>
  <verify>
    <automated>npx prisma validate</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Better Auth schema + Category model + migrate</name>
  <read_first>
    - 01-RESEARCH.md — Auth Setup, DB Schema Minimum
    - prisma.io Better Auth guide — npx @better-auth/cli generate
  </read_first>
  <action>
    Запустити npx @better-auth/cli@latest generate — змержити User, Session, Account, Verification у prisma/schema.prisma.
    Додати model Category: id cuid, name String, slug String @unique, description String?, sortOrder Int @default(0), createdAt, updatedAt.
    Після generate перевірити поле role на User (admin plugin). Зафіксувати admin({ defaultRole: "buyer" }) intent у коментарі schema — якщо plugin лише user/admin, задокументувати mapping у SUMMARY (assumption A1).
    **BLOCKING:** npx prisma migrate dev --name init_foundation (використовує DIRECT_URL на Neon dev branch).
    npx prisma generate.
  </action>
  <acceptance_criteria>
    - Migration folder створено і застосовано на Neon dev
    - model Category присутній у schema
    - Better Auth tables присутні
  </acceptance_criteria>
  <verify>
    <automated>npx prisma migrate status && npx prisma generate</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Seed 8 categories + admin user</name>
  <read_first>
    - 01-CONTEXT.md — D-07, D-19
    - 01-RESEARCH.md — Seed admin, category table
  </read_first>
  <action>
    prisma/seed.ts: upsert 8 categories (sortOrder 1..8, slugify UA names). Fail fast якщо ADMIN_EMAIL або ADMIN_PASSWORD відсутні в dev.
    Admin: створити через auth.api.signUpEmail (після тимчасового src/lib/auth.ts minimal) АБО documented Better Auth seed — потім prisma.user.update role admin. Ніколи не приймати role з client body.
    package.json: prisma.seed → tsx/node prisma/seed.ts.
    Запустити npx prisma db seed — повторний запуск idempotent (upsert).
    Vitest prisma/seed.test.ts або integration: category count === 8.
  </action>
  <acceptance_criteria>
    - SELECT count categories = 8
    - Admin user існує з role admin (або plugin equivalent)
    - Повторний seed не дублює категорії
  </acceptance_criteria>
  <verify>
    <automated>npx prisma db seed && npx vitest run prisma/seed.test.ts</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed script → Neon | Privileged write; ADMIN credentials |
| Prisma CLI → DIRECT_URL | DDL migrations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Elevation | prisma/seed.ts | mitigate | Admin лише через env; signup body never sets admin |
| T-01-02-02 | Information Disclosure | ADMIN_PASSWORD in Vercel prod | mitigate | Seed env лише dev/CI; не production Vercel unless one-time bootstrap doc |
| T-01-02-03 | Tampering | migration SQL | mitigate | migrate dev reviewed; no manual prod DDL in Phase 1 |

</threat_model>

<verification>
npx prisma migrate status
npx prisma db seed
Category count = 8
</verification>

<success_criteria>
Neon dev branch populated; admin + categories ready for UI slices
</success_criteria>

<output>
Create `.planning/phases/01-foundation-auth-design-system/01-02-SUMMARY.md` when done
</output>
