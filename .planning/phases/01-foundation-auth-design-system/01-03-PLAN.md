---
phase: 01-foundation-auth-design-system
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/(storefront)/layout.tsx
  - src/components/layout/store-header.tsx
  - src/components/layout/store-footer.tsx
  - components.json
  - src/components/ui/button.tsx
  - src/components/ui/card.tsx
  - src/components/ui/input.tsx
  - src/components/ui/label.tsx
  - src/components/ui/form.tsx
  - src/components/ui/alert.tsx
  - src/components/ui/separator.tsx
  - src/components/ui/skeleton.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/sheet.tsx
  - src/components/ui/dropdown-menu.tsx
autonomous: true
requirements:
  - UI-02
  - UI-03
context_decisions: [D-01, D-02, D-03, D-04, D-10]
user_setup: []

must_haves:
  truths:
    - "Світла повітряна тема з OKLCH токенами — без dark mode (D-04)"
    - "Storefront має header + footer shell на всіх (storefront) сторінках"
    - "Body text ≥16px на mobile"
  artifacts:
    - path: src/app/globals.css
      provides: "@theme inline OKLCH tokens"
      contains: "@theme inline"
    - path: src/app/(storefront)/layout.tsx
      provides: "StoreHeader + main + StoreFooter"
    - path: components.json
      provides: "shadcn new-york zinc"
  key_links:
    - from: src/app/(storefront)/layout.tsx
      to: store-header.tsx
      via: render
      pattern: "StoreHeader"
---

## Phase Goal

**As a** покупець, **I want to** бачити легкий український інтерфейс зі зрозумілою навігацією, **so that** сайт відчувається сучасним і зручним на телефоні (S3).

<objective>
Ініціалізувати shadcn new-york, design tokens (D-01, D-02, D-03, D-04), Geist Sans, storefront layout shell (D-10).

Purpose: UI-02/03 baseline перед home і auth.
Output: globals.css tokens, shadcn primitives, StoreHeader/Footer.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-auth-design-system/01-UI-SPEC.md — Design System, Color, Typography, Spacing, Component Inventory
@.planning/phases/01-foundation-auth-design-system/01-CONTEXT.md — D-01, D-02, D-03, D-04, D-10, D-12

OKLCH starting point from RESEARCH/UI-SPEC: background oklch(0.99), primary oklch(0.55 0.14 220), radius 0.75rem.
Geist: npm install geist@1.7.0, GeistSans у root layout.
</context>

<tasks>

<task type="auto">
  <name>Task 1: shadcn init + globals.css tokens</name>
  <read_first>
    - 01-UI-SPEC.md — Design System, Color sections
    - 01-CONTEXT.md — D-01, D-02, D-03, D-04
    - ui.shadcn.com/docs/tailwind-v4
  </read_first>
  <action>
    npx shadcn@latest init — style new-york, base color zinc, cssVariables true, src dir.
    Переписати src/app/globals.css: @import "tailwindcss"; :root OKLCH з UI-SPEC (--background, --primary teal, --radius 0.75rem, light shadows); @theme inline map to Tailwind colors; --font-sans Geist.
    Root layout: import geist/font/sans, className GeistSans, body min-h-dvh bg-background text-base antialiased (≥16px D-03).
    Заборонено: dark: variants, dark mode toggle (D-04).
  </action>
  <acceptance_criteria>
    - components.json style = new-york
    - globals.css містить @theme inline і --primary oklch
    - Немає dark mode класів у layout
  </acceptance_criteria>
  <verify>
    <automated>npm run build && grep -q '@theme inline' src/app/globals.css && ! grep -r 'dark:' src/app/layout.tsx src/app/globals.css 2>/dev/null; echo ok</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: shadcn components + storefront route group</name>
  <read_first>
    - 01-UI-SPEC.md — Component Inventory table
  </read_first>
  <action>
    npx shadcn@latest add button card input label form alert separator skeleton badge sheet dropdown-menu.
    Створити src/app/(storefront)/layout.tsx: StoreHeader, main id="main-content", StoreFooter; max-w-6xl container padding per UI-SPEC.
    Перемістити тимчасову page.tsx у src/app/(storefront)/page.tsx (видалити root page.tsx якщо duplicate).
    StoreHeader placeholder: logo «Техніка Львів» → /, guest links Увійти → /uviity, Реєстрація → /reiestratsiia (copy UI-SPEC); mobile Sheet stub.
    StoreFooter: м. Львів, placeholder контакти, © year.
    metadata template: %s | Техніка б/у Львів.
  </action>
  <acceptance_criteria>
    - (storefront) layout рендерить header/footer
    - shadcn ui components існують під src/components/ui/
    - npm run build pass
  </acceptance_criteria>
  <verify>
    <automated>npm run build && test -f src/app/\(storefront\)/layout.tsx</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Layout smoke test (mobile-first)</name>
  <read_first>
    - 01-UI-SPEC.md — Responsive Breakpoints, Accessibility touch 44px
  </read_first>
  <action>
    StoreHeader: кнопки/лінки min-h-11 min-w-11 на mobile (44px touch D-03).
    e2e/home-layout.spec.ts: viewport 390x844 — header visible, footer visible, body font-size computed ≥16px на paragraph stub.
    prefers-reduced-motion: category hover translate вимкнено пізніше на home — задокументувати в header only якщо є motion.
  </action>
  <acceptance_criteria>
    - Playwright mobile viewport проходить layout smoke
    - Touch targets ≥44px на auth nav links у header
  </acceptance_criteria>
  <verify>
    <automated>npm run test:e2e -- e2e/home-layout.spec.ts</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN → shadcn registry | Component supply chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Tampering | shadcn CLI | mitigate | Official registry only per UI-SPEC Registry Safety |
| T-01-03-02 | Spoofing | third-party blocks | accept | No third-party registries Phase 1 |

</threat_model>

<verification>
npm run build
npm run test:e2e -- e2e/home-layout.spec.ts
</verification>

<success_criteria>
Airy light storefront shell ready for home content
</success_criteria>

<output>
Create `.planning/phases/01-foundation-auth-design-system/01-03-SUMMARY.md` when done
</output>
