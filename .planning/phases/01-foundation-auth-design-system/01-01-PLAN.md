---
phase: 01-foundation-auth-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - .env.example
  - vitest.config.ts
  - playwright.config.ts
  - e2e/smoke-deploy.spec.ts
  - src/lib/env.ts
  - src/lib/env.test.ts
autonomous: false
requirements:
  - UI-01
user_setup:
  - service: vercel
    why: "Preview deployment for walking skeleton (D-20)"
    env_vars:
      - name: NEXT_PUBLIC_APP_URL
        source: "Vercel project → Settings → Domains (preview URL)"
  - service: neon
    why: "Dev branch created in plan 01-02; placeholder vars here for parity"
    env_vars:
      - name: DATABASE_URL
        source: "Neon Console → Connection string (pooled)"
      - name: DIRECT_URL
        source: "Neon Console → Direct connection string"

must_haves:
  truths:
    - "Застосунок збирається локально (`npm run build`) без помилок TypeScript"
    - "Root layout має lang=uk і українські metadata defaults"
    - "Є `.env.example` з усіма ключами для Phase 1"
    - "Vitest і Playwright запускаються (навіть якщо один smoke-тест)"
  artifacts:
    - path: package.json
      provides: "Scripts test, test:e2e, postinstall prisma generate placeholder"
      contains: '"test"'
    - path: src/app/layout.tsx
      provides: "lang=uk root shell"
      contains: 'lang="uk"'
    - path: .env.example
      provides: "Documented env contract"
      contains: BETTER_AUTH_SECRET
  key_links:
    - from: src/app/layout.tsx
      to: globals.css
      via: import
      pattern: "import.*globals.css"
---

## Phase Goal

**As a** розробник магазину, **I want to** мати зібраний Next.js проєкт із тестовим раннером і Vercel preview, **so that** наступні вертикальні слайси деплояться без перебудови фундаменту.

<objective>
Ініціалізувати greenfield Next.js 16 monolith, Wave 0 test scaffold, український root layout і перший Vercel deploy (S1).

Purpose: Walking Skeleton — довести build + deploy pipeline до підключення Neon.
Output: Runnable app, `.env.example`, Vitest/Playwright smoke, Vercel preview URL.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-auth-design-system/01-CONTEXT.md
@.planning/phases/01-foundation-auth-design-system/01-RESEARCH.md
@.planning/phases/01-foundation-auth-design-system/SKELETON.md

Package versions (pin at install per RESEARCH): next@16.2.6, react@19.2.6, typescript@~5.8.0, vitest@4.1.6, @playwright/test@1.60.0.
Greenfield — `create-next-app` у корені репозиторію.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Package legitimacy gate before npm install</name>
  <read_first>.planning/phases/01-foundation-auth-design-system/01-RESEARCH.md — section Package Legitimacy Audit</read_first>
  <action>
    Перед `npm install` / `create-next-app` перевірити на npmjs.com: next@16.2.6, better-auth@1.6.11, prisma@7.8.0, @prisma/adapter-neon@7.8.0, next-cloudinary@6.17.5, geist@1.7.0. Підтвердити publisher і відсутність підозрілих postinstall. Slopcheck недоступний — human gate обовʼязковий (T-01-SC).
  </action>
  <acceptance_criteria>
    - Кожен пакет з audit table має статус Approved або задокументоване відхилення
    - Жодного [SLOP] пакета в install list
  </acceptance_criteria>
  <verify>
    <automated>test -f package.json || echo "pending scaffold"</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 1: Scaffold Next.js 16 + test runners + env schema</name>
  <read_first>
    - .planning/phases/01-foundation-auth-design-system/01-RESEARCH.md — Standard Stack, Installation block
    - .planning/phases/01-foundation-auth-design-system/01-CONTEXT.md — D-17, D-18, D-20
  </read_first>
  <action>
    Запустити: npx create-next-app@16.2.6 . --typescript --tailwind --eslint --app --src-dir --turbopack --yes (якщо каталог не порожній — лише відсутні файли, не перезаписувати .planning/.git).
    Додати devDependencies: vitest@4.1.6, @vitejs/plugin-react, @testing-library/react@16.3.2, @playwright/test@1.60.0, typescript@~5.8.0.
    Створити vitest.config.ts, playwright.config.ts, scripts у package.json: "test": "vitest run", "test:e2e": "playwright test".
    Створити src/lib/env.ts — Zod 4 parse: NEXT_PUBLIC_APP_URL (url), BETTER_AUTH_SECRET (min 32), BETTER_AUTH_URL (url), DATABASE_URL, DIRECT_URL, NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME; ADMIN_EMAIL/ADMIN_PASSWORD optional для seed.
    Створити src/lib/env.test.ts — happy path + missing secret fails.
    Створити .env.example з усіма ключами та коментарями UA (без секретів).
    Root src/app/layout.tsx: html lang="uk" (D-01 UI-01), metadata title/description українською («Техніка б/у Львів»), import globals.css.
    src/app/page.tsx — мінімальна заглушка «Скоро магазин» до plan 01-04.
    postinstall у package.json: "prisma generate" (працюватиме після plan 01-02).
  </action>
  <acceptance_criteria>
    - npm run build завершується exit 0
    - npm run test запускає env.test.ts успішно
    - View source / layout містить lang="uk"
    - .env.example перелічує DATABASE_URL, DIRECT_URL, BETTER_AUTH_*, CLOUDINARY cloud name, ADMIN_*
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npm run test</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Vercel deploy + deploy smoke E2E</name>
  <read_first>
    - .planning/phases/01-foundation-auth-design-system/01-RESEARCH.md — Vercel Deploy + Neon Branch
    - SKELETON.md — Deployment row
  </read_first>
  <action>
    Ініціалізувати git remote якщо відсутній; push main. Підключити репозиторій до Vercel (import project). У Vercel Environment Variables додати NEXT_PUBLIC_APP_URL (preview URL), тимчасові placeholder для DATABASE_URL/DIRECT_URL якщо Neon ще в plan 01-02.
    Build command: npm run build (default). Node 20+.
    Створити e2e/smoke-deploy.spec.ts: GET `${process.env.PLAYWRIGHT_BASE_URL || 'http://localhost:3000'}/` → status 200, body містить український текст заглушки.
    Документувати preview URL у SUMMARY після deploy.
    Per D-20: preview env існує з першого PR.
  </action>
  <acceptance_criteria>
    - Vercel preview deployment у статусі Ready
    - PLAYWRIGHT_BASE_URL вказує на preview або localhost — e2e smoke проходить
    - Головна відповідає 200 без auth
  </acceptance_criteria>
  <verify>
    <automated>npm run test:e2e -- e2e/smoke-deploy.spec.ts</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer → npm registry | Supply chain при install |
| Vercel → runtime env | Secrets не в client bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | npm packages | mitigate | Package Legitimacy Audit + blocking human checkpoint before install |
| T-01-02 | Information Disclosure | .env.example | mitigate | Тільки імена змінних, без реальних секретів |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck unavailable — human verify checkpoint Task 1 |

</threat_model>

<verification>
- npm run build && npm run test && npm run test:e2e -- e2e/smoke-deploy.spec.ts
- Vercel dashboard: successful deployment
</verification>

<success_criteria>
- Preview URL доступний
- Український lang на root
- Test runners green
</success_criteria>

<output>
Create `.planning/phases/01-foundation-auth-design-system/01-01-SUMMARY.md` when done
</output>
