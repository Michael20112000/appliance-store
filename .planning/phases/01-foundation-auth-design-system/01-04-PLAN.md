---
phase: 01-foundation-auth-design-system
plan: 04
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
files_modified:
  - src/app/(storefront)/page.tsx
  - src/app/(storefront)/katalog/[slug]/page.tsx
  - src/components/home/hero-section.tsx
  - src/components/home/category-grid.tsx
  - src/components/home/how-to-buy.tsx
  - src/components/layout/store-header.tsx
  - e2e/public-browse.spec.ts
  - e2e/locale.spec.ts
autonomous: true
requirements:
  - AUTH-01
  - UI-01
  - UI-03
user_setup: []

must_haves:
  truths:
    - "Гість відкриває / без логіну і бачить hero + 8 категорій з БД"
    - "Посилання категорій ведуть на /katalog/[slug] з UA copy «Незабаром»"
    - "HTML source містить назви категорій (RSC, не client-only)"
  artifacts:
    - path: src/components/home/category-grid.tsx
      provides: "Prisma category grid"
    - path: src/app/(storefront)/katalog/[slug]/page.tsx
      provides: "Category stub"
    - path: src/app/(storefront)/page.tsx
      provides: "Home composition"
  key_links:
    - from: category-grid.tsx
      to: prisma.category
      via: findMany
      pattern: "prisma.category.findMany"
    - from: store-header.tsx
      to: "/katalog/"
      via: Link
      pattern: "katalog"
---

## Phase Goal

**As a** покупець без облікового запису, **I want to** переглядати головну та категорії українською, **so that** можу орієнтуватися в асортименті до реєстрації (S4, AUTH-01).

<objective>
Публічна головна з hero, 8 seed-категорій, «Як купити», footer; stub `/katalog/[slug]` (D-11, D-12).

Purpose: AUTH-01 + UI-01 public surface.
Output: RSC home + category stubs, public E2E.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-auth-design-system/01-UI-SPEC.md — Home, Category stub, Header, Copywriting
@.planning/phases/01-foundation-auth-design-system/01-CONTEXT.md — D-11, D-12
@.planning/phases/01-foundation-auth-design-system/01-02-SUMMARY.md

Hero image: тимчасово optional placeholder div або static img до plan 01-06 CldImage — НЕ блокувати S4.
Copy: H1 «Б/у побутова техніка у Львові», CTA «Переглянути категорії» → #kategorii.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Home page sections (RSC)</name>
  <read_first>
    - 01-UI-SPEC.md — Home `/` sections 1-4
    - 01-RESEARCH.md — Category grid example
  </read_first>
  <action>
    src/app/(storefront)/page.tsx — compose HeroSection, CategoryGrid id="kategorii", HowToBuy, metadata UA title/description.
    HeroSection: Display H1, body copy Львів, primary Button CTA scroll/link #kategorii; image placeholder aspect 16/10 (OptimizedImage в 01-06).
    CategoryGrid: prisma.category.findMany orderBy sortOrder asc; grid 2 col mobile, 4 md; Card links to /katalog/[slug]; names UA.
    HowToBuy: 3 steps UA per UI-SPEC; bg-muted/50 band.
    Export generateMetadata українською (UI-01).
  </action>
  <acceptance_criteria>
    - / рендерить 8 category names з DB
    - View page source містить «Пральні машини» або іншу seed назву
    - Секції hero, categories, how-to-buy присутні
  </acceptance_criteria>
  <verify>
    <automated>npm run build && curl -s http://localhost:3000/ | grep -q 'Категорії'</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Category stub + header nav links</name>
  <read_first>
    - 01-UI-SPEC.md — Category stub `/katalog/[slug]`, Header D-12
  </read_first>
  <action>
    src/app/(storefront)/katalog/[slug]/page.tsx: prisma.category.findUnique slug; notFound soft UA «Категорію не знайдено»; found → Badge «Незабаром», body copy UI-SPEC, CTA «Усі категорії» → /#kategorii.
    StoreHeader: map categories from prisma (cached) або static seed slugs for nav — links to /katalog/[slug]; mobile Sheet lists categories.
    Не вимагати auth на (storefront) routes (AUTH-01).
  </action>
  <acceptance_criteria>
    - /katalog/pralni-mashyny 200 без login
    - Невідомий slug показує м’який UA message, не blank 500
    - Header містить ≥1 link на katalog
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Public browse + locale E2E</name>
  <read_first>
    - 01-RESEARCH.md — Validation Architecture AUTH-01, UI-01
  </read_first>
  <action>
    e2e/public-browse.spec.ts: guest context — visit /, expect 8 category links; visit /katalog/kholodylnyky expect «Незабаром».
    e2e/locale.spec.ts: document.documentElement.lang === 'uk'; title contains «Львів» або бренд UA.
  </action>
  <acceptance_criteria>
    - e2e public-browse green without storageState
    - lang=uk verified
  </acceptance_criteria>
  <verify>
    <automated>npm run test:e2e -- e2e/public-browse.spec.ts e2e/locale.spec.ts</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Guest → storefront RSC | Read-only public data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Information Disclosure | Category stub | accept | Public category names only; no PII |
| T-01-04-02 | Denial of Service | prisma findMany home | mitigate | 8 rows only; no unbounded collect |

</threat_model>

<verification>
npm run test:e2e -- e2e/public-browse.spec.ts e2e/locale.spec.ts
curl home contains category UA text
</verification>

<success_criteria>
AUTH-01 satisfied: public home + katalog stubs without registration
</success_criteria>

<output>
Create `.planning/phases/01-foundation-auth-design-system/01-04-SUMMARY.md` when done
</output>
