---
phase: 01-foundation-auth-design-system
plan: 05
type: execute
wave: 4
depends_on:
  - 01-02
  - 01-03
files_modified:
  - src/lib/auth.ts
  - src/lib/auth-client.ts
  - src/app/api/auth/[...all]/route.ts
  - src/app/(storefront)/uviity/page.tsx
  - src/app/(storefront)/reiestratsiia/page.tsx
  - src/app/(storefront)/kabinet/page.tsx
  - src/components/auth/auth-form.tsx
  - src/components/layout/store-header.tsx
  - e2e/auth.spec.ts
  - e2e/session-persist.spec.ts
autonomous: true
requirements:
  - AUTH-02
  - AUTH-05
context_decisions: [D-05, D-06, D-07, D-08, D-09]
user_setup: []

must_haves:
  truths:
    - "Користувач реєструється на /reiestratsiia і входить на /uviity"
    - "Після login /kabinet показує вітання з імʼям"
    - "Hard refresh /kabinet — сесія збережена (AUTH-05)"
    - "Sign-up не приймає role admin (D-06, D-07)"
  artifacts:
    - path: src/lib/auth.ts
      provides: "betterAuth + prismaAdapter + admin plugin"
      exports: ["auth"]
    - path: src/app/api/auth/[...all]/route.ts
      provides: "Better Auth handler"
    - path: src/app/(storefront)/kabinet/page.tsx
      provides: "Session-gated stub"
  key_links:
    - from: kabinet/page.tsx
      to: auth.api.getSession
      via: headers()
      pattern: "getSession"
    - from: auth-form.tsx
      to: authClient.signIn.email
      via: client call
      pattern: "signIn.email|signUp.email"
---

## Phase Goal

**As a** покупець, **I want to** зареєструватися та увійти email-паролем із збереженням сесії, **so that** можу повернутися в кабінет після перезавантаження (S5).

<objective>
Better Auth server/client, UA auth pages `/uviity`, `/reiestratsiia`, `/kabinet` stub (D-05, D-06, D-07, D-08, D-09, AUTH-02, AUTH-05).

Purpose: Identity layer for Phase 3+ commerce.
Output: Working email auth + session persistence E2E.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-auth-design-system/01-RESEARCH.md — Auth Setup file map, server config, routes
@.planning/phases/01-foundation-auth-design-system/01-UI-SPEC.md — Login, Sign-up, Cabinet copy
@.planning/phases/01-foundation-auth-design-system/01-CONTEXT.md — D-05, D-06, D-08, D-09

npm install react-hook-form@7.76.0 @hookform/resolvers@5.2.2.
emailAndPassword enabled; requireEmailVerification false v1 (A5).
admin({ defaultRole: "buyer" }) per D-06; nextCookies() last plugin.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Better Auth server + API route</name>
  <read_first>
    - 01-RESEARCH.md — lib/auth.ts, route.ts patterns
    - better-auth.com/docs/integrations/next
  </read_first>
  <action>
    src/lib/auth.ts: betterAuth, prismaAdapter(prisma, postgresql), emailAndPassword enabled, plugins admin({ defaultRole: "buyer" }), nextCookies() last, trustedOrigins BETTER_AUTH_URL.
    src/app/api/auth/[...all]/route.ts: toNextJsHandler(auth) export GET POST.
    BETTER_AUTH_SECRET і BETTER_AUTH_URL з env (validate in env.ts).
    Переконатися signUp handler ігнорує role з body — лише buyer default (D-06).
  </action>
  <acceptance_criteria>
    - POST /api/auth/sign-up/email reachable (200/4xx structured, not 404)
    - auth.ts compiles; prisma adapter wired
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Auth client + UA login/register pages</name>
  <read_first>
    - 01-UI-SPEC.md — Login, Sign-up, Copywriting errors
    - 01-CONTEXT.md — D-09 routes
  </read_first>
  <action>
    src/lib/auth-client.ts: createAuthClient baseURL NEXT_PUBLIC_APP_URL, adminClient plugin.
    src/components/auth/auth-form.tsx: react-hook-form + zod — email, password; mode login | register (confirm password register); Alert destructive on error з UA strings UI-SPEC.
    src/app/(storefront)/uviity/page.tsx: title «Вхід», submit «Увійти», link на /reiestratsiia.
    src/app/(storefront)/reiestratsiia/page.tsx: title «Реєстрація», submit «Створити обліковий запис», min password 8 helper UA.
    Client: signIn.email / signUp.email; redirect success → /kabinet.
    Немає поля role у формах (D-07).
    Create e2e/auth.spec.ts: register new email → /kabinet; login existing → /kabinet; UA error on bad password.
  </action>
  <acceptance_criteria>
    - Manual/automated: new email registers → lands /kabinet
    - Login existing user → /kabinet
    - Error states show UA Alert text
  </acceptance_criteria>
  <verify>
    <automated>npm run test:e2e -- e2e/auth.spec.ts</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Cabinet stub + session persistence E2E</name>
  <read_first>
    - 01-RESEARCH.md — KabinetPage getSession pattern
    - 01-UI-SPEC.md — Cabinet stub copy
  </read_first>
  <action>
    src/app/(storefront)/kabinet/page.tsx: Server Component auth.api.getSession headers; redirect /uviity if null; render «Особистий кабінет», «Вітаємо, {name}», empty state body UI-SPEC, link «На головну».
    StoreHeader: if session — show Кабінет + signOut via authClient; guest links unchanged.
    e2e/session-persist.spec.ts: register or login → /kabinet → page.reload() → still «Особистий кабінет» or user name visible (AUTH-05).
  </action>
  <acceptance_criteria>
    - Unauthenticated /kabinet redirects to /uviity
    - After reload, session persists (cookie)
    - AUTH-05 e2e green
  </acceptance_criteria>
  <verify>
    <automated>npm run test:e2e -- e2e/session-persist.spec.ts</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/auth/* | Credentials, session cookies |
| Client form → auth API | Untrusted signup payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | Elevation | signUp.email | mitigate | No role field; defaultRole buyer; admin seed-only |
| T-01-05-02 | Spoofing | /kabinet | mitigate | getSession server-side, not cookie presence alone |
| T-01-05-03 | Information Disclosure | auth errors | mitigate | Generic UA message; no stack traces to client |
| T-01-05-04 | Tampering | session cookie | mitigate | Better Auth HTTP-only cookies + library CSRF |

</threat_model>

<verification>
npm run test:e2e -- e2e/auth.spec.ts e2e/session-persist.spec.ts
Buyer signup cannot set admin role (grep signUp for role injection test)
</verification>

<success_criteria>
AUTH-02 and AUTH-05 verified with automated E2E
</success_criteria>

<output>
Create `.planning/phases/01-foundation-auth-design-system/01-05-SUMMARY.md` when done
</output>
