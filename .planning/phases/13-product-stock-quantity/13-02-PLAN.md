---
phase: 13-product-stock-quantity
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - src/server/services/product-availability.ts
  - src/server/services/order.service.ts
  - src/server/services/cart.service.ts
  - src/server/services/catalog.service.ts
  - src/server/services/wishlist.service.ts
  - src/server/services/order.service.test.ts
  - src/server/services/cart.service.test.ts
  - src/server/services/catalog.service.test.ts
autonomous: true
requirements:
  - ADM-PRD-03
context_decisions:
  - D-13-03
  - D-13-04
  - D-13-05
  - D-13-06
  - D-13-07

must_haves:
  truths:
    - "Successful checkout decrements Product.quantity by 1 per cart line"
    - "Product becomes SOLD only when quantity reaches 0 after decrement"
    - "Products with quantity 0 cannot be added to cart"
    - "Stale cart lines with quantity 0 are pruned on getCartForUser"
    - "Public catalog hides AVAILABLE listings with quantity 0"
  artifacts:
    - path: src/server/services/order.service.ts
      provides: reserveProductUnitForCheckout + transactional decrement
      exports: ["reserveProductUnitForCheckout", "createOrderFromCart"]
    - path: src/server/services/product-availability.ts
      provides: isProductPurchasable helper
      exports: ["isProductPurchasable"]
  key_links:
    - from: src/server/services/order.service.ts
      to: Product.quantity
      via: "quantity: { decrement: 1 }"
      pattern: "quantity.*decrement"
    - from: src/server/services/cart.service.ts
      to: product-availability
      via: isProductPurchasable / quantity gte 1 where
      pattern: "quantity.*gte.*1"
---

<objective>
Implement checkout stock decrement with conditional SOLD and extend purchasability guards across cart, catalog, and wishlist.

Purpose: D-13-04–07 — N identical units per listing; last unit triggers SOLD; prevent oversell (T-13-01).
Output: Tested server logic; no storefront quantity display (D-13-13).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-product-stock-quantity/13-CONTEXT.md
@.planning/phases/13-product-stock-quantity/13-RESEARCH.md
@.planning/phases/13-product-stock-quantity/13-PATTERNS.md
@.planning/phases/13-product-stock-quantity/13-VALIDATION.md
@src/server/services/order.service.ts
@src/server/services/cart.service.ts
@src/server/services/catalog.service.ts
@src/server/services/wishlist.service.ts
</context>

<interfaces>
From src/server/services/order.service.ts (replace inner loop ~115–123):
- Current: `tx.product.updateMany({ where: { id, status: AVAILABLE }, data: { status: SOLD } })`
- Target: atomic decrement + conditional SOLD per D-13-04–05

From src/server/services/cart.service.ts:
```typescript
export function canAddProductToCart(status: string): boolean;
export async function addToCart(userId: string, productId: string): Promise<void>;
function mapLine(item: CartItemWithProduct): CartLineDto | null;
```

From src/server/services/catalog.service.ts:
```typescript
export function buildPublicProductWhere(input: CatalogFilters & { categoryId?: string }): Prisma.ProductWhereInput;
export function buildCatalogContextWhere(...): Prisma.ProductWhereInput;
```
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checkout transaction | Untrusted concurrent buyers race on last unit |
| cart add | Client productId must not bypass stock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | createOrderFromCart | mitigate | `update` where `status=AVAILABLE AND quantity>=1` + `{ decrement: 1 }` inside `$transaction`; throw `PRODUCT_UNAVAILABLE` on miss |
| T-13-01 | Denial | double checkout | mitigate | Same atomic where — second buyer fails whole transaction |
| T-13-02 | Information | storefront | accept | D-13-13 — no quantity in catalog/PDP types (grep guard in plan 04) |
| T-13-SC | Tampering | npm installs | accept | No new packages this plan |

</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Purchasability helper + cart/catalog/wishlist guards</name>
  <read_first>
    - src/server/services/cart.service.ts — mapLine, addToCart, canAddProductToCart
    - src/server/services/catalog.service.ts — buildPublicProductWhere, buildCatalogContextWhere
    - src/server/services/wishlist.service.ts — addToWishlist findFirst (~111–118)
    - .planning/phases/13-product-stock-quantity/13-RESEARCH.md — isProductPurchasable baseline
  </read_first>
  <files>
    src/server/services/product-availability.ts
    src/server/services/cart.service.ts
    src/server/services/catalog.service.ts
    src/server/services/wishlist.service.ts
    src/server/services/cart.service.test.ts
    src/server/services/catalog.service.test.ts
  </files>
  <behavior>
    - isProductPurchasable("AVAILABLE", 1) === true
    - isProductPurchasable("AVAILABLE", 0) === false
    - isProductPurchasable("SOLD", 5) === false
    - canAddProductToCart("AVAILABLE", 0) === false; ("AVAILABLE", 1) === true
    - buildPublicProductWhere({}) includes quantity: { gte: 1 }
  </behavior>
  <action>
    Create `src/server/services/product-availability.ts` per D-13-06:

    Export `isProductPurchasable(status: ProductStatus | string, quantity: number): boolean` returning `status === "AVAILABLE" && quantity >= 1`.

    **cart.service.ts:**
    - Import helper.
    - `mapLine`: return null when `!isProductPurchasable(item.product.status, item.product.quantity)` (stale prune path unchanged).
    - `addToCart`: extend `findFirst` where with `quantity: { gte: 1 }`.
    - Change `canAddProductToCart` signature to `(status: string, quantity: number)` delegating to helper; update all call sites in repo.

    **catalog.service.ts:** Add `quantity: { gte: 1 }` to `buildPublicProductWhere` AND `buildCatalogContextWhere` (hide ghost AVAILABLE+0).

    **wishlist.service.ts:** Add `quantity: { gte: 1 }` to add-to-wishlist product `findFirst` where (same error `PRODUCT_UNAVAILABLE`).

    Do NOT add quantity to cart DTOs or public catalog card types (D-13-13).

    Extend Vitest files before/with implementation (tdd).
  </action>
  <verify>
    <automated>npm test -- src/server/services/cart.service.test.ts src/server/services/catalog.service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - product-availability.ts exports isProductPurchasable
    - cart addToCart where includes quantity gte 1
    - mapLine drops lines with quantity less than 1
    - catalog where objects include quantity gte 1
    - wishlist add uses quantity gte 1
    - Vitest green for cart + catalog tests
  </acceptance_criteria>
  <done>Purchasability enforced before cart/catalog/wishlist mutations</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Checkout decrement and conditional SOLD</name>
  <read_first>
    - src/server/services/order.service.ts — createOrderFromCart transaction loop
    - .planning/phases/13-product-stock-quantity/13-RESEARCH.md — reserveProductUnitForCheckout baseline
    - src/server/services/admin-order.service.test.ts — tx.product mock pattern
  </read_first>
  <files>src/server/services/order.service.ts</files>
  <action>
    Per D-13-04–05, replace checkout product mutation:

    1. Export async function `reserveProductUnitForCheckout(tx: Prisma.TransactionClient, productId: string): Promise<void>` in order.service.ts (or colocated module imported by order.service).

    2. Implementation:
       - `tx.product.update` with `where: { id: productId, status: "AVAILABLE", quantity: { gte: 1 } }`
       - `data: { quantity: { decrement: 1 } }`, `select: { quantity: true }`
       - On Prisma not-found (P2025) or failed update → `throw new Error("PRODUCT_UNAVAILABLE")` (keep exact string)
       - If returned `quantity === 0` → `tx.product.update({ where: { id: productId }, data: { status: "SOLD" } })`
       - If `quantity > 0` → do NOT change status (stays AVAILABLE)

    3. In `createOrderFromCart` loop: call `reserveProductUnitForCheckout(tx, line.productId)` instead of `updateMany` → SOLD.

    4. Keep OrderItem `quantity: 1` unchanged (D-13-03).

    Add small `isPrismaRecordNotFound(error: unknown): boolean` in order.service or shared prisma util if none exists.

    Do NOT restore quantity on order cancel (out of scope).
  </action>
  <verify>
    <automated>grep -q 'reserveProductUnitForCheckout' src/server/services/order.service.ts && grep -q 'quantity: { decrement: 1 }' src/server/services/order.service.ts</automated>
  </verify>
  <acceptance_criteria>
    - createOrderFromCart no longer uses updateMany to set SOLD on every sale
    - reserveProductUnitForCheckout uses decrement with quantity gte 1 guard
    - SOLD status set only when post-decrement quantity is 0
    - PRODUCT_UNAVAILABLE thrown when decrement cannot proceed
  </acceptance_criteria>
  <done>Checkout atomically reserves one unit per line</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit tests for reserveProductUnitForCheckout</name>
  <read_first>
    - src/server/services/order.service.test.ts — existing generateOrderNumber tests + it.todo
    - src/server/services/admin-order.service.test.ts — vi.fn tx.product pattern
    - Task 2 implementation of reserveProductUnitForCheckout
  </read_first>
  <files>src/server/services/order.service.test.ts</files>
  <behavior>
    - Decrement 2→1: single update call, no status SOLD update
    - Decrement 1→0: second update sets status SOLD
    - Update throws P2025: reserveProductUnitForCheckout throws PRODUCT_UNAVAILABLE
  </behavior>
  <action>
    Replace `it.todo("marks products SOLD atomically")` with Vitest cases mocking `tx.product.update`.

    Import and test `reserveProductUnitForCheckout` directly (export it for tests).

    Use vi.fn() chains: first update returns `{ quantity: N }`, assert second update called only when N===0.

    Remove or update misleading test title — behavior is decrement + conditional SOLD, not always SOLD.
  </action>
  <verify>
    <automated>npm test -- src/server/services/order.service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - order.service.test.ts has at least 3 passing tests for reserveProductUnitForCheckout
    - No it.todo left for createOrderFromCart stock behavior
    - npm test for order.service.test.ts exits 0
  </acceptance_criteria>
  <done>Checkout stock logic covered by unit tests</done>
</task>

</tasks>

<verification>
- `npm test -- src/server/services/order.service.test.ts src/server/services/cart.service.test.ts src/server/services/catalog.service.test.ts`
- Manual: product quantity 2 → two checkouts → third add-to-cart fails (optional UAT)
</verification>

<success_criteria>
- Multi-unit listing stays AVAILABLE until last unit sold
- Concurrent last-unit checkout returns PRODUCT_UNAVAILABLE for loser
- Catalog does not list AVAILABLE products with quantity 0
</success_criteria>

<output>
Create `.planning/phases/13-product-stock-quantity/13-02-SUMMARY.md` when done
</output>
