---
phase: 13-product-stock-quantity
plan: 04
type: execute
wave: 3
depends_on:
  - 13-02
  - 13-03
files_modified:
  - src/components/admin/product-form.tsx
  - src/components/admin/admin-products-table.tsx
  - src/app/(admin)/admin/tovary/[id]/page.tsx
  - e2e/admin-products.spec.ts
  - e2e/checkout.spec.ts
autonomous: true
requirements:
  - ADM-PRD-03
context_decisions:
  - D-13-11
  - D-13-12
  - D-13-13

must_haves:
  truths:
    - "Admin create/edit form shows Кількість field with create default 1"
    - "Admin product list shows Кількість column without sort"
    - "Storefront catalog and PDP do not display product.quantity"
    - "E2E admin flow sets quantity; checkout reduces stock in DB"
  artifacts:
    - path: src/components/admin/product-form.tsx
      provides: quantity input beside price
    - path: src/components/admin/admin-products-table.tsx
      provides: non-sortable Кількість column
    - path: src/app/(admin)/admin/tovary/[id]/page.tsx
      provides: defaultValues.quantity on edit
  key_links:
    - from: src/components/admin/product-form.tsx
      to: upsertProductSchema
      via: react-hook-form register quantity
      pattern: 'register\\("quantity"'
    - from: src/components/admin/admin-products-table.tsx
      to: listAdminProducts
      via: AdminProductListItem.quantity
      pattern: "product\\.quantity"
---

<objective>
Ship admin UI for stock quantity and verify storefront remains quantity-free; extend E2E for admin + checkout stock behavior.

Purpose: D-13-11–13 + ADM-PRD-03 visibility in admin only.
Output: Form field, table column, edit defaults, Playwright coverage.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-product-stock-quantity/13-UI-SPEC.md
@.planning/phases/13-product-stock-quantity/13-PATTERNS.md
@.planning/phases/13-product-stock-quantity/13-VALIDATION.md
@src/components/admin/product-form.tsx
@src/components/admin/admin-products-table.tsx
@src/app/(admin)/admin/tovary/[id]/page.tsx
@e2e/admin-products.spec.ts
@e2e/checkout.spec.ts
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin UI → server actions | quantity is user input; server Zod is authoritative (plan 03) |
| storefront render | must not leak stock counts (D-13-13) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02 | Information | catalog/PDP components | mitigate | Grep regression: no product.quantity in src/components/catalog or product pages |
| V5 | Tampering | product-form | mitigate | min/max HTML attributes + server Zod (plan 03) |
| T-13-SC | Tampering | npm installs | accept | No new shadcn components |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Product form quantity field</name>
  <read_first>
    - src/components/admin/product-form.tsx — priceUah field (~205–220), defaultValues (~72–82)
    - .planning/phases/13-product-stock-quantity/13-UI-SPEC.md — ProductForm quantity spec
    - src/server/validators/admin-product.ts — quantity field names
  </read_first>
  <files>
    src/components/admin/product-form.tsx
    src/app/(admin)/admin/tovary/[id]/page.tsx
  </files>
  <action>
  **product-form.tsx (D-13-11):**
    - Add `quantity: defaultValues?.quantity ?? 1` to useForm defaultValues.
    - In `sm:grid-cols-2` grid, place new field in column beside `priceUah`:
      - Label htmlFor="quantity" text «Кількість»
      - Input type="number" id="quantity" min={mode === "create" ? 1 : 0} max={999} step={1} inputMode="numeric" className="tabular-nums"
      - register("quantity", { valueAsNumber: true })
      - Create-only helper: «Скільки однакових одиниць у цьому оголошенні.» (text-xs text-muted-foreground)
      - Error paragraph from formState.errors.quantity
    - Field stays visible when isSold (D-13-10 edit write-off).

  **tovary/[id]/page.tsx:**
    - Add `quantity: product.quantity` to ProductForm defaultValues (raw Int, no /100).

  Ensure create uses upsertProductSchema path and edit uses updateProductSchema — quantity must flow through existing actions without type hacks.

  Do NOT add quantity to storefront components.
  </action>
  <verify>
    <automated>grep -q 'htmlFor="quantity"' src/components/admin/product-form.tsx && grep -q 'quantity: product.quantity' src/app/\(admin\)/admin/tovary/\[id\]/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - product-form renders Кількість label and input
    - create defaultValues quantity is 1
    - edit page passes server quantity into form
    - min is 1 on create and 0 on edit per mode prop
  </acceptance_criteria>
  <done>Admin form captures and displays quantity</done>
</task>

<task type="auto">
  <name>Task 2: Admin table Кількість column (no sort)</name>
  <read_first>
    - src/components/admin/admin-products-table.tsx — price column, non-sort Фото header
    - .planning/phases/13-product-stock-quantity/13-UI-SPEC.md — column order
    - .planning/phases/12-admin-tables-status-sort/12-CONTEXT.md — sort patterns to avoid
  </read_first>
  <files>src/components/admin/admin-products-table.tsx</files>
  <action>
    Per D-13-12:

    1. Extend `AdminProductListItem` type with `quantity: number`.

    2. Insert plain header after «Ціна», before «Статус»:
       `<th className="px-3 py-2 font-medium">Кількість</th>`
       — NOT AdminSortableTableHeader; do NOT add to SORTABLE_COLUMNS or sort URL keys.

    3. Body cell: `<td className="px-3 py-2 tabular-nums">{product.quantity}</td>`.

    No low-stock badge (UI-SPEC v1 out).

    Confirm list page already passes quantity from Prisma row after plan 03 (no mapper strip).
  </action>
  <verify>
    <automated>grep -q '>Кількість</' src/components/admin/admin-products-table.tsx && grep -q 'quantity: number' src/components/admin/admin-products-table.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Table header «Кількість» exists between price and status
    - Header is not sortable (no AdminSortableTableHeader on that column)
    - Cells render integer quantity with tabular-nums
    - AdminProductListItem includes quantity
  </acceptance_criteria>
  <done>Admin list shows stock per product</done>
</task>

<task type="auto">
  <name>Task 3: E2E admin quantity + storefront regression + checkout stock</name>
  <read_first>
    - e2e/admin-products.spec.ts — create product flow
    - e2e/checkout.spec.ts — checkout happy path
    - .planning/phases/13-product-stock-quantity/13-VALIDATION.md — task 13-04-01/02
    - src/components/catalog — grep for quantity usage before/after
  </read_first>
  <files>
    e2e/admin-products.spec.ts
    e2e/checkout.spec.ts
  </files>
  <action>
    **admin-products.spec.ts:**
    - On create flow, `getByLabel("Кількість").fill("2")` (or assert default 1 then change).
    - After save, navigate to `/admin/tovary` and expect row for title to show «2» in quantity column (use getByRole row + cell text or column position).

    **checkout.spec.ts:**
    - Extend or add test: after successful checkout of a product with quantity 2, re-fetch admin list or DB helper if available — prefer admin UI: login admin, open edit product, assert quantity input value is `1` (one unit sold).
    - If too flaky, document assertion on edit page only after single-unit default product (regression: checkout still works).

    **Storefront regression (D-13-13):**
    - Run grep: `rg 'product\.quantity|quantity.*шт' src/components/catalog src/app/\(storefront\) --glob '*.tsx'` — expect zero matches in display components.
    - If matches found only in admin paths, OK; fix any storefront leaks.

    Do NOT add quantity text to ProductCard or PDP.
  </action>
  <verify>
    <automated>npm run test:e2e -- e2e/admin-products.spec.ts e2e/checkout.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - admin-products e2e fills Кількість and sees value in list
    - checkout e2e still passes
    - grep finds no storefront PDP/catalog quantity display
    - e2e specs exit 0
  </acceptance_criteria>
  <done>Automated coverage for admin visibility and checkout stock reduction</done>
</task>

</tasks>

<verification>
- `npm run test:e2e -- e2e/admin-products.spec.ts e2e/checkout.spec.ts`
- Manual: PDP `/tovar/[slug]` has no stock badge (D-13-13 checklist in 13-UI-SPEC)
</verification>

<success_criteria>
- ADM-PRD-03 satisfied: admin sees/sets quantity; storefront unchanged visually
- Column «Кількість» present without sort affordance
</success_criteria>

<output>
Create `.planning/phases/13-product-stock-quantity/13-04-SUMMARY.md` when done
</output>
