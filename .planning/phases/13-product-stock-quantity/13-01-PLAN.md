---
phase: 13-product-stock-quantity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations
autonomous: true
requirements:
  - ADM-PRD-03
context_decisions:
  - D-13-01
  - D-13-02

must_haves:
  truths:
    - "Product model has NOT NULL quantity Int defaulting to 1"
    - "Existing product rows have quantity 1 after migration"
    - "Generated Prisma client exposes Product.quantity for downstream plans"
  artifacts:
    - path: prisma/schema.prisma
      provides: Product.quantity field
      contains: "quantity Int @default(1)"
    - path: prisma/migrations
      provides: product_quantity migration SQL
  key_links:
    - from: prisma/schema.prisma
      to: src/server/services
      via: "@/generated/prisma/client"
      pattern: "quantity"
---

<objective>
Add `Product.quantity` to the database schema and apply migration so Wave 2+ services can read/write stock.

Purpose: D-13-01/02 — one listing tracks N identical units; all existing rows start at 1.
Output: Applied migration + regenerated Prisma client.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-product-stock-quantity/13-CONTEXT.md
@.planning/phases/13-product-stock-quantity/13-RESEARCH.md
@.planning/phases/13-product-stock-quantity/13-PATTERNS.md
@prisma/schema.prisma
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB schema | quantity is server-controlled Int; clients cannot set negative via DB constraint alone |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | Product.quantity column | mitigate | NOT NULL + application Zod min in plan 03 |
| T-13-SC | Tampering | prisma migrate / generate | mitigate | Use locked Prisma 7.8.0 from package.json only |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add quantity field to Product model</name>
  <read_first>
    - prisma/schema.prisma — model Product (lines ~118–141)
    - .planning/phases/13-product-stock-quantity/13-CONTEXT.md — D-13-01, D-13-02
    - .planning/phases/13-product-stock-quantity/13-PATTERNS.md — schema analog (Category.sortOrder)
  </read_first>
  <files>prisma/schema.prisma</files>
  <action>
    On model `Product`, add field per D-13-01: `quantity Int @default(1)` (NOT NULL via Prisma default).

    Place after `price` or before `condition` — match project field ordering (price-adjacent).

    Do NOT modify `CartItem`, `OrderItem`, or indexes unless Prisma requires it.

    Do NOT run migrate in this task — Task 2 owns migration.
  </action>
  <verify>
    <automated>grep -q 'quantity Int @default(1)' prisma/schema.prisma</automated>
  </verify>
  <acceptance_criteria>
    - schema.prisma Product model contains exactly `quantity Int @default(1)`
    - No other models changed in this task
  </acceptance_criteria>
  <done>Schema declares quantity with default 1</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Migrate and regenerate Prisma client</name>
  <read_first>
    - .planning/phases/13-product-stock-quantity/13-RESEARCH.md — Migration section
    - prisma/schema.prisma — quantity field from Task 1
  </read_first>
  <files>prisma/migrations</files>
  <action>
    Run in project root (requires local PostgreSQL / DATABASE_URL):

    1. `npx prisma migrate dev --name product_quantity`
    2. `npx prisma generate`

    Verify generated migration SQL includes `ALTER TABLE "Product" ADD COLUMN "quantity" INTEGER NOT NULL DEFAULT 1` (or Prisma-equivalent) so existing rows get quantity=1 per D-13-02.

    If migrate fails, fix schema/env — do NOT proceed to Wave 2 plans until this task is green.

    Commit migration folder + schema change atomically with plan summary.
  </action>
  <verify>
    <automated>test -d prisma/migrations && npx prisma validate && npx prisma generate</automated>
  </verify>
  <acceptance_criteria>
    - New migration directory exists under prisma/migrations for product_quantity
    - `npx prisma validate` exits 0
    - `npx prisma generate` completes; `@/generated/prisma/client` types include `Product.quantity`
  </acceptance_criteria>
  <done>Migration applied locally and Prisma client regenerated</done>
</task>

</tasks>

<verification>
- `npx prisma migrate status` shows product_quantity applied
- `grep quantity prisma/schema.prisma` shows Product field only
</verification>

<success_criteria>
- Database column exists with NOT NULL DEFAULT 1
- Downstream plans can import `quantity` on Product without schema edits
</success_criteria>

<output>
Create `.planning/phases/13-product-stock-quantity/13-01-SUMMARY.md` when done
</output>
