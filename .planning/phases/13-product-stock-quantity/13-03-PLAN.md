---
phase: 13-product-stock-quantity
plan: 03
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - src/server/validators/admin-product.ts
  - src/server/services/admin-product.service.ts
  - src/server/validators/admin-product.test.ts
autonomous: true
requirements:
  - ADM-PRD-03
context_decisions:
  - D-13-08
  - D-13-09
  - D-13-10

must_haves:
  truths:
    - "Admin create rejects quantity less than 1 or greater than 999"
    - "Admin edit accepts quantity 0 for write-off"
    - "createProduct and updateProduct persist quantity to database"
    - "Admin save with quantity 0 on AVAILABLE listing sets status SOLD"
  artifacts:
    - path: src/server/validators/admin-product.ts
      provides: quantity on upsertProductSchema and updateProductSchema
    - path: src/server/services/admin-product.service.ts
      provides: quantity in create/update data
  key_links:
    - from: src/server/actions/admin/product.actions.ts
      to: admin-product validators
      via: upsertProductSchema.parse / updateProductSchema.parse
      pattern: "upsertProductSchema"
    - from: admin-product.service.ts
      to: prisma.product
      via: "quantity: data.quantity"
      pattern: "quantity:"
---

<objective>
Wire admin product create/edit validation and persistence for stock quantity with distinct create vs edit rules.

Purpose: D-13-08–10 + ADM-PRD-03 admin metadata path; V5 input validation on quantity.
Output: Zod schemas, service persistence, validator unit tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-product-stock-quantity/13-CONTEXT.md
@.planning/phases/13-product-stock-quantity/13-RESEARCH.md
@.planning/phases/13-product-stock-quantity/13-PATTERNS.md
@.planning/phases/13-product-stock-quantity/13-UI-SPEC.md
@src/server/validators/admin-product.ts
@src/server/services/admin-product.service.ts
@src/server/actions/admin/product.actions.ts
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin mutations | quantity from browser form — must validate server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| V5 | Tampering | upsertProductSchema / updateProductSchema | mitigate | z.coerce.number().int().min(1) create; min(0) edit; max(999) both |
| V4 | Spoofing | createProduct / updateProduct | mitigate | requireAdmin on actions (existing) |
| T-13-01 | Tampering | negative quantity via API | mitigate | Zod min + DB NOT NULL |
| T-13-SC | Tampering | npm installs | accept | No new packages |

</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Zod quantity on create and edit schemas</name>
  <read_first>
    - src/server/validators/admin-product.ts — upsertProductSchema, updateProductSchema, priceUah pattern
    - .planning/phases/13-product-stock-quantity/13-UI-SPEC.md — Copywriting Contract (Ukrainian messages)
    - .planning/phases/13-product-stock-quantity/13-CONTEXT.md — D-13-08, D-13-09, D-13-10
  </read_first>
  <files>
    src/server/validators/admin-product.ts
    src/server/validators/admin-product.test.ts
  </files>
  <behavior>
    - upsertProductSchema.parse({ ...valid, quantity: 1 }) succeeds
    - upsertProductSchema.parse({ ...valid, quantity: 0 }) throws
    - upsertProductSchema.parse({ ...valid, quantity: 1000 }) throws
    - updateProductSchema.parse({ ...valid, id, quantity: 0 }) succeeds
    - updateProductSchema.parse({ ...valid, id, quantity: 1000 }) throws
  </behavior>
  <action>
    Per D-13-08–10, add `quantity` to both schemas and transform outputs:

    **upsertProductSchema (create):**
    `quantity: z.coerce.number().int().min(1, "Кількість має бути не менше 1").max(999, "Максимум 999 одиниць")`

    **updateProductSchema (edit):**
    `quantity: z.coerce.number().int().min(0, "Кількість не може бути від'ємною").max(999, "Максимум 999 одиниць")`

    Include `quantity` in `.transform()` return objects for both schemas.

    Do NOT add quantity to `listAdminProductsSchema` or `adminProductListSortSchema` (no sort D-13-12).

    Extend admin-product.test.ts: import updateProductSchema; add cases above.

    Use same valid fixture fields as existing priceUah tests (title, brand, categoryId cuid, etc.).
  </action>
  <verify>
    <automated>npm test -- src/server/validators/admin-product.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both schemas expose quantity in output type
    - Create min 1 enforced; edit min 0 enforced
    - Max 999 enforced on both
    - admin-product.test.ts passes
  </acceptance_criteria>
  <done>Admin quantity validation rules encoded in Zod</done>
</task>

<task type="auto">
  <name>Task 2: Persist quantity in createProduct and updateProduct</name>
  <read_first>
    - src/server/services/admin-product.service.ts — createProduct (~231–247), updateProduct (~276–303)
    - src/server/validators/admin-product.ts — transformed quantity field
    - .planning/phases/13-product-stock-quantity/13-RESEARCH.md — auto-SOLD on admin qty 0 (A2)
  </read_first>
  <files>src/server/services/admin-product.service.ts</files>
  <action>
    **createProduct:** Add `quantity: data.quantity` to prisma.product.create data (D-13-09 default comes from form/Zod, not DB-only).

    **updateProduct:** Add `quantity: data.quantity` to prisma.product.update data.

    **Auto-SOLD (research A2, aligns D-13-05 ghost prevention):** Before update, compute effective status:
    - If `existing.status !== "SOLD"` and `data.quantity === 0` and resolved `status === "AVAILABLE"` (from existing SOLD lock logic), set persisted `status: "SOLD"`.
    - If product already SOLD, keep status SOLD (existing lock).

    Do NOT change `updateProductStatus` or list sort logic.

    `listAdminProducts` already returns full Product — quantity appears once schema migrated (no select change required unless explicit select omits it).
  </action>
  <verify>
    <automated>grep -q 'quantity: data.quantity' src/server/services/admin-product.service.ts</automated>
  </verify>
  <acceptance_criteria>
    - createProduct data includes quantity from UpsertProductValues
    - updateProduct data includes quantity from UpdateProductValues
    - updateProduct sets status SOLD when admin saves AVAILABLE with quantity 0
    - SOLD products remain status-locked on edit (existing behavior)
  </acceptance_criteria>
  <done>Admin API persists quantity and normalizes zero stock to SOLD</done>
</task>

</tasks>

<verification>
- `npm test -- src/server/validators/admin-product.test.ts`
- Optional: prisma studio — create product with quantity 3 via action
</verification>

<success_criteria>
- Admin actions accept quantity in payload end-to-end to DB
- Invalid quantities rejected before Prisma write
</success_criteria>

<output>
Create `.planning/phases/13-product-stock-quantity/13-03-SUMMARY.md` when done
</output>
