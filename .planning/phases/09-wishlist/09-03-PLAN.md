---
phase: 09-wishlist
plan: 03
type: execute
wave: 3
depends_on:
  - 09-02
files_modified:
  - src/app/(storefront)/layout.tsx
  - src/components/wishlist/wishlist-toggle-button.tsx
  - src/components/catalog/product-card.tsx
  - src/components/catalog/product-grid.tsx
  - src/app/(storefront)/katalog/page.tsx
  - src/app/(storefront)/katalog/[slug]/page.tsx
  - src/app/(storefront)/tovar/[slug]/page.tsx
autonomous: true
requirements:
  - WISH-05
context_decisions:
  - "D-09-09: Heart overlay on ProductCard, stopPropagation"
  - "D-09-10: PDP inline toggle"
  - "D-09-11: aria-pressed + UA labels"
  - "D-09-12: Sonner toasts + sync icon state"
  - "D-09-21: Toaster in storefront layout"
  - "D-09-22: top-center richColors"
  - "D-09-18: session uses actions + router.refresh"

must_haves:
  truths:
    - "Storefront shows Sonner toasts on wishlist toggle"
    - "Catalog card heart toggles without navigating to PDP"
    - "PDP has inline wishlist toggle beside add to cart"
    - "Guest toggle uses localStorage; session uses server actions"
  artifacts:
    - path: src/components/wishlist/wishlist-toggle-button.tsx
      provides: overlay + inline variants
    - path: src/app/(storefront)/layout.tsx
      provides: Toaster (no WishlistMergeGate)
  key_links:
    - from: product-card.tsx
      to: WishlistToggleButton
      via: sibling absolute overlay outside Link
      pattern: "stopPropagation"
    - from: wishlist-toggle-button.tsx
      to: guest-storage / wishlist.actions
      via: hasSession branch

---

<objective>
Storefront Toaster, WishlistToggleButton with Sonner UX, ProductCard overlay + catalog/PDP wiring (WISH-05, D-09-09–12, D-09-21–22).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/09-wishlist/09-UI-SPEC.md — WishlistToggleButton, ProductCard delta, Sonner
@src/components/cart/add-to-cart-button.tsx
@src/components/catalog/product-card.tsx
@src/components/chat/chat-thread.tsx — toast import example
@src/app/(admin)/admin/layout.tsx — Toaster props reference
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client toggle | Guest writes localStorage only; session calls server actions |
| Layout | Must not mount CartPendingMergeGate equivalent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Tampering | guest productId | mitigate | Only store opaque cuid strings from catalog props |
| T-09-03-02 | Spoofing | session toggle | mitigate | Server actions use requireBuyer; ignore client inWishlist for auth |
| T-09-03-03 | Denial of Service | toast spam | accept | Single transition per click; disabled while isPending |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Storefront Toaster — no merge gate (D-09-21, D-09-22, D-09-15)</name>
  <read_first>
    - src/app/(storefront)/layout.tsx — CartPendingMergeGate present; do NOT copy for wishlist
    - src/components/ui/sonner.tsx
    - src/app/(admin)/admin/layout.tsx
  </read_first>
  <files>src/app/(storefront)/layout.tsx</files>
  <action>
    Import Toaster from @/components/ui/sonner (or sonner directly if matching admin).
    Render after main/footer fragment: position="top-center" richColors closeButton per D-09-22 and 09-UI-SPEC.
    Do NOT add WishlistPendingMergeGate, WishlistMergeGate, or any merge component.
    Leave CartPendingMergeGate unchanged (cart-only).
  </action>
  <verify>
    <automated>grep -q 'Toaster' src/app/\(storefront\)/layout.tsx &amp;&amp; grep -q 'top-center' src/app/\(storefront\)/layout.tsx &amp;&amp; ! grep -qi 'Wishlist.*Merge' src/app/\(storefront\)/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Toaster present in storefront layout
    - No wishlist merge gate in layout tree
  </acceptance_criteria>
  <done>Storefront can display Sonner toasts</done>
</task>

<task type="auto">
  <name>Task 2: WishlistToggleButton component (D-09-11, D-09-12)</name>
  <read_first>
    - src/components/cart/add-to-cart-button.tsx — hasSession, useTransition, router.refresh
    - src/lib/wishlist/guest-storage.ts
    - src/server/actions/wishlist.actions.ts
    - .planning/phases/09-wishlist/09-UI-SPEC.md — copy table
  </read_first>
  <files>src/components/wishlist/wishlist-toggle-button.tsx</files>
  <action>
    Client component with props: productId, productTitle, hasSession, initialInWishlist, variant: 'overlay' | 'inline'.
    State inWishlist updates optimistically on click before async completes (D-09-12).
    Guest branch: addGuestWishlistProduct / removeGuestWishlistProduct; on 'max' toast.error('У обраному вже максимум 20 товарів'); on add toast.success('Додано до обраного'); on remove toast.success('Прибрано з обраного'); dispatchWishlistChanged after mutation.
    Session branch: addToWishlistAction / removeFromWishlistAction in useTransition; router.refresh() on success; revert state + toast.error('Не вдалося оновити обране') on failure.
    Overlay: absolute right-2 top-2 z-10; Button size icon secondary with bg-background/90; onClick preventDefault + stopPropagation.
    Inline: outline button full width on mobile with Heart mr-2.
    aria-pressed, aria-label toggle UA strings per UI-SPEC.
    Import toast from 'sonner'.
  </action>
  <verify>
    <automated>grep -q 'WishlistToggleButton' src/components/wishlist/wishlist-toggle-button.tsx &amp;&amp; grep -q 'stopPropagation' src/components/wishlist/wishlist-toggle-button.tsx &amp;&amp; grep -q "Додано до обраного" src/components/wishlist/wishlist-toggle-button.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Component exports overlay and inline variants
    - Ukrainian toast strings match UI-SPEC
  </acceptance_criteria>
  <done>Toggle button ready for catalog and PDP</done>
</task>

<task type="auto">
  <name>Task 3: ProductCard overlay + catalog/PDP wiring (D-09-09, D-09-10)</name>
  <read_first>
    - src/components/catalog/product-card.tsx
    - src/components/catalog/product-grid.tsx
    - src/app/(storefront)/katalog/page.tsx
    - src/app/(storefront)/tovar/[slug]/page.tsx
    - src/server/services/wishlist.service.ts — getWishlistedProductIds
  </read_first>
  <files>
    src/components/catalog/product-card.tsx,
    src/components/catalog/product-grid.tsx,
    src/app/(storefront)/katalog/page.tsx,
    src/app/(storefront)/katalog/[slug]/page.tsx,
    src/app/(storefront)/tovar/[slug]/page.tsx
  </files>
  <action>
    Refactor ProductCard per RESEARCH Pattern 4: outer wrapper div.group h-full; image block relative; WishlistToggleButton overlay as sibling NOT inside Link; Link wraps title/price area OR split so heart click does not navigate (D-09-09).
    Extend ProductCardProps: productId (from product.id), hasSession, initialInWishlist optional.
    ProductGrid: accept hasSession, wishlistedProductIds?: Set&lt;string&gt; and pass to each card.
    Catalog pages: get session via auth.api.getSession; if session.user.id call getWishlistedProductIds once per page; pass hasSession and wishlisted set to ProductGrid.
    PDP page: compute initialInWishlist via isProductInWishlist when session else false; render WishlistToggleButton variant inline below AddToCartButton in flex flex-col gap-3 (D-09-10).
    Guest catalog: hasSession false; toggle reads guest storage on mount via hasGuestWishlistProduct in toggle component useEffect (mirror AddToCartButton guest effect).
  </action>
  <verify>
    <automated>grep -q 'WishlistToggleButton' src/components/catalog/product-card.tsx &amp;&amp; grep -q 'WishlistToggleButton' src/app/\(storefront\)/tovar/\[slug\]/page.tsx &amp;&amp; npm run lint -- --max-warnings=0 2>/dev/null | head -5 || true</automated>
  </verify>
  <acceptance_criteria>
    - ProductCard renders overlay heart
    - PDP renders inline wishlist button
    - No button nested inside single card Link (valid HTML)
  </acceptance_criteria>
  <done>Catalog and PDP show working wishlist toggles</done>
</task>

</tasks>

<verification>
- Manual smoke: click heart on catalog card — stays on catalog, toast shows
- PDP toggle visible next to cart CTA
</verification>

<success_criteria>
- WISH-05 satisfied on catalog card and PDP with Sonner feedback
</success_criteria>

<output>
Create `.planning/phases/09-wishlist/09-03-SUMMARY.md` when done
</output>
