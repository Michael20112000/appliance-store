---
phase: 09-wishlist
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/types/wishlist.ts
  - src/server/validators/wishlist.ts
  - src/server/services/wishlist.service.ts
  - src/server/services/wishlist.service.test.ts
  - src/server/actions/wishlist.actions.ts
autonomous: true
requirements:
  - WISH-02
  - WISH-03
  - WISH-04
context_decisions:
  - "D-09-17: Zod validators + requireBuyer on mutations"
  - "D-09-18: server action + router.refresh pattern"
  - "D-09-19: show SOLD/DRAFT as unavailable rows"
  - "D-09-20: no auto-delete from wishlist"
  - "D-09-15: no merge action"

must_haves:
  truths:
    - "Logged-in user can add/remove wishlist rows scoped to their userId"
    - "listWishlistForUser returns available and unavailable lines without pruning"
    - "Adding to wishlist only succeeds for AVAILABLE products"
    - "No mergePendingWishlist or similar action exists"
  artifacts:
    - path: src/server/services/wishlist.service.ts
      provides: CRUD + list + count + getWishlistedProductIds
    - path: src/server/actions/wishlist.actions.ts
      provides: add/remove/resolve actions
    - path: src/server/services/wishlist.service.test.ts
      provides: D-09-23 service tests
  key_links:
    - from: wishlist.actions.ts
      to: requireBuyer
      via: mutations only
      pattern: "requireBuyer"
    - from: listWishlistForUser
      to: Product.status
      via: available boolean on DTO
      pattern: "available.*status"

---

<objective>
Server-side wishlist persistence: validators, service (no auto-prune per D-09-20), server actions with revalidation — explicit no-merge (WISH-02, WISH-03, WISH-04 backend).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/09-wishlist/09-RESEARCH.md — Pattern 8 contrast cart.service
@src/server/services/cart.service.ts
@src/server/actions/cart.actions.ts
@src/server/validators/cart.ts
@src/lib/permissions.ts
</context>

<interfaces>
From src/server/validators/cart.ts:
- productId: z.string().cuid("Невірний ідентифікатор товару")

Target src/types/wishlist.ts:
- WishlistLineDto: productId, slug, title, brand, priceKopiyky, condition, status, available: boolean, image, addedAt
- WishlistViewDto: { lines: WishlistLineDto[] }
- Action results: { ok: true } | { ok: false, error: 'PRODUCT_UNAVAILABLE' | 'UNKNOWN' }
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Actions | Authenticated mutations; guest must not hit add/remove |
| resolveGuestWishlistProductsAction | Public read by IDs — cap array length |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Elevation | wishlist.service | mitigate | Every query/mutation filters by userId from session, never from client body |
| T-09-02-02 | Tampering | productId | mitigate | Zod cuid() in validators; Prisma parameterized queries |
| T-09-02-03 | Denial of Service | resolveGuestWishlistProductsAction | mitigate | z.array(z.string().cuid()).max(20) on input |
| T-09-02-04 | Repudiation | merge on login | mitigate | Do not implement mergePendingWishlistAction; document WISH-03 |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Types and Zod validators (D-09-17)</name>
  <read_first>
    - src/types/cart.ts — DTO shape patterns
    - src/server/validators/cart.ts
  </read_first>
  <files>src/types/wishlist.ts, src/server/validators/wishlist.ts</files>
  <action>
    Create wishlist.ts types: WishlistLineDto, WishlistViewDto, action result unions.
    Create validators: wishlistProductIdSchema (productId cuid), resolveGuestWishlistSchema (items: z.array(wishlistProductIdSchema).max(20)), optional toggle schema if shared.
    Ukrainian error messages consistent with cart validators.
  </action>
  <verify>
    <automated>grep -q 'WishlistLineDto' src/types/wishlist.ts &amp;&amp; grep -q 'cuid' src/server/validators/wishlist.ts</automated>
  </verify>
  <acceptance_criteria>
    - Exported types usable by service and components
    - Max 20 IDs enforced in resolve schema
  </acceptance_criteria>
  <done>Types and validators committed</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: wishlist.service — list without prune (D-09-19, D-09-20)</name>
  <read_first>
    - src/server/services/cart.service.ts — mapLine, addToCart AVAILABLE check
    - .planning/phases/09-wishlist/09-CONTEXT.md — unavailable display rules
  </read_first>
  <files>src/server/services/wishlist.service.ts, src/server/services/wishlist.service.test.ts</files>
  <behavior>
    - listWishlistForUser includes SOLD/DRAFT with available: false
    - addToWishlist throws or returns error for non-AVAILABLE (match cart PRODUCT_UNAVAILABLE pattern)
    - removeFromWishlist deletes row regardless of product status
    - getWishlistItemCount(userId) returns count
    - getWishlistedProductIds(userId) returns Set or string[] for catalog overlay
    - listWishlistPreviewForUser(userId, limit 3) orders by createdAt desc
    - Does NOT delete stale wishlist rows (contrast cart getCartForUser)
  </behavior>
  <action>
    Implement wishlist.service.ts with prisma.wishlistItem include product + first image (mirror cartLineInclude pattern).
    Map status to available: product.status === 'AVAILABLE'.
    addToWishlist: find product status AVAILABLE else error PRODUCT_UNAVAILABLE; create with skipDuplicates or catch P2002 for idempotent add.
    removeFromWishlist: deleteMany where userId + productId.
    resolveProductsByIds(productIds: string[]): for guest resolution — return lines for all found products including SOLD/DRAFT (for /obrane display per D-09-19).
    Export isProductInWishlist(userId, productId) for PDP/catalog.
    Write wishlist.service.test.ts with mocked prisma or test DB pattern used in cart.service.test.ts if present; else unit-test pure mappers and document integration in manual checklist.
  </action>
  <verify>
    <automated>npm test -- src/server/services/wishlist.service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests assert unavailable lines are returned not deleted
    - Tests assert add rejects non-AVAILABLE
  </acceptance_criteria>
  <done>Service and tests pass; no deleteMany for stale products in list path</done>
</task>

<task type="auto">
  <name>Task 3: Server actions + revalidate (D-09-17, D-09-18)</name>
  <read_first>
    - src/server/actions/cart.actions.ts — revalidateCartPaths pattern
  </read_first>
  <files>src/server/actions/wishlist.actions.ts</files>
  <action>
    Create wishlist.actions.ts with revalidateWishlistPaths(): revalidatePath('/obrane'), revalidatePath('/kabinet'), revalidatePath('/', 'layout').
    addToWishlistAction(productId): requireBuyer(), parse schema, call addToWishlist, revalidate, return { ok } | { ok: false, error }.
    removeFromWishlistAction(productId): same pattern.
    resolveGuestWishlistProductsAction(productIds): NO requireBuyer — parse resolveGuestWishlistSchema, call resolveProductsByIds, return lines (for guest /obrane client).
    Optional getWishlistCountAction if needed for client nav — prefer server prop from header instead.
    Explicitly do NOT add mergeGuestWishlistAction or any merge* export (D-09-15, WISH-03).
  </action>
  <verify>
    <automated>grep -q 'addToWishlistAction' src/server/actions/wishlist.actions.ts &amp;&amp; grep -q 'revalidatePath.*obrane' src/server/actions/wishlist.actions.ts &amp;&amp; ! grep -qi 'merge' src/server/actions/wishlist.actions.ts</automated>
  </verify>
  <acceptance_criteria>
    - Mutations call requireBuyer
    - resolve action accepts max 20 IDs
    - No merge-related exports
  </acceptance_criteria>
  <done>Actions wired with revalidation paths</done>
</task>

</tasks>

<verification>
- `npm test -- src/server/services/wishlist.service.test.ts`
- `grep -ri merge src/server/actions/wishlist.actions.ts src/server/services/wishlist.service.ts` — no wishlist merge
</verification>

<success_criteria>
- Session wishlist CRUD works via service/actions
- List API supports unavailable rows per D-09-19
</success_criteria>

<output>
Create `.planning/phases/09-wishlist/09-02-SUMMARY.md` when done
</output>
