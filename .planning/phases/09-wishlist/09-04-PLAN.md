---
phase: 09-wishlist
plan: 04
type: execute
wave: 4
depends_on:
  - 09-03
files_modified:
  - src/components/wishlist/wishlist-nav-link.tsx
  - src/components/layout/store-header.tsx
  - src/app/(storefront)/obrane/page.tsx
  - src/components/wishlist/wishlist-page-content.tsx
  - src/components/wishlist/wishlist-grid.tsx
  - src/components/wishlist/wishlist-unavailable-card.tsx
  - src/components/wishlist/guest-wishlist-view.tsx
autonomous: true
requirements:
  - WISH-01
  - WISH-02
  - WISH-04
context_decisions:
  - "D-09-01: /obrane full list page"
  - "D-09-03: /obrane public guest + session"
  - "D-09-04: header WishlistNavLink all visitors"
  - "D-09-05: client nav badge LS vs DB"
  - "D-09-06: logged in never read guest key"
  - "D-09-07: logout restores guest LS"
  - "D-09-08: hide badge at 0; 99+ cap"
  - "D-09-19: unavailable row copy"
  - "D-09-20: manual remove only"

must_haves:
  truths:
    - "All visitors see Heart nav link to /obrane with correct badge count"
    - "Guest /obrane lists products from localStorage"
    - "Logged-in /obrane lists DB wishlist including unavailable rows"
    - "After login badge shows DB count only not guest items"
  artifacts:
    - path: src/app/(storefront)/obrane/page.tsx
      provides: public wishlist route
    - path: src/components/wishlist/wishlist-nav-link.tsx
      provides: header badge
  key_links:
    - from: WishlistNavLink
      to: /obrane
      via: Link href
      pattern: 'href="/obrane"'
    - from: guest-wishlist-view.tsx
      to: resolveGuestWishlistProductsAction
      via: productIds from LS

---

<objective>
Header wishlist navigation for all visitors and full `/obrane` page with guest client view + session RSC list including unavailable products (WISH-01, WISH-02, WISH-04, D-09-01–08, D-09-19).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/09-wishlist/09-UI-SPEC.md — WishlistNavLink, /obrane, WishlistUnavailableCard
@src/components/cart/cart-nav-link.tsx
@src/components/layout/store-header.tsx
@src/lib/wishlist/guest-storage.ts
@src/lib/wishlist/wishlist-events.ts
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /obrane page | Public; no requireBuyer on page load |
| WishlistNavLink | Client reads LS only when !hasSession |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Information Disclosure | /obrane session | mitigate | List only current user's wishlist via session.user.id in service |
| T-09-04-02 | Tampering | guest resolve IDs | mitigate | Server action validates cuid array max 20 |
| T-09-04-03 | Spoofing | badge count | accept | Display-only; mutations still server-auth |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: WishlistNavLink + StoreHeader (D-09-04–08)</name>
  <read_first>
    - src/components/cart/cart-nav-link.tsx
    - src/components/layout/store-header.tsx
    - src/server/services/wishlist.service.ts — getWishlistItemCount
  </read_first>
  <files>src/components/wishlist/wishlist-nav-link.tsx, src/components/layout/store-header.tsx</files>
  <action>
    Create WishlistNavLink client component: props hasSession boolean, initialCount optional number from server when session.
    Link to /obrane with Heart size-5; same wrapper classes as CartNavLink per UI-SPEC.
    Badge: hidden when count===0; label count&gt;99 ? '99+' : String(count) (D-09-08, not cart's 9+).
    Guest: on mount read getGuestWishlistCount(); subscribe to 'wishlist:changed' and window storage event for appliance-wishlist-guest (optional cross-tab).
    Session: display initialCount; after navigation refresh from server layout is acceptable; do NOT read guest storage when hasSession true (D-09-06).
    aria-label UA with count or empty per UI-SPEC; aria-current=page on /obrane.
    StoreHeader: import auth session; render WishlistNavLink for ALL visitors before CartNavLink; pass hasSession={!!session?.user} initialCount={session?.user ? await getWishlistItemCount(session.user.id) : undefined}.
    Order: StoreMobileNav → WishlistNavLink → {session ? CartNavLink : null} → StoreHeaderAuth (D-09-04).
    Optional discretion: add «Обране» link in StoreMobileNav sheet — not blocking.
  </action>
  <verify>
    <automated>grep -q 'WishlistNavLink' src/components/layout/store-header.tsx &amp;&amp; grep -q '99+' src/components/wishlist/wishlist-nav-link.tsx</automated>
  </verify>
  <acceptance_criteria>
    - WishlistNavLink visible without login
    - CartNavLink still only when session (unchanged)
  </acceptance_criteria>
  <done>Header shows wishlist heart for guests and users</done>
</task>

<task type="auto">
  <name>Task 2: Unavailable card + grid components (D-09-19)</name>
  <read_first>
    - .planning/phases/09-wishlist/09-UI-SPEC.md — WishlistUnavailableCard wireframe
    - src/components/catalog/product-card.tsx
  </read_first>
  <files>
    src/components/wishlist/wishlist-unavailable-card.tsx,
    src/components/wishlist/wishlist-grid.tsx,
    src/components/wishlist/wishlist-page-content.tsx
  </files>
  <action>
    WishlistUnavailableCard: Card with muted styling; thumb; title line-clamp-2; text «Товар більше недоступний»; WishlistToggleButton inline only; NO AddToCartButton (D-09-19).
    WishlistGrid: split lines into available vs !available; render ProductCard for available (reuse catalog card with wishlist props); unavailable section with subheading «Недоступні» when both sections non-empty; grid cols match product-grid (2/4).
    WishlistPageContent: receives lines + hasSession; empty state heading «Обране порожнє», body and CTA «Перейти до каталогу» → /katalog per UI-SPEC; container mx-auto max-w-6xl py-12.
  </action>
  <verify>
    <automated>grep -q 'Товар більше недоступний' src/components/wishlist/wishlist-unavailable-card.tsx &amp;&amp; grep -q 'WishlistPageContent' src/components/wishlist/wishlist-page-content.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Unavailable rows show UA message and allow remove via heart
    - Available section uses standard product cards
  </acceptance_criteria>
  <done>List UI components ready for /obrane</done>
</task>

<task type="auto">
  <name>Task 3: /obrane route guest + session split (D-09-01, D-09-03)</name>
  <read_first>
    - .planning/phases/09-wishlist/09-RESEARCH.md — Pattern 9 page split
    - src/server/actions/wishlist.actions.ts
    - src/server/services/wishlist.service.ts
  </read_first>
  <files>
    src/app/(storefront)/obrane/page.tsx,
    src/components/wishlist/guest-wishlist-view.tsx
  </files>
  <action>
    obrane/page.tsx: metadata title «Обране»; NO requireBuyer (public D-09-03).
    If session?.user: await listWishlistForUser(userId); render WishlistPageContent with lines server-side.
    Else: render GuestWishlistView client component that reads getGuestWishlistProductIds on mount, calls resolveGuestWishlistProductsAction(ids), shows skeleton while loading, Alert on error per UI-SPEC, empty state when no IDs.
    GuestWishlistView must NOT run when user logs in on same tab without refresh — rely on full page navigation after login or document refresh in manual checklist; RSC branch handles fresh session load (D-09-06 silent DB switch).
    Do not filter out SOLD/DRAFT from resolve results (D-09-19).
  </action>
  <verify>
    <automated>test -f src/app/\(storefront\)/obrane/page.tsx &amp;&amp; grep -q 'Обране' src/app/\(storefront\)/obrane/page.tsx &amp;&amp; grep -q 'GuestWishlistView' src/components/wishlist/guest-wishlist-view.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Route /obrane exists and is public
    - Session path uses DB; guest path uses LS + resolve action
  </acceptance_criteria>
  <done>/obrane works for guest and logged-in users</done>
</task>

</tasks>

<verification>
- Guest: add 2 items → visit /obrane → see products
- Session: DB list shows unavailable section when applicable
</verification>

<success_criteria>
- WISH-04 primary list at /obrane complete
- Header badge reflects correct source per auth state
</success_criteria>

<output>
Create `.planning/phases/09-wishlist/09-04-SUMMARY.md` when done
</output>
