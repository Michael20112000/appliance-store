---
phase: 09-wishlist
plan: 05
type: execute
wave: 5
depends_on:
  - 09-04
files_modified:
  - src/app/(storefront)/kabinet/page.tsx
  - src/components/wishlist/wishlist-cabinet-preview.tsx
  - .planning/phases/09-wishlist/09-MANUAL-CHECKLIST.md
autonomous: true
requirements:
  - WISH-03
  - WISH-04
context_decisions:
  - "D-09-02: kabinet preview max 3 + Дивитись усе"
  - "D-09-15: confirm no merge in codebase"
  - "D-09-23: full vitest suite"
  - "D-09-24: manual no-merge scenario"

must_haves:
  truths:
    - "Logged-in user sees up to 3 wishlist previews in /kabinet with link to /obrane"
    - "Codebase has no wishlist merge gate or merge action"
    - "Vitest wishlist tests pass in CI"
  artifacts:
    - path: src/components/wishlist/wishlist-cabinet-preview.tsx
      provides: cabinet preview block
    - path: .planning/phases/09-wishlist/09-MANUAL-CHECKLIST.md
      provides: D-09-24 manual verification
  key_links:
    - from: kabinet/page.tsx
      to: listWishlistPreviewForUser
      via: server fetch limit 3

---

<objective>
Kabinet «Обране» preview (≤3 cards), phase verification: Vitest suite + anti-merge grep + manual checklist (D-09-02, D-09-23, D-09-24, WISH-03).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/09-wishlist/09-UI-SPEC.md — kabinet section
@src/app/(storefront)/kabinet/page.tsx
@.planning/phases/08-admin-ux-chat-lifecycle/08-MANUAL-CHECKLIST.md — checklist format reference
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /kabinet | requireBuyer — preview only for authenticated buyers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05-01 | Elevation | kabinet preview | mitigate | requireBuyer on page; preview query scoped to session.user.id |
| T-09-05-02 | Repudiation | WISH-03 | mitigate | Manual checklist D-09-24 + automated grep for merge artifacts |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Kabinet wishlist preview (D-09-02)</name>
  <read_first>
    - src/app/(storefront)/kabinet/page.tsx
    - src/server/services/wishlist.service.ts — listWishlistPreviewForUser
    - .planning/phases/09-wishlist/09-UI-SPEC.md — cabinet section layout
  </read_first>
  <files>src/app/(storefront)/kabinet/page.tsx, src/components/wishlist/wishlist-cabinet-preview.tsx</files>
  <action>
    Create WishlistCabinetPreview: accepts WishlistLineDto[] (max 3 from server); grid grid-cols-1 sm:grid-cols-3; show ProductCard for available lines and compact unavailable row for unavailable (or skip unavailable in preview — discretion: show up to 3 most recent regardless of status per D-09-19).
    Empty: «Поки нічого в обраному» + Link «Перейти до каталогу» → /katalog.
    Footer Link/Button outline «Дивитись усе» → /obrane mt-4.
    kabinet/page.tsx: insert section ABOVE «Мої замовлення» with H2 «Обране»; fetch listWishlistPreviewForUser(session.user.id, 3); render preview.
    Section only for logged-in users (page already requireBuyer); guests use /obrane only (D-09-02).
  </action>
  <verify>
    <automated>grep -q 'Обране' src/app/\(storefront\)/kabinet/page.tsx &amp;&amp; grep -q 'Дивитись усе' src/components/wishlist/wishlist-cabinet-preview.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Cabinet shows at most 3 wishlist items
    - Link to /obrane present
  </acceptance_criteria>
  <done>Kabinet preview block shipped</done>
</task>

<task type="auto">
  <name>Task 2: Anti-merge verification + Vitest gate (D-09-15, D-09-23, WISH-03)</name>
  <read_first>
    - src/components/cart/cart-pending-merge-gate.tsx — must NOT have wishlist twin
    - src/app/(storefront)/layout.tsx
  </read_first>
  <files>.planning/phases/09-wishlist/09-MANUAL-CHECKLIST.md</files>
  <action>
    Run automated anti-merge checks and document in 09-MANUAL-CHECKLIST.md prerequisites section:
    - `grep -rni 'wishlist.*merge\|merge.*wishlist\|WishlistPendingMerge' src/` must return no hits (cart merge files excluded if only comment mentions wishlist).
    - Confirm layout has CartPendingMergeGate but NO WishlistPendingMergeGate.
    - Confirm no mergeGuestWishlist or mergePendingWishlist in server actions.
    Run full Vitest: `npm test`.
    If any wishlist test missing from prior plans, add minimal test now.
    Create 09-MANUAL-CHECKLIST.md skeleton with D-09-24 scenarios (executor fills checkboxes during manual run) — see Task 3 in same plan for full checklist content; this task ensures grep + npm test documented as automated gates.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - npm test exits 0
    - No wishlist merge code paths in src/
  </acceptance_criteria>
  <done>Automated verification green; checklist file exists</done>
</task>

<task type="auto">
  <name>Task 3: Complete 09-MANUAL-CHECKLIST.md (D-09-24)</name>
  <read_first>
    - .planning/phases/09-wishlist/09-CONTEXT.md — D-09-24
    - .planning/phases/07-catalog-filters-fix/07-MANUAL-CHECKLIST.md — format
  </read_first>
  <files>.planning/phases/09-wishlist/09-MANUAL-CHECKLIST.md</files>
  <action>
    Write complete manual checklist covering:
    1) Guest: add 3 products via catalog heart → badge shows 3 → reload → still 3 on /obrane.
    2) Login: DB wishlist does NOT contain guest's 3 (silent switch D-09-06); badge shows DB count only.
    3) Logout: guest localStorage list visible again in badge and /obrane (D-09-07).
    4) Overlay click does not open PDP (D-09-09).
    5) Toasts on add/remove; cap toast at 21st guest item (D-09-14).
    6) Unavailable product row on /obrane if test data includes SOLD item in wishlist (D-09-19).
    7) Kabinet preview ≤3 + «Дивитись усе».
    Include checkbox markdown, env prerequisites (dev server, test user), and pass/fail column.
    Reference WISH-03 explicitly in login scenario title.
  </action>
  <verify>
    <automated>test -f .planning/phases/09-wishlist/09-MANUAL-CHECKLIST.md &amp;&amp; grep -q 'D-09-24' .planning/phases/09-wishlist/09-MANUAL-CHECKLIST.md &amp;&amp; grep -q 'без merge' .planning/phases/09-wishlist/09-MANUAL-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - Checklist file complete with login no-merge scenario
    - All D-09-24 steps documented
  </acceptance_criteria>
  <done>Manual checklist ready for human execution</done>
</task>

</tasks>

<verification>
- `npm test`
- `grep -rni 'WishlistPendingMerge' src/` → empty
- Human runs 09-MANUAL-CHECKLIST.md before phase close
</verification>

<success_criteria>
- WISH-03 verified by grep + manual checklist
- WISH-04 kabinet preview complete
- Phase 9 ready for /gsd-verify-work
</success_criteria>

<output>
Create `.planning/phases/09-wishlist/09-05-SUMMARY.md` when done
</output>
