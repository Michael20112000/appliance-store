---
phase: 09-wishlist
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/
  - src/lib/wishlist/guest-storage.ts
  - src/lib/wishlist/guest-storage.test.ts
  - src/lib/wishlist/wishlist-events.ts
autonomous: false
requirements:
  - WISH-01
  - WISH-03
context_decisions:
  - "D-09-13: key appliance-wishlist-guest, v:1 items shape"
  - "D-09-14: MAX_ITEMS 20 with toast error on cap (client)"
  - "D-09-15: no merge gate or merge action"
  - "D-09-16: WishlistItem Prisma model"

must_haves:
  truths:
    - "Guest can add/remove product IDs in localStorage with max 20 cap"
    - "WishlistItem table exists with unique userId+productId"
    - "No WishlistPendingMerge or merge action exists in codebase"
  artifacts:
    - path: prisma/schema.prisma
      provides: WishlistItem model + User/Product relations
    - path: src/lib/wishlist/guest-storage.ts
      provides: guest read/write/add/remove/count
    - path: src/lib/wishlist/guest-storage.test.ts
      provides: WISH-01 unit coverage
  key_links:
    - from: src/lib/wishlist/guest-storage.ts
      to: localStorage
      via: KEY appliance-wishlist-guest
      pattern: "appliance-wishlist-guest"
    - from: src/lib/wishlist/wishlist-events.ts
      to: WishlistNavLink
      via: CustomEvent wishlist:changed
      pattern: "wishlist:changed"

---

<objective>
Prisma `WishlistItem` foundation and guest localStorage module mirroring cart pending storage — without any login merge path (WISH-01, WISH-03 groundwork).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-wishlist/09-CONTEXT.md
@.planning/phases/09-wishlist/09-RESEARCH.md
@src/lib/cart/pending-storage.ts
@prisma/schema.prisma
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser localStorage | Untrusted productId strings; server never trusts guest list without re-fetch |
| Prisma migration | Schema change affects production DB when deployed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Tampering | guest-storage.ts | mitigate | Parse JSON safely; reset corrupt payload to `{ v: 1, items: [] }` |
| T-09-01-02 | Denial of Service | guest add | mitigate | Enforce MAX_ITEMS=20 client-side; server resolve caps at 20 in later plan |
| T-09-01-03 | Elevation | future merge | mitigate | Do NOT add WishlistPendingMergeGate; grep in 09-05 |
| T-09-01-SC | Tampering | prisma migrate | mitigate | Use project migrate dev; no ad-hoc schema drift |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Prisma WishlistItem schema (D-09-16)</name>
  <read_first>
    - prisma/schema.prisma — Product model, Cart/CartItem pattern
    - .planning/phases/09-wishlist/09-RESEARCH.md — Pattern 7 schema snippet
  </read_first>
  <files>prisma/schema.prisma</files>
  <action>
    Add model WishlistItem with fields: id String @id @default(cuid()), userId String, productId String, createdAt DateTime @default(now()).
    Relations: user User @relation(fields: [userId], references: [id], onDelete: Cascade); product Product @relation(fields: [productId], references: [id], onDelete: Cascade).
    Add @@unique([userId, productId]) and @@index([userId]).
    On User model add wishlistItems WishlistItem[].
    On Product model add wishlistItems WishlistItem[].
    Do NOT add Wishlist parent model (YAGNI per RESEARCH).
  </action>
  <verify>
    <automated>grep -q 'model WishlistItem' prisma/schema.prisma &amp;&amp; grep -q '@@unique(\[userId, productId\])' prisma/schema.prisma &amp;&amp; npx prisma validate</automated>
  </verify>
  <acceptance_criteria>
    - prisma validate exits 0
    - WishlistItem has cascade deletes and composite unique
  </acceptance_criteria>
  <done>Schema file contains WishlistItem with relations on User and Product</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>[BLOCKING] Prisma migrate — WishlistItem</name>
  <read_first>
    - prisma/migrations/ — existing migrate dev pattern (e.g. conversation_status)
  </read_first>
  <files>prisma/migrations/</files>
  <action>
    After Task 1 schema edit, apply migration (project standard — migrations folder exists; package.json has no db push script):
    `npx prisma migrate dev --name wishlist_item`
    If interactive prompt blocks automation, operator runs locally and confirms applied.
    Run `npx prisma generate` after migrate (postinstall also runs generate).
    Do NOT add CartPendingMergeGate or any wishlist merge migration data.
  </action>
  <verify>
    <automated>npx prisma migrate status 2>&1 | grep -v '^#' | grep -q 'Database schema is up to date'</automated>
  </verify>
  <acceptance_criteria>
    - Migration SQL creates wishlist_item table
    - Generated client includes WishlistItem types
  </acceptance_criteria>
  <done>Database schema up to date; WishlistItem queryable via Prisma client</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guest storage + events (D-09-13, D-09-14)</name>
  <read_first>
    - src/lib/cart/pending-storage.ts — KEY, v:1, MAX_ITEMS, read/write
    - .planning/phases/09-wishlist/09-UI-SPEC.md — cap toast copy
  </read_first>
  <files>
    src/lib/wishlist/guest-storage.ts,
    src/lib/wishlist/guest-storage.test.ts,
    src/lib/wishlist/wishlist-events.ts
  </files>
  <behavior>
    - addGuestWishlistProduct returns 'added' | 'duplicate' | 'max'
    - removeGuestWishlistProduct is idempotent
    - getGuestWishlistCount matches items length
    - MAX_ITEMS 20 blocks 21st add with 'max'
    - dispatchWishlistChanged fires CustomEvent 'wishlist:changed' after mutations
  </behavior>
  <action>
    Create guest-storage.ts with KEY `appliance-wishlist-guest`, MAX_ITEMS 20, type GuestWishlist { v: 1; items: { productId: string }[] } per D-09-13.
    Export: readGuestWishlist, addGuestWishlistProduct, removeGuestWishlistProduct, hasGuestWishlistProduct, getGuestWishlistProductIds, getGuestWishlistCount, clearGuestWishlist (for tests only if needed).
    Mirror pending-storage defensive parse (invalid JSON → empty list).
    Create wishlist-events.ts with dispatchWishlistChanged() calling window.dispatchEvent(new CustomEvent('wishlist:changed')) when typeof window !== 'undefined'.
    Call dispatchWishlistChanged after successful add/remove in guest-storage.
    Vitest: mock localStorage (pattern from existing cart/catalog tests).
    Do NOT export merge functions; do NOT reference cart-pending-merge.
  </action>
  <verify>
    <automated>npm test -- src/lib/wishlist/guest-storage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All guest-storage tests pass
    - grep -r 'WishlistPendingMerge\|merge.*[Ww]ishlist' src/ returns no matches (excluding comments in cart merge files)
  </acceptance_criteria>
  <done>Guest storage module tested; wishlist:changed event available for nav badge</done>
</task>

</tasks>

<verification>
- `npx prisma validate`
- `npm test -- src/lib/wishlist/guest-storage.test.ts`
- Confirm no merge gate files added under src/components/wishlist/
</verification>

<success_criteria>
- WishlistItem exists in DB and Prisma client
- Guest storage API matches D-09-13/14 with Vitest green
</success_criteria>

<output>
Create `.planning/phases/09-wishlist/09-01-SUMMARY.md` when done
</output>
