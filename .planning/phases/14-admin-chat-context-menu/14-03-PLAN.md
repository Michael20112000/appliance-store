---
phase: 14-admin-chat-context-menu
plan: 03
type: execute
wave: 3
depends_on:
  - 14-02
files_modified:
  - src/components/chat/conversation-list.tsx
  - src/components/chat/admin-chat-inbox.tsx
  - e2e/admin-chat.spec.ts
  - .planning/phases/14-admin-chat-context-menu/14-MANUAL-CHECKLIST.md
autonomous: true
requirements:
  - ADM-CHAT-01
user_setup: []

must_haves:
  truths:
    - "Desktop right-click on inbox row opens context menu with same actions as thread ⋮"
    - "Native browser context menu suppressed on row (D-14-03)"
    - "Left click on row still selects/opens conversation (D-14-11)"
    - "Context menu not rendered on mobile viewport — lifecycle via thread ⋮ only (D-14-09, D-14-10)"
    - "Listbox semantics preserved: role=listbox container, role=option on row button (D-14-13)"
    - "Delete from list shows same confirm dialog before mutation"
    - "E2E covers desktop right-click menuitem visibility (D-14-15)"
  artifacts:
    - path: src/components/chat/conversation-list.tsx
      provides: ContextMenu-wrapped rows when enableContextMenu
    - path: src/components/chat/admin-chat-inbox.tsx
      provides: enableContextMenu={!isMobile}
    - path: e2e/admin-chat.spec.ts
      provides: Desktop RCM + mobile no-menu tests
    - path: .planning/phases/14-admin-chat-context-menu/14-MANUAL-CHECKLIST.md
      provides: D-14-16 manual verification steps
  key_links:
    - from: src/components/chat/conversation-list.tsx
      to: src/components/ui/context-menu.tsx
      via: ContextMenuTrigger render={button role=option}
      pattern: "ContextMenuTrigger"
    - from: src/components/chat/conversation-list.tsx
      to: src/components/chat/use-conversation-lifecycle-actions.ts
      via: per-row hook with conversation.id
      pattern: "useConversationLifecycleActions"
    - from: src/components/chat/admin-chat-inbox.tsx
      to: src/components/chat/conversation-list.tsx
      via: enableContextMenu prop from useIsMobile
      pattern: "enableContextMenu"
---

<objective>
Wire desktop right-click ContextMenu on inbox rows (D-14-01, D-14-03, D-14-09–D-14-14), gate off on mobile (D-14-10), and verify with e2e + manual checklist (D-14-15, D-14-16).

Purpose: Deliver ADM-CHAT-01 — quick chat lifecycle from list without opening thread.
Output: ConversationList + inbox prop + Playwright tests + 14-MANUAL-CHECKLIST.md.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-admin-chat-context-menu/14-CONTEXT.md
@.planning/phases/14-admin-chat-context-menu/14-RESEARCH.md
@.planning/phases/14-admin-chat-context-menu/14-UI-SPEC.md
@.planning/phases/14-admin-chat-context-menu/14-02-SUMMARY.md
@src/components/chat/conversation-list.tsx
@src/components/chat/admin-chat-inbox.tsx
@src/components/chat/use-conversation-lifecycle-actions.ts
@src/components/chat/conversation-lifecycle-menu-items.tsx
@src/components/ui/context-menu.tsx
@e2e/admin-chat.spec.ts
</context>

<interfaces>
From 14-02 shared modules (consume, do not duplicate):

useConversationLifecycleActions(conversationId: string) returns:
- pending, deleteOpen, setDeleteOpen, handleArchive, handleUnarchive, handleDelete, view

ConversationLifecycleMenuItems({ Item, view, status, pending, onArchive, onUnarchive, onRequestDelete })

ConversationLifecycleDeleteDialog({ open, onOpenChange, pending, onConfirm })

ContextMenu pattern (RESEARCH Pattern 1):
- ContextMenuTrigger with nativeButton + render={<button role="option" ... />}
- onContextMenu={(e) => e.preventDefault()} on option button (D-14-03)
- aria-label «Дії з діалогом» on trigger button (D-14-14)
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Inbox row RCM | Admin-only route; actions still server-guarded |
| Mobile touch | Long-press must not open list menu (D-14-09) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-01 | Tampering | delete without confirm from list | mitigate | Reuse ConversationLifecycleDeleteDialog; onRequestDelete opens dialog |
| T-14-03-02 | Information Disclosure | context menu on wrong row | mitigate | Hook bound to row conversationId, not selectedConversationId |
| T-14-03-03 | Elevation | mobile long-press menu | mitigate | Omit entire ContextMenu subtree when enableContextMenu=false (D-14-09) |
| T-14-03-04 | Repudiation | accidental row select after menu action | accept | Base UI menu item onSelect does not bubble to row click (D-14-11) |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: ConversationList ContextMenu + mobile gate (D-14-01,03,09–14)</name>
  <read_first>
    - src/components/chat/conversation-list.tsx
    - src/components/chat/admin-chat-inbox.tsx — useIsMobile 767px
    - .planning/phases/14-admin-chat-context-menu/14-RESEARCH.md — Pitfalls 1–4
    - src/components/ui/context-menu.tsx
    - src/components/chat/chat-thread.tsx — DropdownMenuTrigger render pattern reference
  </read_first>
  <files>src/components/chat/conversation-list.tsx, src/components/chat/admin-chat-inbox.tsx</files>
  <action>
    Add prop to ConversationList: `enableContextMenu?: boolean` default false.

    In `admin-chat-inbox.tsx` AdminChatInboxInner, pass `enableContextMenu={!isMobile}` to ConversationList (D-14-09, D-14-10). Reuse existing `useIsMobile` — do not add ⋮ per row.

    Extract row rendering to inner component `ConversationListRow` (same file or sibling) that can call `useConversationLifecycleActions(conversation.id)` inside AdminChatProvider tree.

    When `enableContextMenu === true` per row:
    - Wrap with ContextMenu / ContextMenuTrigger / ContextMenuContent
    - ContextMenuTrigger: `nativeButton`, `render` prop composing onto the existing row `button` (`type="button"`, `role="option"`) preserving row classes, onClick → onSelect(conversation.id), aria-selected, avatar block
    - `onContextMenu={(event) => event.preventDefault()}` on the option button (D-14-03)
    - `aria-label="Дії з діалогом"` on option button (D-14-14)
    - ContextMenuContent with ConversationLifecycleMenuItems Item={ContextMenuItem}, passing conversation.status and hook handlers; onRequestDelete → setDeleteOpen(true)
    - Render ConversationLifecycleDeleteDialog per row (or single dialog per row — acceptable)

    When `enableContextMenu === false` (mobile): render plain `<button role="option">` exactly as today — no ContextMenu wrapper (prevents Base UI long-press).

    Preserve container `role="listbox"` and row `role="option"` (D-14-13). Do not nest button inside non-render trigger div.

    Do NOT add new server actions (D-14-05). Do NOT add row ⋮ button (deferred).

    Left click behavior unchanged (D-14-11).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - enableContextMenu passed from admin-chat-inbox as !isMobile
    - Desktop: ContextMenuTrigger uses render + nativeButton on role=option button
    - Mobile code path has zero ContextMenu imports rendered
    - preventDefault on contextmenu event for desktop rows
    - Shared hook/menu items used; no duplicate handler logic in conversation-list
  </acceptance_criteria>
  <done>Inbox rows support desktop RCM with lifecycle parity</done>
</task>

<task type="auto">
  <name>Task 2: Playwright e2e — desktop RCM + mobile skip (D-14-15)</name>
  <read_first>
    - e2e/admin-chat.spec.ts
    - .planning/phases/14-admin-chat-context-menu/14-RESEARCH.md — Playwright example
    - e2e/helpers/admin.ts
  </read_first>
  <files>e2e/admin-chat.spec.ts</files>
  <action>
    Extend `e2e/admin-chat.spec.ts` per D-14-15:

    Test 1 — title/grep `right-click`: desktop viewport 1280x720, loginAsAdmin, goto `/admin/chaty`, first `getByRole('option')` visible → `click({ button: 'right' })` → expect `getByRole('menuitem', { name: 'Архівувати' })` visible when on active tab with OPEN conversation. If no options visible, `test.skip()` with message (match existing spec pattern).

    Test 2 — mobile viewport 390x844: right-click first option if visible → expect `getByRole('menuitem')` count 0.

    Test 3 — title/grep `left-click selects`: left-click option → composer `#admin-chat-message` visible (extends existing flow).

    Do NOT assert full archive mutation in CI unless seed guarantees OPEN conversation — menu visibility is sufficient for ADM-CHAT-01 automated gate per RESEARCH open question.

    Use page-level menuitem locators (portal), not scoped to row only.
  </action>
  <verify>
    <automated>npx playwright test e2e/admin-chat.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Spec contains tests matching /right-click/ and /left-click selects/
    - Mobile test expects zero menuitems after right-click
    - `npx playwright test e2e/admin-chat.spec.ts` exits 0 (skips allowed when no seed data)
  </acceptance_criteria>
  <done>E2E covers desktop RCM and mobile absence</done>
</task>

<task type="auto">
  <name>Task 3: Manual checklist D-14-16</name>
  <read_first>
    - .planning/phases/14-admin-chat-context-menu/14-CONTEXT.md — D-14-16
    - .planning/phases/12-admin-tables-status-sort/12-MANUAL-CHECKLIST.md — format reference
  </read_first>
  <files>.planning/phases/14-admin-chat-context-menu/14-MANUAL-CHECKLIST.md</files>
  <action>
    Create `14-MANUAL-CHECKLIST.md` for phase 14 with table columns: # | Route | Action | Expected | Pass.

    Include at minimum these D-14-16 scenarios:
    1. Desktop `/admin/chaty` active tab: ПКМ row → menu opens; лівий клік іншого рядка → selects without stuck menu
    2. Desktop: delete from RCM → AlertDialog UA confirm → confirm → row gone + toast
    3. Desktop: archive from RCM on selected thread → thread clears; on non-selected row → thread stays open
    4. Mobile viewport: no context menu on row long-press/right-click; open thread → ⋮ archive/delete still work
    5. Archive tab: ПКМ archived row → «Повернути з архіву» visible when status ARCHIVED

    Note environment: `npm run dev`, admin signed in.
  </action>
  <verify>
    <automated>test -f .planning/phases/14-admin-chat-context-menu/14-MANUAL-CHECKLIST.md && grep -c '|' .planning/phases/14-admin-chat-context-menu/14-MANUAL-CHECKLIST.md | awk '{exit ($1>=5)?0:1}'</automated>
  </verify>
  <acceptance_criteria>
    - Manual checklist file exists with ≥5 verification rows
    - Covers ПКМ, left click, delete confirm, mobile thread ⋮, archive tab
  </acceptance_criteria>
  <done>D-14-16 manual UAT documented</done>
</task>

</tasks>

<verification>
- `npm run build`
- `npx playwright test e2e/admin-chat.spec.ts`
- `npm test` (full unit suite)
- Run 14-MANUAL-CHECKLIST.md before `/gsd-verify-work`
</verification>

<success_criteria>
- ADM-CHAT-01 complete: desktop RCM lifecycle from inbox list
- ROADMAP success criteria 1–4 satisfied
- All D-14-xx decisions implemented (01–16)
- No new server actions; thread DropdownMenu unchanged
</success_criteria>

<output>
Create `.planning/phases/14-admin-chat-context-menu/14-03-SUMMARY.md` when done
</output>
