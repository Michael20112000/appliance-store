---
phase: 14-admin-chat-context-menu
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - src/components/chat/use-conversation-lifecycle-actions.ts
  - src/components/chat/conversation-lifecycle-menu-items.tsx
  - src/components/chat/conversation-lifecycle-delete-dialog.tsx
  - src/components/chat/conversation-lifecycle-menu-items.test.tsx
  - src/components/chat/chat-thread.tsx
autonomous: true
requirements:
  - ADM-CHAT-01
user_setup: []

must_haves:
  truths:
    - "Archive/unarchive/delete handlers live in one shared module consumed by chat-thread"
    - "Menu visibility matches view + status rules from chat-thread (active+OPEN → archive; archive+ARCHIVED → unarchive; delete always)"
    - "After archive/delete on selected conversation → clearSelectionAndRefresh; on other row → refreshInbox only (D-14-08)"
    - "After unarchive → refreshInbox + success toast"
    - "Delete uses Ukrainian AlertDialog copy identical to pre-refactor chat-thread"
    - "Thread header DropdownMenu visual and behavior unchanged (D-14-02)"
  artifacts:
    - path: src/components/chat/use-conversation-lifecycle-actions.ts
      provides: Shared handlers, pending state, refresh routing
      exports: ["useConversationLifecycleActions"]
    - path: src/components/chat/conversation-lifecycle-menu-items.tsx
      provides: Shared menu items for DropdownMenu and ContextMenu
      exports: ["ConversationLifecycleMenuItems"]
    - path: src/components/chat/conversation-lifecycle-delete-dialog.tsx
      provides: Shared delete AlertDialog
      exports: ["ConversationLifecycleDeleteDialog"]
    - path: src/components/chat/chat-thread.tsx
      provides: Thread ⋮ using shared module only
  key_links:
    - from: src/components/chat/use-conversation-lifecycle-actions.ts
      to: src/server/actions/admin/chat.actions.ts
      via: archiveConversationAction, unarchiveConversationAction, deleteConversationAction
      pattern: "archiveConversationAction|deleteConversationAction"
    - from: src/components/chat/use-conversation-lifecycle-actions.ts
      to: src/components/chat/admin-chat-provider.tsx
      via: useAdminChat clearSelectionAndRefresh / refreshInbox
      pattern: "clearSelectionAndRefresh|refreshInbox"
    - from: src/components/chat/chat-thread.tsx
      to: src/components/chat/conversation-lifecycle-menu-items.tsx
      via: DropdownMenuItem as Item component
      pattern: "ConversationLifecycleMenuItems"
---

<objective>
Extract conversation lifecycle (archive / unarchive / delete) from `chat-thread.tsx` into shared modules (D-14-04, D-14-05, D-14-06, D-14-07, D-14-08) and refactor thread to consume them without visual change (D-14-02).

Purpose: Single source of truth before inbox ContextMenu wiring in plan 14-03.
Output: Hook + menu items + delete dialog + Vitest visibility tests; thread ⋮ unchanged for users.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-admin-chat-context-menu/14-CONTEXT.md
@.planning/phases/14-admin-chat-context-menu/14-RESEARCH.md
@.planning/phases/14-admin-chat-context-menu/14-UI-SPEC.md
@src/components/chat/chat-thread.tsx
@src/components/chat/admin-chat-provider.tsx
@src/server/actions/admin/chat.actions.ts
@src/lib/admin-chat-url.ts
@src/types/chat.ts
</context>

<interfaces>
From src/types/chat.ts:
```typescript
export type ConversationSummaryDto = {
  id: string;
  status: ConversationStatus; // OPEN | ARCHIVED from Prisma
  // ...
};
```

From admin-chat-provider (useAdminChat):
```typescript
view: AdminChatView; // "active" | "archive"
selectedConversationId: string | null;
clearSelectionAndRefresh: () => void;
refreshInbox: () => void;
```

From chat.actions.ts (reuse only — do not modify signatures):
- archiveConversationAction(conversationId)
- unarchiveConversationAction(conversationId)
- deleteConversationAction(conversationId)

UA delete dialog strings (must preserve verbatim from chat-thread.tsx lines 222–236):
- Title: «Видалити діалог назавжди?»
- Description: «Усі повідомлення буде видалено без можливості відновлення. Покупець більше не побачить цей діалог.»
- Buttons: «Скасувати», «Видалити»
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Server Actions | conversationId from UI; must stay validated server-side |
| Destructive delete | User confirm before mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-01 | Elevation | lifecycle actions | mitigate | Reuse existing actions with `requireAdmin()` — no new mutations (D-14-05) |
| T-14-02-02 | Tampering | wrong conversationId on list RCM | mitigate | Hook parameter is explicit `conversationId`; never read selection as implicit target |
| T-14-02-03 | Denial of Service | delete without confirm | mitigate | `ConversationLifecycleDeleteDialog` gates `deleteConversationAction` |
| T-14-02-04 | Spoofing | CSRF on Server Actions | accept | Next.js Server Actions + session — unchanged from Phase 8 |

</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared menu items + visibility Vitest (D-14-06)</name>
  <read_first>
    - src/components/chat/chat-thread.tsx — lines 172–188 menu visibility
    - .planning/phases/14-admin-chat-context-menu/14-RESEARCH.md — Pattern 2 visibility table
    - src/lib/admin-chat-url.ts — AdminChatView type
  </read_first>
  <files>src/components/chat/conversation-lifecycle-menu-items.tsx, src/components/chat/conversation-lifecycle-menu-items.test.tsx</files>
  <behavior>
    - view=active, status=OPEN → renders «Архівувати», not «Повернути з архіву», always «Видалити назавжди»
    - view=archive, status=ARCHIVED → renders «Повернути з архіву», not «Архівувати», always delete
    - view=active, status=ARCHIVED → no archive item, delete only
    - pending=true → all rendered items receive disabled
  </behavior>
  <action>
    Create `conversation-lifecycle-menu-items.tsx` exporting `ConversationLifecycleMenuItems` per D-14-04 and D-14-06.

    Props:
    - `Item`: React component type compatible with DropdownMenuItem and ContextMenuItem (onClick, disabled, variant, children)
    - `view: AdminChatView`
    - `status: ConversationStatus`
    - `pending: boolean`
    - `onArchive`, `onUnarchive`, `onRequestDelete` callbacks

    Visibility rules (identical to chat-thread.tsx):
    - show archive when `view === "active" && status === "OPEN"`
    - show unarchive when `view === "archive" && status === "ARCHIVED"`
    - always render delete with `variant="destructive"`; delete onClick calls `onRequestDelete` (opens dialog), not immediate delete

    Labels UA: «Архівувати», «Повернути з архіву», «Видалити назавжди».

    Add Vitest + RTL test file covering the four behavior cases above using a stub `Item` that renders children and records props.

    Do NOT import ContextMenu or DropdownMenu in this file — keep presentation-agnostic.
  </action>
  <verify>
    <automated>npx vitest run src/components/chat/conversation-lifecycle-menu-items.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All four visibility matrix cases pass in Vitest
    - Delete item always present; archive/unarchive mutually exclusive per view+status
    - No server action imports in menu-items module
  </acceptance_criteria>
  <done>Shared menu items component tested for visibility parity</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useConversationLifecycleActions hook (D-14-04, D-14-07, D-14-08)</name>
  <read_first>
    - src/components/chat/chat-thread.tsx — handleArchive/Unarchive/Delete + toasts
    - src/components/chat/admin-chat-provider.tsx — clearSelectionAndRefresh, refreshInbox, selectedConversationId
    - .planning/phases/14-admin-chat-context-menu/14-RESEARCH.md — afterArchiveOrDelete pattern
  </read_first>
  <files>src/components/chat/use-conversation-lifecycle-actions.ts, src/components/chat/conversation-lifecycle-delete-dialog.tsx</files>
  <behavior>
    - Hook called with conversationId C: archive success → if selectedConversationId===C then clearSelectionAndRefresh else refreshInbox; toast «Діалог архівовано»
    - delete success → same refresh rule as archive; toast «Діалог видалено»; closes delete dialog
    - unarchive success → always refreshInbox (not clearSelection); toast «Діалог повернуто в активні»
    - action failure → error toast (UA strings from chat-thread)
  </behavior>
  <action>
    Create `use-conversation-lifecycle-actions.ts` exporting `useConversationLifecycleActions(conversationId: string)` per D-14-04.

    Inside: `useAdminChat()` for view, selectedConversationId, clearSelectionAndRefresh, refreshInbox; `useTransition` for pending; local `deleteOpen` state.

    Implement handlers calling existing server actions only (D-14-05) — no new mutations.

    Post-action routing per D-14-07 and D-14-08:
    - `afterArchiveOrDelete()`: if `selectedConversationId === conversationId` → `clearSelectionAndRefresh()`; else → `refreshInbox()`
    - unarchive: always `refreshInbox()`

    Create `conversation-lifecycle-delete-dialog.tsx` with shared AlertDialog using exact UA copy from interfaces block. Props: `open`, `onOpenChange`, `pending`, `onConfirm`.

    Return from hook: `{ pending, deleteOpen, setDeleteOpen, handleArchive, handleUnarchive, handleDelete, view }` (view needed by menu items consumer).

    Optional: add focused Vitest for refresh routing with mocked useAdminChat — only if fast; otherwise rely on manual + e2e in 14-03.
  </action>
  <verify>
    <automated>npx vitest run src/components/chat/conversation-lifecycle-menu-items.test.tsx && npx vitest run src/server/services/chat.service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Hook file exports useConversationLifecycleActions
    - Delete dialog UA strings match pre-refactor chat-thread exactly
    - Hook imports only existing chat.actions mutations
    - afterArchiveOrDelete distinguishes selected vs non-selected conversation (D-14-08)
  </acceptance_criteria>
  <done>Shared hook and delete dialog ready for thread and list</done>
</task>

<task type="auto">
  <name>Task 3: Refactor chat-thread to shared module (D-14-02)</name>
  <read_first>
    - src/components/chat/chat-thread.tsx — full file
    - src/components/chat/conversation-lifecycle-menu-items.tsx
    - src/components/chat/use-conversation-lifecycle-actions.ts
    - src/components/chat/conversation-lifecycle-delete-dialog.tsx
  </read_first>
  <files>src/components/chat/chat-thread.tsx</files>
  <action>
    Refactor `ChatThread` per D-14-02: keep `DropdownMenu` + `MoreVertical` trigger unchanged (same Button render prop, aria-label «Дії з діалогом», align end).

    Replace inline handlers and duplicate menu items with:
    - `useConversationLifecycleActions(selectedConversationId)` — only when conversation selected
    - `ConversationLifecycleMenuItems` with `Item={DropdownMenuItem}`
    - `ConversationLifecycleDeleteDialog` instead of inline AlertDialog

    Remove duplicated toast strings, handleArchive/Unarchive/Delete, and inline AlertDialog block.

  Do NOT:
    - Replace DropdownMenu with ContextMenu (D-14-02)
    - Change header layout, icons, or copy
    - Add list ContextMenu (plan 14-03)

    Verify thread path still calls clearSelectionAndRefresh on archive/delete for open thread (selected id always matches hook id).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - chat-thread.tsx imports ConversationLifecycleMenuItems and useConversationLifecycleActions
    - No duplicate archive/unarchive/delete handler bodies remain in chat-thread.tsx
    - DropdownMenu trigger and items visually unchanged (grep «Дії з діалогом» and «Архівувати» still present)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Thread ⋮ uses shared lifecycle; zero user-visible regression</done>
</task>

</tasks>

<verification>
- `npx vitest run src/components/chat/conversation-lifecycle-menu-items.test.tsx`
- `npx vitest run src/server/services/chat.service.test.ts`
- `npm run build`
- Manual smoke: open `/admin/chaty`, ⋮ → archive/delete still work
</verification>

<success_criteria>
- D-14-04, D-14-05, D-14-06, D-14-07, D-14-08 implemented in shared modules
- D-14-02: thread keeps DropdownMenu only
- chat-thread.tsx has no duplicated lifecycle logic
</success_criteria>

<output>
Create `.planning/phases/14-admin-chat-context-menu/14-02-SUMMARY.md` when done
</output>
