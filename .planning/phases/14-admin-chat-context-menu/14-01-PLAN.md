---
phase: 14-admin-chat-context-menu
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/context-menu.tsx
  - components.json
autonomous: true
requirements:
  - ADM-CHAT-01
user_setup: []

must_haves:
  truths:
    - "shadcn context-menu component exists and exports ContextMenu, ContextMenuTrigger, ContextMenuContent, ContextMenuItem"
    - "ContextMenuItem supports variant destructive like DropdownMenuItem"
  artifacts:
    - path: src/components/ui/context-menu.tsx
      provides: Base UI context menu primitives styled for project
      contains: "ContextMenuItem"
  key_links:
    - from: src/components/ui/context-menu.tsx
      to: "@base-ui/react"
      via: ContextMenu primitive import
      pattern: "@base-ui/react"
---

<objective>
Install shadcn `context-menu` component (D-14-01) as the UI primitive for inbox right-click menus.

Purpose: Unblocks shared lifecycle wiring and ConversationList integration in later waves.
Output: `src/components/ui/context-menu.tsx` committed; build passes.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-admin-chat-context-menu/14-CONTEXT.md
@.planning/phases/14-admin-chat-context-menu/14-RESEARCH.md
@.planning/phases/14-admin-chat-context-menu/14-UI-SPEC.md
@components.json
@src/components/ui/dropdown-menu.tsx
</context>

<interfaces>
From src/components/ui/dropdown-menu.tsx (pattern to mirror):
- Trigger uses `render` prop + optional `nativeButton` for composing onto `<button>`
- Menu items support `variant="destructive"`, `disabled`, `onClick`
- File is `"use client"` and uses `@base-ui/react` primitives

New file must export at minimum:
- ContextMenu, ContextMenuTrigger, ContextMenuContent, ContextMenuItem
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → filesystem | shadcn scaffolds source into repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | Tampering | shadcn CLI output | mitigate | Use official `npx shadcn@latest add context-menu`; no hand-rolled menu positioning |
| T-14-SC | Tampering | npm/shadcn install | accept | No new npm package — only source file; uses existing `@base-ui/react` per RESEARCH Package Legitimacy Audit |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn context-menu (D-14-01)</name>
  <read_first>
    - .planning/phases/14-admin-chat-context-menu/14-CONTEXT.md — D-14-01 locked decision
    - components.json — style preset base-nova
    - .planning/phases/14-admin-chat-context-menu/14-RESEARCH.md — Installation section
  </read_first>
  <files>src/components/ui/context-menu.tsx, components.json</files>
  <action>
    Run from repo root: `npx shadcn@latest add context-menu` (non-interactive flags if CLI supports them).

    Per D-14-01: add only the shadcn context-menu component — do NOT add new npm dependencies beyond what CLI updates in lockfile (expect none; `@base-ui/react` already present).

    After install, confirm `src/components/ui/context-menu.tsx` exists and follows same conventions as `dropdown-menu.tsx` (`"use client"`, `data-slot` attributes, Tailwind cn classes).

    Do NOT wire ContextMenu into conversation-list yet — install-only in this plan.
  </action>
  <verify>
    <automated>test -f src/components/ui/context-menu.tsx && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/ui/context-menu.tsx` exists
    - `npm run build` exits 0
    - No unrelated shadcn components added in this task
  </acceptance_criteria>
  <done>context-menu.tsx installed and project builds</done>
</task>

<task type="auto">
  <name>Task 2: Verify exports for downstream plans</name>
  <read_first>
    - src/components/ui/context-menu.tsx — full file after install
    - src/components/ui/dropdown-menu.tsx — export list at file bottom
  </read_first>
  <files>src/components/ui/context-menu.tsx</files>
  <action>
    Confirm named exports required by Phase 14 downstream:

    - `ContextMenu`, `ContextMenuTrigger`, `ContextMenuContent`, `ContextMenuItem`
    - `ContextMenuTrigger` accepts `render` prop (Base UI pattern, same as DropdownMenuTrigger)
    - `ContextMenuItem` accepts `variant` including `destructive`

    If CLI output uses different export names, re-export or align to match dropdown-menu ergonomics — do not change visual tokens.

    Grep gate (comments excluded): `grep -v '^[[:space:]]*//' src/components/ui/context-menu.tsx | grep -c 'export.*ContextMenuItem'` must be ≥ 1.
  </action>
  <verify>
    <automated>grep -v '^[[:space:]]*//' src/components/ui/context-menu.tsx | grep -cE 'export.*(ContextMenu|ContextMenuTrigger|ContextMenuContent|ContextMenuItem)' | awk '{exit ($1>=4)?0:1}'</automated>
  </verify>
  <acceptance_criteria>
    - All four ContextMenu* symbols are exported from context-menu.tsx
    - ContextMenuItem supports destructive variant (prop or data attribute documented in file)
  </acceptance_criteria>
  <done>Export surface verified for Wave 2–3 consumers</done>
</task>

</tasks>

<verification>
- `test -f src/components/ui/context-menu.tsx`
- `npm run build` green
</verification>

<success_criteria>
- D-14-01 satisfied: shadcn context-menu installed at `src/components/ui/context-menu.tsx`
- Wave 2 can import ContextMenu* without further CLI steps
</success_criteria>

<output>
Create `.planning/phases/14-admin-chat-context-menu/14-01-SUMMARY.md` when done
</output>
