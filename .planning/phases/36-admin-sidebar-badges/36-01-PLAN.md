---
phase: 36-admin-sidebar-badges
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/server/services/admin-sidebar.service.test.ts
autonomous: true
requirements:
  - ADM-NAV-01

must_haves:
  truths:
    - "admin-sidebar.service.test.ts exists and all tests are in RED (failing) state before service implementation"
    - "Tests verify getAdminSidebarCounts() returns correct shape with all five count fields"
    - "Tests assert pendingOrders uses status=PENDING filter only (D-01)"
    - "Tests assert unresolvedCallbacks uses status=PENDING AND archivedAt=null (D-05)"
    - "Tests assert categories and products use total count (no filter)"
    - "Tests assert unreadChats delegates to countUnreadForAdmin() correctly"
  artifacts:
    - path: "src/server/services/admin-sidebar.service.test.ts"
      provides: "Failing Vitest unit tests for getAdminSidebarCounts"
      exports: []
  key_links:
    - from: "admin-sidebar.service.test.ts"
      to: "admin-sidebar.service.ts (not yet created)"
      via: "import { getAdminSidebarCounts } from './admin-sidebar.service'"
      pattern: "getAdminSidebarCounts"
---

<objective>
Nyquist Wave 0: Write failing unit tests for `getAdminSidebarCounts()` before the service exists.

Purpose: Establish the contract for the aggregated sidebar count query function so the Wave 1 implementation can be driven by tests passing RED→GREEN.

Output: `src/server/services/admin-sidebar.service.test.ts` with 5+ test cases covering filter correctness, shape, and delegation to `countUnreadForAdmin`. All tests must FAIL (import will fail or assertions will fail) before Wave 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/36-admin-sidebar-badges/36-CONTEXT.md
@.planning/phases/36-admin-sidebar-badges/36-RESEARCH.md
@.planning/phases/36-admin-sidebar-badges/36-PATTERNS.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write failing unit tests for getAdminSidebarCounts()</name>
  <files>src/server/services/admin-sidebar.service.test.ts</files>

  <read_first>
    - src/server/services/callback-request.service.test.ts — vi.mock pattern with import-after-mock ordering; beforeEach vi.clearAllMocks(); describe/it structure
    - src/server/services/chat.service.test.ts — conversation.fields mock required for countUnreadForAdmin(); exact mock shape needed
    - src/server/services/admin-order.service.test.ts — if it exists, for additional Vitest patterns in this codebase
  </read_first>

  <behavior>
    - Test 1 (shape): `getAdminSidebarCounts()` returns an object with keys `categories`, `products`, `pendingOrders`, `unreadChats`, `unresolvedCallbacks` — all numbers
    - Test 2 (categories): `prisma.category.count()` called with no `where` argument (total count, D-03)
    - Test 3 (products): `prisma.product.count()` called with no `where` argument (total count, D-03)
    - Test 4 (pendingOrders): `prisma.order.count()` called with `{ where: { status: "PENDING" } }` (D-01 — PENDING only)
    - Test 5 (unreadChats): `countUnreadForAdmin()` from chat.service is called; its return value maps to `unreadChats` in result
    - Test 6 (unresolvedCallbacks): `prisma.callbackRequest.count()` called with `{ where: { status: "PENDING", archivedAt: null } }` (D-05)
    - Test 7 (parallel execution): all five queries run concurrently — mock all returning different values, assert result maps each correctly
  </behavior>

  <action>
    Create `src/server/services/admin-sidebar.service.test.ts`.

    Structure:
    1. `vi.mock("@/lib/db", ...)` BEFORE any imports — mock must include: `category.count`, `product.count`, `order.count`, `callbackRequest.count`, `conversation.fields.adminLastReadAt` (as string "adminLastReadAt"), `conversation.count`. The `conversation.fields` entry is required because `countUnreadForAdmin` uses `prisma.conversation.fields.adminLastReadAt` as a field reference.
    2. `vi.mock("@/server/services/chat.service", ...)` mocking `countUnreadForAdmin` as `vi.fn()` — because the real implementation uses `prisma.conversation.fields.adminLastReadAt` which is complex to thread through the DB mock.
    3. Imports: `import { prisma } from "@/lib/db"` and `import { countUnreadForAdmin } from "@/server/services/chat.service"` and `import { getAdminSidebarCounts } from "./admin-sidebar.service"`.
    4. `beforeEach(() => { vi.clearAllMocks(); })` to reset mock call counts between tests.
    5. Each test: call `vi.mocked(prisma.category.count).mockResolvedValue(N)` (different N per model), set `vi.mocked(countUnreadForAdmin).mockResolvedValue(M)`, call `await getAdminSidebarCounts()`, assert return value fields.
    6. For filter tests: use `expect(vi.mocked(prisma.order.count)).toHaveBeenCalledWith({ where: { status: "PENDING" } })` and `expect(vi.mocked(prisma.callbackRequest.count)).toHaveBeenCalledWith({ where: { status: "PENDING", archivedAt: null } })`.
    7. For no-filter tests: `expect(vi.mocked(prisma.category.count)).toHaveBeenCalledWith()` (no args) or `toHaveBeenCalledWith(undefined)` — use whichever matches the Vitest behavior for zero-arg calls in this codebase (check callback-request.service.test.ts for precedent).

    File must import from `"./admin-sidebar.service"` which does not yet exist — this causes the import to fail, satisfying the RED requirement.
  </action>

  <verify>
    <automated>npx vitest run src/server/services/admin-sidebar.service.test.ts 2>&1 | head -30</automated>
  </verify>

  <acceptance_criteria>
    - File `src/server/services/admin-sidebar.service.test.ts` exists
    - Running the test command exits non-zero (RED — import fails or assertions fail because service doesn't exist yet)
    - The test file contains at least 6 `it(` or `test(` blocks
    - The mock includes `conversation: { fields: { adminLastReadAt: "adminLastReadAt" }, count: vi.fn() }` (required for countUnreadForAdmin)
    - The mock for `callbackRequest.count` is set up with `{ where: { status: "PENDING", archivedAt: null } }` assertion in at least one test
    - The mock for `order.count` is set up with `{ where: { status: "PENDING" } }` assertion in at least one test
  </acceptance_criteria>

  <done>Test file exists; vitest exits non-zero (RED state confirmed); all 6+ test cases present covering shape, filter correctness for each of the 5 count queries</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → service module | Test file imports not-yet-existing service; import failure is expected and correct in Wave 0 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase |
| T-36-01 | Information Disclosure | test file | accept | Test file is never deployed to production; contains only mock values |
</threat_model>

<verification>
1. `npx vitest run src/server/services/admin-sidebar.service.test.ts` exits non-zero (RED)
2. Test file references `"./admin-sidebar.service"` import (which does not exist yet)
3. Test file contains mocks for all five Prisma models plus `countUnreadForAdmin`
</verification>

<success_criteria>
- Test scaffold in place covering all ADM-NAV-01 filter rules (D-01, D-03, D-05)
- RED state confirmed (tests fail because service not yet created)
- Wave 1 can proceed: implementation driven by these tests going GREEN
</success_criteria>

<output>
Create `.planning/phases/36-admin-sidebar-badges/36-01-SUMMARY.md` when done
</output>
