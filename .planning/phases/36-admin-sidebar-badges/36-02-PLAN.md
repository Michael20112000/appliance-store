---
phase: 36-admin-sidebar-badges
plan: 02
type: execute
wave: 1
depends_on:
  - 36-01
files_modified:
  - src/server/services/admin-sidebar.service.ts
autonomous: true
requirements:
  - ADM-NAV-01

must_haves:
  truths:
    - "getAdminSidebarCounts() exists and returns AdminSidebarBadgeCounts with all five numeric fields"
    - "AdminSidebarBadgeCounts type is exported from admin-sidebar.service.ts for use in shell and sidebar"
    - "pendingOrders count uses prisma.order.count with status=PENDING only (D-01)"
    - "unresolvedCallbacks count uses prisma.callbackRequest.count with status=PENDING AND archivedAt=null (D-05)"
    - "categories and products counts are total (no where clause, D-03)"
    - "unreadChats count delegates to countUnreadForAdmin() from chat.service.ts (D-08)"
    - "All five queries run concurrently via Promise.all (D-06, no N+1)"
    - "All Wave 0 tests pass GREEN"
  artifacts:
    - path: "src/server/services/admin-sidebar.service.ts"
      provides: "getAdminSidebarCounts() aggregated service function + AdminSidebarBadgeCounts type"
      exports: ["getAdminSidebarCounts", "AdminSidebarBadgeCounts"]
  key_links:
    - from: "src/server/services/admin-sidebar.service.ts"
      to: "src/server/services/chat.service.ts"
      via: "import { countUnreadForAdmin }"
      pattern: "countUnreadForAdmin"
    - from: "src/server/services/admin-sidebar.service.ts"
      to: "@/lib/db"
      via: "import { prisma }"
      pattern: "prisma\\.(category|product|order|callbackRequest)\\.count"
---

<objective>
Implement `admin-sidebar.service.ts` making all Wave 0 tests go GREEN.

Purpose: Provide the single aggregated server-side function that fetches all five sidebar badge counts in parallel — zero N+1 calls, correct filters per locked decisions D-01, D-03, D-05, D-06, D-08.

Output: `src/server/services/admin-sidebar.service.ts` with `export type AdminSidebarBadgeCounts` and `export async function getAdminSidebarCounts(): Promise<AdminSidebarBadgeCounts>` making `npx vitest run admin-sidebar.service.test.ts` exit 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/36-admin-sidebar-badges/36-CONTEXT.md
@.planning/phases/36-admin-sidebar-badges/36-RESEARCH.md
@.planning/phases/36-admin-sidebar-badges/36-PATTERNS.md
@.planning/phases/36-admin-sidebar-badges/36-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement admin-sidebar.service.ts (GREEN)</name>
  <files>src/server/services/admin-sidebar.service.ts</files>

  <read_first>
    - src/server/services/admin-sidebar.service.test.ts — the failing tests from Wave 0; implement exactly what these tests expect
    - src/server/services/callback-request.service.ts — lines 84-93: Promise.all parallel count pattern + archivedAt: null filter to replicate
    - src/server/services/admin-order.service.ts — lines 134-153: Promise.all with status: "PENDING" filter pattern to replicate
    - src/server/services/chat.service.ts — countUnreadForAdmin() signature and exports to import correctly
    - prisma/schema.prisma — OrderStatus enum and CallbackRequestStatus enum values, CallbackRequest.archivedAt field type
  </read_first>

  <behavior>
    - getAdminSidebarCounts() returns AdminSidebarBadgeCounts with: categories (total category count), products (total product count), pendingOrders (orders with status=PENDING), unreadChats (from countUnreadForAdmin()), unresolvedCallbacks (callbackRequests with status=PENDING AND archivedAt=null)
    - Promise.all runs all five count queries concurrently (not sequentially)
    - AdminSidebarBadgeCounts type has exactly these five numeric fields: categories, products, pendingOrders, unreadChats, unresolvedCallbacks
    - Both type and function are exported (Wave 2 components import the type)
  </behavior>

  <action>
    Create `src/server/services/admin-sidebar.service.ts` with this structure:

    Imports: `import { prisma } from "@/lib/db"` and `import { countUnreadForAdmin } from "@/server/services/chat.service"`.

    Export the type `AdminSidebarBadgeCounts` with fields: categories, products, pendingOrders, unreadChats, unresolvedCallbacks — all `number`.

    Export async function `getAdminSidebarCounts(): Promise<AdminSidebarBadgeCounts>` that:
    - Calls `Promise.all([...])` with five parallel items:
      1. `prisma.category.count()` — no where clause (D-03)
      2. `prisma.product.count()` — no where clause (D-03)
      3. `prisma.order.count({ where: { status: "PENDING" } })` — PENDING only per D-01
      4. `countUnreadForAdmin()` — delegate to chat.service per D-08
      5. `prisma.callbackRequest.count({ where: { status: "PENDING", archivedAt: null } })` — both conditions required per D-05
    - Destructures result as `[categories, products, pendingOrders, unreadChats, unresolvedCallbacks]`
    - Returns the object `{ categories, products, pendingOrders, unreadChats, unresolvedCallbacks }`

    Use `"PENDING"` string literal for both status filters (verify exact enum string values from prisma/schema.prisma before writing — use the same string already used in admin-order.service.ts lines 134-153 to ensure consistency).

    Do NOT add try/catch — follow the pattern in callback-request.service.ts and admin-order.service.ts which let Prisma errors propagate to the caller.
  </action>

  <verify>
    <automated>npx vitest run src/server/services/admin-sidebar.service.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - File `src/server/services/admin-sidebar.service.ts` exists
    - `npx vitest run src/server/services/admin-sidebar.service.test.ts` exits 0 (all tests GREEN)
    - File contains `export type AdminSidebarBadgeCounts`
    - File contains `export async function getAdminSidebarCounts(`
    - File contains `Promise.all([` (parallel execution pattern, D-06)
    - File contains `status: "PENDING", archivedAt: null` (D-05 exact filter)
    - File imports `countUnreadForAdmin` from `"@/server/services/chat.service"` (D-08)
    - `npm test` exits 0 after implementation (full suite green)
  </acceptance_criteria>

  <done>Service file created; all Wave 0 unit tests pass; npm test is green; type exported for Wave 2 consumption</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC layout → service | getAdminSidebarCounts() is only called from admin layout.tsx which is already behind requireAdmin(); service itself has no auth guard by design |
| Service → Prisma | Read-only count queries; no user input; no injection risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-02 | Information Disclosure | getAdminSidebarCounts() | mitigate | Function is never exported as API route; only called server-side in RSC layout behind requireAdmin() — no direct access path from client |
| T-36-03 | Elevation of Privilege | admin-sidebar.service.ts | accept | Service performs read-only COUNT queries; no write operations; requireAdmin() gate in layout.tsx enforces access control before this service is invoked |
| T-36-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase |
</threat_model>

<verification>
1. `npx vitest run src/server/services/admin-sidebar.service.test.ts` exits 0
2. `npm test` exits 0
3. grep `export type AdminSidebarBadgeCounts` admin-sidebar.service.ts returns 1 match
4. grep `export async function getAdminSidebarCounts` admin-sidebar.service.ts returns 1 match
5. grep `archivedAt: null` admin-sidebar.service.ts returns 1 match
</verification>

<success_criteria>
- All Wave 0 tests GREEN
- Service exports both `AdminSidebarBadgeCounts` type and `getAdminSidebarCounts` function
- Full test suite passes
- Ready for Wave 2: props refactor and badge render
</success_criteria>

<output>
Create `.planning/phases/36-admin-sidebar-badges/36-02-SUMMARY.md` when done
</output>
