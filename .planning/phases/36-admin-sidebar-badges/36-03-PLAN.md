---
phase: 36-admin-sidebar-badges
plan: 03
type: execute
wave: 2
depends_on:
  - 36-02
files_modified:
  - src/app/(admin)/admin/layout.tsx
  - src/components/admin/admin-sidebar-shell.tsx
  - src/components/admin/app-sidebar.tsx
autonomous: false
requirements:
  - ADM-NAV-01

must_haves:
  truths:
    - "Admin layout.tsx calls getAdminSidebarCounts() instead of countUnreadForAdmin(); passes badgeCounts to AdminSidebarShell"
    - "AdminSidebarShell accepts badgeCounts: AdminSidebarBadgeCounts prop (not unreadChatCount: number); passes it to AppSidebar"
    - "AppSidebar accepts badgeCounts: AdminSidebarBadgeCounts prop; renders SidebarMenuBadge on all five nav items"
    - "Categories and products badges use bg-muted text-muted-foreground className (D-04 — secondary/muted, not destructive)"
    - "Orders, chats, callbacks badges use bg-destructive text-destructive-foreground className"
    - "All badges hidden when count = 0 (D-02)"
    - "All badges cap at 99+ when count > 99"
    - "Old unreadChatCount prop removed from all three files"
    - "Human verifier confirms badges visible and correct styling in browser"
  artifacts:
    - path: "src/app/(admin)/admin/layout.tsx"
      provides: "RSC fetch of badgeCounts; passes to AdminSidebarShell"
      contains: "getAdminSidebarCounts"
    - path: "src/components/admin/admin-sidebar-shell.tsx"
      provides: "Props bridge — passes badgeCounts: AdminSidebarBadgeCounts to AppSidebar"
      contains: "badgeCounts: AdminSidebarBadgeCounts"
    - path: "src/components/admin/app-sidebar.tsx"
      provides: "Five badge renders with correct styling and conditional visibility"
      contains: "badgeConfig"
  key_links:
    - from: "layout.tsx"
      to: "AdminSidebarShell"
      via: "badgeCounts={badgeCounts} prop"
      pattern: "badgeCounts=\\{badgeCounts\\}"
    - from: "AdminSidebarShell"
      to: "AppSidebar"
      via: "badgeCounts={badgeCounts} prop passthrough"
      pattern: "badgeCounts=\\{badgeCounts\\}"
    - from: "AppSidebar"
      to: "SidebarMenuBadge"
      via: "badgeConfig map + conditional render"
      pattern: "SidebarMenuBadge"
---

<objective>
Wire the sidebar badge pipeline: update layout.tsx → AdminSidebarShell → AppSidebar props chain from the old `unreadChatCount: number` to `badgeCounts: AdminSidebarBadgeCounts`, then render five badge-eligible nav items with correct styling and conditional visibility. Human verify confirms badges are visible in browser.

Purpose: Close ADM-NAV-01 — operator sees attention signals on all five nav items without N+1 fetch.

Output: Three modified files completing the badge display pipeline; checkpoint confirms visual correctness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/36-admin-sidebar-badges/36-CONTEXT.md
@.planning/phases/36-admin-sidebar-badges/36-RESEARCH.md
@.planning/phases/36-admin-sidebar-badges/36-PATTERNS.md
@.planning/phases/36-admin-sidebar-badges/36-02-SUMMARY.md
</context>

<interfaces>
From src/server/services/admin-sidebar.service.ts (created in Wave 1):

```typescript
export type AdminSidebarBadgeCounts = {
  categories: number;
  products: number;
  pendingOrders: number;
  unreadChats: number;
  unresolvedCallbacks: number;
};

export async function getAdminSidebarCounts(): Promise<AdminSidebarBadgeCounts>;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Refactor layout.tsx and AdminSidebarShell props chain</name>
  <files>src/app/(admin)/admin/layout.tsx, src/components/admin/admin-sidebar-shell.tsx</files>

  <read_first>
    - src/app/(admin)/admin/layout.tsx — read the full file: current import of countUnreadForAdmin, requireAdmin() call on line 17, const unreadChatCount assignment, AdminSidebarShell JSX with unreadChatCount prop
    - src/components/admin/admin-sidebar-shell.tsx — read the full file: current AdminSidebarShellProps type (lines 13-16), current unreadChatCount prop destructure, AppSidebar JSX prop (line 30)
    - src/server/services/admin-sidebar.service.ts — confirm export names: getAdminSidebarCounts and AdminSidebarBadgeCounts
  </read_first>

  <action>
    In `src/app/(admin)/admin/layout.tsx`:
    - Remove import `{ countUnreadForAdmin }` from `"@/server/services/chat.service"`
    - Add import `{ getAdminSidebarCounts }` from `"@/server/services/admin-sidebar.service"`
    - Replace `const unreadChatCount = await countUnreadForAdmin()` with `const badgeCounts = await getAdminSidebarCounts()`
    - Replace `<AdminSidebarShell unreadChatCount={unreadChatCount}>` with `<AdminSidebarShell badgeCounts={badgeCounts}>`
    - Do NOT change requireAdmin() call — it stays on the line before the fetch (auth gate is untouched per RESEARCH.md security domain)

    In `src/components/admin/admin-sidebar-shell.tsx`:
    - Add import `import type { AdminSidebarBadgeCounts } from "@/server/services/admin-sidebar.service"`
    - Replace the `AdminSidebarShellProps` type: remove `unreadChatCount: number`, add `badgeCounts: AdminSidebarBadgeCounts` (keep `children: React.ReactNode`)
    - Update props destructure in the function signature to use `badgeCounts` instead of `unreadChatCount`
    - Replace `<AppSidebar unreadChatCount={unreadChatCount} />` with `<AppSidebar badgeCounts={badgeCounts} />`
    - Ensure no reference to `unreadChatCount` remains in the file
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "admin/layout|admin-sidebar-shell" | head -20</automated>
  </verify>

  <acceptance_criteria>
    - `src/app/(admin)/admin/layout.tsx` contains `import { getAdminSidebarCounts }` from admin-sidebar.service
    - `src/app/(admin)/admin/layout.tsx` does NOT contain `countUnreadForAdmin` (removed)
    - `src/app/(admin)/admin/layout.tsx` contains `badgeCounts={badgeCounts}` in AdminSidebarShell JSX
    - `src/components/admin/admin-sidebar-shell.tsx` contains `badgeCounts: AdminSidebarBadgeCounts` in its props type
    - `src/components/admin/admin-sidebar-shell.tsx` does NOT contain `unreadChatCount` anywhere
    - `npx tsc --noEmit` produces zero errors in layout.tsx and admin-sidebar-shell.tsx (grep for those filenames in output returns no lines)
  </acceptance_criteria>

  <done>layout.tsx and admin-sidebar-shell.tsx updated; TypeScript compiles cleanly for both files; props chain broken ends are now fixed by Task 2 completing app-sidebar.tsx</done>
</task>

<task type="auto">
  <name>Task 2: Render five badge-eligible nav items in AppSidebar</name>
  <files>src/components/admin/app-sidebar.tsx</files>

  <read_first>
    - src/components/admin/app-sidebar.tsx — read full file: current AppSidebarProps type (lines 25-28), current chatBadgeLabel computation (lines 38-39), existing isChat/showChatBadge logic (lines 67-91), SidebarMenuBadge import (line 18), full map() structure to understand where to insert badge logic
    - src/components/admin/admin-nav-items.ts — read to confirm exact href values for all nav items: /admin/kategorii, /admin/tovary, /admin/zamovlennia, /admin/chaty, /admin/dzvinky
  </read_first>

  <action>
    In `src/components/admin/app-sidebar.tsx`:

    1. Add import: `import type { AdminSidebarBadgeCounts } from "@/server/services/admin-sidebar.service"` (if not already present after Task 1 propagation).

    2. Replace `AppSidebarProps` type: remove `unreadChatCount: number`, add `badgeCounts: AdminSidebarBadgeCounts`.

    3. Remove the `chatBadgeLabel` constant (lines ~38-39) and `isChat`/`showChatBadge` constants from inside the map — these will be replaced by the badgeConfig approach.

    4. Above the `.map()` call (before the return/JSX), define a `badgeConfig` object of type `Record<string, { count: number; destructive: boolean } | undefined>` mapping each nav href to its count and style:
       - `"/admin/kategorii"`: count from `badgeCounts.categories`, destructive: false (D-04)
       - `"/admin/tovary"`: count from `badgeCounts.products`, destructive: false (D-04)
       - `"/admin/zamovlennia"`: count from `badgeCounts.pendingOrders`, destructive: true
       - `"/admin/chaty"`: count from `badgeCounts.unreadChats`, destructive: true
       - `"/admin/dzvinky"`: count from `badgeCounts.unresolvedCallbacks`, destructive: true

    5. Inside the `.map()` callback, add these derived variables per item:
       - `const badge = badgeConfig[item.href]`
       - `const showBadge = badge !== undefined && badge.count > 0` (D-02 — hidden at 0)
       - `const badgeLabel = badge && badge.count > 99 ? "99+" : String(badge?.count ?? 0)` (cap at 99+)

    6. Update the `aria-label` on the SidebarMenuButton to include badge context when showBadge is true. Follow the existing chat pattern `"${item.label}, ${badge.count} ..."` using appropriate Ukrainian phrases:
       - kategorii: `"Категорії, ${badge.count} всього"`
       - tovary: `"Товари, ${badge.count} всього"`
       - zamovlennia: `"Замовлення, ${badge.count} висячих"`
       - chaty: `"Чати, ${badge.count} непрочитаних"` (existing pattern, keep it)
       - dzvinky: `"Дзвінки, ${badge.count} невирішених"`
       Use the generic `showBadge ? \`${item.label}, ${badge.count} ...\` : item.label` pattern or per-href aria-label switch — executor discretion.

    7. Replace the existing `{showChatBadge ? <SidebarMenuBadge ...> ...}` block with a unified conditional render:
       ```
       {showBadge ? (
         <SidebarMenuBadge
           className={badge.destructive
             ? "bg-destructive text-destructive-foreground"
             : "bg-muted text-muted-foreground"}
         >
           {badgeLabel}
         </SidebarMenuBadge>
       ) : null}
       ```

    8. Ensure NO remaining references to `unreadChatCount` exist in the file.

    Exact className strings to use:
    - Destructive badges (orders, chats, callbacks): `"bg-destructive text-destructive-foreground"`
    - Secondary badges (categories, products, D-04): `"bg-muted text-muted-foreground"`
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30 && npx vitest run src/server/services/admin-sidebar.service.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `src/components/admin/app-sidebar.tsx` contains `badgeCounts: AdminSidebarBadgeCounts` in props type
    - `src/components/admin/app-sidebar.tsx` does NOT contain `unreadChatCount` anywhere
    - `src/components/admin/app-sidebar.tsx` contains `badgeConfig` object with all five nav hrefs as keys
    - `src/components/admin/app-sidebar.tsx` contains `"bg-muted text-muted-foreground"` (secondary badge className, D-04)
    - `src/components/admin/app-sidebar.tsx` contains `"bg-destructive text-destructive-foreground"` (destructive badge className)
    - `src/components/admin/app-sidebar.tsx` contains `badge.count > 99 ? "99+"` (cap logic)
    - `src/components/admin/app-sidebar.tsx` contains `badge.count > 0` or `showBadge` condition (D-02 hidden at 0)
    - `npx tsc --noEmit` exits 0 (no TypeScript errors across all three refactored files)
    - `npx vitest run src/server/services/admin-sidebar.service.test.ts` exits 0
    - `npm test` exits 0
  </acceptance_criteria>

  <done>AppSidebar renders five badge-eligible nav items; TypeScript clean; test suite green</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Three files wired: layout.tsx calls getAdminSidebarCounts() and passes badgeCounts to AdminSidebarShell which passes to AppSidebar. AppSidebar renders five conditional SidebarMenuBadge components — categories and products with muted styling, orders/chats/callbacks with destructive styling. All hidden when count = 0.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000/admin in your browser.
    2. Check the sidebar — you should see badge counts on nav items that have data:
       - Категорії — grey/muted badge with total category count (e.g. "5")
       - Товари — grey/muted badge with total product count (e.g. "12")
       - Замовлення — red/destructive badge with PENDING order count (0 = no badge)
       - Чати — red/destructive badge with unread chat count (0 = no badge)
       - Дзвінки — red/destructive badge with unresolved callback count (0 = no badge)
    3. Confirm: categories and products badges look GREY/MUTED (not red).
    4. Confirm: orders/chats/callbacks badges (when non-zero) look RED/DESTRUCTIVE.
    5. Confirm: any section with count=0 shows NO badge at all.
    6. Collapse the sidebar to icon-only mode — confirm badges disappear (expected shadcn behavior).
    7. Confirm the browser console has no errors.
  </how-to-verify>
  <resume-signal>Type "approved" if all badges display correctly, or describe any visual issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC layout → client components | badgeCounts plain object crosses RSC→client serialization boundary via AdminSidebarShell props |
| Browser → visible badge counts | Operator-only page; counts reflect DB state readable by any authenticated admin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-04 | Information Disclosure | AppSidebar badge values | accept | Admin sidebar is only rendered behind requireAdmin() in RSC layout; badge counts (number of pending orders etc.) are not sensitive — operator is expected to know these counts |
| T-36-05 | Spoofing | badgeCounts prop | accept | No user input flows into badge counts; all values computed server-side from Prisma queries in RSC; client component only receives plain numbers via props |
| T-36-06 | Tampering | admin-sidebar-shell.tsx prop passthrough | accept | Props are serialized plain numbers crossing RSC→client boundary; React enforces prop immutability at render time |
| T-36-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase |
</threat_model>

<verification>
1. `npx tsc --noEmit` exits 0 (all three files type-safe)
2. `npm test` exits 0 (full suite green)
3. `grep -r "unreadChatCount" src/app/\(admin\)/admin/layout.tsx src/components/admin/admin-sidebar-shell.tsx src/components/admin/app-sidebar.tsx` returns no matches
4. Human verifier confirms badges visible with correct styling in browser
</verification>

<success_criteria>
- All five nav items receive badge counts from a single aggregated fetch (no N+1)
- Categories and products: grey/muted badge, hidden at 0
- Orders: red badge showing PENDING count only, hidden at 0
- Chats: red badge showing unread count, hidden at 0
- Callbacks: red badge showing PENDING+non-archived count, hidden at 0
- TypeScript clean, test suite green
- Human verify checkpoint approved
- ADM-NAV-01 requirement COMPLETE
</success_criteria>

<output>
Create `.planning/phases/36-admin-sidebar-badges/36-03-SUMMARY.md` when done
</output>
