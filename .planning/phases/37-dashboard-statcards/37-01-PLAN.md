---
phase: 37-dashboard-statcards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(admin)/admin/page.tsx
autonomous: true
requirements:
  - ADM-DASH-05
  - ADM-DASH-06

must_haves:
  truths:
    - "Admin sees a StatCard labelled «Нові дзвінки» on /admin with a count sourced from unresolvedCallbacks"
    - "Admin sees a StatCard labelled «Активні чати» on /admin with a count sourced from unreadChats"
    - "Both new StatCards are clickable links (href provided) matching the nav destinations /admin/dzvinky and /admin/chaty"
    - "The five StatCards render in the existing responsive grid (sm:grid-cols-2 lg:grid-cols-3) without any grid class change"
    - "Page data fetch uses a single Promise.all with three calls — no sequential awaits and no duplicate Prisma queries"
  artifacts:
    - path: "src/app/(admin)/admin/page.tsx"
      provides: "Extended RSC dashboard page with 5 StatCards"
      contains: "getAdminSidebarCounts"
  key_links:
    - from: "src/app/(admin)/admin/page.tsx"
      to: "src/server/services/admin-sidebar.service.ts"
      via: "getAdminSidebarCounts() in Promise.all"
      pattern: "getAdminSidebarCounts"
    - from: "src/app/(admin)/admin/page.tsx"
      to: "src/components/admin/stat-card.tsx"
      via: "<StatCard> with sidebarCounts fields"
      pattern: "sidebarCounts\\.unresolvedCallbacks|sidebarCounts\\.unreadChats"
---

<objective>
Extend the admin dashboard RSC page with two new StatCards — «Нові дзвінки» (ADM-DASH-05) and «Активні чати» (ADM-DASH-06) — by adding getAdminSidebarCounts() to the existing Promise.all and appending two <StatCard> nodes to the existing grid.

Purpose: Admins can see pending call and unread chat counts at a glance on the main dashboard, consistent with sidebar badge data.
Output: src/app/(admin)/admin/page.tsx with 5 StatCards and a 3-element Promise.all.
</objective>

<execution_context>
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/PROJECT.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/ROADMAP.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/STATE.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/admin/stat-card.tsx:
type StatCardProps = {
  label: string;
  count: number;
  href?: string;
  className?: string;
  icon?: React.ElementType;  // pass component reference: icon={Phone}, NOT icon={<Phone />}
};

From src/server/services/admin-sidebar.service.ts:
export type AdminSidebarBadgeCounts = {
  categories: number;
  products: number;
  pendingOrders: number;
  unreadChats: number;          // ADM-DASH-06
  unresolvedCallbacks: number;  // ADM-DASH-05
};
export function getAdminSidebarCounts(): Promise<AdminSidebarBadgeCounts>;

Current src/app/(admin)/admin/page.tsx Promise.all (lines 11-14):
  const [stats, analyticsPreview] = await Promise.all([
    getAdminDashboardStats(),
    getDashboardAnalyticsPreview(),
  ]);

Current lucide-react import (line 2):
  import { Eye, Package, PackageX, Plus, ShoppingBag } from "lucide-react";
  // Phone and MessageSquare are NOT yet imported in this file
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add two StatCards for calls and chats to the dashboard</name>
  <files>src/app/(admin)/admin/page.tsx</files>
  <read_first>
    - src/app/(admin)/admin/page.tsx — current file; executor must read this before making any changes
    - src/server/services/admin-sidebar.service.ts — confirms getAdminSidebarCounts signature and AdminSidebarBadgeCounts type
    - src/components/admin/stat-card.tsx — confirms StatCardProps (label, count, href, icon as React.ElementType)
    - .planning/phases/37-dashboard-statcards/37-PATTERNS.md — exact before/after diff for Promise.all and StatCard grid
  </read_first>
  <action>
    Modify src/app/(admin)/admin/page.tsx with three additive changes only — do not alter any existing code outside these three spots:

    1. Extend the lucide-react import on line 2: add Phone and MessageSquare so the full import reads:
       import { Eye, MessageSquare, Package, PackageX, Phone, Plus, ShoppingBag } from "lucide-react";

    2. Add a new import after the existing service imports (after line 8):
       import { getAdminSidebarCounts } from "@/server/services/admin-sidebar.service";

    3. Extend the Promise.all destructuring (lines 11-14): insert getAdminSidebarCounts() as the second element and shift analyticsPreview to index 2:
       const [stats, sidebarCounts, analyticsPreview] = await Promise.all([
         getAdminDashboardStats(),
         getAdminSidebarCounts(),
         getDashboardAnalyticsPreview(),
       ]);

    4. Append two <StatCard> nodes inside the existing grid div (after the PackageX StatCard, before the closing </div> on line 39):
       - label="Нові дзвінки", count={sidebarCounts.unresolvedCallbacks}, href="/admin/dzvinky", icon={Phone}
       - label="Активні чати", count={sidebarCounts.unreadChats}, href="/admin/chaty", icon={MessageSquare}

    Do NOT change the grid className — "grid gap-4 sm:grid-cols-2 lg:grid-cols-3" reflows naturally to 3+2 for 5 cards.
    Do NOT add new Prisma queries or a new service — reuse getAdminSidebarCounts() exclusively.
    Do NOT import from admin-nav-items.ts — icons must be imported directly from lucide-react in this file.
  </action>
  <verify>
    <automated>npx vitest run src/server/services/admin-sidebar.service.test.ts</automated>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(admin)/admin/page.tsx imports Phone and MessageSquare from lucide-react (on the same import line as Eye, Package, etc.)
    - src/app/(admin)/admin/page.tsx imports getAdminSidebarCounts from "@/server/services/admin-sidebar.service"
    - Promise.all array has exactly 3 elements: getAdminDashboardStats(), getAdminSidebarCounts(), getDashboardAnalyticsPreview()
    - Destructuring reads: const [stats, sidebarCounts, analyticsPreview]
    - Grid div contains exactly 5 <StatCard> nodes
    - Fourth StatCard has label="Нові дзвінки", count={sidebarCounts.unresolvedCallbacks}, href="/admin/dzvinky", icon={Phone}
    - Fifth StatCard has label="Активні чати", count={sidebarCounts.unreadChats}, href="/admin/chaty", icon={MessageSquare}
    - Grid className is unchanged: "grid gap-4 sm:grid-cols-2 lg:grid-cols-3"
    - npx vitest run src/server/services/admin-sidebar.service.test.ts passes (all tests green)
    - npx tsc --noEmit exits 0 (no TypeScript errors)
  </acceptance_criteria>
  <done>admin/page.tsx renders 5 StatCards; the two new cards link to /admin/dzvinky and /admin/chaty with counts from getAdminSidebarCounts(); TypeScript compiles clean; service tests pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>admin/page.tsx now fetches getAdminSidebarCounts() and renders «Нові дзвінки» and «Активні чати» StatCards in the existing grid.</what-built>
  <how-to-verify>
    1. Start the dev server: npm run dev
    2. Sign in as admin and navigate to http://localhost:3000/admin
    3. Verify the StatCard grid shows 5 cards total (3 in first row, 2 in second at lg breakpoint)
    4. Confirm «Нові дзвінки» card is visible with a numeric count and a Phone icon
    5. Confirm «Активні чати» card is visible with a numeric count and a MessageSquare icon
    6. Click «Нові дзвінки» — browser navigates to /admin/dzvinky
    7. Click «Активні чати» — browser navigates to /admin/chaty
    8. Confirm no console errors or layout breakage
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues observed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /admin (RSC) | Unauthenticated request crosses here; must not reach page handler |
| RSC page → Prisma (getAdminSidebarCounts) | Server-internal; data flows server-side only, never to client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Elevation of Privilege | GET /admin page | mitigate | `requireAdmin()` called in `src/app/(admin)/admin/layout.tsx` — all `/admin/*` routes require an authenticated admin session before the RSC page executes; no per-page guard needed |
| T-37-02 | Information Disclosure | StatCard count values | accept | Counts (unresolvedCallbacks, unreadChats) are admin-only operational data; the page is behind requireAdmin(); values are never rendered to unauthenticated users |
| T-37-03 | Denial of Service | Promise.all with 3 DB queries | accept | All three queries are cheap COUNT/aggregate queries against small tables; no user-controlled input affects query shape or scope |
| T-37-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase; attack surface is unchanged |
</threat_model>

<verification>
Run after plan execution:

1. npx vitest run src/server/services/admin-sidebar.service.test.ts — all tests pass
2. npx vitest run — full suite green
3. npx tsc --noEmit — exits 0
4. Manual: load /admin, confirm 5 StatCards render with correct labels, icons, counts, and hrefs
</verification>

<success_criteria>
- ADM-DASH-05: «Нові дзвінки» StatCard visible on /admin with count from unresolvedCallbacks and href=/admin/dzvinky
- ADM-DASH-06: «Активні чати» StatCard visible on /admin with count from unreadChats and href=/admin/chaty
- Both cards render in the existing grid alongside the three existing StatCards without layout regression
- getAdminSidebarCounts() is the sole data source for both new counts (no duplicate Prisma queries)
- TypeScript compiles clean; all existing tests pass
</success_criteria>

<output>
Create `.planning/phases/37-dashboard-statcards/37-01-SUMMARY.md` when done
</output>
