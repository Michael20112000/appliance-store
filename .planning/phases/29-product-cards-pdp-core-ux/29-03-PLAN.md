---
phase: 29-product-cards-pdp-core-ux
plan: 03
type: execute
wave: 2
depends_on:
  - 29-01
  - 29-02
files_modified:
  - src/components/cart/add-to-cart-button.tsx
  - src/components/cart/pdp-cart-fab.tsx
  - src/app/(storefront)/tovar/[slug]/page.tsx
autonomous: true
requirements:
  - PDP-06
must_haves:
  truths:
    - "D-10: In-cart primary label «Вже в кошику»"
    - "D-11: Trash icon-only remove beside primary"
    - "D-12: No «Перейти до кошика» link in AddToCartButton"
    - "D-13: At most two cart controls in in-cart row"
    - "D-14: Guest and session cart paths unchanged functionally"
    - "D-15: PdpCartFab on PDP only, links to /koszyk"
    - "D-16: FAB visible when cart count >= 1"
    - "D-17: Guest FAB uses getPendingItemCount + CART_CHANGED_EVENT"
    - "D-18: FAB fixed above ChatFab with safe-area"
    - "D-19: FAB navigates to /koszyk with ShoppingCart icon and numeric badge"
    - "D-20: OpenChatButton stays inline; global ChatFab unchanged"
    - "D-22: npm test + npm run build green; manual UAT per UI-SPEC"
  artifacts:
    - path: src/components/cart/pdp-cart-fab.tsx
      provides: PDP-only cart FAB
      min_lines: 40
    - path: src/components/cart/add-to-cart-button.tsx
      provides: In-cart two-control layout
      contains: "Вже в кошику"
  key_links:
    - from: src/app/(storefront)/tovar/[slug]/page.tsx
      to: src/components/cart/pdp-cart-fab.tsx
      via: mount with initialCount
      pattern: PdpCartFab
---

<objective>
Refactor PDP add-to-cart in-cart UI and add a PDP-only cart FAB stacked above the chat FAB (PDP-06).

Purpose: Clear in-cart state without button clutter; quick navigation to cart from PDP when items present.
Output: «Вже в кошику» + trash, no go-to-cart link in button group; `PdpCartFab` on product detail page only.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-product-cards-pdp-core-ux/29-CONTEXT.md
@.planning/phases/29-product-cards-pdp-core-ux/29-RESEARCH.md
@.planning/phases/29-product-cards-pdp-core-ux/29-UI-SPEC.md
@src/components/cart/add-to-cart-button.tsx
@src/components/chat/chat-fab.tsx
@src/lib/cart/pending-storage.ts
@src/lib/cart/cart-events.ts
@src/server/services/cart.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: AddToCartButton in-cart layout</name>
  <files>src/components/cart/add-to-cart-button.tsx</files>
  <read_first>
    src/components/cart/add-to-cart-button.tsx
    .planning/phases/29-product-cards-pdp-core-ux/29-UI-SPEC.md
    .planning/phases/29-product-cards-pdp-core-ux/29-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - In-cart branch shows disabled secondary Button with text «Вже в кошику» (not «У кошику»)
    - Adjacent Button size icon variant outline with Trash2 icon only
    - aria-label on trash: Прибрати {productTitle} з кошика
    - No Link or button text «Перейти до кошика» in file
    - Row uses flex items-center gap-2; at most two buttons in in-cart row
    - handleAdd and handleRemove logic unchanged
  </acceptance_criteria>
  <action>
    Refactor in-cart JSX per D-10-D-13. Remove Link to /koszyk and full-width «Прибрати з кошика» text button. Import Trash2 from lucide-react. Keep pending/session transitions and error display.
  </action>
  <verify>
    <automated>! grep -q 'Перейти до кошика' src/components/cart/add-to-cart-button.tsx && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>PDP cart controls match locked copy and layout.</done>
</task>

<task type="auto">
  <name>Task 2: PdpCartFab component</name>
  <files>src/components/cart/pdp-cart-fab.tsx</files>
  <read_first>
    src/components/chat/chat-fab.tsx
    src/lib/cart/pending-storage.ts
    src/lib/cart/cart-events.ts
    .planning/phases/29-product-cards-pdp-core-ux/29-UI-SPEC.md
  </read_first>
  <acceptance_criteria>
    - Client component PdpCartFab with props initialCount number, hasSession boolean
    - Returns null when count is 0
    - Link href=/koszyk, fixed right-6 bottom-[5.75rem] z-[59], size-14, primary styling like ChatFab
    - ShoppingCart icon; badge shows count or 9+ when count &gt; 9
    - Guest: useEffect listens CART_CHANGED_EVENT and updates count via getPendingItemCount
    - pb max safe-area-inset-bottom on fixed element
    - aria-label Перейти до кошика
  </acceptance_criteria>
  <action>
    Create pdp-cart-fab.tsx. Mirror ChatFab sizing; position above chat per UI-SPEC. Wire guest count from pending-storage; session relies on initialCount plus optional refresh after parent router.refresh from cart actions.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>FAB component ready for PDP mount.</done>
</task>

<task type="auto">
  <name>Task 3: Wire PDP page + phase gate</name>
  <files>src/app/(storefront)/tovar/[slug]/page.tsx</files>
  <read_first>
    src/app/(storefront)/tovar/[slug]/page.tsx
    src/server/services/cart.service.ts
    .planning/phases/29-product-cards-pdp-core-ux/29-VALIDATION.md
  </read_first>
  <acceptance_criteria>
    - page imports getCartItemCount when session user id present
    - cartCount passed to PdpCartFab as initialCount (0 for guest server render)
    - PdpCartFab rendered once per PDP view with hasSession prop
    - D-20: chat-fab.tsx and chat-provider.tsx not modified; OpenChatButton remains on PDP
    - D-22: manual UAT steps from 29-UI-SPEC.md documented in SUMMARY (hover, lightbox, FAB stack)
    - npm test and npm run build exit 0
  </acceptance_criteria>
  <action>
    In ProductDetailPage compute cartCount for signed-in users via getCartItemCount. Mount PdpCartFab at end of page content (fragment). Do not add FAB to layout or catalog. Do not edit chat-fab.tsx per D-20. Run full npm test and npm run build. Note manual UAT checklist in SUMMARY per D-22.
  </action>
  <verify>
    <automated>npm test && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>PDP-06 complete; manual UAT FAB stack and in-cart row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cart FAB → /koszyk | Fixed path only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Spoofing | PdpCartFab link | mitigate | href hardcoded /koszyk; no user input in URL |

</threat_model>

<verification>
- grep confirms no «Перейти до кошика» in add-to-cart-button
- Manual: add to cart → «Вже в кошику» + trash; FAB above chat with badge
- npm test + npm run build
</verification>
