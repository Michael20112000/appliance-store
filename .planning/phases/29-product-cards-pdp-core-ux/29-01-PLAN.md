---
phase: 29-product-cards-pdp-core-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/catalog.ts
  - src/server/services/catalog.service.ts
  - src/components/catalog/product-card.tsx
  - src/components/catalog/product-card-image-stack.tsx
  - src/components/wishlist/wishlist-grid.tsx
  - src/components/wishlist/wishlist-cabinet-preview.tsx
autonomous: true
requirements:
  - CARD-01
must_haves:
  truths:
    - "D-01: PublicProductCard includes up to 5 preview images from list query"
    - "D-02: Desktop hover crossfade ~3s; first image immediate on hover enter"
    - "D-03: Touch and mobile show first image only"
    - "D-04: prefers-reduced-motion disables rotation"
    - "D-05: Rotation pauses on mouseleave"
    - "D-06: Single-image products have no timer"
    - "D-21: Vitest for previewImages mapper when test file added"
  artifacts:
    - path: src/components/catalog/product-card-image-stack.tsx
      provides: Client hover crossfade stack
      min_lines: 60
    - path: src/server/services/catalog.service.ts
      provides: cardInclude take 5 + previewImages mapper
      contains: "take: 5"
  key_links:
    - from: src/components/catalog/product-card.tsx
      to: src/components/catalog/product-card-image-stack.tsx
      via: previewImages prop
      pattern: ProductCardImageStack
---

<objective>
Extend catalog list data with up to five preview images per product and implement desktop-only hover crossfade on product cards (CARD-01).

Purpose: Catalog cards feel alive on desktop without changing mobile behavior or PDP.
Output: `previewImages` on `PublicProductCard`, `ProductCardImageStack` client component, wishlist mappers updated for type compatibility.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/29-product-cards-pdp-core-ux/29-CONTEXT.md
@.planning/phases/29-product-cards-pdp-core-ux/29-RESEARCH.md
@.planning/phases/29-product-cards-pdp-core-ux/29-PATTERNS.md
@.planning/phases/29-product-cards-pdp-core-ux/29-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend PublicProductCard and catalog mapper</name>
  <files>src/types/catalog.ts, src/server/services/catalog.service.ts</files>
  <read_first>
    src/types/catalog.ts
    src/server/services/catalog.service.ts
    .planning/phases/29-product-cards-pdp-core-ux/29-CONTEXT.md
    .planning/phases/29-product-cards-pdp-core-ux/29-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - PublicProductCard has previewImages: Array of { cloudinaryPublicId, alt } (max 5 from DB)
    - image field remains first preview or null for backward compatibility
    - cardInclude.images uses take: 5, orderBy sortOrder asc
    - mapToCard sets previewImages from product.images and image = previewImages[0] ?? null
  </acceptance_criteria>
  <action>
    Add previewImages to PublicProductCard type. Change cardInclude take from 1 to 5. Update mapToCard to map all returned images to previewImages and set singular image to first element. Do not change PublicProductDetail or PDP query in this task.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>List API returns up to five images per card with stable types.</done>
</task>

<task type="auto">
  <name>Task 2: ProductCardImageStack hover crossfade</name>
  <files>src/components/catalog/product-card-image-stack.tsx, src/components/catalog/product-card.tsx</files>
  <read_first>
    src/components/catalog/product-card.tsx
    src/components/media/optimized-image.tsx
    .planning/phases/29-product-cards-pdp-core-ux/29-UI-SPEC.md
    .planning/phases/28-nav-homepage-catalog-labels/28-UI-SPEC.md
  </read_first>
  <acceptance_criteria>
    - New client component ProductCardImageStack accepts images[], alt, sizes
    - length &lt;= 1: single OptimizedImage, no setInterval
    - md+ hover fine pointer: mouseenter starts 3000ms interval; index 0 shown immediately on enter
    - opacity transition duration-300 between layers
    - mouseleave clears interval and resets to index 0
    - @media (prefers-reduced-motion: reduce) or max-md: no interval
    - ProductCard uses stack when product.previewImages.length &gt; 0 else placeholder path unchanged
  </acceptance_criteria>
  <action>
    Create product-card-image-stack.tsx with use client. Implement layered absolute images with opacity crossfade. Wire into product-card.tsx image area; pass product.previewImages and existing alt logic. Keep ConditionBadge z-10 above stack.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>Desktop hover rotates preview images per CARD-01; mobile static.</done>
</task>

<task type="auto">
  <name>Task 3: Wishlist mappers + tests</name>
  <files>src/components/wishlist/wishlist-grid.tsx, src/components/wishlist/wishlist-cabinet-preview.tsx</files>
  <read_first>
    src/components/wishlist/wishlist-grid.tsx
    src/components/wishlist/wishlist-cabinet-preview.tsx
    .planning/phases/29-product-cards-pdp-core-ux/29-VALIDATION.md
  </read_first>
  <acceptance_criteria>
    - toPublicProductCard sets previewImages to [image] when image present else []
    - TypeScript compiles for wishlist ProductCard usage
    - D-21: unit test asserts previewImages length &lt;= 5 on mapToCard (required)
    - npm test and npm run build exit 0
  </acceptance_criteria>
  <action>
    Update both wishlist toPublicProductCard helpers to include previewImages array. Add or extend catalog.service test for mapToCard previewImages per 29-PATTERNS analog. Run npm test and npm run build.
  </action>
  <verify>
    <automated>npm test && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>CARD-01 complete; wishlist cards still render.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Catalog API → card | Public product fields only; quantity gte 1 filter unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-SC | Tampering | npm installs | accept | No new dependencies |

</threat_model>

<verification>
- Desktop manual: multi-image card crossfade ~3s
- Mobile manual: static first image
- npm test + npm run build green
</verification>
