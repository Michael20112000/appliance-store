---
phase: 29-product-cards-pdp-core-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/catalog/product-gallery.tsx
autonomous: true
requirements:
  - PDP-05
must_haves:
  truths:
    - "D-07: Lightbox drag snaps to nearest slide without post-release jerk"
    - "D-08: loop true when multiple images in dialog carousel"
    - "D-09: Thumbnail strip carousel behavior unchanged unless regression"
  artifacts:
    - path: src/components/catalog/product-gallery.tsx
      provides: Tuned dialog Carousel opts
      contains: "dragFree: false"
  key_links:
    - from: src/components/catalog/product-gallery.tsx
      to: src/components/ui/carousel.tsx
      via: opts prop on dialog Carousel
      pattern: "duration:"
---

<objective>
Tune PDP lightbox Embla carousel for momentum drag with smooth snap on release, eliminating visible jerk (PDP-05).

Purpose: Mobile gallery feels native when swiping photos in the dialog.
Output: Updated `opts` on dialog `Carousel` only; thumbnail rail unchanged unless fix requires shared default.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-product-cards-pdp-core-ux/29-CONTEXT.md
@.planning/phases/29-product-cards-pdp-core-ux/29-RESEARCH.md
@.planning/phases/29-product-cards-pdp-core-ux/29-UI-SPEC.md
@src/components/catalog/product-gallery.tsx
@src/components/ui/carousel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lightbox Carousel opts</name>
  <files>src/components/catalog/product-gallery.tsx</files>
  <read_first>
    src/components/catalog/product-gallery.tsx
    src/components/ui/carousel.tsx
    .planning/phases/29-product-cards-pdp-core-ux/29-RESEARCH.md
    node_modules/embla-carousel/README.md
  </read_first>
  <acceptance_criteria>
    - Dialog Carousel opts include loop: hasMultiple, startIndex: dialogIndex
    - opts include duration: 25, dragFree: false, skipSnaps: false, containScroll: false (dialog only)
    - dialogApi.scrollTo on open uses jump false when already synced (avoid jerk on reopen)
    - Thumbnail Carousel opts remain align start, containScroll trimSnaps only
  </acceptance_criteria>
  <action>
    Update only the Dialog inner Carousel opts per RESEARCH. Review useEffect that calls scrollTo(dialogIndex, true) on open — prefer false for smooth alignment when index unchanged. Do not add new dependencies. Tune duration 20-30 if manual test still jerky.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>Lightbox configured for snap-after-drag.</done>
</task>

<task type="auto">
  <name>Task 2: Build gate + manual swipe note</name>
  <files>src/components/catalog/product-gallery.tsx</files>
  <read_first>
    .planning/phases/29-product-cards-pdp-core-ux/29-UI-SPEC.md
  </read_first>
  <acceptance_criteria>
    - npm run build exits 0
    - SUMMARY or plan notes manual check: mobile viewport, open lightbox, drag partial slide, release — lands on nearest slide without bounce jerk
  </acceptance_criteria>
  <action>
    Run npm run build. Document manual PDP-05 verification steps in execution summary for human UAT.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>PDP-05 ready for UAT.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Disposition |
|-----------|----------|-------------|
| T-29-SC | Tampering deps | accept — no new packages |

</threat_model>

<verification>
- Manual mobile lightbox swipe/release
- npm run build green
</verification>
