---
phase: 11-admin-list-row-ux
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - src/components/admin/admin-products-table.tsx
  - src/app/(admin)/admin/tovary/page.tsx
autonomous: true
requirements:
  - ADM-PRD-01
  - UX-02
context_decisions:
  - "D-11-07: Products keep existing row-click UX; refactor to shared helper only"
  - "D-11-09: Plus size-4 left of Додати товар"
  - "D-11-10: Same Button variant/size"
  - "D-11-12: ProductListStatusSelect stopPropagation unchanged"

must_haves:
  truths:
    - "Product rows still navigate to /admin/tovary/[id] on click and Enter/Space"
    - "Create CTA shows Plus icon size-4 left of label"
    - "Clicking status Select does not trigger row navigation"
  artifacts:
    - path: src/components/admin/admin-products-table.tsx
      provides: Refactored rows using shared helper
    - path: src/app/(admin)/admin/tovary/page.tsx
      provides: Plus on create Button
  key_links:
    - from: src/components/admin/admin-products-table.tsx
      to: src/lib/admin/use-admin-clickable-row.ts
      via: useAdminClickableRow per row
      pattern: "useAdminClickableRow"
    - from: src/components/admin/product-list-status-select.tsx
      to: row onClick
      via: stopPropagation on SelectTrigger
      pattern: "stopPropagation"

---

<objective>
Refactor products table to the shared clickable-row module (D-11-07) and add Plus to «Додати товар» (ADM-PRD-01, D-11-09, D-11-10) without changing end-user behavior.

Purpose: Validates helper on the canonical table before orders/categories adopt it.
Output: Updated admin-products-table.tsx and tovary/page.tsx CTA.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-admin-list-row-ux/11-01-SUMMARY.md
@.planning/phases/11-admin-list-row-ux/11-CONTEXT.md
@.planning/phases/11-admin-list-row-ux/11-UI-SPEC.md
@src/components/admin/admin-products-table.tsx
@src/components/admin/product-list-status-select.tsx
@src/app/(admin)/admin/tovary/page.tsx
@src/lib/admin/clickable-table-row.ts
@src/lib/admin/use-admin-clickable-row.ts

<interfaces>
AdminProductListItem — unchanged export from admin-products-table.tsx
ProductListStatusSelect — SelectTrigger has onClick={(e) => e.stopPropagation()} (do not remove)
Plus CTA pattern: import { Plus } from "lucide-react"; &lt;Plus className="size-4" aria-hidden /&gt; inside Button before label
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Row href | Built as `/admin/tovary/${product.id}` from server-provided items prop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | Elevation | nested Select in row | mitigate | Keep stopPropagation on ProductListStatusSelect (D-11-12); do not refactor select away |
| T-11-02-SC | Tampering | lucide-react Plus | accept | Already installed dependency |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Refactor AdminProductsTable to shared helper (D-11-07, D-11-08)</name>
  <read_first>
    - src/components/admin/admin-products-table.tsx — full file
    - src/lib/admin/clickable-table-row.ts — adminClickableRowClassName
    - src/lib/admin/use-admin-clickable-row.ts
    - 11-01-SUMMARY.md — helper API if present
  </read_first>
  <files>src/components/admin/admin-products-table.tsx</files>
  <action>
    Per D-11-07 refactor only — preserve UX:

    - Remove inline role/tabIndex/onClick/onKeyDown and duplicated hover classes from each tr.
    - Inside items.map: const rowNav = useAdminClickableRow(`/admin/tovary/${product.id}`) — call hook inside map is invalid; instead use getAdminClickableRowProps with useRouter once at component top OR call useAdminClickableRow in a small inner AdminProductRow client child. Preferred: single useRouter at top + getAdminClickableRowProps({ href, onNavigate: router.push }) per row to avoid rules-of-hooks violation.

    - Spread row props on tr; className={cn("border-b border-border last:border-0", adminClickableRowClassName)}.

    - Do NOT change columns, thumb rendering, or ProductListStatusSelect usage.
    - Do NOT add action column or inline order-style controls.
  </action>
  <verify>
    <automated>! grep -q 'role="link"' src/components/admin/admin-products-table.tsx || grep -q 'getAdminClickableRowProps\|useAdminClickableRow\|adminClickableRowClassName' src/components/admin/admin-products-table.tsx; npx vitest run src/lib/admin/clickable-table-row.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - No duplicated keyboard handler block (logic lives in lib)
    - adminClickableRowClassName used on body rows
    - ProductListStatusSelect still rendered in status cell
  </acceptance_criteria>
  <done>Products table uses shared contract</done>
</task>

<task type="auto">
  <name>Task 2: Plus icon on Додати товар (D-11-09, D-11-10, ADM-PRD-01)</name>
  <read_first>
    - src/app/(admin)/admin/tovary/page.tsx — Button lines 70–72
    - .planning/phases/11-admin-list-row-ux/11-UI-SPEC.md — Plus CTA spec
  </read_first>
  <files>src/app/(admin)/admin/tovary/page.tsx</files>
  <action>
    On tovary/page.tsx create CTA only (RSC stays server):

    - import Plus from lucide-react
    - Inside existing Button render={Link href="/admin/tovary/novyi"}: add Plus className="size-4" aria-hidden before text «Додати товар»
    - Keep default Button variant and size unchanged (D-11-10)
    - Do not add Plus to dashboard outline button (D-11-11 — out of scope)
  </action>
  <verify>
    <automated>grep -q 'Plus' src/app/\(admin\)/admin/tovary/page.tsx &amp;&amp; grep -q 'size-4' src/app/\(admin\)/admin/tovary/page.tsx &amp;&amp; grep -q 'Додати товар' src/app/\(admin\)/admin/tovary/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Plus imported and rendered left of label
    - Button variant/size unchanged from before
  </acceptance_criteria>
  <done>ADM-PRD-01 Plus CTA on products list</done>
</task>

</tasks>

<verification>
- npx vitest run src/lib/admin/clickable-table-row.test.ts
- Manual spot-check (D-11-15 subset): row click + status select no navigate — documented in 11-MANUAL-CHECKLIST.md plan 05
</verification>

<success_criteria>
- Products table behavior unchanged for operators; implementation centralized
- «Додати товар» shows Plus per success criteria 3 in ROADMAP
</success_criteria>

<output>
Create .planning/phases/11-admin-list-row-ux/11-02-SUMMARY.md when done
</output>
