---
phase: 11-admin-list-row-ux
plan: 03
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - src/components/admin/orders-data-table.tsx
autonomous: true
requirements:
  - ADM-ORD-01
  - UX-02
context_decisions:
  - "D-11-05: Remove actions column; row to /admin/zamovlennia/[orderNumber]"
  - "D-11-13: Status remains read-only OrderStatusBadge — no inline edit"
  - "D-11-08: Override shadcn TableRow hover with shared classes"

must_haves:
  truths:
    - "Orders list has no «Відкрити» column or actions id column"
    - "Clicking a body row navigates to order detail"
    - "Header sort Links unchanged; status cell is badge only"
  artifacts:
    - path: src/components/admin/orders-data-table.tsx
      provides: Row-click on TableRow without actions column
  key_links:
    - from: src/components/admin/orders-data-table.tsx
      to: /admin/zamovlennia/[orderNumber]
      via: getAdminClickableRowProps + router.push
      pattern: "zamovlennia/"
    - from: SortableHeader
      to: adminOrdersUrl
      via: Link in header only
      pattern: "SortableHeader"

---

<objective>
Orders admin table: remove «Відкрити» actions column and enable full-row navigation to order detail (ADM-ORD-01, D-11-05, D-11-13).

Purpose: Align orders with unified row-click pattern; prepare Phase 12 for status control with stopPropagation.
Output: Updated orders-data-table.tsx only.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-admin-list-row-ux/11-01-SUMMARY.md
@.planning/phases/11-admin-list-row-ux/11-CONTEXT.md
@.planning/phases/11-admin-list-row-ux/11-RESEARCH.md
@src/components/admin/orders-data-table.tsx
@src/components/ui/table.tsx
@src/lib/admin/clickable-table-row.ts
@src/lib/admin/orders-url.ts

<interfaces>
AdminOrderSummaryDto — from @/server/services/admin-order.service
OrdersDataTableProps — data, filter, page, pageSize, totalPages, sort, dir
Actions column to DELETE — id "actions" lines ~214–225
Detail href: `/admin/zamovlennia/${row.original.orderNumber}`
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orderNumber in href | Must come from row.original.orderNumber (server DTO), not URL input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | Spoofing | row href | mitigate | Template literal only with orderNumber from table data |
| T-11-03-02 | Tampering | sort header Link | accept | Header links remain; body rows separate — no bubble conflict in Phase 11 |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Remove actions column (D-11-05)</name>
  <read_first>
    - src/components/admin/orders-data-table.tsx — columns array id actions ~214–225
    - .planning/phases/11-admin-list-row-ux/11-CONTEXT.md — D-11-05, deferred Phase 12 inline status
  </read_first>
  <files>src/components/admin/orders-data-table.tsx</files>
  <action>
    Delete the entire column definition with id "actions" (header sr-only «Дії», cell Link «Відкрити»).

    Do NOT add replacement action column.
    Do NOT implement inline status Select (Phase 12 / ADM-ORD-02).
    Keep status column cell as OrderStatusBadge only (D-11-13).
  </action>
  <verify>
    <automated>! grep -q 'id: "actions"' src/components/admin/orders-data-table.tsx &amp;&amp; ! grep -q 'Відкрити' src/components/admin/orders-data-table.tsx</automated>
  </verify>
  <acceptance_criteria>
    - No actions column id in columns array
    - No «Відкрити» string in file
  </acceptance_criteria>
  <done>Actions column removed</done>
</task>

<task type="auto">
  <name>Task 2: Row-click on TableRow (D-11-01, D-11-08, UX-02)</name>
  <read_first>
    - src/components/admin/orders-data-table.tsx — TableBody map ~285–293
    - src/lib/admin/clickable-table-row.ts — getAdminClickableRowProps, adminClickableRowClassName
    - .planning/phases/11-admin-list-row-ux/11-RESEARCH.md — Pattern 3 TanStack row
  </read_first>
  <files>src/components/admin/orders-data-table.tsx</files>
  <action>
    Add useRouter from next/navigation at component top.

    In table.getRowModel().rows.map:
    - const href = `/admin/zamovlennia/${row.original.orderNumber}`
    - const rowProps = getAdminClickableRowProps({ href, onNavigate: (h) => router.push(h) })
    - Spread {...rowProps} on TableRow
    - className={cn("border-b border-border last:border-0", adminClickableRowClassName)} to override default hover:bg-muted/50 from ui/table

    Header TableRow: keep hover:bg-transparent — do not attach row navigation props to header rows.

    Do not attach row onClick to individual cells. Pagination AdminListPagination unchanged.
  </action>
  <verify>
    <automated>grep -q 'getAdminClickableRowProps' src/components/admin/orders-data-table.tsx &amp;&amp; grep -q 'adminClickableRowClassName' src/components/admin/orders-data-table.tsx &amp;&amp; grep -q 'zamovlennia/' src/components/admin/orders-data-table.tsx; npx vitest run src/lib/admin/clickable-table-row.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Body TableRow spreads shared props
    - href uses orderNumber from row.original
    - Shared hover/focus classes applied
  </acceptance_criteria>
  <done>ADM-ORD-01 row navigation on orders list</done>
</task>

</tasks>

<verification>
- grep confirms no actions column
- npx vitest run src/lib/admin/clickable-table-row.test.ts
- Manual: row opens detail (11-MANUAL-CHECKLIST.md)
</verification>

<success_criteria>
- ROADMAP criterion 1: orders open on row click; «Відкрити» absent
- Six data columns remain per 11-UI-SPEC
</success_criteria>

<output>
Create .planning/phases/11-admin-list-row-ux/11-03-SUMMARY.md when done
</output>
