---
phase: 11-admin-list-row-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/admin/clickable-table-row.ts
  - src/lib/admin/clickable-table-row.test.ts
  - src/lib/admin/use-admin-clickable-row.ts
autonomous: true
requirements:
  - UX-02
context_decisions:
  - "D-11-01: Canonical row-click contract (role=link, Enter/Space)"
  - "D-11-02: Shared getAdminClickableRowProps in lib/admin"
  - "D-11-08: adminClickableRowClassName hover/focus tokens"
  - "D-11-14: Vitest smoke on pure helper"

must_haves:
  truths:
    - "getAdminClickableRowProps returns role=link, tabIndex=0, and keyboard handlers"
    - "Enter and Space call onNavigate(href); Space calls preventDefault"
    - "adminClickableRowClassName exports D-11-08 cursor/hover/focus classes"
    - "useAdminClickableRow wires router.push without duplicating handler logic"
  artifacts:
    - path: src/lib/admin/clickable-table-row.ts
      provides: Pure props factory + className constant
      exports: ["adminClickableRowClassName", "getAdminClickableRowProps"]
    - path: src/lib/admin/clickable-table-row.test.ts
      provides: Unit tests (no DOM)
    - path: src/lib/admin/use-admin-clickable-row.ts
      provides: Client hook wrapping helper
      exports: ["useAdminClickableRow"]
  key_links:
    - from: src/lib/admin/use-admin-clickable-row.ts
      to: src/lib/admin/clickable-table-row.ts
      via: getAdminClickableRowProps
      pattern: "getAdminClickableRowProps"
    - from: src/lib/admin/clickable-table-row.test.ts
      to: src/lib/admin/clickable-table-row.ts
      via: import pure module only
      pattern: "clickable-table-row"

---

<objective>
Establish the shared admin clickable-row contract (D-11-01, D-11-02, D-11-08, D-11-14) so all four tables spread identical props and class tokens.

Purpose: Single a11y + navigation implementation; downstream plans only wire hrefs.
Output: `clickable-table-row.ts`, Vitest tests, `use-admin-clickable-row.ts`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-admin-list-row-ux/11-CONTEXT.md
@.planning/phases/11-admin-list-row-ux/11-RESEARCH.md
@.planning/phases/11-admin-list-row-ux/11-PATTERNS.md
@src/components/admin/admin-products-table.tsx
@src/lib/admin/products-url.test.ts
@vitest.config.ts

<interfaces>
From admin-products-table.tsx (canonical behavior to extract):
- role="link", tabIndex={0}
- onClick → navigate to href
- onKeyDown: Enter or Space → preventDefault + navigate
- Classes: cursor-pointer, transition-colors, hover:bg-muted/40, focus-visible:bg-muted/40, focus-visible:outline-none

Target signatures:
- getAdminClickableRowProps({ href: string; onNavigate: (href: string) => void })
- useAdminClickableRow(href: string) → ReturnType of getAdminClickableRowProps
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| href → onNavigate | Caller supplies path; helper must not read untrusted query strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Spoofing | getAdminClickableRowProps | mitigate | Callers build href only from server-known id/orderNumber — never from client query params inside helper |
| T-11-01-SC | Tampering | npm installs | accept | No new packages this plan |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Pure helper + className (D-11-02, D-11-08)</name>
  <read_first>
    - src/components/admin/admin-products-table.tsx — lines 55–69 (canonical handlers)
    - .planning/phases/11-admin-list-row-ux/11-RESEARCH.md — Pattern 1 target module
    - .planning/phases/11-admin-list-row-ux/11-CONTEXT.md — D-11-08
  </read_first>
  <files>src/lib/admin/clickable-table-row.ts</files>
  <action>
    Create src/lib/admin/clickable-table-row.ts (no "use client").

    Export adminClickableRowClassName exactly:
    cursor-pointer transition-colors hover:bg-muted/40 focus-visible:bg-muted/40 focus-visible:outline-none

    Export getAdminClickableRowProps({ href, onNavigate }) returning:
    - role: "link" as const
    - tabIndex: 0
    - onClick: () => onNavigate(href)
    - onKeyDown: if key is Enter or Space → event.preventDefault() then onNavigate(href); other keys no-op

    Do NOT import next/navigation here. Do NOT add open-redirect logic — helper only calls onNavigate with the href argument passed in.
    Border classes (border-b, last:border-0) stay at call sites per PATTERNS.md.
  </action>
  <verify>
    <automated>grep -q 'adminClickableRowClassName' src/lib/admin/clickable-table-row.ts &amp;&amp; grep -q 'getAdminClickableRowProps' src/lib/admin/clickable-table-row.ts &amp;&amp; ! grep -q '"use client"' src/lib/admin/clickable-table-row.ts</automated>
  </verify>
  <acceptance_criteria>
    - Pure module with no client directive
    - Class string matches D-11-08 tokens
    - Factory returns role link + tabIndex 0 + click/key handlers
  </acceptance_criteria>
  <done>clickable-table-row.ts committed with exports</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Vitest smoke tests (D-11-14)</name>
  <read_first>
    - src/lib/admin/products-url.test.ts — describe/it structure
    - vitest.config.ts — environment node, include src/**/*.test.ts
    - .planning/phases/11-admin-list-row-ux/11-VALIDATION.md — test cases table
  </read_first>
  <files>src/lib/admin/clickable-table-row.test.ts</files>
  <behavior>
    - Default props: role === "link", tabIndex === 0
    - onClick invokes onNavigate with href
    - onKeyDown Enter invokes onNavigate(href)
    - onKeyDown Space calls preventDefault and onNavigate(href)
    - onKeyDown other keys do not call onNavigate
  </behavior>
  <action>
    Add clickable-table-row.test.ts using vi.fn() for onNavigate and preventDefault.
    Use synthetic KeyboardEvent objects cast as needed (no RTL, no DOM per vitest node env).
    RED first if tests fail, then ensure implementation passes.
  </action>
  <verify>
    <automated>npx vitest run src/lib/admin/clickable-table-row.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All five behaviors above covered
    - Command exits 0
  </acceptance_criteria>
  <done>Vitest green for clickable-table-row.test.ts</done>
</task>

<task type="auto">
  <name>Task 3: Client hook useAdminClickableRow (D-11-02)</name>
  <read_first>
    - src/lib/admin/clickable-table-row.ts — Task 1 output
    - .planning/phases/11-admin-list-row-ux/11-RESEARCH.md — use-admin-clickable-row.ts pattern
  </read_first>
  <files>src/lib/admin/use-admin-clickable-row.ts</files>
  <action>
    Create src/lib/admin/use-admin-clickable-row.ts with "use client".

    useAdminClickableRow(href: string):
    - useRouter from next/navigation
    - useMemo returning getAdminClickableRowProps({ href, onNavigate: (target) => router.push(target) })
    - Deps: [href, router]

    Tables will spread hook result on tr/TableRow and merge adminClickableRowClassName via cn() at call site.
  </action>
  <verify>
    <automated>grep -q 'useAdminClickableRow' src/lib/admin/use-admin-clickable-row.ts &amp;&amp; grep -q 'getAdminClickableRowProps' src/lib/admin/use-admin-clickable-row.ts &amp;&amp; npx vitest run src/lib/admin/clickable-table-row.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Hook file is client-only; tests still import pure module only
    - Hook delegates to getAdminClickableRowProps
  </acceptance_criteria>
  <done>use-admin-clickable-row.ts ready for table refactors</done>
</task>

</tasks>

<verification>
- npx vitest run src/lib/admin/clickable-table-row.test.ts
- No new npm dependencies
</verification>

<success_criteria>
- Shared helper + tests land before any table refactor
- All UX-02 keyboard/role behavior testable without browser
</success_criteria>

<output>
Create .planning/phases/11-admin-list-row-ux/11-01-SUMMARY.md when done
</output>
