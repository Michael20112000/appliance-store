---
phase: 11-admin-list-row-ux
plan: 04
type: execute
wave: 3
depends_on:
  - 11-01
files_modified:
  - src/components/admin/admin-categories-table.tsx
  - src/app/(admin)/admin/kategorii/page.tsx
autonomous: true
requirements:
  - ADM-CAT-01
  - ADM-CAT-02
  - UX-02
context_decisions:
  - "D-11-04: AdminCategoriesTable client island; page stays RSC"
  - "D-11-03: Minimal client — table only, not full page"
  - "D-11-09/10: Plus on Додати категорію"

must_haves:
  truths:
    - "Category list has no «Редагувати» column"
    - "Row click opens /admin/kategorii/[id]"
    - "«Додати категорію» shows Plus size-4 left of label"
    - "kategorii/page.tsx remains async server component"
  artifacts:
    - path: src/components/admin/admin-categories-table.tsx
      provides: Client table with clickable rows
    - path: src/app/(admin)/admin/kategorii/page.tsx
      provides: RSC fetch + Plus CTA + table child
  key_links:
    - from: src/app/(admin)/admin/kategorii/page.tsx
      to: src/components/admin/admin-categories-table.tsx
      via: categories prop from listCategoriesAdmin()
      pattern: "AdminCategoriesTable"
    - from: src/components/admin/admin-categories-table.tsx
      to: /admin/kategorii/
      via: row href per category.id
      pattern: "kategorii/"

---

<objective>
Categories admin: extract client table with row-click to edit route, remove «Редагувати» column, add Plus on create CTA (ADM-CAT-01, ADM-CAT-02, D-11-04).

Purpose: RSC page preserved; smallest client boundary for keyboard parity with products.
Output: admin-categories-table.tsx + updated kategorii/page.tsx.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-admin-list-row-ux/11-01-SUMMARY.md
@.planning/phases/11-admin-list-row-ux/11-CONTEXT.md
@.planning/phases/11-admin-list-row-ux/11-PATTERNS.md
@src/app/(admin)/admin/kategorii/page.tsx
@src/components/admin/admin-products-table.tsx
@src/server/services/admin-catalog.service.ts

<interfaces>
listCategoriesAdmin() returns categories with: id, name, sortOrder, _count.products
Props for AdminCategoriesTable: serializable array { id, name, sortOrder, productCount } mapped from _count.products
Row href: `/admin/kategorii/${category.id}`
Table wrapper: same overflow-x-auto rounded-lg border as current page
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC → client props | Only serializable category fields; ids from DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | Spoofing | category row href | mitigate | href built only from category.id in props |
| T-11-04-02 | Information disclosure | client bundle | accept | Table shows same data already rendered server-side |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create AdminCategoriesTable client component (D-11-04, D-11-01)</name>
  <read_first>
    - src/app/(admin)/admin/kategorii/page.tsx — current table markup lines 20–51
    - src/components/admin/admin-products-table.tsx — table shell + row pattern
    - src/lib/admin/clickable-table-row.ts
    - .planning/phases/11-admin-list-row-ux/11-RESEARCH.md — Pattern 2
  </read_first>
  <files>src/components/admin/admin-categories-table.tsx</files>
  <action>
    Create "use client" AdminCategoriesTable.

    Export type AdminCategoryRow = { id: string; name: string; sortOrder: number; productCount: number }.

    Render w-full text-sm table inside same bordered wrapper div as products (overflow-x-auto rounded-lg border bg-background).

    thead: Назва, Товарів, Порядок — three columns only (no empty action header).

    tbody map: useRouter + getAdminClickableRowProps({ href: `/admin/kategorii/${id}`, onNavigate: router.push }) spread on tr; cn border + adminClickableRowClassName.

  Do NOT use stretched Link overlay pattern. Do NOT add edit link column.
  </action>
  <verify>
    <automated>test -f src/components/admin/admin-categories-table.tsx &amp;&amp; grep -q 'getAdminClickableRowProps' src/components/admin/admin-categories-table.tsx &amp;&amp; ! grep -q 'Редагувати' src/components/admin/admin-categories-table.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Client component file exists with three data columns
    - Shared row props used; no Редагувати
  </acceptance_criteria>
  <done>AdminCategoriesTable ready for page wiring</done>
</task>

<task type="auto">
  <name>Task 2: Wire RSC page + Plus CTA (D-11-09, D-11-10, ADM-CAT-01/02)</name>
  <read_first>
    - src/app/(admin)/admin/kategorii/page.tsx — full file
    - src/app/(admin)/admin/tovary/page.tsx — Plus Button pattern after plan 02
  </read_first>
  <files>src/app/(admin)/admin/kategorii/page.tsx</files>
  <action>
    kategorii/page.tsx stays async server component (no "use client").

    - import AdminCategoriesTable and Plus from lucide-react
    - Button «Додати категорію»: add Plus className="size-4" aria-hidden before label; keep existing Button/Link render unchanged otherwise
    - Replace inline table block with AdminCategoriesTable categories={categories.map(c => ({ id: c.id, name: c.name, sortOrder: c.sortOrder, productCount: c._count.products }))}
    - Empty state unchanged (no table when length 0)

    Remove fourth th and edit Link td entirely from page (now in component removal).
  </action>
  <verify>
    <automated>! grep -q '"use client"' src/app/\(admin\)/admin/kategorii/page.tsx &amp;&amp; grep -q 'AdminCategoriesTable' src/app/\(admin\)/admin/kategorii/page.tsx &amp;&amp; grep -q 'Plus' src/app/\(admin\)/admin/kategorii/page.tsx &amp;&amp; ! grep -q 'Редагувати' src/app/\(admin\)/admin/kategorii/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Page remains RSC
    - Plus on create CTA
    - No Редагувати on page or table component
  </acceptance_criteria>
  <done>ADM-CAT-01 and ADM-CAT-02 satisfied</done>
</task>

</tasks>

<verification>
- npx vitest run src/lib/admin/clickable-table-row.test.ts
- Manual category row opens edit (11-MANUAL-CHECKLIST.md)
</verification>

<success_criteria>
- ROADMAP criteria 2 and 4 for categories
- D-11-03 minimal client boundary respected
</success_criteria>

<output>
Create .planning/phases/11-admin-list-row-ux/11-04-SUMMARY.md when done
</output>
