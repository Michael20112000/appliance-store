---
phase: 11-admin-list-row-ux
plan: 05
type: execute
wave: 3
depends_on:
  - 11-01
files_modified:
  - src/components/admin/admin-recent-orders-table.tsx
  - src/app/(admin)/admin/page.tsx
  - .planning/phases/11-admin-list-row-ux/11-MANUAL-CHECKLIST.md
autonomous: true
requirements:
  - ADM-ORD-01
  - UX-02
context_decisions:
  - "D-11-06: Dashboard recent orders row-click; no Відкрити"
  - "D-11-11: Dashboard outline Додати товар Plus optional — skip unless trivial"
  - "D-11-15: Manual checklist artifact"

must_haves:
  truths:
    - "Dashboard recent orders block has no «Відкрити» column"
    - "Row click opens same order detail URL as main orders table"
    - "admin/page.tsx stays RSC; only table island is client"
  artifacts:
    - path: src/components/admin/admin-recent-orders-table.tsx
      provides: Client recent orders table
    - path: src/app/(admin)/admin/page.tsx
      provides: Uses AdminRecentOrdersTable
    - path: .planning/phases/11-admin-list-row-ux/11-MANUAL-CHECKLIST.md
      provides: D-11-15 manual verification steps
  key_links:
    - from: src/app/(admin)/admin/page.tsx
      to: getAdminDashboardStats().recentOrders
      via: orders prop
      pattern: "recentOrders"
    - from: src/components/admin/admin-recent-orders-table.tsx
      to: /admin/zamovlennia/
      via: orderNumber in href
      pattern: "orderNumber"

---

<objective>
Dashboard «Останні замовлення»: row-click to order detail, remove «Відкрити», add D-11-15 manual checklist (D-11-06, ADM-ORD-01).

Purpose: Consistent orders UX between list and dashboard.
Output: admin-recent-orders-table.tsx, updated admin/page.tsx, 11-MANUAL-CHECKLIST.md.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-admin-list-row-ux/11-01-SUMMARY.md
@.planning/phases/11-admin-list-row-ux/11-CONTEXT.md
@src/app/(admin)/admin/page.tsx
@src/server/services/admin-order.service.ts
@src/components/admin/order-status-badge.tsx

<interfaces>
getAdminDashboardStats() returns recentOrders: AdminOrderSummaryDto[] (mapOrderSummary)
AdminRecentOrdersTable props: orders with orderNumber, createdAt, status, totalKopiyky (and id for key)
formatDate — can duplicate small uk-UA formatter from admin/page.tsx or import shared if exists
href: `/admin/zamovlennia/${order.orderNumber}`
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard table | Same admin layout auth; no new public routes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05-01 | Spoofing | order row href | mitigate | orderNumber from server-mapped DTO only |
| T-11-05-02 | Repudiation | manual checklist | accept | Human verification per D-11-15 |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: AdminRecentOrdersTable client component (D-11-06)</name>
  <read_first>
    - src/app/(admin)/admin/page.tsx — recent orders table lines 58–105
    - src/components/admin/orders-data-table.tsx — after plan 03 row pattern
    - src/lib/admin/clickable-table-row.ts
  </read_first>
  <files>src/components/admin/admin-recent-orders-table.tsx</files>
  <action>
    Create "use client" AdminRecentOrdersTable.

    Props: orders: { id: string; orderNumber: string; createdAt: Date; status: AdminOrderSummaryDto['status']; totalKopiyky: number }[] (match mapOrderSummary fields used on dashboard).

    Columns: Номер, Дата, Статус, Сума — four headers only (remove fifth empty th).

    Each tr: shared getAdminClickableRowProps + adminClickableRowClassName; href `/admin/zamovlennia/${order.orderNumber}`.

    Cells: orderNumber font-medium; formatDate for createdAt; OrderStatusBadge; formatPriceKopiyky for total.

    Wrapper: overflow-x-auto rounded-lg border border-border (match dashboard current).

    No «Відкрити» Link. Do not add Plus to dashboard outline buttons unless operator requests (D-11-11).
  </action>
  <verify>
    <automated>test -f src/components/admin/admin-recent-orders-table.tsx &amp;&amp; grep -q 'getAdminClickableRowProps' src/components/admin/admin-recent-orders-table.tsx &amp;&amp; ! grep -q 'Відкрити' src/components/admin/admin-recent-orders-table.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Four columns, clickable rows, no action column
  </acceptance_criteria>
  <done>Recent orders client table implemented</done>
</task>

<task type="auto">
  <name>Task 2: Wire dashboard page + manual checklist (D-11-15)</name>
  <read_first>
    - src/app/(admin)/admin/page.tsx — section Останні замовлення
    - .planning/phases/11-admin-list-row-ux/11-VALIDATION.md — Manual-Only table
    - .planning/phases/08-admin-ux-chat-lifecycle/08-MANUAL-CHECKLIST.md — format reference if exists
  </read_first>
  <files>src/app/(admin)/admin/page.tsx, .planning/phases/11-admin-list-row-ux/11-MANUAL-CHECKLIST.md</files>
  <action>
    admin/page.tsx (RSC):
    - import AdminRecentOrdersTable
    - Replace inline recent orders table with &lt;AdminRecentOrdersTable orders={stats.recentOrders} /&gt;
    - Remove formatDate from page if only used by extracted table (move to component or keep duplicate minimally)
    - Remove fifth th and Відкрити td from page (handled in component)
    - Keep StatCards and outline buttons unchanged

    Create 11-MANUAL-CHECKLIST.md with numbered steps for D-11-15:
    1. /admin/zamovlennia — row click opens detail; no Відкрити column
    2. /admin/kategorii — row click opens edit; no Редагувати; Plus on create
    3. /admin/tovary — Plus on create; row click opens product; status select does NOT navigate
    4. /admin — recent orders row click opens detail; no Відкрити
    5. Keyboard: Tab to row, Enter opens detail on one table
    Mark pass/fail columns for operator.
  </action>
  <verify>
    <automated>grep -q 'AdminRecentOrdersTable' src/app/\(admin\)/admin/page.tsx &amp;&amp; ! grep -q 'Відкрити' src/app/\(admin\)/admin/page.tsx &amp;&amp; test -f .planning/phases/11-admin-list-row-ux/11-MANUAL-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - Dashboard uses client table component
    - Manual checklist file exists with all five scenarios
  </acceptance_criteria>
  <done>Dashboard ADM-ORD-01 parity + D-11-15 checklist</done>
</task>

</tasks>

<verification>
- npx vitest run src/lib/admin/clickable-table-row.test.ts
- Operator runs 11-MANUAL-CHECKLIST.md before phase verify
</verification>

<success_criteria>
- Dashboard recent orders match main orders row-click behavior
- Phase manual gate documented
</success_criteria>

<output>
Create .planning/phases/11-admin-list-row-ux/11-05-SUMMARY.md when done
</output>
