---
phase: 24-product-edit-auto-save-ux
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/lib/debounce.ts
  - src/lib/debounce.test.ts
  - src/hooks/admin/use-product-auto-save.ts
  - src/hooks/admin/use-product-auto-save.test.ts
  - src/components/admin/product-edit-header.tsx
  - src/components/admin/product-edit-delete-button.tsx
  - src/components/admin/product-edit-delete-button.test.tsx
  - src/components/admin/product-edit-page-content.tsx
  - src/components/admin/product-form.tsx
  - src/app/(admin)/admin/tovary/[id]/page.tsx
autonomous: true
requirements:
  - ADM-PRD-05
must_haves:
  truths:
    - "D-01: «Назад» Link with ArrowLeft + visible label above h1 on edit page"
    - "D-02: Back href is adminProductsUrl({ categoryId: product.categoryId })"
    - "D-03: Header layout — back link top; flex row h1 left + delete right"
    - "D-04: Header delete is ghost icon-only Trash2 with aria-label Видалити товар"
    - "D-05: Delete confirms via AlertDialog (list copy); success toast + category-filtered redirect"
    - "D-06: Delete control on edit page shell only — not in create footer"
    - "D-07: Edit mode has no sticky footer (no Зберегти, Скасувати, На вітрині, Видалити)"
    - "D-08: Auto-save via useWatch + 500ms debounce; blur flush on title and description"
    - "D-09: safeParse before updateProductAction; invalid skips network; no validation toast"
    - "D-10: Snapshot diff skips redundant updateProductAction calls"
    - "D-11: Stale updateProductAction responses ignored via generation counter"
    - "D-12: Success silent — inline Збереження… / brief Збережено (~2s); no success toast"
    - "D-13: Save failure uses toast.error with errorMessages map"
    - "D-14: Create page /admin/tovary/novyi keeps manual Save/Cancel footer unchanged"
    - "D-15: ProductImageUpload section unchanged below form on edit"
  artifacts:
    - path: src/lib/debounce.ts
      provides: createDebounce with flush/cancel
    - path: src/hooks/admin/use-product-auto-save.ts
      provides: debounced edit save orchestration
    - path: src/components/admin/product-edit-page-content.tsx
      provides: edit client shell (header + form)
    - path: src/components/admin/product-edit-delete-button.tsx
      provides: header AlertDialog delete
    - path: src/app/(admin)/admin/tovary/[id]/page.tsx
      provides: server page wires ProductEditPageContent
  key_links:
    - from: src/hooks/admin/use-product-auto-save.ts
      to: src/server/actions/admin/product.actions.ts
      via: updateProductAction
      pattern: updateProductAction\\(
    - from: src/components/admin/product-edit-delete-button.tsx
      to: deleteProductFromListAction
      via: startTransition confirm handler
      pattern: deleteProductFromListAction
    - from: src/components/admin/product-edit-header.tsx
      to: src/lib/admin/products-url.ts
      via: adminProductsUrl categoryId back link
      pattern: adminProductsUrl
---

<objective>
Deliver ADM-PRD-05 edit-product UX: category-aware «Назад», header trash delete with AlertDialog, debounced auto-save without Save/На вітрині footer, inline save status, error toasts only.

Purpose: Operators edit products from a filtered list without redundant clicks or losing list context.
Output: `createDebounce`, `useProductAutoSave`, edit page shell components, refactored `ProductForm` (edit) + `[id]/page.tsx`; Vitest coverage for debounce, hook, delete button.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-product-edit-auto-save-ux/24-CONTEXT.md
@.planning/phases/24-product-edit-auto-save-ux/24-RESEARCH.md
@.planning/phases/24-product-edit-auto-save-ux/24-UI-SPEC.md
@.planning/phases/24-product-edit-auto-save-ux/24-VALIDATION.md
@src/components/admin/product-form.tsx
@src/components/admin/product-list-delete-button.tsx
@src/components/admin/product-list-delete-button.test.tsx
@src/app/(admin)/admin/tovary/[id]/page.tsx
@src/app/(admin)/admin/tovary/novyi/page.tsx
@src/lib/catalog/throttle.ts
@src/lib/admin/products-url.ts
@src/server/actions/admin/product.actions.ts
@src/server/validators/admin-product.ts

<interfaces>
From src/lib/catalog/throttle.ts — mirror API for debounce:
- export type ThrottledInvoke = { (fn: () => void): void; flush: () => void }
- export function createThrottle(ms: number): ThrottledInvoke

From src/components/admin/product-list-delete-button.tsx:
- AlertDialogTitle: «Видалити товар?»
- AlertDialogDescription: cart/active-order warning (Ukrainian)
- Uses deleteProductFromListAction(productId) — NOT deleteProductAction
- toast.success("Товар видалено") on ok; toast.error(errorMessages[...]) on fail

From src/server/actions/admin/product.actions.ts:
- updateProductAction(input) → { ok: true } | { ok: false, error: string }
- deleteProductFromListAction(id: string) → same shape; no redirect

From src/lib/admin/products-url.ts:
- adminProductsUrl({ categoryId }) → `/admin/tovary?categoryId=...`

From src/server/validators/admin-product.ts:
- editProductFormSchema.safeParse(values) gates client save (D-09)
- UpsertProductInput type for form values

From product-form.tsx (remove on edit path):
- window.confirm + deleteProductAction on onDelete
- sticky footer with Зберегти, Скасувати, На вітрині, Видалити when mode === edit
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Server Actions | Auto-save and delete invoke `updateProductAction` / `deleteProductFromListAction` with operator-controlled form values |
| Admin route | `/admin/tovary/[id]` remains behind existing admin auth (unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Spoofing | updateProductAction | mitigate | Existing `requireAdmin()` in action — no change; hook must not bypass action |
| T-24-02 | Tampering | Form field values | mitigate | D-09: `editProductFormSchema.safeParse` before call; server `updateProductSchema.parse` unchanged |
| T-24-03 | Denial of Service | Rapid auto-save | mitigate | D-08 debounce 500ms + D-10 snapshot skip reduces call volume |
| T-24-04 | Information Disclosure | Error toasts | accept | Same errorMessages as today; no new PII in messages |
| T-24-05 | Elevation | deleteProductFromListAction | mitigate | Existing `requireAdmin()`; client only triggers after AlertDialog confirm |
| T-24-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase |

No high-severity unmitigated threats. No package installs.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 0): createDebounce utility + unit tests</name>
  <read_first>
- src/lib/catalog/throttle.ts
- .planning/phases/24-product-edit-auto-save-ux/24-RESEARCH.md (Pattern 2)
- .planning/phases/24-product-edit-auto-save-ux/24-CONTEXT.md (D-08 blur flush)
  </read_first>
  <files>
- src/lib/debounce.ts
- src/lib/debounce.test.ts
  </files>
  <action>
1. Create `src/lib/debounce.ts` exporting `DebouncedInvoke` type and `createDebounce(ms: number)` matching throttle ergonomics: callable schedules `fn` after `ms` quiet period; each new schedule resets timer; `flush()` runs pending `fn` immediately and clears timer; `cancel()` clears pending without invoke.
2. Do NOT add npm dependencies — hand-roll only (per RESEARCH).
3. Create `src/lib/debounce.test.ts` with `vi.useFakeTimers()`:
   - After schedule, advancing &lt; ms does not invoke.
   - Advancing ≥ ms invokes once.
   - Second schedule within window resets timer (only latest runs).
   - `flush()` before timeout invokes immediately.
   - `cancel()` prevents pending invoke.
4. Export from `@/lib/debounce` (not under catalog/) per RESEARCH file layout.
  </action>
  <verify>
    <automated>npx vitest run src/lib/debounce.test.ts</automated>
  </verify>
  <acceptance_criteria>
- `src/lib/debounce.ts` exports `createDebounce` with `flush` and `cancel`
- Debounce tests pass: delay, reset, flush, cancel behaviors
- `npx vitest run src/lib/debounce.test.ts` exits 0
- `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2 (Wave 0): useProductAutoSave hook + unit tests</name>
  <read_first>
- src/lib/debounce.ts (from Task 1)
- src/server/validators/admin-product.ts (editProductFormSchema)
- src/components/admin/product-form.tsx (errorMessages map)
- .planning/phases/24-product-edit-auto-save-ux/24-CONTEXT.md (D-08–D-13)
- .planning/phases/24-product-edit-auto-save-ux/24-RESEARCH.md (Pattern 3, pitfalls 1–3)
  </read_first>
  <files>
- src/hooks/admin/use-product-auto-save.ts
- src/hooks/admin/use-product-auto-save.test.ts
  </files>
  <action>
1. Create `useProductAutoSave` in `src/hooks/admin/use-product-auto-save.ts`:
   - Props: `control` (RHF Control&lt;UpsertProductInput&gt;), `productId`, `enabled`, `initialValues` (for snapshot seed).
   - `useWatch({ control })` drives saves when `enabled` (D-08).
   - Internal `createDebounce(500)` schedules `runSave`.
   - `runSave`: `editProductFormSchema.safeParse` — return early if fail (D-09, no toast).
   - Serialize `parsed.data` with `JSON.stringify` for snapshot compare; skip if equals `snapshotRef` (D-10).
   - Increment `generationRef` before `await updateProductAction({ id: productId, ...parsed.data })`; discard result if generation stale (D-11).
   - Status: `idle` | `saving` | `saved` (D-12). On success update snapshot, set `saved`, reset to `idle` after ~2s via timeout.
   - On `!result.ok`: `toast.error` with same `errorMessages` keys as product-form (D-13); set status `idle`.
   - Return `{ status, flush }` where `flush` calls debounce.flush (D-08 blur).
   - Seed `snapshotRef` from parsed `initialValues` on mount to avoid mount save (pitfall 2).
   - Cleanup: debounce.cancel on unmount.
2. Mock `updateProductAction` and `sonner` toast in tests (`/** @vitest-environment jsdom */`).
3. Test cases (fake timers + RTL wrapper with minimal RHF form):
   - **debounce**: change field → advance 499ms → action not called; advance 500ms → called once.
   - **validation**: invalid price/empty title → `updateProductAction` not called.
   - **snapshot**: save success then watch same values → no second call.
   - **stale**: two overlapping saves — older resolution does not update snapshot/status.
   - **flush**: call `flush()` before 500ms elapses → save runs immediately.
4. Do not import UI components in the hook file.
  </action>
  <verify>
    <automated>npx vitest run src/hooks/admin/use-product-auto-save.test.ts</automated>
  </verify>
  <acceptance_criteria>
- Hook file exports `useProductAutoSave` and `SaveStatus` type
- All five test groups (debounce, validation, snapshot, stale, flush) exist and pass
- `updateProductAction` mock called only when parse succeeds and snapshot differs
- `npx vitest run src/hooks/admin/use-product-auto-save.test.ts` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3 (Wave 1): Edit page shell — header, delete, page content</name>
  <read_first>
- src/components/admin/product-list-delete-button.tsx
- src/components/admin/product-list-delete-button.test.tsx
- src/lib/admin/products-url.ts
- .planning/phases/24-product-edit-auto-save-ux/24-UI-SPEC.md
- .planning/phases/24-product-edit-auto-save-ux/24-CONTEXT.md (D-01–D-06, D-12)
  </read_first>
  <files>
- src/components/admin/product-edit-header.tsx
- src/components/admin/product-edit-delete-button.tsx
- src/components/admin/product-edit-delete-button.test.tsx
- src/components/admin/product-edit-page-content.tsx
  </files>
  <action>
**Prerequisite:** Task 1–2 vitest green before starting Wave 1.

1. **ProductEditHeader** (`product-edit-header.tsx`):
   - Props: `categoryId`, `saveStatus: SaveStatus`.
   - Render `Link` above title row: `ArrowLeft` `size-4` `aria-hidden` + «Назад» → `adminProductsUrl({ categoryId })` (D-01, D-02).
   - Below: `flex items-start justify-between gap-4` with `h1` «Редагувати товар» and slot for delete button (D-03).
   - Inline muted status from `saveStatus`: `Збереження…` when saving; `Збережено` when saved (fade optional via opacity transition) (D-12).

2. **ProductEditDeleteButton** (`product-edit-delete-button.tsx`):
   - Props: `productId`, `categoryId` (nullable).
   - Mirror list AlertDialog copy exactly (D-05). Button: `variant="ghost"` `size="icon"`, `Trash2` `size-4`, `aria-label="Видалити товар"` (D-04).
   - On confirm: `deleteProductFromListAction(productId)` — NOT `deleteProductAction` (D-05).
   - Success: `toast.success("Товар видалено")` + `router.push(adminProductsUrl({ categoryId }))` or `/admin/tovary` if no categoryId.
   - Omit `stopRowNav` / `data-admin-row-interactive` (not in table) (D-06).

3. **ProductEditPageContent** (`product-edit-page-content.tsx`):
   - Client component composing header + `ProductForm` + passing `saveStatus` from hook (wired in Task 4) or accept `renderForm` callback — prefer owning form inside shell with props for productId, categories, defaultValues, images.
   - Pass `categoryId` to header and delete button.

4. **product-edit-delete-button.test.tsx**:
   - Mock `useRouter` (`push`), `deleteProductFromListAction`, `toast`.
   - Click delete button → dialog title «Видалити товар?» visible.
   - Confirm → action called with productId; on ok `router.push` includes `categoryId=` when categoryId provided.

5. Do not modify `product-form.tsx` or `[id]/page.tsx` in this task.
  </action>
  <verify>
    <automated>npx vitest run src/components/admin/product-edit-delete-button.test.tsx</automated>
  </verify>
  <acceptance_criteria>
- ProductEditHeader back link uses `adminProductsUrl({ categoryId })` with ArrowLeft + «Назад»
- ProductEditDeleteButton uses AlertDialog (not window.confirm) and deleteProductFromListAction
- Delete test asserts dialog opens and successful delete calls router.push with category filter
- `npx vitest run src/components/admin/product-edit-delete-button.test.tsx` exits 0
- `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4 (Wave 1): Wire auto-save, refactor ProductForm, update edit page</name>
  <read_first>
- src/hooks/admin/use-product-auto-save.ts
- src/components/admin/product-edit-page-content.tsx
- src/components/admin/product-form.tsx
- src/app/(admin)/admin/tovary/[id]/page.tsx
- src/app/(admin)/admin/tovary/novyi/page.tsx
- .planning/phases/24-product-edit-auto-save-ux/24-CONTEXT.md (D-07–D-15)
  </read_first>
  <files>
- src/components/admin/product-form.tsx
- src/components/admin/product-edit-page-content.tsx
- src/app/(admin)/admin/tovary/[id]/page.tsx
  </files>
  <action>
1. **ProductForm** refactor:
   - Add optional props for edit auto-save: `onSaveStatusChange?: (status: SaveStatus) => void` OR lift hook to `ProductEditPageContent` and pass `flush` into form — preferred: call `useProductAutoSave` inside `ProductEditPageContent` with `control` from form via ref/callback, OR keep hook inside form when `mode === "edit"` only.
   - When `mode === "edit"`: remove sticky footer entirely (D-07); remove `onDelete`, `window.confirm`, `deleteProductAction` import usage; remove «На вітрині» button; stop using `storefrontSlug` on edit (remove prop from edit page call).
   - Wire `title` and `description` registers with merged `onBlur` that calls RHF blur then `autoSave.flush()` (D-08).
   - Remove edit-path `Alert` for save errors if hook handles via toast (D-13); keep create-mode error `Alert`.
   - When `mode === "create"`: keep existing footer (Зберегти, Скасувати) unchanged (D-14).
   - `ProductImageUpload` block unchanged for edit (D-15).

2. **ProductEditPageContent**:
   - Render `ProductEditHeader` with `saveStatus` from hook.
   - Place `ProductEditDeleteButton` in header right slot.
   - Render `ProductForm` with `mode="edit"` below.

3. **[id]/page.tsx**:
   - Remove bare `h1`; render `ProductEditPageContent` with productId, categoryId, categories, defaultValues, images.
   - Keep `ProductOrdersSection` below shell in server `space-y-6` layout.
   - Remove `storefrontSlug` prop.

4. **Regression guard:** Do not edit `novyi/page.tsx` except verify it still passes `mode="create"` only.

5. Run full related vitest + tsc.
  </action>
  <verify>
    <automated>npx vitest run src/lib/debounce.test.ts src/hooks/admin/use-product-auto-save.test.ts src/components/admin/product-edit-delete-button.test.tsx</automated>
  </verify>
  <acceptance_criteria>
- Edit page has no sticky footer; create page still shows Зберегти and Скасувати buttons
- `product-form.tsx` edit mode does not reference window.confirm, deleteProductAction, or storefrontSlug
- `[id]/page.tsx` uses ProductEditPageContent; no standalone h1 duplicate
- All Wave 0 + delete vitest files pass; `npx tsc --noEmit` exits 0
- Grep edit form: no «На вітрині» in edit footer path — `grep -n "На вітрині" src/components/admin/product-form.tsx` only matches create guard or zero matches when `mode === "edit"` footer removed
  </acceptance_criteria>
</task>

</tasks>

<verification>
## Plan-level checks

- [ ] Wave 0: `npx vitest run src/lib/debounce.test.ts src/hooks/admin/use-product-auto-save.test.ts` green
- [ ] Wave 1: `npx vitest run src/components/admin/product-edit-delete-button.test.tsx` green
- [ ] `npx tsc --noEmit` green
- [ ] `npm test` green before `/gsd-verify-work`
- [ ] Manual: `/admin/tovary/novyi` still has Save/Cancel (D-14)
- [ ] Manual: edit product — back link, auto-save status, header delete AlertDialog

## Source coverage audit

| Source | Item | Plan task |
|--------|------|-----------|
| GOAL | Auto-save edit without Save; clear nav | Task 3–4 |
| REQ ADM-PRD-05 | Full edit UX | All tasks |
| D-01–D-06 | Chrome + delete | Task 3–4 |
| D-07–D-13 | Auto-save + feedback | Task 2, 4 |
| D-14–D-15 | Create unchanged; images unchanged | Task 4 |
| RESEARCH | debounce, hook, deleteFromListAction | Task 1–3 |
</verification>

<success_criteria>
- All D-01–D-15 truths observable on edit page; create flow unchanged
- ADM-PRD-05 automated tests pass (debounce, hook, delete button)
- No new server actions or schema changes
- Typecheck clean
</success_criteria>

<output>
Create `.planning/phases/24-product-edit-auto-save-ux/24-01-SUMMARY.md` when done
</output>
