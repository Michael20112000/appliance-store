---
phase: 42-floating-action-buttons
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/components/layout/storefront-fabs.tsx
  - src/components/layout/storefront-fabs.test.tsx
autonomous: true
requirements:
  - FAB-01
  - FAB-02

must_haves:
  truths:
    - "Cart FAB renders on screen even when cart count is 0"
    - "Cart FAB always links to /koszyk"
    - "Cart FAB shows a badge only when count > 0"
    - "Cart FAB badge shows '9+' when count exceeds 9"
    - "Callback FAB opens a Dialog on click"
    - "Dialog shows the store phone number when phones are provided"
    - "Dialog contains the callback request form with idPrefix='fab'"
    - "Guest cart count syncs from localStorage via CART_CHANGED_EVENT"
    - "Session user receives initialCartCount as prop — no localStorage sync"
  artifacts:
    - path: "src/components/layout/storefront-fabs.tsx"
      provides: "StorefrontFabs client component — both FABs in a fixed bottom-left wrapper"
      exports: ["StorefrontFabs"]
    - path: "src/components/layout/storefront-fabs.test.tsx"
      provides: "Unit tests covering FAB-01 and FAB-02"
      contains: "@vitest-environment jsdom"
  key_links:
    - from: "src/components/layout/storefront-fabs.tsx"
      to: "/koszyk"
      via: "Link href"
      pattern: "href=\"/koszyk\""
    - from: "src/components/layout/storefront-fabs.tsx"
      to: "src/components/ui/dialog.tsx"
      via: "Dialog open/onOpenChange controlled state"
      pattern: "open={callbackOpen}"
    - from: "src/components/layout/storefront-fabs.tsx"
      to: "src/components/layout/callback-request-form.tsx"
      via: "idPrefix='fab' prop"
      pattern: "idPrefix=\"fab\""
---

<objective>
Create the StorefrontFabs client component (both FABs) with full unit test coverage using TDD.

Purpose: Delivers FAB-01 (persistent cart FAB, always visible) and FAB-02 (callback FAB opening dialog with store phone + form).
Output: storefront-fabs.tsx (component) and storefront-fabs.test.tsx (tests). The component is not yet wired into the storefront layout — that is Plan 02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/42-floating-action-buttons/42-RESEARCH.md

TDD cycle: RED (write failing tests) → GREEN (write component) → REFACTOR (clean up). Commit after each phase.
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/server/services/store-settings.service.ts:
  export type PublicStorePhone = {
    id: string;
    digits: string;
    label: string | null;
  };

From src/components/layout/callback-request-form.tsx:
  export function CallbackRequestForm({
    idPrefix?: string;   // defaults to "footer" — use "fab" in dialog
    compact?: boolean;
    className?: string;
  }: CallbackRequestFormProps)
  // Uses: submitCallbackRequestAction, react-hook-form, zodResolver

From src/lib/cart/cart-events.ts + pending-storage.ts:
  export const CART_CHANGED_EVENT: string;    // event name for localStorage cart
  export function getPendingItemCount(): number;  // reads localStorage

From src/components/ui/dialog.tsx (wraps @base-ui/react/dialog):
  // Controlled usage — no DialogTrigger needed:
  <Dialog open={open} onOpenChange={setOpen}>
    <DialogContent>
      <DialogHeader><DialogTitle>...</DialogTitle></DialogHeader>
      {/* content */}
    </DialogContent>
  </Dialog>
  // DialogContent has built-in close button and portal/overlay

From src/components/cart/pdp-cart-fab.tsx (positioning + badge pattern):
  // Fixed positioning — new FABs use left-6 (not right-6)
  // z-[59] — below ChatFab's z-[60], dialog renders above via portal
  // pb-[max(0px,env(safe-area-inset-bottom))] — safe-area on wrapper div
  // Badge: className="absolute -right-0.5 -top-0.5 min-w-5 justify-center px-1 text-[10px]"
  // KEY DIFFERENCE: do NOT copy the `if (count < 1) return null` guard — FAB-01 requires always visible

From src/components/layout/callback-request-form.test.tsx (test pattern to follow):
  /** @vitest-environment jsdom */
  import { afterEach, beforeEach, describe, expect, it, vi } from "vitest";
  import { cleanup, fireEvent, render, screen, waitFor } from "@testing-library/react";
  // Mocks: vi.mock("@/server/actions/callback.actions", ...)
  // Mocks: vi.mock("sonner", ...)
  // Assertions: .toBeDefined() (not .toBeInTheDocument())

StorefrontFabsProps interface (to create in storefront-fabs.tsx):
  type StorefrontFabsProps = {
    phones: PublicStorePhone[];
    initialCartCount: number;
    hasSession: boolean;
  };
</interfaces>

<tasks>

<task type="tdd">
  <name>Task 1: RED — Write failing tests for StorefrontFabs (FAB-01 and FAB-02)</name>
  <files>src/components/layout/storefront-fabs.test.tsx</files>
  <read_first>
    src/components/layout/callback-request-form.test.tsx
    src/components/layout/store-mobile-nav.test.tsx
    src/components/layout/callback-request-form.tsx
  </read_first>
  <behavior>
    - FAB-01-a: renders cart FAB when initialCartCount is 0 (no early return)
    - FAB-01-b: cart FAB has href="/koszyk" and role="link" or is an anchor element
    - FAB-01-c: does NOT show badge when initialCartCount is 0
    - FAB-01-d: shows badge with "3" when initialCartCount is 3
    - FAB-01-e: shows badge "9+" when initialCartCount is 10
    - FAB-02-a: renders callback FAB button with aria-label="Замовити дзвінок"
    - FAB-02-b: clicking callback FAB opens dialog (dialog title "Зв'яжіться з нами" becomes visible)
    - FAB-02-c: dialog shows formatted phone when phones prop is non-empty
    - FAB-02-d: dialog contains callback form heading "Вкажіть свій номер — ми передзвонимо"
  </behavior>
  <action>
    Create src/components/layout/storefront-fabs.test.tsx with the jsdom directive at line 1 and all required mocks. The file does NOT yet import a real storefront-fabs.tsx (that file doesn't exist yet), so the test file itself compiles but all tests fail at runtime when importing the component.

    Required mocks (vi.mock calls placed before imports of the mocked modules):
    - "next/link" — mock as a component forwarding href and children: vi.mock("next/link", () => ({ default: ({ href, children, className, 'aria-label': ariaLabel }: { href: string; children: React.ReactNode; className?: string; 'aria-label'?: string }) => React.createElement("a", { href, className, 'aria-label': ariaLabel }, children) }))
    - "next/navigation" — vi.mock("next/navigation", () => ({ useRouter: () => ({ push: vi.fn() }), usePathname: () => "/" }))
    - "@/lib/cart/cart-events" — vi.mock("@/lib/cart/cart-events", () => ({ CART_CHANGED_EVENT: "cart-changed" }))
    - "@/lib/cart/pending-storage" — vi.mock("@/lib/cart/pending-storage", () => ({ getPendingItemCount: vi.fn().mockReturnValue(0) }))
    - "@/server/actions/callback.actions" — vi.mock("@/server/actions/callback.actions", () => ({ submitCallbackRequestAction: vi.fn() }))
    - "sonner" — vi.mock("sonner", () => ({ toast: { error: vi.fn(), success: vi.fn() } }))

    Test structure: single describe("StorefrontFabs") block with one it() per behavior item above.

    For FAB-02-b: use fireEvent.click on the button with aria-label="Замовити дзвінок", then assert screen.getByText("Зв'яжіться з нами") is defined.

    For FAB-02-c: pass phones prop as [{ id: "p1", digits: "0971112233", label: null }] and assert the formatted phone text is in the document after clicking the callback FAB.

    Phone display format: the test can assert screen.getByText(/097/) to avoid coupling to exact formatting.

    Use cleanup in afterEach. Use beforeEach to vi.clearAllMocks().
  </action>
  <verify>
    <automated>npm test -- src/components/layout/storefront-fabs.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>
    storefront-fabs.test.tsx exists. Tests compile. All tests FAIL (RED state — component not yet created).
    The failure output shows "Cannot find module" or "is not a function" — not a syntax error in the test file itself.
  </done>
</task>

<task type="tdd">
  <name>Task 2: GREEN — Implement StorefrontFabs component to pass all tests</name>
  <files>src/components/layout/storefront-fabs.tsx</files>
  <read_first>
    src/components/cart/pdp-cart-fab.tsx
    src/components/layout/storefront-fabs.test.tsx
    src/components/ui/dialog.tsx
    src/components/layout/callback-request-form.tsx
    src/lib/phone/format-ua.ts
  </read_first>
  <behavior>
    Component is "use client". Exports named function StorefrontFabs(props: StorefrontFabsProps).

    State:
    - cartCount: number — initialized from initialCartCount prop
    - callbackOpen: boolean — initialized false

    Effects:
    - useEffect([initialCartCount]) — setCartCount(initialCartCount) when prop changes
    - useEffect([hasSession]) — if hasSession skip; else sync from getPendingItemCount() and attach CART_CHANGED_EVENT listener; cleanup removes listener

    Cart FAB (FAB-01):
    - Renders as Link href="/koszyk"
    - aria-label: cartCount > 0 ? "Кошик, {N} товарів" : "Кошик"
    - No early return when count === 0 (critical difference from PdpCartFab)
    - Badge: rendered only when cartCount > 0; text is cartCount > 9 ? "9+" : String(cartCount)
    - className: cn("flex size-14 items-center justify-center rounded-full bg-primary text-primary-foreground shadow-lg", "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring relative")
    - Badge className: "absolute -right-0.5 -top-0.5 min-w-5 justify-center px-1 text-[10px]"
    - Icon: ShoppingCart className="size-6" aria-hidden

    Callback FAB (FAB-02):
    - Renders as button type="button" onClick={() => setCallbackOpen(true)}
    - aria-label: "Замовити дзвінок"
    - Same rounded-full bg-primary className as cart FAB
    - Icon: Phone className="size-6" aria-hidden

    Wrapper div: className="fixed bottom-6 left-6 z-[59] flex flex-col items-center gap-3 pb-[max(0px,env(safe-area-inset-bottom))]"
    Order in wrapper: CallbackFab above CartFab (callback rendered first in JSX, cart last)

    Dialog (FAB-02):
    - Dialog open={callbackOpen} onOpenChange={setCallbackOpen}
    - DialogContent > DialogHeader > DialogTitle: "Зв'яжіться з нами"
    - If phones.length > 0: ul.space-y-1.text-sm listing each phone as anchor href={uaPhoneTelHref(phone.digits)}
    - Each phone anchor text: formatUaPhoneDisplay(phone.digits)
    - If phone.label: span.ml-2.text-muted-foreground with label text
    - CallbackRequestForm idPrefix="fab" compact — always rendered after the phone list (or when phones is empty)

    Imports needed: Link from "next/link", useEffect+useState from "react", Phone+ShoppingCart from "lucide-react", Badge from "@/components/ui/badge", Dialog+DialogContent+DialogHeader+DialogTitle from "@/components/ui/dialog", CallbackRequestForm from "@/components/layout/callback-request-form", CART_CHANGED_EVENT from "@/lib/cart/cart-events", getPendingItemCount from "@/lib/cart/pending-storage", formatUaPhoneDisplay+uaPhoneTelHref from "@/lib/phone/format-ua", PublicStorePhone type from "@/server/services/store-settings.service", cn from "@/lib/utils"
  </action>
  <verify>
    <automated>npm test -- src/components/layout/storefront-fabs.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>
    All tests in storefront-fabs.test.tsx pass (GREEN state).
    npm test exits 0 for this test file.
    No TypeScript errors: npx tsc --noEmit exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→server action | CallbackRequestForm submits user-entered phone number via submitCallbackRequestAction |
| RSC→client prop | Store phones and cart count passed from RSC to client component as plain props |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Denial of Service | submitCallbackRequestAction (via CallbackRequestForm in dialog) | accept | Existing CallbackRateLimitError in createCallbackRequest service already handles rate limiting — no new action code |
| T-42-02 | Information Disclosure | Store phone displayed in callback dialog | accept | Store phone is already public information displayed in footer; no new exposure |
| T-42-03 | Spoofing | CART_CHANGED_EVENT on window | accept | Event carries no payload; handler calls getPendingItemCount() from localStorage — attacker can only affect their own cart display |
| T-42-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase |
</threat_model>

<verification>
npm test -- src/components/layout/storefront-fabs.test.tsx
npx tsc --noEmit
</verification>

<success_criteria>
- storefront-fabs.tsx exists at src/components/layout/storefront-fabs.tsx
- storefront-fabs.test.tsx exists and all tests pass (GREEN)
- TypeScript compiles without errors (npx tsc --noEmit)
- Cart FAB does NOT disappear when initialCartCount is 0 (verified by FAB-01-a test)
- Callback FAB opens dialog with phone and form (verified by FAB-02-b, FAB-02-c, FAB-02-d tests)
- idPrefix="fab" passed to CallbackRequestForm (verified by FAB-02-d test — no ID collision with footer form)
</success_criteria>

<output>
Create .planning/phases/42-floating-action-buttons/42-01-SUMMARY.md when done
</output>
