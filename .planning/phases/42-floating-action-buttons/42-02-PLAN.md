---
phase: 42-floating-action-buttons
plan: 02
type: execute
wave: 2
depends_on:
  - 42-01
files_modified:
  - src/app/(storefront)/layout.tsx
autonomous: false
requirements:
  - FAB-01
  - FAB-02

must_haves:
  truths:
    - "StorefrontFabs appears on every storefront page"
    - "StorefrontFabs does NOT appear on admin pages"
    - "Store phone numbers are fetched server-side and passed as props"
    - "Session cart count is fetched server-side and passed as initialCartCount"
    - "Guest users get initialCartCount=0 (no session cart query)"
  artifacts:
    - path: "src/app/(storefront)/layout.tsx"
      provides: "Storefront layout RSC with StorefrontFabs injected after Toaster"
      contains: "StorefrontFabs"
  key_links:
    - from: "src/app/(storefront)/layout.tsx"
      to: "src/components/layout/storefront-fabs.tsx"
      via: "JSX import and render with phones, initialCartCount, hasSession props"
      pattern: "<StorefrontFabs"
    - from: "src/app/(storefront)/layout.tsx"
      to: "src/server/services/store-settings.service.ts"
      via: "getPublicStoreContacts() called in async layout RSC"
      pattern: "getPublicStoreContacts"
    - from: "src/app/(storefront)/layout.tsx"
      to: "src/server/services/cart.service.ts"
      via: "getCartItemCount(session.user.id) called conditionally when session exists"
      pattern: "getCartItemCount"
---

<objective>
Wire StorefrontFabs into the storefront layout RSC and visually verify both FABs on a live page.

Purpose: Connects the component created in Plan 01 to the actual app. The layout fetches phones and cart count server-side, injects them as props.
Output: Modified src/app/(storefront)/layout.tsx with three new imports and StorefrontFabs rendered after Toaster.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/42-floating-action-buttons/42-RESEARCH.md
@.planning/phases/42-floating-action-buttons/42-01-SUMMARY.md
</context>

<interfaces>
<!-- Key contracts from codebase -->

From src/app/(storefront)/layout.tsx (CURRENT STATE — read before modifying):
  // Non-async function — must be converted to async to await getSession, getPublicStoreContacts, getCartItemCount
  export default function StorefrontLayout({ children }: { children: React.ReactNode })
  // Current JSX ends with: <Toaster richColors position="top-center" closeButton />
  // No auth, no data fetching currently in this layout

Auth pattern from src/app/(storefront)/tovar/[slug]/page.tsx:
  import { auth } from "@/lib/auth";
  import { headers } from "next/headers";
  const session = await auth.api.getSession({ headers: await headers() });

From src/server/services/store-settings.service.ts:
  export async function getPublicStoreContacts(): Promise<{ phones: PublicStorePhone[]; emails: ...; addresses: ... }>
  // Returns phones[] — pass contacts.phones to StorefrontFabs

From src/server/services/cart.service.ts:
  export async function getCartItemCount(userId: string): Promise<number>
  // Call only when session?.user exists; guard: session?.user?.id

From src/components/layout/storefront-fabs.tsx (created in Plan 01):
  export function StorefrontFabs(props: {
    phones: PublicStorePhone[];
    initialCartCount: number;
    hasSession: boolean;
  })
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire StorefrontFabs into storefront layout RSC</name>
  <files>src/app/(storefront)/layout.tsx</files>
  <read_first>
    src/app/(storefront)/layout.tsx
    src/components/layout/storefront-fabs.tsx
    src/app/(storefront)/tovar/[slug]/page.tsx
    node_modules/next/dist/docs/02-app/02-api-reference/03-file-conventions/layout.md
  </read_first>
  <action>
    Read src/app/(storefront)/layout.tsx fully first to see its current state.

    Changes to make:

    1. Convert the function signature to async: "export default async function StorefrontLayout"

    2. Add three new imports at the top (after existing imports):
       - import { auth } from "@/lib/auth"
       - import { headers } from "next/headers"
       - import { getPublicStoreContacts } from "@/server/services/store-settings.service"
       - import { getCartItemCount } from "@/server/services/cart.service"
       - import { StorefrontFabs } from "@/components/layout/storefront-fabs"

    3. Inside the function body, before the return statement, add three awaits:
       - const session = await auth.api.getSession({ headers: await headers() })
       - const contacts = await getPublicStoreContacts()
       - const cartCount = session?.user ? await getCartItemCount(session.user.id) : 0

    4. In the JSX return, add StorefrontFabs after the existing Toaster element and before the closing fragment:
       - phones={contacts.phones}
       - initialCartCount={cartCount}
       - hasSession={Boolean(session?.user)}

    The Toaster line currently ends the JSX. StorefrontFabs goes after it. Keep everything else unchanged.

    Do NOT place StorefrontFabs inside the main element or inside Suspense — it must be a sibling to StoreHeader, main, StoreFooter, and Toaster at the fragment root level.

    Do NOT add StorefrontFabs to app/layout.tsx or any admin layout — storefront route group isolation is sufficient.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(storefront)/layout.tsx contains "async function StorefrontLayout"
    - src/app/(storefront)/layout.tsx contains "getPublicStoreContacts"
    - src/app/(storefront)/layout.tsx contains "getCartItemCount"
    - src/app/(storefront)/layout.tsx contains "StorefrontFabs"
    - src/app/(storefront)/layout.tsx contains "contacts.phones"
    - src/app/(storefront)/layout.tsx contains "initialCartCount={cartCount}"
    - src/app/(storefront)/layout.tsx contains "hasSession={Boolean(session?.user)}"
    - npx tsc --noEmit exits 0 (no TypeScript errors)
    - npm test exits 0 (full suite green)
  </acceptance_criteria>
  <done>
    Layout is modified and TypeScript-clean. StorefrontFabs is wired with phones, initialCartCount, and hasSession props.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of both FABs on storefront</name>
  <what-built>
    Plan 01 created StorefrontFabs with CartFab (left-6, always visible, /koszyk link) and CallbackFab (left-6 above cart, opens dialog with phone + form).
    Plan 02 wired it into the storefront layout with real data from the database.
  </what-built>
  <how-to-verify>
    1. Start the dev server: npm run dev
    2. Visit http://localhost:3000 (homepage)
    3. Confirm: two circular buttons visible in the bottom-left corner
       - Lower button: shopping cart icon — clicking it navigates to /koszyk
       - Upper button: phone icon — clicking it opens a dialog
    4. Click the phone icon button
    5. Confirm dialog opens titled "Зв'яжіться з нами"
    6. Confirm dialog contains a form with heading "Вкажіть свій номер — ми передзвонимо"
    7. Confirm dialog closes when clicking the X button or pressing Esc
    8. Visit http://localhost:3000/admin (any admin page)
    9. Confirm: NO floating buttons visible on admin pages
    10. Empty the cart (if items present), return to homepage
    11. Confirm: cart FAB still visible at bottom-left even with empty cart
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC→client | phones[] and cartCount passed from server to StorefrontFabs client component as serializable props |
| storefront route group | (storefront)/layout.tsx scope — admin route group has its own layout, no inheritance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-04 | Information Disclosure | contacts.phones passed as prop | accept | Store phones are already public (displayed in footer); passing to another component adds no new exposure |
| T-42-05 | Elevation of Privilege | Admin page FAB absence | accept | Route group isolation — (storefront)/layout.tsx is not inherited by (admin)/ layout; no auth gate needed for FAB suppression |
| T-42-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase |
</threat_model>

<verification>
npx tsc --noEmit
npm test
</verification>

<success_criteria>
- src/app/(storefront)/layout.tsx is async and imports StorefrontFabs
- TypeScript compiles without errors
- Full test suite is green
- FABs visible bottom-left on all storefront pages (user confirms "approved" at checkpoint)
- FABs absent on admin pages (user confirms at checkpoint)
- Cart FAB visible even when cart is empty (user confirms at checkpoint)
- Callback dialog opens with store phone and callback form (user confirms at checkpoint)
</success_criteria>

<output>
Create .planning/phases/42-floating-action-buttons/42-02-SUMMARY.md when done
</output>
