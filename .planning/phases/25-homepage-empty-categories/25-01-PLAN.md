---
phase: 25-homepage-empty-categories
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/catalog/categories.test.ts
  - src/server/services/catalog.service.test.ts
  - src/server/services/catalog.service.ts
  - src/components/home/category-grid.tsx
autonomous: true
requirements:
  - HOME-03
user_setup: []

must_haves:
  truths:
    - "Homepage «Категорії» shows only categories with at least one in-stock catalog product (productCount > 0)"
    - "When no categories pass the filter, homepage has no #kategorii section, no h2, and no empty-state copy"
    - "Filtered homepage categories match header nav category set (same pipeline as StoreHeader)"
    - "Category cards keep Cloudinary image, «Без фото» fallback, title, «Переглянути», and /katalog/{slug} links"
    - "Filtered list preserves sortOrder ascending from listCategories (empty categories removed, order unchanged)"
  artifacts:
    - path: src/components/home/category-grid.tsx
      provides: "Homepage category grid with service pipeline and empty-section guard"
      contains: "listCategoriesWithProductCounts"
    - path: src/server/services/catalog.service.ts
      provides: "Category DTO with imagePublicId, imageAlt, productCount"
      contains: "imagePublicId"
    - path: src/lib/catalog/categories.test.ts
      provides: "Filter contract tests including order preservation"
    - path: src/server/services/catalog.service.test.ts
      provides: "listCategoriesWithProductCounts DTO mapping test"
  key_links:
    - from: src/components/home/category-grid.tsx
      to: src/server/services/catalog.service.ts
      via: "listCategoriesWithProductCounts import and await"
      pattern: "listCategoriesWithProductCounts"
    - from: src/components/home/category-grid.tsx
      to: src/lib/catalog/categories.ts
      via: "categoriesWithAvailableProducts before render"
      pattern: "categoriesWithAvailableProducts"
    - from: src/components/home/category-grid.tsx
      to: "null render"
      via: "if (categories.length === 0) return null before section"
      pattern: "categories\\.length === 0.*return null"
---

<objective>
Align homepage `CategoryGrid` with storefront header/catalog filter pipeline so empty categories never appear on `/`, and hide the entire «Категорії» section when nothing remains.

Purpose: After DB purge/seed, homepage must not show dead category tiles; parity with `StoreHeader` (HOME-03).
Output: Service DTO extended for card images; `CategoryGrid` uses catalog.service + shared filter; unit tests document contract; green build.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-homepage-empty-categories/25-CONTEXT.md
@.planning/phases/25-homepage-empty-categories/25-RESEARCH.md
@.planning/phases/25-homepage-empty-categories/25-PATTERNS.md
@.planning/phases/25-homepage-empty-categories/25-UI-SPEC.md
@.planning/phases/25-homepage-empty-categories/25-VALIDATION.md

@src/components/layout/store-header.tsx
@src/components/home/category-grid.tsx
@src/server/services/catalog.service.ts
@src/lib/catalog/categories.ts
@src/lib/catalog/categories.test.ts
@src/server/services/catalog.service.test.ts

<interfaces>
From src/lib/catalog/categories.ts:
```typescript
export function categoriesWithAvailableProducts<
  T extends { productCount: number },
>(categories: T[]): T[] {
  return categories.filter((category) => category.productCount > 0);
}
```

From src/server/services/catalog.service.ts (do not change buildCatalogContextWhere):
```typescript
export function buildCatalogContextWhere(
  categoryId?: string,
): Prisma.ProductWhereInput {
  return {
    quantity: { gte: 1 },
    ...(categoryId && { categoryId }),
  };
}

export async function listCategoriesWithProductCounts() {
  // returns { totalProductCount, categories: [{ id, slug, name, productCount }] }
  // EXTEND map to include imagePublicId, imageAlt from listCategories() rows
}
```

From src/components/layout/store-header.tsx (reference pipeline, lines 15-18):
```typescript
const { categories: categoriesWithCounts } =
  await listCategoriesWithProductCounts();
const availableCategories =
  categoriesWithAvailableProducts(categoriesWithCounts);
```

Current CategoryGrid anti-pattern to remove:
```typescript
import { prisma } from "@/lib/db";
const categories = await prisma.category.findMany({ orderBy: { sortOrder: "asc" } });
```
</interfaces>
</context>

<tasks>

<task type="auto" id="25-01-01">
  <name>Task 1: Document filter contract in categories.test.ts</name>
  <read_first>
    - src/lib/catalog/categories.ts
    - src/lib/catalog/categories.test.ts
    - 25-CONTEXT.md decisions D-02, D-03, D-08
  </read_first>
  <files>src/lib/catalog/categories.test.ts</files>
  <action>
    Per D-03 and D-08: add a Vitest case `preserves input order after filtering` to the existing `describe("categoriesWithAvailableProducts")` block.

    Test input: three items with slugs `first` (productCount 1), `empty` (productCount 0), `last` (productCount 3). Assert `categoriesWithAvailableProducts(input).map((c) => c.slug)` equals `["first", "last"]` — proves filter does not reorder, only removes zeros.

    Do not change `categoriesWithAvailableProducts` implementation unless the new test fails for a real bug (it should pass today).
  </action>
  <acceptance_criteria>
    - New `it("preserves input order after filtering", ...)` exists in categories.test.ts
    - Test passes without changing categories.ts filter logic
    - Existing `keeps categories with productCount > 0` test still passes
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run src/lib/catalog/categories.test.ts</automated>
  </verify>
  <done>Filter contract includes order-preservation coverage; categories.test.ts green</done>
</task>

<task type="auto" id="25-01-02">
  <name>Task 2: Extend listCategoriesWithProductCounts DTO and test</name>
  <read_first>
    - src/server/services/catalog.service.ts (listCategories, listCategoriesWithProductCounts, getCatalogCategoryCounts)
    - src/server/services/catalog.service.test.ts (vi.mock pattern)
    - 25-CONTEXT.md D-01, D-02, D-07
    - 25-RESEARCH.md Pattern 3 (image fields)
  </read_first>
  <files>src/server/services/catalog.service.ts, src/server/services/catalog.service.test.ts</files>
  <action>
    Per D-01/D-07: in `listCategoriesWithProductCounts`, extend the categories map to pass through `imagePublicId` and `imageAlt` from each `listCategories()` row (no extra DB query). Keep existing fields `id`, `slug`, `name`, `productCount` (from `counts.byCategoryId[category.id] ?? 0`). Do not alter `buildCatalogContextWhere` or count logic (D-02).

    Per D-08: add `describe("listCategoriesWithProductCounts")` in catalog.service.test.ts. Extend the existing `vi.mock("@/lib/db")` with `category.findMany`, `product.count`, and `product.groupBy` as needed. Mock `listCategories` return of two categories (one with image fields, one without) and `getCatalogCategoryCounts` via count/groupBy mocks so `byCategoryId` assigns productCount 2 and 0 respectively. Assert returned DTO includes `imagePublicId`, `imageAlt`, and correct `productCount` per category.

    Import `listCategoriesWithProductCounts` in the test file alongside existing imports.
  </action>
  <acceptance_criteria>
    - listCategoriesWithProductCounts returns imagePublicId and imageAlt on each category item
    - productCount still derived from getCatalogCategoryCounts / buildCatalogContextWhere only
    - New service test passes and documents DTO shape for CategoryGrid
    - buildCatalogContextWhere unchanged
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run src/server/services/catalog.service.test.ts</automated>
  </verify>
  <done>Service DTO includes image fields; catalog.service.test.ts green</done>
</task>

<task type="auto" id="25-01-03">
  <name>Task 3: Wire CategoryGrid to storefront pipeline</name>
  <read_first>
    - src/components/home/category-grid.tsx (full file — preserve card markup)
    - src/components/layout/store-header.tsx (lines 4-18 pipeline)
    - 25-CONTEXT.md D-01 through D-06
    - 25-UI-SPEC.md section visibility and grid rules
  </read_first>
  <files>src/components/home/category-grid.tsx</files>
  <action>
    Per D-01: remove `import { prisma } from "@/lib/db"` and raw `prisma.category.findMany`. Import `categoriesWithAvailableProducts` from `@/lib/catalog/categories` and `listCategoriesWithProductCounts` from `@/server/services/catalog.service`.

    Mirror StoreHeader data path:
    - `const { categories: categoriesWithCounts } = await listCategoriesWithProductCounts();`
    - `const categories = categoriesWithAvailableProducts(categoriesWithCounts);`

    Per D-04/D-05: immediately after filtering, `if (categories.length === 0) return null;` — before any `<section id="kategorii">` markup. No empty-state UI.

    Per D-06/D-07: keep existing section, h2 «Категорії», grid classes `grid grid-cols-2 gap-4 md:grid-cols-4`, and card loop unchanged (OptimizedImage, «Без фото», CardTitle, «Переглянути», Link to `/katalog/{slug}`). Do not add productCount badges.

    Per D-03: do not re-sort after filter; rely on listCategories sortOrder asc.

    Optional discretion: do not extract getStorefrontCategories() unless header is updated in the same commit — YAGNI default is inline pipeline only in CategoryGrid.
  </action>
  <acceptance_criteria>
    - category-grid.tsx has no prisma import or direct DB access
    - Early return null when filtered categories array is empty
    - Section and h2 render only when at least one category remains
    - Card UI and grid classes unchanged from pre-phase behavior
    - npm run build succeeds
    - Full unit suite passes
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run src/lib/catalog/categories.test.ts src/server/services/catalog.service.test.ts</automated>
    <automated>npm run build</automated>
  </verify>
  <done>CategoryGrid matches header filter pipeline; build and targeted tests green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server RSC → PostgreSQL | Read-only category/product queries via Prisma (unchanged auth surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Information disclosure | CategoryGrid | accept | Hiding empty categories is UX parity, not an access-control boundary |
| T-25-02 | Tampering | catalog.service queries | accept | No new user inputs; Prisma parameterized queries unchanged |
</threat_model>

<verification>
**Automated (required before phase sign-off):**
- `npm test -- --run src/lib/catalog/categories.test.ts src/server/services/catalog.service.test.ts`
- `npm test` (full suite)
- `npm run build`

**Manual (HOME-03 parity — after execute, optional for /gsd-verify-work):**
1. With dev DB after purge/seed: load `/` — if all categories empty, no `#kategorii` block and no orphan h2.
2. Compare header category links vs homepage cards — same category slugs visible on both surfaces.
3. With 1–3 categories: grid stays `grid-cols-2 md:grid-cols-4` without centering hacks.

**Source coverage (GOAL / HOME-03 / D-01–D-08 / RESEARCH):** All items addressed in tasks 25-01-01 through 25-01-03. Deferred: FOOT-04, sparse grid polish (CONTEXT deferred).
</verification>

<success_criteria>
- HOME-03 satisfied: homepage «Категорії» uses `listCategoriesWithProductCounts` + `categoriesWithAvailableProducts` identical to StoreHeader
- Empty filtered result yields `null` from CategoryGrid (no section shell)
- Image fields available on DTO so cards do not regress to «Без фото» for categories with images
- categories.test.ts and catalog.service.test.ts document filter/DTO contracts
- `npm run build` and `npm test` green
</success_criteria>

<output>
Create `.planning/phases/25-homepage-empty-categories/25-01-SUMMARY.md` when done
</output>
