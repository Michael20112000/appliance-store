---
phase: 33-admin-categories-dnd-links
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: false
requirements: [ADM-CAT-06]
user_setup: []

must_haves:
  truths:
    - "@dnd-kit/core, @dnd-kit/sortable, and @dnd-kit/utilities are resolvable in node_modules"
    - "TypeScript compiler can import from @dnd-kit/core without errors"
  artifacts:
    - path: "node_modules/@dnd-kit/core"
      provides: "DnD context and sensor primitives"
    - path: "node_modules/@dnd-kit/sortable"
      provides: "useSortable hook and SortableContext"
    - path: "node_modules/@dnd-kit/utilities"
      provides: "CSS.Transform.toString helper"
  key_links:
    - from: "src/components/admin/admin-categories-table.tsx"
      to: "@dnd-kit/core"
      via: "import { DndContext } from '@dnd-kit/core'"
      pattern: "dnd-kit/core"
    - from: "src/components/admin/admin-categories-table.tsx"
      to: "@dnd-kit/sortable"
      via: "import { useSortable } from '@dnd-kit/sortable'"
      pattern: "dnd-kit/sortable"
---

<objective>
Verify package legitimacy and install the three @dnd-kit packages required for the DnD implementation.

Purpose: ADM-CAT-06 depends on @dnd-kit/core, @dnd-kit/sortable, and @dnd-kit/utilities. RESEARCH.md Package Legitimacy Audit flagged both core packages as [ASSUMED] — a human checkpoint is required before install per GSD protocol.

Output: node_modules contains @dnd-kit/core 6.3.1, @dnd-kit/sortable 10.0.0, @dnd-kit/utilities 3.2.2; package.json updated.
</objective>

<execution_context>
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/PROJECT.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/ROADMAP.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/phases/33-admin-categories-dnd-links/33-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify @dnd-kit package legitimacy before install</name>
  <what-built>
    RESEARCH.md Package Legitimacy Audit identified two new packages as [ASSUMED] (slopcheck unavailable at research time). Per GSD protocol, a human must confirm legitimacy before npm install proceeds.

    Packages to verify:
    - @dnd-kit/core — 6.3.1 — github.com/clauderic/dnd-kit — ~16M downloads/week
    - @dnd-kit/sortable — 10.0.0 — github.com/clauderic/dnd-kit — ~13M downloads/week
  </what-built>
  <how-to-verify>
    1. Visit https://www.npmjs.com/package/@dnd-kit/core — confirm: publisher "clauderic", version 6.3.1 published ~2024-12-05, weekly downloads > 10M, GitHub link points to github.com/clauderic/dnd-kit
    2. Visit https://www.npmjs.com/package/@dnd-kit/sortable — confirm: same publisher "clauderic", version 10.0.0, weekly downloads > 10M, same monorepo
    3. Confirm neither package is a typosquat or clone of the expected repo
  </how-to-verify>
  <resume-signal>Type "approved" if both packages check out, or describe any concerns</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install @dnd-kit packages</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - /Users/michael_ivashko/WebStormProjects/web/appliance-store/package.json — to confirm @dnd-kit packages are not already listed
  </read_first>
  <action>
    Run: npm install @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities

    This installs @dnd-kit/core@6.3.1, @dnd-kit/sortable@10.0.0, and @dnd-kit/utilities@3.2.2 into dependencies. The explicit inclusion of @dnd-kit/utilities avoids relying on transitive resolution for CSS.Transform.toString (per D-03 and RESEARCH.md open question 2).

    After install, run: npm run build -- --dry-run (or tsc --noEmit if no dry-run flag) to confirm TypeScript can resolve the new packages without errors. If tsc --noEmit is available, prefer it.
  </action>
  <verify>
    <automated>node -e "require('@dnd-kit/core'); require('@dnd-kit/sortable'); require('@dnd-kit/utilities'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - package.json dependencies contains "@dnd-kit/core", "@dnd-kit/sortable", "@dnd-kit/utilities"
    - node_modules/@dnd-kit/core/package.json exists with version field "6.x.x"
    - node_modules/@dnd-kit/sortable/package.json exists with version field "10.x.x"
    - node_modules/@dnd-kit/utilities/package.json exists with version field "3.x.x"
    - `node -e "require('@dnd-kit/core'); require('@dnd-kit/sortable'); require('@dnd-kit/utilities'); console.log('ok')"` prints "ok" and exits 0
  </acceptance_criteria>
  <done>All three @dnd-kit packages installed and resolvable from node</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → node_modules | Third-party package code enters the project's build and runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering | npm install @dnd-kit/* | mitigate | Human checkpoint (Task 1) verifies publisher identity, download count, and GitHub repo match before install |
| T-33-SC | Tampering | npm install | mitigate | slopcheck was unavailable; human-verify checkpoint (Task 1) is the blocking gate — auto_advance is ignored for this task |
</threat_model>

<verification>
After Task 2:
- `node -e "require('@dnd-kit/core'); require('@dnd-kit/sortable'); require('@dnd-kit/utilities'); console.log('ok')"` exits 0
- package.json lists all three @dnd-kit packages under dependencies
</verification>

<success_criteria>
@dnd-kit/core, @dnd-kit/sortable, and @dnd-kit/utilities are installed, resolvable, and present in package.json dependencies. Phase 33 plans 02–04 can now import from these packages.
</success_criteria>

<output>
Create `.planning/phases/33-admin-categories-dnd-links/33-01-SUMMARY.md` when done
</output>
