---
phase: 33-admin-categories-dnd-links
plan: 02
type: execute
wave: 2
depends_on: [33-01]
files_modified:
  - src/server/services/admin-catalog.service.ts
  - src/server/services/admin-catalog-reorder.service.test.ts
autonomous: true
requirements: [ADM-CAT-06]
user_setup: []

must_haves:
  truths:
    - "reorderCategories(orderedIds) assigns sortOrder 1..n to each ID in array order"
    - "Vitest test for B-before-A scenario passes and asserts sortOrder calls in order"
    - "prisma.$transaction is used (not n sequential awaited updates)"
  artifacts:
    - path: "src/server/services/admin-catalog.service.ts"
      provides: "reorderCategories exported function"
      exports: ["reorderCategories"]
    - path: "src/server/services/admin-catalog-reorder.service.test.ts"
      provides: "Vitest unit tests for reorderCategories"
      contains: "describe(\"reorderCategories\""
  key_links:
    - from: "src/server/services/admin-catalog-reorder.service.test.ts"
      to: "src/server/services/admin-catalog.service.ts"
      via: "import { reorderCategories } from './admin-catalog.service'"
      pattern: "reorderCategories"
    - from: "src/server/services/admin-catalog.service.ts"
      to: "@/lib/db"
      via: "prisma.$transaction([...array])"
      pattern: "prisma\\.\\$transaction"
---

<objective>
Add the reorderCategories service method to admin-catalog.service.ts and write a Vitest unit test file for it.

Purpose: The server action added in Plan 03 calls reorderCategories. This plan delivers the service layer first so the action can import it cleanly. The Vitest test satisfies ROADMAP success criterion 3 (D-09).

Output: reorderCategories exported from admin-catalog.service.ts; admin-catalog-reorder.service.test.ts with two passing tests.
</objective>

<execution_context>
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/PROJECT.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/ROADMAP.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/phases/33-admin-categories-dnd-links/33-CONTEXT.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/phases/33-admin-categories-dnd-links/33-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/server/services/admin-catalog.service.ts (line 1-14):
```typescript
import { prisma } from "@/lib/db";
// prisma.$transaction([...array of prisma client operations]) — array form is used here
// prisma.category.update({ where: { id }, data: { sortOrder: index + 1 } })

export const CATEGORY_HAS_PRODUCTS = "CATEGORY_HAS_PRODUCTS";
export const SLUG_ALREADY_EXISTS = "SLUG_ALREADY_EXISTS";
export const CATEGORY_NOT_FOUND = "CATEGORY_NOT_FOUND";
```

From src/server/services/admin-catalog.service.test.ts (line 1-19):
```typescript
import { beforeEach, describe, expect, it, vi } from "vitest";
import { prisma } from "@/lib/db";

vi.mock("@/lib/db", () => ({
  prisma: {
    category: {
      findUnique: vi.fn(),
      update: vi.fn(),
    },
  },
}));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add reorderCategories to admin-catalog.service.ts</name>
  <files>src/server/services/admin-catalog.service.ts</files>
  <read_first>
    - /Users/michael_ivashko/WebStormProjects/web/appliance-store/src/server/services/admin-catalog.service.ts — read full file to find correct append location (after deleteCategory, before end of file) and confirm prisma import already present
  </read_first>
  <behavior>
    - reorderCategories(["a", "b", "c"]) calls prisma.$transaction with an array of 3 prisma.category.update operations
    - The first element in the array targets id "a" with data { sortOrder: 1 }
    - The second element targets id "b" with data { sortOrder: 2 }
    - reorderCategories(["b", "a"]) → id "b" gets sortOrder 1, id "a" gets sortOrder 2
    - reorderCategories([]) → prisma.$transaction called with empty array, no error thrown
    - Function returns Promise<void>
  </behavior>
  <action>
    Append the following exported function at the end of src/server/services/admin-catalog.service.ts, after the deleteCategory function:

    Export name: reorderCategories
    Signature: async function reorderCategories(orderedIds: string[]): Promise<void>
    Implementation: calls prisma.$transaction with the array-form (not callback form) — the argument is orderedIds.map((id, index) => prisma.category.update({ where: { id }, data: { sortOrder: index + 1 } }))

    This is the array form of $transaction (not the callback form with async tx => {}). The array form is sufficient because all updates are independent — no intermediate reads needed (per D-07 and RESEARCH.md Pitfall 4).

    Do not modify any existing functions. Place the new function after the last existing export in the file.
  </action>
  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && grep -n "export async function reorderCategories" src/server/services/admin-catalog.service.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/server/services/admin-catalog.service.ts contains `export async function reorderCategories(orderedIds: string[]): Promise<void>`
    - The function body calls `prisma.$transaction(` with an array argument (not a callback)
    - The map produces `prisma.category.update({ where: { id }, data: { sortOrder: index + 1 } })` for each element
    - No existing functions in the file are modified
  </acceptance_criteria>
  <done>reorderCategories exported from admin-catalog.service.ts</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write Vitest unit tests for reorderCategories</name>
  <files>src/server/services/admin-catalog-reorder.service.test.ts</files>
  <read_first>
    - /Users/michael_ivashko/WebStormProjects/web/appliance-store/src/server/services/admin-catalog.service.test.ts — read to confirm the vi.mock("@/lib/db") pattern and import style to replicate exactly
    - /Users/michael_ivashko/WebStormProjects/web/appliance-store/src/server/services/admin-catalog.service.ts — confirm the reorderCategories export was added (from Task 1)
  </read_first>
  <behavior>
    - Test "assigns sortOrder 1..n based on orderedIds array position": call reorderCategories(["a","b","c"]), assert prisma.$transaction called once with array of length 3
    - Test "placing B before A assigns B sortOrder 1 and A sortOrder 2": configure prisma.$transaction to resolve array form by calling Promise.all(ops), call reorderCategories(["b","a"]), assert prisma.category.update called first with { where: { id: "b" }, data: { sortOrder: 1 } } and second with { where: { id: "a" }, data: { sortOrder: 2 } }
  </behavior>
  <action>
    Create a new file at src/server/services/admin-catalog-reorder.service.test.ts.

    The file must:
    1. Import { beforeEach, describe, expect, it, vi } from "vitest"
    2. Import { prisma } from "@/lib/db"
    3. Import { reorderCategories } from "./admin-catalog.service"
    4. Call vi.mock("@/lib/db", ...) with a factory that mocks prisma.category.update as vi.fn() and prisma.$transaction as vi.fn()
    5. Inside describe("reorderCategories"), call beforeEach(() => vi.clearAllMocks())

    Test 1 — "assigns sortOrder 1..n based on orderedIds array position":
    - vi.mocked(prisma.$transaction).mockResolvedValue([])
    - await reorderCategories(["a", "b", "c"])
    - expect(prisma.$transaction).toHaveBeenCalledTimes(1)
    - const calls = vi.mocked(prisma.$transaction).mock.calls[0][0]
    - expect(Array.isArray(calls)).toBe(true)
    - expect(calls).toHaveLength(3)

    Test 2 — "placing B before A assigns B sortOrder 1 and A sortOrder 2":
    - vi.mocked(prisma.category.update).mockResolvedValueOnce({ id: "b", sortOrder: 1 } as never).mockResolvedValueOnce({ id: "a", sortOrder: 2 } as never)
    - vi.mocked(prisma.$transaction).mockImplementation(async (ops: unknown) => { if (Array.isArray(ops)) return Promise.all(ops); return (ops as (p: typeof prisma) => Promise<unknown>)(prisma); })
    - await reorderCategories(["b", "a"])
    - expect(prisma.category.update).toHaveBeenNthCalledWith(1, expect.objectContaining({ where: { id: "b" }, data: { sortOrder: 1 } }))
    - expect(prisma.category.update).toHaveBeenNthCalledWith(2, expect.objectContaining({ where: { id: "a" }, data: { sortOrder: 2 } }))

    Do not include fenced code blocks in the file — write the actual TypeScript content.
  </action>
  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npm test -- src/server/services/admin-catalog-reorder.service.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File src/server/services/admin-catalog-reorder.service.test.ts exists
    - File contains `describe("reorderCategories"` with exactly 2 `it(` blocks
    - `npm test -- src/server/services/admin-catalog-reorder.service.test.ts` exits 0 with "2 passed" in output
    - No existing test files are modified
  </acceptance_criteria>
  <done>Both reorderCategories Vitest tests pass; npm test exits 0 for this file</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server action → service | orderedIds array passes from action into reorderCategories before DB write |
| service → Prisma | Each element of orderedIds passed as WHERE id clause — parameterized by Prisma |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-02 | Tampering | reorderCategories — orderedIds elements | accept | Prisma parameterizes `where: { id }` — arbitrary string values do not produce SQL injection; Prisma throws RecordNotFound if ID is unknown, which bubbles as error to action which returns { ok: false } |
| T-33-03 | Tampering | reorderCategories — orderedIds not-array | mitigate | Validation performed in reorderCategoriesAction (Plan 03) before service call; service itself trusts validated input from action |
</threat_model>

<verification>
- `grep -n "export async function reorderCategories" src/server/services/admin-catalog.service.ts` returns a match
- `npm test -- src/server/services/admin-catalog-reorder.service.test.ts` exits 0 with "2 passed"
- `npm test` (full suite) exits 0
</verification>

<success_criteria>
reorderCategories is exported from admin-catalog.service.ts and its correctness is proven by two passing Vitest tests that assert sortOrder assignment order. Plan 03 can now import reorderCategories.
</success_criteria>

<output>
Create `.planning/phases/33-admin-categories-dnd-links/33-02-SUMMARY.md` when done
</output>
