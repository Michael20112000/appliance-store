---
phase: 31-order-status-ux-bugfix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/order/admin-status-errors.ts
  - src/lib/order/admin-status-errors.test.ts
  - src/lib/order/status-styles.ts
  - src/lib/order/status-styles.test.ts
  - src/components/admin/order-list-status-select.tsx
  - src/components/admin/order-status-select.tsx
  - src/components/admin/order-list-status-select.test.tsx
  - src/server/services/admin-order.service.test.ts
autonomous: true
requirements:
  - ORD-05
  - BUG-24
must_haves:
  truths:
    - "List select shows «Недостатньо товару на складі для підтвердження» (not UNKNOWN) when server returns INSUFFICIENT_STOCK (D-03, D-06)"
    - "Stock error toast includes actionable hint to restore qty or cancel order (D-07)"
    - "CONFIRMED still requires successful reserve — no bypass when product.quantity is 0 (D-05)"
    - "Status SelectTrigger shows distinct light accent per OrderStatus on list and detail (D-09, ORD-05)"
    - "«Підтверджено (поточний)» is not clipped in list select at min-w ~14rem (D-10, ORD-05)"
    - "Vitest covers list error mapping and service PENDING→CONFIRMED stock failure (D-11)"
    - "ASL-20260519-0013 root cause documented; confirm succeeds only after qty restored (D-01, D-02, D-04, D-08, D-12)"
  artifacts:
    - path: src/lib/order/admin-status-errors.ts
      provides: ORDER_STATUS_ERROR_MESSAGES, showOrderStatusErrorToast
      exports: ["ORDER_STATUS_ERROR_MESSAGES", "showOrderStatusErrorToast"]
    - path: src/lib/order/status-styles.ts
      provides: getOrderStatusAccentClass
      exports: ["getOrderStatusAccentClass"]
    - path: src/components/admin/order-list-status-select.tsx
      provides: list select with shared errors, accents, width
    - path: src/components/admin/order-status-select.tsx
      provides: detail select parity with list
  key_links:
    - from: src/components/admin/order-list-status-select.tsx
      to: src/lib/order/admin-status-errors.ts
      via: showOrderStatusErrorToast on !result.ok
      pattern: "showOrderStatusErrorToast"
    - from: src/components/admin/order-list-status-select.tsx
      to: src/lib/order/status-styles.ts
      via: cn(..., getOrderStatusAccentClass(status)) on SelectTrigger
      pattern: "getOrderStatusAccentClass"
    - from: src/server/services/admin-order.service.ts
      to: src/server/services/product-inventory.ts
      via: reserveProductUnitsForOrder on PENDING→CONFIRMED (unchanged)
      pattern: "reserveProductUnitsForOrder"
---

<objective>
Close BUG-24 (ASL-20260519-0013) operator-facing error mapping and deliver ORD-05 status UX on admin order selects.

Purpose: Operators see correct stock errors and status at a glance; server stock rules stay unchanged per D-05.
Output: Shared error + accent helpers, updated list/detail selects, Vitest regression, documented root cause for ASL-20260519-0013.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-order-status-ux-bugfix/31-CONTEXT.md
@.planning/phases/31-order-status-ux-bugfix/31-RESEARCH.md
@.planning/phases/31-order-status-ux-bugfix/31-UI-SPEC.md
@src/components/admin/order-list-status-select.tsx
@src/components/admin/order-status-select.tsx
@src/components/admin/order-list-status-select.test.tsx
@src/server/services/admin-order.service.ts
@src/server/services/admin-order.service.test.ts
@src/server/actions/admin/order.actions.ts
@src/lib/order/status-labels.ts

<interfaces>
From `src/server/actions/admin/order.actions.ts` (unchanged contract):
- `updateOrderStatusAction({ orderId, status })` → `{ ok: true }` | `{ ok: false, error: "INSUFFICIENT_STOCK" | "INVALID_STATUS_TRANSITION" | "ORDER_NOT_FOUND" | ... }`

From `src/components/admin/order-status-select.tsx` (reference — detail already maps stock):
- `INSUFFICIENT_STOCK` → title «Недостатньо товару на складі для підтвердження.»

From `src/components/admin/order-list-status-select.tsx` (bug — missing key):
- `errorMessages` lacks `INSUFFICIENT_STOCK` → falls through to `UNKNOWN`

From `31-UI-SPEC.md`:
- Accents: `getOrderStatusAccentClass(status)` per status table (amber/sky/violet/emerald/red + dark variants)
- Width: list `min-w-[14rem] w-auto max-w-[18rem] whitespace-nowrap`; detail `min-w-[14rem] w-full max-w-md`
- Stock toast: `toast.error(title, { description: "Збільште кількість товару в адмінці або скасуйте замовлення." })` for `INSUFFICIENT_STOCK` only
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin session → `updateOrderStatusAction` | Only authenticated admins change order status |
| Client error mapping | UI is not security; server must reject invalid confirms |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering | `updateOrderStatus` / confirm path | mitigate | Do **not** add confirm-without-reserve or skip reserve when qty=0 (D-05); keep `reserveProductUnitsForOrder` on `PENDING→CONFIRMED` |
| T-31-02 | Spoofing | Crafted action with `CONFIRMED` on zero-stock order | mitigate | Server already throws `INSUFFICIENT_STOCK`; action maps to stable code — verify test in Task 3 |
| T-31-03 | Information disclosure | Toast messages | accept | UA operator hints only; no PII beyond existing order context |
| T-31-04 | Denial of service | N/A | accept | No new endpoints |
| T-31-SC | Tampering | npm installs | accept | No new packages this phase |

**Blockers:** Any proposal to bypass `reserveProductUnitsForOrder` on confirm is **high severity** and must be rejected (violates D-05, BUG-15, ASVS L1 integrity).
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Shared status error + accent helpers</name>
  <read_first>
- src/components/admin/order-status-select.tsx (lines 29–34 — canonical INSUFFICIENT_STOCK copy)
- src/components/admin/order-list-status-select.tsx (lines 28–32 — missing INSUFFICIENT_STOCK)
- .planning/phases/31-order-status-ux-bugfix/31-UI-SPEC.md (accent table + toast description)
- src/lib/order/status-labels.ts
  </read_first>
  <files>
- src/lib/order/admin-status-errors.ts
- src/lib/order/admin-status-errors.test.ts
- src/lib/order/status-styles.ts
- src/lib/order/status-styles.test.ts
  </files>
  <action>
Create `src/lib/order/admin-status-errors.ts`:

1. Export `ORDER_STATUS_ERROR_MESSAGES: Record&lt;string, string&gt;` with keys at minimum: `INVALID_STATUS_TRANSITION`, `INSUFFICIENT_STOCK`, `ORDER_NOT_FOUND`, `UNKNOWN` — UA strings matching detail select today; `INSUFFICIENT_STOCK` title per D-06 (with period as in detail).
2. Export `ORDER_STATUS_ERROR_DESCRIPTIONS: Partial&lt;Record&lt;string, string&gt;&gt;` with only `INSUFFICIENT_STOCK`: «Збільште кількість товару в адмінці або скасуйте замовлення.» (D-07).
3. Export `showOrderStatusErrorToast(errorCode: string): void` — import `toast` from `sonner`; resolve title from map; if description exists for code, call `toast.error(title, { description })`, else `toast.error(title)`.

Create `src/lib/order/status-styles.ts`:

1. Import `OrderStatus` from `@/generated/prisma/client`.
2. Export `getOrderStatusAccentClass(status: OrderStatus): string` returning Tailwind classes per 31-UI-SPEC.md (trigger-only accents): PENDING amber, CONFIRMED sky, READY_FOR_PICKUP + OUT_FOR_DELIVERY violet, COMPLETED emerald, CANCELLED red; include `dark:` variants from UI-SPEC.

Add Vitest files:
- `admin-status-errors.test.ts`: `showOrderStatusErrorToast("INSUFFICIENT_STOCK")` calls sonner with title + description; unknown code uses UNKNOWN title.
- `status-styles.test.ts`: each `OrderStatus` returns non-empty string containing expected hue token (e.g. `amber` for PENDING).

Do **not** change `status-transitions.ts`, `product-inventory.ts`, or checkout reservation policy.
  </action>
  <verify>
    <automated>npx vitest run src/lib/order/admin-status-errors.test.ts src/lib/order/status-styles.test.ts</automated>
  </verify>
  <acceptance_criteria>
- `admin-status-errors.ts` exports `ORDER_STATUS_ERROR_MESSAGES`, `showOrderStatusErrorToast`
- `INSUFFICIENT_STOCK` maps to «Недостатньо товару на складі для підтвердження.» and has description hint (D-06, D-07)
- `status-styles.ts` exports `getOrderStatusAccentClass` covering all six `OrderStatus` values (D-09)
- Both new test files pass under vitest
  </acceptance_criteria>
  <done>Shared helpers exist with unit tests green; list/detail can import one source of truth for errors and accents.</done>
</task>

<task type="auto">
  <name>Task 2: Wire list + detail selects (BUG-24 + ORD-05)</name>
  <read_first>
- src/lib/order/admin-status-errors.ts (Task 1)
- src/lib/order/status-styles.ts (Task 1)
- src/components/admin/order-list-status-select.tsx
- src/components/admin/order-status-select.tsx
- src/components/admin/order-list-status-select.test.tsx
- .planning/phases/31-order-status-ux-bugfix/31-UI-SPEC.md (width + accent on trigger only)
  </read_first>
  <files>
- src/components/admin/order-list-status-select.tsx
- src/components/admin/order-status-select.tsx
- src/components/admin/order-list-status-select.test.tsx
  </files>
  <action>
**order-list-status-select.tsx** (per D-03, D-06, D-09, D-10):

1. Remove local `errorMessages` object; on `!result.ok` call `showOrderStatusErrorToast(result.error)` from `@/lib/order/admin-status-errors`.
2. Import `cn` from `@/lib/utils` and `getOrderStatusAccentClass` from `@/lib/order/status-styles`.
3. On **both** `SelectTrigger` branches (disabled-only at ~line 60 and interactive at ~line 105): replace `w-[11rem]` with `min-w-[14rem] w-auto max-w-[18rem] whitespace-nowrap` and add `cn(getOrderStatusAccentClass(status))`.

**order-status-select.tsx** (parity D-06, D-09, D-10):

1. Remove local `errorMessages`; use `showOrderStatusErrorToast` on failure.
2. Apply `cn(getOrderStatusAccentClass(order.status))` on `SelectTrigger`; replace `max-w-xs` with `min-w-[14rem] w-full max-w-md whitespace-nowrap`.

**order-list-status-select.test.tsx** (D-11):

1. Mock `updateOrderStatusAction` to resolve `{ ok: false, error: "INSUFFICIENT_STOCK" }`.
2. Render `OrderListStatusSelect` with `status="PENDING"`, `deliveryType="PICKUP"`, open select, choose CONFIRMED (or trigger apply path that calls action).
3. Assert `toast.error` called with stock title string (not UNKNOWN generic «Не вдалося оновити статус»).
4. Assert description hint passed for INSUFFICIENT_STOCK (second arg object or combined per sonner mock).

Keep existing stopPropagation and OUT_FOR_DELIVERY pickup tests; update mocks if toast import path changes.

Do **not** change transition matrix, cancel dialog, or `orders-data-table.tsx` row styling (trigger-only per D-09 discretion).
  </action>
  <verify>
    <automated>npx vitest run src/components/admin/order-list-status-select.test.tsx src/lib/order/admin-status-errors.test.ts</automated>
  </verify>
  <acceptance_criteria>
- List select no longer maps `INSUFFICIENT_STOCK` to UNKNOWN (D-03)
- Detail select uses same `showOrderStatusErrorToast` as list (D-06); detail parity covered by shared `admin-status-errors` tests + same helper import (no separate detail component test required)
- Both triggers use `getOrderStatusAccentClass` and min-w ~14rem (D-09, D-10, ORD-05)
- New/updated component test fails before fix and passes after (INSUFFICIENT_STOCK toast)
- `npx vitest run src/components/admin/order-list-status-select.test.tsx` exits 0
  </acceptance_criteria>
  <done>Admin list and detail status selects share error/accent/width behavior; BUG-24 list mapping fixed.</done>
</task>

<task type="auto">
  <name>Task 3: Service stock regression + phase verification</name>
  <read_first>
- src/server/services/admin-order.service.ts (`updateOrderStatus`, reserve on PENDING→CONFIRMED)
- src/server/services/admin-order.service.test.ts (`updateOrderStatus` describe, product-inventory mock)
- .planning/phases/31-order-status-ux-bugfix/31-CONTEXT.md (D-01, D-02, D-05, D-08, D-11, D-12)
  </read_first>
  <files>
- src/server/services/admin-order.service.test.ts
  </files>
  <action>
**admin-order.service.test.ts** (D-11):

1. Import `INSUFFICIENT_STOCK` and mocked `reserveProductUnitsForOrder`, `shouldReserveInventoryOnTransition` from `@/server/services/product-inventory`.
2. Add test: `updateOrderStatus` with order `status: "PENDING"`, `deliveryType: "PICKUP"`, non-empty `items`; mock `shouldReserveInventoryOnTransition` to return `true`; mock `reserveProductUnitsForOrder` to reject/throw with message `INSUFFICIENT_STOCK` (match `product-inventory` throw pattern).
3. Expect `updateOrderStatus(orderId, "CONFIRMED")` rejects with `INSUFFICIENT_STOCK` and **does not** call `tx.order.update` (or transaction rolls back without status change).
4. Assert `reserveProductUnitsForOrder` was called (e.g. `toHaveBeenCalledWith` matching order id/items) before failure — guards against silent removal of reserve on confirm (D-05).

Do **not** modify `admin-order.service.ts` business logic unless test reveals a regression; reserve path must remain mandatory (D-05).

**Documentation in SUMMARY** (executor): Record root cause for ASL-20260519-0013 per D-01/D-02/D-04 — PENDING pickup order, product qty 0 at confirm; explain likely scenario: customer ordered while qty≥1, later admin set product qty to 0 or another confirmed order consumed the last unit (D-04); operator may restore qty in `/admin/tovary/[id]` then confirm (D-08).

Run from repo root: `npm test` then `npm run build`. Fix only phase-31-related failures.
  </action>
  <verify>
    <automated>npx vitest run src/server/services/admin-order.service.test.ts && npm test && npm run build</automated>
    <human-check>
1. `/admin/zamovlennia` — find ASL-20260519-0013 (or seed PENDING order with line product qty 0).
2. List select: attempt Підтверджено → toast shows stock message + hint, **not** «Не вдалося оновити статус…» (D-12).
3. Restore product `quantity ≥ 1` in admin product edit → confirm succeeds (D-08).
4. Visual: each status trigger has distinct light accent; «Підтверджено (поточний)» fully visible (ORD-05).
    </human-check>
  </verify>
  <acceptance_criteria>
- Service test proves PENDING→CONFIRMED throws `INSUFFICIENT_STOCK` when reserve fails (D-11)
- Service test asserts `reserveProductUnitsForOrder` was invoked on that path (D-05)
- No code path skips reserve on confirm (D-05)
- `npm test` and `npm run build` exit 0
- SUMMARY documents ASL-20260519-0013 root cause including D-04 data scenario and valid confirm path after qty fix (D-01, D-02, D-04, D-08)
  </acceptance_criteria>
  <done>Automated regression suite green; manual ASL checklist recorded for phase UAT.</done>
</task>

</tasks>

<verification>
## Source audit

| SOURCE | ID | Feature | Plan | Status |
|--------|-----|---------|------|--------|
| GOAL | — | Status at a glance + BUG-24 closed | 01 | COVERED |
| REQ | ORD-05 | Colors + select width | 01 Task 2 | COVERED |
| REQ | BUG-24 | ASL confirm / stock error fix | 01 Task 2–3 | COVERED |
| RESEARCH | — | Shared error helper | 01 Task 1 | COVERED |
| RESEARCH | — | Status accents | 01 Task 1–2 | COVERED |
| RESEARCH | — | Vitest list + service | 01 Task 2–3 | COVERED |
| CONTEXT | D-01–D-12 | All locked decisions | 01 | COVERED |
| CONTEXT | Deferred | Soft reservation, qty guard | — | N/A (deferred) |

## Commands

```bash
npx vitest run src/lib/order/admin-status-errors.test.ts src/lib/order/status-styles.test.ts
npx vitest run src/components/admin/order-list-status-select.test.tsx
npx vitest run src/server/services/admin-order.service.test.ts
npm test
npm run build
```
</verification>

<success_criteria>
1. ORD-05: Status triggers show light per-status accents; long UA label not clipped (min-w ~14rem).
2. BUG-24: List and detail show stock UA message + hint for `INSUFFICIENT_STOCK`; ASL-20260519-0013 documented; confirm works after qty restored, not via reserve bypass.
3. ROADMAP criterion 4: Vitest regression for error mapping and stock-at-zero on PENDING→CONFIRMED.
4. Transition matrix and checkout stock policy unchanged.
</success_criteria>

<output>
Create `.planning/phases/31-order-status-ux-bugfix/31-01-SUMMARY.md` when done
</output>
