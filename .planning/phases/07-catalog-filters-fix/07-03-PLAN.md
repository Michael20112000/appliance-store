---
phase: 07-catalog-filters-fix
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/components/catalog/catalog-brand-param-guard.tsx
  - src/components/catalog/active-filter-chips.tsx
  - src/components/catalog/catalog-toolbar.tsx
  - src/app/(storefront)/katalog/[slug]/page.tsx
autonomous: true
requirements:
  - CAT-03
context_decisions:
  - "D-07-10: preserve brend when valid on category change"
  - "D-07-11: silent replace drop invalid brend"
  - "D-07-12: active chips above grid"
  - "D-07-13: nuqs sync only"

must_haves:
  truths:
    - "Invalid brend in category URL is removed without toast"
    - "Active chips show brand, price range, and conditions"
    - "Removing a chip updates URL and resets page when appropriate"
  artifacts:
    - path: src/components/catalog/active-filter-chips.tsx
      provides: removable filter chips
    - path: src/components/catalog/catalog-brand-param-guard.tsx
      provides: D-07-11 enforcement
  key_links:
    - from: catalog-toolbar.tsx
      to: active-filter-chips.tsx
      pattern: "ActiveFilterChips"

---

## Phase Goal

**As a** покупець, **I want** бачити активні фільтри й не застрягати на невалідному бренді, **so that** каталог завжди показує коректні товари.

<objective>
Додати guard для невалідного `brend` на категорійних сторінках і активні чіпси фільтрів у toolbar.
</objective>

<context>
@.planning/phases/07-catalog-filters-fix/07-CONTEXT.md
@.planning/phases/07-catalog-filters-fix/07-UI-SPEC.md
@src/components/catalog/catalog-toolbar.tsx
@src/lib/catalog/search-params.ts
@src/lib/catalog/format.ts
</context>

<threat_model>
| ID | Threat | Mitigation |
|----|--------|------------|
| T-07-03-01 | Open redirect via URL | Only mutate known nuqs keys; no external URLs |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: CatalogBrandParamGuard (D-07-11)</name>
  <read_first>
    - src/app/(storefront)/katalog/[slug]/page.tsx
    - src/lib/catalog/search-params.ts
    - .planning/phases/07-catalog-filters-fix/07-RESEARCH.md — client replace pattern
  </read_first>
  <files>src/components/catalog/catalog-brand-param-guard.tsx, src/app/(storefront)/katalog/[slug]/page.tsx</files>
  <action>
    Create client component `CatalogBrandParamGuard` props: `brands: string[]`.
    Uses `useQueryStates(catalogParsers, { urlKeys: catalogUrlKeys, shallow: false })`.
    `useEffect`: if `params.brend` is non-null and not in `brands`, call `setParams({ brend: null, storinka: 1 }, { history: 'replace' })`.
    No toast.
    Mount on category catalog page only (not global /katalog — all brands valid there).
    D-07-10: preserving brend across category navigation is default Link behavior when brand exists in new list — no extra code unless broken; document in summary if verified.
  </action>
  <acceptance_criteria>
    - `catalog-brand-param-guard.tsx` exists as client component
    - Category page renders guard with `brands` prop from server
    - Effect uses `history: 'replace'` (or nuqs equivalent) when clearing invalid brend
    - No `toast()` calls in file
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ActiveFilterChips (D-07-12, D-07-13)</name>
  <read_first>
    - src/components/catalog/catalog-toolbar.tsx
    - src/lib/catalog/format.ts — formatPriceKopiyky, conditionLabelUa
    - .planning/phases/07-catalog-filters-fix/07-UI-SPEC.md — chips section
    - src/components/ui/badge.tsx (if exists) or button styling from project
  </read_first>
  <files>src/components/catalog/active-filter-chips.tsx, src/components/catalog/catalog-toolbar.tsx</files>
  <action>
    Create `ActiveFilterChips` client component:
    - Read `useQueryStates` with same parsers/keys
    - Render chip when: `brend` set; `cinaVid` and/or `cinaDo` set (one combined chip); each `stan` value
    - Labels: brand name; price `від X ₴` / `до Y ₴` / range using formatPriceKopiyky on kopiyky (UAH×100)
    - Dismiss: clear respective param(s); `storinka: 1`
    - Use Badge + button × with min height 44px touch target
    Integrate in `CatalogToolbar` above search row (`flex-col` first child).
  </action>
  <acceptance_criteria>
    - `active-filter-chips.tsx` exists
    - `catalog-toolbar.tsx` renders chips when any filter active
    - Dismissing brand chip sets `brend: null`
    - Dismissing price chip clears both `cinaVid` and `cinaDo`
    - `npm run lint` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
npm run lint
```
Manual: `07-MANUAL-CHECKLIST.md` §3–4.
</verification>
