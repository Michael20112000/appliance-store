---
phase: 07-catalog-filters-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/services/catalog.service.ts
  - src/server/services/catalog.service.test.ts
  - src/app/(storefront)/katalog/page.tsx
  - src/app/(storefront)/katalog/[slug]/page.tsx
  - src/components/catalog/catalog-filters.tsx
  - src/components/ui/slider.tsx
autonomous: true
requirements:
  - CAT-03
context_decisions:
  - "D-07-08: getDistinctBrands(categoryId?)"
  - "D-07-09: category pages pass category.id"
  - "D-07-03: price bounds from AVAILABLE products in context"
  - "D-07-14: Vitest for getDistinctBrands and bounds"

must_haves:
  truths:
    - "Category catalog pages only list brands that have products in that category"
    - "Global /katalog lists all AVAILABLE brands"
    - "Both catalog pages receive min/max UAH bounds for price UI"
    - "shadcn slider component exists in components/ui"
  artifacts:
    - path: src/server/services/catalog.service.ts
      provides: getDistinctBrands(categoryId?), getCatalogPriceBounds(categoryId?)
    - path: src/components/ui/slider.tsx
      provides: shadcn Slider primitive
  key_links:
    - from: katalog/[slug]/page.tsx
      to: getDistinctBrands
      via: category.id argument
      pattern: "getDistinctBrands(category.id)"

---

## Phase Goal

**As a** покупець, **I want** бачити лише релевантні бренди в категорії, **so that** не обираю Bosch на сторінці телефонів.

<objective>
Додати серверні хелпери для брендів і меж цін, підключити їх на обох сторінках каталогу, встановити shadcn `slider`, передати `priceBounds` у `CatalogFilters` (UI слайдера — план 07-02).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-catalog-filters-fix/07-CONTEXT.md
@.planning/phases/07-catalog-filters-fix/07-RESEARCH.md
@.planning/phases/07-catalog-filters-fix/07-PATTERNS.md
@src/server/services/catalog.service.ts
@src/server/services/catalog.service.test.ts
</context>

<threat_model>
| ID | Threat | Mitigation |
|----|--------|------------|
| T-07-01-01 | Information disclosure via brand list | Only AVAILABLE products; same as public catalog |
| T-07-01-02 | DoS via heavy aggregate | Single aggregate per page load; indexed `status, price` |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn slider</name>
  <read_first>
    - .planning/phases/07-catalog-filters-fix/07-UI-SPEC.md — shadcn add command
    - components.json — project shadcn config
  </read_first>
  <files>src/components/ui/slider.tsx</files>
  <action>
    Run `npx shadcn@latest add slider` (non-interactive flags per project convention).
    Verify `src/components/ui/slider.tsx` exists and exports `Slider`.
    Do not modify globals.css tokens beyond what the CLI adds.
  </action>
  <acceptance_criteria>
    - `src/components/ui/slider.tsx` exists
    - File exports `Slider` component
    - `npm run lint` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: getDistinctBrands(categoryId?) and getCatalogPriceBounds(categoryId?)</name>
  <read_first>
    - src/server/services/catalog.service.ts — buildPublicProductWhere, getDistinctBrands
    - src/server/services/catalog.service.test.ts — existing buildPublicProductWhere tests
    - .planning/phases/07-catalog-filters-fix/07-RESEARCH.md — aggregate pattern
  </read_first>
  <files>src/server/services/catalog.service.ts, src/server/services/catalog.service.test.ts</files>
  <action>
    Change `getDistinctBrands` signature to `getDistinctBrands(categoryId?: string)`.
    When `categoryId` provided, add `categoryId` to Prisma `where` alongside `status: AVAILABLE`.
    Add `getCatalogPriceBounds(categoryId?: string)` returning `{ minUah: number; maxUah: number } | null`:
    - Use `prisma.product.aggregate` with `_min`/`_max` on `price` (kopiyky)
    - Reuse same status/category filter as public listing
    - Convert to UAH integers: `Math.floor(min/100)`, `Math.ceil(max/100)`
    - Return `null` when no products
    Add Vitest:
    - `getDistinctBrands` with mocked prisma OR pure where-builder test if prisma mocked — prefer testing exported where logic via spy on findMany args, or integration-style with existing test patterns in file
    - At minimum: test that `buildPublicProductWhere` + document getDistinctBrands filter shape in new tests using prisma mock if project uses it; else add tests for price bounds pure function extracted if needed
    Follow existing `catalog.service.test.ts` style (direct `buildPublicProductWhere` tests + any service tests present).
  </action>
  <acceptance_criteria>
    - `getDistinctBrands` accepts optional `categoryId` in TypeScript signature
    - `getCatalogPriceBounds` exported from catalog.service.ts
    - `catalog.service.test.ts` includes test for price gte-only via buildPublicProductWhere with minPrice only
    - `catalog.service.test.ts` includes test describing category-scoped brand query (findMany where includes categoryId when passed)
    - `npm test -- src/server/services/catalog.service.test.ts` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Wire catalog pages and filter props</name>
  <read_first>
    - src/app/(storefront)/katalog/page.tsx
    - src/app/(storefront)/katalog/[slug]/page.tsx
    - src/components/catalog/catalog-filters.tsx
  </read_first>
  <files>
    src/app/(storefront)/katalog/page.tsx,
    src/app/(storefront)/katalog/[slug]/page.tsx,
    src/components/catalog/catalog-filters.tsx
  </files>
  <action>
    On `/katalog`: `getDistinctBrands()` no arg; `getCatalogPriceBounds()` no arg.
    On `/katalog/[slug]`: pass `category.id` to both helpers.
    Add optional prop to `CatalogFilters`: `priceBounds: { minUah: number; maxUah: number } | null`.
  </action>
  <acceptance_criteria>
    - `katalog/[slug]/page.tsx` calls `getDistinctBrands(category.id)` and `getCatalogPriceBounds(category.id)`
    - `katalog/page.tsx` calls helpers without categoryId
    - `CatalogFilters` type includes `priceBounds` prop (may be unused until 07-02)
    - `npm run lint` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
npm test -- src/server/services/catalog.service.test.ts
npm run lint
```
</verification>
