---
phase: 07-catalog-filters-fix
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/components/catalog/catalog-filters.tsx
  - src/components/catalog/catalog-filters-sheet.tsx
  - src/app/(storefront)/katalog/page.tsx
  - src/app/(storefront)/katalog/[slug]/page.tsx
  - src/lib/catalog/throttle.ts
autonomous: true
requirements:
  - CAT-01
  - CAT-02
context_decisions:
  - "D-07-01: dual-thumb Slider + inputs"
  - "D-07-02: step 50 UAH"
  - "D-07-04: mobile same Slider in sheet"
  - "D-07-05: throttle 200ms"
  - "D-07-06: storinka 1 on price change"

must_haves:
  truths:
    - "Price section shows dual-thumb Slider synced with від/до inputs"
    - "Slider drag updates cina-vid/cina-do in URL at most every 200ms"
    - "Any price change resets сторінка to 1"
    - "Mobile users open filter sheet with same price Slider"
  artifacts:
    - path: src/components/catalog/catalog-filters.tsx
      provides: Slider + inputs
    - path: src/components/catalog/catalog-filters-sheet.tsx
      provides: mobile filter entry
  key_links:
    - from: catalog-filters.tsx
      to: catalogParsers
      via: useQueryStates
      pattern: "cinaVid"

---

## Phase Goal

**As a** покупець, **I want** зручно обрати діапазон ціни, **so that** бачу лише товари в бюджеті.

<objective>
Реалізувати dual-thumb Slider (крок 50 ₴), синхронізацію з URL, throttle 200ms, mobile filter sheet з тим самим UI.
</objective>

<context>
@.planning/phases/07-catalog-filters-fix/07-CONTEXT.md
@.planning/phases/07-catalog-filters-fix/07-UI-SPEC.md
@.planning/phases/07-catalog-filters-fix/07-RESEARCH.md
@src/components/catalog/catalog-filters.tsx
@src/components/ui/slider.tsx
</context>

<threat_model>
| ID | Threat | Mitigation |
|----|--------|------------|
| T-07-02-01 | URL spam on drag | throttle 200ms (D-07-05) |
| T-07-02-02 | Invalid range (min > max) | clamp thumbs; swap or constrain max ≥ min |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared filter panel and throttle helper</name>
  <read_first>
    - src/components/catalog/catalog-filters.tsx
    - .planning/phases/07-catalog-filters-fix/07-RESEARCH.md — throttle pattern
  </read_first>
  <files>src/lib/catalog/throttle.ts, src/components/catalog/catalog-filters.tsx</files>
  <action>
    Add `src/lib/catalog/throttle.ts` with `createThrottle(ms)` returning `(fn: () => void) => void` for 200ms trailing/throttled invoke.
    Refactor `CatalogFilters` to export inner `CatalogFiltersPanel` (same props) used by sidebar and sheet.
    Panel accepts `priceBounds: { minUah; maxUah } | null`.
  </action>
  <acceptance_criteria>
    - `src/lib/catalog/throttle.ts` exports throttle helper
    - `CatalogFiltersPanel` exported or co-located for reuse
    - Existing desktop aside still renders full filter panel
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Dual-thumb Slider with step 50 and URL sync</name>
  <read_first>
    - src/components/ui/slider.tsx — Slider API
    - src/lib/catalog/search-params.ts — cinaVid, cinaDo parsers
    - .planning/phases/07-catalog-filters-fix/07-UI-SPEC.md
  </read_first>
  <files>src/components/catalog/catalog-filters.tsx</files>
  <action>
    In price section replace inputs-only with:
    - `Slider` `min={priceBounds.minUah}` `max={priceBounds.maxUah}` `step={50}` dual values `[cinaVid ?? min, cinaDo ?? max]` (when one bound null use min/max defaults for thumb position)
    - Keep «Від»/«До» number inputs; sync on change
    - On slider `onValueChange`: update local state immediately; throttled `setParams({ cinaVid, cinaDo, storinka: 1 })` every 200ms
    - On `onValueCommit` (if Slider supports): flush params immediately
    - On input change: snap to step 50 (round), clamp to bounds, `storinka: 1`
    - If `priceBounds` null: disable Slider and inputs; show muted helper «Немає товарів для фільтра ціни»
    - `shallow: false` on useQueryStates (already)
  </action>
  <acceptance_criteria>
    - `catalog-filters.tsx` imports `Slider` from `@/components/ui/slider`
    - Slider uses `step={50}`
    - Price input onChange calls `setParams` with `storinka: 1`
    - Throttle interval is 200ms (constant in file or throttle helper)
    - `npm run lint` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Mobile catalog filters sheet</name>
  <read_first>
    - src/components/ui/sheet.tsx
    - .planning/phases/07-catalog-filters-fix/07-UI-SPEC.md — mobile section
    - src/components/chat/chat-panel.tsx — useIsMobile pattern (optional)
  </read_first>
  <files>
    src/components/catalog/catalog-filters-sheet.tsx,
    src/app/(storefront)/katalog/page.tsx,
    src/app/(storefront)/katalog/[slug]/page.tsx
  </files>
  <action>
    Create `CatalogFiltersSheet` visible only `lg:hidden`:
    - Trigger button «Фільтри» above grid or in toolbar area
    - Sheet contains `CatalogFiltersPanel` with same props as desktop
    - Footer button «Показати результати» closes sheet
    Hide desktop `aside` on `<lg` (`hidden lg:block` on aside wrapper).
    Mount sheet on both catalog pages passing brands, categories, priceBounds, activeCategorySlug.
  </action>
  <acceptance_criteria>
    - `catalog-filters-sheet.tsx` exists
    - Below `lg`, aside hidden and sheet trigger visible
    - Sheet includes price Slider section (same component as desktop)
    - `npm run lint` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
npm run lint
npm test
```
Manual: complete items 1–2 in `07-MANUAL-CHECKLIST.md` after execution.
</verification>
