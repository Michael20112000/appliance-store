---
phase: 07-catalog-filters-fix
plan: 04
type: execute
wave: 3
depends_on:
  - 07-02
  - 07-03
files_modified:
  - src/lib/catalog/search-params.test.ts
  - src/server/services/catalog.service.test.ts
  - .planning/phases/07-catalog-filters-fix/07-MANUAL-CHECKLIST.md
autonomous: true
requirements:
  - CAT-02
context_decisions:
  - "D-07-14: Vitest parsersToFilters, getDistinctBrands, buildPublicProductWhere"
  - "D-07-15: manual checklist not Playwright price spec"
  - "D-07-16: do not break catalog-filters-url e2e"

must_haves:
  truths:
    - "Vitest covers cina-vid-only filter conversion and where clause"
    - "Manual checklist documents all 5 roadmap success criteria"
    - "Existing catalog-filters-url e2e still passes"
  artifacts:
    - path: .planning/phases/07-catalog-filters-fix/07-MANUAL-CHECKLIST.md
      provides: operator manual gate
    - path: src/lib/catalog/search-params.test.ts
      provides: parser regression tests

---

## Phase Goal

**As a** розробник, **I want** автоматичні тести й чекліст, **so that** CAT-02 не повернеться після релізу.

<objective>
Розширити Vitest для цінового фільтра та брендів; додати manual checklist; прогнати e2e regression.
</objective>

<context>
@.planning/ROADMAP.md — Phase 7 success criteria
@.planning/phases/07-catalog-filters-fix/07-VALIDATION.md
@src/lib/catalog/search-params.test.ts
@e2e/catalog-filters-url.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vitest expansion (D-07-14, CAT-02)</name>
  <read_first>
    - src/lib/catalog/search-params.ts
    - src/lib/catalog/search-params.test.ts
    - src/server/services/catalog.service.test.ts
  </read_first>
  <files>src/lib/catalog/search-params.test.ts, src/server/services/catalog.service.test.ts</files>
  <action>
    Add tests to `search-params.test.ts`:
    - `cinaVid` only → `minPrice` kopiyky, `maxPrice` undefined (13000 → 1_300_000)
    - `cinaDo` only → `maxPrice` only
    - both bounds
    Add to `catalog.service.test.ts`:
    - `buildPublicProductWhere` with only `minPrice` → `price.gte` without `lte`
    - If getCatalogPriceBounds testable without DB, add unit test; else document aggregate in comment and rely on manual
  </action>
  <acceptance_criteria>
    - `search-params.test.ts` includes case `cinaVid: 13000` → `minPrice: 1_300_000`
    - `catalog.service.test.ts` includes gte-only price filter case
    - `npm test -- src/lib/catalog/search-params.test.ts src/server/services/catalog.service.test.ts` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Manual checklist and e2e regression (D-07-15, D-07-16)</name>
  <read_first>
    - .planning/ROADMAP.md — Phase 7 success criteria (5 items)
    - e2e/catalog-filters-url.spec.ts
  </read_first>
  <files>.planning/phases/07-catalog-filters-fix/07-MANUAL-CHECKLIST.md</files>
  <action>
    Create `07-MANUAL-CHECKLIST.md` with checkbox steps for each ROADMAP success criterion:
    1. `/katalog?cina-vid=13000` — no cheaper products visible
    2. Slider updates URL + page resets to 1
    3. Category page brand dropdown scoped
    4. Vitest command reference
    5. Slider drag → grid refresh observation
    Run `npm run test:e2e -- e2e/catalog-filters-url.spec.ts` and fix only if failure caused by this phase (do not add price Playwright spec).
  </action>
  <acceptance_criteria>
    - `07-MANUAL-CHECKLIST.md` exists with 5 numbered criteria matching ROADMAP
    - `npm run test:e2e -- e2e/catalog-filters-url.spec.ts` exits 0
    - No new `e2e/*price*` or `*slider*` spec files added
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
npm test
npm run test:e2e -- e2e/catalog-filters-url.spec.ts
```
</verification>
