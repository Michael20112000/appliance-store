---
phase: 23-admin-category-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(admin)/admin/kategorii/[id]/page.tsx
  - src/components/admin/admin-categories-table.tsx
  - src/components/admin/admin-categories-table.test.tsx
  - e2e/admin-categories.spec.ts
autonomous: true
requirements:
  - ADM-CAT-03
  - ADM-CAT-04
must_haves:
  truths:
    - "D-01: Edit toolbar uses icon + visible Ukrainian label (not icon-only); icons have aria-hidden"
    - "D-02: «Переглянути товари» uses Eye; «Додати товар» uses Plus"
    - "D-03: Product links unchanged — /admin/tovary?categoryId={id} and /admin/tovary/novyi?categoryId={id}"
    - "D-04: List header «Товари» with Link «Переглянути» to filtered products"
    - "D-05: Count shown as muted (N) after link — e.g. Переглянути (12)"
    - "D-06: Zero products still shows Переглянути (0); link not hidden"
    - "D-07: List link stopPropagation on click and pointerDown (clickable-table-row pattern)"
    - "D-08: No separate Редагувати button; row click is sole edit affordance"
  artifacts:
    - path: src/app/(admin)/admin/kategorii/[id]/page.tsx
      provides: icon toolbar ADM-CAT-03
    - path: src/components/admin/admin-categories-table.tsx
      provides: products link column ADM-CAT-04
    - path: src/components/admin/admin-categories-table.test.tsx
      provides: row vs link navigation guard
  key_links:
    - from: src/components/admin/admin-categories-table.tsx
      to: src/lib/admin/products-url.ts
      via: adminProductsUrl({ categoryId })
    - from: src/components/admin/admin-categories-table.tsx
      to: src/lib/admin/clickable-table-row.ts
      via: getAdminClickableRowProps + stopPropagation on link
---

<objective>
Polish admin category navigation: toolbar icons on edit (ADM-CAT-03) and «Товари» / «Переглянути» column on list (ADM-CAT-04) without breaking row-click edit.

Purpose: Faster operator flow between categories and filtered products.
Output: Two UI touchpoints + Vitest coverage for list link behavior; optional e2e smoke.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-admin-category-polish/23-CONTEXT.md
@.planning/phases/23-admin-category-polish/23-RESEARCH.md
@.planning/phases/23-admin-category-polish/23-UI-SPEC.md
@.planning/phases/23-admin-category-polish/23-PATTERNS.md
@src/app/(admin)/admin/kategorii/page.tsx
@src/app/(admin)/admin/kategorii/[id]/page.tsx
@src/components/admin/admin-categories-table.tsx
@src/components/admin/product-list-delete-button.tsx
@src/lib/admin/products-url.ts
@src/lib/admin/clickable-table-row.ts
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin-only routes | Unchanged — no new public surfaces |

## Threats

| ID | Threat | Severity | Mitigation in plan |
|----|--------|----------|-------------------|
| T1 | Wrong categoryId in products link leaks wrong filter | low | Use `adminProductsUrl({ categoryId: category.id })`; test href |
| T2 | Link click accidentally opens edit (UX/confusion) | medium | stopPropagation on link; Vitest asserts router.push not called for row href |
| T3 | Icon-only buttons harm a11y | low | CONTEXT D-01: visible labels + aria-hidden icons |

No auth, PII, or payment changes.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Edit category toolbar icons (ADM-CAT-03)</name>
  <read_first>
- src/app/(admin)/admin/kategorii/[id]/page.tsx
- src/app/(admin)/admin/kategorii/page.tsx
- .planning/phases/23-admin-category-polish/23-CONTEXT.md (D-01–D-03)
  </read_first>
  <files>
- src/app/(admin)/admin/kategorii/[id]/page.tsx
  </files>
  <action>
1. Import `Eye` and `Plus` from `lucide-react`.
2. In the «Переглянути товари» `Button` (outline, `size="sm"`), add before the text: `<Eye className="size-4" aria-hidden />`.
3. In the «Додати товар» `Button`, add before the text: `<Plus className="size-4" aria-hidden />`.
4. Do not change `Link` hrefs: keep `/admin/tovary?categoryId=${category.id}` and `/admin/tovary/novyi?categoryId=${category.id}`.
5. Do not add icon-only `aria-label` that hides visible text — accessible name comes from visible Ukrainian labels (D-01).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
- `page.tsx` imports `Eye` and `Plus` from `lucide-react`
- «Переглянути товари» button children include `Eye` with `className="size-4"` and `aria-hidden`
- «Додати товар» button children include `Plus` with `className="size-4"` and `aria-hidden`
- Link targets unchanged (`categoryId` query on both product links)
- `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: List «Товари» column with filtered link (ADM-CAT-04)</name>
  <read_first>
- src/components/admin/admin-categories-table.tsx
- src/lib/admin/products-url.ts
- src/components/admin/product-list-delete-button.tsx
- .planning/phases/23-admin-category-polish/23-CONTEXT.md (D-04–D-08)
  </read_first>
  <files>
- src/components/admin/admin-categories-table.tsx
  </files>
  <action>
1. Import `Link` from `next/link` and `adminProductsUrl` from `@/lib/admin/products-url`.
2. Change table header text from «Товарів» to «Товари».
3. Replace the product count-only cell with:
   - `Link` `href={adminProductsUrl({ categoryId: category.id })}`
   - Visible link text «Переглянути»
   - Muted count suffix: space + `({category.productCount})` in `text-muted-foreground` (show for 0 too — D-06)
4. On the `Link`, add `onClick` and `onPointerDown` handlers that call `event.stopPropagation()` (same `stopRowNav` pattern as `product-list-delete-button.tsx`).
5. Preserve `getAdminClickableRowProps` and row `href` `/admin/kategorii/${category.id}` unchanged (D-08).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
- Table header cell text is «Товари» (not «Товарів»)
- Link `href` equals `/admin/tovary?categoryId={category.id}` for a row with id `abc`
- Cell renders «Переглянути» and count in parentheses for `productCount` 0 and N&gt;0
- Link has both `onClick` and `onPointerDown` calling `stopPropagation`
- Row still uses `getAdminClickableRowProps` with href `/admin/kategorii/{id}`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Vitest — list link vs row navigation</name>
  <read_first>
- src/components/admin/admin-categories-table.tsx
- src/components/admin/product-list-delete-button.test.tsx
- src/lib/admin/products-url.ts
  </read_first>
  <files>
- src/components/admin/admin-categories-table.test.tsx
  </files>
  <action>
Create `admin-categories-table.test.tsx` following project RTL/Vitest conventions:

1. Mock `useRouter` from `next/navigation`; spy on `router.push`.
2. Render `AdminCategoriesTable` with one category `{ id: "cat-1", name: "Test", sortOrder: 1, productCount: 3 }`.
3. Assert link has `href` containing `categoryId=cat-1` (or full `adminProductsUrl` output).
4. Fire `click` on the «Переглянути» link; assert `router.push` was NOT called with `/admin/kategorii/cat-1`.
5. Fire `click` on the row name cell (or row outside link); assert `router.push` WAS called with `/admin/kategorii/cat-1`.

If no RTL setup exists for admin tables, use minimal render consistent with `product-list-delete-button.test.tsx`.
  </action>
  <verify>
    <automated>npx vitest run src/components/admin/admin-categories-table.test.tsx</automated>
  </verify>
  <acceptance_criteria>
- Test file exists at `src/components/admin/admin-categories-table.test.tsx`
- Test asserts products link href includes `categoryId=cat-1`
- Test asserts link click does not invoke row navigation to edit URL
- Test asserts row click (non-link) invokes `router.push` with `/admin/kategorii/cat-1`
- `npx vitest run src/components/admin/admin-categories-table.test.tsx` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4 (optional): E2E smoke for list link and edit icons</name>
  <read_first>
- e2e/admin-categories.spec.ts
- e2e/helpers/admin.ts
  </read_first>
  <files>
- e2e/admin-categories.spec.ts
  </files>
  <action>
Extend existing `admin can create and delete a category` flow (or add sibling test):

1. After category is created and list shows name, assert row has link «Переглянути» with href matching `/admin/tovary?categoryId=`.
2. On edit page, assert buttons «Переглянути товари» and «Додати товар» are visible (getByRole).
3. Click «Переглянути» on list (if on list step) and expect URL `/admin/tovary` with correct `categoryId` — only if flow stays stable without flaking.

Skip if e2e env unstable; Vitest in Task 3 is the required gate.
  </action>
  <verify>
    <automated>npx playwright test e2e/admin-categories.spec.ts</automated>
  </verify>
  <acceptance_criteria>
- E2E spec still passes OR task explicitly skipped in SUMMARY with reason
- If implemented: list «Переглянути» navigates to filtered products URL
  </acceptance_criteria>
</task>

</tasks>

<verification>
## Plan-level checks

- [ ] `npx vitest run src/components/admin/admin-categories-table.test.tsx` green
- [ ] `npx tsc --noEmit` green
- [ ] Manual: row click opens edit; link opens filtered products
</verification>

## PLANNING COMPLETE
