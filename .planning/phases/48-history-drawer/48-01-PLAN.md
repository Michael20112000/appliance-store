---
phase: 48-history-drawer
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/server/services/chat.service.test.ts
  - src/app/api/chat/conversations/route.test.ts
  - src/components/chat/chat-panel.test.tsx
autonomous: true
requirements:
  - CHAT-06
  - CHAT-07

must_haves:
  truths:
    - "RED stubs exist for listConversationsForBuyer in chat.service.test.ts"
    - "RED stubs exist for GET /api/chat/conversations in route.test.ts"
    - "RED stubs exist for PanelHeader Menu icon visibility in chat-panel.test.tsx"
    - "All new test stubs fail (RED) before implementation exists"
  artifacts:
    - path: "src/server/services/chat.service.test.ts"
      provides: "Unit tests for listConversationsForBuyer (extends existing file)"
      contains: "listConversationsForBuyer"
    - path: "src/app/api/chat/conversations/route.test.ts"
      provides: "Unit tests for GET /api/chat/conversations route"
      exports: ["GET"]
    - path: "src/components/chat/chat-panel.test.tsx"
      provides: "Unit tests for PanelHeader menu icon visibility gated on hasSession"
      contains: "Menu"
  key_links:
    - from: "src/server/services/chat.service.test.ts"
      to: "src/server/services/chat.service.ts"
      via: "@ts-expect-error import of listConversationsForBuyer"
      pattern: "listConversationsForBuyer"
    - from: "src/app/api/chat/conversations/route.test.ts"
      to: "src/app/api/chat/conversations/route.ts"
      via: "dynamic import of GET after vi.mock calls"
      pattern: "GET.*conversations/route"
---

<objective>
Write failing RED test stubs for all three Wave 0 gaps identified in VALIDATION.md: the
service function, the API route, and the PanelHeader menu icon guard.

Purpose: Establish the Nyquist-compliant feedback loop before any production code is written.
All three test files (two extended, one new) must fail with "not a function" or import errors
before Wave 1/2 implementation runs.

Output: Three test files with failing stubs; full suite still at or below baseline failure count
(5 pre-existing failures) once the @ts-expect-error imports are in place.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-history-drawer/48-RESEARCH.md
@.planning/phases/48-history-drawer/48-PATTERNS.md
@.planning/phases/48-history-drawer/48-VALIDATION.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: RED stub — extend chat.service.test.ts with listConversationsForBuyer</name>
  <files>src/server/services/chat.service.test.ts</files>
  <read_first>
    - src/server/services/chat.service.test.ts (read ENTIRE file — know existing imports, mock setup, and describe blocks before touching anything)
    - src/server/services/chat.service.ts (scan exports to confirm listConversationsForBuyer does NOT yet exist)
  </read_first>
  <behavior>
    - Test 1: listConversationsForBuyer("buyer-1") calls prisma.conversation.findMany with where: { userId: "buyer-1" } (CHAT-07)
    - Test 2: result has length 2, result[0].id is "conv-2" (sorted desc by lastMessageAt), result[0].buyerName is "Ви", result[0].buyerEmail is "", result[0].unreadForAdmin is false
    - Test 3: prisma.conversation.findMany is called WITHOUT isActive in the where clause (CHAT-07 — all history, not just active)
    - All three tests must be RED before plan 02 implements the function
  </behavior>
  <action>
    In the import block at the top of chat.service.test.ts, add listConversationsForBuyer with a @ts-expect-error comment (same pattern as claimGuestConversation and createNewConversation at lines 9-14):

      // @ts-expect-error — not exported yet (Wave 0 RED stub)
      listConversationsForBuyer,

    Then add a new describe("listConversationsForBuyer") block after the existing describe blocks. The mock for prisma.conversation.findMany already exists in the vi.mock setup at lines 29-54 — do NOT add a new mock. Use vi.mocked(prisma.conversation.findMany).mockResolvedValueOnce(...) per test.

    Test data for the two-item test: rows = [
      { id: "conv-2", userId: "buyer-1", status: "OPEN", lastMessagePreview: "b", lastMessageAt: new Date("2026-05-26") },
      { id: "conv-1", userId: "buyer-1", status: "ARCHIVED", lastMessagePreview: "a", lastMessageAt: new Date("2026-05-25") },
    ] cast as never.

    For the isActive test: call listConversationsForBuyer("buyer-1") with findMany mocked to resolve []. Then assert that mock.calls[0]![0] (the first argument object) does NOT have property "isActive" — use expect(call.where).not.toHaveProperty("isActive").

    Each it() block must call vi.clearAllMocks() in a beforeEach or inline. Use the existing beforeEach pattern from other describe blocks in the file.
  </action>
  <verify>
    <automated>npx vitest run src/server/services/chat.service.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    chat.service.test.ts contains a describe("listConversationsForBuyer") block with 3 failing tests (RED). The @ts-expect-error import line for listConversationsForBuyer exists in the import block. Running the test file produces failures referencing "listConversationsForBuyer is not a function" or similar — NOT a parse/import error that blocks other tests.
  </done>
</task>

<task type="tdd">
  <name>Task 2: RED stub — create route.test.ts for GET /api/chat/conversations</name>
  <files>src/app/api/chat/conversations/route.test.ts</files>
  <read_first>
    - src/app/api/chat/claim/route.test.ts (exact mock structure to replicate — vi.mock hoisting pattern, dynamic import of route handler after mocks)
    - src/app/api/chat/conversations/ (check if route.ts already exists — it should NOT yet)
  </read_first>
  <behavior>
    - Test 1: GET() with getSession returning null → response.status is 401 (CHAT-07 auth guard)
    - Test 2: GET() with getSession returning { user: { id: "buyer-1" } } and listConversationsForBuyer returning [{ id: "conv-1", buyerName: "Ви" }] → response.status is 200, body.conversations equals that array, listConversationsForBuyer was called with "buyer-1"
    - Both tests must be RED (route.ts does not exist yet) producing module-not-found error at import time
  </behavior>
  <action>
    Create src/app/api/chat/conversations/route.test.ts. File structure follows claim/route.test.ts exactly:

    1. Import describe/expect/it/vi from vitest.
    2. Declare getSession and listConversationsForBuyer as local vi.fn() variables BEFORE the vi.mock() calls (hoisting pattern from PATTERNS.md).
    3. vi.mock("next/headers", ...) — same as claim test.
    4. vi.mock("@/lib/auth", ...) — getSession wired through the local variable.
    5. vi.mock("@/server/services/chat.service", ...) — listConversationsForBuyer wired through local variable.
    6. Dynamic import: import { GET } from "@/app/api/chat/conversations/route" — this import will fail RED because the route file does not exist.
    7. describe("GET /api/chat/conversations") with two it() blocks.

    In the auth-failure test: getSession.mockResolvedValueOnce(null). Call GET() (no arguments — route takes no request param). Assert response.status === 401.

    In the happy-path test: getSession.mockResolvedValueOnce({ user: { id: "buyer-1" } }). listConversationsForBuyer.mockResolvedValueOnce([{ id: "conv-1", buyerName: "Ви" }]). Call const response = await GET(). Assert response.status === 200. Assert (await response.json()).conversations equals the mocked array. Assert listConversationsForBuyer was called with "buyer-1".
  </action>
  <verify>
    <automated>npx vitest run src/app/api/chat/conversations/route.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    src/app/api/chat/conversations/route.test.ts exists with 2 tests. Running the test file produces a module-not-found or import error for the route (RED). The test file itself has no syntax errors — only the missing route causes failures.
  </done>
</task>

<task type="tdd">
  <name>Task 3: RED stub — create chat-panel.test.tsx for Menu icon visibility</name>
  <files>src/components/chat/chat-panel.test.tsx</files>
  <read_first>
    - src/components/chat/chat-panel.tsx (read full file — know the PanelHeader component, its current structure, what imports exist, what useChat fields are consumed)
    - src/components/chat/chat-provider.tsx (read lines 46-70 — ChatContextValue type — to know what fields must be in mock context)
    - src/components/chat/conversation-lifecycle-menu-items.test.tsx (check for an existing chat component test as pattern reference for how vi.mock of useChat is structured)
  </read_first>
  <behavior>
    - Test 1: PanelHeader rendered with hasSession=false in mocked ChatContext → no element with aria-label "Відкрити меню чатів" is present (CHAT-06 guest guard)
    - Test 2: PanelHeader rendered with hasSession=true in mocked ChatContext → an element with aria-label "Відкрити меню чатів" is present (CHAT-06 auth visibility)
    - Both tests must be RED before plan 03 adds the Menu button to PanelHeader
  </behavior>
  <action>
    Create src/components/chat/chat-panel.test.tsx as a Vitest + React Testing Library test file.

    Mock useChat from "@/components/chat/chat-provider" using vi.mock. The mock must return a factory function (vi.fn()) that can be overridden per test with mockReturnValue. Minimal mock context shape: { hasSession: boolean, openHistory: vi.fn(), closePanel: vi.fn(), isOpen: true, messages: [], isLoading: false, loadError: null, isDisconnected: false, productContext: null, conversationStatus: null }.

    Also mock all child components to avoid cascading dependency failures:
    - "@/components/chat/message-list" → returns null
    - "@/components/chat/chat-composer" → returns null
    - "@/components/chat/archived-chat-banner" → returns null
    - "@/components/chat/product-context-banner" → returns null
    - "next/dynamic" → returns the wrapped component directly (pass-through)

    PanelHeader is NOT exported from chat-panel.tsx — it is internal. Render ChatPanel (or PanelBody — check which is exported). Since PanelHeader is inside PanelBody, and PanelBody renders when panelView === "thread", also mock panelView: "thread" in the context.

    In the guest test: mock useChat to return hasSession: false. Render PanelBody (or the parent component that renders PanelHeader). Assert that queryByRole("button", { name: /Відкрити меню чатів/ }) returns null.

    In the auth test: mock useChat to return hasSession: true, openHistory: vi.fn(). Assert that getByRole("button", { name: /Відкрити меню чатів/ }) is present in the document.

    These tests will be RED because PanelHeader does not yet have the Menu button — queryByRole will wrongly return null in both cases, making the auth-visible test fail.
  </action>
  <verify>
    <automated>npx vitest run src/components/chat/chat-panel.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>
    src/components/chat/chat-panel.test.tsx exists with 2 tests. At least the "auth user sees menu icon" test is RED (button not found). The guest-hides-menu test may or may not pass at this stage — both RED is preferred. The file has no syntax errors or unresolvable import errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → production module | Test stubs import production modules that don't exist yet — expected RED state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this plan; test-only file changes |
</threat_model>

<verification>
After all three tasks:

```
npx vitest run src/server/services/chat.service.test.ts src/app/api/chat/conversations/route.test.ts src/components/chat/chat-panel.test.tsx 2>&1 | grep -E "(FAIL|PASS|Tests)"
```

Expected: listConversationsForBuyer tests FAIL (3 new failures), conversations/route tests FAIL (2), chat-panel tests FAIL (1-2). No new passing suites that shouldn't pass yet.

Full suite baseline check:
```
npx vitest run 2>&1 | grep -E "^(Tests|Test Files)" | tail -5
```
Total failures must not exceed baseline (5) plus the new RED stubs (up to 7 new failures).
</verification>

<success_criteria>
- src/server/services/chat.service.test.ts has @ts-expect-error import of listConversationsForBuyer and a describe block with 3 failing RED tests
- src/app/api/chat/conversations/route.test.ts exists with 2 failing RED tests (module not found)
- src/components/chat/chat-panel.test.tsx exists with 2 RED tests for Menu icon visibility
- No pre-existing passing tests are broken
- Total test failure count is at most baseline + 7
</success_criteria>

<output>
Create `.planning/phases/48-history-drawer/48-01-SUMMARY.md` when done
</output>
