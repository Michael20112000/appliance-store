---
phase: 48-history-drawer
plan: 03
type: execute
wave: 3
depends_on:
  - 48-02
files_modified:
  - src/components/chat/chat-provider.tsx
  - src/components/chat/chat-panel.tsx
  - src/components/chat/history-drawer.tsx
autonomous: false
requirements:
  - CHAT-06
  - CHAT-07
  - CHAT-08

must_haves:
  truths:
    - "Authenticated user sees a Menu icon button in the widget header"
    - "Guest user does not see the Menu icon button"
    - "Clicking the Menu icon opens the history panel (panelView switches to 'history')"
    - "History panel shows a list of past conversations fetched from GET /api/chat/conversations"
    - "Clicking a conversation in the list switches the widget to that thread"
    - "Clicking 'Новий чат' creates a new conversation and opens the thread view"
    - "Closing the widget and reopening resets to thread view, not history"
  artifacts:
    - path: "src/components/chat/chat-provider.tsx"
      provides: "panelView state + openHistory/closeHistory callbacks in ChatContextValue"
      contains: "panelView"
    - path: "src/components/chat/chat-panel.tsx"
      provides: "PanelHeader with auth-gated Menu button; ChatPanel branching on panelView"
      contains: "openHistory"
    - path: "src/components/chat/history-drawer.tsx"
      provides: "HistoryDrawer component with header, ConversationList, new-chat button"
      contains: "HistoryDrawer"
  key_links:
    - from: "src/components/chat/chat-panel.tsx"
      to: "src/components/chat/chat-provider.tsx"
      via: "useChat() — panelView, hasSession, openHistory"
      pattern: "panelView.*history"
    - from: "src/components/chat/history-drawer.tsx"
      to: "/api/chat/conversations"
      via: "fetch on mount useEffect"
      pattern: "fetch.*api/chat/conversations"
    - from: "src/components/chat/history-drawer.tsx"
      to: "src/components/chat/chat-provider.tsx"
      via: "useChat() — setConversationId, setConversationStatus, resetMessages, closeHistory"
      pattern: "closeHistory"
---

<objective>
Implement the client-side history drawer: extend ChatProvider with panelView state,
add the auth-gated Menu icon to PanelHeader, create the HistoryDrawer component, and
wire ChatPanel to branch on panelView. Finalize with a human UAT checkpoint.

Purpose: Completes all three CHAT-06/07/08 requirements. After this plan, authenticated
users can browse their conversation history and start new chats from within the widget.

Output: history-drawer.tsx (new), chat-provider.tsx and chat-panel.tsx modified. Human UAT confirms all four Phase 48 success criteria.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-history-drawer/48-RESEARCH.md
@.planning/phases/48-history-drawer/48-PATTERNS.md
@.planning/phases/48-history-drawer/48-02-SUMMARY.md

<interfaces>
<!-- ChatContextValue fields to add — all three must appear in the type, useMemo value object, and useMemo deps array -->

New fields for ChatContextValue type (src/components/chat/chat-provider.tsx lines 46-70):
```typescript
panelView: "thread" | "history";
openHistory: () => void;
closeHistory: () => void;
```

New state + callbacks in ChatProvider body (add after line 125 existing state block):
```typescript
const [panelView, setPanelView] = useState<"thread" | "history">("thread");
const openHistory = useCallback(() => setPanelView("history"), []);
const closeHistory = useCallback(() => setPanelView("thread"), []);
```

closePanel must be extended to call setPanelView("thread") (Pitfall 2 from RESEARCH.md):
```typescript
// Current closePanel (lines 149-152):
const closePanel = useCallback(() => {
  void setQuery({ chat: null, productId: null });
  setProductContext(null);
}, [setQuery]);

// Extended closePanel:
const closePanel = useCallback(() => {
  void setQuery({ chat: null, productId: null });
  setProductContext(null);
  setPanelView("thread");
}, [setQuery]);
```

ConversationList component props (src/components/chat/conversation-list.tsx — read to confirm props):
The component accepts: conversations, selectedId, isLoading, onSelect, enableContextMenu, showUnreadHighlight, emptyTitle, emptyBody
```typescript
// Props verification — read conversation-list.tsx before using these
<ConversationList
  conversations={conversations}
  selectedId={conversationId}
  isLoading={isLoading}
  enableContextMenu={false}
  showUnreadHighlight={false}
  onSelect={handleSelect}
  emptyTitle="Ще немає чатів"
  emptyBody="Розпочніть розмову з магазином."
/>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend ChatProvider with panelView state and openHistory/closeHistory</name>
  <files>src/components/chat/chat-provider.tsx</files>
  <read_first>
    - src/components/chat/chat-provider.tsx (read ENTIRE file — locate: ChatContextValue type lines ~46-70; state block lines ~112-126; closePanel lines ~149-152; useMemo value object lines ~463-511; useMemo deps array)
    - src/components/chat/chat-panel.test.tsx (plan 01 RED stub — confirm mock context shape the test expects)
  </read_first>
  <behavior>
    - panelView defaults to "thread"
    - openHistory() sets panelView to "history"
    - closeHistory() sets panelView to "thread"
    - closePanel() resets panelView to "thread" (so next open always shows thread)
    - panelView, openHistory, closeHistory are all present in ChatContextValue type, useMemo value object, and useMemo deps array
    - The chat-panel.test.tsx RED stubs for Menu icon visibility turn GREEN after this task (because PanelHeader in Task 2 uses hasSession and openHistory from context)
  </behavior>
  <action>
    Make five targeted edits to chat-provider.tsx:

    1. ChatContextValue type (near line 46): add three fields at the end of the type block —
       panelView: "thread" | "history";
       openHistory: () => void;
       closeHistory: () => void;

    2. State declarations (after the guestToken useState near line 125): add —
       const [panelView, setPanelView] = useState<"thread" | "history">("thread");

    3. Callbacks (after closePanel): add —
       const openHistory = useCallback(() => setPanelView("history"), []);
       const closeHistory = useCallback(() => setPanelView("thread"), []);

    4. closePanel (lines ~149-152): add setPanelView("thread") as the third statement inside the callback body. Also add setPanelView to the deps array if eslint-react-hooks complains — but setPanelView is stable so it need not be listed.

    5. useMemo value object and deps array: add panelView, openHistory, closeHistory to both. Place them after updateGuestToken in the value object and after updateGuestToken in the deps array.

    Do NOT change any other logic. The Pusher effects, fetch effects, and all other state fields remain unchanged.
  </action>
  <verify>
    <automated>npx vitest run src/components/chat/chat-panel.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>
    src/components/chat/chat-provider.tsx exports panelView, openHistory, closeHistory through ChatContextValue. The chat-panel.test.tsx Menu-visible test passes GREEN (once Task 2 also completes — if running after Task 2). TypeScript finds no errors in chat-provider.tsx.
  </done>
</task>

<task type="auto">
  <name>Task 2: Modify PanelHeader and ChatPanel; create HistoryDrawer</name>
  <files>
    src/components/chat/chat-panel.tsx
    src/components/chat/history-drawer.tsx
  </files>
  <read_first>
    - src/components/chat/chat-panel.tsx (read ENTIRE file — exact PanelHeader JSX, ChatPanel render, existing imports)
    - src/components/chat/archived-chat-banner.tsx (fetch + useChat + error handling pattern for HistoryDrawer)
    - src/components/chat/conversation-list.tsx (read to confirm ALL accepted props names and types before using them in HistoryDrawer)
    - src/types/chat.ts (confirm ConversationSummaryDto import path)
  </read_first>
  <action>
    PART A — Modify src/components/chat/chat-panel.tsx:

    1. Add Menu to the lucide-react import: change "import { X } from 'lucide-react'" to "import { Menu, X } from 'lucide-react'".

    2. In PanelHeader: add useChat() call inside the function body to get { hasSession, openHistory }. Change the title div from "min-w-0 pr-2" to "min-w-0 flex-1 pr-2" so it expands. After the title div and BEFORE the existing X Button, insert the conditional Menu button —
       {hasSession ? (
         <Button type="button" variant="ghost" size="icon" className="size-9 shrink-0"
           onClick={openHistory} aria-label="Відкрити меню чатів">
           <Menu className="size-4" />
         </Button>
       ) : null}

    3. Add import of HistoryDrawer at the top of the file:
       import { HistoryDrawer } from "@/components/chat/history-drawer";

    4. In ChatPanel: destructure panelView from useChat() alongside isOpen and closePanel.

    5. Replace the two PanelBody render sites with conditional logic:
       - Desktop div: {isOpen ? (panelView === "history" ? <HistoryDrawer /> : <PanelBody useNativeScroll={false} />) : null}
       - SheetContent: {panelView === "history" ? <HistoryDrawer /> : <PanelBody useNativeScroll stickyHeader />}

    PART B — Create src/components/chat/history-drawer.tsx:

    "use client" directive at the top.

    Imports: useEffect + useState from react; ArrowLeft + Plus from lucide-react; useChat from "@/components/chat/chat-provider"; ConversationList from "@/components/chat/conversation-list"; Button from "@/components/ui/button"; ConversationSummaryDto from "@/types/chat".

    Component body:
    - Destructure from useChat(): conversationId, setConversationId, setConversationStatus, resetMessages, closeHistory.
    - State: conversations (ConversationSummaryDto[], init []), isLoading (bool, init true).
    - useEffect on mount (empty deps []): fetch /api/chat/conversations. If ok, parse { conversations } from JSON cast as { conversations: ConversationSummaryDto[] } and setConversations. Set isLoading(false) in both ok and not-ok branches.
    - handleSelect(id: string): find conversation in state, call resetMessages(), setConversationId(id), setConversationStatus(conv.status), closeHistory().
    - handleNewChat (async): fetch POST /api/chat/new with { method: "POST", headers: { "Content-Type": "application/json" }, body: "{}" }. If ok, parse { conversationId } from JSON. Call resetMessages(), setConversationId(data.conversationId), setConversationStatus("OPEN"), closeHistory(). If not ok, return without action (no toast needed — arch decision).

    Return JSX:
    - Outer div: className="flex h-full min-h-0 flex-1 flex-col overflow-hidden"
    - Header row div: className="flex shrink-0 items-center justify-between border-b border-border px-4 py-3"
      - Back Button (variant="ghost" size="sm" onClick={closeHistory}): <ArrowLeft className="mr-1 size-4" /> Назад
      - <p className="text-sm font-semibold">Мої чати</p>
      - New Chat Button (variant="outline" size="sm" onClick={() => void handleNewChat()}): <Plus className="mr-1 size-4" /> Новий чат
    - List wrapper div: className="min-h-0 flex-1 overflow-y-auto"
      - <ConversationList conversations={conversations} selectedId={conversationId} isLoading={isLoading} enableContextMenu={false} showUnreadHighlight={false} onSelect={handleSelect} emptyTitle="Ще немає чатів" emptyBody="Розпочніть розмову з магазином." />
  </action>
  <verify>
    <automated>npx vitest run src/components/chat/chat-panel.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>
    Both chat-panel.test.tsx tests pass GREEN (guest: no Menu button; auth: Menu button present). history-drawer.tsx exists and imports compile without TypeScript errors. chat-panel.tsx renders HistoryDrawer when panelView === "history". Full suite failure count at or below baseline + 0 (all Wave 0 stubs are now GREEN).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Three CHAT requirements fully wired end-to-end:
    - CHAT-06: Menu icon in widget header, visible only to authenticated users
    - CHAT-07: History panel with past conversations; clicking one switches to that thread
    - CHAT-08: "Новий чат" button in the history panel creates a new conversation
  </what-built>
  <how-to-verify>
    Start the dev server if not running: npm run dev

    Test 1 — CHAT-06 Guest: Open the chat widget while logged out. Confirm NO menu/hamburger icon appears in the widget header — only the X close button is visible.

    Test 2 — CHAT-06 Auth + CHAT-07 History: Log in as a user who has at least one past conversation. Open the chat widget. Confirm a menu icon (three horizontal lines) appears in the widget header next to the X button. Click the menu icon. Confirm the view switches INSIDE the widget to a panel titled "Мої чати" showing a list of past conversations. The widget does not close, navigate, or resize.

    Test 3 — CHAT-07 Thread Switch: In the history panel, click on a past conversation. Confirm the panel switches back to the message thread view showing that conversation's messages. The widget remains open.

    Test 4 — CHAT-08 New Chat: Open the history drawer again (click the menu icon). Click "Новий чат". Confirm a new empty message thread opens inside the widget (no messages, composer enabled).

    Test 5 — Close + Reopen: While in the history drawer, close the widget (click X). Reopen the widget by clicking the chat FAB. Confirm the widget opens showing the message thread view (NOT the history panel).

    Test 6 — Full Suite Green:
    npx vitest run 2>&1 | grep -E "^(Tests|Test Files)" | tail -5
    Confirm failure count is at or below 5 (pre-existing baseline).
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 checks pass, or describe which check failed and what you observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /api/chat/conversations | Already guarded in plan 02; HistoryDrawer's fetch relies on the session cookie being present |
| client → POST /api/chat/new | Existing route — already auth-gated; HistoryDrawer calls it without a body (userId comes from session) |
| ChatProvider state | panelView is local client state; no sensitive data crosses the boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-03-01 | Information Disclosure | HistoryDrawer fetch /api/chat/conversations | accept | Route returns 401 for unauthenticated requests (plan 02); HistoryDrawer is only rendered inside the chat widget which requires hasSession=true guard on the Menu icon |
| T-48-03-02 | Information Disclosure | handleSelect — accessing archived conversation messages | accept | GET /api/chat/messages uses assertConversationAccess which enforces conversation.userId === session.user.id; accessing another user's thread is blocked server-side |
| T-48-03-03 | Denial of Service | handleNewChat — POST /api/chat/new | accept | Auth users only; single-admin store; rate-limiting is post-v3.0 scope per RESEARCH.md |
| T-48-03-04 | Spoofing | Menu icon visible to guests | mitigate | hasSession guard in PanelHeader: {hasSession ? <MenuButton /> : null}; hasSession comes from server-rendered ChatProvider prop, not client state |
| T-48-03-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this plan |
</threat_model>

<verification>
Full automated suite after UAT approval:

```
npx vitest run 2>&1 | grep -E "^(Tests|Test Files)" | tail -5
```

Expected: 0 new failures beyond baseline (5 pre-existing). All plan 01 RED stubs (5 tests across service + route + chat-panel files) are now GREEN.

TypeScript check:
```
npx tsc --noEmit 2>&1 | grep -c "error TS" || echo "0 errors"
```
</verification>

<success_criteria>
- ChatContextValue exposes panelView, openHistory, closeHistory
- PanelHeader renders Menu button when hasSession=true, nothing when hasSession=false
- ChatPanel renders HistoryDrawer when panelView==="history", PanelBody otherwise
- HistoryDrawer fetches GET /api/chat/conversations on mount, renders ConversationList
- Clicking a row calls setConversationId + setConversationStatus + resetMessages + closeHistory
- Clicking "Новий чат" calls POST /api/chat/new, sets new conversationId, calls closeHistory
- closePanel resets panelView to "thread"
- Human UAT: all 6 checks confirmed "approved"
- Full test suite at or below baseline failure count
</success_criteria>

<output>
Create `.planning/phases/48-history-drawer/48-03-SUMMARY.md` when done
</output>
