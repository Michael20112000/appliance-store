---
phase: 35-callback-calls
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/server/services/callback-request.service.test.ts
  - src/server/validators/admin-callback.test.ts
autonomous: true
requirements:
  - CALL-02
  - CALL-03
  - CALL-04

must_haves:
  truths:
    - "Service tests define list view filter, status update, note null normalization, and archive gate contracts"
    - "Validator tests define view enum, status enum, and note max length (4000)"
    - "Tests run via vitest — some RED until Plan 02 implements functions"
  artifacts:
    - path: "src/server/services/callback-request.service.test.ts"
      provides: "Extended admin callback service contract tests"
      contains: "describe(\"listCallbackRequestsAdmin\")"
    - path: "src/server/validators/admin-callback.test.ts"
      provides: "Admin callback Zod schema tests"
      contains: "updateCallbackNoteSchema"
  key_links:
    - from: "src/server/services/callback-request.service.test.ts"
      to: "src/server/services/callback-request.service.ts"
      via: "named imports for admin functions"
      pattern: "from.*callback-request\\.service"
---

<objective>
Create Wave 0 Nyquist test stubs for Phase 35 admin callback behavior. Tests document contracts before schema migration and service implementation in Plan 02.

Purpose: Automated verify exists for CALL-02–04 server rules (archive gate, note null, list filters) per 35-VALIDATION.md Wave 0 gaps.
Output: Extended `callback-request.service.test.ts` + new `admin-callback.test.ts`.
</objective>

<execution_context>
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/PROJECT.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/ROADMAP.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/phases/35-callback-calls/35-CONTEXT.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/phases/35-callback-calls/35-RESEARCH.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/phases/35-callback-calls/35-PATTERNS.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/phases/35-callback-calls/35-VALIDATION.md
</context>

<interfaces>
<!-- From src/server/services/callback-request.service.test.ts (existing mock) -->
```typescript
vi.mock("@/lib/db", () => ({
  prisma: {
    callbackRequest: {
      count: vi.fn(),
      create: vi.fn(),
      findMany: vi.fn(),
      findUnique: vi.fn(),
      update: vi.fn(),
    },
  },
}));
```

<!-- Functions to import in Plan 02 (tests RED until implemented) -->
- listCallbackRequestsAdmin({ view: "active" | "archive", limit?: number })
- updateCallbackRequestStatus(id, status)
- updateCallbackRequestNote(id, note: string)
- archiveCallbackRequest(id)
- getCallbackViewCounts() — optional; test only if exported

<!-- Error constants (Plan 02) -->
- CALLBACK_NOT_FOUND
- CALLBACK_NOT_CONSULTED
- CALLBACK_ALREADY_ARCHIVED

<!-- Note convention (D-10, locked in Plan 02) -->
- Empty/whitespace-only note persists as `null`, never `""`

<!-- Validators (admin-callback.ts — Plan 02) -->
- callbackListViewSchema: z.enum(["active", "archive"]).default("active")
- updateCallbackStatusSchema: { id: cuid, status: "PENDING" | "CONSULTED" }
- updateCallbackNoteSchema: { id: cuid, note: z.string().max(4000).optional() }
- archiveCallbackSchema: { id: cuid }
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend callback-request.service.test.ts admin contracts</name>
  <files>src/server/services/callback-request.service.test.ts</files>
  <read_first>
    - src/server/services/callback-request.service.test.ts (full file — keep existing createCallbackRequest tests)
    - src/server/services/callback-request.service.ts (current exports only)
    - .planning/phases/35-callback-calls/35-RESEARCH.md (Implementation Approach §2, archive gate sketch)
    - .planning/phases/35-callback-calls/35-PATTERNS.md (service test extend section)
  </read_first>
  <behavior>
    - describe("listCallbackRequestsAdmin"):
      1. "active view filters archivedAt null" — mock findMany; call with view active; assert where.archivedAt === null
      2. "archive view filters archivedAt not null" — assert where.archivedAt.not === null
    - describe("updateCallbackRequestStatus"):
      1. "updates status when row exists and not archived" — findUnique returns active row; update called with status CONSULTED
    - describe("updateCallbackRequestNote"):
      1. "stores trimmed note" — input "  hello  " → update data note "hello"
      2. "empty note saves as null" — input "" or "   " → update data note null (per D-10)
    - describe("archiveCallbackRequest"):
      1. "rejects when status is not CONSULTED" — findUnique PENDING → throws/rejects CALLBACK_NOT_CONSULTED; update not called
      2. "sets archivedAt when CONSULTED" — findUnique CONSULTED, archivedAt null → update with archivedAt Date
      3. "rejects when already archived" — archivedAt set → CALLBACK_ALREADY_ARCHIVED
  </behavior>
  <action>
    Extend the existing vitest file. Add findMany, findUnique, update to the prisma.callbackRequest mock (keep count, create).

    Import new functions and error constants from "./callback-request.service" (they do not exist yet — RED is expected).

    Use vi.mocked(prisma.callbackRequest.findMany) etc. with beforeEach vi.clearAllMocks.

    Do NOT implement service functions in this plan — tests only.

    Preserve the two existing createCallbackRequest rate-limit tests unchanged.
  </action>
  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npm test -- --run src/server/services/callback-request.service.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - Mock includes findMany, findUnique, update on prisma.callbackRequest
    - At least 7 new test cases under admin describe blocks (list 2, status 1, note 2, archive 3)
    - Empty note test asserts null, not empty string
    - Archive test asserts update not called when status PENDING
    - Existing createCallbackRequest tests still present
  </acceptance_criteria>
  <done>Service test file defines admin callback contracts; vitest fails on missing exports until Plan 02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create admin-callback.validator.test.ts stubs</name>
  <files>src/server/validators/admin-callback.test.ts</files>
  <read_first>
    - src/server/validators/callback.test.ts (storefront phone schema test style)
    - src/server/validators/admin-order.ts (enum + cuid patterns)
    - .planning/phases/35-callback-calls/35-RESEARCH.md (validators table)
  </read_first>
  <behavior>
    - updateCallbackStatusSchema: accepts PENDING and CONSULTED; rejects invalid status string
    - updateCallbackNoteSchema: accepts note under 4000 chars; rejects note over 4000
    - updateCallbackNoteSchema: accepts omitted note (optional)
    - callbackListViewSchema: defaults to "active"; accepts "archive"
    - archiveCallbackSchema: requires valid cuid id
  </behavior>
  <action>
    Create src/server/validators/admin-callback.test.ts importing schemas from "./admin-callback" (file created in Plan 02).

    Use describe/it/expect from vitest. No implementation file in this plan.

    Ukrainian error messages are not required in assertions — only schema pass/fail.
  </action>
  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npm test -- --run src/server/validators/admin-callback.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File exists with at least 5 test cases across schemas
    - Note max length test uses string length 4001 to fail
    - Imports from "./admin-callback" (module missing until Plan 02 — expected RED)
  </acceptance_criteria>
  <done>Validator test stubs committed; Plan 02 implements admin-callback.ts to satisfy them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test files only | No production mutations; no runtime trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-SC | Tampering | npm/pip/cargo installs | accept | No new packages — test files only |
</threat_model>

<verification>
Both test files exist. Vitest run may RED on missing modules/functions — acceptable Wave 0 outcome.
No changes to prisma/schema.prisma or admin-analytics.service.ts.
</verification>

<success_criteria>
Nyquist Wave 0 complete: admin callback service and validator test contracts committed; Plan 02 can implement to green.
</success_criteria>

<output>
Create .planning/phases/35-callback-calls/35-01-SUMMARY.md when done.
</output>
