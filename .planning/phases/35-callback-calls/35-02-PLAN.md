---
phase: 35-callback-calls
plan: 02
type: execute
wave: 1
depends_on:
  - "35-01"
files_modified:
  - prisma/schema.prisma
  - prisma/migrations
  - src/lib/callback/status-labels.ts
  - src/server/services/callback-request.service.ts
  - src/server/services/store-settings.service.ts
  - src/server/validators/admin-callback.ts
  - src/server/actions/admin/callback.actions.ts
autonomous: false
requirements:
  - CALL-02
  - CALL-03
  - CALL-04

must_haves:
  truths:
    - "CallbackRequest has status PENDING|CONSULTED, optional note, optional archivedAt; migration applied"
    - "listCallbackRequestsAdmin lives in callback-request.service with active/archive where clauses"
    - "Archive only succeeds when status is CONSULTED — enforced in service, not UI-only"
    - "Empty note persists as null (never empty string)"
    - "store-settings.service no longer exports listCallbackRequestsAdmin or CallbackRequestAdminRow"
    - "admin-analytics.service.ts callback KPI query unchanged (D-12)"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "CallbackRequestStatus enum and admin fields"
      contains: "enum CallbackRequestStatus"
    - path: "src/server/services/callback-request.service.ts"
      provides: "Admin list, status, note, archive mutations"
      exports:
        - "listCallbackRequestsAdmin"
        - "updateCallbackRequestStatus"
        - "updateCallbackRequestNote"
        - "archiveCallbackRequest"
        - "getCallbackViewCounts"
    - path: "src/server/actions/admin/callback.actions.ts"
      provides: "requireAdmin-guarded mutations"
      exports:
        - "updateCallbackStatusAction"
        - "updateCallbackNoteAction"
        - "archiveCallbackRequestAction"
  key_links:
    - from: "src/server/actions/admin/callback.actions.ts"
      to: "src/lib/permissions requireAdmin"
      via: "await requireAdmin() first line"
      pattern: "requireAdmin"
    - from: "archiveCallbackRequest"
      to: "prisma.callbackRequest.update"
      via: "status === CONSULTED gate before update"
      pattern: "CONSULTED"
    - from: "src/server/services/admin-analytics.service.ts"
      to: "prisma.callbackRequest.count"
      via: "createdAt gte since only — no status/archivedAt filter"
      pattern: "callbackRequest\\.count"
---

<objective>
Apply Prisma schema extension and migration, move admin callback data layer into callback-request.service, add Zod validators and requireAdmin server actions. Storefront createCallbackRequest unchanged; analytics KPI unchanged per D-12.

Purpose: Backend foundation for CALL-02–04 before admin UI in Plan 03.
Output: Migrated schema, service functions, three admin actions, green Plan 01 tests.
</objective>

<execution_context>
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/phases/35-callback-calls/35-CONTEXT.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/phases/35-callback-calls/35-RESEARCH.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/phases/35-callback-calls/35-PATTERNS.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/phases/35-callback-calls/35-01-SUMMARY.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/src/server/actions/admin/order.actions.ts
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/src/server/services/store-settings.service.ts
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/AGENTS.md
</context>

<interfaces>
<!-- Prisma target (D-06, D-07) -->
```prisma
enum CallbackRequestStatus {
  PENDING
  CONSULTED
}

model CallbackRequest {
  id         String                @id @default(cuid())
  phone      String
  ipAddress  String?
  createdAt  DateTime              @default(now())
  status     CallbackRequestStatus @default(PENDING)
  note       String?               @db.Text
  archivedAt DateTime?
  @@index([ipAddress, createdAt])
  @@index([archivedAt, createdAt])
}
```

<!-- CallbackRequestAdminRow -->
```typescript
export type CallbackRequestAdminRow = {
  id: string;
  phone: string;
  createdAt: Date;
  status: CallbackRequestStatus;
  note: string | null;
  archivedAt: Date | null;
};
```

<!-- Action result shape (mirror order.actions) -->
```typescript
type CallbackActionResult =
  | { ok: true }
  | { ok: false; error: "VALIDATION" | "NOT_FOUND" | "NOT_CONSULTED" | "ALREADY_ARCHIVED" | "UNKNOWN" };
```

<!-- D-12 — DO NOT MODIFY -->
admin-analytics.service.ts line ~33:
prisma.callbackRequest.count({ where: { createdAt: { gte: since } } })
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1 [BLOCKING]: Prisma schema + migrate + generate</name>
  <files>prisma/schema.prisma, prisma/migrations</files>
  <read_first>
    - prisma/schema.prisma (CallbackRequest model ~279–286, ConversationStatus enum precedent ~218)
    - .planning/phases/35-callback-calls/35-CONTEXT.md (D-06, D-07)
    - AGENTS.md (Next.js — no unrelated schema edits)
  </read_first>
  <action>
    Per D-06/D-07: add enum CallbackRequestStatus { PENDING CONSULTED } and on CallbackRequest:
    - status CallbackRequestStatus @default(PENDING)
    - note String? @db.Text
    - archivedAt DateTime?
    - @@index([archivedAt, createdAt])

    Run from project root (requires DATABASE_URL and running PostgreSQL):
    npx prisma migrate dev --name callback_admin_fields
    npx prisma generate

    If migrate fails (DB down, auth error): STOP and report — plan is autonomous: false; human must fix DB then resume. Do not skip migration or hand-roll SQL.

    Do NOT edit src/server/services/admin-analytics.service.ts (D-12).
  </action>
  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npx prisma validate && test -d prisma/migrations && ls prisma/migrations | rg -c callback_admin_fields | awk '{exit ($1>=1)?0:1}'</automated>
  </verify>
  <acceptance_criteria>
    - prisma/schema.prisma contains CallbackRequestStatus enum and three new fields
    - Migration folder *callback_admin_fields* exists
    - prisma generate succeeds (client types include CallbackRequestStatus)
    - admin-analytics.service.ts diff is empty (no edits)
  </acceptance_criteria>
  <done>Schema migrated; generated client includes status, note, archivedAt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Service layer — move list + admin mutations</name>
  <files>
    src/server/services/callback-request.service.ts
    src/server/services/store-settings.service.ts
    src/lib/callback/status-labels.ts
  </files>
  <read_first>
    - src/server/services/callback-request.service.ts
    - src/server/services/store-settings.service.ts (lines 31–35, 113–121 — remove)
    - src/lib/order/status-labels.ts (label map pattern)
    - src/server/services/callback-request.service.test.ts (Plan 01 contracts)
    - .planning/phases/35-callback-calls/35-PATTERNS.md (service section)
  </read_first>
  <behavior>
    - listCallbackRequestsAdmin: view active → archivedAt null; archive → not null; orderBy createdAt desc; take limit ?? 200
    - updateCallbackRequestStatus: reject if archived; allow PENDING ↔ CONSULTED
    - updateCallbackRequestNote: trim; empty → null (D-10 convention — document in service JSDoc one line)
    - archiveCallbackRequest: NOT_FOUND, ALREADY_ARCHIVED, NOT_CONSULTED errors as string constants thrown or returned per test contract
    - getCallbackViewCounts: { active: number, archive: number } via two count queries or groupBy
    - createCallbackRequest: unchanged data shape (status defaults via Prisma)
  </behavior>
  <action>
    Per D-05: Move CallbackRequestAdminRow type and listCallbackRequestsAdmin from store-settings.service.ts into callback-request.service.ts. Delete those exports from store-settings.service.ts entirely.

    Create src/lib/callback/status-labels.ts:
    CALLBACK_STATUS_LABELS_UA: PENDING → «Очікує на дзвінок», CONSULTED → «Проконсультовано» (import CallbackRequestStatus from generated client).

    Implement admin functions to satisfy Plan 01 tests. Export error constants: CALLBACK_NOT_FOUND, CALLBACK_NOT_CONSULTED, CALLBACK_ALREADY_ARCHIVED.

    Grep repo for imports of CallbackRequestAdminRow or listCallbackRequestsAdmin from store-settings — update imports to callback-request.service (callback-requests-table fixed in Plan 03 if still broken).

    Do NOT touch admin-analytics.service.ts.
  </action>
  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npm test -- --run src/server/services/callback-request.service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All tests in callback-request.service.test.ts pass (create + admin)
    - store-settings.service.ts has zero references to CallbackRequest or listCallbackRequestsAdmin
    - status-labels.ts exports CALLBACK_STATUS_LABELS_UA with both enum keys
    - Note normalization: whitespace-only → null in updateCallbackRequestNote
  </acceptance_criteria>
  <done>Admin callback service complete; Plan 01 service tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Validators + admin server actions</name>
  <files>
    src/server/validators/admin-callback.ts
    src/server/actions/admin/callback.actions.ts
  </files>
  <read_first>
    - src/server/validators/admin-order.ts
    - src/server/actions/admin/order.actions.ts (requireAdmin, revalidatePath, error mapping)
    - src/server/validators/admin-callback.test.ts
    - .planning/phases/35-callback-calls/35-CONTEXT.md (D-08 discretion: three actions)
  </read_first>
  <behavior>
    - Each action: requireAdmin → Zod parse → service → revalidatePath("/admin/dzvinky") → { ok: true }
    - Zod failure → { ok: false, error: "VALIDATION" }
    - Service errors mapped to NOT_FOUND, NOT_CONSULTED, ALREADY_ARCHIVED
  </behavior>
  <action>
    Per discretion (three actions, not one union):

    Create admin-callback.ts validators:
    - callbackListViewSchema, listCallbacksAdminPageSchema
    - updateCallbackStatusSchema (id cuid, status enum)
    - updateCallbackNoteSchema (id cuid, note max 4000 optional)
    - archiveCallbackSchema (id cuid)

    Create src/server/actions/admin/callback.actions.ts:
    - updateCallbackStatusAction(input: unknown)
    - updateCallbackNoteAction(input: unknown)
    - archiveCallbackRequestAction(input: unknown)

    Mirror order.actions.ts: "use server", requireAdmin first, try/catch Error.message for service constants.

    revalidatePath("/admin/dzvinky") on success only.

    Do NOT revalidate or change analytics paths. Do NOT add Phase 36 badge queries.
  </action>
  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npm test -- --run src/server/validators/admin-callback.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - admin-callback.test.ts all green
    - Three exported actions from callback.actions.ts
    - Each action calls requireAdmin before parse
    - tsc --noEmit passes (fix any broken imports from moved types)
  </acceptance_criteria>
  <done>Validators and actions ready for Plan 03 UI wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Server Action | Unauthenticated users must not invoke admin callback mutations |
| Service → Database | Archive and status changes must enforce business rules server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Elevation of Privilege | updateCallbackStatusAction, updateCallbackNoteAction, archiveCallbackRequestAction | mitigate | requireAdmin() as first statement in each action; admin layout already guards routes |
| T-35-02 | Tampering | archiveCallbackRequest | mitigate | Service rejects archive when status !== CONSULTED (CALLBACK_NOT_CONSULTED); unit test in Plan 01 |
| T-35-03 | Tampering | updateCallbackNoteAction | mitigate | Zod max(4000) on note; trim; reject oversize before DB write |
| T-35-04 | Information Disclosure | listCallbackRequestsAdmin | accept | Admin-only surface; single-tenant store; requireAdmin on page in Plan 03 |
| T-35-SC | Tampering | prisma migrate / generate | mitigate | Official Prisma CLI only; no ad-hoc SQL; human resumes if DB unavailable |
</threat_model>

<verification>
npm test -- --run src/server/services/callback-request.service.test.ts
npm test -- --run src/server/validators/admin-callback.test.ts
npx tsc --noEmit
rg "listCallbackRequestsAdmin" src/server/services/store-settings.service.ts → 0 matches
rg "admin-analytics.service" prisma/schema.prisma → no erroneous edits; git diff admin-analytics.service.ts empty
</verification>

<success_criteria>
Migration applied; admin service + actions implemented; all Wave 0/1 unit tests green; analytics service untouched; storefront create still compiles.
</success_criteria>

<output>
Create .planning/phases/35-callback-calls/35-02-SUMMARY.md when done.
</output>
