---
phase: 35-callback-calls
plan: 03
type: execute
wave: 2
depends_on:
  - "35-02"
files_modified:
  - src/lib/admin/callbacks-url.ts
  - src/app/(admin)/admin/dzvinky/page.tsx
  - src/app/(admin)/admin/nalashtuvannia/page.tsx
  - src/components/admin/admin-nav-items.ts
  - src/components/admin/callback-list-filters.tsx
  - src/components/admin/callback-list-status-select.tsx
  - src/components/admin/callback-note-field.tsx
  - src/components/admin/callback-archive-button.tsx
  - src/components/admin/callback-requests-table.tsx
autonomous: false
requirements:
  - CALL-01
  - CALL-02
  - CALL-03
  - CALL-04

must_haves:
  truths:
    - "D-01: Dedicated page src/app/(admin)/admin/dzvinky/page.tsx is the callback workspace"
    - "D-02: Active/archive tab chips via ?view=active|archive (default active, no query)"
    - "D-03: Table lists up to 200 rows sorted createdAt desc"
    - "D-04: «В архів» only enabled when status CONSULTED; one-way archive (no unarchive)"
    - "D-05: nalashtuvannia/page.tsx has no callback section or listCallbackRequestsAdmin import"
    - "D-08: CallbackListStatusSelect in active rows (PENDING|CONSULTED)"
    - "D-09: CallbackNoteField Textarea + explicit «Зберегти» (no blur auto-save)"
    - "D-11: Sidebar «Дзвінки» → /admin/dzvinky with Phone icon after «Чати»"
    - "D-12: No edits to admin-analytics.service.ts in this phase"
    - "Operator opens /admin/dzvinky from sidebar «Дзвінки» and sees callback table with active/archive tabs"
    - "/admin/nalashtuvannia has no «Заявки на дзвінок» section"
    - "Active tab: change status, save note, archive when CONSULTED"
    - "Archive tab: read-only rows (no status select, no save, no archive button)"
    - "Archive button disabled with tooltip when status is not CONSULTED"
  artifacts:
    - path: "src/app/(admin)/admin/dzvinky/page.tsx"
      provides: "Dedicated callback admin page"
      min_lines: 25
    - path: "src/components/admin/callback-list-filters.tsx"
      provides: "Active/archive tab chips"
    - path: "src/components/admin/admin-nav-items.ts"
      provides: "Дзвінки nav entry with Phone icon"
      contains: "/admin/dzvinky"
  key_links:
    - from: "src/app/(admin)/admin/dzvinky/page.tsx"
      to: "listCallbackRequestsAdmin"
      via: "RSC fetch with parsed view"
      pattern: "listCallbackRequestsAdmin"
    - from: "callback-list-status-select.tsx"
      to: "updateCallbackStatusAction"
      via: "client call + router.refresh"
      pattern: "updateCallbackStatusAction"
    - from: "src/app/(admin)/admin/nalashtuvannia/page.tsx"
      to: "StoreSettingsForm only"
      via: "no CallbackRequestsTable import"
      pattern: "StoreSettingsForm"
---

<objective>
Build the admin UI for callback requests: dedicated page, filters, extended table with client row controls, sidebar nav, and remove callback block from store settings.

Purpose: Deliver CALL-01–04 operator-facing workflow per 35-UI-SPEC.md and D-01–D-11. Do not implement Phase 36 sidebar badges (ADM-NAV-01).
Output: `/admin/dzvinky` production-ready; settings page callback-free.
</objective>

<execution_context>
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/phases/35-callback-calls/35-CONTEXT.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/phases/35-callback-calls/35-UI-SPEC.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/phases/35-callback-calls/35-PATTERNS.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/phases/35-callback-calls/35-02-SUMMARY.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/src/app/(admin)/admin/zamovlennia/page.tsx
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/src/components/admin/order-list-filters.tsx
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/src/components/admin/order-list-status-select.tsx
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/src/components/admin/callback-requests-table.tsx
</context>

<interfaces>
<!-- Page props (Next 16 — searchParams Promise) -->
```typescript
type PageProps = {
  searchParams: Promise<{ view?: string }>;
};
```

<!-- Service imports -->
```typescript
import {
  listCallbackRequestsAdmin,
  getCallbackViewCounts,
  type CallbackRequestAdminRow,
} from "@/server/services/callback-request.service";
import { listCallbacksAdminPageSchema } from "@/server/validators/admin-callback";
```

<!-- callbacks-url.ts -->
```typescript
export type AdminCallbackView = "active" | "archive";
export function adminCallbacksUrl(params?: { view?: AdminCallbackView }): string;
// active: /admin/dzvinky — archive: /admin/dzvinky?view=archive
```

<!-- Client action imports -->
```typescript
import { updateCallbackStatusAction, updateCallbackNoteAction, archiveCallbackRequestAction } from "@/server/actions/admin/callback.actions";
import { CALLBACK_STATUS_LABELS_UA } from "@/lib/callback/status-labels";
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: URL helper, list filters, extend table shell</name>
  <files>
    src/lib/admin/callbacks-url.ts
    src/components/admin/callback-list-filters.tsx
    src/components/admin/callback-requests-table.tsx
  </files>
  <read_first>
    - src/lib/admin/orders-url.ts
    - src/lib/admin-chat-url.ts (active/archive query omission pattern)
    - src/components/admin/order-list-filters.tsx (chip Link styling)
    - src/components/admin/callback-requests-table.tsx (current 2-column table)
    - .planning/phases/35-callback-calls/35-UI-SPEC.md (columns, empty states)
  </read_first>
  <action>
    Per D-02: Create src/lib/admin/callbacks-url.ts — CALLBACKS_PATH = "/admin/dzvinky"; omit view query for active; set view=archive for archive tab.

    Per D-02/D-11: Create callback-list-filters.tsx — two chips «Активні (N)» / «Архів (N)» using adminCallbacksUrl; same cn() classes as OrderListFilters (border-primary active state).

    Extend callback-requests-table.tsx:
    - Import CallbackRequestAdminRow from callback-request.service (not store-settings)
    - Add prop view: "active" | "archive"
    - Columns: Телефон, Дата, Статус, Нотатка, Дії (per UI-SPEC)
    - Phone: formatUaPhoneDisplay + tabular-nums; Date: uk-UA short
    - Empty copy: active → «Немає активних заявок»; archive → «Архів порожній»
    - Table shell: keep overflow-x-auto rounded-md border pattern

    For active view cells: import and render CallbackListStatusSelect, CallbackNoteField, CallbackArchiveButton (create stubs in Task 2 if needed — prefer Task 2 first for client files then wire here, or placeholder text until Task 2 completes within same plan wave).

    For archive view: status as static text via CALLBACK_STATUS_LABELS_UA; note truncated (e.g. line-clamp-2 or slice 80 chars) or «—» if null; no action column controls.

    Do NOT add sidebar badge counts (Phase 36).
  </action>
  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - callbacks-url.ts exports adminCallbacksUrl with correct paths
    - callback-list-filters.tsx uses Link chips, not shadcn Tabs
    - callback-requests-table accepts view prop and shows 5 column headers
    - No import from store-settings.service
  </acceptance_criteria>
  <done>Table shell and filters ready for client row widgets.</done>
</task>

<task type="auto">
  <name>Task 2: Client row controls (status, note, archive)</name>
  <files>
    src/components/admin/callback-list-status-select.tsx
    src/components/admin/callback-note-field.tsx
    src/components/admin/callback-archive-button.tsx
  </files>
  <read_first>
    - src/components/admin/order-list-status-select.tsx (full file — toast, refresh, Select sm)
    - src/components/admin/product-form.tsx (Зберегти button copy)
    - .planning/phases/35-callback-calls/35-UI-SPEC.md (status accents, note rows, archive tooltip)
    - .planning/phases/35-callback-calls/35-CONTEXT.md (D-08, D-09, D-04)
  </read_first>
  <action>
    Per D-08: callback-list-status-select.tsx — "use client"; props { id, status }; Select size sm min-w-[12rem]; options PENDING and CONSULTED with CALLBACK_STATUS_LABELS_UA; on change call updateCallbackStatusAction; toast success «Статус оновлено»; on NOT_CONSULTED etc. toast.error UA; router.refresh(); optional accent classes amber/emerald on trigger per UI-SPEC.

    Per D-09: callback-note-field.tsx — Textarea rows={3} className min-w-[16rem] max-w-md; local state from defaultValue note ?? ""; Button outline sm «Зберегти»; useTransition; updateCallbackNoteAction; toast «Нотатку збережено»; NO blur auto-save.

    Per D-04: callback-archive-button.tsx — Button outline sm «В архів»; disabled when status !== CONSULTED || pending; title tooltip «Спочатку позначте як проконсультовано» when disabled; archiveCallbackRequestAction; handle NOT_CONSULTED error toast.

    Wire all three into callback-requests-table.tsx active view only.

    Ukrainian error strings for operator toasts (match order patterns).
  </action>
  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npm run lint -- --max-warnings=0 src/components/admin/callback-list-status-select.tsx src/components/admin/callback-note-field.tsx src/components/admin/callback-archive-button.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - All three components are "use client"
    - Status select calls updateCallbackStatusAction with { id, status }
    - Note field uses explicit save button only
    - Archive disabled when status PENDING with visible title attribute
    - No badge or Phase 36 code
  </acceptance_criteria>
  <done>Row-level operator controls implemented and wired into table.</done>
</task>

<task type="auto">
  <name>Task 3: Dzvinky page, nav, strip nalashtuvannia (CALL-01)</name>
  <files>
    src/app/(admin)/admin/dzvinky/page.tsx
    src/components/admin/admin-nav-items.ts
    src/app/(admin)/admin/nalashtuvannia/page.tsx
  </files>
  <read_first>
    - src/app/(admin)/admin/zamovlennia/page.tsx (RSC searchParams + Promise.all pattern)
    - src/app/(admin)/admin/nalashtuvannia/page.tsx (section to remove)
    - src/components/admin/admin-nav-items.ts
    - .planning/phases/35-callback-calls/35-CONTEXT.md (D-01, D-05, D-11)
  </read_first>
  <action>
    Per D-01: Create src/app/(admin)/admin/dzvinky/page.tsx:
    - metadata.title = "Дзвінки"
    - Parse searchParams with listCallbacksAdminPageSchema (default view active)
    - Promise.all([ listCallbackRequestsAdmin({ view, limit: 200 }), getCallbackViewCounts() ])
    - Layout: min-w-0 space-y-6; H1 «Дзвінки» text-2xl font-semibold; optional subcopy text-sm text-muted-foreground «Заявки на зворотний дзвінок з сайту»
    - CallbackListFilters + CallbackRequestsTable

    Per D-11: admin-nav-items.ts — import Phone from lucide-react; insert after «Чати» (/admin/chaty), before «Аналітика»: { href: "/admin/dzvinky", label: "Дзвінки", icon: Phone }. No badge property.

    Per D-05/CALL-01: nalashtuvannia/page.tsx — remove CallbackRequestsTable import, listCallbackRequestsAdmin, parallel fetch of callbacks, entire section «Заявки на дзвінок». Keep only getAdminStoreSettings + StoreSettingsForm. Reduce outer spacing if only one section remains (space-y-8 acceptable).

    Verification grep gate for CALL-01:
    rg "Заявки на дзвінок" src/app/(admin)/admin/nalashtuvannia --glob "*.tsx" must return 0 lines.

    Manual UAT note (D-12): do not change admin-analytics; operator can verify KPI still counts archived requests in period.

    Do NOT implement ADM-NAV-01 unresolved badge on Дзвінки (Phase 36).
  </action>
  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && rg "Заявки на дзвінок" src/app/\(admin\)/admin/nalashtuvannia -c 2>/dev/null | awk '{exit ($1==0)?0:1}' && rg "/admin/dzvinky" src/components/admin/admin-nav-items.ts -c | awk '{exit ($1>=1)?0:1}' && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - dzvinky/page.tsx exists and is default-export async RSC
    - Nav contains Дзвінки → /admin/dzvinky with Phone icon between Chats and Analytics
    - nalashtuvannia has zero «Заявки на дзвінок» strings
    - tsc --noEmit passes
    - admin-analytics.service.ts not modified in this plan
  </acceptance_criteria>
  <done>CALL-01 delivered: dedicated page + nav; settings decoupled.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verify — dzvinky page and operator workflow</name>
  <read_first>
    - .planning/phases/35-callback-calls/35-UI-SPEC.md (tabs, columns, archive gate, empty states)
    - .planning/phases/35-callback-calls/35-CONTEXT.md (D-01..D-12, D-12 analytics unchanged)
    - src/app/(admin)/admin/dzvinky/page.tsx
    - src/app/(admin)/admin/nalashtuvannia/page.tsx
    - src/app/(admin)/admin/analityka/page.tsx (KPI regression only — read, do not edit)
    - src/components/admin/admin-nav-items.ts
  </read_first>
  <what-built>
    - Sidebar «Дзвінки» → /admin/dzvinky with active/archive tabs
    - Active tab: status select, note save, archive when CONSULTED
    - Archive tab: read-only rows
    - /admin/nalashtuvannia without callback section
  </what-built>
  <acceptance_criteria>
    - `/admin/nalashtuvannia` renders with zero occurrences of «Заявки на дзвінок» (grep or visual)
    - Sidebar «Дзвінки» navigates to `/admin/dzvinky` with H1 «Дзвінки» and default «Активні» tab
    - Active row: status «Очікує на дзвінок» by default; change to «Проконсультовано» persists after refresh
    - «В архів» disabled when status is PENDING (tooltip visible on hover)
    - After CONSULTED, «В архів» removes row from active list; row appears under «Архів» tab read-only
    - Note: save shows text after refresh; cleared note shows «—»
    - Archive tab: no status Select, no «Зберегти», no «В архів» on archived rows
    - `/admin/analityka` callback KPI loads without error (D-12 — no logic change required)
    - Browser devtools console has no errors during steps 2–7
  </acceptance_criteria>
  <how-to-verify>
    1. Start dev server: npm run dev
    2. Log in as admin; open http://localhost:3000/admin/nalashtuvannia
       - Verify: no «Заявки на дзвінок» section; only store settings form
    3. Sidebar: click «Дзвінки» → http://localhost:3000/admin/dzvinky
       - Verify: H1 «Дзвінки»; tabs «Активні (N)» and «Архів (N)»; default is active
    4. Active tab — existing or new callback row:
       - Verify: default status «Очікує на дзвінок»
       - Change to «Проконсультовано» → toast success → refresh keeps value
       - «В архів» disabled while PENDING; tooltip on hover when disabled
       - After CONSULTED: «В архів» works → row leaves active list
    5. Note: enter text → «Зберегти» → refresh shows note; clear note → save → shows «—»
    6. Archive tab: archived row visible; no status select, no save, no archive button
    7. Open /admin/analityka — callback KPI still loads (D-12 regression; count unchanged logic)
    8. No console errors in devtools
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. archive still enabled on PENDING).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Server Action | Malicious client could POST forged ids/status/note/archive |
| RSC page → Service | view param must be validated before query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Elevation of Privilege | Client row actions | mitigate | Actions call requireAdmin (Plan 02); UI is not sole gate |
| T-35-02 | Tampering | archiveCallbackRequestAction | mitigate | Server rejects non-CONSULTED (Plan 02); UI disables button + title tooltip |
| T-35-03 | Tampering | updateCallbackNoteAction | mitigate | Zod max 4000; empty → null in service |
| T-35-05 | Spoofing | view searchParam | mitigate | listCallbacksAdminPageSchema parse on RSC page before list query |
| T-35-SC | Tampering | npm installs | accept | No new packages in Plan 03 |
</threat_model>

<verification>
rg "Заявки на дзвінок" src/app/(admin)/admin/nalashtuvannia → 0
rg "listCallbackRequestsAdmin" src/app/(admin)/admin/nalashtuvannia → 0
rg "/admin/dzvinky" src/components/admin/admin-nav-items.ts → match
npm test && npx tsc --noEmit
Human verification Task 4 approved.
</verification>

<success_criteria>
Operator can manage callbacks on /admin/dzvinky; settings page has no callback table; all CALL-01–04 behaviors visible in UI; Phase 36 badges not added; human UAT approved.
</success_criteria>

<output>
Create .planning/phases/35-callback-calls/35-03-SUMMARY.md when done.
</output>
