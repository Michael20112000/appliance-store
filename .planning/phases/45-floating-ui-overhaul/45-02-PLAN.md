---
phase: 45-floating-ui-overhaul
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/storefront-fabs.tsx
  - src/components/layout/storefront-fabs.test.tsx
  - src/components/chat/chat-provider.tsx
  - src/components/chat/chat-provider-gate.tsx
  - src/app/(storefront)/layout.tsx
autonomous: true
requirements:
  - FAB-04

must_haves:
  truths:
    - "All three FABs (callback, cart, chat) appear in the bottom-right corner, stacked vertically in that order"
    - "The callback dialog renders visually above the FAB group — no buttons bleed through the backdrop"
    - "The chat button disappears when the chat panel is open"
    - "StorefrontFabs renders inside ChatContext.Provider so useChat() is available"
    - "ChatFab is no longer rendered separately by ChatProvider"
  artifacts:
    - path: "src/components/layout/storefront-fabs.tsx"
      provides: "Consolidated FAB group: callback + cart + chat, positioned right-6 z-[49]"
      contains: "right-6"
    - path: "src/components/chat/chat-provider.tsx"
      provides: "Renders StorefrontFabs instead of ChatFab inside ChatContext.Provider"
      contains: "StorefrontFabs"
    - path: "src/components/chat/chat-provider-gate.tsx"
      provides: "Threads phones and initialCartCount props to ChatProvider"
      contains: "phones"
    - path: "src/app/(storefront)/layout.tsx"
      provides: "Passes phones/cartCount to ChatProviderGate; no longer renders StorefrontFabs directly"
    - path: "src/components/layout/storefront-fabs.test.tsx"
      provides: "Tests verifying chat button presence, visibility toggling, and right-6 wrapper"
      contains: "FAB-04"
  key_links:
    - from: "layout.tsx"
      to: "ChatProviderGate"
      via: "phones + initialCartCount props"
      pattern: "phones=\\{contacts\\.phones\\}"
    - from: "ChatProviderGate"
      to: "ChatProvider"
      via: "phones + initialCartCount pass-through"
      pattern: "phones=\\{phones\\}"
    - from: "ChatProvider JSX"
      to: "StorefrontFabs"
      via: "render inside ChatContext.Provider"
      pattern: "<StorefrontFabs"
    - from: "StorefrontFabs"
      to: "useChat()"
      via: "import from chat-provider, call inside component"
      pattern: "useChat\\(\\)"
---

<objective>
Consolidate all three floating action buttons (callback, cart, chat) into a single bottom-right column inside `StorefrontFabs`, fix the z-index so the dialog backdrop covers the FAB group, and move `StorefrontFabs` render point into `ChatProvider`'s JSX so `useChat()` is accessible.

Purpose: FAB-04 — currently `StorefrontFabs` (callback + cart) sits at `bottom-left` z-[59] and `ChatFab` is separate at `bottom-right` z-[60]. This phase merges them: one group, `right-6`, `z-[49]`, with the dialog at `z-50` correctly covering it.

Output: Five file changes (storefront-fabs, chat-provider, chat-provider-gate, layout, test file). No new packages. Chat button inherits context access by being rendered inside `ChatContext.Provider`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/45-floating-ui-overhaul/45-RESEARCH.md
@.planning/phases/45-floating-ui-overhaul/45-PATTERNS.md
@.planning/phases/45-floating-ui-overhaul/45-VALIDATION.md
</context>

<interfaces>
From src/components/layout/storefront-fabs.tsx (current):
- Wrapper div: `className="fixed bottom-6 left-6 z-[59] flex flex-col items-center gap-3 pb-[max(0px,env(safe-area-inset-bottom))]"`
- Props: `{ phones: PublicStorePhone[], initialCartCount: number, hasSession: boolean }`
- Two FABs: callback button + cart Link; Dialog inside same wrapper div
- No useChat import

From src/components/chat/chat-provider.tsx (current):
- `ChatProviderProps`: `{ children?, hasSession, initialConversationId?, initialConversationStatus?, initialUnreadFromStore? }`
- Renders `<ChatFab />` (dynamic import) + `<ChatPanel />` inside `<ChatContext.Provider value={value}>`
- `useChat()` export at line 383 — throws if called outside ChatContext.Provider
- Dynamic import block lines 25-28: `const ChatFab = dynamic(...)`

From src/components/chat/chat-provider-gate.tsx (current):
- Async RSC; props: `{ children: React.ReactNode }` only
- Renders `<ChatProvider hasSession initialConversationId initialConversationStatus initialUnreadFromStore>{children}</ChatProvider>`

From src/app/(storefront)/layout.tsx (current):
- Renders `<StorefrontFabs phones={contacts.phones} initialCartCount={cartCount} hasSession={Boolean(session?.user)} />` after `<Toaster />`
- Renders `<ChatProviderGate>` with no data props

From src/components/chat/chat-fab.tsx (not modified, for reference):
- `"use client"` — calls `useChat()` for `{ isOpen, openPanel, unreadFromStore, hasSession }`
- Returns null when `isOpen` is true
- Renders button with `MessageSquare` icon and unread dot badge
- Current position: `fixed bottom-6 right-6 z-[60]`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update storefront-fabs.test.tsx — add useChat mock and FAB-04 tests</name>
  <files>src/components/layout/storefront-fabs.test.tsx</files>
  <read_first>
    src/components/layout/storefront-fabs.test.tsx
    src/components/layout/storefront-fabs.tsx
    src/components/chat/chat-provider.tsx
  </read_first>
  <behavior>
    These tests MUST fail before Task 2 is applied:
    - FAB-04-a: wrapper div has className containing "right-6" (currently it has "left-6")
    - FAB-04-b: chat button with aria-label "Відкрити чат з магазином" is present when isOpen=false
    - FAB-04-c: chat button is NOT in the DOM when isOpen=true
    FAB-04-d (already passes after Task 2): chat button visible when isOpen=false (same as FAB-04-b, separate assertion style)
  </behavior>
  <action>
    Add to `storefront-fabs.test.tsx` at the top of the file, after the existing vi.mock blocks and before the `import React` line:

    New vi.mock for chat-provider:
    ```
    vi.mock("@/components/chat/chat-provider", () => ({
      useChat: vi.fn().mockReturnValue({
        isOpen: false,
        openPanel: vi.fn(),
        unreadFromStore: false,
        hasSession: false,
      }),
    }))
    ```

    Add the import line: `import { useChat } from "@/components/chat/chat-provider"` after the `import { StorefrontFabs }` line.

    Add four new tests inside the existing `describe("StorefrontFabs")` block:

    Test "FAB-04-a: wrapper div className includes right-6":
    - render `<StorefrontFabs phones={[]} initialCartCount={0} hasSession={false} />`
    - query the wrapper via `document.querySelector('.fixed.bottom-6')` or get the button with aria-label "Замовити дзвінок" and walk to parentElement
    - assert that the wrapper element's className includes "right-6" and does NOT include "left-6"

    Test "FAB-04-b: chat FAB renders when isOpen is false":
    - `vi.mocked(useChat).mockReturnValue({ isOpen: false, openPanel: vi.fn(), unreadFromStore: false, hasSession: false })`
    - render `<StorefrontFabs phones={[]} initialCartCount={0} hasSession={false} />`
    - `screen.getByRole("button", { name: "Відкрити чат з магазином" })` must be defined

    Test "FAB-04-c: chat FAB is hidden when isOpen is true":
    - `vi.mocked(useChat).mockReturnValue({ isOpen: true, openPanel: vi.fn(), unreadFromStore: false, hasSession: false })`
    - render `<StorefrontFabs phones={[]} initialCartCount={0} hasSession={false} />`
    - `screen.queryByRole("button", { name: "Відкрити чат з магазином" })` must be null

    Test "FAB-04-d: chat FAB has correct aria-label":
    - `vi.mocked(useChat).mockReturnValue({ isOpen: false, openPanel: vi.fn(), unreadFromStore: false, hasSession: false })`
    - render `<StorefrontFabs phones={[]} initialCartCount={0} hasSession={false} />`
    - button with name "Відкрити чат з магазином" must exist

    Run the tests — FAB-04-a, FAB-04-b, FAB-04-c should fail (StorefrontFabs doesn't have useChat yet). Confirm RED state before proceeding to Task 2.
  </action>
  <verify>
    <automated>npm test -- --reporter=verbose src/components/layout/storefront-fabs.test.tsx 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `storefront-fabs.test.tsx` contains `vi.mock("@/components/chat/chat-provider", ...)` with `useChat` stub
    - `storefront-fabs.test.tsx` contains tests named FAB-04-a, FAB-04-b, FAB-04-c, FAB-04-d
    - Tests FAB-04-a, FAB-04-b, FAB-04-c fail at this stage (RED) — they will pass after Task 2 implements the changes
    - All pre-existing FAB-01 and FAB-02 tests still pass (the useChat mock does not break existing tests because the current component doesn't call useChat)
  </acceptance_criteria>
  <done>Four new FAB-04 tests exist; three are red (expected); existing tests remain green.</done>
</task>

<task type="auto">
  <name>Task 2: Implement FAB consolidation — storefront-fabs, chat-provider, chat-provider-gate, layout</name>
  <files>
    src/components/layout/storefront-fabs.tsx,
    src/components/chat/chat-provider.tsx,
    src/components/chat/chat-provider-gate.tsx,
    src/app/(storefront)/layout.tsx
  </files>
  <read_first>
    src/components/layout/storefront-fabs.tsx
    src/components/chat/chat-provider.tsx
    src/components/chat/chat-provider-gate.tsx
    src/app/(storefront)/layout.tsx
    src/components/chat/chat-fab.tsx
    .planning/phases/45-floating-ui-overhaul/45-PATTERNS.md
  </read_first>
  <action>
    Make the following four changes in order. Each change is surgical — modify only the specified lines/blocks.

    --- storefront-fabs.tsx ---
    1. Add `MessageSquare` to the lucide-react import (line 5): `import { MessageSquare, Phone, ShoppingCart } from "lucide-react";`
    2. Add import after the `cn` import line: `import { useChat } from "@/components/chat/chat-provider";`
    3. Inside the `StorefrontFabs` function body, add after the existing `useState` calls (after the `setCallbackOpen` line, before the first `useEffect`):
       `const { isOpen: chatIsOpen, openPanel, unreadFromStore, hasSession: chatHasSession } = useChat();`
       Rename to avoid shadowing: `isOpen` → `chatIsOpen`, `hasSession` → `chatHasSession`.
    4. Change the wrapper div className from `"fixed bottom-6 left-6 z-[59] ..."` to `"fixed bottom-6 right-6 z-[49] ..."` (two class changes: left-6→right-6 and z-[59]→z-[49]). All other classes on that div stay identical.
    5. After the closing `</Link>` of the cart FAB (currently the last item before the Dialog), and before `{/* FAB-02: Callback Dialog */}`, insert the chat FAB slot:
       A `<button>` with `type="button"`, `onClick={() => openPanel()}`, `aria-label="Відкрити чат з магазином"`, and the same `cn(...)` className pattern as the existing callback/cart FABs (add "relative" to it for badge positioning). Inside: `<MessageSquare className="size-6" aria-hidden />`. Below that, conditionally render the unread dot: `{chatHasSession && unreadFromStore ? (<span className="absolute -top-0.5 -right-0.5 size-3 rounded-full bg-primary ring-2 ring-background" aria-hidden />) : null}`. Wrap the entire button in `{!chatIsOpen && (...)}` so it hides when the chat panel is open.

    --- chat-provider.tsx ---
    6. Delete the four-line `ChatFab` dynamic import block (lines 25-28): `const ChatFab = dynamic(...)`.
    7. Add two imports at the top of the file (after the existing imports): `import { StorefrontFabs } from "@/components/layout/storefront-fabs";` and `import type { PublicStorePhone } from "@/server/services/store-settings.service";`.
    8. Add two props to `ChatProviderProps` type: `phones: PublicStorePhone[];` and `initialCartCount: number;`.
    9. Add `phones` and `initialCartCount` to the `ChatProvider` function destructuring parameters.
    10. In the JSX return, replace `<ChatFab />` with `<StorefrontFabs phones={phones} initialCartCount={initialCartCount} hasSession={hasSession} />`. `ChatPanel` dynamic import and render remain unchanged.

    --- chat-provider-gate.tsx ---
    11. Add import: `import type { PublicStorePhone } from "@/server/services/store-settings.service";`
    12. Add `phones: PublicStorePhone[];` and `initialCartCount: number;` to the function props destructuring and the inline type.
    13. Pass `phones={phones}` and `initialCartCount={initialCartCount}` to the `<ChatProvider>` render. All existing props stay.

    --- layout.tsx ---
    14. Remove the `import { StorefrontFabs } from "@/components/layout/storefront-fabs";` line.
    15. Add `phones={contacts.phones}` and `initialCartCount={cartCount}` props to `<ChatProviderGate>`.
    16. Delete the entire `<StorefrontFabs phones={contacts.phones} initialCartCount={cartCount} hasSession={Boolean(session?.user)} />` element (the three-line JSX block after `<Toaster />`). The comment line if any should also be removed.

    Note: `hasSession` no longer needs to be passed from `layout.tsx` — `ChatProviderGate` derives it from its own `auth.api.getSession` call and passes it to `ChatProvider`.

    After all changes: run `npm test -- src/components/layout/storefront-fabs.test.tsx` to confirm FAB-04-a through FAB-04-d are now GREEN.
  </action>
  <verify>
    <automated>npm test -- --reporter=verbose src/components/layout/storefront-fabs.test.tsx src/components/layout/callback-request-form.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `storefront-fabs.tsx` wrapper div className contains "right-6" and "z-[49]" (no "left-6", no "z-[59]")
    - `storefront-fabs.tsx` imports `useChat` from "@/components/chat/chat-provider"
    - `storefront-fabs.tsx` has a button with `aria-label="Відкрити чат з магазином"` inside the wrapper div
    - `storefront-fabs.tsx` wraps the chat button with `{!chatIsOpen && (...)}`
    - `chat-provider.tsx` has no `ChatFab` dynamic import (grep for "chat-fab" returns no results in that file)
    - `chat-provider.tsx` imports `StorefrontFabs` from "@/components/layout/storefront-fabs"
    - `chat-provider.tsx` renders `<StorefrontFabs phones={phones} initialCartCount={initialCartCount} hasSession={hasSession} />` in place of `<ChatFab />`
    - `chat-provider-gate.tsx` accepts and passes `phones` and `initialCartCount` props to `ChatProvider`
    - `layout.tsx` has no `StorefrontFabs` import and no `<StorefrontFabs` JSX
    - `layout.tsx` renders `<ChatProviderGate phones={contacts.phones} initialCartCount={cartCount}>`
    - `npm test -- src/components/layout/storefront-fabs.test.tsx` exits 0 with all FAB-01, FAB-02, and FAB-04 tests green
    - `npm test` exits 0 (full suite green)
    - TypeScript: `npx tsc --noEmit` exits 0 (no new type errors)
  </acceptance_criteria>
  <done>All five files updated; all FAB-04 tests green; full suite green; no TypeScript errors; chat FAB is inside the right-side column and disappears when chat is open.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC layout → ChatProviderGate | phones and initialCartCount flow from server-fetched data (getPublicStoreContacts, getCartItemCount) — trusted server-side values |
| ChatContext.Provider → StorefrontFabs | StorefrontFabs reads useChat() context — no external input crosses this boundary |
| user click → openPanel() | User triggers chat panel open; hasSession check inside openPanel() redirects guests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-02 | Tampering | `storefront-fabs.tsx` chat button onClick | accept | `openPanel()` in chat-provider already checks `hasSession`; guests are redirected to login. No new input surface. |
| T-45-03 | Information Disclosure | `storefront-fabs.tsx` unread dot | accept | `unreadFromStore` is derived from session-scoped server data; no leakage to other users. |
| T-45-04 | Spoofing | `chat-provider-gate.tsx` session check | accept | Session check via `auth.api.getSession` unchanged; adding prop threading does not affect auth flow. |
| T-45-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan. |
</threat_model>

<verification>
Run after both tasks complete:

1. `npm test -- --reporter=verbose src/components/layout/storefront-fabs.test.tsx` — all tests green including FAB-04-a through FAB-04-d
2. `npm test` — full suite green
3. `npx tsc --noEmit` — exits 0 (no type errors from prop threading)
4. Manual check: visit any storefront page → FABs appear bottom-right in column (callback top, cart middle, chat bottom)
5. Manual check: open callback dialog → no FABs visible through backdrop
6. Manual check: open chat panel → chat FAB disappears
</verification>

<success_criteria>
1. Three FABs in single bottom-right column — confirmed by FAB-04-a test (wrapper has "right-6") and visual check
2. Dialog backdrop covers all FABs — confirmed by z-[49] vs z-50 stacking (FAB wrapper at z-[49], dialog at z-50)
3. Chat button hidden when chat is open — confirmed by FAB-04-c test
4. No TypeScript errors from prop threading
5. Full test suite exits 0
</success_criteria>

<output>
Create `.planning/phases/45-floating-ui-overhaul/45-02-SUMMARY.md` when done
</output>
