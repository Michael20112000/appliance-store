---
phase: 17-admin-chat-inbox-layout
plan: 02
type: execute
wave: 2
depends_on:
  - "17-01"
files_modified:
  - src/components/chat/conversation-list.tsx
  - src/components/chat/chat-thread.tsx
autonomous: true
requirements:
  - ADM-CHAT-02
context_decisions:
  - "D-17-07: full-height panels with centered empty states"
  - "D-17-12: desktop thread ScrollArea via MessageList default"
  - "D-17-13: list native overflow-y-auto only"
  - "D-17-14: min-h-0 on column scroll shells"
  - "D-17-15: composer fixed at bottom of thread column"
  - "D-17-16: preserve context menu and lifecycle UX"
  - "Discretion: useNativeScroll true on mobile admin thread"
  - "Discretion: isPanelOpen wired for auto-scroll"

must_haves:
  truths:
    - "Conversation list scrolls inside left column with native overflow-y-auto"
    - "Empty and loading list states fill panel height and center content"
    - "Thread messages scroll inside MessageList; composer stays at column bottom"
    - "Desktop uses ScrollArea for messages; mobile admin thread uses native scroll"
    - "Opening a thread scrolls to latest when isPanelOpen is true"
  artifacts:
    - path: src/components/chat/conversation-list.tsx
      provides: full-height column shell + listbox scroll
      contains: "overflow-y-auto"
    - path: src/components/chat/chat-thread.tsx
      provides: thread column flex + MessageList props
      contains: "isPanelOpen"
  key_links:
    - from: src/components/chat/chat-thread.tsx
      to: src/components/chat/message-list.tsx
      via: MessageList props useNativeScroll isPanelOpen
      pattern: "isPanelOpen=\\{Boolean"
    - from: src/components/chat/conversation-list.tsx
      to: grid column wrapper
      via: flex-1 min-h-0 parent from admin-chat-inbox
      pattern: "flex-1 min-h-0 overflow-y-auto"

---

<objective>
Implement internal scroll in list and thread columns and wire MessageList scroll props for admin (D-17-07, D-17-12–15).

Purpose: ADM-CHAT-02 — bounded height from plan 01 enables leaf scroll containers; this plan adds scroll shells and auto-scroll wiring.
Output: `conversation-list.tsx` and `chat-thread.tsx` with Messenger-style full-height panels and correct scroll primitives.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-admin-chat-inbox-layout/17-01-SUMMARY.md
@.planning/phases/17-admin-chat-inbox-layout/17-UI-SPEC.md
@.planning/phases/17-admin-chat-inbox-layout/17-RESEARCH.md
@.planning/phases/17-admin-chat-inbox-layout/17-PATTERNS.md
@.planning/debug/resolved/mobile-chat-scroll.md
@src/components/chat/conversation-list.tsx
@src/components/chat/chat-thread.tsx
@src/components/chat/message-list.tsx
@src/components/chat/chat-panel.tsx
</context>

<interfaces>
From message-list.tsx MessageListProps:
- useNativeScroll?: boolean — native div with touch-pan-y when true
- isPanelOpen?: boolean — gates auto-scroll effects (lines 65-94)

From chat-panel.tsx (buyer reference — lines 101-107):
- useNativeScroll={useNativeScroll}
- isPanelOpen={isOpen}

From conversation-list.tsx (current gaps):
- loading/empty/populated branches lack shared column shell with flex-1 min-h-0
- populated root: overflow-y-auto on outer div without flex-1 min-h-0 inner listbox

Target ConversationList structure (17-UI-SPEC):
- Outer: `flex min-h-0 flex-1 flex-col overflow-hidden border-r border-border`
- Listbox: `flex-1 min-h-0 overflow-y-auto` with role="listbox" aria-label preserved
- Empty: `flex flex-1 min-h-0 flex-col items-center justify-center px-4 py-12 text-center`
- Loading: full-height column with aria-busy skeletons

Target ChatThread:
- Root: `flex min-h-0 flex-1 flex-col overflow-hidden`
- Header + disconnect banner: `shrink-0`
- MessageList: flex-1 min-h-0 region (via MessageList classes)
- AdminChatComposer: after MessageList, shrink-0 behavior preserved
- Empty unselected: `flex min-h-0 flex-1 flex-col items-center justify-center`

useIsMobile: duplicate hook exists in admin-chat-inbox.tsx — import shared `@/hooks/use-mobile` if project has it, else copy minimal matchMedia hook into chat-thread.tsx only (do not refactor inbox in this plan unless hook already exported).
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client UI | Layout and scroll behavior only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02-01 | Tampering | Context menu row handlers | mitigate | Do not modify ContextMenuTrigger, onContextMenu, or lifecycle menu wiring (D-17-16) |
| T-17-02-02 | Tampering | message-list data-slot | mitigate | Do not rename scroll-area-viewport data-slot; auto-scroll uses closest() |
| T-17-SC | Tampering | npm installs | accept | No new packages |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: ConversationList full-height column and native scroll</name>
  <read_first>
    - src/components/chat/conversation-list.tsx — loading (187-197), empty (200-206), populated (209-226)
    - .planning/phases/17-admin-chat-inbox-layout/17-UI-SPEC.md — ConversationList scroll container
    - .planning/phases/17-admin-chat-inbox-layout/17-CONTEXT.md — D-17-07, D-17-13
    - .planning/phases/17-admin-chat-inbox-layout/17-PATTERNS.md — conversation-list target shape
  </read_first>
  <files>src/components/chat/conversation-list.tsx</files>
  <action>
    Per D-17-07, D-17-13, D-17-14:

    Introduce shared column shell used by ALL branches (loading, empty, populated):
    - Outer wrapper: `flex min-h-0 flex-1 flex-col overflow-hidden border-r border-border`

    Loading branch:
    - Use column shell; inner `flex flex-1 flex-col gap-1 p-2` with skeletons
    - Keep `aria-busy="true"`

    Empty branch:
    - Use column shell; inner `flex flex-1 min-h-0 flex-col items-center justify-center px-4 py-12 text-center`
    - Preserve emptyTitle / emptyBody copy exactly

    Populated branch:
    - Column shell outer
    - Inner listbox: `className="min-h-0 flex-1 overflow-y-auto"` with `role="listbox"` and `aria-label="Діалоги з покупцями"`
    - Map rows unchanged; preserve enableContextMenu, ContextMenu, ConversationListRow

    Do NOT wrap list in ScrollArea (D-17-13).
    Do NOT change row onClick, onContextMenu, or lifecycle hooks.
  </action>
  <verify>
    <automated>grep -q 'flex-1 min-h-0 overflow-y-auto' src/components/chat/conversation-list.tsx && ! grep -q 'ScrollArea' src/components/chat/conversation-list.tsx && npm run lint 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - All three branches use shared full-height column shell
    - Populated list scrolls on inner listbox with `flex-1 min-h-0 overflow-y-auto`
    - No ScrollArea import added
    - Context menu and listbox ARIA preserved
    - `npm run lint` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ChatThread column flex and MessageList scroll wiring</name>
  <read_first>
    - src/components/chat/chat-thread.tsx — empty state (65-75), root (84), MessageList call (150-157)
    - src/components/chat/chat-panel.tsx — MessageList prop wiring
    - src/components/chat/message-list.tsx — useNativeScroll branch and isPanelOpen guards
    - src/components/chat/admin-chat-inbox.tsx — useIsMobile hook (lines 13-25)
    - .planning/phases/17-admin-chat-inbox-layout/17-RESEARCH.md — Pitfall 5/6
  </read_first>
  <files>src/components/chat/chat-thread.tsx</files>
  <action>
    Per D-17-12, D-17-14, D-17-15, D-17-16 and RESEARCH discretion:

    Empty unselected state (no selectedConversationId):
    - Outer: `flex min-h-0 flex-1 flex-col items-center justify-center px-6 py-12 text-center`
    - Copy unchanged («Оберіть діалог» / «Оберіть покупця зі списку…»)

    Selected thread root:
    - Change to `flex min-h-0 flex-1 flex-col overflow-hidden`
    - Header block (lines 85-133): ensure `shrink-0` on header container
    - Disconnect banner block: wrap or add `shrink-0`

    MessageList — add props:
    - `useNativeScroll={isMobile}` where isMobile from `@/hooks/use-mobile` OR duplicated matchMedia `(max-width: 767px)` hook consistent with admin-chat-inbox
    - `isPanelOpen={Boolean(selectedConversationId)}`

    Do NOT change markAdminRead effect, lifecycle dropdown, AdminChatComposer placement, or delete dialog.

    Do NOT edit message-list.tsx unless a one-line class tweak is required for flex-1 (prefer wiring only).

    Verify mentally: composer remains after MessageList; archived threads hide composer as today.
  </action>
  <verify>
    <automated>grep -q 'isPanelOpen' src/components/chat/chat-thread.tsx && grep -q 'useNativeScroll' src/components/chat/chat-thread.tsx && npm run lint 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Thread root has `overflow-hidden` and `min-h-0 flex-1`
    - MessageList receives `isPanelOpen={Boolean(selectedConversationId)}`
    - MessageList receives `useNativeScroll` tied to mobile breakpoint
    - Header/banner are shrink-0; composer still renders below MessageList when not archived
    - Lifecycle ⋮ menu and onBack «До списку» unchanged
    - `npm run lint` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `npm run lint` on conversation-list.tsx and chat-thread.tsx
- Desktop `/admin/chaty`: with 10+ conversations, window does not scroll; list column shows scrollbar
- Long thread: messages scroll inside right panel; `#admin-chat-message` visible at bottom
</verification>

<success_criteria>
- ADM-CHAT-02 internal scroll works in list and thread columns on desktop and mobile split views
- Full-height empty states; composer anchored at thread column bottom
- Auto-scroll hooks active via isPanelOpen
</success_criteria>

<output>
Create `.planning/phases/17-admin-chat-inbox-layout/17-02-SUMMARY.md` when done
</output>
