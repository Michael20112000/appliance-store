---
phase: 17-admin-chat-inbox-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/admin/admin-sidebar-shell.tsx
  - src/components/chat/admin-chat-inbox.tsx
autonomous: true
requirements:
  - ADM-CHAT-02
context_decisions:
  - "D-17-01: H1 + tabs fixed chrome, scroll only in grid"
  - "D-17-02: zero document scroll on /admin/chaty"
  - "D-17-03: gap-6 equivalent to space-y-6"
  - "D-17-04: flex flex-col + min-h-0 height chain on page"
  - "D-17-05: grid flex-1 fills viewport under chrome"
  - "D-17-06: shell main + inner card propagate min-h-0"
  - "D-17-08: md:grid-cols-[320px_1fr] unchanged"
  - "D-17-09: mobile list keeps H1"
  - "D-17-10: mobile thread keeps tabs"
  - "D-17-14: grid overflow-hidden + column min-h-0 wrappers"

must_haves:
  truths:
    - "Admin shell main and inner card pass bounded height to child pages via min-h-0 flex chain"
    - "AdminChatInbox page root uses flex-1 min-h-0 flex-col gap-6 with shrink-0 H1 and tabs"
    - "Inbox grid uses flex-1 min-h-0 overflow-hidden instead of min-h-[calc(100dvh-12rem)]"
    - "Grid columns wrapped in min-h-0 flex flex-col overflow-hidden shells"
    - "Other admin routes still render inside shell without forced flex-1 on page root"
  artifacts:
    - path: src/components/admin/admin-sidebar-shell.tsx
      provides: viewport height chain for admin pages
      contains: "min-h-0"
    - path: src/components/chat/admin-chat-inbox.tsx
      provides: page flex layout and grid sizing
      contains: "flex-1 min-h-0"
  key_links:
    - from: src/components/admin/admin-sidebar-shell.tsx
      to: src/components/chat/admin-chat-inbox.tsx
      via: children slot in inner card
      pattern: "flex min-h-0 flex-1 flex-col"
    - from: src/components/chat/admin-chat-inbox.tsx
      to: ConversationList / ChatThread
      via: grid column wrapper divs
      pattern: "min-h-0 flex-col overflow-hidden"

---

<objective>
Establish admin shell and `/admin/chaty` page height chain so the inbox grid can consume remaining viewport under fixed H1 + tabs (D-17-04–06, D-17-01–02).

Purpose: ADM-CHAT-02 — without a bounded flex chain, internal column scroll cannot activate.
Output: `admin-sidebar-shell.tsx` and `admin-chat-inbox.tsx` with flex-1/min-h-0 layout; calc-only `min-h` removed from grid.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-admin-chat-inbox-layout/17-CONTEXT.md
@.planning/phases/17-admin-chat-inbox-layout/17-UI-SPEC.md
@.planning/phases/17-admin-chat-inbox-layout/17-RESEARCH.md
@.planning/phases/17-admin-chat-inbox-layout/17-PATTERNS.md
@src/components/admin/admin-sidebar-shell.tsx
@src/components/chat/admin-chat-inbox.tsx
@src/components/chat/chat-panel.tsx
@src/app/(admin)/admin/zamovlennia/page.tsx
</context>

<interfaces>
From admin-sidebar-shell.tsx (current — replace calc min-h):
- main: `flex flex-1 flex-col bg-muted p-4 md:p-6`
- inner card: `min-h-[calc(100dvh-3rem)] flex-1` — REMOVE calc; use `flex min-h-0 flex-1 flex-col`

From admin-chat-inbox.tsx (current — lines 90–114):
- Page: `space-y-6` + grid `min-h-[calc(100dvh-12rem)]` — REPLACE
- showList / showThread logic: preserve exactly (D-17-09/10/11)

Target page root (17-UI-SPEC):
- `flex min-h-0 flex-1 flex-col gap-6`
- h1: `shrink-0 text-2xl font-semibold`
- AdminChatTabs wrapper: add `shrink-0` on tablist container
- grid: `grid min-h-0 flex-1 overflow-hidden rounded-lg border border-border md:grid-cols-[320px_1fr]`

Column wrappers (new):
- `{showList ? <motion.div className="flex min-h-0 flex-col overflow-hidden">...</motion.div> : null}`
- `{showThread ? <motion.div className="flex min-h-0 flex-col overflow-hidden"><ChatThread .../></motion.div> : null}`

Reference buyer chain (chat-panel.tsx line 94):
- `flex h-full min-h-0 flex-1 flex-col overflow-hidden`

Regression guard: zamovlennia/page.tsx uses `space-y-6` only — shell must NOT require all children to be `flex-1`.
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser CSS layout | Height chain affects all admin routes via shell |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01-01 | Tampering | admin-sidebar-shell layout | accept | Layout-only; no auth/data impact |
| T-17-01-02 | Denial of Service | Other admin pages layout break | mitigate | Shell adds min-h-0 only; do not force flex-1 on all page children — only AdminChatInbox opts in |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | No new packages this plan |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: AdminSidebarShell flex height chain</name>
  <read_first>
    - src/components/admin/admin-sidebar-shell.tsx — lines 29-32
    - .planning/phases/17-admin-chat-inbox-layout/17-UI-SPEC.md — Viewport Height Chain § shell touchpoints
    - .planning/phases/17-admin-chat-inbox-layout/17-PATTERNS.md — admin-sidebar-shell analog
    - src/components/chat/chat-panel.tsx — PanelBody flex pattern
    - src/app/(admin)/admin/zamovlennia/page.tsx — regression: space-y-6 pages must still work
  </read_first>
  <files>src/components/admin/admin-sidebar-shell.tsx</files>
  <action>
    Per D-17-06: propagate bounded height from shell to page children.

    On `main` (line 29): add `min-h-0` → `flex min-h-0 flex-1 flex-col bg-muted p-4 md:p-6`.

    On inner content card (line 30): remove `min-h-[calc(100dvh-3rem)]` and `md:min-h-[calc(100dvh-3rem)]`. Replace with `flex min-h-0 flex-1 flex-col rounded-lg border border-border bg-background p-4 shadow-sm md:p-6`.

    Do NOT add `flex-1` or `min-h-0` requirements to `{children}` — only structural ancestors change.

    If scroll still fails after plan 02, discretion A2: pass `className="min-h-0"` to `SidebarInset` — defer unless needed.

    Do not change SidebarProvider, AppSidebar, or mobile header `h-12 shrink-0`.
  </action>
  <verify>
    <automated>! grep -q 'min-h-\[calc' src/components/admin/admin-sidebar-shell.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `admin-sidebar-shell.tsx` has `min-h-0` on both `main` and inner card
    - No `min-h-[calc(100dvh` remains in shell file
    - `npm run lint` passes for touched file
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: AdminChatInbox page flex and grid column wrappers</name>
  <read_first>
    - src/components/chat/admin-chat-inbox.tsx — AdminChatInboxInner return block lines 90-114
    - .planning/phases/17-admin-chat-inbox-layout/17-CONTEXT.md — D-17-01 through D-17-05, D-17-08, D-17-14
    - .planning/phases/17-admin-chat-inbox-layout/17-UI-SPEC.md — Page Structure desktop + mobile
    - .planning/phases/17-admin-chat-inbox-layout/17-PATTERNS.md — admin-chat-inbox grid wrappers
  </read_first>
  <files>src/components/chat/admin-chat-inbox.tsx</files>
  <action>
    Per D-17-01, D-17-03, D-17-04, D-17-05, D-17-08, D-17-14:

    Replace outer `div className="space-y-6"` with `div className="flex min-h-0 flex-1 flex-col gap-6"`.

    Add `shrink-0` to h1 (already implicit block; add class for clarity per UI-SPEC).

    Wrap `AdminChatTabs` in `motion.div` or `div` with `shrink-0` (or add shrink-0 to AdminChatTabs root in same file).

    Replace grid classes:
    - REMOVE: `min-h-[calc(100dvh-12rem)]`
    - ADD: `grid min-h-0 flex-1 overflow-hidden rounded-lg border border-border md:grid-cols-[320px_1fr]`

    Wrap each visible column:
    - When `showList`: wrap `ConversationList` in `div className="flex min-h-0 flex-col overflow-hidden"`
    - When `showThread`: wrap `ChatThread` in `motion.div className="flex min-h-0 flex-col overflow-hidden"` (keep existing motion if present; otherwise plain div)

  Preserve unchanged:
    - `useIsMobile`, `showList`, `showThread`, `onBack`, `enableContextMenu={!isMobile}`, empty copy, AdminChatProvider wrapper
    - Mobile: H1 always visible (D-17-09); tabs always visible on thread view (D-17-10)
    - Do NOT hide tabs or h1 on mobile thread

  Do NOT edit ConversationList or ChatThread internals in this task — plan 02 owns column scroll leaves.
  </action>
  <verify>
    <automated>! grep -q 'min-h-\[calc(100dvh-12rem)\]' src/components/chat/admin-chat-inbox.tsx && grep -q 'flex-1 min-h-0' src/components/chat/admin-chat-inbox.tsx && npm run lint 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Page root uses `flex min-h-0 flex-1 flex-col gap-6`
    - Grid uses `flex-1 min-h-0 overflow-hidden` and `md:grid-cols-[320px_1fr]`
    - No `min-h-[calc(100dvh-12rem)]` in admin-chat-inbox.tsx
    - List and thread each wrapped in `min-h-0 flex-col overflow-hidden` container
    - `npm run lint` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `npm run lint` on modified TSX files
- Quick smoke: `/admin/zamovlennia` still renders table (no collapsed shell)
- `/admin/chaty` shows H1 + tabs + grid border (scroll behavior completed in plan 02)
</verification>

<success_criteria>
- Shell and inbox page establish flex-1/min-h-0 chain from SidebarInset main to inbox grid
- Calc-based grid min-height removed
- ADM-CHAT-02 height foundation ready for column scroll in plan 02
</success_criteria>

<output>
Create `.planning/phases/17-admin-chat-inbox-layout/17-01-SUMMARY.md` when done
</output>
