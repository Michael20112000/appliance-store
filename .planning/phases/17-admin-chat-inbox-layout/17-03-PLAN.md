---
phase: 17-admin-chat-inbox-layout
plan: 03
type: execute
wave: 3
depends_on:
  - "17-02"
files_modified:
  - .planning/phases/17-admin-chat-inbox-layout/17-MANUAL-CHECKLIST.md
  - e2e/admin-chat.spec.ts
autonomous: false
requirements:
  - ADM-CHAT-02
context_decisions:
  - "D-17-15: manual checklist 10+ dialogs, long thread, zero page scroll"
  - "D-17-16: regression desktop ПКМ, mobile split, lifecycle menus"

must_haves:
  truths:
    - "17-MANUAL-CHECKLIST.md exists with all regression steps from 17-UI-SPEC"
    - "Playwright asserts document is not vertically scrollable on /admin/chaty desktop"
    - "Existing admin-chat e2e tests still pass"
    - "Human signs off manual scroll and Phase 14/8 regression per D-17-15/16"
  artifacts:
    - path: .planning/phases/17-admin-chat-inbox-layout/17-MANUAL-CHECKLIST.md
      provides: operator manual verification script
    - path: e2e/admin-chat.spec.ts
      provides: automated scroll gate + existing chat e2e
  key_links:
    - from: e2e/admin-chat.spec.ts
      to: /admin/chaty
      via: page.goto and scrollHeight evaluate
      pattern: "scrollHeight.*clientHeight"

---

<objective>
Add verification artifacts and sign-off gate for ADM-CHAT-02 layout (D-17-15, D-17-16).

Purpose: Scroll behavior is manual-first; optional Playwright hardening catches document-scroll regressions; human checklist covers touch/mobile and context menu UX.
Output: `17-MANUAL-CHECKLIST.md`, extended `e2e/admin-chat.spec.ts`, human verification checkpoint.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-admin-chat-inbox-layout/17-02-SUMMARY.md
@.planning/phases/17-admin-chat-inbox-layout/17-UI-SPEC.md
@.planning/phases/17-admin-chat-inbox-layout/17-VALIDATION.md
@e2e/admin-chat.spec.ts
@e2e/helpers/admin.ts
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E test env | Uses admin login helper; no production data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03-01 | Repudiation | Manual checklist skipped | mitigate | blocking human-verify checkpoint before phase close |
| T-17-SC | Tampering | npm installs | accept | No new packages |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create 17-MANUAL-CHECKLIST.md</name>
  <read_first>
    - .planning/phases/17-admin-chat-inbox-layout/17-UI-SPEC.md — Regression Checklist § Layout & scroll, Phase 14/8, Realtime, Shell
    - .planning/phases/17-admin-chat-inbox-layout/17-CONTEXT.md — D-17-15, D-17-16
    - .planning/phases/14-admin-chat-context-menu/14-MANUAL-CHECKLIST.md — format reference if exists
  </read_first>
  <files>.planning/phases/17-admin-chat-inbox-layout/17-MANUAL-CHECKLIST.md</files>
  <action>
    Create `17-MANUAL-CHECKLIST.md` by copying the regression checklist from `17-UI-SPEC.md` § «Regression Checklist (manual — D-17-15, D-17-16)» into a standalone operator doc.

    Structure:
    - Title: Phase 17 — Admin Chat Inbox Layout — Manual Checklist
    - Prerequisites: `npm run dev`, admin login, seed with 10+ conversations and one long thread (50+ messages) if possible
    - Sections: Layout & scroll (desktop + mobile), Phase 14/8 UX, Realtime/messages, Shell/other admin routes
    - Each item: checkbox `- [ ]`, exact steps, expected outcome (zero page scroll, internal panel scroll, composer visible)
    - Footer: Sign-off lines (Date, Operator, Pass/Fail)

    Do not invent new copy strings for UI — reference existing UA labels from UI-SPEC.
    Do not add deferred scope (buyer chat, pagination, new tabs).
  </action>
  <verify>
    <automated>test -f .planning/phases/17-admin-chat-inbox-layout/17-MANUAL-CHECKLIST.md && grep -c '^- \[ \]' .planning/phases/17-admin-chat-inbox-layout/17-MANUAL-CHECKLIST.md | xargs test 10 -le</automated>
  </verify>
  <acceptance_criteria>
    - File exists with at least 10 checkbox items
    - Includes desktop 10+ dialogs, long thread, mobile list/thread, tab switch, ПКМ regression
    - Includes shell regression for another admin route
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Playwright document scroll gate on admin chat</name>
  <read_first>
    - e2e/admin-chat.spec.ts — existing tests and skip patterns
    - .planning/phases/17-admin-chat-inbox-layout/17-RESEARCH.md — Playwright scroll gate example
    - e2e/helpers/admin.ts — loginAsAdmin
  </read_first>
  <files>e2e/admin-chat.spec.ts</files>
  <action>
    Add a new test `admin chat inbox has no document scroll on desktop` (or equivalent name):

    - `page.setViewportSize({ width: 1280, height: 720 })`
    - `loginAsAdmin(page)` then `page.goto('/admin/chaty')`
    - Assert H1 «Чати» visible
    - Evaluate: `document.documentElement.scrollHeight <= document.documentElement.clientHeight + 2`
    - If first conversation option visible: click it; wait for `#admin-chat-message`; re-run scrollHeight evaluate (thread open must not enable page scroll)

    Follow existing skip pattern when no conversations: `test.skip(true, 'No conversations in inbox seed data')` only for thread-specific assertions, not for initial page load assertion.

    Do not remove or weaken existing tests (reply, ПКМ desktop/mobile, left-click).

    Keep timeout values consistent with file (15_000 where used).
  </action>
  <verify>
    <automated>grep -q 'scrollHeight' e2e/admin-chat.spec.ts && npm run lint 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - New test asserts document not scrollable on inbox load and after thread select (when data exists)
    - Existing admin-chat tests unchanged in behavior
    - `npm run lint` exits 0
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual checklist sign-off</name>
  <read_first>
    - .planning/phases/17-admin-chat-inbox-layout/17-MANUAL-CHECKLIST.md
  </read_first>
  <files>.planning/phases/17-admin-chat-inbox-layout/17-MANUAL-CHECKLIST.md</files>
  <action>
    Operator executes full manual regression per D-17-15 and D-17-16 using `17-MANUAL-CHECKLIST.md`. Claude does not mark phase complete until resume signal "approved" or documented failures.
  </action>
  <verify>
    <automated>npm run test:e2e -- e2e/admin-chat.spec.ts</automated>
  </verify>
  <done>All manual checklist items pass; e2e admin-chat green; operator typed "approved"</done>
  <what-built>Phase 17 layout: shell + inbox flex chain, column internal scroll, MessageList wiring, Playwright scroll gate</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open `http://localhost:3000/admin/chaty` (or project port).
    2. Work through every item in `17-MANUAL-CHECKLIST.md` on desktop (1280×720) and mobile (390×844).
    3. Run `npm run test:e2e -- e2e/admin-chat.spec.ts` — all tests green.
    4. Spot-check `/admin/zamovlennia` — layout not broken by shell change.
    5. Confirm: with 10+ dialogs and a long thread, the browser page itself does not scroll; only list and message panels scroll; composer stays at bottom of thread column.
  </how-to-verify>
  <resume-signal>Type "approved" when checklist passes, or list failing items</resume-signal>
</task>

</tasks>

<verification>
- `npm run test:e2e -- e2e/admin-chat.spec.ts` (requires dev DB seed + admin credentials from e2e helpers)
- All items in `17-MANUAL-CHECKLIST.md` checked by operator
</verification>

<success_criteria>
- Manual checklist artifact committed and executed
- Playwright scroll gate passes in CI/local e2e
- Human approves D-17-15 and D-17-16 regression
- ADM-CHAT-02 ready for `/gsd-verify-work`
</success_criteria>

<output>
Create `.planning/phases/17-admin-chat-inbox-layout/17-03-SUMMARY.md` when done
</output>
