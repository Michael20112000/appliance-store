---
phase: 38-dashboard-data-completeness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(admin)/admin/page.tsx
files_deleted:
  - src/components/admin/analytics-dashboard-preview.tsx
autonomous: true
requirements:
  - ADM-DASH-07

must_haves:
  truths:
    - "Dashboard /admin renders AnalyticsCharts (not mini preview charts) with orders and revenue series"
    - "Charts use the same component as /admin/analityka — grid, XAxis, CartesianGrid, ChartTooltip, h-[220px]"
    - "Data comes from getDashboardAnalyticsPreview() (30-day window) — no new analytics queries"
    - "Section header «Графіки» with link «Детальна аналітика →» to /admin/analityka"
    - "analytics-dashboard-preview.tsx is removed — no orphaned imports"
  artifacts:
    - path: "src/app/(admin)/admin/page.tsx"
      provides: "Dashboard with full AnalyticsCharts section"
      contains: "AnalyticsCharts"
  key_links:
    - from: "src/app/(admin)/admin/page.tsx"
      to: "src/components/admin/analytics-charts.tsx"
      via: "<AnalyticsCharts ordersByDay revenueByDay />"
      pattern: "AnalyticsCharts"
    - from: "src/app/(admin)/admin/page.tsx"
      to: "src/server/services/admin-analytics.service.ts"
      via: "getDashboardAnalyticsPreview()"
      pattern: "getDashboardAnalyticsPreview"
---

<objective>
Replace the stripped-down AnalyticsDashboardPreview on /admin with the full AnalyticsCharts component used on /admin/analityka, so dashboard charts are visually and functionally identical for the same 30-day dataset (ADM-DASH-07).

Purpose: Operators see the same chart fidelity on the home dashboard as on the dedicated analytics page.
Output: Updated admin/page.tsx; deleted analytics-dashboard-preview.tsx.
</objective>

<execution_context>
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/PROJECT.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/ROADMAP.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/phases/38-dashboard-data-completeness/38-RESEARCH.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/phases/38-dashboard-data-completeness/38-PATTERNS.md
</context>

<interfaces>
From src/components/admin/analytics-charts.tsx:
type AnalyticsChartsProps = {
  ordersByDay: DayPoint[];
  revenueByDay: DayPoint[];
};

From admin-analytics.service.ts:
export async function getDashboardAnalyticsPreview(): Promise<AnalyticsData>;
// Returns getAnalyticsData(30) — ordersByDay, revenueByDay already on analyticsPreview in page.tsx
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Swap dashboard preview for full AnalyticsCharts</name>
  <files>src/app/(admin)/admin/page.tsx, src/components/admin/analytics-dashboard-preview.tsx</files>
  <read_first>
    - src/app/(admin)/admin/page.tsx
    - src/app/(admin)/admin/analityka/page.tsx (charts section reference)
    - src/components/admin/analytics-charts.tsx
    - .planning/phases/38-dashboard-data-completeness/38-PATTERNS.md
  </read_first>
  <action>
    1. In src/app/(admin)/admin/page.tsx:
       - Remove import of AnalyticsDashboardPreview.
       - Add import: `import { AnalyticsCharts } from "@/components/admin/analytics-charts";`
       - Ensure `import Link from "next/link";` exists (already used for Buttons — reuse).
       - Replace the `<AnalyticsDashboardPreview … />` block (lines ~77-82) with:

```tsx
      <section className="space-y-4">
        <div className="flex items-center justify-between">
          <h2 className="text-lg font-semibold">Графіки</h2>
          <Link
            href="/admin/analityka"
            className="text-sm text-muted-foreground hover:text-foreground"
          >
            Детальна аналітика →
          </Link>
        </div>
        <AnalyticsCharts
          ordersByDay={analyticsPreview.ordersByDay}
          revenueByDay={analyticsPreview.revenueByDay}
        />
      </section>
```

       - Do NOT pass totalOrders/totalRevenue to charts (AnalyticsCharts does not accept KPI props).
       - Do NOT change Promise.all or getDashboardAnalyticsPreview call.

    2. Delete src/components/admin/analytics-dashboard-preview.tsx entirely.

    3. Grep repo for `analytics-dashboard-preview` — must return zero imports after delete.
  </action>
  <verify>
    <automated>npx vitest run src/server/services/admin-analytics.service.test.ts</automated>
    <automated>rg "analytics-dashboard-preview" --glob "*.tsx" --glob "*.ts" || true</automated>
  </verify>
  <acceptance_criteria>
    - admin/page.tsx imports AnalyticsCharts, not AnalyticsDashboardPreview
    - Section title is «Графіки» with link to /admin/analityka
    - AnalyticsCharts receives ordersByDay and revenueByDay from analyticsPreview
    - analytics-dashboard-preview.tsx file deleted
    - No remaining imports of analytics-dashboard-preview
    - vitest admin-analytics.service.test.ts passes
  </acceptance_criteria>
  <done>Dashboard shows full-size charts identical to /admin/analityka chart block; preview component removed</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>/admin dashboard «Графіки» section uses AnalyticsCharts with grid, axes, and tooltips.</what-built>
  <how-to-verify>
    1. npm run dev
    2. Open /admin and /admin/analityka?days=30 side by side (or sequentially)
    3. Confirm both show paired LineCharts at ~220px height with grid lines, X-axis date labels, and hover tooltips
    4. Revenue tooltip formats UAH via formatRevenue
    5. «Детальна аналітика →» navigates to /admin/analityka
  </how-to-verify>
  <resume-signal>Type "approved" or describe chart differences</resume-signal>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Disposition | Mitigation |
|-----------|----------|-------------|------------|
| T-38-01 | Elevation of Privilege | mitigate | requireAdmin() in admin layout |
| T-38-02 | Information Disclosure | accept | Same analytics aggregates as /admin/analityka |
| T-38-03 | DoS | accept | No new queries; same getDashboardAnalyticsPreview |
| T-38-SC | Tampering | accept | No new packages |
</threat_model>

<verification>
1. npx vitest run src/server/services/admin-analytics.service.test.ts
2. rg analytics-dashboard-preview — zero hits in src/
3. Manual: /admin charts match /admin/analityka?days=30
</verification>

<success_criteria>
- ADM-DASH-07: Dashboard charts identical to analytics page (component + visual treatment)
</success_criteria>

<output>
Create `.planning/phases/38-dashboard-data-completeness/38-01-SUMMARY.md` when done
</output>
