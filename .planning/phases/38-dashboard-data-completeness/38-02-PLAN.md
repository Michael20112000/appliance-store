---
phase: 38-dashboard-data-completeness
plan: 02
type: execute
wave: 2
depends_on:
  - 38-01-PLAN.md
files_modified:
  - src/server/services/admin-order.service.ts
  - src/server/services/admin-order.service.test.ts
  - src/components/admin/admin-recent-orders-table.tsx
autonomous: true
requirements:
  - ADM-DASH-08

must_haves:
  truths:
    - "getAdminDashboardStats returns at most 10 recent orders ordered by createdAt desc"
    - "AdminRecentOrdersTable shows 6 columns: Номер, Дата, Покупець, Доставка, Сума, Статус"
    - "Status column uses OrderListStatusSelect with accent styling (same as /admin/zamovlennia)"
    - "Row click navigates to /admin/zamovlennia/[orderNumber]"
    - "Table has no filter tabs, no pagination, no sortable column headers"
    - "Table wrapper matches orders page: rounded-lg border bg-background overflow-x-auto"
  artifacts:
    - path: "src/components/admin/admin-recent-orders-table.tsx"
      provides: "Dashboard recent orders table with full column parity"
      contains: "OrderListStatusSelect"
    - path: "src/server/services/admin-order.service.ts"
      provides: "take: 10 for recent orders"
      contains: "take: 10"
  key_links:
    - from: "admin-recent-orders-table.tsx"
      to: "orders-data-table.tsx"
      via: "column cell markup parity"
      pattern: "OrderListStatusSelect"
    - from: "admin/page.tsx"
      to: "admin-recent-orders-table.tsx"
      via: "orders={stats.recentOrders}"
      pattern: "AdminRecentOrdersTable"
---

<objective>
Upgrade the dashboard «Останні замовлення» table to match /admin/zamovlennia structure — six columns, interactive status accents, row navigation — while capping at 10 rows with no filters or pagination (ADM-DASH-08).

Purpose: Operators see the same order context on the dashboard as on the orders list without leaving home.
Output: take: 10 in service; refactored AdminRecentOrdersTable; updated unit test.
</objective>

<execution_context>
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/phases/38-dashboard-data-completeness/38-RESEARCH.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/phases/38-dashboard-data-completeness/38-PATTERNS.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/phases/37-dashboard-statcards/37-01-SUMMARY.md
</context>

<interfaces>
From admin-order.service.ts:
export type AdminOrderSummaryDto = {
  id: string;
  orderNumber: string;
  status: OrderStatus;
  deliveryType: DeliveryType;
  customerName: string;
  customerPhone: string;
  totalKopiyky: number;
  itemCount: number;
  createdAt: Date;
};

From orders-data-table.tsx — copy deliveryLabel and formatDate helpers inline in admin-recent-orders-table.tsx (do not import private helpers from orders-data-table).
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Bump recent orders limit to 10 and assert in tests</name>
  <files>src/server/services/admin-order.service.ts, src/server/services/admin-order.service.test.ts</files>
  <action>
    1. In getAdminDashboardStats(), change prisma.order.findMany take from 5 to 10.

    2. In admin-order.service.test.ts describe("getAdminDashboardStats"):
       - Add assertion: expect(prisma.order.findMany).toHaveBeenCalledWith(
           expect.objectContaining({ take: 10, orderBy: { createdAt: "desc" } })
         );
       - Keep existing count assertions intact.
  </action>
  <verify>
    <automated>npx vitest run src/server/services/admin-order.service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - findMany uses take: 10
    - Test asserts take: 10 and orderBy createdAt desc
    - All admin-order.service tests pass
  </acceptance_criteria>
  <done>Service returns up to 10 recent orders; test locked</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Refactor AdminRecentOrdersTable to 6-column parity</name>
  <files>src/components/admin/admin-recent-orders-table.tsx</files>
  <read_first>
    - src/components/admin/admin-recent-orders-table.tsx
    - src/components/admin/orders-data-table.tsx (columns customer, delivery, status)
    - src/components/admin/order-list-status-select.tsx
    - .planning/phases/38-dashboard-data-completeness/38-PATTERNS.md
  </read_first>
  <action>
    Rewrite admin-recent-orders-table.tsx:

    1. Props: `orders: AdminOrderSummaryDto[]` — remove AdminRecentOrderRow Pick type export (or keep export only if used elsewhere; grep first).

    2. Replace raw `<table>` with shadcn Table components:
       import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";

    3. Import OrderListStatusSelect from "@/components/admin/order-list-status-select".
       Remove OrderStatusBadge import.

    4. Add local helpers (copy from orders-data-table):
       - formatDate (uk-UA medium+short)
       - deliveryLabel: PICKUP → «Самовивіз», else «Доставка по Львову»

    5. Six plain TableHead columns (no SortableHeader, no links in headers):
       Номер | Дата | Покупець | Доставка | Сума | Статус

    6. Cell markup:
       - Номер: font-medium orderNumber
       - Дата: text-muted-foreground formatDate(createdAt)
       - Покупець: customerName + muted customerPhone
       - Доставка: deliveryLabel(deliveryType)
       - Сума: tabular-nums formatPriceKopiyky
       - Статус: OrderListStatusSelect with orderId, status, deliveryType

    7. Wrapper: `<div className="min-w-0 overflow-x-auto rounded-lg border border-border bg-background">`

    8. Keep row navigation via getAdminClickableRowProps + adminClickableRowClassName unchanged.

    9. Do NOT add AdminListPagination, OrderListFilters, TanStack table, or sort indicators.

    10. admin/page.tsx should need no changes (stats.recentOrders already AdminOrderSummaryDto[]).
  </action>
  <verify>
    <automated>npx vitest run src/server/services/admin-order.service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 6 columns rendered with correct Ukrainian headers
    - OrderListStatusSelect in status column (not OrderStatusBadge)
    - Props accept AdminOrderSummaryDto[]
    - No pagination or filter UI in component
    - Row click still navigates to /admin/zamovlennia/[orderNumber]
  </acceptance_criteria>
  <done>Dashboard recent orders table matches orders list column structure and interactions</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Recent orders table on /admin shows up to 10 rows with full column parity to /admin/zamovlennia.</what-built>
  <how-to-verify>
    1. npm run dev — open /admin
    2. Scroll to «Останні замовлення»
    3. Confirm 6 columns and status selects with colored accent (if non-terminal status)
    4. Click a row — lands on /admin/zamovlennia/[orderNumber]
    5. Open /admin/zamovlennia — compare column order and cell content (no filter tabs on dashboard section)
    6. Confirm at most 10 rows on dashboard; no pagination controls under table
  </how-to-verify>
  <resume-signal>Type "approved" or describe table differences</resume-signal>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Disposition | Mitigation |
|-----------|----------|-------------|------------|
| T-38-04 | Elevation of Privilege | mitigate | requireAdmin() in layout |
| T-38-05 | Information Disclosure | accept | Same PII as orders list page |
| T-38-06 | Tampering via status select | accept | Intentional parity — same server action as orders page |
| T-38-SC | Tampering (deps) | accept | No new packages |
</threat_model>

<verification>
1. npx vitest run src/server/services/admin-order.service.test.ts
2. Manual: /admin table vs /admin/zamovlennia structure
</verification>

<success_criteria>
- ADM-DASH-08: Full orders table structure, max 10 rows, no filters/pagination, row-click navigation
</success_criteria>

<output>
Create `.planning/phases/38-dashboard-data-completeness/38-02-SUMMARY.md` when done
</output>
