---
phase: 19-database-purge-empty-states
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - .planning/phases/19-database-purge-empty-states/19-MANUAL-CHECKLIST.md
autonomous: true
requirements:
  - DATA-02
must_haves:
  truths:
    - "After purge on dev DB, / and /katalog render without 500; homepage shows Hero without CategoryGrid when no categories with products"
    - "Cart and wishlist show existing empty components; admin dashboard shows zeros and «Замовлень ще немає»"
    - "Admin list routes show existing Ukrainian empty copy at 0 rows; /admin/tovary/novyi shows category hint not 500"
    - "/katalog/[slug] returns 404 for unknown slug, not 500"
    - "19-MANUAL-CHECKLIST.md signed with all D-19-18 routes checked"
  artifacts:
    - path: .planning/phases/19-database-purge-empty-states/19-MANUAL-CHECKLIST.md
      provides: operator smoke checklist for purged DB
    - path: src/app/(storefront)/katalog/page.tsx
      provides: fix-only if smoke finds 500
      optional: true
    - path: src/app/(admin)/admin/page.tsx
      provides: fix-only if smoke finds 500
      optional: true
  key_links:
    - from: 19-MANUAL-CHECKLIST.md
      to: npm run db:purge
      via: prerequisite step before route smoke
      pattern: "db:purge"
    - from: storefront/admin pages
      to: prisma empty tables
      via: RSC queries returning [] or count 0
      pattern: "length === 0|total === 0|return null"
---

<objective>
Verify storefront and admin survive a fully purged business database (DATA-02) via manual smoke; fix only real 500/blank-screen regressions — no empty-state redesign (D-19-11…21).

Purpose: ROADMAP success criteria §3–4 — no unhandled errors at zero rows; dashboard zeros and existing copy sufficient.
Output: `19-MANUAL-CHECKLIST.md`, signed smoke, optional minimal hotfixes only if checklist fails.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-database-purge-empty-states/19-CONTEXT.md
@.planning/phases/19-database-purge-empty-states/19-RESEARCH.md
@.planning/phases/19-database-purge-empty-states/19-PATTERNS.md
@.planning/phases/19-database-purge-empty-states/19-VALIDATION.md
@.planning/phases/19-database-purge-empty-states/19-01-SUMMARY.md

@.planning/phases/18-product-delete-from-list/18-MANUAL-CHECKLIST.md
@.planning/phases/17-admin-chat-inbox-layout/17-MANUAL-CHECKLIST.md

@src/components/home/category-grid.tsx
@src/app/(storefront)/katalog/page.tsx
@src/app/(storefront)/katalog/[slug]/page.tsx
@src/components/catalog/catalog-filters.tsx
@src/components/cart/cart-empty.tsx
@src/components/wishlist/wishlist-page-content.tsx
@src/components/layout/store-header.tsx
@src/app/(admin)/admin/page.tsx
@src/app/(admin)/admin/tovary/page.tsx
@src/app/(admin)/admin/kategorii/page.tsx
@src/app/(admin)/admin/zamovlennia/page.tsx
@src/app/(admin)/admin/tovary/novyi/page.tsx
@src/components/chat/admin-chat-inbox.tsx
@src/server/services/admin-order.service.ts
</context>

<interfaces>
Expected empty behaviors (audit baseline — do not change copy unless smoke fails):

| Route | Expected |
|-------|----------|
| `/` | CategoryGrid returns null; Hero + HowToBuy render (D-19-11) |
| `/katalog` | ProductGrid dashed empty; price filter «Немає товарів для фільтра ціни» when bounds null (D-19-12) |
| `/katalog/[slug]` | notFound() for missing category — 404 OK (D-19-18) |
| `/koszyk` | CartEmpty (D-19-13) |
| `/obrane` | WishlistEmptyState (D-19-13, optional in checklist) |
| `/admin` | StatCards 0; «Замовлень ще немає» (D-19-19) |
| `/admin/tovary` | «Товарів не знайдено…» |
| `/admin/kategorii` | «Немає категорій» |
| `/admin/zamovlennia` | «Замовлень ще немає…» when filter=all and total=0 |
| `/admin/chaty` | emptyTitle / emptyBody in inbox |
| `/admin/tovary/novyi` | Hint «Спочатку створіть хоча б одну категорію…» (D-19-21) |

Fix-only scope (D-19-20): patch throwing code paths only; keep existing UA strings; no new marketing homepage block; no Cloudinary work.
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → RSC pages | Read-only smoke after purge; no new destructive surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02-01 | Denial | Empty DB 500 on catalog/admin | mitigate | Fix-only patches if smoke finds throw; prefer null-safe queries |
| T-19-02-02 | Information disclosure | Error pages leak stack | mitigate | Fix root throw; use notFound() or empty branches, not raw error pages |
| T-19-02-03 | Tampering | Adding purge HTTP route during fixes | mitigate | Do NOT add web purge endpoint while fixing empty states |
| T-19-SC | Tampering | npm installs | accept | Fix-only plan expects zero or minimal deps; no new packages |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: 19-MANUAL-CHECKLIST.md</name>
  <read_first>
    - .planning/phases/18-product-delete-from-list/18-MANUAL-CHECKLIST.md — structure
    - .planning/phases/17-admin-chat-inbox-layout/17-MANUAL-CHECKLIST.md — sign-off table
    - .planning/phases/19-database-purge-empty-states/19-CONTEXT.md — D-19-18 route list
    - .planning/phases/19-database-purge-empty-states/19-PATTERNS.md — checklist template
    - .planning/phases/19-database-purge-empty-states/19-VALIDATION.md — manual-only rows
  </read_first>
  <files>.planning/phases/19-database-purge-empty-states/19-MANUAL-CHECKLIST.md</files>
  <action>
    Create `19-MANUAL-CHECKLIST.md` in phase directory.

    Header: Phase 19 — Database Purge & Empty States; Requirements DATA-01, DATA-02; Run after Plan 19-01.

    Prerequisites section:
    - `npm run dev` running
    - Purge executed: `CONFIRM_DB_PURGE=yes npm run db:purge` on dev DB (not production unless intentional)
    - Admin signs in with existing credentials (auth tables untouched per D-19-02)
    - Note: optional `npx prisma db seed` only if operator wants categories again — document whether seed was run
    - Note: clear guest cart localStorage optional if stale IDs confuse testing

    Sections with checkboxes per D-19-18:
    1. **Purge verification (DATA-01):** User count unchanged; Category/Product/Order counts 0; re-run purge idempotent (exit 0)
    2. **Storefront:** `/`, `/katalog`, `/katalog/nonexistent-slug` (expect 404), `/koszyk`, `/obrane` (optional)
    3. **Header:** nav loads; no console error with empty categories (D-19-14)
    4. **Admin:** `/admin`, `/admin/tovary`, `/admin/kategorii`, `/admin/zamovlennia` (filter=all), `/admin/chaty`, `/admin/tovary/novyi`
    5. Each item: «no 500», «browser devtools console clean», expected copy from interfaces table

    Sign-off table: Date | Operator | Result | Failing items — mirror Phase 17/18.

    Include note: run `npx prisma db seed` before `npm run test:e2e` if e2e needed after purge.
  </action>
  <verify>
    <automated>test -f .planning/phases/19-database-purge-empty-states/19-MANUAL-CHECKLIST.md && grep -q 'DATA-01' .planning/phases/19-database-purge-empty-states/19-MANUAL-CHECKLIST.md && grep -q 'DATA-02' .planning/phases/19-database-purge-empty-states/19-MANUAL-CHECKLIST.md && grep -q '/admin/chaty' .planning/phases/19-database-purge-empty-states/19-MANUAL-CHECKLIST.md && grep -q 'db:purge' .planning/phases/19-database-purge-empty-states/19-MANUAL-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - 19-MANUAL-CHECKLIST.md exists with prerequisites, all D-19-18 routes, purge verification steps, and sign-off table
    - Checklist references CONFIRM_DB_PURGE and db:purge
    - Copy expectations reference existing UA strings, not new marketing blocks
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Purge dev DB and smoke empty routes</name>
  <read_first>
    - .planning/phases/19-database-purge-empty-states/19-MANUAL-CHECKLIST.md — created in Task 1
    - .planning/phases/19-database-purge-empty-states/19-RESEARCH.md — Empty-State Audit (fix-only guidance)
    - Touchpoint files listed in plan context @interfaces table
  </read_first>
  <files>.planning/phases/19-database-purge-empty-states/19-MANUAL-CHECKLIST.md</files>
  <action>
    Execute manual smoke per checklist on **dev database** (executor with running dev server):

    1. Run `CONFIRM_DB_PURGE=yes npm run db:purge` (and `npm run dev` if not running).
    2. Walk every checklist route; record pass/fail in checklist file.
    3. **If all pass:** Fill sign-off table with Pass; no application code changes (expected per research A1).
    4. **If 500 or blank screen without text:** Apply **minimal fix-only** patch to failing route/service (D-19-20, D-19-21):
       - Prefer existing empty branches (length === 0, notFound(), return null)
       - Do NOT redesign CartEmpty, ProductGrid empty, or dashboard widgets
       - Do NOT add Cloudinary purge, web purge API, or auto-seed
       - Re-run failed routes until pass
    5. Update checklist sign-off with date and result.

    Optional automated sanity (non-blocking if manual signed): `npx vitest run src/server/services/admin-order.service.test.ts -x` confirms stats helper handles zero counts.

    Document in 19-02-SUMMARY.md: purge row counts, routes tested, any fix files touched (or «no code changes»).
  </action>
  <verify>
    <automated>grep -q 'Pass\|pass\|approved' .planning/phases/19-database-purge-empty-states/19-MANUAL-CHECKLIST.md && npx vitest run src/server/services/admin-order.service.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - 19-MANUAL-CHECKLIST.md sign-off shows Pass (or lists fixed items then Pass)
    - All D-19-18 routes verified without unhandled 500 after purge
    - Any code changes are fix-only for failures; no scope creep (no Cloudinary, no purge endpoint, no empty-state redesign)
    - admin-order.service.test.ts passes if run
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Checklist signed Pass
- npm test for admin-order.service if touched files include services
- No regression: lint on any fix-only files via npm run lint
</verification>

<success_criteria>
- DATA-02 satisfied: storefront and admin work at zero business rows with existing empty UX
- Operator checklist signed; phase ready for `/gsd-verify-work`
</success_criteria>

<output>
Create `.planning/phases/19-database-purge-empty-states/19-02-SUMMARY.md` when done
</output>
