---
phase: 19-database-purge-empty-states
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/purge-business-data.ts
  - prisma/purge-business-data.test.ts
  - package.json
  - README.md
autonomous: true
requirements:
  - DATA-01
must_haves:
  truths:
    - "Operator runs npm run db:purge with explicit confirm and deletes all business rows in FK-safe order inside one transaction"
    - "User, Session, Account, Verification rows remain after purge; admin can still sign in"
    - "Re-running purge on empty business tables exits 0 with zero counts per table"
    - "Production purge blocked unless ALLOW_PRODUCTION_PURGE=yes; stdout reports per-table counts and Cloudinary warning"
    - "README documents backup → purge → optional seed workflow without auto-seed after purge"
  artifacts:
    - path: prisma/purge-business-data.ts
      provides: CLI purge with guards, D-19-01 delete order, stdout report
      exports: ["assertPurgeAllowed", "purgeBusinessData"]
    - path: package.json
      provides: db:purge script separate from prisma seed
      contains: "db:purge"
    - path: README.md
      provides: operator subsection for dev/staging purge
      contains: "db:purge"
    - path: prisma/purge-business-data.test.ts
      provides: unit tests for guards and tx delete order
  key_links:
    - from: package.json
      to: prisma/purge-business-data.ts
      via: "db:purge": "tsx prisma/purge-business-data.ts"
      pattern: "db:purge"
    - from: prisma/purge-business-data.ts
      to: src/lib/db
      via: import { prisma }
      pattern: "from \"../src/lib/db\""
    - from: prisma/purge-business-data.ts
      to: prisma models
      via: tx.message.deleteMany through tx.category.deleteMany
      pattern: "\\$transaction"
---

<objective>
Ship guarded CLI tooling so operators can wipe all business data in PostgreSQL while preserving Better Auth tables — DATA-01 (D-19-01 through D-19-10, D-19-15, D-19-16).

Purpose: One documented, safe command for a clean catalog before real product entry; no web endpoint, no Cloudinary purge, no auto-seed.
Output: `prisma/purge-business-data.ts`, `npm run db:purge`, README operator section, optional Vitest guards.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-database-purge-empty-states/19-CONTEXT.md
@.planning/phases/19-database-purge-empty-states/19-RESEARCH.md
@.planning/phases/19-database-purge-empty-states/19-PATTERNS.md
@.planning/phases/19-database-purge-empty-states/19-VALIDATION.md
@prisma/schema.prisma
@prisma/seed.ts
@prisma/seed-products.ts
@prisma/backfill-category-images.ts
@src/lib/db.ts
@package.json
@README.md
</context>

<interfaces>
From prisma/seed.ts (script lifecycle pattern):

- Line 1: `import "dotenv/config"`
- Client: `import { prisma } from "../src/lib/db"`
- Entry: `main().catch(console.error + process.exit(1)).finally(() => prisma.$disconnect())`

From src/lib/db.ts:

- Throws if `DATABASE_URL` missing before any query

D-19-01 delete order (interactive `$transaction` — use `tx`, not root `prisma` inside callback):

1. message
2. conversation
3. orderItem
4. order
5. cartItem
6. cart
7. wishlistItem
8. productImage
9. product
10. category

Auth tables NEVER touched: User, Session, Account, Verification.

Export for tests (per 19-PATTERNS.md):

- `export function assertPurgeAllowed(env?: NodeJS.ProcessEnv, argv?: string[]): void` — reads `process.env` / `process.argv` by default; calls `process.exit(1)` on failure (test via spawn or extract pure helpers `isPurgeConfirmed` + `isProductionPurgeAllowed` if preferred)
- `export async function purgeBusinessData(): Promise<Record<string, number>>` — runs transaction, returns label→count map

Stdout contract (D-19-09):

- Log each step: `Message: N`, … `Category: N`
- Log `Total deleted: <sum>`
- Final line: `Cloudinary assets were NOT deleted. Orphaned media may remain.`
- Exit 0 on success; exit 1 on guard failure or transaction error
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator shell → PostgreSQL | Destructive delete; only via local CLI with env/argv gates |
| App HTTP surface | Must NOT expose purge; no new API routes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | Remote wipe via HTTP | mitigate | CLI-only script; no Next.js route or Server Action for purge |
| T-19-02 | Denial | Accidental production data loss | mitigate | `NODE_ENV === "production"` blocks unless `ALLOW_PRODUCTION_PURGE=yes`; plus `CONFIRM_DB_PURGE=yes` or `--confirm` (D-19-07, D-19-08) |
| T-19-03 | Information disclosure | DATABASE_URL in logs | mitigate | Log only redacted host hint if needed; never print full connection string |
| T-19-04 | Spoofing | Unauthenticated script run | accept | Operator has shell + DB credentials; out of app auth scope |
| T-19-SC | Tampering | npm installs | accept | No new packages; existing `tsx` + Prisma only |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: purge-business-data.ts with guards and transaction</name>
  <read_first>
    - prisma/seed.ts — dotenv, prisma import, main/catch/finally
    - prisma/seed-products.ts — cartItem before product delete pattern
    - prisma/backfill-category-images.ts — file header comment style
    - src/server/services/order.service.ts — interactive $transaction with tx
    - .planning/phases/19-database-purge-empty-states/19-RESEARCH.md — FK order table, guard table
    - .planning/phases/19-database-purge-empty-states/19-PATTERNS.md — purge script section
    - prisma/schema.prisma — business vs auth models
  </read_first>
  <files>prisma/purge-business-data.ts</files>
  <action>
    Create `prisma/purge-business-data.ts` per D-19-01…05, D-19-07…09.

    File header block comment (D-19-10): document FK order, `npm run db:purge`, confirm flags, production gate, Cloudinary not purged, optional `npx prisma db seed` after (not automatic).

    Implement `assertPurgeAllowed()` at top of `main()`:
    - Exit 1 unless `process.argv.includes("--confirm")` OR `process.env.CONFIRM_DB_PURGE === "yes"` — stderr explains both options (D-19-07)
    - Exit 1 if `process.env.NODE_ENV === "production"` AND `process.env.ALLOW_PRODUCTION_PURGE !== "yes"` — stderr explains ALLOW_PRODUCTION_PURGE (D-19-08)
    - Optional: log `NODE_ENV` and redacted DATABASE_URL host only

    Implement `purgeBusinessData()`:
    - Single `prisma.$transaction(async (tx) => { ... })` (D-19-04)
    - Sequential `tx.<model>.deleteMany({})` in exact D-19-01 order; collect `{ count }` per label
    - Return `Record<string, number>` for reporting
    - Idempotent: empty tables return count 0 without error (D-19-05)

    `main()`: assertPurgeAllowed → purgeBusinessData → print per-table counts + total + Cloudinary warning → exit 0.

    Do NOT: TRUNCATE CASCADE, delete User/Session/Account/Verification, call seed or seedProducts, add HTTP endpoint, purge Cloudinary (deferred per CONTEXT).

    Export `assertPurgeAllowed` and `purgeBusinessData` for Vitest (Task 3).
  </action>
  <verify>
    <automated>test -f prisma/purge-business-data.ts && grep -v '^[[:space:]]*//' prisma/purge-business-data.ts | grep -q 'assertPurgeAllowed' && grep -v '^[[:space:]]*//' prisma/purge-business-data.ts | grep -q '\$transaction' && grep -v '^[[:space:]]*//' prisma/purge-business-data.ts | grep -q 'tx\.message\.deleteMany' && grep -v '^[[:space:]]*//' prisma/purge-business-data.ts | grep -q 'tx\.category\.deleteMany' && ! grep -v '^[[:space:]]*//' prisma/purge-business-data.ts | grep -qE 'user\.deleteMany|session\.deleteMany'</automated>
  </verify>
  <acceptance_criteria>
    - prisma/purge-business-data.ts exists with dotenv/config and prisma from ../src/lib/db
    - assertPurgeAllowed enforces confirm and production gates per D-19-07 and D-19-08
    - All ten business models deleted in D-19-01 order inside one transaction
    - No auth model deleteMany calls
    - Stdout includes per-table counts, total, and Cloudinary not-deleted warning
    - Exported assertPurgeAllowed and purgeBusinessData for tests
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: db:purge script and README operator docs</name>
  <read_first>
    - package.json — scripts and prisma.seed block (keep seed separate)
    - README.md — place subsection after Continuous Integration
    - .planning/phases/19-database-purge-empty-states/19-CONTEXT.md — D-19-06, D-19-15, D-19-16, D-19-17
    - .planning/phases/19-database-purge-empty-states/19-PATTERNS.md — package.json and README sections
  </read_first>
  <files>package.json, README.md</files>
  <action>
    package.json (D-19-06): Add script `"db:purge": "tsx prisma/purge-business-data.ts"` under `"scripts"`. Do NOT add purge to `"prisma"."seed"` or chain seed after purge.

    README.md (D-19-10, D-19-16): Add section `## Оператор: очистка БД (dev/staging)` after `## Continuous Integration` with numbered steps:
    1. Backup (pg_dump / Neon branch) — operator responsibility, not automated
    2. `CONFIRM_DB_PURGE=yes npm run db:purge` (dev/staging)
    3. Production only: also set `ALLOW_PRODUCTION_PURGE=yes` when intentional
    4. Optional separate step: `npx prisma db seed` — warn that seed runs seedProducts (80+ products + Cloudinary uploads), not a wipe
    5. Refill via `/admin/tovary`; admin login unchanged if User rows exist (D-19-17)
    6. Note: `npm test` / prisma/seed.test.ts expect seeded data — re-seed before tests after purge

    Do NOT document Cloudinary purge. Do NOT imply purge auto-runs seed (D-19-15).
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.scripts['db:purge']?.includes('purge-business-data')) process.exit(1)" && grep -q 'db:purge' README.md && grep -q 'CONFIRM_DB_PURGE' README.md && grep -q 'ALLOW_PRODUCTION_PURGE' README.md</automated>
  </verify>
  <acceptance_criteria>
    - package.json defines db:purge pointing at tsx prisma/purge-business-data.ts
    - prisma.seed unchanged and separate from db:purge
    - README has Ukrainian operator subsection with backup, purge, optional seed, and seedProducts warning
    - README states purge does not auto-run seed
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Vitest guards and transaction order</name>
  <read_first>
    - .planning/phases/19-database-purge-empty-states/19-VALIDATION.md — tasks 19-01-01…03
    - src/server/services/admin-order.service.test.ts — vi.mock @/lib/db pattern
    - src/server/services/chat.service.test.ts — $transaction mock with tx
    - vitest.config.ts — prisma/**/*.test.ts include
  </read_first>
  <files>prisma/purge-business-data.test.ts</files>
  <behavior>
    - Without confirm: assertPurgeAllowed (or isPurgeConfirmed) returns false / exits when CONFIRM unset and no --confirm
    - With CONFIRM_DB_PURGE=yes: confirm passes
    - Production: NODE_ENV=production without ALLOW_PRODUCTION_PURGE fails guard
    - purgeBusinessData mock: $transaction callback receives tx; deleteMany called in order Message → … → Category
  </behavior>
  <action>
    Create `prisma/purge-business-data.test.ts`.

    Prefer extracting pure functions `isPurgeConfirmed(env, argv)` and `isProductionPurgeAllowed(env)` from purge script for unit tests without mocking process.exit; keep assertPurgeAllowed as thin wrapper that exits 1.

    Mock `../src/lib/db` prisma: `$transaction` invokes callback with tx object where each model has `deleteMany: vi.fn().mockResolvedValue({ count: 0 })`.

    Tests per 19-VALIDATION.md (T-19-01, T-19-02, delete order):
    - confirm guard rejects/accepts
    - production guard rejects without ALLOW_PRODUCTION_PURGE
    - purgeBusinessData calls deleteMany on tx in D-19-01 order (spy call order)

    Do NOT hit real DATABASE_URL in these tests (no integration wipe in CI).
  </action>
  <verify>
    <automated>npx vitest run prisma/purge-business-data.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - prisma/purge-business-data.test.ts exists and passes via npx vitest run prisma/purge-business-data.test.ts -x
    - Tests cover confirm gate, production gate, and deleteMany call order without real DB
    - No new npm dependencies added
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Automated grep and vitest per tasks
- Operator manual (post-plan 19-02): on dev DB clone run `CONFIRM_DB_PURGE=yes npm run db:purge`; verify `prisma user.count()` unchanged; business tables at 0
</verification>

<success_criteria>
- DATA-01 satisfied: documented `npm run db:purge` with guards, FK-safe transactional wipe, auth preserved, idempotent re-run
- All 19-01 automated verifies green
</success_criteria>

<output>
Create `.planning/phases/19-database-purge-empty-states/19-01-SUMMARY.md` when done
</output>
