---
phase: 34-admin-analytics
plan: 05
type: execute
wave: 3
depends_on:
  - "34-03"
  - "34-04"
files_modified:
  - src/app/(admin)/admin/analityka/page.tsx
  - src/app/(admin)/admin/page.tsx
autonomous: false
requirements:
  - AN-01
  - AN-02

must_haves:
  truths:
    - "/admin/analityka renders KPI cards (замовлень, виручка, дзвінків) + PeriodSelector + 2 line charts"
    - "Period selector default is 30 days (no ?days param → days=30)"
    - "Period selector allowlist validates: ?days=7→7, ?days=90→90, anything else→30"
    - "Revenue KPI shows formatted string '1 200 грн' (per D-02)"
    - "/admin dashboard shows AnalyticsDashboardPreview section before «Останні замовлення» section (per D-07)"
    - "Dashboard preview contains 2 mini-charts + «Детальна аналітика →» link"
    - "Both pages load real Prisma data — no mock values"
  artifacts:
    - path: "src/app/(admin)/admin/analityka/page.tsx"
      provides: "Analytics page RSC — reads searchParams, fetches data, renders KPI + PeriodSelector + AnalyticsCharts"
      exports: ["default (AnalitykaPage)", "metadata"]
    - path: "src/app/(admin)/admin/page.tsx"
      provides: "Dashboard page RSC — updated to fetch getDashboardAnalyticsPreview in parallel and render preview section"
  key_links:
    - from: "src/app/(admin)/admin/analityka/page.tsx"
      to: "src/server/services/admin-analytics.service.ts"
      via: "getAnalyticsData import"
      pattern: "getAnalyticsData"
    - from: "src/app/(admin)/admin/analityka/page.tsx"
      to: "src/components/admin/analytics-charts.tsx"
      via: "AnalyticsCharts import"
      pattern: "AnalyticsCharts"
    - from: "src/app/(admin)/admin/page.tsx"
      to: "src/components/admin/analytics-dashboard-preview.tsx"
      via: "AnalyticsDashboardPreview import"
      pattern: "AnalyticsDashboardPreview"
    - from: "src/app/(admin)/admin/page.tsx"
      to: "src/server/services/admin-analytics.service.ts"
      via: "getDashboardAnalyticsPreview import"
      pattern: "getDashboardAnalyticsPreview"
---

<objective>
Wire the analytics page and dashboard preview using all components and service functions built in prior plans. This is the final assembly plan — it creates the route at /admin/analityka and inserts the preview section into /admin.

Purpose: AN-01 (analytics page) and AN-02 (dashboard preview) are fully delivered here. After this plan the operator can navigate to /admin/analityka to view KPIs and charts, and see a preview on /admin.
Output: New page src/app/(admin)/admin/analityka/page.tsx + updated src/app/(admin)/admin/page.tsx.
</objective>

<execution_context>
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/PROJECT.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/phases/34-admin-analytics/34-CONTEXT.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/phases/34-admin-analytics/34-RESEARCH.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/phases/34-admin-analytics/34-PATTERNS.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/phases/34-admin-analytics/34-03-SUMMARY.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/phases/34-admin-analytics/34-04-SUMMARY.md
</context>

<interfaces>
<!-- From src/app/(admin)/admin/zamovlennia/page.tsx — async searchParams RSC pattern -->

```typescript
type PageProps = {
  searchParams: Promise<{ [key: string]: string | string[] | undefined }>;
};
export default async function SomePage({ searchParams }: PageProps) {
  const rawParams = await searchParams;
  // parse params here
}
```

<!-- From src/app/(admin)/admin/page.tsx (current file, lines 1–60) -->

Current imports:
```typescript
import Link from "next/link";
import { Eye, Package, PackageX, Plus, ShoppingBag } from "lucide-react";
import { StatCard } from "@/components/admin/stat-card";
import { AdminRecentOrdersTable } from "@/components/admin/admin-recent-orders-table";
import { Button } from "@/components/ui/button";
import { getAdminDashboardStats } from "@/server/services/admin-order.service";
```

Current fetch:
```typescript
const stats = await getAdminDashboardStats();
```

Insertion target: before line 50 `<section className="space-y-4"><h2>Останні замовлення</h2>...`

<!-- From src/components/admin/stat-card.tsx — StatCard accepts count: number only -->
StatCard props: { label: string; count: number; href?: string; icon?: React.ElementType }
Revenue must NOT use StatCard directly — use a custom inline card or AnalyticsKpiCard.

<!-- From src/server/services/admin-analytics.service.ts (Plan 02) -->
getAnalyticsData(days: 7|30|90): Promise<AnalyticsData>
getDashboardAnalyticsPreview(): Promise<AnalyticsData>
formatRevenue(grn: number): string
AnalyticsData = { kpi: { totalOrders, totalRevenue, totalCallbacks }, ordersByDay, revenueByDay }
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create /admin/analityka page (AN-01)</name>
  <files>src/app/(admin)/admin/analityka/page.tsx</files>
  <read_first>
    - src/app/(admin)/admin/zamovlennia/page.tsx (full file — async searchParams pattern, Metadata export, page root layout)
    - src/app/(admin)/admin/page.tsx (page root layout pattern: div className="space-y-8", h1 className)
    - src/app/(admin)/admin/nalashtuvannia/page.tsx (section heading pattern: section className="space-y-4")
    - src/components/admin/stat-card.tsx (full file — StatCard props: count is number, cannot display string)
    - src/server/services/admin-analytics.service.ts (getAnalyticsData, formatRevenue, AnalyticsData type)
    - src/components/admin/period-selector.tsx (PeriodSelector props: active: 7|30|90)
    - src/components/admin/analytics-charts.tsx (AnalyticsCharts props: ordersByDay, revenueByDay)
    - .planning/phases/34-admin-analytics/34-PATTERNS.md (analityka page.tsx section — full layout pattern)
    - .planning/phases/34-admin-analytics/34-CONTEXT.md (D-03: KPI cards; D-04: default 30 days; D-06: KPI→selector→charts layout)
    - node_modules/next/dist/docs/ (search for "searchParams" — confirm async Promise pattern for this Next.js version)
  </read_first>
  <action>
    Create directory src/app/(admin)/admin/analityka/ then create page.tsx:

    1. Metadata: export const metadata: Metadata = { title: "Аналітика" }. Import Metadata from "next".
    2. PageProps type: { searchParams: Promise<{ days?: string }> }.
    3. Days parsing (allowlist — per T-34-03 mitigation): const rawDays = (await searchParams).days; const days: 7|30|90 = rawDays === "7" ? 7 : rawDays === "90" ? 90 : 30.
    4. Fetch: const data = await getAnalyticsData(days).
    5. Page layout (div className="space-y-8"):
       - h1 className="text-2xl font-semibold md:text-3xl": «Аналітика»
       - KPI grid (div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3"):
         a. <StatCard label="Замовлень" count={data.kpi.totalOrders} /> — uses number directly
         b. Revenue KPI — inline styled div matching StatCard visual (do NOT use StatCard.count for string value):
            <div className="rounded-lg border border-border bg-card p-4 shadow-sm">
              <p className="text-sm font-medium text-muted-foreground">Виручка</p>
              <p className="mt-2 text-3xl font-semibold tabular-nums">{formatRevenue(data.kpi.totalRevenue)}</p>
            </div>
         c. <StatCard label="Дзвінків" count={data.kpi.totalCallbacks} />
       - <PeriodSelector active={days} /> (below KPI grid, before charts)
       - Charts section (section className="space-y-4"):
         <h2 className="text-lg font-semibold">Графіки</h2>
         <AnalyticsCharts ordersByDay={data.ordersByDay} revenueByDay={data.revenueByDay} />
    6. Imports: Metadata from "next"; StatCard from "@/components/admin/stat-card"; PeriodSelector from "@/components/admin/period-selector"; AnalyticsCharts from "@/components/admin/analytics-charts"; getAnalyticsData, formatRevenue from "@/server/services/admin-analytics.service".
    7. No "use client" — this is an async RSC page.
  </action>
  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npx tsc --noEmit 2>&1 | grep -i "analityka" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(admin)/admin/analityka/page.tsx exists
    - File does NOT contain "use client"
    - export const metadata with title "Аналітика" is present
    - searchParams is awaited (const rawDays = (await searchParams).days)
    - Days allowlist validation: rawDays === "7" ? 7 : rawDays === "90" ? 90 : 30
    - getAnalyticsData(days) is called with the validated number
    - StatCard used for totalOrders and totalCallbacks (count: number)
    - Revenue displayed via inline div with formatRevenue(data.kpi.totalRevenue) — NOT via StatCard
    - PeriodSelector active={days} rendered
    - AnalyticsCharts ordersByDay={data.ordersByDay} revenueByDay={data.revenueByDay} rendered
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Analytics page created. AN-01 is functionally complete.</done>
</task>

<task type="auto">
  <name>Task 2: Insert AnalyticsDashboardPreview into /admin dashboard (AN-02)</name>
  <files>src/app/(admin)/admin/page.tsx</files>
  <read_first>
    - src/app/(admin)/admin/page.tsx (full file — all current imports, function body, exact line before «Останні замовлення» section at line 50)
    - src/components/admin/analytics-dashboard-preview.tsx (AnalyticsDashboardPreview props: ordersByDay, revenueByDay, totalOrders, totalRevenue)
    - src/server/services/admin-analytics.service.ts (getDashboardAnalyticsPreview signature and return type)
    - .planning/phases/34-admin-analytics/34-PATTERNS.md (admin/page.tsx section — insertion point and parallel fetch pattern)
    - .planning/phases/34-admin-analytics/34-CONTEXT.md (D-07: preview before «Останні замовлення»; D-08: mini-charts h-[120px])
  </read_first>
  <action>
    Edit src/app/(admin)/admin/page.tsx:

    1. Add two new imports at the top (after existing imports):
       - import { AnalyticsDashboardPreview } from "@/components/admin/analytics-dashboard-preview";
       - import { getDashboardAnalyticsPreview } from "@/server/services/admin-analytics.service";
    2. Change the fetch from sequential to parallel:
       Replace: const stats = await getAdminDashboardStats();
       With: const [stats, analyticsPreview] = await Promise.all([getAdminDashboardStats(), getDashboardAnalyticsPreview()]);
    3. Insert <AnalyticsDashboardPreview> section immediately before the existing <section className="space-y-4"> that contains «Останні замовлення» (currently line 50):
       <AnalyticsDashboardPreview
         ordersByDay={analyticsPreview.ordersByDay}
         revenueByDay={analyticsPreview.revenueByDay}
         totalOrders={analyticsPreview.kpi.totalOrders}
         totalRevenue={analyticsPreview.kpi.totalRevenue}
       />
    4. Do NOT modify any existing JSX below the insertion point (StatCards, buttons, «Останні замовлення» section must remain unchanged).
    5. No "use client" — this remains an async RSC page.
  </action>
  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npx tsc --noEmit 2>&1 | grep -i "admin/page" | head -10 && npm test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(admin)/admin/page.tsx imports AnalyticsDashboardPreview from "@/components/admin/analytics-dashboard-preview"
    - src/app/(admin)/admin/page.tsx imports getDashboardAnalyticsPreview from "@/server/services/admin-analytics.service"
    - Promise.all([getAdminDashboardStats(), getDashboardAnalyticsPreview()]) is used (parallel fetch)
    - <AnalyticsDashboardPreview> element appears in JSX before the «Останні замовлення» section
    - All four props passed: ordersByDay, revenueByDay, totalOrders, totalRevenue
    - Existing StatCards, Button row, and «Останні замовлення» section are preserved (no deletions)
    - npx tsc --noEmit exits 0
    - npm test passes (all existing + new tests green)
  </acceptance_criteria>
  <done>Dashboard preview wired. AN-02 complete. Operator sees preview on /admin before «Останні замовлення».</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — analytics page and dashboard preview</name>
  <what-built>
    - /admin/analityka: KPI cards (замовлень, виручка, дзвінків) + period selector (7/30/90) + 2 line charts
    - /admin: analytics preview section with 2 mini-charts + KPI row + «Детальна аналітика →» link, before «Останні замовлення»
  </what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev`
    2. Log in as admin and navigate to http://localhost:3000/admin
       - Verify: An "Аналітика (30 днів)" section appears above "Останні замовлення"
       - Verify: The section contains 2 mini line charts (compact, ~120px tall, no axes)
       - Verify: A KPI row shows «Замовлень» count and «Виручка» with format "X XXX грн"
       - Verify: A "Детальна аналітика →" link is visible and points to /admin/analityka
    3. Navigate to http://localhost:3000/admin/analityka
       - Verify: Page loads with H1 «Аналітика»
       - Verify: 3 KPI cards render (Замовлень as number, Виручка as "X XXX грн", Дзвінків as number)
       - Verify: Period selector shows 3 tabs: "7 днів", "30 днів", "90 днів" — default (no param) is "30 днів" highlighted
       - Verify: Clicking "7 днів" updates URL to ?days=7 and re-renders with 7-day data
       - Verify: 2 line charts render below the selector (orders and revenue trends)
    4. Verify «Аналітика» link appears in the admin sidebar navigation
    5. Verify no console errors in browser dev tools
  </how-to-verify>
  <resume-signal>Type "approved" if all items verified, or describe specific issues found (e.g., "charts not rendering", "sidebar link missing").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser URL searchParams → RSC page | ?days= param parsed server-side; only allowed values (7, 30, 90) reach getAnalyticsData; all other values default to 30 |
| RSC → client chart component | Serialized DayPoint[] (plain numbers + strings) passed as props; no user-controlled data in chart props |
| Admin layout → analytics page | requireAdmin() in src/app/(admin)/admin/layout.tsx protects entire /admin/* subtree including /admin/analityka |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Information Disclosure | /admin/analityka route | mitigate | requireAdmin() in admin layout (src/app/(admin)/admin/layout.tsx) blocks unauthenticated access to analytics data |
| T-34-02 | Tampering | $queryRaw sinceDate injection | mitigate | sinceDate is a JS Date object interpolated as Prisma parameterized value — Prisma escapes it; not a string interpolation |
| T-34-03 | Tampering | searchParams days validation | mitigate | Explicit allowlist in page: rawDays === "7" ? 7 : rawDays === "90" ? 90 : 30 — only valid integers reach service |
| T-34-04 | Information Disclosure | Revenue data exposure | accept | Revenue covers all order statuses (D-01); data is visible only to authenticated admin — risk is low |
| T-34-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
npx tsc --noEmit exits 0.
npm test passes (all tests green including Plan 01 unit tests).
/admin/analityka accessible, renders KPI + charts + period selector.
/admin dashboard shows preview section before «Останні замовлення».
</verification>

<success_criteria>
AN-01: /admin/analityka renders KPI cards, period selector (7/30/90, default 30), and 2 line charts sourced from real Prisma data.
AN-02: /admin dashboard shows AnalyticsDashboardPreview (2 mini-charts, KPI row, analytics link) before «Останні замовлення».
Human verification approved.
Full test suite green.
</success_criteria>

<output>
Create .planning/phases/34-admin-analytics/34-05-SUMMARY.md when done.
</output>
