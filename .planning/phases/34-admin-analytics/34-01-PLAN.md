---
phase: 34-admin-analytics
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/server/services/admin-analytics.service.test.ts
autonomous: true
requirements:
  - AN-01

must_haves:
  truths:
    - "Test file exists and covers getAnalyticsData shape, BigInt conversion, zero-fill, and KPI totals"
    - "Test file covers getDashboardAnalyticsPreview fixed-30-day behavior"
    - "All tests run via vitest with the existing config — no new config needed"
  artifacts:
    - path: "src/server/services/admin-analytics.service.test.ts"
      provides: "Unit test stubs for analytics service"
      contains: "describe(\"getAnalyticsData\")"
  key_links:
    - from: "src/server/services/admin-analytics.service.test.ts"
      to: "src/server/services/admin-analytics.service.ts"
      via: "named import getAnalyticsData, getDashboardAnalyticsPreview"
      pattern: "from.*admin-analytics\\.service"
---

<objective>
Create the Wave 0 test scaffold for the analytics service. These failing tests define the contract that the service implementation (Plan 02) must satisfy.

Purpose: Nyquist compliance — automated tests must exist before implementation. Tests document the expected shape of getAnalyticsData(), getDashboardAnalyticsPreview(), BigInt conversion, zero-fill, and KPI totals.
Output: src/server/services/admin-analytics.service.test.ts (test file with failing stubs ready for Plan 02 implementation).
</objective>

<execution_context>
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/PROJECT.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/ROADMAP.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/phases/34-admin-analytics/34-CONTEXT.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/phases/34-admin-analytics/34-RESEARCH.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/phases/34-admin-analytics/34-PATTERNS.md
</context>

<interfaces>
<!-- Analog test file to replicate structure from -->
<!-- From src/server/services/admin-order.service.test.ts (lines 1–28) -->

Mock setup:
```typescript
vi.mock("@/lib/db", () => ({
  prisma: {
    order: { count: vi.fn() },
    callbackRequest: { count: vi.fn() },
    $queryRaw: vi.fn(),
  },
}));
```

Service functions to import (not yet implemented — tests will be "red"):
- getAnalyticsData(days: 7 | 30 | 90): Promise<AnalyticsData>
- getDashboardAnalyticsPreview(): Promise<...>

Expected return shape:
```typescript
type DayPoint = { day: string; value: number };
type AnalyticsData = {
  kpi: { totalOrders: number; totalRevenue: number; totalCallbacks: number };
  ordersByDay: DayPoint[];
  revenueByDay: DayPoint[];
};
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create admin-analytics.service.test.ts with contract stubs</name>
  <files>src/server/services/admin-analytics.service.test.ts</files>
  <read_first>
    - src/server/services/admin-order.service.test.ts (exact mock setup pattern and $queryRaw mock approach — read fully)
    - .planning/phases/34-admin-analytics/34-PATTERNS.md (test mock patterns section)
    - .planning/phases/34-admin-analytics/34-RESEARCH.md (Pattern 1: Prisma Time-Series, code examples)
  </read_first>
  <behavior>
    - describe("getAnalyticsData"): beforeEach(vi.clearAllMocks), 4 tests:
      1. "returns correct kpi totals from $queryRaw results" — mock $queryRaw twice (orderRows, revenueRows), mock callbackRequest.count; assert result.kpi.totalOrders, totalRevenue, totalCallbacks
      2. "BigInt count values are converted to number (not BigInt)" — mock $queryRaw with BigInt(5); assert typeof result.kpi.totalOrders === "number"
      3. "BigInt revenue values are converted to number (not BigInt)" — mock $queryRaw revenue with BigInt(12000); assert typeof result.kpi.totalRevenue === "number"
      4. "zero-fills missing days to produce full N-day array" — mock $queryRaw with 1 row (only one day has data); assert result.ordersByDay.length === 7 for days=7
    - describe("getDashboardAnalyticsPreview"): 1 test:
      1. "returns ordersByDay and revenueByDay with 30 entries" — mock $queryRaw with empty arrays; assert result.ordersByDay.length === 30 and result.revenueByDay.length === 30
  </behavior>
  <action>
    Create src/server/services/admin-analytics.service.test.ts. Structure:

    1. Imports: beforeEach, describe, expect, it, vi from "vitest"; prisma from "@/lib/db"; getAnalyticsData, getDashboardAnalyticsPreview from "./admin-analytics.service".
    2. vi.mock("@/lib/db") with prisma.order.count as vi.fn(), prisma.callbackRequest.count as vi.fn(), prisma.$queryRaw as vi.fn().
    3. describe("getAnalyticsData") block with beforeEach(() => vi.clearAllMocks()) and the 4 behavior tests.
    4. For $queryRaw mock: use mockResolvedValueOnce chaining. First call = order rows (Array<{day: Date, count: bigint}>), second call = revenue rows (Array<{day: Date, revenue: bigint}>).
    5. Use BigInt() literals (e.g., BigInt(3), BigInt(6000)) in mock data — this is what $queryRaw actually returns.
    6. describe("getDashboardAnalyticsPreview") block with 1 test mocking $queryRaw with empty arrays and asserting array lengths.

    The imported functions do not exist yet — tests will fail (RED phase). That is expected. Do not create the service file in this plan.
  </action>
  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npx vitest run src/server/services/admin-analytics.service.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File src/server/services/admin-analytics.service.test.ts exists
    - File imports from "./admin-analytics.service" (not yet implemented — expect module-not-found error, not test logic errors)
    - vi.mock("@/lib/db") is present covering prisma.order.count, prisma.callbackRequest.count, prisma.$queryRaw
    - 5 test cases exist: 4 under getAnalyticsData, 1 under getDashboardAnalyticsPreview
    - BigInt() used in mock data (not Number())
    - vitest run exits with error only due to missing service module, not test file syntax errors: `npx tsc --noEmit src/server/services/admin-analytics.service.test.ts` passes if the service stubs existed (file itself is syntactically valid)
  </acceptance_criteria>
  <done>Test file committed. Running vitest against it fails with "Cannot find module ./admin-analytics.service" — contract is defined, ready for Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test file only | No production code in this plan; no external input or trust boundaries apply |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — test file only |
</threat_model>

<verification>
File exists at src/server/services/admin-analytics.service.test.ts.
Vitest run produces a module-not-found error (expected — service not yet implemented).
File is syntactically valid TypeScript.
</verification>

<success_criteria>
Test scaffold committed. Plan 02 (analytics service) can now be executed; when the service is complete, all 5 tests will turn green.
</success_criteria>

<output>
Create .planning/phases/34-admin-analytics/34-01-SUMMARY.md when done.
</output>
