---
phase: 34-admin-analytics
plan: 04
type: execute
wave: 2
depends_on:
  - "34-02"
  - "34-03"
files_modified:
  - src/components/admin/analytics-charts.tsx
  - src/components/admin/analytics-dashboard-preview.tsx
autonomous: true
requirements:
  - AN-01
  - AN-02

must_haves:
  truths:
    - "AnalyticsCharts renders 2 full-height line charts: orders trend (h-[220px]) and revenue trend (h-[220px])"
    - "AnalyticsDashboardPreview renders 2 compact mini-charts (h-[120px]) with no axes + Детальна аналітика link (per D-08)"
    - "Both components have 'use client' directive — recharts is DOM-dependent"
    - "Revenue tooltip/KPI displays formatRevenue(grn) output: '1 200 грн' format (per D-02)"
    - "Mini-chart section heading: «Аналітика (30 днів)» with link «Детальна аналітика →» to /admin/analityka"
  artifacts:
    - path: "src/components/admin/analytics-charts.tsx"
      provides: "Full analytics page chart pair (orders + revenue line charts)"
      exports: ["AnalyticsCharts"]
    - path: "src/components/admin/analytics-dashboard-preview.tsx"
      provides: "Dashboard preview — mini-charts + KPI row + link to analytics page"
      exports: ["AnalyticsDashboardPreview"]
  key_links:
    - from: "src/components/admin/analytics-charts.tsx"
      to: "src/components/ui/chart.tsx"
      via: "ChartContainer, ChartTooltip, ChartTooltipContent"
      pattern: "ChartContainer"
    - from: "src/components/admin/analytics-dashboard-preview.tsx"
      to: "src/components/ui/chart.tsx"
      via: "ChartContainer"
      pattern: "ChartContainer"
    - from: "src/components/admin/analytics-dashboard-preview.tsx"
      to: "/admin/analityka"
      via: "Link href"
      pattern: "href=\"/admin/analityka\""
---

<objective>
Create the two "use client" chart wrapper components. AnalyticsCharts provides the full two-chart view for the analytics page. AnalyticsDashboardPreview provides the compact mini-chart section inserted on the dashboard.

Purpose: Recharts is DOM-dependent and cannot run in RSC — these components form the client boundary. They receive serialized DayPoint[] data from server components as props and never fetch independently.
Output: analytics-charts.tsx + analytics-dashboard-preview.tsx.
</objective>

<execution_context>
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/PROJECT.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/phases/34-admin-analytics/34-CONTEXT.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/phases/34-admin-analytics/34-RESEARCH.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/phases/34-admin-analytics/34-PATTERNS.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/phases/34-admin-analytics/34-02-SUMMARY.md
</context>

<interfaces>
<!-- From src/server/services/admin-analytics.service.ts (created in Plan 02) -->

Types to import:
```typescript
import type { DayPoint } from "@/server/services/admin-analytics.service";
import { formatRevenue } from "@/server/services/admin-analytics.service";
```

DayPoint = { day: string; value: number }

<!-- From src/components/ui/chart.tsx (installed in Plan 02 by shadcn CLI) -->
Key exports: ChartConfig, ChartContainer, ChartTooltip, ChartTooltipContent

<!-- From src/app/globals.css — CSS variables already defined -->
Chart color variables: var(--chart-1), var(--chart-2), var(--chart-3), var(--chart-4), var(--chart-5)

<!-- From recharts (installed in Plan 02) -->
Imports: Line, LineChart, CartesianGrid, XAxis, Area, AreaChart from "recharts"

<!-- From src/components/admin/orders-data-table.tsx — "use client" boundary pattern -->
Line 1: "use client";
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create AnalyticsCharts component</name>
  <files>src/components/admin/analytics-charts.tsx</files>
  <read_first>
    - src/components/ui/chart.tsx (full file — ChartContainer, ChartConfig, ChartTooltip, ChartTooltipContent API and accepted props)
    - src/server/services/admin-analytics.service.ts (DayPoint type, formatRevenue export)
    - src/components/admin/orders-data-table.tsx (lines 1–5 — "use client" boundary pattern)
    - .planning/phases/34-admin-analytics/34-RESEARCH.md (Pattern 2: shadcn ChartContainer + LineChart — exact code template)
    - .planning/phases/34-admin-analytics/34-PATTERNS.md (analytics-charts.tsx section)
    - src/app/globals.css (lines 27–40 — confirm --chart-1..5 variable names)
  </read_first>
  <action>
    Create src/components/admin/analytics-charts.tsx:

    1. First line: "use client";
    2. Imports: Line, LineChart, CartesianGrid, XAxis from "recharts"; ChartConfig, ChartContainer, ChartTooltip, ChartTooltipContent from "@/components/ui/chart"; DayPoint from "@/server/services/admin-analytics.service".
    3. Props type: AnalyticsChartsProps = { ordersByDay: DayPoint[]; revenueByDay: DayPoint[] }.
    4. Two ChartConfig constants:
       - ordersChartConfig: { value: { label: "Замовлень", color: "var(--chart-1)" } } satisfies ChartConfig
       - revenueChartConfig: { value: { label: "Виручка (грн)", color: "var(--chart-2)" } } satisfies ChartConfig
    5. Named export: export function AnalyticsCharts({ ordersByDay, revenueByDay }: AnalyticsChartsProps).
    6. Return: <div className="grid gap-6 md:grid-cols-2"> containing two sections, each with:
       - <h3 className="text-sm font-medium text-muted-foreground"> heading ("Замовлення" / "Виручка")
       - <ChartContainer config={...} className="h-[220px] w-full">
         - <LineChart data={...} accessibilityLayer>
           - <CartesianGrid vertical={false} />
           - <XAxis dataKey="day" tickLine={false} tickMargin={8} axisLine={false} tickFormatter={(v: string) => v.slice(5)} />
           - <ChartTooltip content={<ChartTooltipContent />} />
           - <Line dataKey="value" stroke="var(--color-value)" strokeWidth={2} dot={false} />
         - </LineChart>
       - </ChartContainer>
    7. For the revenue chart: add a custom tooltip formatter that calls formatRevenue() to display "1 200 грн" in the tooltip. Use the ChartTooltipContent formatter prop: formatter={(value) => formatRevenue(value as number)}.
  </action>
  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npx tsc --noEmit 2>&1 | grep -i "analytics-charts" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/admin/analytics-charts.tsx exists
    - First line is "use client"
    - File exports named function AnalyticsCharts
    - File imports ChartContainer from "@/components/ui/chart"
    - File imports Line, LineChart from "recharts"
    - Props type accepts ordersByDay: DayPoint[] and revenueByDay: DayPoint[]
    - Two ChartContainer elements with className="h-[220px] w-full"
    - Revenue chart tooltip uses formatRevenue for display
    - npx tsc --noEmit exits 0 with no errors from this file
  </acceptance_criteria>
  <done>AnalyticsCharts created. Plan 05 imports it for the analytics page.</done>
</task>

<task type="auto">
  <name>Task 2: Create AnalyticsDashboardPreview component</name>
  <files>src/components/admin/analytics-dashboard-preview.tsx</files>
  <read_first>
    - src/components/admin/analytics-charts.tsx (just created — use same import pattern and ChartConfig shape)
    - src/components/ui/chart.tsx (ChartContainer props)
    - src/server/services/admin-analytics.service.ts (DayPoint, formatRevenue)
    - .planning/phases/34-admin-analytics/34-PATTERNS.md (analytics-dashboard-preview.tsx section — full component pattern)
    - .planning/phases/34-admin-analytics/34-CONTEXT.md (D-07: 2 mini-charts + link; D-08: height ~120px)
  </read_first>
  <action>
    Create src/components/admin/analytics-dashboard-preview.tsx:

    1. First line: "use client";
    2. Imports: Line, LineChart from "recharts"; ChartConfig, ChartContainer from "@/components/ui/chart"; Link from "next/link"; DayPoint, formatRevenue from "@/server/services/admin-analytics.service".
    3. Props type: AnalyticsDashboardPreviewProps = { ordersByDay: DayPoint[]; revenueByDay: DayPoint[]; totalOrders: number; totalRevenue: number }.
    4. Two ChartConfig constants (same as AnalyticsCharts: ordersChartConfig with chart-1, revenueChartConfig with chart-2).
    5. Named export: export function AnalyticsDashboardPreview({ ordersByDay, revenueByDay, totalOrders, totalRevenue }: AnalyticsDashboardPreviewProps).
    6. Return structure:
       <section className="space-y-4">
         <div className="flex items-center justify-between">
           <h2 className="text-lg font-semibold">Аналітика (30 днів)</h2>
           <Link href="/admin/analityka" className="text-sm text-muted-foreground hover:text-foreground">Детальна аналітика →</Link>
         </div>
         <!-- KPI row: 2 stat figures side by side -->
         <div className="flex gap-6">
           <div><p className="text-sm text-muted-foreground">Замовлень</p><p className="text-2xl font-semibold tabular-nums">{totalOrders}</p></div>
           <div><p className="text-sm text-muted-foreground">Виручка</p><p className="text-2xl font-semibold tabular-nums">{formatRevenue(totalRevenue)}</p></div>
         </div>
         <!-- 2 mini-charts side by side -->
         <div className="grid gap-4 md:grid-cols-2">
           <ChartContainer config={ordersChartConfig} className="h-[120px] w-full">
             <LineChart data={ordersByDay}>
               <Line dataKey="value" stroke="var(--color-value)" strokeWidth={2} dot={false} />
             </LineChart>
           </ChartContainer>
           <ChartContainer config={revenueChartConfig} className="h-[120px] w-full">
             <LineChart data={revenueByDay}>
               <Line dataKey="value" stroke="var(--color-value)" strokeWidth={2} dot={false} />
             </LineChart>
           </ChartContainer>
         </div>
       </section>
    7. Mini-charts have NO axes (no CartesianGrid, no XAxis, no ChartTooltip) — purely visual trend (per D-08).
  </action>
  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npx tsc --noEmit 2>&1 | grep -i "analytics-dashboard-preview" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/admin/analytics-dashboard-preview.tsx exists
    - First line is "use client"
    - File exports named function AnalyticsDashboardPreview
    - Props include ordersByDay, revenueByDay, totalOrders, totalRevenue
    - Two ChartContainer elements with className="h-[120px] w-full"
    - No XAxis, CartesianGrid, or ChartTooltip in either mini-chart (no axes per D-08)
    - Link href="/admin/analityka" with text "Детальна аналітика →" is present
    - formatRevenue(totalRevenue) used for revenue KPI display
    - npx tsc --noEmit exits 0 with no errors from this file
    - npm test passes (full suite still green)
  </acceptance_criteria>
  <done>AnalyticsDashboardPreview created. Plan 05 imports it for the dashboard page.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC → client chart component | Data passed as serialized props — only number[] and string[] cross the boundary; no user-controlled values |
| Chart component → DOM | Recharts renders to SVG — no innerHTML or dangerouslySetInnerHTML; no XSS surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-P04-01 | Information Disclosure | analytics chart data props | accept | Data originates from server-side Prisma queries, passed as plain numbers to client component; no sensitive PII in chart data |
| T-34-P04-02 | Tampering | recharts SVG rendering | accept | Recharts uses React's safe JSX rendering — no eval(), no dangerouslySetInnerHTML; input is validated numeric arrays |
| T-34-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan — recharts already installed in Plan 02 |
</threat_model>

<verification>
npx tsc --noEmit exits 0 — no TypeScript errors.
Both files export their named functions.
Mini-charts have h-[120px], full charts have h-[220px].
npm test passes.
</verification>

<success_criteria>
Both chart components committed with zero TypeScript errors. Plan 05 can wire them into pages immediately.
</success_criteria>

<output>
Create .planning/phases/34-admin-analytics/34-04-SUMMARY.md when done.
</output>
