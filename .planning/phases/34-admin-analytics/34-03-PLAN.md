---
phase: 34-admin-analytics
plan: 03
type: execute
wave: 1
depends_on:
  - "34-01"
files_modified:
  - src/components/admin/admin-nav-items.ts
  - src/components/admin/period-selector.tsx
autonomous: true
requirements:
  - AN-01

must_haves:
  truths:
    - "Admin sidebar shows «Аналітика» nav link pointing to /admin/analityka"
    - "PeriodSelector renders 3 Link buttons: «7 днів», «30 днів», «90 днів» (per D-04)"
    - "Active period link is visually distinct (border-primary bg-primary)"
    - "Non-active links navigate via Link href=?days={N}"
  artifacts:
    - path: "src/components/admin/admin-nav-items.ts"
      provides: "Admin sidebar navigation items — updated with Аналітика entry"
      contains: "href: \"/admin/analityka\""
    - path: "src/components/admin/period-selector.tsx"
      provides: "Period toggle component for analytics page"
      exports: ["PeriodSelector"]
  key_links:
    - from: "src/components/admin/admin-nav-items.ts"
      to: "src/components/admin/admin-nav.tsx"
      via: "adminNavItems import"
      pattern: "adminNavItems"
    - from: "src/components/admin/period-selector.tsx"
      to: "src/app/(admin)/admin/analityka/page.tsx"
      via: "PeriodSelector import"
      pattern: "PeriodSelector"
---

<objective>
Add the «Аналітика» sidebar nav entry and create the PeriodSelector component. These are independent of the service and chart layers and can be built in Wave 1 in parallel with Plan 02.

Purpose: Nav entry makes the analytics page discoverable. PeriodSelector drives the 7/30/90-day toggle on the analytics page via URL navigation (no client state).
Output: Updated admin-nav-items.ts + new period-selector.tsx.
</objective>

<execution_context>
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/PROJECT.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/phases/34-admin-analytics/34-CONTEXT.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/phases/34-admin-analytics/34-PATTERNS.md
</context>

<interfaces>
<!-- From src/components/admin/admin-nav-items.ts (current file, lines 1–17) -->

Current structure:
```typescript
import {
  FolderTree,
  LayoutDashboard,
  MessageSquare,
  Package,
  Settings,
  ShoppingBag,
} from "lucide-react";

export const adminNavItems = [
  { href: "/admin", label: "Панель", icon: LayoutDashboard },
  { href: "/admin/kategorii", label: "Категорії", icon: FolderTree },
  { href: "/admin/tovary", label: "Товари", icon: Package },
  { href: "/admin/zamovlennia", label: "Замовлення", icon: ShoppingBag },
  { href: "/admin/chaty", label: "Чати", icon: MessageSquare },
  { href: "/admin/nalashtuvannia", label: "Налаштування", icon: Settings },
] as const;
```

<!-- From src/components/admin/order-list-filters.tsx — Link + cn active-state styling analog -->
Active/inactive button styling:
```typescript
className={cn(
  "rounded-md border px-3 py-1.5 text-sm transition-colors",
  active === filter.key
    ? "border-primary bg-primary text-primary-foreground"
    : "border-border bg-background text-muted-foreground hover:text-foreground",
)}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Аналітика nav item to admin-nav-items.ts</name>
  <files>src/components/admin/admin-nav-items.ts</files>
  <read_first>
    - src/components/admin/admin-nav-items.ts (full file — current nav items, import list, as const pattern)
    - src/components/admin/admin-nav.tsx (to confirm how adminNavItems is consumed — nav link rendering)
  </read_first>
  <action>
    Edit src/components/admin/admin-nav-items.ts:
    1. Add BarChart2 to the lucide-react import (alongside existing imports FolderTree, LayoutDashboard, MessageSquare, Package, Settings, ShoppingBag).
    2. Insert a new nav item before the «Налаштування» entry:
       { href: "/admin/analityka", label: "Аналітика", icon: BarChart2 }
    3. Keep `as const` on the array.
    4. Do not reorder any existing items.
  </action>
  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && grep -c "analityka" src/components/admin/admin-nav-items.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "analityka" src/components/admin/admin-nav-items.ts outputs 1
    - grep -c "BarChart2" src/components/admin/admin-nav-items.ts outputs 2 (import + usage)
    - The entry { href: "/admin/analityka", label: "Аналітика", icon: BarChart2 } appears before the Settings entry
    - `npx tsc --noEmit` exits 0 (no type errors introduced)
  </acceptance_criteria>
  <done>Nav item added. Аналітика appears in the admin sidebar after deploying.</done>
</task>

<task type="auto">
  <name>Task 2: Create PeriodSelector component</name>
  <files>src/components/admin/period-selector.tsx</files>
  <read_first>
    - src/components/admin/order-list-filters.tsx (full file — Link-based tab pattern, cn active styling, props pattern)
    - src/lib/utils.ts (cn function — confirm import path)
    - .planning/phases/34-admin-analytics/34-PATTERNS.md (period-selector section for the full component pattern)
    - .planning/phases/34-admin-analytics/34-RESEARCH.md (Pattern 3: Period Selector via searchParams — note on "use client" not needed if receiving active as prop)
  </read_first>
  <action>
    Create src/components/admin/period-selector.tsx:

    1. No "use client" directive — this is a server component (receives active as prop from RSC page, renders only Link elements; no hooks needed).
    2. Props type: PeriodSelectorProps = { active: 7 | 30 | 90 }.
    3. Periods constant: const periods = [7, 30, 90] as const (type: readonly [7, 30, 90]).
    4. Import: Link from "next/link"; cn from "@/lib/utils".
    5. Named export: export function PeriodSelector({ active }: PeriodSelectorProps).
    6. Render: <div className="flex gap-2"> wrapping {periods.map(p => <Link key={p} href={`?days=${p}`} className={cn(...)}>…</Link>)}.
    7. Active link className: "rounded-md border px-3 py-1.5 text-sm transition-colors border-primary bg-primary text-primary-foreground".
    8. Inactive link className: "rounded-md border px-3 py-1.5 text-sm transition-colors border-border bg-background text-muted-foreground hover:text-foreground".
    9. Link label text: `${p} днів` (e.g., "7 днів", "30 днів", "90 днів").
  </action>
  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npx tsc --noEmit 2>&1 | grep -i "period-selector" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/admin/period-selector.tsx exists
    - File does NOT contain "use client" directive (server component per RESEARCH.md Pattern 3)
    - File exports named function PeriodSelector
    - Renders 3 Link elements with href="?days=7", href="?days=30", href="?days=90"
    - Active link has border-primary bg-primary classes when active === p
    - `npx tsc --noEmit` exits 0 with no errors from this file
    - grep -c "PeriodSelector" src/components/admin/period-selector.tsx outputs 2 (type + function)
  </acceptance_criteria>
  <done>PeriodSelector created. Ready to be imported by the analytics page in Plan 05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nav config → sidebar render | Static config array, no user input; no trust boundary |
| PeriodSelector href | Link hrefs use literal period values (7, 30, 90) — not derived from user input; not an injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-P03-01 | Information Disclosure | /admin/analityka nav link | accept | Link is only visible to authenticated admin users inside the admin layout — requireAdmin() in layout.tsx already gates all /admin/* |
| T-34-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
grep "analityka" src/components/admin/admin-nav-items.ts → 1 match.
grep "PeriodSelector" src/components/admin/period-selector.tsx → 2+ matches.
npx tsc --noEmit exits 0.
</verification>

<success_criteria>
Nav item committed, PeriodSelector committed. No type errors. Plan 05 can import PeriodSelector.
</success_criteria>

<output>
Create .planning/phases/34-admin-analytics/34-03-SUMMARY.md when done.
</output>
