---
phase: 16-shadcn-select-audit-verify
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/catalog/catalog-toolbar.tsx
  - src/components/catalog/catalog-filters.tsx
autonomous: true
requirements:
  - UX-01
context_decisions:
  - "D-16-01: catalog-toolbar + catalog-filters migration"
  - "D-16-02: nuqs controlled Select"
  - "D-16-05: reset storinka on change"
  - "D-16-06: brand sentinel __all__"
  - "D-16-07: sort w-36"
  - "D-16-08: brand w-full"
  - "D-16-10: UA aria-labels"

must_haves:
  truths:
    - "catalog-toolbar sort uses shadcn Select with aria-label Сортування"
    - "Changing sort resets storinka to 1"
    - "Brand filter maps sentinel to brend null"
    - "No native select in catalog-toolbar or catalog-filters"
  artifacts:
    - path: src/components/catalog/catalog-toolbar.tsx
      provides: shadcn sort Select
    - path: src/components/catalog/catalog-filters.tsx
      provides: shadcn brand Select
  key_links:
    - from: catalog-toolbar
      to: catalogParsers.sort
      pattern: "setParams.*storinka: 1"

---

<objective>
Migrate storefront catalog native selects to shadcn (D-16-01, D-16-02, D-16-05–08).

Purpose: UX-01 storefront half — consistent controls with admin; preserve nuqs deep links.
Output: `catalog-toolbar.tsx` and `catalog-filters.tsx` free of `<select>`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-shadcn-select-audit-verify/16-CONTEXT.md
@.planning/phases/16-shadcn-select-audit-verify/16-RESEARCH.md
@.planning/phases/16-shadcn-select-audit-verify/16-UI-SPEC.md
@.planning/phases/16-shadcn-select-audit-verify/16-PATTERNS.md
@src/components/catalog/catalog-toolbar.tsx
@src/components/catalog/catalog-filters.tsx
@src/lib/catalog/search-params.ts
@src/components/ui/select.tsx
</context>

<threat_model>
| ID | Threat | Disposition | Mitigation |
|----|--------|-------------|------------|
| T-16-01-01 | Sentinel `__all__` written to URL as brend | mitigate | Map to `null` in onValueChange only |
| T-16-01-02 | Sort value outside enum breaks parser | mitigate | Cast to union matching `catalogParsers.sort` |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: catalog-toolbar sort Select</name>
  <read_first>
    - src/components/catalog/catalog-toolbar.tsx — current native select block lines 63–77
    - src/lib/catalog/search-params.ts — sort enum values
    - .planning/phases/16-shadcn-select-audit-verify/16-PATTERNS.md — toolbar pattern
    - src/components/ui/select.tsx — exports
  </read_first>
  <files>src/components/catalog/catalog-toolbar.tsx</files>
  <action>
    Remove native `<select>` for sort.

    Import `Select`, `SelectContent`, `SelectItem`, `SelectTrigger`, `SelectValue` from `@/components/ui/select`.

    Controlled Select:
    - `value={params.sort}`
    - `onValueChange={(value) => setParams({ sort: value as "novi" | "cina-asc" | "cina-desc", storinka: 1 })}`
    - `SelectTrigger` with `className="w-36"` and `aria-label="Сортування"`
    - Three `SelectItem` values: `novi`, `cina-asc`, `cina-desc` with UA labels «Новіші», «Ціна ↑», «Ціна ↓»

    Do not change search debounce or ActiveFilterChips behavior.
  </action>
  <verify>
    <automated>! grep -q '&lt;select' src/components/catalog/catalog-toolbar.tsx && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `catalog-toolbar.tsx` contains `SelectTrigger` and no `<select`
    - `aria-label="Сортування"` present on trigger
    - `onValueChange` includes `storinka: 1`
    - `npm run build` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: catalog-filters brand Select</name>
  <read_first>
    - src/components/catalog/catalog-filters.tsx — brand section ~lines 210–225
    - .planning/phases/16-shadcn-select-audit-verify/16-CONTEXT.md — D-16-06
    - .planning/phases/16-shadcn-select-audit-verify/16-PATTERNS.md — ALL_BRANDS sentinel
  </read_first>
  <files>src/components/catalog/catalog-filters.tsx</files>
  <action>
    Add module-level constant `const ALL_BRANDS = "__all__"` (or equivalent sentinel not in brands list).

    Replace native brand `<select>` with shadcn Select:
    - `value={params.brend ?? ALL_BRANDS}`
    - `onValueChange={(value) => setParams({ brend: value === ALL_BRANDS ? null : value, storinka: 1 })}`
    - `SelectTrigger className="w-full"`
    - First `SelectItem value={ALL_BRANDS}` label «Усі бренди»
    - Map `brands` array to `SelectItem` per brand string

    Preserve surrounding section heading «Бренд» and layout.
  </action>
  <verify>
    <automated>! grep -q '&lt;select' src/components/catalog/catalog-filters.tsx && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `catalog-filters.tsx` has no `<select`
    - Sentinel maps to `brend: null` in setParams
    - `storinka: 1` on brand change
    - `npm run build` exits 0
  </acceptance_criteria>
</task>

</tasks>

## PLAN COMPLETE
