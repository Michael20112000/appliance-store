---
phase: 16-shadcn-select-audit-verify
plan: 03
type: execute
wave: 2
depends_on:
  - "16-01"
  - "16-02"
files_modified:
  - src/components/admin/product-form.tsx
  - src/components/admin/category-form.tsx
  - .planning/phases/16-shadcn-select-audit-verify/16-MANUAL-CHECKLIST.md
autonomous: false
requirements:
  - UX-01
  - POL-01
  - POL-02
context_decisions:
  - "D-16-11: grep src/components zero select"
  - "D-16-13: manual checklist gallery"
  - "D-16-17: slug hint verify"
  - "D-16-21: build + tests green"

must_haves:
  truths:
    - "grep -r '<select' src/components returns no matches"
    - "16-MANUAL-CHECKLIST.md exists with gallery scenarios"
    - "npm run build and npm run test exit 0"
    - "Product create has no slug input; edit shows read-only URL"
  artifacts:
    - path: .planning/phases/16-shadcn-select-audit-verify/16-MANUAL-CHECKLIST.md
      provides: POL-01 manual gate
  key_links:
    - from: 16-MANUAL-CHECKLIST.md
      to: src/components/catalog/product-gallery.tsx
      pattern: "Dialog"

---

<objective>
Verify slug policy, run PDP gallery manual pass, enforce grep + build gate (POL-01, POL-02, D-16-11, D-16-21).

Purpose: Milestone closeout — no native selects in components; gallery and slug behavior confirmed.
Output: Manual checklist file; optional minor copy fixes; full test/build green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-shadcn-select-audit-verify/16-CONTEXT.md
@.planning/phases/16-shadcn-select-audit-verify/16-RESEARCH.md
@src/components/catalog/product-gallery.tsx
@src/components/admin/product-form.tsx
@src/components/admin/category-form.tsx
@.planning/phases/14-admin-chat-context-menu/14-MANUAL-CHECKLIST.md
</context>

<threat_model>
| ID | Threat | Disposition | Mitigation |
|----|--------|-------------|------------|
| T-16-03-01 | select in src/app breaks grep gate | accept | D-16-12: only src/components in gate; note in summary if found |
| T-16-03-02 | Gallery blocker unfixed | mitigate | Fix only blocking bugs per D-16-14 |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create 16-MANUAL-CHECKLIST.md</name>
  <read_first>
    - .planning/phases/14-admin-chat-context-menu/14-MANUAL-CHECKLIST.md — table format
    - .planning/phases/16-shadcn-select-audit-verify/16-CONTEXT.md — D-16-13 scenarios
    - src/components/catalog/product-gallery.tsx — Carousel + Dialog structure
  </read_first>
  <files>.planning/phases/16-shadcn-select-audit-verify/16-MANUAL-CHECKLIST.md</files>
  <action>
    Create manual checklist markdown with table columns: # | Route | Action | Expected | Pass.

    Rows (UA copy):
    1. PDP with 1 image — `/tovar/{slug}` — open page — single image, no broken layout
    2. PDP 3+ images — same — thumb strip + main carousel sync
    3. Mobile ~375px — resize — layout usable, thumbs scroll if needed
    4. Tap main image — Dialog opens full-size lightbox
    5. Dialog arrows + close — navigate images; after close main thumb matches last viewed

    Footer: fix only blocking bugs; swipe optional per D-16-15.
  </action>
  <verify>
    <automated>test -f .planning/phases/16-shadcn-select-audit-verify/16-MANUAL-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists with at least 5 scenario rows
    - References `/tovar/` PDP route
    - Notes blocking-only fix policy
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Verify auto slug UI (POL-02)</name>
  <read_first>
    - src/components/admin/product-form.tsx — create hint + edit URL line
    - src/components/admin/category-form.tsx — confirm no slug field
    - .planning/phases/16-shadcn-select-audit-verify/16-CONTEXT.md — D-16-17–20
  </read_first>
  <files>src/components/admin/product-form.tsx</files>
  <action>
    Verify (read-only audit; edit only if gap found):
    - Create mode: no `slug` input; hint under title mentions auto URL (adjust UA to «URL згенерується з назви» only if current copy differs materially)
    - Edit mode: read-only `URL: /tovar/{slug}` line, no editable slug
    - category-form: no slug field on create or edit

    Do NOT change `admin-product.service.ts` slug resolution logic.

    If already compliant, no code change required — document in plan summary.
  </action>
  <verify>
    <automated>! grep -q 'register("slug")' src/components/admin/product-form.tsx && ! grep -q 'id="slug"' src/components/admin/category-form.tsx</automated>
  </verify>
  <acceptance_criteria>
    - No slug input on product create/edit forms
    - Create shows auto-slug hint or equivalent UA message
    - Edit shows read-only storefront URL when slug known
  </acceptance_criteria>
</task>

<task type="manual">
  <name>Task 3: Execute gallery manual checklist</name>
  <read_first>
    - .planning/phases/16-shadcn-select-audit-verify/16-MANUAL-CHECKLIST.md
    - src/components/catalog/product-gallery.tsx
  </read_first>
  <action>
    With `npm run dev`, walk all rows in `16-MANUAL-CHECKLIST.md` on desktop and mobile viewport.

    Mark Pass column. If blocking bug found, fix in `product-gallery.tsx` only (minimal diff per D-16-14).

    Swipe in dialog is not required for pass.
  </action>
  <acceptance_criteria>
    - All checklist rows marked Pass OR blocking fixes committed
    - No gallery Playwright added
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Grep gate + full test/build</name>
  <read_first>
    - .planning/phases/16-shadcn-select-audit-verify/16-CONTEXT.md — D-16-11, D-16-12
  </read_first>
  <files></files>
  <action>
    Run `grep -r '&lt;select' src/components` — must return empty (exit 1 from grep = success).

    If matches in `src/app`, note in SUMMARY per D-16-12 without expanding scope.

    Run `npm run test` then `npm run build` — both must exit 0.

    Optional smoke: `npx playwright test e2e/public-browse.spec.ts` if catalog e2e exists and is fast.
  </action>
  <verify>
    <automated>! grep -r '&lt;select' src/components && npm run test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Zero `<select` in `src/components`
    - `npm run test` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
</task>

</tasks>

## PLAN COMPLETE
