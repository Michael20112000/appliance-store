---
phase: 16-shadcn-select-audit-verify
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/admin/product-form.tsx
autonomous: true
requirements:
  - UX-01
context_decisions:
  - "D-16-03: RHF Controller + Select"
  - "D-16-04: no shared wrapper"
  - "D-16-09: w-full triggers"

must_haves:
  truths:
    - "product-form categoryId, condition, status use Controller + shadcn Select"
    - "No native select in product-form.tsx"
    - "UA labels preserved for condition and status"
  artifacts:
    - path: src/components/admin/product-form.tsx
      provides: admin form selects
  key_links:
    - from: product-form
      to: upsertProductSchema
      pattern: "Controller.*categoryId"

---

<objective>
Migrate admin product form native selects to shadcn + react-hook-form Controller (D-16-03, D-16-04, D-16-09).

Purpose: UX-01 admin half — align with ProductListStatusSelect styling.
Output: Three RHF-controlled Select fields; zero `<select>` in product-form.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-shadcn-select-audit-verify/16-CONTEXT.md
@.planning/phases/16-shadcn-select-audit-verify/16-RESEARCH.md
@.planning/phases/16-shadcn-select-audit-verify/16-PATTERNS.md
@src/components/admin/product-form.tsx
@src/components/admin/product-list-status-select.tsx
@src/components/ui/select.tsx
</context>

<threat_model>
| ID | Threat | Disposition | Mitigation |
|----|--------|-------------|------------|
| T-16-02-01 | RHF field not updated on Select change | mitigate | Use Controller `field.onChange` |
| T-16-02-02 | Invalid enum string from Select | mitigate | Values match existing option values only |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Controller selects for categoryId, condition, status</name>
  <read_first>
    - src/components/admin/product-form.tsx — imports, form setup, three native selects ~181–261
    - src/components/admin/product-list-status-select.tsx — Select composition
    - .planning/phases/16-shadcn-select-audit-verify/16-PATTERNS.md — Controller template
    - CONDITION / conditionLabelUa imports in product-form
  </read_first>
  <files>src/components/admin/product-form.tsx</files>
  <action>
    Import `Controller` from `react-hook-form` and Select primitives from `@/components/ui/select`.

    Replace each native `<select>` with `Controller`:
    1. **categoryId** — `SelectItem value={category.id}` label `category.name`
    2. **condition** — items from `CONDITIONS` map with `conditionLabelUa(condition)`
    3. **status** (when `!isSold`) — `DRAFT` / `AVAILABLE` UA labels «Чернетка» / «В наявності»

    Each: `SelectTrigger` with `id` matching Label `htmlFor`, `className="w-full"`.

    Remove `{...form.register("categoryId")}` (and condition/status) from native elements.

    Do not change slug hint block, quantity field, or submit actions.
  </action>
  <verify>
    <automated>! grep -q '&lt;select' src/components/admin/product-form.tsx && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `product-form.tsx` imports `Controller` and has three `Controller` blocks for selects
    - No `<select` substring in file
    - `SelectTrigger` uses `className="w-full"` on form selects
    - `npm run build` exits 0
  </acceptance_criteria>
</task>

</tasks>

## PLAN COMPLETE
