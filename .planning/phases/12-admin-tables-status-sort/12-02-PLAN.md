---
phase: 12-admin-tables-status-sort
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - src/server/validators/admin-product.ts
  - src/server/validators/admin-product.test.ts
  - src/server/services/admin-product.service.ts
  - src/lib/admin/products-url.ts
  - src/lib/admin/products-url.test.ts
autonomous: true
requirements:
  - ADM-PRD-02
context_decisions:
  - "Server-side sort via URL sort/dir — mirror orders"
  - "Default orderBy updatedAt desc when sort omitted"
  - "Sort keys: title, category, price, status — no updatedAt in URL enum"

must_haves:
  truths:
    - "listAdminProductsSchema accepts sort and dir with safe defaults"
    - "listAdminProducts uses buildPrismaProductOrderBy(sort, dir)"
    - "adminProductsUrl emits sort and dir when non-default"
    - "Vitest covers URL and schema for sort params"
  artifacts:
    - path: src/server/validators/admin-product.ts
      provides: adminProductListSortSchema, adminProductListDirSchema
    - path: src/server/services/admin-product.service.ts
      provides: buildPrismaProductOrderBy + listAdminProducts orderBy
    - path: src/lib/admin/products-url.ts
      provides: sort/dir in AdminProductsUrlParams
  key_links:
    - from: src/lib/admin/products-url.ts
      to: src/server/validators/admin-product.ts
      via: shared sort enum values
      pattern: "title|category|price|status"
    - from: src/server/services/admin-product.service.ts
      to: prisma.product.findMany
      via: orderBy from buildPrismaProductOrderBy
      pattern: "orderBy:"

---

<objective>
ADM-PRD-02 backend: product list sorting via URL query params and Prisma orderBy (mirror orders stack).

Purpose: UI plan 12-03 needs typed sort/dir from page through service; URL builder must preserve sort across pagination.
Output: Validator + service + products-url + tests. No table UI changes in this plan.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-admin-tables-status-sort/12-RESEARCH.md
@.planning/phases/12-admin-tables-status-sort/12-UI-SPEC.md
@.planning/phases/12-admin-tables-status-sort/12-PATTERNS.md
@src/server/validators/admin-order.ts
@src/server/services/admin-order.service.ts
@src/lib/admin/orders-url.ts
@src/lib/admin/products-url.ts
@src/server/validators/admin-product.test.ts
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| searchParams sort/dir | Zod coerce + enum whitelist only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-01 | Tampering | sort param | mitigate | Zod enum — reject unknown keys |
| T-12-02-02 | DoS | orderBy | accept | Indexed columns only; existing pagination cap |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add product sort/dir schemas</name>
  <read_first>
    - src/server/validators/admin-order.ts — adminOrderListSortSchema, adminOrderListDirSchema, listOrdersAdminSchema
    - src/server/validators/admin-product.ts — listAdminProductsSchema
    - src/server/validators/admin-product.test.ts
  </read_first>
  <files>src/server/validators/admin-product.ts, src/server/validators/admin-product.test.ts</files>
  <action>
    Add and export:

    `adminProductListSortSchema = z.enum(["title", "category", "price", "status"])`

    `adminProductListDirSchema = z.enum(["asc", "desc"])`

    Extend `listAdminProductsSchema` with:
    - `sort: adminProductListSortSchema.optional()` (no default in schema — absence means backend default updatedAt)
    - `dir: adminProductListDirSchema.default("desc")`

    Export types `AdminProductListSort`, `AdminProductListDir`.

    Add tests: parse valid sort+dir; reject invalid sort string; dir defaults to desc when sort provided.
  </action>
  <verify>
    <automated>npx vitest run src/server/validators/admin-product.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - admin-product.ts exports AdminProductListSort and AdminProductListDir
    - listAdminProductsSchema.parse({ sort: "title", dir: "asc" }) succeeds
    - parse({ sort: "invalid" }) throws
  </acceptance_criteria>
  <done>Product sort validator ready</done>
</task>

<task type="auto">
  <name>Task 2: buildPrismaProductOrderBy + listAdminProducts</name>
  <read_first>
    - src/server/services/admin-order.service.ts — buildPrismaOrderBy function
    - src/server/services/admin-product.service.ts — listAdminProducts (~178-201)
    - src/server/validators/admin-product.ts — sort types from Task 1
  </read_first>
  <files>src/server/services/admin-product.service.ts</files>
  <action>
    Add exported function `buildPrismaProductOrderBy(sort: AdminProductListSort | undefined, dir: AdminProductListDir)` returning Prisma.ProductOrderByWithRelationInput:

    - sort undefined → `{ updatedAt: "desc" }`
    - title → `{ title: dir }`
    - category → `{ category: { name: dir } }`
    - price → `{ price: dir }`
    - status → `{ status: dir }`

    In `listAdminProducts`, replace hardcoded `orderBy: { updatedAt: "desc" }` with `orderBy: buildPrismaProductOrderBy(filters.sort, filters.dir)`.

    Ensure ListAdminProductsFilters type includes optional sort and dir from schema output.
  </action>
  <verify>
    <automated>npx vitest run src/server/validators/admin-product.test.ts &amp;&amp; npm run build 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - listAdminProducts uses buildPrismaProductOrderBy
    - No hardcoded updatedAt orderBy left inline in findMany call
    - TypeScript compiles
  </acceptance_criteria>
  <done>Server-side product sort implemented</done>
</task>

<task type="auto">
  <name>Task 3: adminProductsUrl sort/dir + tests</name>
  <read_first>
    - src/lib/admin/orders-url.ts — DEFAULT_SORT, DEFAULT_DIR, param emission
    - src/lib/admin/products-url.ts — current implementation
    - src/lib/admin/products-url.test.ts
    - src/lib/admin/orders-url.test.ts — sort/dir test case
  </read_first>
  <files>src/lib/admin/products-url.ts, src/lib/admin/products-url.test.ts</files>
  <action>
    Extend `AdminProductsUrlParams` with optional `sort?: AdminProductListSort` and `dir?: AdminProductListDir`.

    Import sort types from `@/server/validators/admin-product`.

    Do NOT emit `sort` or `dir` when sort is undefined (default updatedAt backend behavior).

    When sort is set: emit `sort` param; emit `dir` only when dir !== "desc" OR always emit both when sort set (match orders-url style: orders omits default createdAt and default desc — mirror: omit sort when undefined; when sort present include dir if non-default desc).

    Follow orders-url.ts pattern exactly for consistency.

    Update products-url.test.ts:
    - `adminProductsUrl({ sort: "title", dir: "asc" })` includes query string
    - defaults omit sort/dir
    - pagination preserves sort: `adminProductsUrl({ page: 3, sort: "price", dir: "desc", status: "DRAFT" })`
  </action>
  <verify>
    <automated>npx vitest run src/lib/admin/products-url.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - products-url.test.ts has at least 2 cases for sort/dir
    - All products-url tests pass
  </acceptance_criteria>
  <done>URL layer supports product sort state</done>
</task>

</tasks>

<verification>
## Plan 12-02 verification

- [ ] `npx vitest run src/lib/admin/products-url.test.ts src/server/validators/admin-product.test.ts`
- [ ] `npm test` passes
</verification>
