---
phase: 12-admin-tables-status-sort
plan: 03
type: execute
wave: 3
depends_on:
  - 12-02
files_modified:
  - src/app/(admin)/admin/tovary/page.tsx
  - src/components/admin/admin-products-table.tsx
  - src/components/admin/products-list-pagination.tsx
  - src/components/admin/product-list-filters.tsx
  - src/components/admin/admin-sortable-table-header.tsx
  - .planning/phases/12-admin-tables-status-sort/12-MANUAL-CHECKLIST.md
autonomous: true
requirements:
  - ADM-ORD-02
  - ADM-PRD-02
context_decisions:
  - "Products keep plain table — sort via Link headers like orders SortableHeader"
  - "Optional shared admin-sortable-table-header.tsx for DRY"
  - "Preserve sort/dir in filters and pagination links"

must_haves:
  truths:
    - "Products table headers for Назва, Категорія, Ціна, Статус are sortable Links with arrow icons"
    - "Фото column has no sort control"
    - "Changing sort updates URL and reorders list server-side"
    - "Pagination and filter chips preserve active sort and dir"
    - "ProductListStatusSelect still stopPropagation — row-click regression safe"
  artifacts:
    - path: src/components/admin/admin-products-table.tsx
      provides: Sortable column headers + aria-sort
    - path: src/app/(admin)/admin/tovary/page.tsx
      provides: Parses sort/dir searchParams, passes to table
    - path: .planning/phases/12-admin-tables-status-sort/12-MANUAL-CHECKLIST.md
      provides: Manual UAT for phase 12
  key_links:
    - from: src/app/(admin)/admin/tovary/page.tsx
      to: src/server/services/admin-product.service.ts
      via: listAdminProducts(filters with sort)
      pattern: "listAdminProducts"
    - from: src/components/admin/admin-products-table.tsx
      to: src/lib/admin/products-url.ts
      via: adminProductsUrl in header hrefs
      pattern: "adminProductsUrl"

---

<objective>
ADM-PRD-02 UI: wire product list sort headers, page searchParams, and URL preservation across filters/pagination.

Purpose: Complete phase goal — products table sorts like orders Data Table pattern.
Output: Updated table + page + pagination + filters + manual checklist.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-admin-tables-status-sort/12-UI-SPEC.md
@.planning/phases/12-admin-tables-status-sort/12-RESEARCH.md
@.planning/phases/12-admin-tables-status-sort/12-PATTERNS.md
@src/components/admin/orders-data-table.tsx
@src/components/admin/admin-products-table.tsx
@src/app/(admin)/admin/tovary/page.tsx
@src/components/admin/products-list-pagination.tsx
@src/components/admin/product-list-filters.tsx
@src/lib/admin/products-url.ts
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Header hrefs | Built only via adminProductsUrl with server-known filter state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03-01 | Tampering | filter links dropping sort | mitigate | Pass sort/dir on every adminProductsUrl call in filters |
| T-12-03-02 | Tampering | pagination dropping sort | mitigate | ProductsListPagination hrefFor includes sort/dir |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Shared sort header helper (optional but recommended)</name>
  <read_first>
    - src/components/admin/orders-data-table.tsx — SortableHeader, nextSortDir, getAriaSort (lines 63-125)
    - .planning/phases/12-admin-tables-status-sort/12-UI-SPEC.md — Sortable table header visual spec
  </read_first>
  <files>src/components/admin/admin-sortable-table-header.tsx</files>
  <action>
    Create `admin-sortable-table-header.tsx` exporting:

    - `nextSortDir(currentSort, currentDir, column)` — copy logic from orders-data-table
    - `getAriaSort(column, sort, dir)` — copy logic from orders-data-table
    - `AdminSortableTableHeader` component: props `{ href: string; label: string; sort; dir; column }` rendering Link + icons per 12-UI-SPEC (inline-flex gap-1, ArrowUpDownIcon opacity-50 when inactive, etc.)

    Refactor `orders-data-table.tsx` to import shared helpers OR duplicate inline in products only — **prefer shared** to avoid drift.

    If refactoring orders, behavior must remain identical (no visual change).
  </action>
  <verify>
    <automated>npm run build 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - AdminSortableTableHeader matches orders SortableHeader visuals
    - nextSortDir: same column toggles asc/desc; new column starts desc
  </acceptance_criteria>
  <done>Shared sort header primitive available</done>
</task>

<task type="auto">
  <name>Task 2: AdminProductsTable sortable headers</name>
  <read_first>
    - src/components/admin/admin-products-table.tsx — thead static headers
    - src/components/admin/admin-sortable-table-header.tsx — Task 1 output
    - src/lib/admin/products-url.ts — sort/dir params
  </read_first>
  <files>src/components/admin/admin-products-table.tsx</files>
  <action>
    Extend `AdminProductsTableProps` with:
    `sort?: AdminProductListSort`, `dir: AdminProductListDir` (dir required when sort set; when sort undefined use dir "desc" for aria only), `pageSize`, `status?`, `categoryId?`, `q?`.

    Replace static sortable `<th>` labels with:
    - Фото — plain text, NO aria-sort
    - Назва, Категорія, Ціна, Статус — `AdminSortableTableHeader` with `aria-sort` on `<th>`

    Each header href: `adminProductsUrl({ sort: column, dir: nextSortDir(...), page: 1, pageSize, status, categoryId, q })`.

    Body cells unchanged; `ProductListStatusSelect` unchanged.

    Table remains client component with useRouter row-click.
  </action>
  <verify>
    <automated>grep -q 'AdminSortableTableHeader\|adminProductsUrl' src/components/admin/admin-products-table.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Four sortable headers present
    - Photo header has no sort link
    - aria-sort set on four sortable th elements
  </acceptance_criteria>
  <done>Products table shows sort UI</done>
</task>

<task type="auto">
  <name>Task 3: Wire page, pagination, filters</name>
  <read_first>
    - src/app/(admin)/admin/tovary/page.tsx — parseListFilters, searchParams type
    - src/app/(admin)/admin/zamovlennia/page.tsx — orders sort parsing pattern
    - src/components/admin/products-list-pagination.tsx
    - src/components/admin/product-list-filters.tsx — all adminProductsUrl calls
  </read_first>
  <files>src/app/(admin)/admin/tovary/page.tsx, src/components/admin/products-list-pagination.tsx, src/components/admin/product-list-filters.tsx</files>
  <action>
    Extend `PageProps searchParams` with optional `sort?: string`, `dir?: string`.

    Pass through `listAdminProductsSchema.parse({ ..., sort: params.sort, dir: params.dir })`.

    Pass `sort`, `dir`, `pageSize`, `status`, `categoryId`, `q` to `AdminProductsTable`.

    Update `ProductsListPagination` props to accept optional `sort` and `dir`; include in every `adminProductsUrl` inside `hrefFor`.

    Update `ProductListFilters` to accept optional `sort` and `dir`; add to EVERY `adminProductsUrl({ ... })` call (status chips, category chips, clear filters).

    When user changes filter, preserve current sort/dir (do not reset unless product decision: reset page to 1 only — match orders filter behavior).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - tovary/page.tsx passes sort and dir from URL to listAdminProducts and table
    - Pagination link includes sort= when active
    - Filter chip link includes same sort/dir as current page
  </acceptance_criteria>
  <done>End-to-end URL sort wiring complete</done>
</task>

<task type="auto">
  <name>Task 4: Manual checklist</name>
  <read_first>
    - .planning/phases/11-admin-list-row-ux/11-MANUAL-CHECKLIST.md — format reference
    - .planning/phases/12-admin-tables-status-sort/12-UI-SPEC.md — Verification Checklist section
  </read_first>
  <files>.planning/phases/12-admin-tables-status-sort/12-MANUAL-CHECKLIST.md</files>
  <action>
    Create `12-MANUAL-CHECKLIST.md` with numbered manual tests:

    1. Orders status Select — no row nav, transition save, cancel dialog
    2. Products sort each column — URL updates, list order changes
    3. Sort preserved on pagination and filter change
    4. Product status Select regression — no row nav
    5. Photo column not sortable

    Ukrainian labels for tester; link paths `/admin/zamovlennia`, `/admin/tovary`.
  </action>
  <verify>
    <automated>test -f .planning/phases/12-admin-tables-status-sort/12-MANUAL-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists with at least 5 manual test items covering ADM-ORD-02 and ADM-PRD-02
  </acceptance_criteria>
  <done>Manual UAT checklist ready</done>
</task>

</tasks>

<verification>
## Plan 12-03 verification

- [ ] `npm test` green
- [ ] `npm run build` green
- [ ] Complete `12-MANUAL-CHECKLIST.md` on dev server
</verification>
