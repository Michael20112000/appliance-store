---
phase: 12-admin-tables-status-sort
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/admin/order-list-status-select.test.tsx
autonomous: true
requirements:
  - ADM-ORD-02
context_decisions:
  - "ADM-ORD-02 shipped in Phase 11 UAT — verify, do not rebuild Select"
  - "ROADMAP #4: Vitest smoke for status control isolation"

must_haves:
  truths:
    - "Order status cell uses OrderListStatusSelect (not read-only badge)"
    - "Select trigger stopPropagation prevents row navigation to order detail"
    - "Allowed transitions enforced via getAllowedNextStatuses + server action"
    - "CANCELLED requires AlertDialog confirm before save"
  artifacts:
    - path: src/components/admin/order-list-status-select.tsx
      provides: Inline order status control (existing)
    - path: src/components/admin/order-list-status-select.test.tsx
      provides: stopPropagation smoke test
  key_links:
    - from: src/components/admin/orders-data-table.tsx
      to: src/components/admin/order-list-status-select.tsx
      via: status column cell render
      pattern: "OrderListStatusSelect"
    - from: src/components/admin/order-list-status-select.tsx
      to: src/server/actions/admin/order.actions.ts
      via: updateOrderStatusAction
      pattern: "updateOrderStatusAction"

---

<objective>
Verify and lock ADM-ORD-02: inline order status in `/admin/zamovlennia` table (already implemented in code).

Purpose: Phase 11 brought `OrderListStatusSelect` forward; Phase 12 confirms behavior, adds automated regression, marks requirement ready for traceability.
Output: Vitest test file + no functional regressions in orders table.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-admin-tables-status-sort/12-RESEARCH.md
@.planning/phases/12-admin-tables-status-sort/12-UI-SPEC.md
@.planning/phases/11-admin-list-row-ux/11-UAT.md
@src/components/admin/order-list-status-select.tsx
@src/components/admin/orders-data-table.tsx
@src/lib/order/status-transitions.ts
@src/server/actions/admin/order.actions.ts
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orderId prop | CUID from server DTO only |
| status transitions | Server re-validates via assertTransitionAllowed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-01 | Tampering | updateOrderStatusAction | mitigate | Server-side transition guard (existing) |
| T-12-01-02 | Spoofing | row navigation | mitigate | stopPropagation on Select trigger |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Audit existing OrderListStatusSelect wiring</name>
  <read_first>
    - src/components/admin/orders-data-table.tsx — status column cell (~218-223)
    - src/components/admin/order-list-status-select.tsx — full file
    - .planning/phases/12-admin-tables-status-sort/12-UI-SPEC.md — OrderListStatusSelect spec
  </read_first>
  <files>src/components/admin/orders-data-table.tsx</files>
  <action>
    Confirm orders table status column renders `OrderListStatusSelect` with `orderId` and `status` from row DTO.

    Confirm NO `OrderStatusBadge`-only cell in list (badge may exist elsewhere, not in list status column).

    If status column regressed to badge-only, restore `OrderListStatusSelect` per 12-UI-SPEC — do NOT add new component.

    Do NOT change sort headers or row-click behavior.
  </action>
  <verify>
    <automated>grep -q 'OrderListStatusSelect' src/components/admin/orders-data-table.tsx</automated>
  </verify>
  <acceptance_criteria>
    - orders-data-table.tsx imports and renders OrderListStatusSelect in status cell
    - No «Відкрити» actions column present (Phase 11 regression guard)
  </acceptance_criteria>
  <done>Orders status column verified wired to Select</done>
</task>

<task type="auto">
  <name>Task 2: Vitest — SelectTrigger stopPropagation</name>
  <read_first>
    - src/components/admin/order-list-status-select.tsx — stopRowNav on SelectTrigger
    - src/components/admin/product-list-status-select.tsx — if similar test exists
    - vitest.config.ts — jsdom / testing-library setup
  </read_first>
  <files>src/components/admin/order-list-status-select.test.tsx</files>
  <action>
    Create `order-list-status-select.test.tsx` using @testing-library/react.

    Render `OrderListStatusSelect` with a mock orderId and status `PENDING` (has allowed next statuses).

    Find the Select trigger (role combobox or button per shadcn).

    Dispatch `click` and `pointerdown` on trigger with a parent element that has an `onClick` spy.

    Assert parent onClick was NOT called (stopPropagation).

    Mock `updateOrderStatusAction` and `useRouter` if needed (@/server/actions/admin/order.actions, next/navigation) — follow existing component test patterns in repo.

    Do NOT test full server action integration in this task.
  </action>
  <verify>
    <automated>npx vitest run src/components/admin/order-list-status-select.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists and exits 0
    - At least one assertion proves click on trigger does not bubble to parent
  </acceptance_criteria>
  <done>Vitest regression for ADM-ORD-02 row isolation</done>
</task>

</tasks>

<verification>
## Plan 12-01 verification

- [ ] `npx vitest run src/components/admin/order-list-status-select.test.tsx` passes
- [ ] Manual: `/admin/zamovlennia` — status Select opens; change saves; row does not navigate on Select click
- [ ] Manual: CANCELLED shows confirm dialog
</verification>
