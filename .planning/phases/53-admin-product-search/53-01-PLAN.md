---
phase: 53-admin-product-search
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/components/admin/product-search-input.test.tsx
  - src/server/services/admin-product.service.test.ts
autonomous: true
requirements:
  - ADM-SRCH-01

must_haves:
  truths:
    - "product-search-input.test.tsx exists and all tests fail with import/not-found errors (RED state)"
    - "admin-product.service.test.ts contains a q-filter test case for buildAdminProductWhere"
  artifacts:
    - path: "src/components/admin/product-search-input.test.tsx"
      provides: "Failing unit tests for ProductSearchInput (render, debounce, URL, page reset, sync)"
    - path: "src/server/services/admin-product.service.test.ts"
      provides: "Extended test for buildAdminProductWhere q-filter branch"
  key_links:
    - from: "src/components/admin/product-search-input.test.tsx"
      to: "src/components/admin/product-search-input.tsx"
      via: "named import ProductSearchInput"
      pattern: "import.*ProductSearchInput.*from.*product-search-input"
---

<objective>
Create Wave 0 failing test stubs for `ProductSearchInput`. Write the full test file before the component exists (RED state). Extend the existing `buildAdminProductWhere` test suite with a `q`-filter case.

Purpose: Establish the Nyquist gate — no implementation ships without tests. Both test files drive the component shape (props, behavior) before a single line of implementation is written.
Output: Two test files; test run fails with import errors or assertion failures (RED is correct here).
</objective>

<execution_context>
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/phases/53-admin-product-search/53-RESEARCH.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/phases/53-admin-product-search/53-PATTERNS.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/phases/53-admin-product-search/53-VALIDATION.md
</execution_context>

<context>
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/ROADMAP.md
@/Users/michael_ivashko/WebStormProjects/web/appliance-store/.planning/REQUIREMENTS.md

<interfaces>
<!-- Key types the test file will import — extracted from codebase -->

From src/server/validators/admin-product.ts (relevant exports):
```typescript
export type AdminProductStockFilter = "in_stock" | "out_of_stock" | undefined;
export type AdminProductListSort = string;
export type AdminProductListDir = "asc" | "desc";
```

From src/lib/pagination.ts:
```typescript
export type AdminPageSize = 10 | 20 | 50;
```

From src/server/services/admin-product.service.ts — existing test shape:
```typescript
import { buildAdminProductWhere } from "./admin-product.service";
// q param: activates OR clause for title/brand/description when q.length >= 2
```

From analog test (order-list-status-select.test.tsx) — setup pattern:
```typescript
/** @vitest-environment jsdom */
import { afterEach, describe, expect, it, vi } from "vitest";
import { cleanup, fireEvent, render, screen, waitFor } from "@testing-library/react";
vi.mock("next/navigation", () => ({
  useRouter: () => ({ replace: vi.fn() }),
}));
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write failing unit tests for ProductSearchInput</name>
  <files>src/components/admin/product-search-input.test.tsx</files>

  <read_first>
    - src/components/admin/order-list-status-select.test.tsx — exact jsdom + router mock + fireEvent + waitFor pattern to replicate
    - src/components/admin/product-list-delete-button.test.tsx — router mock variant with replace (not refresh)
    - src/server/validators/admin-product.ts — confirm AdminProductStockFilter, AdminProductListSort, AdminProductListDir exports
    - src/lib/pagination.ts — confirm AdminPageSize type
    - .planning/phases/53-admin-product-search/53-PATTERNS.md — test structure excerpt (lines 132–169)
  </read_first>

  <behavior>
    - Test 1 (render): `render(<ProductSearchInput pageSize={20} />)` → `screen.getByPlaceholderText("Назва або бренд…")` is truthy
    - Test 2 (debounce fires router.replace with q): vi.useFakeTimers() → fireEvent.change on input to "Samsung" → vi.advanceTimersByTime(300) → router.replace called with URL containing "q=Samsung" and "page=1"
    - Test 3 (clears q when input emptied): type "Samsung", advance 300 ms, type "", advance 300 ms → router.replace called with URL NOT containing "q="
    - Test 4 (page reset to 1): render with q="Sony" pageSize=20 → type new value → advance 300 ms → router.replace URL contains "page=1"
    - Test 5 (prop sync on q change): render with q="Sony", then re-render with q="LG" → input value is "LG" (useEffect sync)
  </behavior>

  <action>
    Create `src/components/admin/product-search-input.test.tsx` with the jsdom environment directive as the very first line, the five test cases described in `<behavior>`, and these constraints:

    - Mock `next/navigation` as `{ useRouter: () => ({ replace: mockReplace }) }` where `mockReplace` is a `vi.fn()` captured before the describe block so each test can assert on it. Reset it with `vi.fn()` in `beforeEach` or use `mockReplace.mockClear()` in `afterEach`.
    - Do NOT mock `@/lib/debounce` — the real `createDebounce` runs inside jsdom; `vi.useFakeTimers()` controls its internal `setTimeout`.
    - Use `vi.useFakeTimers()` in `beforeEach` and `vi.useRealTimers()` in `afterEach` (alongside `cleanup()`).
    - For Test 2 and 3 (URL assertions): capture the first argument of `mockReplace.mock.calls[0][0]` (a string) and check it with `toContain("q=Samsung")` or `not.toContain("q=")`.
    - For Test 5 (prop sync): use `rerender` from `render`'s return value. No fake timers needed — the useEffect runs synchronously in jsdom after rerender.
    - The import path is `"./product-search-input"`. The component does not exist yet; the file will fail at import. That is correct RED state.
    - Use `afterEach(() => { cleanup(); vi.useRealTimers(); })` to reset state between tests.
    - Do not mock `@/components/ui/input` — let it render (it wraps a standard input element; jsdom handles it fine).
  </action>

  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npx vitest run src/components/admin/product-search-input.test.tsx 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - File exists at `src/components/admin/product-search-input.test.tsx`
    - Running `npx vitest run src/components/admin/product-search-input.test.tsx` exits non-zero (RED — component does not exist yet)
    - The failure reason is an import/module-not-found error for `./product-search-input`, NOT a syntax error in the test file itself
    - All five `it(...)` blocks are present: render placeholder, debounce router.replace, clear q, page reset, prop sync
    - The file begins with `/** @vitest-environment jsdom */`
    - `vi.mock("next/navigation", ...)` returns `{ replace: vi.fn() }` (not `refresh`)
  </acceptance_criteria>

  <done>product-search-input.test.tsx exists, all 5 tests are syntactically valid, file fails at import with "Cannot find module './product-search-input'"</done>
</task>

<task type="auto">
  <name>Task 2: Extend buildAdminProductWhere tests with q-filter case</name>
  <files>src/server/services/admin-product.service.test.ts</files>

  <read_first>
    - src/server/services/admin-product.service.test.ts — read the ENTIRE existing file to see current test cases and ensure new cases append without duplication
    - src/server/services/admin-product.service.ts — read buildAdminProductWhere to confirm: q ignored when length < 2, OR clause on title/brand/description when length >= 2
  </read_first>

  <behavior>
    - Test A: `buildAdminProductWhere({ q: "S" })` → returned object has no `OR` property (single-char guard)
    - Test B: `buildAdminProductWhere({ q: "Samsung" })` → `where.OR` is an array with exactly 3 items: `{ title: { contains: "Samsung", mode: "insensitive" } }`, `{ brand: { contains: "Samsung", mode: "insensitive" } }`, `{ description: { contains: "Samsung", mode: "insensitive" } }`
    - Test C: `buildAdminProductWhere({ q: "  " })` (whitespace only) → no `OR` property (trim guard)
  </behavior>

  <action>
    Append three new `it(...)` test cases inside the existing `describe("buildAdminProductWhere", ...)` block in `src/server/services/admin-product.service.test.ts`. Do not modify any existing tests. Add after the last existing `it(...)`. The three new cases implement tests A, B, and C from `<behavior>` above. Use `expect(where).not.toHaveProperty("OR")` for cases A and C. For case B, assert `expect(where.OR).toHaveLength(3)` and check the first element with `expect(where.OR?.[0]).toEqual({ title: { contains: "Samsung", mode: "insensitive" } })`.
  </action>

  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npx vitest run src/server/services/admin-product.service.test.ts 2>&1 | tail -15</automated>
  </verify>

  <acceptance_criteria>
    - All existing tests in admin-product.service.test.ts still pass (no regression)
    - Three new test cases are present inside `describe("buildAdminProductWhere", ...)`
    - All three new cases pass (GREEN — they test already-implemented backend code)
    - Test B asserts `OR` array length is 3 and checks the `title` element's exact shape
    - Tests A and C assert no `OR` property is present
  </acceptance_criteria>

  <done>admin-product.service.test.ts has 7 passing tests total (4 existing + 3 new); full describe block passes green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → codebase | Vitest imports production code under jsdom; no user-controlled input in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; all dependencies already in node_modules |
</threat_model>

<verification>
After both tasks:
- `npx vitest run src/components/admin/product-search-input.test.tsx` — exits non-zero (RED, import fails — expected)
- `npx vitest run src/server/services/admin-product.service.test.ts` — exits zero, 7 tests pass
- `npx vitest run` — full suite green except product-search-input.test.tsx (which correctly fails)
</verification>

<success_criteria>
- product-search-input.test.tsx: 5 well-formed tests that fail at import (RED) — correct Wave 0 state
- admin-product.service.test.ts: 3 new q-filter cases, all green
- Zero syntax errors in either file
</success_criteria>

<output>
Create `.planning/phases/53-admin-product-search/53-01-SUMMARY.md` when done
</output>
