---
phase: 21-bugfix-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/21-bugfix-stabilization/21-MANUAL-CHECKLIST.md
  - .planning/todos/pending/bugfix-intake-2026-05-19.md
  - .planning/STATE.md
autonomous: false
requirements:
  - BUGFIX-INTAKE-2026-05-19
must_haves:
  truths:
    - "npm run build exits 0 on main after waves 1–2 bugfixes"
    - "npm test exits 0; inventory and admin-order transition tests pass"
    - "BUG-12…17 surfaces pass 21-MANUAL-CHECKLIST.md without blocker/major failures"
    - "bugfix-intake-2026-05-19.md marked completed when verify clean; no code churn unless regression found"
    - "Operator goal met: main stays stable; fixes are docs-only or minimal regression patches"
  artifacts:
    - path: .planning/phases/21-bugfix-stabilization/21-MANUAL-CHECKLIST.md
      provides: Consolidated manual verify for BUG-12…17
    - path: .planning/todos/pending/bugfix-intake-2026-05-19.md
      provides: Intake status completed after verify
  key_links:
    - from: 21-01-PLAN.md
      to: src/server/services/product-inventory.ts
      via: automated vitest + optional manual admin order flow
      pattern: "shouldReserveInventoryOnTransition"
    - from: 21-MANUAL-CHECKLIST.md
      to: src/app/(admin)/admin/kategorii
      via: operator manual pass
      pattern: "sortOrder"
---

<objective>
Verify-only close for Phase 21: confirm waves 1–2 operator bugs (BUG-12…17) remain correct on `main` after v1.3 phases 17–20 and prior bugfix commits. No re-implementation of shipped fixes (D-21-03, D-21-04).

Purpose: Defensive regression gate before marking v1.4 stabilization intake complete.
Output: Green CI, completed manual checklist, intake `status: completed` (or `21-02-PLAN` only if verify finds a blocker).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/BUGFIX-WORKFLOW.md
@.planning/phases/21-bugfix-stabilization/21-CONTEXT.md
@.planning/todos/pending/bugfix-intake-2026-05-19.md
@.planning/todos/completed/bugfix-v1.4-intake.md
@src/server/services/product-inventory.ts
@src/server/services/product-inventory.test.ts
@src/server/services/admin-order.service.ts
@src/server/services/admin-order.service.test.ts
@src/server/services/admin-product.service.test.ts
@src/server/services/catalog.service.test.ts
@src/app/(admin)/admin/kategorii/[id]/page.tsx
@src/components/admin/category-form.tsx
@src/components/admin/admin-categories-table.tsx
@src/components/admin/product-form.tsx
@package.json
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verify-only scope | Executor must not refactor inventory/category/product code unless a test or checklist step fails |
| Intake closure | Marking intake `completed` without manual checklist is a process failure (false green) |
| Regression fix | If code changes are required, limit diff to failing surface; document in 21-01-SUMMARY.md |

## Threats

| ID | Threat | Severity | Mitigation in plan |
|----|--------|----------|-------------------|
| T1 | Re-implementing BUG-12…17 and reintroducing regressions | high | D-21-03: verify-only tasks; no feature tasks |
| T2 | Skipping manual admin UI while unit tests pass | medium | Task 3 requires 21-MANUAL-CHECKLIST.md |
| T3 | Checkout still decrements qty (BUG-15 regression) | high | Vitest matrix + manual order confirm/cancel in checklist |

No new auth or data-exposure surface in this plan.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Automated verify — build and targeted Vitest</name>
  <read_first>
- .planning/phases/21-bugfix-stabilization/21-CONTEXT.md (D-21-06, D-21-08…D-21-11)
- package.json (scripts: build, test)
- src/server/services/product-inventory.test.ts
- src/server/services/admin-order.service.test.ts
  </read_first>
  <files>package.json</files>
  <action>
From repo root:

1. Run `npm run build` — must exit 0.
2. Run `npm test` — must exit 0.
3. If full suite is slow or flaky, re-run narrowed scope and record command in summary:
   - `npx vitest run src/server/services/product-inventory.test.ts`
   - `npx vitest run src/server/services/admin-order.service.test.ts`
   - `npx vitest run src/server/services/admin-product.service.test.ts`
   - `npx vitest run src/server/services/catalog.service.test.ts`

Do not modify production code in this task. If any command fails, stop and open blocker path (Task 4 note): minimal fix only for the failing assertion, then re-run failed commands.
  </action>
  <verify>
    <automated>npm run build && npm test</automated>
  </verify>
  <acceptance_criteria>
- `npm run build` exit code 0
- `npm test` exit code 0 OR documented subset with all four inventory/admin/catalog test files passing
- `product-inventory.test.ts` contains passing tests for `PENDING → CONFIRMED` reserve and `CONFIRMED → CANCELLED` release
- `product-inventory.test.ts` `reserveProductUnitsForOrder` uses `decrement: 2` for quantity 2 line (multi-qty, D-21-11)
- No commits that change `src/` unless a test failure forced Task 4 regression fix
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Static verify — shipped implementations match intake contracts</name>
  <read_first>
- .planning/todos/pending/bugfix-intake-2026-05-19.md (BUG-12…17 expected behavior)
- src/server/services/product-inventory.ts (shouldReserveInventoryOnTransition, shouldReleaseInventoryOnTransition)
- src/app/(admin)/admin/kategorii/[id]/page.tsx (Переглянути товари link)
- src/components/admin/product-form.tsx (quantity field; no ProductStatus enum in form)
- prisma/schema.prisma (Product.quantity; no ProductStatus enum if removed)
  </read_first>
  <files>
    src/server/services/product-inventory.ts
    src/app/(admin)/admin/kategorii/[id]/page.tsx
    src/components/admin/product-form.tsx
    prisma/schema.prisma
  </files>
  <action>
Read-only audit (grep/read). Confirm without editing:

1. **BUG-14/16:** No `ProductStatus` enum in Prisma `Product` model; admin product list has no status column (grep `ProductStatus`, `ProductListStatusSelect` in `src/components/admin` — expect no list status select).
2. **BUG-15:** `product-inventory.ts` reserves only on `PENDING` → `CONFIRMED`; releases on cancel from `CONFIRMED` / `READY_FOR_PICKUP` / `OUT_FOR_DELIVERY`; not on `PENDING` → `CANCELLED`.
3. **BUG-13:** Category edit page link href is `/admin/tovary?categoryId=${category.id}` with label «Переглянути товари».
4. **BUG-12:** Category sort uses 1-based validation in server/validator (grep `sortOrder` min 1 in admin category validators or service — document file:line in summary if found).

If static audit contradicts intake, treat as verify failure → Task 4.
  </action>
  <verify>
    <automated>! grep -q 'enum ProductStatus' prisma/schema.prisma 2>/dev/null; grep -q 'Переглянути товари' src/app/\(admin\)/admin/kategorii/\[id\]/page.tsx; grep -q 'shouldReserveInventoryOnTransition' src/server/services/product-inventory.ts; grep -q 'PENDING.*CONFIRMED' src/server/services/product-inventory.test.ts</automated>
  </verify>
  <acceptance_criteria>
- Grep `ProductStatus` in `prisma/schema.prisma` returns no enum definition on Product (or documents intentional remnant with failure)
- `src/app/(admin)/admin/kategorii/[id]/page.tsx` includes string `Переглянути товари` and `categoryId=${category.id}` in Link href
- `shouldReserveInventoryOnTransition("PENDING", "CONFIRMED")` is true in source; `shouldReleaseInventoryOnTransition("PENDING", "CANCELLED")` is false in source
- Audit notes recorded in plan execution summary (for 21-01-SUMMARY.md)
  </acceptance_criteria>
</task>

<task type="checkpoint">
  <name>Task 3: Manual verify — 21-MANUAL-CHECKLIST.md</name>
  <read_first>
- .planning/phases/21-bugfix-stabilization/21-MANUAL-CHECKLIST.md
- .planning/phases/21-bugfix-stabilization/21-CONTEXT.md (D-21-05, D-21-07)
- .planning/BUGFIX-WORKFLOW.md §6
  </read_first>
  <files>
    .planning/phases/21-bugfix-stabilization/21-MANUAL-CHECKLIST.md
    .planning/todos/pending/bugfix-intake-2026-05-19.md
    .planning/STATE.md
  </files>
  <action>
1. Ensure `npm run dev` is running and admin user can sign in.
2. Execute every checkbox in `21-MANUAL-CHECKLIST.md` for BUG-12…17.
3. Mark checklist sign-off table with date and Pass/Fail.
4. If any blocker/major row fails, do not close intake; document failure and proceed to Task 4 (or spawn 21-02-PLAN per D-21-04).

If all pass: update `.planning/todos/pending/bugfix-intake-2026-05-19.md` frontmatter `status: completed` and move file to `.planning/todos/completed/bugfix-intake-2026-05-19.md` (or set completed in place per project convention).
  </action>
  <verify>
    <automated>test -f .planning/phases/21-bugfix-stabilization/21-MANUAL-CHECKLIST.md && grep -q 'Sign-off' .planning/phases/21-bugfix-stabilization/21-MANUAL-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
- Every section in `21-MANUAL-CHECKLIST.md` has checked boxes or documented skip with reason
- Sign-off table filled with Pass when phase closes
- Intake file `status: completed` when manual + automated verify green
- `.planning/STATE.md` updated: phase 21 ready for execute summary / verify-work handoff
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Regression fix path (conditional — only if Tasks 1–3 fail)</name>
  <read_first>
- Failing test output or checklist row from Tasks 1–3
- .planning/phases/21-bugfix-stabilization/21-CONTEXT.md (D-21-04 minimal fix)
- Files implicated by failure only
  </read_first>
  <files>src/</files>
  <action>
**Skip entirely if Tasks 1–3 pass.**

If failure:
1. Create minimal patch for the specific regression (one logical commit).
2. Re-run failed `npm test` / `npm run build` commands.
3. Re-run only failed manual checklist sections.
4. Write `21-02-PLAN.md` only if fix scope exceeds one commit or touches unrelated surfaces (planner discretion per D-21-04).

Do not re-implement entire BUG waves from scratch.
  </action>
  <verify>
    <automated>npm run build && npm test</automated>
  </verify>
  <acceptance_criteria>
- If skipped: no `src/` changes in phase 21 execute
- If executed: `npm run build` and affected tests exit 0 after fix
- `21-01-SUMMARY.md` lists files changed and maps fix to BUG-ID
- No new features; diff limited to regression
  </acceptance_criteria>
</task>

</tasks>

<verification>
## Phase-level verification (after plan 01)

| Check | Command / artifact | Pass |
|-------|-------------------|------|
| Build | `npm run build` | exit 0 |
| Tests | `npm test` | exit 0 |
| Manual | `21-MANUAL-CHECKLIST.md` sign-off Pass | all BUG-12…17 |
| Intake | `bugfix-intake-2026-05-19.md` | status completed |
| Code churn | git diff `src/` | empty unless Task 4 |

## Goal-backward (ROADMAP Phase 21)

1. Intake wave 1–2 rows remain `done` and behavior verified — checklist + tests
2. `21-01-PLAN.md` executed — this plan
3. CI green — build + test
4. Manual checklist for changed surfaces — `21-MANUAL-CHECKLIST.md`
</verification>

<risks>
- Operator reports «works on my machine» but CI fails in clean env → always run build/test locally before close
- Skipping guest checkout path in manual pass may miss BUG-15 on guest orders (D-21-10) — checklist includes guest order confirm
</risks>

<dependencies>
- Phases 17–20 shipped on main
- BUG-01…11 from `bugfix-v1.4-intake.md` already done (out of scope except smoke)
</dependencies>
