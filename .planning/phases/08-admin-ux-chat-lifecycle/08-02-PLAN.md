---
phase: 08-admin-ux-chat-lifecycle
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/server/validators/admin-order.ts
  - src/server/validators/admin-order.test.ts
  - src/server/services/admin-order.service.ts
  - src/server/services/admin-order.service.test.ts
  - src/lib/admin/orders-url.ts
  - src/lib/admin/orders-url.test.ts
autonomous: true
requirements:
  - ADM-02
context_decisions:
  - "D-08-08: listOrdersAdminPaginated + countOrdersAdmin"
  - "D-08-09: URL params filter, page, pageSize, sort, dir"
  - "D-08-10: filter/pageSize change resets page to 1"
  - "D-08-26: Vitest paginated orders helper"
  - "Plain searchParams not nuqs per RESEARCH A4"

must_haves:
  truths:
    - "Orders list returns one page with correct total count"
    - "Invalid sort/dir/pageSize values clamped or rejected by Zod"
    - "totalKopiyky sort orders by computed sum across items"
  artifacts:
    - path: src/server/validators/admin-order.ts
      provides: listOrdersAdminSchema
    - path: src/server/services/admin-order.service.ts
      provides: listOrdersAdminPaginated, countOrdersAdmin
    - path: src/lib/admin/orders-url.ts
      provides: adminOrdersUrl helper for Link navigation
  key_links:
    - from: listOrdersAdminPaginated
      to: prisma.order.findMany
      via: skip/take + orderBy
      pattern: "skip.*take"

---

<objective>
Backend for paginated, sortable admin orders (ADM-02 foundation): Zod URL contract, paginated service mirroring listAdminProducts, URL builder for plain searchParams links.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-admin-ux-chat-lifecycle/08-RESEARCH.md — Pattern 2, Pitfall 1 totalKopiyky
@src/server/services/admin-product.service.ts — pagination pattern
@src/server/validators/admin-product.ts — listAdminProductsSchema pattern
@src/server/services/admin-order.service.ts
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| list params | Untrusted searchParams → Zod whitelist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Tampering | sort/dir/pageSize | mitigate | Zod enums: sort in createdAt, totalKopiyky, orderNumber, status; dir asc|desc; pageSize 10|20|50 |
| T-08-02-02 | DoS | page/pageSize | mitigate | page max 1000; pageSize enum only |
| T-08-02-03 | Information disclosure | filter | accept | Same status groups as existing listAllOrders; admin-only caller |

</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: listOrdersAdminSchema + adminOrdersUrl (D-08-09, D-08-10)</name>
  <read_first>
    - src/server/validators/admin-product.ts
    - src/server/validators/admin-product.test.ts
    - src/app/(admin)/admin/zamovlennia/page.tsx — existing filter parsing
  </read_first>
  <files>
    src/server/validators/admin-order.ts,
    src/server/validators/admin-order.test.ts,
    src/lib/admin/orders-url.ts,
    src/lib/admin/orders-url.test.ts
  </files>
  <behavior>
    - Default parse {} → filter all, page 1, pageSize 20, sort createdAt, dir desc
    - pageSize 99 → fails or coerces to allowed enum per schema design
    - adminOrdersUrl({ filter: new, pageSize: 50 }) omits page when 1; includes filter=new, pageSize=50
    - adminOrdersUrl on filter change should use page 1 (caller passes page: 1)
  </behavior>
  <action>
    Create `listOrdersAdminSchema` with: filter (same union as AdminOrderListFilter), page coerce min 1 default 1, pageSize enum 10|20|50 default 20, sort enum createdAt|totalKopiyky|orderNumber|status default createdAt, dir asc|desc default desc.
    Export `ListOrdersAdminParams` type.
    Add `adminOrdersUrl()` in `src/lib/admin/orders-url.ts` building `/admin/zamovlennia?...` omitting default query keys (page 1, pageSize 20, sort createdAt, dir desc, filter all).
    Vitest both files per D-08-26.
    Use plain URLSearchParams — not nuqs (RESEARCH A4).
  </action>
  <verify>
    <automated>npm test -- src/server/validators/admin-order.test.ts src/lib/admin/orders-url.test.ts</automated>
  </verify>
  <done>Schema + URL helper tested; defaults match D-08-09</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: listOrdersAdminPaginated + countOrdersAdmin (D-08-08)</name>
  <read_first>
    - src/server/services/admin-order.service.ts — buildOrderWhere via FILTER_STATUS_MAP, mapOrderSummary
    - .planning/phases/08-admin-ux-chat-lifecycle/08-RESEARCH.md — Pitfall 1 totalKopiyky raw SQL
  </read_first>
  <files>src/server/services/admin-order.service.ts, src/server/services/admin-order.service.test.ts</files>
  <behavior>
    - page 1 pageSize 10 with 25 matching orders → returns 10 items, total 25, totalPages 3
    - sort orderNumber asc changes orderBy field
    - sort totalKopiyky uses aggregate ordering (not wrong client sort)
  </behavior>
  <action>
    Add `countOrdersAdmin(filter)` and `listOrdersAdminPaginated(params: ListOrdersAdminParams)` returning `{ items, total, page, pageSize, totalPages }`.
    Reuse existing filter → status where logic from listAllOrders.
    orderBy: createdAt, orderNumber, status via Prisma orderBy.
    For sort=totalKopiyky: use Prisma $queryRaw to select order ids ordered by SUM(priceSnapshot * quantity) then findMany with id in ordered ids (preserve order) — per RESEARCH Pitfall 1; do NOT skip this sort column (D-08-11).
    Keep listAllOrders for dashboard recent orders; do not break getAdminDashboardStats.
    Add Vitest with prisma mock or extracted pure helpers following chat.service.test.ts / admin-product.test.ts style.
  </action>
  <verify>
    <automated>npm test -- src/server/services/admin-order.service.test.ts</automated>
  </verify>
  <done>Paginated service + count exported; totalKopiyky sort implemented; tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Export types and document listAllOrders deprecation path</name>
  <read_first>
    - src/app/(admin)/admin/zamovlennia/page.tsx — still uses listAllOrders until 08-03
  </read_first>
  <files>src/server/services/admin-order.service.ts</files>
  <action>
    Export `AdminOrderListSort` and `AdminOrderListDir` types aligned with schema enums.
    Ensure mapOrderSummary still used for paginated rows.
    No change to zamovlennia page in this plan (08-03 wires UI).
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npm test -- src/server/services/admin-order.service.test.ts</automated>
  </verify>
  <done>Public types stable for RSC page in plan 08-03</done>
</task>

</tasks>

<verification>
```bash
npm test -- src/server/validators/admin-order.test.ts src/lib/admin/orders-url.test.ts src/server/services/admin-order.service.test.ts
npm run lint
```
</verification>

<success_criteria>
- Paginated API ready for RSC page
- All ADM-02 backend requirements addressable in 08-03
- D-08-26 partial: order service tests green
</success_criteria>

<output>
Create `.planning/phases/08-admin-ux-chat-lifecycle/08-02-SUMMARY.md` when done
</output>
