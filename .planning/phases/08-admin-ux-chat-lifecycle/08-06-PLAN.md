---
phase: 08-admin-ux-chat-lifecycle
plan: 06
type: execute
wave: 5
depends_on:
  - 08-05
files_modified:
  - src/app/(admin)/admin/chaty/page.tsx
  - src/components/chat/admin-chat-inbox.tsx
  - src/components/chat/chat-thread.tsx
  - src/components/chat/admin-chat-provider.tsx
  - src/components/chat/conversation-list.tsx
  - e2e/admin-chat.spec.ts
autonomous: true
requirements:
  - CHAT-05
  - CHAT-06
context_decisions:
  - "D-08-18: view=active|archive tabs on /admin/chaty"
  - "D-08-19: thread header DropdownMenu + AlertDialog delete"
  - "D-08-20: UA delete confirm; clear selection after delete"
  - "D-08-22: router.refresh or refetch after lifecycle"
  - "D-08-27: e2e only if base flow breaks"

must_haves:
  truths:
    - "Admin sees Active and Archive tabs filtering conversations"
    - "Admin can archive, unarchive, and permanently delete with confirmation"
    - "Deleted chat disappears from DB and UI selection clears"
    - "Archived chat leaves active tab and appears in archive tab"
  artifacts:
    - path: src/app/(admin)/admin/chaty/page.tsx
      provides: view param → status filter
    - path: src/components/chat/chat-thread.tsx
      provides: lifecycle dropdown + delete dialog
  key_links:
    - from: chat-thread.tsx
      to: admin/chat.actions.ts
      via: server actions
      pattern: "archiveConversationAction|deleteConversationAction"
    - from: admin/chaty/page.tsx
      to: listConversationsForAdmin
      via: status from view param
      pattern: "listConversationsForAdmin"

---

<objective>
Admin chat UI: Active/Archive tabs, thread lifecycle actions with Ukrainian confirm copy, list refresh after mutations (CHAT-05, CHAT-06 admin UX).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-admin-ux-chat-lifecycle/08-UI-SPEC.md — Copywriting Contract delete/archive
@src/components/chat/admin-chat-inbox.tsx
@src/components/chat/chat-thread.tsx
@src/server/actions/admin/chat.actions.ts
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin UI | Actions call server actions only — no direct prisma from client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06-01 | Elevation | thread actions | mitigate | Client calls server actions that requireAdmin |
| T-08-06-02 | Tampering | delete confirm | mitigate | AlertDialog blocks accidental delete; destructive variant |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: chaty page view=active|archive + filtered fetch (D-08-18)</name>
  <read_first>
    - src/app/(admin)/admin/chaty/page.tsx
    - .planning/phases/08-admin-ux-chat-lifecycle/08-RESEARCH.md — chat page snippet
  </read_first>
  <files>src/app/(admin)/admin/chaty/page.tsx</files>
  <action>
    Parse searchParams Promise for `view` (plain, not nuqs): default active → OPEN; archive → ARCHIVED.
    Call listConversationsForAdmin({ status }) per D-08-18.
    Pass view and conversations to AdminChatInbox/AdminChatProvider.
    No separate /admin/chaty/arkhiv route (deferred).
  </action>
  <verify>
    <automated>grep -q 'listConversationsForAdmin' src/app/\(admin\)/admin/chaty/page.tsx &amp;&amp; grep -qE 'view|ARCHIVED|OPEN' src/app/\(admin\)/admin/chaty/page.tsx &amp;&amp; npm run lint</automated>
  </verify>
  <done>Page loads correct status bucket from URL tab</done>
</task>

<task type="auto">
  <name>Task 2: Admin tabs + inbox wiring (D-08-18, D-08-22)</name>
  <read_first>
    - src/components/chat/admin-chat-inbox.tsx
    - src/components/ui/tabs.tsx
    - .planning/phases/08-admin-ux-chat-lifecycle/08-UI-SPEC.md — tab labels Активні / Архів
  </read_first>
  <files>
    src/components/chat/admin-chat-inbox.tsx,
    src/components/chat/conversation-list.tsx,
    src/components/chat/admin-chat-provider.tsx
  </files>
  <action>
    Add shadcn Tabs at top of inbox: «Активні» → Link `?view=active` (or bare URL), «Архів» → `?view=archive` — preserve conversationId query if present.
    Pass conversation.status into provider state; after lifecycle action call router.refresh() and/or refetch conversations in AdminChatProvider (D-08-22).
    Optional: hide unread styling in archive tab for archived rows.
  </action>
  <verify>
    <automated>grep -qE 'Активні|Архів' src/components/chat/admin-chat-inbox.tsx &amp;&amp; npm run lint</automated>
  </verify>
  <done>Tabs switch view param; list updates after refresh</done>
</task>

<task type="auto">
  <name>Task 3: Thread lifecycle menu + delete AlertDialog (D-08-19, D-08-20)</name>
  <read_first>
    - src/components/chat/chat-thread.tsx
    - src/components/ui/dropdown-menu.tsx, alert-dialog.tsx
    - 08-UI-SPEC.md — delete confirm UA copy
  </read_first>
  <files>src/components/chat/chat-thread.tsx</files>
  <action>
    In thread header when conversation selected: DropdownMenu with:
    - «Архівувати» when status OPEN → archiveConversationAction
    - «Повернути з архіву» when ARCHIVED → unarchiveConversationAction
    - «Видалити назавжди» → AlertDialog with UA title/description per 08-UI-SPEC; confirm calls deleteConversationAction (D-08-16 hard delete).
    After delete: clear selected conversationId from URL/state (D-08-20).
    Disable/hide AdminChatComposer when status ARCHIVED (RESEARCH Pitfall 4 — admin symmetry).
    On success: toast + router.refresh().
  </action>
  <verify>
    <automated>grep -q deleteConversationAction src/components/chat/chat-thread.tsx &amp;&amp; grep -q AlertDialog src/components/chat/chat-thread.tsx &amp;&amp; npm run lint</automated>
  </verify>
  <done>All three actions work from thread header; delete requires confirm</done>
</task>

</tasks>

<verification>
```bash
npm run lint
npm run test:e2e -- e2e/admin-chat.spec.ts
```
Manual checklist (archive/delete) deferred to 08-07; run e2e only — update selectors per D-08-27 if ADM-01 sidebar breaks link name match.
</verification>

<success_criteria>
- Roadmap criteria 5–6 admin paths demonstrable in browser
- Active vs archive lists correct
</success_criteria>

<output>
Create `.planning/phases/08-admin-ux-chat-lifecycle/08-06-SUMMARY.md` when done
</output>
