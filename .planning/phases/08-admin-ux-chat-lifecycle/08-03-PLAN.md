---
phase: 08-admin-ux-chat-lifecycle
plan: 03
type: execute
wave: 3
depends_on:
  - 08-02
files_modified:
  - src/app/(admin)/admin/zamovlennia/page.tsx
  - src/components/admin/orders-data-table.tsx
  - src/components/admin/order-list-filters.tsx
  - src/components/admin/orders-table.tsx
autonomous: true
requirements:
  - ADM-02
context_decisions:
  - "D-08-11: sortable columns createdAt, totalKopiyky, orderNumber, status"
  - "D-08-12: OrderListFilters above table; pagination below"
  - "D-08-13: RSC fetch one page; Link-based sort/pagination"
  - "D-08-07: TanStack manual pagination/sorting"

must_haves:
  truths:
    - "/admin/zamovlennia shows one page of orders with pagination controls"
    - "User can set page size 10/20/50 via URL"
    - "Column header click toggles sort/dir in URL"
    - "Changing filter or page size resets to page 1"
  artifacts:
    - path: src/components/admin/orders-data-table.tsx
      provides: Data Table UI with Link navigation
    - path: src/app/(admin)/admin/zamovlennia/page.tsx
      provides: searchParams → paginated fetch
  key_links:
    - from: zamovlennia/page.tsx
      to: listOrdersAdminPaginated
      via: listOrdersAdminSchema.parse(searchParams)
      pattern: "listOrdersAdminPaginated"
    - from: orders-data-table.tsx
      to: adminOrdersUrl
      via: Link href on headers and pagination
      pattern: "adminOrdersUrl"

---

<objective>
Wire orders admin page to paginated backend with shadcn Data Table pattern: URL-driven sort/page/filter, minimal client JS via Link navigation (ADM-02 UI).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-admin-ux-chat-lifecycle/08-UI-SPEC.md — Orders table copy, sort icons
@src/components/admin/orders-table.tsx — column baseline
@src/components/admin/order-list-filters.tsx
@src/lib/admin/orders-url.ts
@src/server/validators/admin-order.ts
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC page | Parses searchParams only through Zod schema from 08-02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Tampering | zamovlennia searchParams | mitigate | listOrdersAdminSchema.parse before DB call |
| T-08-03-02 | Information disclosure | orders table | accept | Page already requireAdmin via layout |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: RSC zamovlennia page — parse searchParams and fetch page (D-08-09, D-08-13)</name>
  <read_first>
    - src/app/(admin)/admin/tovary/page.tsx — searchParams Promise pattern
    - src/app/(admin)/admin/zamovlennia/page.tsx
    - node_modules/next/dist/docs/ — searchParams in Server Components
  </read_first>
  <files>src/app/(admin)/admin/zamovlennia/page.tsx</files>
  <action>
    Extend PageProps searchParams: filter, page, pageSize, sort, dir (all optional strings).
    Parse with listOrdersAdminSchema from raw searchParams object.
    Fetch listOrdersAdminPaginated(parsed) in parallel with count if needed (total in paginated result).
    Pass parsed params + result to OrdersDataTable.
    Keep empty states: filter=all and total=0 → roadmap copy; filtered empty → «Нічого не знайдено» (08-UI-SPEC).
  </action>
  <verify>
    <automated>grep -q listOrdersAdminPaginated src/app/\(admin\)/admin/zamovlennia/page.tsx &amp;&amp; grep -q listOrdersAdminSchema src/app/\(admin\)/admin/zamovlennia/page.tsx &amp;&amp; npm run lint</automated>
  </verify>
  <done>Page loads single paginated slice; no listAllOrders on main table path</done>
</task>

<task type="auto">
  <name>Task 2: OrdersDataTable with TanStack + Link sort/pagination (D-08-07, D-08-11)</name>
  <read_first>
    - src/components/admin/orders-table.tsx
    - .planning/phases/08-admin-ux-chat-lifecycle/08-RESEARCH.md — Pattern 3 manual pagination
    - src/components/ui/table.tsx, pagination.tsx
  </read_first>
  <files>src/components/admin/orders-data-table.tsx</files>
  <action>
    Create client `OrdersDataTable` using @tanstack/react-table: getCoreRowModel only; manualPagination and manualSorting true; pageCount from total/pageSize.
    Columns: orderNumber, createdAt, customer (name+phone), delivery, totalKopiyky (tabular-nums), status badge, «Відкрити» link — match existing orders-table content.
    Sortable headers (D-08-11): createdAt, totalKopiyky, orderNumber, status — use Link + adminOrdersUrl toggling sort/dir (ArrowUpDown icons per 08-UI-SPEC).
    Footer: shadcn Pagination as Links (prev/next/page numbers), Select for pageSize 10|20|50 linking with page=1 on change (D-08-10).
    Ukrainian labels per 08-UI-SPEC.
  </action>
  <verify>
    <automated>test -f src/components/admin/orders-data-table.tsx &amp;&amp; grep -q manualPagination src/components/admin/orders-data-table.tsx &amp;&amp; npm run lint</automated>
  </verify>
  <done>Table renders page; sort and pagination navigate via URL without client-only full list</done>
</task>

<task type="auto">
  <name>Task 3: OrderListFilters URL preservation + remove legacy table (D-08-10, D-08-12)</name>
  <read_first>
    - src/components/admin/order-list-filters.tsx
    - src/lib/admin/orders-url.ts
  </read_first>
  <files>
    src/components/admin/order-list-filters.tsx,
    src/app/(admin)/admin/zamovlennia/page.tsx,
    src/components/admin/orders-table.tsx
  </files>
  <action>
    Update OrderListFilters to accept current pageSize, sort, dir props; each filter Link uses adminOrdersUrl({ filter, page: 1, pageSize, sort, dir }) per D-08-10.
    Render OrderListFilters above OrdersDataTable; pagination inside data table below rows per D-08-12.
    Stop importing OrdersTable from zamovlennia page; delete orders-table.tsx only if fully replaced and no other imports — grep before delete.
  </action>
  <verify>
    <automated>grep -q adminOrdersUrl src/components/admin/order-list-filters.tsx &amp;&amp; grep -q OrdersDataTable src/app/\(admin\)/admin/zamovlennia/page.tsx &amp;&amp; npm run lint</automated>
  </verify>
  <done>Filter change resets page; layout matches UI spec; no duplicate table component usage</done>
</task>

</tasks>

<verification>
```bash
npm run lint
npm run build
```
Manual: open `/admin/zamovlennia?page=2&pageSize=10&sort=orderNumber&dir=asc` — verify URL sync and row order.
</verification>

<success_criteria>
- Roadmap ADM-02 criteria 3 satisfied (pagination, page size, sort)
- Shareable admin order URLs work after refresh
</success_criteria>

<output>
Create `.planning/phases/08-admin-ux-chat-lifecycle/08-03-SUMMARY.md` when done
</output>
