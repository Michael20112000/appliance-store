---
phase: 08-admin-ux-chat-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(admin)/admin/page.tsx
  - src/app/(admin)/admin/layout.tsx
  - src/components/admin/app-sidebar.tsx
  - src/components/admin/admin-nav.tsx
  - src/components/ui/sidebar.tsx
  - src/components/ui/table.tsx
  - src/components/ui/pagination.tsx
  - src/components/ui/tabs.tsx
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - FIX-01
  - ADM-01
context_decisions:
  - "D-08-01: StatCard Чернетки → /admin/tovary?status=DRAFT"
  - "D-08-02: shadcn sidebar + SidebarProvider"
  - "D-08-03: mobile SidebarTrigger + Sheet"
  - "D-08-04: desktop collapsible icon + SidebarRail"
  - "D-08-05: nav items unchanged from admin-nav"
  - "D-08-06: requireAdmin + countUnreadForAdmin stay in server layout"

must_haves:
  truths:
    - "Dashboard «Чернетки» opens products filtered to DRAFT"
    - "Admin uses shadcn Sidebar with mobile Sheet trigger"
    - "Desktop sidebar visible with icon collapse via SidebarRail"
    - "Nav items, unread chat badge, На сайт, Вийти work as before"
  artifacts:
    - path: src/components/admin/app-sidebar.tsx
      provides: migrated sidebar navigation
    - path: src/components/ui/sidebar.tsx
      provides: shadcn Sidebar primitives
    - path: src/app/(admin)/admin/layout.tsx
      provides: SidebarProvider shell
  key_links:
    - from: src/app/(admin)/admin/layout.tsx
      to: countUnreadForAdmin
      via: server fetch before client shell
      pattern: "countUnreadForAdmin"
    - from: src/components/admin/app-sidebar.tsx
      to: /admin/chaty
      via: nav link with badge
      pattern: "unreadChatCount"

---

<objective>
Fix dashboard drafts link (FIX-01) and replace CSS grid admin shell with shadcn Sidebar (ADM-01). Install shared UI primitives needed by later plans (table, pagination, tabs, TanStack Table).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-admin-ux-chat-lifecycle/08-CONTEXT.md
@.planning/phases/08-admin-ux-chat-lifecycle/08-RESEARCH.md
@.planning/phases/08-admin-ux-chat-lifecycle/08-UI-SPEC.md
@node_modules/next/dist/docs/
@src/app/(admin)/admin/layout.tsx
@src/components/admin/admin-nav.tsx
@src/app/(admin)/admin/page.tsx
@components.json
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin layout | Server runs requireAdmin before rendering shell |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Elevation | admin/layout.tsx | mitigate | Keep await requireAdmin() at layout entry |
| T-08-01-02 | Spoofing | signOut in sidebar | mitigate | Reuse authClient.signOut from admin-nav; no new auth path |
| T-08-01-SC | Tampering | npm install @tanstack/react-table | mitigate | Package Legitimacy Audit in 08-RESEARCH.md — Approved |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: FIX-01 — StatCard drafts href (D-08-01)</name>
  <read_first>
    - src/app/(admin)/admin/page.tsx — StatCard «Чернетки»
    - src/app/(admin)/admin/tovary/page.tsx — confirms status query parsing
  </read_first>
  <files>src/app/(admin)/admin/page.tsx</files>
  <action>
    Change only the «Чернетки» StatCard `href` from `/admin/tovary` to `/admin/tovary?status=DRAFT` per D-08-01. Do not alter other StatCards or dashboard logic.
  </action>
  <verify>
    <automated>grep -n 'status=DRAFT' src/app/(admin)/admin/page.tsx | grep -v '^#' | wc -l | awk '{exit ($1>=1)?0:1}'</automated>
  </verify>
  <done>StatCard «Чернетки» href is `/admin/tovary?status=DRAFT`</done>
</task>

<task type="auto">
  <name>Task 2: Install shadcn sidebar, table, pagination, tabs + TanStack Table</name>
  <read_first>
    - .planning/phases/08-admin-ux-chat-lifecycle/08-UI-SPEC.md — install commands
    - .planning/phases/08-admin-ux-chat-lifecycle/08-RESEARCH.md — Package Legitimacy Audit
    - components.json
  </read_first>
  <files>
    src/components/ui/sidebar.tsx,
    src/components/ui/table.tsx,
    src/components/ui/pagination.tsx,
    src/components/ui/tabs.tsx,
    package.json,
    package-lock.json
  </files>
  <action>
    Run non-interactive: `npx shadcn@latest add sidebar table pagination tabs`.
    Install dependency: `npm install @tanstack/react-table@^8.21.3` (Approved per 08-RESEARCH Package Legitimacy Audit).
    Verify exports: SidebarProvider, SidebarTrigger, SidebarInset, Table, Pagination, Tabs from ui components.
    Do not hand-roll sidebar CSS — use CLI output. globals.css already has --sidebar-* tokens; only accept CLI additions.
  </action>
  <verify>
    <automated>test -f src/components/ui/sidebar.tsx &amp;&amp; test -f src/components/ui/table.tsx &amp;&amp; npm ls @tanstack/react-table --depth=0 2>/dev/null | grep -q react-table</automated>
  </verify>
  <done>sidebar, table, pagination, tabs components exist; @tanstack/react-table in package.json</done>
</task>

<task type="auto">
  <name>Task 3: AppSidebar + SidebarProvider admin layout (D-08-02–D-08-06)</name>
  <read_first>
    - src/components/admin/admin-nav.tsx — nav items, badge, footer actions
    - .planning/phases/08-admin-ux-chat-lifecycle/08-UI-SPEC.md — Admin chrome classes
    - node_modules/next/dist/docs/ — App Router layout patterns if unsure
  </read_first>
  <files>
    src/components/admin/app-sidebar.tsx,
    src/app/(admin)/admin/layout.tsx,
    src/components/admin/admin-nav.tsx
  </files>
  <action>
    Create `src/components/admin/app-sidebar.tsx` (client): migrate nav from admin-nav into shadcn Sidebar structure per D-08-02/05:
    - Sidebar collapsible="icon" with SidebarRail on desktop (D-08-04)
    - SidebarHeader «Адмін-панель», SidebarContent menu, SidebarFooter «На сайт» + «Вийти»
    - Preserve active route logic, chat unread Badge, aria-labels, min-h-11 touch targets (08-UI-SPEC)
    Update `admin/layout.tsx` (server): keep await requireAdmin() and countUnreadForAdmin(); replace grid with SidebarProvider → AppSidebar(unreadChatCount) → SidebarInset; mobile header with SidebarTrigger (md:hidden) per D-08-03; keep Toaster and inner content card (`bg-background rounded-lg border shadow-sm`).
    Deprecate or thin admin-nav.tsx — either re-export menu items for AppSidebar or delete unused export after migration; no duplicate nav rendering.
  </action>
  <verify>
    <automated>grep -q SidebarProvider src/app/\(admin\)/admin/layout.tsx &amp;&amp; grep -q AppSidebar src/app/\(admin\)/admin/layout.tsx &amp;&amp; npm run lint</automated>
  </verify>
  <done>Admin layout uses SidebarProvider; mobile trigger opens sheet; desktop icon collapse works; requireAdmin + unread badge unchanged</done>
</task>

</tasks>

<verification>
```bash
grep -n 'status=DRAFT' src/app/(admin)/admin/page.tsx
npm run lint
npm run build
```
Spot-check: `/admin` on mobile width — SidebarTrigger opens nav; `/admin` drafts card links to filtered products.
</verification>

<success_criteria>
- FIX-01 and ADM-01 roadmap criteria 1–2 satisfied
- No regression in requireAdmin gate or chat unread badge prop
</success_criteria>

<output>
Create `.planning/phases/08-admin-ux-chat-lifecycle/08-01-SUMMARY.md` when done
</output>
