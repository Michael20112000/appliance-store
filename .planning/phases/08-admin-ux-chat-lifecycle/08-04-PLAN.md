---
phase: 08-admin-ux-chat-lifecycle
plan: 04
type: execute
wave: 3
depends_on:
  - 08-01
files_modified:
  - src/app/(admin)/admin/kategorii/page.tsx
autonomous: true
requirements:
  - ADM-03
context_decisions:
  - "D-08-14: remove Slug column from categories table only"

must_haves:
  truths:
    - "Categories admin table shows Name, product count, sort order, edit link — no Slug column"
    - "Category edit form still has slug field"
  artifacts:
    - path: src/app/(admin)/admin/kategorii/page.tsx
      provides: table without Slug header/cell
  key_links:
    - from: kategorii/[id]/page.tsx
      to: slug field
      via: unchanged edit form
      pattern: "slug:"

---

<objective>
Remove Slug column from admin categories list table (ADM-03). Slug remains editable on category detail form.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-admin-ux-chat-lifecycle/08-CONTEXT.md — D-08-14
@src/app/(admin)/admin/kategorii/page.tsx
@src/app/(admin)/admin/kategorii/[id]/page.tsx
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Presentational column removal only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Information disclosure | slug in list | accept | Slug still in edit form; list hiding is UX not security |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Remove Slug column from categories table (D-08-14)</name>
  <read_first>
    - src/app/(admin)/admin/kategorii/page.tsx
    - src/app/(admin)/admin/kategorii/[id]/page.tsx — confirm slug in form unchanged
  </read_first>
  <files>src/app/(admin)/admin/kategorii/page.tsx</files>
  <action>
    Remove `<th>Slug</th>` and matching `<td>{category.slug}</td>` from kategorii list table per D-08-14.
    Do not remove slug from Prisma, services, or edit page.
    Adjust colspan if any empty-state row uses colspan.
  </action>
  <verify>
    <automated>! grep -q '>Slug<' src/app/\(admin\)/admin/kategorii/page.tsx &amp;&amp; npm run lint</automated>
  </verify>
  <done>Table has no Slug column; edit form still exposes slug</done>
</task>

<task type="auto">
  <name>Task 2: Verify edit form slug unchanged</name>
  <read_first>
    - src/app/(admin)/admin/kategorii/[id]/page.tsx
  </read_first>
  <files>src/app/(admin)/admin/kategorii/[id]/page.tsx</files>
  <action>
    Read-only verification task: confirm category edit still passes slug to form component. If accidentally removed in prior work, restore — otherwise no code change (mark done with no diff).
  </action>
  <verify>
    <automated>grep -q slug src/app/\(admin\)/admin/kategorii/\[id\]/page.tsx</automated>
  </verify>
  <done>slug remains on category edit route</done>
</task>

</tasks>

<verification>
```bash
npm run lint
```
Visual: `/admin/kategorii` — no Slug column; edit category — slug field present.
</verification>

<success_criteria>
- Roadmap ADM-03 criterion 4 satisfied
</success_criteria>

<output>
Create `.planning/phases/08-admin-ux-chat-lifecycle/08-04-SUMMARY.md` when done
</output>
