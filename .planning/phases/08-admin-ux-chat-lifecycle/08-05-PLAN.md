---
phase: 08-admin-ux-chat-lifecycle
plan: 05
type: execute
wave: 4
depends_on:
  - 08-01
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/
  - src/server/services/chat.service.ts
  - src/server/services/chat.service.test.ts
  - src/server/actions/admin/chat.actions.ts
  - src/types/chat.ts
autonomous: false
requirements:
  - CHAT-05
  - CHAT-06
context_decisions:
  - "D-08-15: ConversationStatus OPEN | ARCHIVED + index"
  - "D-08-16: hard delete prisma.conversation.delete"
  - "D-08-17: archive = ARCHIVED, active inbox OPEN only"
  - "D-08-21: countUnreadForAdmin OPEN only"
  - "D-08-24: sendMessage rejects ARCHIVED"
  - "D-08-25: getOrCreateConversation returns same row"

must_haves:
  truths:
    - "Every conversation has status OPEN or ARCHIVED defaulting OPEN"
    - "listConversationsForAdmin filters by status"
    - "archive/unarchive/delete service functions work"
    - "sendMessage throws CHAT_ARCHIVED for archived threads"
    - "countUnreadForAdmin counts only OPEN conversations"
  artifacts:
    - path: prisma/schema.prisma
      provides: ConversationStatus enum and status field
    - path: src/server/services/chat.service.ts
      provides: lifecycle + guards
    - path: src/server/actions/admin/chat.actions.ts
      provides: requireAdmin archive/delete/unarchive
  key_links:
    - from: sendMessage
      to: conversation.status
      via: guard before create message
      pattern: "CHAT_ARCHIVED"
    - from: countUnreadForAdmin
      to: status OPEN
      via: where clause
      pattern: "status.*OPEN"

---

<objective>
Prisma ConversationStatus migration and chat service lifecycle API with server actions (CHAT-05/06 backend). Schema push/migrate before any UI wiring.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-admin-ux-chat-lifecycle/08-RESEARCH.md — Pattern 4, migration SQL
@prisma/schema.prisma
@src/server/services/chat.service.ts
@src/server/services/chat.service.test.ts
@.planning/phases/04-admin-operations/04-PATTERNS.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin actions | requireAdmin before mutate |
| sendMessage | Buyer/store must not bypass ARCHIVED guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-01 | Elevation | admin/chat.actions.ts | mitigate | requireAdmin() first line every action |
| T-08-05-02 | Tampering | sendMessage | mitigate | Check conversation.status === ARCHIVED → ChatServiceError CHAT_ARCHIVED |
| T-08-05-03 | Repudiation | deleteConversation | mitigate | Hard delete only via admin action after confirm (UI in 08-06) |
| T-08-05-04 | IDOR | conversationId | mitigate | Zod cuid() on action inputs; service verify exists |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Prisma ConversationStatus schema (D-08-15)</name>
  <read_first>
    - prisma/schema.prisma — Conversation model
    - .planning/phases/08-admin-ux-chat-lifecycle/08-RESEARCH.md — schema snippet
  </read_first>
  <files>prisma/schema.prisma</files>
  <action>
    Add enum ConversationStatus { OPEN ARCHIVED }.
    Add field `status ConversationStatus @default(OPEN)` to Conversation.
    Add @@index([status, lastMessageAt]) per D-08-15.
    Do not add deletedAt (D-08-16 hard delete).
  </action>
  <verify>
    <automated>grep -q 'enum ConversationStatus' prisma/schema.prisma &amp;&amp; grep -q 'status ConversationStatus' prisma/schema.prisma &amp;&amp; npx prisma validate</automated>
  </verify>
  <done>Schema validates with enum and index defined</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>[BLOCKING] Prisma migrate — ConversationStatus</name>
  <files>prisma/migrations/</files>
  <action>
    After Task 1 schema edit, run migration (project uses migrations folder):
    `npx prisma migrate dev --name conversation_status`
    If interactive prompt blocks automation, operator runs command locally and confirms migration applied.
    Run BEFORE downstream service changes that expect `status` column.
    Do NOT use db push if migrate dev is the project standard unless migrate fails and operator explicitly chooses push.
  </action>
  <verify>
    <automated>npx prisma migrate status 2>&1 | grep -v '^#' | grep -q 'Database schema is up to date'</automated>
  </verify>
  <done>Migration applied; prisma generate succeeds; Conversation.status exists in DB</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: chat.service lifecycle + guards (D-08-17, D-08-21, D-08-24, D-08-25)</name>
  <read_first>
    - src/server/services/chat.service.ts — listConversationsForAdmin, countUnreadForAdmin, sendMessage, getOrCreateConversation
    - src/types/chat.ts
  </read_first>
  <files>
    src/server/services/chat.service.ts,
    src/server/services/chat.service.test.ts,
    src/types/chat.ts
  </files>
  <behavior>
    - listConversationsForAdmin({ status: OPEN }) excludes ARCHIVED rows
    - archiveConversation sets ARCHIVED; unarchive sets OPEN
    - deleteConversation calls prisma.conversation.delete
    - sendMessage on ARCHIVED throws CHAT_ARCHIVED
    - getOrCreateConversation returns existing archived row (no new conversation)
    - countUnreadForAdmin only counts OPEN where buyer unread
  </behavior>
  <action>
    Extend ConversationSummaryDto with `status: ConversationStatus` in types/chat.ts.
    Update listConversationsForAdmin to accept `{ status: ConversationStatus }` filter (D-08-17).
    Add archiveConversation, unarchiveConversation, deleteConversation (hard delete D-08-16).
    Update countUnreadForAdmin where: status OPEN + existing unread logic (D-08-21).
    In sendMessage: if conversation.status === ARCHIVED throw ChatServiceError CHAT_ARCHIVED with UA message per RESEARCH (D-08-24).
    getOrCreateConversation: return existing conversation regardless of status — no fork (D-08-25).
    Extend chat.service.test.ts for all behaviors per D-08-26.
  </action>
  <verify>
    <automated>npm test -- src/server/services/chat.service.test.ts</automated>
  </verify>
  <done>Service tests pass; DTO includes status</done>
</task>

<task type="auto">
  <name>Task 3: Admin chat lifecycle server actions (D-08-16, D-08-22)</name>
  <read_first>
    - .planning/phases/04-admin-operations/04-PATTERNS.md
    - src/server/actions/chat.actions.ts — existing pattern
  </read_first>
  <files>src/server/actions/admin/chat.actions.ts</files>
  <action>
    Create `src/server/actions/admin/chat.actions.ts` with:
    - archiveConversationAction(conversationId: string)
    - unarchiveConversationAction(conversationId: string)
    - deleteConversationAction(conversationId: string)
    Each: await requireAdmin(); validate cuid; call service; revalidatePath('/admin/chaty') and revalidatePath('/', 'layout') for nav badge (D-08-22).
    Return { ok: true as const } or typed errors consistent with project.
  </action>
  <verify>
    <automated>grep -q requireAdmin src/server/actions/admin/chat.actions.ts &amp;&amp; grep -q deleteConversation src/server/actions/admin/chat.actions.ts &amp;&amp; npm run lint</automated>
  </verify>
  <done>Three admin actions exported with requireAdmin and revalidation</done>
</task>

</tasks>

<verification>
```bash
npx prisma validate
npm test -- src/server/services/chat.service.test.ts
npm run lint
```
</verification>

<success_criteria>
- DB has ConversationStatus; services and actions ready for admin/buyer UI plans
- CHAT-05/06 backend complete before 08-06/08-07
</success_criteria>

<output>
Create `.planning/phases/08-admin-ux-chat-lifecycle/08-05-SUMMARY.md` when done
</output>
