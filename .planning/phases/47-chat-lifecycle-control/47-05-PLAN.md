---
phase: 47-chat-lifecycle-control
plan: 05
type: execute
wave: 4
depends_on: [47-04]
files_modified: []
autonomous: false
requirements: [CHAT-02, CHAT-04, CHAT-05]

must_haves:
  truths:
    - "Admin closes a chat — buyer's open widget shows Чат завершено within ~1 second and composer input is locked"
    - "Guest messages are preserved after login — conversation appears in account history"
    - "Closed chat banner shows Почати новий чат button; clicking it opens a fresh active conversation"
    - "Full test suite passes (excluding 2 pre-existing seed failures)"
  artifacts:
    - path: "src/components/chat/archived-chat-banner.tsx"
      provides: "Banner with Чат завершено heading + Почати новий чат button"
      contains: "Чат завершено|Почати новий чат"
    - path: "src/components/chat/chat-provider.tsx"
      provides: "conversation:closed binding + claim effect"
      contains: "conversation:closed|claimAttemptedRef"
    - path: "src/server/services/chat.service.ts"
      provides: "claimGuestConversation + createNewConversation exported"
      contains: "claimGuestConversation|createNewConversation"
  key_links:
    - from: "Admin closes chat"
      to: "Buyer widget shows Чат завершено"
      via: "archiveConversationAction → Pusher trigger → ChatProvider conversation:closed handler → setConversationStatus(ARCHIVED)"
      pattern: "conversation:closed"
    - from: "Guest logs in"
      to: "Messages preserved in account"
      via: "ChatProvider claim effect → POST /api/chat/claim → claimGuestConversation"
      pattern: "claimGuestConversation"
---

<objective>
Human UAT verification of all Phase 47 success criteria. Confirm all three requirements work end-to-end in a live browser with real Pusher connections.

Purpose: Phase gate before marking Phase 47 complete. Automated tests cover service/action/API layers; this checkpoint covers the real-time and cross-browser behaviors that only a human can verify.
Output: Human sign-off on all three Phase 47 success criteria.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/47-chat-lifecycle-control/47-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite — confirm baseline before UAT</name>
  <files></files>
  <read_first>
    .planning/phases/47-chat-lifecycle-control/47-VALIDATION.md
  </read_first>
  <action>
    Run the full Vitest suite: `npx vitest run`. Confirm that all Phase 47 stubs created in Plan 47-01 are now GREEN. Confirm that the total failure count is at most 2 (the pre-existing prisma/seed.test.ts failures). Report the test result counts.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -15</automated>
  </verify>
  <done>
    `npx vitest run` output shows: pass count ≥ 368 + (count of new Phase 47 tests), fail count ≤ 2. All Phase 47 test IDs from VALIDATION.md (47-W0-01 through 47-03-04) are GREEN.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT — three Phase 47 success criteria</name>
  <what-built>
    - Admin closes a chat → buyer's open widget shows "Чат завершено" in real-time (CHAT-04)
    - Closed chat shows "Почати новий чат" button that opens a fresh conversation (CHAT-05)
    - Guest sends messages then registers/logs in → messages preserved in account (CHAT-02)
  </what-built>
  <how-to-verify>
    Start the dev server: `npm run dev`

    **Test 1 — CHAT-04: Real-time close notification**
    1. Open the storefront in Tab A (not logged in as admin).
    2. Click the chat FAB to open the chat widget. Send one test message (as guest or logged-in user).
    3. In Tab B, open the admin panel at /admin/chaty.
    4. Find the conversation from Tab A and click "Завершити" (archive/close action).
    5. Observe Tab A within ~1 second: the widget heading area should show "Чат завершено" and the message composer input should be locked (disabled).
    6. Expected: no page reload required; update is real-time via Pusher.

    **Test 2 — CHAT-05: Start new chat after closure**
    1. Continuing from Test 1 — the buyer widget in Tab A shows the closed banner.
    2. Verify the banner heading reads "Чат завершено" (not "Діалог закрито магазином").
    3. Click the "Почати новий чат" button.
    4. Expected: the button shows a loading/disabled state briefly, then the widget transitions to a new empty conversation with the composer input active again.
    5. Send a test message in the new conversation to confirm it's active.

    **Test 3 — CHAT-02: Guest-to-account conversation claim**
    1. Open a fresh incognito/private browser window and navigate to the storefront.
    2. Open the chat widget, send 2–3 test messages as a guest.
    3. Register a new account (or log in to an existing test account) via the auth flow.
    4. After login, navigate back to the storefront and open the chat widget.
    5. Expected: the prior guest messages are visible in the widget under the logged-in account.
    6. (Optional) Check the admin panel — the conversation should now show the user's name, not "Гість".

    Report: pass/fail for each test, and any unexpected behavior observed.
  </how-to-verify>
  <verify>
    <human-check>All three success criteria confirmed working in browser</human-check>
  </verify>
  <resume-signal>Type "approved" if all three tests pass, or describe which test(s) failed and what was observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human tester → dev server | UAT uses local dev server with real Pusher credentials from .env.local |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-UAT-01 | Information Disclosure | UAT with test accounts | accept | UAT uses test/dev data only; no real customer data involved |
| T-47-SC | Tampering | npm/pip/cargo installs | accept | No new packages; pre-audited in RESEARCH.md |
</threat_model>

<verification>
- `npx vitest run` exits with ≤ 2 failures
- Human confirms all three Phase 47 success criteria in browser
- No unexpected regressions in existing chat functionality observed during UAT
</verification>

<success_criteria>
All three ROADMAP Phase 47 success criteria are confirmed by a human tester:
1. Admin closes chat → buyer widget shows "Чат завершено" + locked composer within ~1 second
2. "Почати новий чат" button opens a fresh active conversation
3. Guest messages preserved after login — conversation appears in account history
</success_criteria>

<output>
Create `.planning/phases/47-chat-lifecycle-control/47-05-SUMMARY.md` when done.
</output>
