---
phase: 04-admin-operations
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - src/server/services/admin-catalog.service.ts
  - src/server/services/admin-catalog.service.test.ts
  - src/server/validators/category.ts
  - src/server/validators/category.test.ts
  - src/server/actions/admin/category.actions.ts
  - src/components/admin/category-form.tsx
  - src/components/admin/admin-nav.tsx
  - src/app/(admin)/admin/layout.tsx
  - src/app/(admin)/admin/kategorii/page.tsx
  - src/app/(admin)/admin/kategorii/novyi/page.tsx
  - src/app/(admin)/admin/kategorii/[id]/page.tsx
autonomous: true
requirements:
  - ADM-01
  - AUTH-04
context_decisions:
  - "D-04-01: admin-catalog.service separate from catalog.service (PUBLIC_STATUS unchanged)"
  - "D-04-03: Ukrainian routes /admin/kategorii/* (override UI-SPEC English /admin/categories)"
  - "D-04-05: requireAdmin() in every category action"
  - "Category pages as dedicated routes (novyi, [id]) not dialog-only MVP per phase slice"
  - "Slug: slugify locale uk; auto on create; stable on update unless explicit slug field (04-RESEARCH Pattern 4)"

must_haves:
  truths:
    - "Admin can list, create, edit, and delete categories at /admin/kategorii"
    - "Delete category with products is blocked with Ukrainian error"
    - "Storefront /katalog reflects category changes after save (revalidatePath)"
    - "Buyer cannot invoke category server actions (requireAdmin)"
  artifacts:
    - path: src/server/services/admin-catalog.service.ts
      provides: listCategoriesAdmin, createCategory, updateCategory, deleteCategory, uniqueSlug helper
      exports: ["listCategoriesAdmin", "createCategory", "updateCategory", "deleteCategory"]
    - path: src/server/actions/admin/category.actions.ts
      provides: createCategoryAction, updateCategoryAction, deleteCategoryAction
    - path: src/app/(admin)/admin/kategorii/page.tsx
      provides: Category list table
    - path: src/components/admin/admin-nav.tsx
      provides: Sidebar nav stub with link to kategorii
  key_links:
    - from: src/server/actions/admin/category.actions.ts
      to: src/server/services/admin-catalog.service.ts
      via: service call after Zod parse
      pattern: createCategory|updateCategory
    - from: category.actions.ts
      to: /katalog
      via: revalidatePath
      pattern: revalidatePath\("/katalog

---

## Phase Goal

**As a** адміністратор магазину, **I want to** керувати категоріями через українські URL, **so that** вітрина `/katalog` показує актуальну структуру.

<objective>
Реалізувати `admin-catalog.service`, Zod validators, server actions, і сторінки `/admin/kategorii` (+ novyi, [id]) з формою та nav stub.

Purpose: ADM-01 vertical slice без залежності від Cloudinary (wave 2 parallel з 04-01).
Output: Повний category CRUD + delete guard CATEGORY_HAS_PRODUCTS.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-admin-operations/04-RESEARCH.md
@.planning/phases/04-admin-operations/04-PATTERNS.md
@.planning/phases/04-admin-operations/04-CONTEXT.md
@.planning/phases/04-admin-operations/04-UI-SPEC.md
@src/server/services/catalog.service.ts
@src/server/actions/cart.actions.ts
@prisma/schema.prisma
@src/app/(admin)/admin/layout.tsx

<interfaces>
From prisma/schema.prisma Category:
- id, name, slug @unique, description?, sortOrder default 0, products[]

From catalog.service.ts:
- listCategories() for public — do NOT add admin writes there

From 04-PATTERNS delete guard:
- CATEGORY_HAS_PRODUCTS when products.count > 0
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: admin-catalog.service + category validators + unit tests</name>
  <read_first>
    - 04-RESEARCH.md — Pattern 4 slug, Pattern 5 delete guards
    - prisma/seed.ts — slugify uk pattern
    - src/server/services/catalog.service.test.ts — Vitest style
  </read_first>
  <files>
    src/server/services/admin-catalog.service.ts
    src/server/services/admin-catalog.service.test.ts
    src/server/validators/category.ts
    src/server/validators/category.test.ts
  </files>
  <behavior>
    - uniqueSlugFromName("Холодильники") → slug with uk transliteration
    - deleteCategory when product count > 0 throws CATEGORY_HAS_PRODUCTS
    - upsertCategorySchema rejects name shorter than 2 chars (UA message)
  </behavior>
  <action>
    Create `src/server/validators/category.ts`:
    - upsertCategorySchema: name min 2, slug optional regex, description optional, sortOrder int default 0
    - Ukrainian Zod messages: «Вкажіть назву категорії», «Латиниця, дефіси»
    Implement `admin-catalog.service.ts` (prisma only):
    - listCategoriesAdmin() — orderBy sortOrder asc, include _count products
    - getCategoryById(id)
    - createCategory(data) — auto slug from name if empty; uniqueness check
    - updateCategory({ id, ... }) — do not auto-change slug from name unless slug provided
    - deleteCategory(id) — guard CATEGORY_HAS_PRODUCTS
    Extract pure helpers `slugFromName`, `appendSlugSuffix` for tests.
    Unit tests: slug helper; delete guard logic (mock prisma or pure throw predicate).
  </action>
  <verify>
    <automated>npm test -- src/server/services/admin-catalog.service.test.ts src/server/validators/category.test.ts</automated>
  </verify>
  <done>Category service + validators tested; delete guard enforced</done>
</task>

<task type="auto">
  <name>Task 2: category.actions + revalidate helpers</name>
  <read_first>
    - 04-PATTERNS.md — Server action pattern (cart.actions analog)
  </read_first>
  <files>src/server/actions/admin/category.actions.ts</files>
  <action>
    Create `src/server/actions/admin/category.actions.ts`:
    - `"use server"`; per D-04-05 `await requireAdmin()` first in each export
    - createCategoryAction, updateCategoryAction, deleteCategoryAction
    - Parse with upsertCategorySchema; map errors to { ok: false, error: "CATEGORY_HAS_PRODUCTS" | ... }
    - revalidatePath("/admin/kategorii"), "/katalog", `/katalog/${slug}` on success
    - create → redirect(`/admin/kategorii/${id}`); handle isRedirectError
    - delete → redirect("/admin/kategorii") or return ok
    Do NOT import prisma in actions.
  </action>
  <verify>
    <automated>grep -l requireAdmin src/server/actions/admin/category.actions.ts && npx tsc --noEmit 2>/dev/null | head -3 || test -f src/server/actions/admin/category.actions.ts</automated>
  </verify>
  <done>All category mutations guarded and revalidate storefront catalog paths</done>
</task>

<task type="auto">
  <name>Task 3: Category pages + form + AdminNav stub</name>
  <read_first>
    - 04-UI-SPEC.md — copy table (Назва, Зберегти, empty states) — use Ukrainian routes per D-04-03
    - src/components/checkout/checkout-form.tsx — RHF pattern
  </read_first>
  <files>
    src/components/admin/category-form.tsx
    src/components/admin/admin-nav.tsx
    src/app/(admin)/admin/layout.tsx
    src/app/(admin)/admin/kategorii/page.tsx
    src/app/(admin)/admin/kategorii/novyi/page.tsx
    src/app/(admin)/admin/kategorii/[id]/page.tsx
  </files>
  <action>
    `category-form.tsx` (client): RHF + zodResolver(upsertCategorySchema); fields Назва, Slug (helper «Використовується в URL: /katalog/…»), Опис, Порядок; submit → actions; Alert for errors; «Зберегти» / «Збереження…»
    `admin-nav.tsx`: stub vertical nav — links Панель `/admin`, Категорії `/admin/kategorii`, placeholder disabled Товари/Замовлення (href #) until later plans; «На сайт» `/`
    Update `layout.tsx`: grid sidebar + main (bg-muted shell per UI-SPEC); render AdminNav; keep requireAdmin + robots noindex
  Pages (server components):
    - kategorii/page.tsx — table: Назва, Slug, Товарів, Порядок, link Редагувати; CTA «Додати категорію» → novyi; empty «Немає категорій»
    - kategorii/novyi/page.tsx — H1 «Нова категорія», CategoryForm
    - kategorii/[id]/page.tsx — load category, notFound(), H1 «Редагувати категорію», CategoryForm defaultValues
    Use shadcn Table/Button/Input if already in project; else minimal Tailwind table matching admin density (text-sm, py-2).
  </action>
  <verify>
    <automated>test -f src/app/\(admin\)/admin/kategorii/page.tsx && test -f src/components/admin/category-form.tsx</automated>
  </verify>
  <done>Admin can navigate kategorii list/new/edit; layout shows AdminNav stub</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Actions → admin-catalog.service | Only after requireAdmin |
| admin-catalog → Postgres | Slug uniqueness, delete guards |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Elevation | category.actions | mitigate | requireAdmin every action; AUTH-04 |
| T-04-02-02 | Tampering | upsertCategorySchema | mitigate | Zod allowlist fields |
| T-04-02-03 | Denial of Service | deleteCategory | mitigate | CATEGORY_HAS_PRODUCTS guard |

</threat_model>

<verification>
npm test -- src/server/services/admin-catalog.service.test.ts src/server/validators/category.test.ts
npm run build
</verification>

<success_criteria>
Category CRUD live at /admin/kategorii; delete blocked when products exist; catalog paths revalidated
</success_criteria>

<output>
Create `.planning/phases/04-admin-operations/04-02-SUMMARY.md` when done
</output>
