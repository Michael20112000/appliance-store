---
phase: 04-admin-operations
plan: 03
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - src/server/services/admin-product.service.ts
  - src/server/services/admin-product.service.test.ts
  - src/server/validators/admin-product.ts
  - src/server/validators/admin-product.test.ts
  - src/server/actions/admin/product.actions.ts
  - src/components/admin/product-form.tsx
  - src/components/admin/product-image-upload.tsx
  - src/app/(admin)/admin/tovary/page.tsx
  - src/app/(admin)/admin/tovary/novyi/page.tsx
  - src/app/(admin)/admin/tovary/[id]/page.tsx
  - src/components/admin/admin-nav.tsx
autonomous: true
requirements:
  - ADM-02
  - ADM-03
context_decisions:
  - "D-04-01: admin-product.service — no PUBLIC_STATUS filter on admin list"
  - "D-04-02: CldUploadWidget signatureEndpoint=/api/upload/sign (depends 04-01)"
  - "D-04-03: routes /admin/tovary/*"
  - "Two-phase save: create DRAFT → upload images → attach ProductImage → AVAILABLE when ready (04-RESEARCH Pattern 2)"
  - "Price: UAH in form, kopiyky in DB; never set SOLD from admin form (orders only)"
  - "Slug stable on title edit unless admin edits slug field (D-04-05 / A5)"
  - "revalidatePath /katalog, /tovar/[slug], / on publish"

must_haves:
  truths:
    - "Admin lists all product statuses at /admin/tovary with filters"
    - "Admin creates/edits product with title, brand, price, condition, category, status"
    - "Admin uploads multiple images via signed Cloudinary widget; rows saved in ProductImage"
    - "Published AVAILABLE product appears on storefront /tovar/[slug] after revalidate"
  artifacts:
    - path: src/server/services/admin-product.service.ts
      provides: listAdminProducts, createProduct, updateProduct, deleteProduct, syncProductImages
      exports: ["listAdminProducts", "createProduct", "updateProduct", "syncProductImages"]
    - path: src/components/admin/product-image-upload.tsx
      provides: CldUploadWidget client uploader
    - path: src/app/(admin)/admin/tovary/[id]/page.tsx
      provides: Edit product + uploader
  key_links:
    - from: src/components/admin/product-image-upload.tsx
      to: /api/upload/sign
      via: signatureEndpoint prop
      pattern: signatureEndpoint="/api/upload/sign"
    - from: src/server/actions/admin/product.actions.ts
      to: admin-product.service
      via: syncProductImages after upload
      pattern: syncProductImages

---

## Phase Goal

**As a** адміністратор магазину, **I want to** CRUD товарів із кількома фото Cloudinary, **so that** покупці бачать актуальні оголошення на вітрині.

<objective>
`admin-product.service`, product actions, форми, сторінки `/admin/tovary`, і `ProductImageUploader` з signed upload.

Purpose: ADM-02 + ADM-03 vertical slice.
Output: Product CRUD + multi-image + storefront revalidation.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-admin-operations/04-RESEARCH.md
@.planning/phases/04-admin-operations/04-PATTERNS.md
@.planning/phases/04-admin-operations/04-CONTEXT.md
@.planning/phases/04-admin-operations/04-01-SUMMARY.md
@src/server/validators/product.ts
@src/lib/catalog/format.ts
@prisma/schema.prisma

<interfaces>
From product.ts validators:
- productConditionSchema, productStatusSchema enums

From prisma Product + ProductImage:
- price Int kopiyky, status ProductStatus, images relation

From 04-01 sign route:
- POST /api/upload/sign returns { signature }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: admin-product.service + validators + unit tests</name>
  <read_first>
    - 04-RESEARCH.md — admin product list without AVAILABLE filter; delete guards PRODUCT_IN_CART, PRODUCT_IN_ACTIVE_ORDER
    - catalog.service.ts — cardInclude shape for reference
  </read_first>
  <files>
    src/server/services/admin-product.service.ts
    src/server/services/admin-product.service.test.ts
    src/server/validators/admin-product.ts
    src/server/validators/admin-product.test.ts
  </files>
  <behavior>
    - buildAdminProductWhere includes status filter when provided but never forces AVAILABLE
    - deleteProduct throws PRODUCT_IN_CART when cartItem count > 0
    - deleteProduct throws PRODUCT_IN_ACTIVE_ORDER when orderItem linked to non-terminal order
    - syncProductImages replaces sortOrder 0..n-1 from input array max 8 images
    - priceUahToKopiyky(4500) → 450000
  </behavior>
  <action>
    Validators `admin-product.ts`:
    - upsertProductSchema: title, slug optional, description, brand, categoryId cuid, condition, status, priceUah positive int (convert in service)
    - productImageInputSchema: cloudinaryPublicId, alt optional, sortOrder, width/height optional
    - listAdminProductsSchema: page, pageSize, status optional, categoryId optional, q optional
    Service `admin-product.service.ts`:
    - listAdminProducts(filters) — all statuses; include category + first image thumb
    - getProductAdmin(id)
    - createProduct — default DRAFT; unique slug with -2 suffix collision
    - updateProduct — no auto slug from title change
    - syncProductImages(productId, images[]) — transaction deleteMany + createMany or upsert by public_id
    - deleteProduct — guards per 04-RESEARCH; prefer error codes not generic throw
    Export pure priceUahToKopiyky for unit test.
    Tests: where builder; price conversion; delete guard predicates.
  </action>
  <verify>
    <automated>npm test -- src/server/services/admin-product.service.test.ts src/server/validators/admin-product.test.ts</automated>
  </verify>
  <done>Product service and validators tested; admin list not filtered to AVAILABLE only</done>
</task>

<task type="auto">
  <name>Task 2: product.actions + image save action</name>
  <read_first>
    - 04-PATTERNS.md — revalidateStorefrontProduct helper
  </read_first>
  <files>src/server/actions/admin/product.actions.ts</files>
  <action>
    Create `product.actions.ts`:
    - requireAdmin() on all exports (D-04-05)
    - createProductAction → redirect `/admin/tovary/[id]` (DRAFT)
    - updateProductAction → revalidate `/admin/tovary`, `/tovar/${slug}`, `/katalog`, `/katalog/${categorySlug}`, `/` layout
    - saveProductImagesAction(productId, images[]) → syncProductImages + revalidate product paths
    - deleteProductAction → error codes to Ukrainian toast messages in form
    - Do not expose manual SOLD status in schema if product on active order — allow SOLD only via orders; admin form status enum: DRAFT | AVAILABLE (document in action if schema allows SOLD read-only display)
    Map PRODUCT_IN_CART, PRODUCT_IN_ACTIVE_ORDER to { ok: false, error }.
  </action>
  <verify>
    <automated>grep -c requireAdmin src/server/actions/admin/product.actions.ts | awk '{exit ($1<3)?1:0}'</automated>
  </verify>
  <done>Product mutations delegate to service with full revalidatePath set</done>
</task>

<task type="auto">
  <name>Task 3: Product pages + form + image uploader</name>
  <read_first>
    - 04-UI-SPEC.md — ProductImageUploader contract (72px thumbs, max 8, «Додати фото»)
    - next-cloudinary CldUploadWidget signed uploads
  </read_first>
  <files>
    src/components/admin/product-form.tsx
    src/components/admin/product-image-upload.tsx
    src/app/(admin)/admin/tovary/page.tsx
    src/app/(admin)/admin/tovary/novyi/page.tsx
    src/app/(admin)/admin/tovary/[id]/page.tsx
    src/components/admin/admin-nav.tsx
  </files>
  <action>
    `product-image-upload.tsx` (client):
    - CldUploadWidget signatureEndpoint="/api/upload/sign" (D-04-02)
    - options multiple maxFiles 8, sources local
    - onSuccess extract public_id, width, height → callback onUploaded
    - UI: «Додати фото», grid thumbs 72px, remove per image, optional alt
    - After batch upload call saveProductImagesAction
    `product-form.tsx`:
    - Fields: Назва, Slug (helper), Бренд, Категорія select (from listCategoriesAdmin), Стан, Ціна (грн), Опис, Статус (Чернетка / В наявності)
    - Sticky footer «Зберегти» «Скасувати»
    - Embed ProductImageUploader on edit + novyi after first save (create DRAFT then enable upload) OR single page with productId after create redirect
    Pages:
    - tovary/page.tsx — filters status/category, table with thumb, badges В наявності/Продано/Чернетка, «Додати товар» → novyi
    - tovary/novyi/page.tsx — create flow
    - tovary/[id]/page.tsx — edit + link «На вітрині» → /tovar/[slug] when not DRAFT
    Update admin-nav: enable Товари link `/admin/tovary`.
  </action>
  <verify>
    <automated>test -f src/components/admin/product-image-upload.tsx && grep -q 'signatureEndpoint="/api/upload/sign"' src/components/admin/product-image-upload.tsx</automated>
  </verify>
  <done>Product admin UI wired to sign route and image persistence</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser widget → /api/upload/sign | Signed upload only for admin |
| product.actions → DB | Price/status server-validated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Elevation | product.actions | mitigate | requireAdmin each action |
| T-04-03-02 | Tampering | price/status | mitigate | Zod + kopiyky server-side; no client price |
| T-04-03-03 | Repudiation | unsigned upload | mitigate | Depends 04-01 signed route only |

</threat_model>

<verification>
npm test -- src/server/services/admin-product.service.test.ts
grep signatureEndpoint src/components/admin/product-image-upload.tsx
npm run build
</verification>

<success_criteria>
Admin product CRUD at /admin/tovary; multi-image upload persists ProductImage; storefront paths revalidated on publish
</success_criteria>

<output>
Create `.planning/phases/04-admin-operations/04-03-SUMMARY.md` when done
</output>
