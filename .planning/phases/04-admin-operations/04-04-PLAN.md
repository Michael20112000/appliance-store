---
phase: 04-admin-operations
plan: 04
type: execute
wave: 4
depends_on: []
files_modified:
  - src/server/services/admin-order.service.ts
  - src/server/services/admin-order.service.test.ts
  - src/server/validators/admin-order.ts
  - src/server/validators/admin-order.test.ts
  - src/server/actions/admin/order.actions.ts
  - src/components/admin/orders-table.tsx
  - src/components/admin/order-status-select.tsx
  - src/app/(admin)/admin/zamovlennia/page.tsx
  - src/app/(admin)/admin/zamovlennia/[orderNumber]/page.tsx
  - src/components/admin/admin-nav.tsx
autonomous: true
requirements:
  - ADM-04
context_decisions:
  - "D-04-04: CANCELLED → revert productId SOLD→AVAILABLE in $transaction"
  - "D-04-03: routes /admin/zamovlennia/*"
  - "Linear order status transitions per 04-RESEARCH table — reject INVALID_STATUS_TRANSITION in service"
  - "D-04-05: requireAdmin in order.actions"
  - "Admin list all orders (no userId filter) — separate from order.service listOrdersForUser"

must_haves:
  truths:
    - "Admin sees all orders at /admin/zamovlennia sorted newest first"
    - "Admin opens order detail by orderNumber and changes status"
    - "Cancelling order returns linked products from SOLD to AVAILABLE"
    - "Illegal status jumps rejected server-side with clear error"
  artifacts:
    - path: src/server/services/admin-order.service.ts
      provides: listAllOrders, getOrderAdmin, updateOrderStatus, assertTransitionAllowed
      exports: ["listAllOrders", "getOrderAdmin", "updateOrderStatus"]
    - path: src/components/admin/order-status-select.tsx
      provides: Status change UI with cancel confirm
    - path: src/app/(admin)/admin/zamovlennia/[orderNumber]/page.tsx
      provides: Order detail + line items
  key_links:
    - from: admin-order.service updateOrderStatus
      to: prisma.product.updateMany
      via: SOLD→AVAILABLE when CANCELLED
      pattern: status:\s*"SOLD"
    - from: order.actions.ts
      to: admin-order.service.ts
      via: updateOrderStatusAction

---

## Phase Goal

**As a** адміністратор магазину, **I want to** переглядати замовлення і змінювати статус, **so that** виконання та скасування коректно відображають наявність товарів.

<objective>
`admin-order.service` з матрицею переходів і revert інвентарю, actions, таблиця замовлень і детальна сторінка.

Purpose: ADM-04 vertical slice (no Cloudinary dependency).
Output: Order admin UI + SOLD→AVAILABLE on cancel (D-04-04).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-admin-operations/04-RESEARCH.md
@.planning/phases/04-admin-operations/04-PATTERNS.md
@.planning/phases/04-admin-operations/04-CONTEXT.md
@src/server/services/order.service.ts
@src/components/account/order-history-card.tsx
@src/types/order.ts

<interfaces>
OrderStatus enum: PENDING, CONFIRMED, READY_FOR_PICKUP, OUT_FOR_DELIVERY, COMPLETED, CANCELLED

Allowed transitions (04-RESEARCH):
- PENDING → CONFIRMED, CANCELLED
- CONFIRMED → READY_FOR_PICKUP, OUT_FOR_DELIVERY, CANCELLED
- READY_FOR_PICKUP → COMPLETED, CANCELLED
- OUT_FOR_DELIVERY → COMPLETED, CANCELLED
- COMPLETED, CANCELLED terminal

From order-history-card.tsx — STATUS_LABELS UA pattern to reuse
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: admin-order.service + validators + unit tests</name>
  <read_first>
    - 04-RESEARCH.md — Pattern 3 transaction cancel revert
    - order.service.ts — createOrderFromCart SOLD transition (mirror revert)
  </read_first>
  <files>
    src/server/services/admin-order.service.ts
    src/server/services/admin-order.service.test.ts
    src/server/validators/admin-order.ts
    src/server/validators/admin-order.test.ts
  </files>
  <behavior>
    - assertTransitionAllowed(PENDING, CONFIRMED) ok; (PENDING, COMPLETED) throws INVALID_STATUS_TRANSITION
    - cancel transition triggers SOLD→AVAILABLE for items with productId (pure function test)
    - updateOrderStatusSchema rejects unknown enum
  </behavior>
  <action>
    `admin-order.ts` validator: updateOrderStatusSchema { orderId, status: OrderStatus enum }
    Export `ORDER_STATUS_LABELS_UA` map aligned with buyer card.
    Export pure `assertTransitionAllowed(from, to)` and `getAllowedNextStatuses(from)`.
    Service:
    - listAllOrders(filters?) — include items, user email via separate user query or denormalized customer fields on Order (use customerName/Phone from order; optional join User by userId for email if needed)
    - getOrderAdmin(orderNumber) — full detail DTO
    - updateOrderStatus(orderId, newStatus) — transaction per 04-RESEARCH Pattern 3 (D-04-04)
    Unit tests: all illegal pairs; cancel revert logic with mocked tx.
  </action>
  <verify>
    <automated>npm test -- src/server/services/admin-order.service.test.ts src/server/validators/admin-order.test.ts</automated>
  </verify>
  <done>Transition matrix and cancel inventory revert covered by unit tests</done>
</task>

<task type="auto">
  <name>Task 2: order.actions</name>
  <files>src/server/actions/admin/order.actions.ts</files>
  <action>
    Create order.actions.ts:
    - updateOrderStatusAction — requireAdmin, parse schema, call updateOrderStatus
    - revalidatePath `/admin/zamovlennia`, `/admin/zamovlennia/${orderNumber}`, `/kabinet` (buyer history)
    - Return { ok: false, error: "INVALID_STATUS_TRANSITION" } for expected failures
    - No prisma in actions
  </action>
  <verify>
    <automated>grep requireAdmin src/server/actions/admin/order.actions.ts</automated>
  </verify>
  <done>Order status mutations admin-only with revalidation</done>
</task>

<task type="auto">
  <name>Task 3: Orders list + detail + status select UI</name>
  <read_first>
    - 04-UI-SPEC.md — orders table columns, cancel AlertDialog copy
  </read_first>
  <files>
    src/components/admin/orders-table.tsx
    src/components/admin/order-status-select.tsx
    src/app/(admin)/admin/zamovlennia/page.tsx
    src/app/(admin)/admin/zamovlennia/[orderNumber]/page.tsx
    src/components/admin/admin-nav.tsx
  </files>
  <action>
    `orders-table.tsx`: columns Номер, Дата, Покупець (імʼя + телефон), Доставка, Сума (tabular-nums), Статус badge, «Відкрити»
    `order-status-select.tsx` (client):
    - Select allowed next statuses from server prop or static map
    - On CANCELLED — AlertDialog: «Скасувати замовлення? Товари повернуться в наявність.» — «Так, скасувати»
    - Toast «Статус оновлено» on success
    zamovlennia/page.tsx — filter tabs Усі / Нові / В роботі / Завершені / Скасовані (map to status sets)
    zamovlennia/[orderNumber]/page.tsx — header, customer block, line items table, summary, OrderStatusSelect
    Update admin-nav: Замовлення → `/admin/zamovlennia`
    Ukrainian copy throughout; no payment columns.
  </action>
  <verify>
    <automated>test -f src/app/\(admin\)/admin/zamovlennia/page.tsx && test -f src/components/admin/order-status-select.tsx</automated>
  </verify>
  <done>Admin can list orders and update status with cancel confirmation</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| order.actions | Admin-only status changes |
| admin-order.service | Inventory side effects on cancel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Tampering | updateOrderStatus | mitigate | Transition matrix in service |
| T-04-04-02 | Elevation | order.actions | mitigate | requireAdmin |
| T-04-04-03 | Integrity | cancel revert | mitigate | $transaction SOLD→AVAILABLE per D-04-04 |

</threat_model>

<verification>
npm test -- src/server/services/admin-order.service.test.ts
npm run build
</verification>

<success_criteria>
Order list/detail work; cancel reverts SOLD products; illegal transitions rejected
</success_criteria>

<output>
Create `.planning/phases/04-admin-operations/04-04-SUMMARY.md` when done
</output>
