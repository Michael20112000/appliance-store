---
phase: 04-admin-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/lib/cloudinary.ts
  - src/lib/env.ts
  - src/app/api/upload/sign/route.ts
  - src/app/api/upload/sign/route.test.ts
autonomous: false
requirements:
  - ADM-03
context_decisions:
  - "D-04-02: POST /api/upload/sign + cloudinary SDK api_sign_request (not Server Action)"
  - "D-04-05: sign route uses session+role check → 401 JSON (not redirect); same auth as requireAdmin"
  - "CLOUDINARY_API_KEY/SECRET required at sign time via getCloudinaryConfig() fail-fast (not optional in route)"
  - "No CLOUDINARY_API_SECRET in NEXT_PUBLIC_* (D-16 / PITFALLS #5)"

must_haves:
  truths:
    - "Admin session can obtain Cloudinary upload signature via POST /api/upload/sign"
    - "Buyer or unauthenticated POST to /api/upload/sign is rejected before signing"
    - "Missing CLOUDINARY_API_KEY or CLOUDINARY_API_SECRET returns clear 503/500 without leaking secrets"
  artifacts:
    - path: src/lib/cloudinary.ts
      provides: Server-only cloudinary v2 config + getCloudinaryConfig()
      exports: ["getCloudinaryConfig", "signUploadParams"]
    - path: src/app/api/upload/sign/route.ts
      provides: Signed upload endpoint for CldUploadWidget
      exports: ["POST"]
    - path: package.json
      provides: cloudinary@2.10.0 dependency
      contains: '"cloudinary"'
  key_links:
    - from: src/app/api/upload/sign/route.ts
      to: src/lib/permissions.ts
      via: assertAdminApi() session role check → 401 JSON
      pattern: role.*admin|401
    - from: src/app/api/upload/sign/route.ts
      to: cloudinary.utils.api_sign_request
      via: signUploadParams(paramsToSign)
      pattern: api_sign_request

---

## Phase Goal

**As a** адміністратор магазину, **I want to** безпечно завантажувати фото товарів через Cloudinary, **so that** зображення не можна підробити або зловжити unsigned upload.

<objective>
Встановити `cloudinary@2.10.0`, server-only конфіг, і `POST /api/upload/sign` з `requireAdmin()` для `CldUploadWidget`.

Purpose: Інфраструктура ADM-03 — підпис upload до product CRUD (04-03).
Output: `lib/cloudinary.ts`, sign route, env fail-fast, unit test на відхилення не-адміна.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-admin-operations/04-RESEARCH.md
@.planning/phases/04-admin-operations/04-CONTEXT.md
@src/lib/permissions.ts
@src/lib/env.ts
@.env.example

<interfaces>
From src/lib/permissions.ts:
- requireAdmin() — redirects to /uviity if !session?.user || role !== "admin"

From 04-RESEARCH Pattern 2:
- POST body: { paramsToSign: Record<string, unknown> }
- Response: { signature: string }
- cloudinary.config from NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME, CLOUDINARY_API_KEY, CLOUDINARY_API_SECRET
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Gate: cloudinary@2.10.0 package legitimacy</name>
  <files>package.json</files>
  <action>
    Before `npm install cloudinary@2.10.0`, human verifies package on npmjs: publisher cloudinary, repo github.com/cloudinary/cloudinary_npm, version 2.10.0 matches 04-RESEARCH Package Legitimacy Audit (Recommended add). Reply "approved" to unblock Task 1 install.
  </action>
  <verify>
    <automated>test -f package.json</automated>
  </verify>
  <done>Human approved cloudinary@2.10.0 install; resume signal "approved" received</done>
  <what-built>Pre-install verification for official Cloudinary Node SDK</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/cloudinary — publisher cloudinary, repo github.com/cloudinary/cloudinary_npm
    2. Confirm version 2.10.0 exists and matches 04-RESEARCH Package Legitimacy Audit (Recommended add)
    3. Reply "approved" to proceed with npm install
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Install cloudinary + server config</name>
  <read_first>
    - 04-RESEARCH.md — Pattern 2 sign route + env table
    - src/lib/env.ts — optional CLOUDINARY_* today
  </read_first>
  <files>package.json, package-lock.json, src/lib/cloudinary.ts, src/lib/env.ts</files>
  <action>
    Run `npm install cloudinary@2.10.0` after human gate approved (per D-04-02).
    Create `src/lib/cloudinary.ts` (server-only — no "use client"):
    - Import `v2 as cloudinary` from "cloudinary"
    - Export `getCloudinaryConfig()` that reads via `getEnv()` and throws descriptive Error if `CLOUDINARY_API_KEY` or `CLOUDINARY_API_SECRET` missing (Ukrainian message for logs ok; API returns generic UA copy)
    - Export `signUploadParams(paramsToSign: Record<string, string | number>)` calling `cloudinary.utils.api_sign_request` with secret from config
    - Call `cloudinary.config({ cloud_name, api_key, api_secret })` once inside getCloudinaryConfig or module init
    Do NOT add secrets to NEXT_PUBLIC_*.
    Optional: add comment in `.env.example` that API_KEY/SECRET required for admin upload (no new env var names unless CLOUDINARY_UPLOAD_PRESET documented as optional server-only).
  </action>
  <verify>
    <automated>node -e "require('./package.json').dependencies.cloudinary && console.log('ok')" && test -f src/lib/cloudinary.ts</automated>
  </verify>
  <done>cloudinary@2.10.0 in package.json; getCloudinaryConfig fails fast without key/secret</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: POST /api/upload/sign + auth rejection test</name>
  <read_first>
    - next-cloudinary signed uploads doc (04-RESEARCH citation)
    - src/app/api/auth/[...all]/route.ts — thin route pattern
  </read_first>
  <files>src/app/api/upload/sign/route.ts, src/app/api/upload/sign/route.test.ts</files>
  <behavior>
    - When requireAdmin would redirect (no session / buyer): POST returns 401 JSON { error: "UNAUTHORIZED" } — mock requireAdmin to throw redirect or export testable guard; prefer extracting assertAdminApi() that returns 401 Response instead of redirect for API routes
    - When admin + valid paramsToSign: 200 { signature: string }
    - When secrets missing: 503 { error: "CLOUDINARY_NOT_CONFIGURED" }
  </behavior>
  <action>
    Implement `src/app/api/upload/sign/route.ts`:
    - `export async function POST(request: Request)`
    - Per D-04-05: verify admin session via `auth.api.getSession` + role check (API must return 401/403, not redirect — redirect breaks fetch from widget; if requireAdmin only redirects, duplicate session check for route returning JSON 401)
    - Parse JSON `{ paramsToSign }`; validate paramsToSign is non-null object
    - Call signUploadParams; return `Response.json({ signature })`
    - Never log CLOUDINARY_API_SECRET
    Add `route.test.ts` with Vitest: mock session buyer → 401; mock admin + mocked sign → 200. Document in SUMMARY if integration test needs real env.
    **Note:** If implementing redirect-only requireAdmin in route, document manual verify: buyer fetch POST /api/upload/sign → not 200 with signature.
  </action>
  <verify>
    <automated>npm test -- src/app/api/upload/sign/route.test.ts</automated>
  </verify>
  <done>Sign route returns signature for admin; non-admin rejected; unit test green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (CldUploadWidget) → POST /api/upload/sign | Untrusted client requests signatures |
| Sign route → Cloudinary secret | Server-only signing material |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Elevation | /api/upload/sign | mitigate | Admin role check before sign; unit test buyer rejection |
| T-04-01-02 | Tampering | Unsigned upload | mitigate | Signed uploads only; no upload preset in NEXT_PUBLIC |
| T-04-01-03 | Information Disclosure | CLOUDINARY_API_SECRET | mitigate | Server-only env; response is signature only |
| T-04-01-SC | Tampering | npm cloudinary@2.10.0 | mitigate | Human-verify npmjs + official repo before install |

</threat_model>

<verification>
npm test -- src/app/api/upload/sign/route.test.ts
npm run build
</verification>

<success_criteria>
cloudinary installed; sign route admin-only; getCloudinaryConfig fail-fast; route test passes
</success_criteria>

<output>
Create `.planning/phases/04-admin-operations/04-01-SUMMARY.md` when done
</output>
