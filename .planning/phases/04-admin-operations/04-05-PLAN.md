---
phase: 04-admin-operations
plan: 05
type: execute
wave: 5
depends_on:
  - "04-02"
  - "04-03"
  - "04-04"
files_modified:
  - src/app/(admin)/admin/layout.tsx
  - src/app/(admin)/admin/page.tsx
  - src/components/admin/admin-nav.tsx
  - src/components/admin/stat-card.tsx
  - e2e/admin-rbac.spec.ts
  - e2e/admin-categories.spec.ts
  - e2e/admin-products.spec.ts
  - e2e/admin-orders.spec.ts
  - src/server/services/admin-catalog.service.test.ts
  - src/server/services/admin-product.service.test.ts
  - src/server/services/admin-order.service.test.ts
autonomous: true
requirements:
  - AUTH-04
  - ADM-01
  - ADM-02
  - ADM-03
  - ADM-04
context_decisions:
  - "D-04-06: Nav «Чати» disabled + badge «Незабаром» — NO chat implementation (ADM-05 Phase 5)"
  - "Full admin shell per 04-UI-SPEC: sidebar w-60, bg-muted, inset main panel"
  - "Dashboard stats: PENDING orders, AVAILABLE products, DRAFT products counts"
  - "E2E proves AUTH-04 + CRUD smoke across slices"
  - "Wave 0: fill any MISSING unit/e2e scaffolds referenced in 04-VALIDATION.md"

must_haves:
  truths:
    - "Admin dashboard shows live counts and quick links"
    - "AdminNav links all sections with Чати disabled «Незабаром»"
    - "E2E: buyer blocked from /admin and sign route; admin CRUD smoke passes"
    - "All phase requirements have automated verify path in 04-VALIDATION.md"
  artifacts:
    - path: src/app/(admin)/admin/page.tsx
      provides: Dashboard with StatCard grid + recent orders
    - path: src/components/admin/admin-nav.tsx
      provides: Full sidebar including disabled Чати per D-04-06
    - path: e2e/admin-categories.spec.ts
      provides: Category CRUD smoke
    - path: e2e/admin-products.spec.ts
      provides: Product publish smoke
    - path: e2e/admin-orders.spec.ts
      provides: Order status change smoke
  key_links:
    - from: e2e/admin-rbac.spec.ts
      to: /api/upload/sign
      via: buyer POST expects non-200 signature
      pattern: /api/upload/sign

---

## Phase Goal

**As a** адміністратор магазину, **I want to** мати цілісну панель керування з перевіреним RBAC, **so that** Phase 4 готова до verify-work без прогалин у тестах.

<objective>
Допрацювати layout/dashboard, disabled Чати nav (ADM-05 defer), E2E + Wave 0 unit gaps.

Purpose: AUTH-04 proof + integration confidence across ADM-01–04.
Output: Production-ready admin chrome + green targeted e2e/unit suite.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-admin-operations/04-VALIDATION.md
@.planning/phases/04-admin-operations/04-UI-SPEC.md
@.planning/phases/04-admin-operations/04-CONTEXT.md
@e2e/admin-rbac.spec.ts
@src/app/(admin)/admin/layout.tsx

<interfaces>
Admin login pattern from e2e/admin-rbac.spec.ts:
- ADMIN_EMAIL / ADMIN_PASSWORD env
- Buyer register → /admin redirects /uviity
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin shell + dashboard stats</name>
  <read_first>
    - 04-UI-SPEC.md — Admin Shell Layout, Dashboard, AdminNav table (use Ukrainian hrefs)
  </read_first>
  <files>
    src/app/(admin)/admin/layout.tsx
    src/app/(admin)/admin/page.tsx
    src/components/admin/admin-nav.tsx
    src/components/admin/stat-card.tsx
  </files>
  <action>
    Run shadcn add if missing: table, dialog, select, textarea, alert-dialog, sonner, scroll-area (per 04-UI-SPEC) — only components not yet installed.
    `stat-card.tsx`: label + count + optional link.
    `admin-nav.tsx` full sidebar (D-04-03 paths):
    - Панель `/admin`, Категорії `/admin/kategorii`, Товари `/admin/tovary`, Замовлення `/admin/zamovlennia`
    - **Чати** (D-04-06 / ADM-05 out of scope): `href="#"`, aria-disabled, opacity-60, Badge «Незабаром», NO chat routes or API
    - Footer: «На сайт» `/`, sign out
    `layout.tsx`: grid `grid-cols-[240px_1fr]`, min-h-dvh bg-muted, main inset `bg-background rounded-lg border p-4 md:p-6`
    `page.tsx` dashboard:
    - H1 **Панель керування**
    - Stats: Нові замовлення (PENDING count), Товари в наявності (AVAILABLE), Чернетки (DRAFT) — server queries via admin services
    - Quick links: «Додати товар», «Переглянути замовлення»
    - Recent orders table (5 rows) with links to zamovlennia detail
  </action>
  <verify>
    <automated>grep -q 'Незабаром' src/components/admin/admin-nav.tsx && grep -q zamovlennia src/components/admin/admin-nav.tsx</automated>
  </verify>
  <done>Dashboard and full nav; Чати disabled only, no chat code</done>
</task>

<task type="auto">
  <name>Task 2: Wave 0 unit test gap fill</name>
  <files>
    src/server/services/admin-catalog.service.test.ts
    src/server/services/admin-product.service.test.ts
    src/server/services/admin-order.service.test.ts
  </files>
  <action>
    Audit 04-VALIDATION.md Wave 0 checklist — ensure each file exists with at least one meaningful test (not empty describe):
    - admin-catalog: slug + CATEGORY_HAS_PRODUCTS
    - admin-product: admin where not AVAILABLE-only + PRODUCT_IN_CART guard
    - admin-order: transition matrix + cancel SOLD→AVAILABLE
    Add missing cases if prior plans left scaffolds empty.
    Do not add ADM-05 / chat tests.
  </action>
  <verify>
    <automated>npm test -- src/server/services/admin-catalog.service.test.ts src/server/services/admin-product.service.test.ts src/server/services/admin-order.service.test.ts</automated>
  </verify>
  <done>All three admin service test files pass npm test</done>
</task>

<task type="auto">
  <name>Task 3: E2E admin CRUD + RBAC extension</name>
  <files>
    e2e/admin-rbac.spec.ts
    e2e/admin-categories.spec.ts
    e2e/admin-products.spec.ts
    e2e/admin-orders.spec.ts
  </files>
  <action>
    Extend `admin-rbac.spec.ts`:
    - After buyer login, `request.post('/api/upload/sign', { data: { paramsToSign: { timestamp: 1 } } })` → expect not ok with signature (401/403)
    - Admin same POST with storageState/cookies → 200 when env configured OR skip with test.skip if no Cloudinary secrets in CI
    Create `admin-categories.spec.ts`: admin login → create category «Тестова» → visible in list → delete or cleanup
    Create `admin-products.spec.ts`: create DRAFT product → set AVAILABLE (use seed cloudinary id in form if upload env missing) → guest `/tovar/[slug]` shows title OR skip upload path
    Create `admin-orders.spec.ts`: use seed order if exists → open detail → change PENDING→CONFIRMED (or cancel flow on test order) — avoid breaking production data; prefer test order from seed
    Use Ukrainian button labels from copy contract.
  </action>
  <verify>
    <automated>npx playwright test e2e/admin-rbac.spec.ts e2e/admin-categories.spec.ts e2e/admin-products.spec.ts e2e/admin-orders.spec.ts</automated>
  </verify>
  <done>E2E suite covers AUTH-04, ADM-01–04 smoke paths</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E → /admin, /api/upload/sign | Proves RBAC end-to-end |
| Disabled Чати nav | No false sense of security — must not expose chat API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Elevation | admin E2E | mitigate | Buyer cannot access admin or sign |
| T-04-05-02 | Spoofing | ADM-05 defer | accept | Disabled nav only; Phase 5 implements chat |

</threat_model>

<verification>
npm test -- src/server/services/admin-*.test.ts
npx playwright test e2e/admin-rbac.spec.ts e2e/admin-categories.spec.ts e2e/admin-products.spec.ts e2e/admin-orders.spec.ts
</verification>

<success_criteria>
Admin shell complete; Чати disabled; unit + e2e green for phase validation matrix
</success_criteria>

<output>
Create `.planning/phases/04-admin-operations/04-05-SUMMARY.md` when done
</output>
