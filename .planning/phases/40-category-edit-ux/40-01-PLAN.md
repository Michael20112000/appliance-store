---
phase: 40-category-edit-ux
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/hooks/admin/use-category-auto-save.ts
  - src/hooks/admin/use-category-auto-save.test.ts
  - src/components/admin/category-edit-delete-button.tsx
  - src/components/admin/category-edit-delete-button.test.tsx
autonomous: true
requirements: [ADM-CAT-09, ADM-CAT-10]

must_haves:
  truths:
    - "useCategoryAutoSave debounces saves by 500ms and skips saves when schema validation fails"
    - "useCategoryAutoSave skips saves when snapshot is unchanged after a successful save"
    - "useCategoryAutoSave shows error toast on failed save without rolling back field text"
    - "useCategoryAutoSave transitions status: idle → saving → saved → idle"
    - "CategoryEditDeleteButton renders a ghost icon-only Trash2 button with aria-label=Видалити категорію"
    - "CategoryEditDeleteButton opens AlertDialog and calls deleteCategoryFromListAction on confirm"
    - "CategoryEditDeleteButton pushes /admin/kategorii on success, shows error toast on failure without redirecting"
  artifacts:
    - path: "src/hooks/admin/use-category-auto-save.ts"
      provides: "useCategoryAutoSave hook + SaveStatus type export"
      exports: ["useCategoryAutoSave", "SaveStatus"]
    - path: "src/hooks/admin/use-category-auto-save.test.ts"
      provides: "Unit tests for hook — 5 cases"
    - path: "src/components/admin/category-edit-delete-button.tsx"
      provides: "CategoryEditDeleteButton component"
      exports: ["CategoryEditDeleteButton"]
    - path: "src/components/admin/category-edit-delete-button.test.tsx"
      provides: "Unit tests for delete button — 3 cases"
  key_links:
    - from: "src/hooks/admin/use-category-auto-save.ts"
      to: "src/server/actions/admin/category.actions.ts"
      via: "updateCategoryAction call in save chain"
      pattern: "updateCategoryAction\\(\\{"
    - from: "src/components/admin/category-edit-delete-button.tsx"
      to: "src/server/actions/admin/category.actions.ts"
      via: "deleteCategoryFromListAction (NOT deleteCategoryAction)"
      pattern: "deleteCategoryFromListAction"
---

<objective>
Create the two leaf components/hooks that the category edit page wrapper depends on:
`useCategoryAutoSave` (RHF-based auto-save hook) and `CategoryEditDeleteButton` (icon-trash + AlertDialog delete).
Both are direct mirrors of their product-edit equivalents. Tests are written first (RED) before implementation (GREEN).

Purpose: Establish the auto-save primitive and the delete primitive so Plan 02 can wire them into the page without re-implementing logic.
Output: Two new .ts/.tsx files + two .test files, all passing under `vitest run`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-category-edit-ux/40-CONTEXT.md
@.planning/phases/40-category-edit-ux/40-RESEARCH.md
@.planning/phases/40-category-edit-ux/40-PATTERNS.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: useCategoryAutoSave hook — RED tests then GREEN implementation</name>
  <files>src/hooks/admin/use-category-auto-save.test.ts, src/hooks/admin/use-category-auto-save.ts</files>

  <read_first>
    - src/hooks/admin/use-product-auto-save.ts — exact mirror analog; copy structure substituting category types
    - src/hooks/admin/use-product-auto-save.test.ts — exact mirror for test file structure (fake timers, renderHook, useForm harness)
    - src/hooks/admin/use-callback-note-auto-save.ts — simpler variant; confirms SaveStatus defined locally is the project pattern
    - src/server/actions/admin/category.actions.ts — verify updateCategoryAction signature and return shape
    - src/server/validators/category.ts — upsertCategorySchema type and transform behaviour
    - src/lib/debounce.ts — createDebounce API (flush / cancel interface)
  </read_first>

  <behavior>
    - Test 1 (debounce): setValue("name", "New") → advance 400ms → action NOT called; advance 100ms more → action called once
    - Test 2 (schema guard): setValue("name", "") → advance 500ms → updateCategoryAction NOT called (empty name fails upsertCategorySchema)
    - Test 3 (snapshot dedup): action resolves ok → setValue("name", same value) → advance 500ms → action NOT called again (snapshot unchanged)
    - Test 4 (error toast, no rollback): updateCategoryAction rejects { ok: false, error: "UNKNOWN" } → toast.error called with "Не вдалося зберегти категорію. Спробуйте ще раз." → status returns to "idle"
    - Test 5 (status transitions): setValue → advance 500ms → status "saving" while action pending → resolves ok → status "saved" → advance 2000ms → status "idle"
  </behavior>

  <action>
    RED phase: Create src/hooks/admin/use-category-auto-save.test.ts with @vitest-environment jsdom pragma. Use the harness pattern from use-product-auto-save.test.ts: vi.useFakeTimers in beforeEach, vi.useRealTimers in afterEach. Mock `@/server/actions/admin/category.actions` with vi.mock returning { updateCategoryAction: vi.fn() }. Mock `sonner` with { toast: { error: vi.fn(), success: vi.fn() } }. Define validValues as { name: "Холодильники", sortOrder: 1 }. Wrap renderHook in a useForm harness. Write 5 describe/it blocks matching the behavior above. Run vitest — tests must fail (hook file missing).

    GREEN phase: Create src/hooks/admin/use-category-auto-save.ts. Add "use client" directive. Export `SaveStatus = "idle" | "saving" | "saved"` type. Set DEBOUNCE_MS = 500 (per D-02) and SAVED_DISPLAY_MS = 2000. Define errorMessages Record with keys CATEGORY_HAS_PRODUCTS, CATEGORY_NOT_FOUND, UNKNOWN. Define UseCategoryAutoSaveOptions with fields: control: Control&lt;UpsertCategoryInput&gt;, categoryId: string, enabled: boolean, initialValues: UpsertCategoryInput. Export function useCategoryAutoSave. Inside: useState&lt;SaveStatus&gt;("idle"), snapshotRef (string), generationRef (number), savedTimeoutRef, debounceRef = createDebounce(DEBOUNCE_MS), saveChainRef = Promise.resolve(), watchedValues = useWatch({ control }), watchedRef synced to watchedValues. Init useEffect: parse initialValues with upsertCategorySchema.safeParse — serialize parsed.data to snapshotRef (NOT raw initialValues, per Pitfall 2). runSave useCallback: parse watchedRef.current via upsertCategorySchema.safeParse; return if !success; serialize parsed.data; return if serialized === snapshotRef.current; increment generation; setStatus("saving"); call updateCategoryAction({ id: categoryId, ...parsed.data }); if generation stale return; if !result.ok toast.error + setStatus("idle"); else snapshotRef.current = serialized, setStatus("saved"), setTimeout → setStatus("idle") after SAVED_DISPLAY_MS. Watch useEffect: serialize watchedValues → trigger debounce → runSave. flush useCallback wrapping debounceRef.current.flush(). Return { status, flush }. Run vitest — all 5 tests must pass.
  </action>

  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npx vitest run --reporter=verbose src/hooks/admin/use-category-auto-save.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - src/hooks/admin/use-category-auto-save.ts exists and exports useCategoryAutoSave and SaveStatus
    - src/hooks/admin/use-category-auto-save.test.ts exists with 5 test cases
    - `vitest run src/hooks/admin/use-category-auto-save.test.ts` exits 0 with 5 passing
    - File contains `DEBOUNCE_MS = 500` (not 400 — different from callback hook)
    - File imports updateCategoryAction from @/server/actions/admin/category.actions
    - File imports upsertCategorySchema from @/server/validators/category
    - File imports createDebounce from @/lib/debounce
    - runSave calls upsertCategorySchema.safeParse (not raw values) for both snapshot init and save comparison
    - No toast.success call inside the hook (D-04: error-only toasts)
  </acceptance_criteria>

  <done>vitest run exits 0 for use-category-auto-save.test.ts; hook file is complete and all 5 behaviours verified by tests.</done>
</task>

<task type="tdd">
  <name>Task 2: CategoryEditDeleteButton — RED tests then GREEN implementation</name>
  <files>src/components/admin/category-edit-delete-button.test.tsx, src/components/admin/category-edit-delete-button.tsx</files>

  <read_first>
    - src/components/admin/product-edit-delete-button.tsx — exact mirror analog; copy structure substituting category action and messages
    - src/components/admin/product-edit-delete-button.test.tsx — exact mirror for test file (vi.mock next/navigation, deleteCategoryFromListAction, sonner)
    - src/components/admin/category-table-delete-button.tsx — Phase 39 AlertDialog pattern with category errorMessages (D-11 source)
    - src/server/actions/admin/category.actions.ts — verify deleteCategoryFromListAction signature (line ~108) does NOT call redirect(); verify deleteCategoryAction (line ~89) DOES call redirect() — use the former only
  </read_first>

  <behavior>
    - Test 1 (happy path): render with categoryId → click ghost button (aria-label="Видалити категорію") → AlertDialog opens with title "Видалити категорію?" → click confirm → deleteCategoryFromListAction called with categoryId → toast.success("Категорію видалено") → router.push("/admin/kategorii")
    - Test 2 (error): deleteCategoryFromListAction resolves { ok: false, error: "CATEGORY_HAS_PRODUCTS" } → toast.error("У категорії є товари. Спочатку перемістіть або видаліть їх.") → router.push NOT called
    - Test 3 (cancel): click ghost button → dialog opens → click "Скасувати" → deleteCategoryFromListAction NOT called
  </behavior>

  <action>
    RED phase: Create src/components/admin/category-edit-delete-button.test.tsx with @vitest-environment jsdom pragma. vi.mock("next/navigation", () => ({ useRouter: () => ({ push }) })) where push = vi.fn(). vi.mock("@/server/actions/admin/category.actions", () => ({ deleteCategoryFromListAction: vi.fn() })). vi.mock("sonner", () => ({ toast: { error: vi.fn(), success: vi.fn() } })). Write 3 test cases matching behavior above using render, fireEvent, waitFor from @testing-library/react. Run vitest — tests must fail (component missing).

    GREEN phase: Create src/components/admin/category-edit-delete-button.tsx. Add "use client" directive. Import useRouter from next/navigation, useState + useTransition from react, Trash2 from lucide-react, toast from sonner, deleteCategoryFromListAction from @/server/actions/admin/category.actions, AlertDialog + AlertDialogAction + AlertDialogCancel + AlertDialogContent + AlertDialogDescription + AlertDialogFooter + AlertDialogHeader + AlertDialogTitle from @/components/ui/alert-dialog, Button from @/components/ui/button. Define errorMessages Record with CATEGORY_HAS_PRODUCTS ("У категорії є товари. Спочатку перемістіть або видаліть їх."), CATEGORY_NOT_FOUND ("Категорію не знайдено."), UNKNOWN ("Не вдалося видалити категорію. Спробуйте ще раз.") — per D-11. Props: { categoryId: string }. State: pending (useTransition), open (useState false). handleConfirm: startTransition async → deleteCategoryFromListAction(categoryId) → if !result.ok: toast.error + return; toast.success("Категорію видалено"); setOpen(false); router.push("/admin/kategorii") — per D-10. Render: Button with variant="ghost" size="icon" aria-label="Видалити категорію" disabled={pending} onClick={() => setOpen(true)} containing &lt;Trash2 className="size-4" /&gt; — per D-07. AlertDialog open={open} onOpenChange={setOpen} with title "Видалити категорію?", description about irreversibility and product guard, cancel button, destructive confirm button calling handleConfirm — per D-09. Run vitest — all 3 tests must pass.
  </action>

  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npx vitest run --reporter=verbose src/components/admin/category-edit-delete-button.test.tsx</automated>
  </verify>

  <acceptance_criteria>
    - src/components/admin/category-edit-delete-button.tsx exists and exports CategoryEditDeleteButton
    - src/components/admin/category-edit-delete-button.test.tsx exists with 3 test cases
    - `vitest run src/components/admin/category-edit-delete-button.test.tsx` exits 0 with 3 passing
    - Component imports deleteCategoryFromListAction (grep: `deleteCategoryFromListAction` present in file)
    - Component does NOT import deleteCategoryAction (grep: `deleteCategoryAction` not present, or only as part of `deleteCategoryFromListAction`)
    - Button has variant="ghost" size="icon" aria-label="Видалити категорію"
    - AlertDialogTitle renders text "Видалити категорію?"
    - router.push("/admin/kategorii") called on success
    - toast.error called with CATEGORY_HAS_PRODUCTS message on failure
  </acceptance_criteria>

  <done>vitest run exits 0 for category-edit-delete-button.test.tsx; component file is complete and all 3 behaviours verified by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → server action (updateCategoryAction) | Client hook sends watched form values through server action |
| browser → server action (deleteCategoryFromListAction) | Client component sends categoryId through server action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | Elevation of Privilege | updateCategoryAction | accept | requireAdmin() already present in action (verified in category.actions.ts); no change needed in this plan |
| T-40-02 | Elevation of Privilege | deleteCategoryFromListAction | accept | requireAdmin() already present in action (verified in category.actions.ts); no change needed in this plan |
| T-40-03 | Tampering | updateCategoryAction input | accept | updateCategorySchema.parse(input) in action strips unknown fields and validates types; upsertCategorySchema.safeParse guards in hook before action call |
| T-40-04 | Denial of Service | auto-save debounce | mitigate | 500ms debounce + saveChainRef serialization prevent rapid-fire save flooding; at most one in-flight action at a time |
| T-40-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase; all dependencies already in package.json (verified in RESEARCH.md) |
</threat_model>

<verification>
Both vitest test suites pass independently:

```bash
cd /Users/michael_ivashko/WebStormProjects/web/appliance-store
npx vitest run src/hooks/admin/use-category-auto-save.test.ts src/components/admin/category-edit-delete-button.test.tsx
```

Full suite regression check:
```bash
npx vitest run
```
</verification>

<success_criteria>
- `use-category-auto-save.ts` exports useCategoryAutoSave with DEBOUNCE_MS=500, snapshot from safeParse output, generation guard, save chain, SaveStatus type
- `category-edit-delete-button.tsx` uses deleteCategoryFromListAction (not deleteCategoryAction), ghost+icon button, AlertDialog, router.push on success
- 8 total unit tests pass (5 hook + 3 delete button)
- `vitest run` full suite exits 0
</success_criteria>

<output>
Create `.planning/phases/40-category-edit-ux/40-01-SUMMARY.md` when done
</output>
