---
phase: 40-category-edit-ux
plan: 02
type: execute
wave: 2
depends_on: [40-01]
files_modified:
  - src/components/admin/category-edit-header.tsx
  - src/components/admin/category-edit-page-content.tsx
  - src/components/admin/category-form.tsx
  - src/app/(admin)/admin/kategorii/[id]/page.tsx
autonomous: true
requirements: [ADM-CAT-09, ADM-CAT-10]

must_haves:
  truths:
    - "Category edit page shows no Save button and no Delete button in the form body"
    - "Category edit page auto-saves name and sortOrder changes after 500ms quiet period"
    - "Status line shows Збереження… during save and Збережено after, then returns to empty"
    - "Icon-only trash button appears in the top-right corner of the page header"
    - "Back link flushes pending debounced save before navigating to /admin/kategorii"
    - "Переглянути товари and Додати товар action buttons remain and appear below the status line"
    - "CategoryForm create-mode is completely unchanged"
  artifacts:
    - path: "src/components/admin/category-edit-header.tsx"
      provides: "CategoryEditHeader with back link, h1, status line, delete slot, action buttons"
      exports: ["CategoryEditHeader"]
    - path: "src/components/admin/category-edit-page-content.tsx"
      provides: "CategoryEditPageContent client wrapper orchestrating form + header + auto-save"
      exports: ["CategoryEditPageContent"]
    - path: "src/components/admin/category-form.tsx"
      provides: "CategoryForm with mode-conditional buttons and auto-save hook wiring in edit mode"
    - path: "src/app/(admin)/admin/kategorii/[id]/page.tsx"
      provides: "Server page rendering CategoryEditPageContent wrapper instead of bare CategoryForm"
  key_links:
    - from: "src/components/admin/category-edit-page-content.tsx"
      to: "src/components/admin/category-form.tsx"
      via: "onSaveStatusChange + onAutoSaveFlushReady props"
      pattern: "onSaveStatusChange"
    - from: "src/components/admin/category-edit-header.tsx"
      to: "src/components/admin/category-edit-page-content.tsx"
      via: "onNavigateBack prop → flushRef.current?.()"
      pattern: "onNavigateBack"
    - from: "src/app/(admin)/admin/kategorii/\\[id\\]/page.tsx"
      to: "src/components/admin/category-edit-page-content.tsx"
      via: "CategoryEditPageContent import and render"
      pattern: "CategoryEditPageContent"
---

<objective>
Wire together the primitives from Plan 01 into a fully-functional category edit page.
Creates CategoryEditHeader and CategoryEditPageContent (new files), refactors CategoryForm (removes
manual save/delete buttons, wires useCategoryAutoSave in edit mode), and updates the server page
to render the new client wrapper.

Purpose: Deliver the complete ADM-CAT-09 (auto-save) and ADM-CAT-10 (icon-trash header) UX on
the category edit page, making it identical in behaviour to the product edit page.
Output: Fully working /admin/kategorii/[id] page with auto-save and icon-only trash button.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-category-edit-ux/40-CONTEXT.md
@.planning/phases/40-category-edit-ux/40-RESEARCH.md
@.planning/phases/40-category-edit-ux/40-PATTERNS.md
@.planning/phases/40-category-edit-ux/40-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output — executor uses these directly, no exploration needed -->

From src/hooks/admin/use-category-auto-save.ts (created in Plan 01):
  export type SaveStatus = "idle" | "saving" | "saved";
  export function useCategoryAutoSave(options: {
    control: Control&lt;UpsertCategoryInput&gt;;
    categoryId: string;
    enabled: boolean;
    initialValues: UpsertCategoryInput;
  }): { status: SaveStatus; flush: () => void }

From src/components/admin/category-edit-delete-button.tsx (created in Plan 01):
  export function CategoryEditDeleteButton({ categoryId }: { categoryId: string }): JSX.Element
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CategoryEditHeader + CategoryEditPageContent — new client components</name>
  <files>src/components/admin/category-edit-header.tsx, src/components/admin/category-edit-page-content.tsx</files>

  <read_first>
    - src/components/admin/product-edit-header.tsx — exact mirror analog; copy structure substituting category-specific back href, h1 text, and action-button row
    - src/components/admin/product-edit-page-content.tsx — exact mirror analog for wrapper; copy useState/useRef pattern
    - src/app/(admin)/admin/kategorii/[id]/page.tsx — read current page to understand which imports/JSX to move into header (action buttons section, lines 26–49)
    - src/hooks/admin/use-category-auto-save.ts — confirms SaveStatus export path for import
    - src/components/admin/category-edit-delete-button.tsx — confirms export name for import
  </read_first>

  <behavior>
    - CategoryEditHeader renders a back link to /admin/kategorii with ArrowLeft icon; onClick calls onNavigateBack (flush guard)
    - CategoryEditHeader renders h1 "Редагувати категорію" and a p element with aria-live="polite" and className containing "min-h-5 text-sm text-muted-foreground"; text is "Збереження…" when saveStatus="saving", "Збережено" when saveStatus="saved", empty otherwise
    - CategoryEditHeader renders deleteButton prop in top-right position (flex justify-between with h1 block)
    - CategoryEditHeader renders action buttons row below the status line: Button linking to /admin/tovary?categoryId={categoryId} and Button linking to /admin/tovary/novyi?categoryId={categoryId}
    - CategoryEditPageContent initialises saveStatus state as "idle" and a flushRef
    - CategoryEditPageContent passes onNavigateBack={() => flushRef.current?.()} to CategoryEditHeader
    - CategoryEditPageContent passes deleteButton={&lt;CategoryEditDeleteButton categoryId={categoryId} /&gt;} to CategoryEditHeader
    - CategoryEditPageContent passes onSaveStatusChange={setSaveStatus} and onAutoSaveFlushReady={(flush) => { flushRef.current = flush; }} to CategoryForm
  </behavior>

  <action>
    Create src/components/admin/category-edit-header.tsx with "use client" directive (receives onNavigateBack?: () => void from client parent — passing a function prop to a server component violates Next.js serialization rules and fails at runtime). Import Link from "next/link", ArrowLeft + Eye + Plus from "lucide-react", SaveStatus from "@/hooks/admin/use-category-auto-save", Button from "@/components/ui/button". Define CategoryEditHeaderProps: saveStatus: SaveStatus; categoryId: string; deleteButton: React.ReactNode; onNavigateBack?: () => void. Export function CategoryEditHeader. Structure (per D-06 layout): back link with ArrowLeft + "Назад" text, href="/admin/kategorii", onClick={onNavigateBack}; flex row with h1 "Редагувати категорію" and deleteButton slot (flex items-start justify-between gap-4); status p element with min-h-5 text-sm text-muted-foreground transition-opacity duration-200 aria-live="polite" — text "Збереження…" when saving, "Збережено" when saved, null otherwise (per D-03); action buttons row below status (per D-08) with two Buttons using render prop pattern for Link: one to /admin/tovary?categoryId={categoryId} with Eye icon + "Переглянути товари", one to /admin/tovary/novyi?categoryId={categoryId} with Plus icon + "Додати товар", both size="sm".

    Create src/components/admin/category-edit-page-content.tsx with "use client" directive. Import useRef + useState from "react", SaveStatus from "@/hooks/admin/use-category-auto-save", CategoryEditDeleteButton from "@/components/admin/category-edit-delete-button", CategoryEditHeader from "@/components/admin/category-edit-header", CategoryForm from "@/components/admin/category-form". Define CategoryEditPageContentProps: categoryId: string; categoryCount: number; defaultValues: { name: string; sortOrder: number }. Export function CategoryEditPageContent. Body: useState&lt;SaveStatus&gt;("idle") as saveStatus/setSaveStatus; useRef&lt;(() =&gt; void) | null&gt;(null) as flushRef. Build formDefaults as UpsertCategoryInput from defaultValues.name and defaultValues.sortOrder. Render div.space-y-8 containing CategoryEditHeader (props: saveStatus, categoryId, onNavigateBack={() =&gt; flushRef.current?.(), deleteButton=&lt;CategoryEditDeleteButton categoryId={categoryId} /&gt;}) and CategoryForm (props: mode="edit", categoryId, categoryCount, defaultValues=formDefaults, onSaveStatusChange={setSaveStatus}, onAutoSaveFlushReady={(flush) =&gt; { flushRef.current = flush; }}).
  </action>

  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npx tsc --noEmit 2>&1 | grep -E "category-edit-header|category-edit-page-content" || echo "TypeScript: no errors in new files"</automated>
  </verify>

  <acceptance_criteria>
    - src/components/admin/category-edit-header.tsx exists and exports CategoryEditHeader
    - src/components/admin/category-edit-page-content.tsx exists and exports CategoryEditPageContent
    - category-edit-header.tsx has "use client" directive at the top of the file
    - category-edit-header.tsx contains aria-live="polite" in status p element
    - category-edit-header.tsx contains "Збереження…" and "Збережено" as conditional strings (per D-03)
    - category-edit-header.tsx contains href="/admin/kategorii" for back link
    - category-edit-header.tsx contains href including "/admin/tovary?categoryId=" and "/admin/tovary/novyi?categoryId=" (per D-08)
    - category-edit-page-content.tsx contains flushRef and onNavigateBack={() => flushRef.current?.()
    - category-edit-page-content.tsx contains CategoryEditDeleteButton import
    - TypeScript: `tsc --noEmit` produces no errors in these two files
  </acceptance_criteria>

  <done>Both files created with "use client" directives, TypeScript clean, CategoryEditHeader renders complete layout per D-06, CategoryEditPageContent orchestrates state and child components correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor CategoryForm + rewire server page</name>
  <files>src/components/admin/category-form.tsx, src/app/(admin)/admin/kategorii/[id]/page.tsx</files>

  <read_first>
    - src/components/admin/category-form.tsx — current state: understand existing props, onDelete function, window.confirm block, button group at bottom; identify exact lines to remove/add
    - src/components/admin/product-form.tsx — if exists, confirm how onSaveStatusChange + onAutoSaveFlushReady are wired in ProductForm as the pattern to mirror
    - src/app/(admin)/admin/kategorii/[id]/page.tsx — current state: understand full import list and JSX to identify what moves to header vs. what to delete
    - src/server/actions/admin/category.actions.ts — confirm deleteCategoryAction is imported in category-form.tsx (to remove) and updateCategoryAction exists (to use in hook)
    - src/server/validators/category.ts — confirm UpsertCategoryInput type for formDefaults in CategoryEditPageContent
  </read_first>

  <behavior>
    - CategoryForm with mode="edit" renders no Submit button and no Delete button in the button group area
    - CategoryForm with mode="create" still renders a "Зберегти" Submit button (unchanged behaviour)
    - CategoryForm with mode="edit" wires useCategoryAutoSave internally: { status, flush } from hook; calls onSaveStatusChange(status) via useEffect; calls onAutoSaveFlushReady(flush) via useEffect
    - CategoryForm no longer contains window.confirm or any reference to deleteCategoryAction
    - Server page /admin/kategorii/[id]/page.tsx renders CategoryEditPageContent instead of CategoryForm
    - Server page no longer imports Link, Eye, Plus, Button, CategoryForm — those are now owned by the header and wrapper
    - Server page still imports and renders CategoryImageUpload (unchanged section)
  </behavior>

  <action>
    Modify src/components/admin/category-form.tsx:
    1. Remove import of deleteCategoryAction from "@/server/actions/admin/category.actions" (keep updateCategoryAction if used, but the hook will call it — confirm whether the form currently calls it directly in a manual submit or if the hook replaces that; in edit mode the hook replaces it entirely).
    2. Add import of useCategoryAutoSave and SaveStatus from "@/hooks/admin/use-category-auto-save".
    3. Add to CategoryFormProps: onSaveStatusChange?: (status: SaveStatus) => void; and onAutoSaveFlushReady?: (flush: () => void) => void;
    4. Inside the function body: when mode === "edit", call useCategoryAutoSave({ control: form.control, categoryId: categoryId!, enabled: mode === "edit", initialValues: form.getValues() }) → destructure { status, flush }. Add useEffect(() => { onSaveStatusChange?.(status); }, [status, onSaveStatusChange]). Add useEffect(() => { onAutoSaveFlushReady?.(flush); }, [flush, onAutoSaveFlushReady]).
    5. Remove the onDelete async function and any window.confirm block entirely.
    6. Replace the button group at the bottom with: {mode === "create" ? (&lt;Button type="submit" disabled={isSubmitting}&gt;{isSubmitting ? "Збереження…" : "Зберегти"}&lt;/Button&gt;) : null} — no Delete button at all.

    Modify src/app/(admin)/admin/kategorii/[id]/page.tsx:
    1. Remove imports: Link, Eye, Plus from lucide-react (or from next/link), CategoryForm, Button.
    2. Add import: CategoryEditPageContent from "@/components/admin/category-edit-page-content".
    3. Remove the entire header div block (back link + h1 + Переглянути/Додати buttons) that lived in page.tsx — this is now in CategoryEditHeader.
    4. Replace &lt;CategoryForm mode="edit" categoryId={category.id} categoryCount={await getCategoryCount()} defaultValues={...} /&gt; with &lt;CategoryEditPageContent categoryId={category.id} defaultValues={{ name: category.name, sortOrder: category.sortOrder }} categoryCount={await getCategoryCount()} /&gt;.
    5. Keep getCategoryById, getCategoryCount, notFound, CategoryImageUpload exactly as-is.
  </action>

  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npx tsc --noEmit 2>&1 | head -30 && npx vitest run src/hooks/admin/use-category-auto-save.test.ts src/components/admin/category-edit-delete-button.test.tsx</automated>
  </verify>

  <acceptance_criteria>
    - src/components/admin/category-form.tsx does NOT contain "window.confirm" (grep: 0 occurrences)
    - src/components/admin/category-form.tsx does NOT contain "deleteCategoryAction" unqualified import (only deleteCategoryFromListAction is allowed if present; but neither should be needed in form after refactor)
    - src/components/admin/category-form.tsx contains "onSaveStatusChange" prop definition
    - src/components/admin/category-form.tsx contains "onAutoSaveFlushReady" prop definition
    - src/components/admin/category-form.tsx contains "useCategoryAutoSave" call
    - src/components/admin/category-form.tsx button group: grep for `type="submit"` shows it is wrapped in `mode === "create"` condition; no unconditional submit button
    - src/app/(admin)/admin/kategorii/[id]/page.tsx contains "CategoryEditPageContent" import
    - src/app/(admin)/admin/kategorii/[id]/page.tsx does NOT contain "CategoryForm" import
    - src/app/(admin)/admin/kategorii/[id]/page.tsx does NOT contain "window.confirm"
    - src/app/(admin)/admin/kategorii/[id]/page.tsx still contains "CategoryImageUpload"
    - `npx tsc --noEmit` exits 0 (full project TypeScript clean)
    - `npx vitest run` exits 0 (no regressions to existing tests)
  </acceptance_criteria>

  <done>CategoryForm is simplified for edit mode (no manual save/delete buttons), auto-save hook is wired. Server page renders CategoryEditPageContent. TypeScript clean. Full vitest suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → server page | Server fetches category data; passes only id + name + sortOrder to client wrapper |
| client wrapper → server action | CategoryForm auto-save hook sends form values through updateCategoryAction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-05 | Elevation of Privilege | CategoryEditPageContent / CategoryForm (edit) | accept | requireAdmin() in updateCategoryAction already verified; no new auth surface created |
| T-40-06 | Tampering | CategoryForm auto-save (field values to updateCategoryAction) | accept | upsertCategorySchema.safeParse in hook pre-validates before calling action; updateCategorySchema.parse in action validates and strips unknown fields; no raw passthrough |
| T-40-07 | Information Disclosure | Server page data passed to client wrapper | accept | Only non-sensitive fields (name, sortOrder, id) passed as props; no passwords/secrets in category data |
| T-40-08 | Tampering | category-form.tsx losing validation in create mode | mitigate | Verify create-mode onSubmit path remains unchanged — RHF zodResolver(upsertCategorySchema) still guards create; no changes to create-mode submit handler |
| T-40-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase; all dependencies already in package.json |
</threat_model>

<verification>
Full integration verification:

```bash
cd /Users/michael_ivashko/WebStormProjects/web/appliance-store

# TypeScript clean
npx tsc --noEmit

# All tests including new ones
npx vitest run

# Structural spot-checks
grep -c "window.confirm" src/components/admin/category-form.tsx    # must be 0
grep -c "CategoryEditPageContent" src/app/\(admin\)/admin/kategorii/\[id\]/page.tsx  # must be 1
grep -c "CategoryForm" src/app/\(admin\)/admin/kategorii/\[id\]/page.tsx  # must be 0 (removed)
grep -c "onSaveStatusChange" src/components/admin/category-form.tsx  # must be >= 1
```

Manual verification (checkpoint:human-verify — user to run after deploy):
- Visit /admin/kategorii/[id] for an existing category
- Edit the name field — after 500ms, status line shows "Збереження…" then "Збережено"
- No "Зберегти" button visible in the form body
- Trash icon visible in top-right corner of page header
- Click trash → AlertDialog appears → confirm → redirect to /admin/kategorii
- Visit /admin/kategorii/novy (create mode) — "Зберегти" button still present and functional
</verification>

<success_criteria>
- Auto-save works on /admin/kategorii/[id]: name and sortOrder changes debounce 500ms, status line shows Збереження…/Збережено, no Save button in form
- Icon-only trash button (Trash2, ghost, size-icon, aria-label="Видалити категорію") in page header triggers AlertDialog → deleteCategoryFromListAction → router.push("/admin/kategorii")
- Back link calls flush before navigating (no silent data loss)
- Create mode unchanged: /admin/kategorii/novy still has Зберегти submit button
- `npx tsc --noEmit` exits 0
- `npx vitest run` exits 0 (8 new tests + no regressions)
</success_criteria>

<output>
Create `.planning/phases/40-category-edit-ux/40-02-SUMMARY.md` when done
</output>
