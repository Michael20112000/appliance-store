---
phase: 44-mobile-header-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/storefront-auth-links.tsx
autonomous: true
requirements:
  - HDR-02

must_haves:
  truths:
    - "Clicking 'Вийти' immediately disables the button and changes its label to 'Виходимо...'"
    - "The button stays in loading state until authClient.signOut() resolves"
    - "After sign-out resolves, navigation happens (router.push('/')) and the component unmounts, so no stuck loader is visible"
    - "Guest state (Увійти / Реєстрація links) is unaffected by this change"
  artifacts:
    - path: "src/components/layout/storefront-auth-links.tsx"
      provides: "Sign-out button with isPending state"
      contains: "isPending"
  key_links:
    - from: "storefront-auth-links.tsx sign-out button"
      to: "authClient.signOut()"
      via: "await inside onClick — isPending set true before call"
      pattern: "isPending"
---

<objective>
Add a loading/pending state to the "Вийти" sign-out button in storefront-auth-links.tsx so that clicking it immediately shows "Виходимо..." and disables the button until the sign-out request completes.

Purpose: Per HDR-02, users need feedback that sign-out is in progress. Without this, the button appears unresponsive during the network round-trip.
Output: Updated storefront-auth-links.tsx with isPending useState controlling button label and disabled state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key contracts from files this plan touches. -->

From src/components/layout/storefront-auth-links.tsx (current sign-out handler):
  const router = useRouter();
  onClick: async () => {
    await authClient.signOut();
    router.push("/");
    router.refresh();
  }

  Button props currently: variant="outline" size="sm" className="min-h-11"
  -- No disabled prop, no loading text, no useState in the component

From @/components/ui/button:
  Button accepts standard HTML button props including `disabled`
  -- disabled renders the button visually muted and non-interactive

Note: storefront-auth-links.tsx is already "use client" — useState import is available from react.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add isPending state to sign-out button</name>
  <files>src/components/layout/storefront-auth-links.tsx</files>

  <read_first>
    - src/components/layout/storefront-auth-links.tsx — full file; understand current imports, useState usage (none currently), and the sign-out onClick handler before making any changes
  </read_first>

  <action>
    In storefront-auth-links.tsx, make the following changes:

    1. Add useState to the React import. The current import is `import { ... } from "react"` — check if useState is already imported; if not, add it.

    2. Inside the StorefrontAuthLinks function body, add: `const [isPending, setIsPending] = useState(false);`

    3. Replace the current sign-out onClick handler with one that:
       a. Sets isPending to true synchronously before awaiting
       b. Awaits authClient.signOut()
       c. Calls router.push("/") and router.refresh() after resolve
       The handler does NOT set isPending back to false — the component unmounts after navigation, making cleanup unnecessary.

    4. Update the sign-out Button element:
       - Add `disabled={isPending}` prop
       - Change children from the static string "Вийти" to: isPending ? "Виходимо..." : "Вийти"

    Do not change the guest state (the return branch that renders Увійти / Реєстрація links). Do not change any className values. Do not add a spinner element — text change only per the requirement.
  </action>

  <verify>
    <automated>grep -n "isPending\|Виходимо\|setIsPending" src/components/layout/storefront-auth-links.tsx</automated>
  </verify>

  <acceptance_criteria>
    - grep output shows "isPending" appears on at least 3 lines: useState declaration, disabled prop, ternary in button children
    - grep output shows "Виходимо" appears exactly once (in the ternary expression)
    - grep output shows "setIsPending" appears exactly twice: once in useState destructuring, once in the onClick handler (setIsPending(true))
    - The file still contains "Вийти" as the non-pending label in the same ternary expression
    - No line sets setIsPending(false) — pending state is intentionally not reset (component unmounts on navigation)
    - Running `grep "disabled" src/components/layout/storefront-auth-links.tsx` shows disabled={isPending} on the sign-out Button
  </acceptance_criteria>

  <done>
    Sign-out button renders "Вийти" at rest and "Виходимо..." with disabled=true immediately on click.
    The pending state persists until component unmount (after router.push resolves).
    Guest auth links (Увійти / Реєстрація) are unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→auth service | authClient.signOut() issues a network request to invalidate the session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-02-01 | Denial of Service | sign-out button | accept | Button is disabled while pending, preventing double-submit. authClient.signOut() is an existing call — no new attack surface introduced |
| T-44-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After the task completes:

1. Source assertion — pending state: `grep -c "isPending" src/components/layout/storefront-auth-links.tsx` must return 3 or more
2. Source assertion — button disabled: `grep "disabled" src/components/layout/storefront-auth-links.tsx` must show `disabled={isPending}`
3. Source assertion — no false reset: `grep "setIsPending(false)" src/components/layout/storefront-auth-links.tsx` must return empty (exit code 1)
4. Build check: `cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npx tsc --noEmit 2>&1 | head -20` must produce no type errors in storefront-auth-links.tsx
</verification>

<success_criteria>
- storefront-auth-links.tsx has useState(false) for isPending
- Sign-out onClick sets isPending=true before awaiting authClient.signOut()
- Button has disabled={isPending} and children ternary "Виходимо..." : "Вийти"
- No setIsPending(false) call exists (no stuck-loader risk)
- TypeScript compilation produces no errors in storefront-auth-links.tsx
</success_criteria>

<output>
Create `.planning/phases/44-mobile-header-cleanup/44-02-SUMMARY.md` when done
</output>
