---
phase: 44-mobile-header-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/store-header.tsx
autonomous: true
requirements:
  - HDR-01

must_haves:
  truths:
    - "On mobile viewports, no auth buttons ('Увійти', 'Реєстрація', 'Кабінет', 'Вийти') are rendered in the header bar itself"
    - "The burger/menu icon is the rightmost interactive element on mobile — nothing sits to its right"
    - "On desktop (md+), auth buttons remain fully visible; burger remains hidden"
    - "Wishlist and Cart icons appear to the left of the burger on mobile"
  artifacts:
    - path: "src/components/layout/store-header.tsx"
      provides: "Restructured header flex container with correct mobile element order"
      contains: "hidden md:flex"
  key_links:
    - from: "src/components/layout/store-header.tsx"
      to: "src/components/layout/store-mobile-nav.tsx"
      via: "StoreMobileNav rendered last in flex container"
      pattern: "StoreMobileNav"
    - from: "src/components/layout/store-header.tsx"
      to: "src/components/layout/store-header-auth.tsx"
      via: "StoreHeaderAuth wrapped in hidden md:flex div"
      pattern: "hidden md:flex"
---

<objective>
Restructure the store-header.tsx flex container so that on mobile:
- Auth buttons (StoreHeaderAuth) are hidden entirely (available in the drawer instead)
- StoreMobileNav (burger) is the rightmost element

Desktop layout is unchanged: SocialNavLinks + Wishlist + Cart + Auth all visible, burger hidden.

Purpose: Unclutter the mobile header bar per HDR-01. Auth flows on mobile are handled by the drawer that StoreMobileNav already contains.
Output: Updated store-header.tsx with correct element ordering and visibility classes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key exports from files this plan touches. Executor needs no codebase exploration. -->

From src/components/layout/store-mobile-nav.tsx:
  export function StoreMobileNav({ categories, session }: { categories: MobileNavCategory[]; session: StorefrontAuthSession })
  -- SheetTrigger has className="md:hidden" so the burger button is already hidden on desktop
  -- The drawer already contains StorefrontAuthLinks (auth in drawer confirmed)

From src/components/layout/store-header-auth.tsx:
  export function StoreHeaderAuth({ session }: StoreHeaderAuthProps)
  -- Currently rendered without any responsive wrapper in store-header.tsx

From src/components/layout/store-header.tsx (current items div, left to right):
  1. StoreMobileNav (md:hidden burger)
  2. SocialNavLinks (hidden md:flex)
  3. WishlistNavLink
  4. CartNavLink / GuestCartNavLink
  5. StoreHeaderAuth (always visible — NO responsive wrapper)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reorder header items and hide auth on mobile</name>
  <files>src/components/layout/store-header.tsx</files>

  <read_first>
    - src/components/layout/store-header.tsx — full file; understand the items div flex container and current element order before making any changes
    - src/components/layout/store-mobile-nav.tsx — confirm SheetTrigger already has md:hidden; confirm drawer contains StorefrontAuthLinks
  </read_first>

  <action>
    In store-header.tsx, modify only the items div (the div with className="flex items-center gap-2").

    Make two changes:

    1. Wrap StoreHeaderAuth in a div with className="hidden md:flex items-center" so it is invisible on mobile and becomes a flex container on desktop (matching the SocialNavLinks wrapper pattern already in the file).

    2. Move the StoreMobileNav element to be the LAST child inside the items div. The final order of children inside the items div must be:
       a. SocialNavLinks wrapper (hidden md:flex)
       b. WishlistNavLink
       c. CartNavLink / GuestCartNavLink conditional
       d. StoreHeaderAuth wrapper (hidden md:flex items-center) — per HDR-01
       e. StoreMobileNav — LAST, rightmost on mobile

    Do not change any other part of the file. Do not alter props passed to any component. Do not change the desktop nav, the Link logo, or any className outside the items div.
  </action>

  <verify>
    <automated>grep -n "StoreMobileNav\|StoreHeaderAuth\|hidden md:flex" src/components/layout/store-header.tsx</automated>
  </verify>

  <acceptance_criteria>
    - grep output shows StoreHeaderAuth preceded by a line containing "hidden md:flex" on the wrapping div
    - grep output shows StoreMobileNav appears AFTER StoreHeaderAuth in line-number order (StoreMobileNav line number > StoreHeaderAuth line number)
    - The file contains exactly one occurrence of "StoreMobileNav" and it is the last interactive element inside the items div
    - "hidden md:flex" appears at least twice in the file: once on the SocialNavLinks wrapper (already present) and once on the new StoreHeaderAuth wrapper
    - No line in the file renders StoreHeaderAuth without a "hidden md:flex" parent wrapper
  </acceptance_criteria>

  <done>
    On mobile viewports: header shows Logo → [Wishlist] [Cart] [Burger] with no auth buttons.
    On desktop (md+): header shows Logo → [nav links] → [social] [wishlist] [cart] [auth] with no burger.
    StoreHeaderAuth is wrapped in hidden md:flex. StoreMobileNav is the last element in the items div.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→server | Session data flows from server component (StoreHeader) to client components via props |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Information Disclosure | store-header.tsx | accept | Session object passed as prop to client components — already existing pattern, no new exposure introduced by this layout change |
| T-44-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After the task completes, verify the three layout behaviors:

1. Source assertion — auth wrapper: `grep -A1 "StoreHeaderAuth" src/components/layout/store-header.tsx` must show the StoreHeaderAuth JSX element nested inside a div containing "hidden md:flex"
2. Source assertion — burger last: `grep -n "StoreMobileNav\|StoreHeaderAuth" src/components/layout/store-header.tsx` must show StoreMobileNav on a higher line number than StoreHeaderAuth
3. Build check: `cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npx tsc --noEmit 2>&1 | head -20` must produce no new type errors related to store-header.tsx
</verification>

<success_criteria>
- store-header.tsx items div has StoreHeaderAuth wrapped in "hidden md:flex items-center" div
- StoreMobileNav is the final child of the items div
- TypeScript compilation produces no errors in store-header.tsx
- Drawer still receives session prop (StoreMobileNav session={session} unchanged)
</success_criteria>

<output>
Create `.planning/phases/44-mobile-header-cleanup/44-01-SUMMARY.md` when done
</output>
