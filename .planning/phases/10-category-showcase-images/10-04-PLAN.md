---
phase: 10-category-showcase-images
plan: 04
type: execute
wave: 4
depends_on:
  - 10-01
  - 10-03
files_modified:
  - src/components/home/category-grid.tsx
  - prisma/seed.ts
  - prisma/seed-cloudinary.ts
  - .planning/phases/10-category-showcase-images/10-MANUAL-CHECKLIST.md
autonomous: true
requirements:
  - HOME-01
context_decisions:
  - "D-10-04: CategoryGrid 4:3 image area"
  - "D-10-05: OptimizedImage sizes (max-width: 768px) 50vw, 25vw"
  - "D-10-06: placeholder Без фото"
  - "D-10-12: seed only when imagePublicId null"
  - "D-10-14: manual checklist"

must_haves:
  truths:
    - "Homepage category cards show Cloudinary image when imagePublicId set"
    - "Cards without image show muted placeholder text Без фото"
    - "Grid layout and links unchanged from pre-phase-10"
    - "Manual checklist documents upload → homepage → remove flow"
  artifacts:
    - path: src/components/home/category-grid.tsx
      provides: HOME-01 storefront
    - path: .planning/phases/10-category-showcase-images/10-MANUAL-CHECKLIST.md
      provides: D-10-14 verification
  key_links:
    - from: category-grid.tsx
      to: OptimizedImage
      via: imagePublicId
      pattern: "OptimizedImage"

---

<objective>
Homepage CategoryGrid with images + optional seed backfill + phase manual verification (HOME-01, D-10-04–07, D-10-12, D-10-14).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/10-category-showcase-images/10-UI-SPEC.md
@.planning/phases/10-category-showcase-images/10-CONTEXT.md
@src/components/home/category-grid.tsx
@src/components/catalog/product-card.tsx
@prisma/seed-cloudinary.ts
</context>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | Information disclosure | alt text | mitigate | Fallback alt from category.name; escape via React text |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: CategoryGrid image + placeholder (D-10-04–07)</name>
  <read_first>
    - src/components/home/category-grid.tsx — current Card-only layout
    - src/components/catalog/product-card.tsx — image block lines 32-45
    - src/components/media/optimized-image.tsx
    - .planning/phases/10-category-showcase-images/10-UI-SPEC.md — 4:3, sizes, placeholder
  </read_first>
  <files>src/components/home/category-grid.tsx</files>
  <action>
    Refactor each category card:
    - Outer Link to `/katalog/${category.slug}` unchanged
    - Card with overflow-hidden pt-0 (match ProductCard)
    - Top: div relative aspect-[4/3] min-h-48 w-full bg-muted
      - If category.imagePublicId: OptimizedImage fill object-cover sizes="(max-width: 768px) 50vw, 25vw" alt={category.imageAlt?.trim() || `${category.name} — категорія, Львів`}
      - Else: centered text-sm text-muted-foreground «Без фото»
    - CardHeader below image: CardTitle category.name, CardDescription «Переглянути» (keep Ukrainian copy)
    - Remove duplicate title-only layout without image area
    prisma findMany already returns all scalar fields — no query change unless select was narrowed
  </action>
  <verify>
    <automated>grep -q 'OptimizedImage' src/components/home/category-grid.tsx &amp;&amp; grep -q 'Без фото' src/components/home/category-grid.tsx &amp;&amp; grep -q 'aspect-\[4/3\]' src/components/home/category-grid.tsx</automated>
  </verify>
  <acceptance_criteria>
    - category-grid.tsx uses OptimizedImage when imagePublicId present
    - Placeholder string exactly «Без фото»
    - sizes attribute matches D-10-05
  </acceptance_criteria>
  <done>HOME-01 UI complete</done>
</task>

<task type="auto">
  <name>Task 2: Optional seed backfill (D-10-12)</name>
  <read_first>
    - prisma/seed-cloudinary.ts — ensureCategorySeedImage
    - prisma/seed.ts — category upsert loop if present
  </read_first>
  <files>prisma/seed.ts, prisma/seed-cloudinary.ts</files>
  <action>
    After categories exist in seed, for each category where imagePublicId is null:
    - call ensureCategorySeedImage(category.slug, 0) to get publicId
    - update category row set imagePublicId only (do not overwrite non-null)
    Skip entire block when SEED_SKIP_IMAGE_UPLOAD=1 (use deterministic public_id string only without DB write if upload skipped — discretion: still set public_id string from seedImagePublicId helper without upload).
    Do NOT run seed in production plan execution unless operator runs seed manually.
  </action>
  <verify>
    <automated>grep -q 'imagePublicId' prisma/seed.ts || grep -q 'imagePublicId' prisma/seed-cloudinary.ts</automated>
  </verify>
  <acceptance_criteria>
    - Seed path only updates when imagePublicId is null
    - No overwrite of existing admin-set images in seed logic
  </acceptance_criteria>
  <done>Dev seed optional path documented in SUMMARY</done>
</task>

<task type="auto">
  <name>Task 3: Manual checklist + Vitest gate (D-10-14, D-10-13)</name>
  <read_first>
    - .planning/phases/09-wishlist/09-MANUAL-CHECKLIST.md — format
    - .planning/phases/10-category-showcase-images/10-CONTEXT.md — D-10-14 scenarios
  </read_first>
  <files>.planning/phases/10-category-showcase-images/10-MANUAL-CHECKLIST.md</files>
  <action>
    Create 10-MANUAL-CHECKLIST.md with checkboxes:
    1. Homepage shows «Без фото» for category without imagePublicId
    2. Admin upload image on /admin/kategorii/[id] → save → homepage card shows image (hard refresh if needed)
    3. Admin «Прибрати фото» → homepage returns to placeholder
    4. Existing categories without upload still render grid and links
    5. Alt fallback readable in devtools when imageAlt empty
    Run full `npm test` and record pass in checklist header.
  </action>
  <verify>
    <automated>test -f .planning/phases/10-category-showcase-images/10-MANUAL-CHECKLIST.md &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - 10-MANUAL-CHECKLIST.md exists with 5+ items
    - npm test exits 0
  </acceptance_criteria>
  <done>Phase verification artifacts ready</done>
</task>

</tasks>

<verification>
- [ ] `npm test` full suite green
- [ ] Manual checklist completed by operator before phase close
</verification>

## PLAN COMPLETE
