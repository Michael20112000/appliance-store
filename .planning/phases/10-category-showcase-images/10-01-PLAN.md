---
phase: 10-category-showcase-images
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/
  - src/server/validators/category.ts
  - src/server/validators/category.test.ts
autonomous: false
requirements:
  - HOME-01
  - HOME-02
context_decisions:
  - "D-10-01: Category.imagePublicId String?"
  - "D-10-02: Category.imageAlt String? optional"
  - "D-10-03: nullable migration, no backfill in SQL"
  - "D-10-13: validator unit tests"

must_haves:
  truths:
    - "Category model has nullable imagePublicId and imageAlt"
    - "Existing category rows survive migration without required image"
    - "updateCategoryImageSchema validates cuid and optional clear via null"
  artifacts:
    - path: prisma/schema.prisma
      provides: Category image fields
    - path: src/server/validators/category.ts
      provides: updateCategoryImageSchema
    - path: src/server/validators/category.test.ts
      provides: Zod coverage for image payload
  key_links:
    - from: prisma/schema.prisma
      to: Category
      via: imagePublicId String?
      pattern: "imagePublicId"

---

<objective>
Prisma schema for category showcase images and Zod validator foundation (D-10-01–03, D-10-13).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-category-showcase-images/10-CONTEXT.md
@.planning/phases/10-category-showcase-images/10-RESEARCH.md
@.planning/phases/10-category-showcase-images/10-PATTERNS.md
@prisma/schema.prisma
@src/server/validators/category.ts
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PostgreSQL migration | Schema change on deploy; nullable only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Tampering | migration | mitigate | Nullable columns; no NOT NULL backfill |
| T-10-01-SC | Tampering | prisma migrate | mitigate | `npx prisma migrate dev --name category_image` |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add Category image fields (D-10-01, D-10-02)</name>
  <read_first>
    - prisma/schema.prisma — model Category (lines ~105–114)
    - .planning/phases/10-category-showcase-images/10-CONTEXT.md — D-10-01, D-10-02
  </read_first>
  <files>prisma/schema.prisma</files>
  <action>
    On model Category add:
    - imagePublicId String? (Cloudinary public_id)
    - imageAlt String? (optional a11y text)
    Do NOT rename to cloudinaryPublicId. Do NOT add ProductImage-style relation table.
  </action>
  <verify>
    <automated>grep -q 'imagePublicId' prisma/schema.prisma &amp;&amp; grep -q 'imageAlt' prisma/schema.prisma &amp;&amp; npx prisma validate</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma validate` exits 0
    - Category has both optional String fields
  </acceptance_criteria>
  <done>schema.prisma updated</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>[BLOCKING] Prisma migrate — category_image</name>
  <read_first>
    - prisma/migrations/ — prior migration naming pattern
    - .planning/phases/09-wishlist/09-01-PLAN.md — migrate checkpoint pattern
  </read_first>
  <files>prisma/migrations/</files>
  <action>
    Run: `npx prisma migrate dev --name category_image`
    Then: `npx prisma generate`
    Migration SQL must only ADD nullable columns; no UPDATE backfill.
    If non-interactive env blocks migrate, operator runs locally and confirms folder created.
  </action>
  <verify>
    <automated>test -d prisma/migrations &amp;&amp; ls prisma/migrations/*category_image* 2>/dev/null | head -1</automated>
  </verify>
  <acceptance_criteria>
    - Migration folder exists for category_image
    - `npx prisma generate` succeeds
  </acceptance_criteria>
  <done>DB schema matches Prisma file</done>
</task>

<task type="auto">
  <name>Task 2: updateCategoryImageSchema + tests (D-10-13)</name>
  <read_first>
    - src/server/validators/category.ts — updateCategorySchema pattern
    - src/server/validators/admin-product.ts — cloudinaryPublicId string validation if present
    - src/server/validators/category.test.ts — existing tests or create file
  </read_first>
  <files>src/server/validators/category.ts, src/server/validators/category.test.ts</files>
  <action>
    Export `updateCategoryImageSchema` as z.object({
      categoryId: z.string().cuid("Невірний ідентифікатор категорії"),
      imagePublicId: z.string().trim().min(1).nullable(),
      imageAlt: z.union([z.string().trim().max(500), z.literal("")]).optional(),
    }).transform(data => ({
      categoryId: data.categoryId,
      imagePublicId: data.imagePublicId,
      imageAlt: data.imageAlt === "" ? null : data.imageAlt ?? null,
    }))
    Export type UpdateCategoryImageValues.
    Add Vitest: accepts valid cuid + public_id; rejects invalid cuid; accepts imagePublicId null for clear; rejects empty string when not null.
  </action>
  <verify>
    <automated>npm test -- src/server/validators/category.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - category.test.ts exits 0
    - Schema file exports updateCategoryImageSchema
  </acceptance_criteria>
  <done>Validator ready for service layer</done>
</task>

</tasks>

<verification>
- [ ] `npx prisma validate` green
- [ ] Migration applied locally
- [ ] `npm test -- src/server/validators/category.test.ts -x` green
</verification>

## PLAN COMPLETE
