---
phase: 10-category-showcase-images
plan: 03
type: execute
wave: 3
depends_on:
  - 10-02
files_modified:
  - src/components/admin/category-image-upload.tsx
  - src/app/(admin)/admin/kategorii/[id]/page.tsx
autonomous: true
requirements:
  - HOME-02
context_decisions:
  - "D-10-08: CategoryImageUpload single file CldUploadWidget"
  - "D-10-10: section Зображення категорії below CategoryForm"
  - "D-10-11: persist via updateCategoryImageAction"

must_haves:
  truths:
    - "Admin edit category page shows upload, preview, remove for one image"
    - "Upload uses signed /api/upload/sign (same as products)"
    - "Removing image clears DB field and refreshes preview"
  artifacts:
    - path: src/components/admin/category-image-upload.tsx
      provides: client upload UI
    - path: src/app/(admin)/admin/kategorii/[id]/page.tsx
      provides: wires uploader with category id + initial values
  key_links:
    - from: category-image-upload.tsx
      to: updateCategoryImageAction
      via: server action on upload/remove
      pattern: "updateCategoryImageAction"

---

<objective>
Admin category image upload UI mirroring ProductImageUpload with maxFiles: 1 (HOME-02, D-10-08–10).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/10-category-showcase-images/10-UI-SPEC.md
@.planning/phases/10-category-showcase-images/10-CONTEXT.md
@src/components/admin/product-image-upload.tsx
@src/app/(admin)/admin/kategorii/[id]/page.tsx
@src/server/actions/admin/category.actions.ts
</context>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Spoofing | CldUploadWidget | mitigate | signatureEndpoint only; no unsigned preset |
| T-10-03-02 | Information disclosure | env guard | mitigate | isUploadWidgetConfigured() Alert when keys missing |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: CategoryImageUpload component (D-10-08)</name>
  <read_first>
    - src/components/admin/product-image-upload.tsx — full file (CldUploadWidget, extractUploadInfo, isUploadWidgetConfigured)
    - .planning/phases/10-category-showcase-images/10-UI-SPEC.md — admin section, copy, 4:3 preview 160×120
  </read_first>
  <files>src/components/admin/category-image-upload.tsx</files>
  <action>
    Create client component `CategoryImageUpload` with props:
    - categoryId: string
    - initialImagePublicId: string | null
    - initialImageAlt: string | null
    State: current publicId (nullable), alt string, error, isPending (useTransition).
    Copy isUploadWidgetConfigured and extractUploadInfo from product-image-upload.tsx.
    CldUploadWidget: signatureEndpoint="/api/upload/sign", options multiple false maxFiles 1 sources ["local"].
    onSuccess: call updateCategoryImageAction({ categoryId, imagePublicId: public_id, imageAlt: alt or null }).
    Preview: when publicId set, CldImage aspect 4:3 w-40 rounded-md border; alt Input optional max 500 on blur save.
  Buttons (Ukrainian per UI-SPEC):
    - «Завантажити зображення» outline opens widget
    - «Прибрати фото» destructive calls action with imagePublicId: null
    Disabled states when isPending or widget not configured (Alert same copy as product uploader).
    Do NOT delete asset from Cloudinary API on remove (DB null only).
  </action>
  <verify>
    <automated>test -f src/components/admin/category-image-upload.tsx &amp;&amp; grep -q 'CldUploadWidget' src/components/admin/category-image-upload.tsx &amp;&amp; grep -q 'updateCategoryImageAction' src/components/admin/category-image-upload.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Component file exists with maxFiles 1 pattern
    - Uses updateCategoryImageAction not raw fetch to DB
  </acceptance_criteria>
  <done>Uploader component ready</done>
</task>

<task type="auto">
  <name>Task 2: Wire admin edit page (D-10-10)</name>
  <read_first>
    - src/app/(admin)/admin/kategorii/[id]/page.tsx
    - .planning/phases/10-category-showcase-images/10-UI-SPEC.md — section divider mt-8 pt-8 border-t
  </read_first>
  <files>src/app/(admin)/admin/kategorii/[id]/page.tsx</files>
  <action>
    After getCategoryById, pass to page:
    - CategoryForm unchanged
    - New section below with h2 «Зображення категорії» className text-lg font-semibold, wrapper mt-8 pt-8 border-t border-border space-y-6
    - CategoryImageUpload with categoryId, initialImagePublicId: category.imagePublicId ?? null, initialImageAlt: category.imageAlt ?? null
    Ensure getCategoryById return type includes new fields (Prisma auto).
  </action>
  <verify>
    <automated>grep -q 'CategoryImageUpload' src/app/\(admin\)/admin/kategorii/\[id\]/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Edit page imports and renders CategoryImageUpload
    - Section title «Зображення категорії» present
  </acceptance_criteria>
  <done>Admin route complete for HOME-02</done>
</task>

</tasks>

<verification>
- [ ] Manual: open /admin/kategorii/[id], upload shows preview (deferred to 10-04 checklist)
- [ ] Typecheck: `npx tsc --noEmit` or project lint if available
</verification>

## PLAN COMPLETE
