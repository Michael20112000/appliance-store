---
phase: 10-category-showcase-images
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/server/services/admin-catalog.service.ts
  - src/server/services/admin-catalog.service.test.ts
  - src/server/actions/admin/category.actions.ts
autonomous: true
requirements:
  - HOME-01
  - HOME-02
context_decisions:
  - "D-10-09: updateCategoryImageAction + service"
  - "D-10-11: revalidatePath(\"/\") on image save"
  - "D-10-13: service unit tests"

must_haves:
  truths:
    - "Admin can persist or clear category imagePublicId via server action"
    - "Homepage revalidates when category image changes"
    - "Non-admin cannot call updateCategoryImageAction"
  artifacts:
    - path: src/server/services/admin-catalog.service.ts
      provides: updateCategoryImage(categoryId, imagePublicId, imageAlt?)
    - path: src/server/actions/admin/category.actions.ts
      provides: updateCategoryImageAction
  key_links:
    - from: category.actions.ts
      to: revalidatePath("/")
      via: revalidateCategoryPaths
      pattern: 'revalidatePath\("/"\)'

---

<objective>
Server persistence and cache invalidation for category images (D-10-09, D-10-11, HOME-02).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/10-category-showcase-images/10-CONTEXT.md
@.planning/phases/10-category-showcase-images/10-RESEARCH.md
@src/server/actions/admin/product.actions.ts
@src/server/actions/admin/category.actions.ts
@src/server/services/admin-catalog.service.ts
</context>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | Elevation | updateCategoryImageAction | mitigate | requireAdmin() first line |
| T-10-02-02 | Spoofing | imagePublicId | mitigate | Zod min length; admin-only write |
| T-10-02-03 | Tampering | imageAlt | mitigate | max 500 chars |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: updateCategoryImage service</name>
  <read_first>
    - src/server/services/admin-catalog.service.ts — updateCategory, CATEGORY_NOT_FOUND
    - src/server/validators/category.ts — updateCategoryImageSchema output type
  </read_first>
  <files>src/server/services/admin-catalog.service.ts, src/server/services/admin-catalog.service.test.ts</files>
  <action>
    Add `updateCategoryImage(input: { categoryId: string; imagePublicId: string | null; imageAlt?: string | null })`:
    - findUnique by id; if missing throw CATEGORY_NOT_FOUND
    - prisma.category.update with imagePublicId and imageAlt (null allowed)
    - return updated category with slug for revalidation
    Vitest (mock prisma or integration per project pattern in admin-product.service.test.ts):
    - updates imagePublicId when category exists
    - sets null on clear
    - throws CATEGORY_NOT_FOUND for unknown id
  </action>
  <verify>
    <automated>npm test -- src/server/services/admin-catalog.service.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - Service tests pass
    - updateCategoryImage exported from admin-catalog.service.ts
  </acceptance_criteria>
  <done>Service layer complete</done>
</task>

<task type="auto">
  <name>Task 2: updateCategoryImageAction + revalidate homepage (D-10-11)</name>
  <read_first>
    - src/server/actions/admin/category.actions.ts — revalidateCategoryPaths, updateCategoryAction
    - src/lib/permissions.ts — requireAdmin
  </read_first>
  <files>src/server/actions/admin/category.actions.ts</files>
  <action>
    Extend `revalidateCategoryPaths(slug?: string)` to always call:
    - revalidatePath("/")
    - revalidatePath("/admin/kategorii")
    - revalidatePath("/katalog")
    - if slug: revalidatePath(`/katalog/${slug}`)
    Add `updateCategoryImageAction(input: unknown)`:
    - await requireAdmin()
    - parse with updateCategoryImageSchema
    - call updateCategoryImage
    - revalidateCategoryPaths(category.slug)
    - return { ok: true as const }
    - catch map CATEGORY_NOT_FOUND → same error codes as other category actions
  </action>
  <verify>
    <automated>grep -q 'revalidatePath("/")' src/server/actions/admin/category.actions.ts &amp;&amp; grep -q 'updateCategoryImageAction' src/server/actions/admin/category.actions.ts</automated>
  </verify>
  <acceptance_criteria>
    - revalidateCategoryPaths includes revalidatePath("/")
    - updateCategoryImageAction exported and uses requireAdmin
  </acceptance_criteria>
  <done>Action + cache invalidation wired</done>
</task>

</tasks>

<verification>
- [ ] `npm test -- src/server/services/admin-catalog.service.test.ts -x` green
- [ ] grep confirms homepage revalidate
</verification>

## PLAN COMPLETE
