---
phase: 05-realtime-chat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - prisma/schema.prisma
  - prisma/migrations/
  - src/lib/env.ts
  - src/lib/env.test.ts
  - src/lib/pusher-server.ts
  - src/lib/pusher-client.ts
  - src/types/chat.ts
  - src/server/validators/chat.ts
  - src/server/validators/chat.test.ts
  - src/server/services/chat.service.ts
  - src/server/services/chat.service.test.ts
autonomous: true
requirements:
  - CHAT-03
  - CHAT-02
context_decisions:
  - "D-05-01: Conversation.userId @unique — one thread per buyer"
  - "D-05-02: contextProductId + contextProductTitle optional on Conversation; no per-product threads"
  - "D-05-13: Pusher Channels via pusher@5.3.3 + pusher-js@8.5.0"
  - "D-05-14: DB-first send contract in chat.service (trigger deferred to 05-02 route)"
  - "D-05-18: body max 2000 chars; rate limit 20 messages / 60s per senderId (RESEARCH A1)"
  - "D-05-20: MessageSender.STORE for admin replies; UI label «Магазин» in 05-03/04"

must_haves:
  truths:
    - "PostgreSQL stores Conversation and Message rows with correct indexes"
    - "chat.service can get-or-create a buyer conversation and list message history"
    - "Pusher server/client singletons exist and env schema documents required keys"
  artifacts:
    - path: prisma/schema.prisma
      provides: Conversation, Message, MessageSender enum
      contains: model Conversation
    - path: src/server/services/chat.service.ts
      provides: getOrCreateConversation, listMessages, sendMessage (returns DTO for trigger), countUnreadForAdmin, markAdminRead, markBuyerRead
      exports: ["getOrCreateConversation", "listMessages", "sendMessage", "countUnreadForAdmin"]
    - path: src/lib/pusher-server.ts
      provides: getPusherServer, conversationChannel
      exports: ["getPusherServer", "conversationChannel"]
  key_links:
    - from: src/server/services/chat.service.ts
      to: prisma.message
      via: create inside $transaction before external trigger
      pattern: prisma\.\$transaction
    - from: src/lib/pusher-server.ts
      to: process.env
      via: PUSHER_APP_ID, PUSHER_KEY, PUSHER_SECRET, PUSHER_CLUSTER
      pattern: PUSHER_APP_ID

---

## Phase Goal

**As a** авторизований покупець, **I want to** мати один постійний діалог з магазином у базі даних, **so that** наступні кроки (API, віджет, адмін-інбокс) працюють на єдиному джерелі правди.

<objective>
Додати Prisma Conversation/Message, **[BLOCKING] міграцію**, `chat.service` з unread/read cursors, Zod validators, Pusher singletons, npm deps і unit-тести.

Purpose: Data + domain layer для realtime чату (CHAT-03 backbone, CHAT-02 infra).
Output: Migrated schema, tested chat.service, pusher-server/client stubs, env vars in schema.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-realtime-chat/05-CONTEXT.md
@.planning/phases/05-realtime-chat/05-RESEARCH.md
@.planning/phases/05-realtime-chat/05-PATTERNS.md
@prisma/schema.prisma
@src/lib/db.ts
@src/server/services/cart.service.ts
@src/lib/cloudinary.ts

<interfaces>
From src/lib/permissions.ts (unchanged this plan; used in 05-02):
- requireBuyer(callbackPath), requireAdmin(), assertAdminApi()

From src/server/services/cart.service.ts — copy getOrCreate P2002 pattern:
- getOrCreateCart(userId) → getOrCreateConversation(userId)

Prisma models per 05-RESEARCH Pattern 1 (use userId not buyerId):
- enum MessageSender { BUYER, STORE }
- Conversation: userId @unique, contextProductId?, contextProductTitle?, buyerLastReadAt, adminLastReadAt, lastMessageAt?, lastMessagePreview?, lastMessageSender?
- Message: conversationId, senderId, senderRole, body, @@index([conversationId, createdAt])

DTOs in src/types/chat.ts:
- MessageDto: id, conversationId, body, senderRole, senderId, createdAt
- ConversationSummaryDto: id, userId, buyerName, buyerEmail, lastMessagePreview, lastMessageAt, unreadForAdmin
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Pusher + Prisma chat schema + [BLOCKING] migrate</name>
  <read_first>
    - 05-RESEARCH.md — Package Legitimacy Audit (pusher 5.3.3, pusher-js 8.5.0 approved)
    - 05-RESEARCH.md — Pattern 1 Prisma block
    - prisma/schema.prisma — existing enums/models style
  </read_first>
  <files>package.json, package-lock.json, prisma/schema.prisma, prisma/migrations/</files>
  <action>
    Run `npm install pusher@5.3.3 pusher-js@8.5.0` (official Pusher SDKs per RESEARCH; no other chat packages).
    Add enum MessageSender and models Conversation, Message exactly per 05-RESEARCH Pattern 1 (userId @unique on Conversation; no orderId; cascade delete messages).
    Do NOT add datasource url to schema.prisma (Prisma 7 — url in prisma.config.ts).
    **[BLOCKING]** Run `npx prisma migrate dev --name add_chat` with DIRECT_URL set (same as Phase 3 cart migration).
    Run `npx prisma generate`.
    Production/CI note in SUMMARY: apply with `npx prisma migrate deploy` (non-TTY).
    Phase verification CANNOT pass until migration is applied on dev DB.
  </action>
  <acceptance_criteria>
    - Migration folder exists for add_chat
    - prisma validate exits 0
    - Generated client exports Conversation, Message, MessageSender
    - package.json lists pusher and pusher-js at pinned versions
  </acceptance_criteria>
  <verify>
    <automated>npx prisma validate && npx prisma migrate status 2>&1 | grep -v '^#' | grep -c add_chat | awk '{exit ($1>=1)?0:1}'</automated>
  </verify>
  <done>Conversation and Message tables exist; Pusher packages installed</done>
</task>

<task type="auto">
  <name>Task 2: chat types, validators, env + Pusher singletons</name>
  <read_first>
    - 05-PATTERNS.md — validators/chat.ts analog cart.ts
    - 05-PATTERNS.md — pusher-server analog cloudinary.ts
    - src/lib/env.ts — optional Cloudinary pattern
    - src/lib/cloudinary.ts — CloudinaryNotConfiguredError pattern
  </read_first>
  <files>
    src/types/chat.ts
    src/server/validators/chat.ts
    src/server/validators/chat.test.ts
    src/lib/env.ts
    src/lib/env.test.ts
    src/lib/pusher-server.ts
    src/lib/pusher-client.ts
  </files>
  <action>
    Create src/types/chat.ts with MessageDto and ConversationSummaryDto (serializable for RSC/API).
    Create sendMessageSchema: body trim min 1 max 2000 (Ukrainian Zod messages per D-05-18); optional conversationId cuid (admin); optional productId cuid (buyer context for D-05-02).
    Create chat.test.ts: reject empty, reject &gt;2000, accept trimmed body.
    Extend env.ts: PUSHER_APP_ID, PUSHER_KEY, PUSHER_SECRET, PUSHER_CLUSTER optional on server; NEXT_PUBLIC_PUSHER_KEY, NEXT_PUBLIC_PUSHER_CLUSTER optional on client. Extend env.test.ts with parse fixture including Pusher keys.
    Create pusher-server.ts: lazy singleton, PusherNotConfiguredError (mirror Cloudinary), export getPusherServer() and conversationChannel(id) returning `private-conversation-${id}`.
    Create pusher-client.ts with top comment: import ONLY from client components. Export getPusherClient() singleton with channelAuthorization endpoint `/api/chat/pusher/auth` (D-05-15, D-05-16) — route implemented in 05-02.
    Do NOT call trigger from client.
  </action>
  <acceptance_criteria>
    - sendMessageSchema enforces 2000 char max
    - getPusherServer throws PusherNotConfiguredError when secrets missing
    - env tests pass with Pusher vars
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/server/validators/chat.test.ts src/lib/env.test.ts</automated>
  </verify>
  <done>Validators and Pusher/env infrastructure ready for API routes</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: chat.service + unit tests (DB-first, unread, rate limit)</name>
  <read_first>
    - 05-RESEARCH.md — Pattern 1 unread logic, Pattern 6 rate limit
    - 05-PATTERNS.md — chat.service analog cart.service + admin-order.service
    - src/server/services/admin-order.service.test.ts — Vitest prisma mock style
  </read_first>
  <files>src/server/services/chat.service.ts, src/server/services/chat.service.test.ts</files>
  <behavior>
    - getOrCreateConversation: returns existing or creates; handles P2002 race like cart
    - sendMessage: rejects when rate limit exceeded (20 in 60s per senderId)
    - sendMessage: updates lastMessage* denorm fields and returns MessageDto
    - listMessages: returns last 50 ordered asc by createdAt for conversationId
    - countUnreadForAdmin: counts conversations where lastMessageSender BUYER and lastMessageAt &gt; adminLastReadAt
    - assertConversationAccess: buyer only own userId; admin any conversation
  </behavior>
  <action>
    Implement chat.service.ts (prisma from @/lib/db` only; no React):
    - getOrCreateConversation(userId, optional { productId, productTitle } for D-05-02 — set context fields only when still null)
    - getConversationForBuyer(userId)
    - listMessages(conversationId, { limit: 50 })
    - sendMessage({ senderId, senderRole, conversationId?, userId?, body, productContext? }) — transaction: create message, update conversation denorm; export MessageDto; do NOT import pusher here (route calls trigger after service returns per D-05-14)
    - enforceRateLimit(senderId) — prisma.message.count last 60s; throw ChatRateLimitError constant
    - markAdminRead(conversationId), markBuyerRead(conversationId)
    - listConversationsForAdmin() — orderBy lastMessageAt desc nulls last, include buyer user name/email via separate user lookup or denorm preview only
    - countUnreadForAdmin()
    - parseConversationChannel(channelName) — regex validate private-conversation-{cuid}
    - assertConversationAccess(session, conversationId)
    Export error constants: CONVERSATION_NOT_FOUND, FORBIDDEN, CHAT_RATE_LIMIT.
    Mock prisma in chat.service.test.ts; mock pusher NOT needed in this plan.
    Do NOT subscribe to Pusher in service layer.
  </action>
  <acceptance_criteria>
    - npm test passes chat.service.test.ts
    - Rate limit test fails on 21st message in window
    - Unread count logic covered for admin badge
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/server/services/chat.service.test.ts src/server/validators/chat.test.ts</automated>
  </verify>
  <done>Domain layer complete; ready for POST routes in 05-02</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chat.service → Postgres | Messages keyed by conversationId; buyer scoped by userId |
| env → Pusher | Server secrets never exposed to client bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Elevation | getOrCreateConversation | mitigate | userId from session only in routes (05-02); unique constraint |
| T-05-01-02 | Denial of Service | sendMessage rate limit | mitigate | Prisma count 20/60s per senderId in chat.service |
| T-05-01-03 | Tampering | sendMessageSchema | mitigate | Zod max 2000 trim; reject empty body |
| T-05-01-SC | Tampering | npm install pusher | mitigate | Official Pusher packages per RESEARCH audit; pinned versions |

</threat_model>

<verification>
npx prisma migrate status
npm test -- src/server/services/chat.service.test.ts src/server/validators/chat.test.ts src/lib/env.test.ts
</verification>

<success_criteria>
Migration add_chat applied; chat.service tested; Pusher singletons and env schema exist; packages installed
</success_criteria>

<output>
Create `.planning/phases/05-realtime-chat/05-01-SUMMARY.md` when done
</output>
