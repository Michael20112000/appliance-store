---
phase: 05-realtime-chat
plan: 05
type: execute
wave: 4
depends_on:
  - 05-03
  - 05-04
files_modified:
  - .env.example
  - e2e/chat-auth.spec.ts
  - e2e/chat-persistence.spec.ts
  - e2e/chat-widget.spec.ts
  - e2e/admin-chat.spec.ts
  - e2e/helpers/pusher.ts
  - e2e/admin-rbac.spec.ts
autonomous: true
requirements:
  - CHAT-01
  - CHAT-02
  - CHAT-03
  - CHAT-04
  - AUTH-03
context_decisions:
  - "D-05-04: E2E guest FAB → /uviity?callbackUrl="
  - "D-05-17: Live Pusher E2E optional via hasPusherSecrets() — persistence E2E required"
  - "RESEARCH: E2E asserts DB persistence + auth gates; skip realtime when no secrets"

must_haves:
  truths:
    - "Playwright proves guest cannot use chat without login"
    - "Playwright proves message text survives page reload"
    - "Playwright proves buyer blocked from /admin/chaty"
    - ".env.example documents all Pusher variables"
  artifacts:
    - path: e2e/chat-auth.spec.ts
      provides: guest redirect tests
    - path: e2e/chat-persistence.spec.ts
      provides: reload persistence test
    - path: e2e/helpers/pusher.ts
      provides: hasPusherSecrets()
    - path: .env.example
      provides: Pusher env documentation
  key_links:
    - from: e2e/chat-persistence.spec.ts
      to: /api/chat/messages
      via: UI send + reload assert text
      pattern: chat-persistence

---

## Phase Goal

**As a** команда магазину, **I want to** автоматичні тести на чат і документовані env, **so that** Phase 5 не регресує при deploy і CI.

<objective>
E2E suite для CHAT-01–04 + AUTH-03; optional live Pusher tests; .env.example; extend admin RBAC spec.

Purpose: Phase gate per 05-VALIDATION.md; production env docs.
Output: Green required e2e; optional realtime when secrets present.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-realtime-chat/05-VALIDATION.md
@e2e/cart-auth.spec.ts
@e2e/admin-rbac.spec.ts
@e2e/helpers/admin.ts
@.env.example

<interfaces>
From e2e/cart-auth.spec.ts:
- test guest redirect with callbackUrl regex

From e2e/helpers/admin.ts:
- loginAsAdmin(page)
- hasCloudinarySecrets() pattern → hasPusherSecrets()

Playwright config already loads dotenv/config (04-05).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: E2E auth gates + helper</name>
  <read_first>
    - e2e/cart-auth.spec.ts
    - 05-VALIDATION.md — CHAT-01 guest FAB row
  </read_first>
  <files>e2e/chat-auth.spec.ts, e2e/helpers/pusher.ts</files>
  <action>
    Create e2e/helpers/pusher.ts: export hasPusherSecrets() — true when PUSHER_APP_ID, PUSHER_KEY, PUSHER_SECRET, PUSHER_CLUSTER, NEXT_PUBLIC_PUSHER_KEY, NEXT_PUBLIC_PUSHER_CLUSTER all non-empty (mirror hasCloudinarySecrets style from e2e/helpers/admin.ts).
    Create e2e/chat-auth.spec.ts:
    - Guest: goto storefront home, click FAB aria-label «Відкрити чат з магазином» → expect URL /uviity?callbackUrl=
    - Guest: POST /api/chat/messages without cookies → expect 401
    - Authenticated buyer (use existing test user seed / login helper from cart e2e): open /kabinet, click «Відкрити чат» → expect chat panel title «Чат з магазином» visible
    Do NOT require Pusher for these tests.
  </action>
  <acceptance_criteria>
    - chat-auth.spec.ts runs in CI without Pusher secrets
    - Guest redirect and 401 covered
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test e2e/chat-auth.spec.ts --reporter=line</automated>
  </verify>
  <done>CHAT-01 + AUTH-03 automated gates</done>
</task>

<task type="auto">
  <name>Task 2: E2E persistence + widget + admin RBAC</name>
  <read_first>
    - e2e/checkout.spec.ts or orders-history — reload patterns
    - e2e/admin-rbac.spec.ts
    - 05-VALIDATION.md — persistence and admin rows
  </read_first>
  <files>
    e2e/chat-persistence.spec.ts
    e2e/chat-widget.spec.ts
    e2e/admin-chat.spec.ts
    e2e/admin-rbac.spec.ts
  </files>
  <action>
    chat-persistence.spec.ts: login as buyer → open FAB → type unique message string (uuid) → send → reload page → open FAB → expect message text visible (CHAT-03). Uses API/UI only; no Pusher assertion required.
    chat-widget.spec.ts: login → goto PDP with known product → click «Запитати про цей товар» → expect ProductContextBanner «Питання про:» (D-05-05). Skip if catalog empty — use seed product slug from e2e/helpers/catalog.ts if exists.
    admin-chat.spec.ts: loginAsAdmin → goto /admin/chaty → expect h1 «Чати»; if conversations exist select first and send admin reply; else skip send. Assert nav link «Чати» not disabled.
    Extend admin-rbac.spec.ts: buyer visiting /admin/chaty redirected or 403; buyer cannot POST admin-style message (optional API check via page.request).
    Optional test.describe with test.skip(!hasPusherSecrets()): two contexts buyer+admin instant delivery — manual doc in SUMMARY if skipped in CI.
  </action>
  <acceptance_criteria>
    - chat-persistence passes on local with DB
    - admin-rbac covers /admin/chaty for buyer
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test e2e/chat-persistence.spec.ts e2e/admin-chat.spec.ts e2e/admin-rbac.spec.ts --reporter=line</automated>
  </verify>
  <done>CHAT-03 and CHAT-04 e2e coverage</done>
</task>

<task type="auto">
  <name>Task 3: .env.example + phase verification gate</name>
  <read_first>
    - 05-RESEARCH.md — Environment Availability
    - .env.example
  </read_first>
  <files>.env.example</files>
  <action>
    Add section to .env.example:
    # Pusher Channels (Phase 5 — realtime chat)
    # Create app at dashboard.pusher.com — enable private channels
    PUSHER_APP_ID=
    PUSHER_KEY=
    PUSHER_SECRET=
    PUSHER_CLUSTER=eu
    NEXT_PUBLIC_PUSHER_KEY=
    NEXT_PUBLIC_PUSHER_CLUSTER=eu
    Document: NEXT_PUBLIC_* must match PUSHER_KEY and PUSHER_CLUSTER; server routes return 503 when unset.
    Run phase gate: npm test && npx playwright test e2e/chat-auth.spec.ts e2e/chat-persistence.spec.ts
    Confirm **[BLOCKING]** migrate from 05-01 applied: npx prisma migrate deploy (or status shows add_chat).
    Update 05-VALIDATION.md task statuses to green in SUMMARY only if executor policy allows — otherwise note in 05-05-SUMMARY.md.
  </action>
  <acceptance_criteria>
    - .env.example lists all six Pusher vars
    - Full unit suite green
    - Required chat e2e green
  </acceptance_criteria>
  <verify>
    <automated>npm test && npx playwright test e2e/chat-auth.spec.ts e2e/chat-persistence.spec.ts --reporter=line</automated>
  </verify>
  <done>Phase 5 verification gate documented and passing</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI test credentials | Use seed admin/buyer only; no production secrets in repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Info disclosure | .env.example | mitigate | Placeholders only; no real secrets committed |
| T-05-05-02 | Elevation | RBAC e2e | mitigate | admin-rbac + chat-auth cover buyer/admin separation |

</threat_model>

<verification>
npm test
npx playwright test e2e/chat-auth.spec.ts e2e/chat-persistence.spec.ts e2e/admin-chat.spec.ts
npx prisma migrate status
</verification>

<success_criteria>
Required e2e green; env documented; migration confirmed applied; optional Pusher live test skipped gracefully without secrets
</success_criteria>

<output>
Create `.planning/phases/05-realtime-chat/05-05-SUMMARY.md` when done
</output>
