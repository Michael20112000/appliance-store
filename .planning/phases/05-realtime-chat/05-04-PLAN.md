---
phase: 05-realtime-chat
plan: 04
type: execute
wave: 3
depends_on:
  - 05-02
  - 05-03
files_modified:
  - src/app/(admin)/admin/layout.tsx
  - src/app/(admin)/admin/chaty/page.tsx
  - src/components/admin/admin-nav.tsx
  - src/components/chat/admin-chat-inbox.tsx
  - src/components/chat/admin-chat-provider.tsx
  - src/components/chat/conversation-list.tsx
  - src/components/chat/chat-thread.tsx
  - src/server/actions/chat.actions.ts
autonomous: true
requirements:
  - CHAT-04
  - ADM-05
context_decisions:
  - "D-05-08: All admins see all conversations"
  - "D-05-09: Sort inbox by lastMessageAt desc"
  - "D-05-10: Enable nav Чати → /admin/chaty (replaces D-04-06 disabled)"
  - "D-05-11: Desktop split 320px list + thread; mobile list → thread with back"
  - "D-05-12: Unread badge on admin nav from countUnreadForAdmin RSC"
  - "D-05-20: Admin thread shows buyer name on buyer bubbles; store as «Магазин»"
  - "D-05-16: AdminChatProvider is sole Pusher subscribe site for admin (not chat-thread.tsx)"

must_haves:
  truths:
    - "Admin opens /admin/chaty and sees all buyer conversations sorted by recent activity"
    - "Admin selects a thread, replies, and message persists as STORE"
    - "Admin nav shows Чати link with unread count when buyer messages unread"
    - "Non-admin cannot access /admin/chaty"
  artifacts:
    - path: src/app/(admin)/admin/chaty/page.tsx
      provides: Admin inbox page
    - path: src/components/chat/admin-chat-inbox.tsx
      provides: Split layout orchestrator
    - path: src/components/admin/admin-nav.tsx
      provides: Enabled Чати link + badge
      pattern: /admin/chaty
  key_links:
    - from: src/app/(admin)/admin/chaty/page.tsx
      to: listConversationsForAdmin
      via: RSC data fetch
      pattern: listConversationsForAdmin
    - from: src/components/chat/chat-thread.tsx
      to: /api/chat/messages
      via: POST admin reply
      pattern: conversationId

---

## Phase Goal

**As a** адміністратор магазину, **I want to** бачити всі діалоги з покупцями в одному інбоксі і відповідати в realtime, **so that** питання покупців не губляться.

<objective>
Адмін vertical slice: `/admin/chaty` split inbox, enable Чати nav + unread badge, admin reply via shared composer, mark read on thread open.

Purpose: CHAT-04 + ADM-05; replaces Phase 4 disabled «Незабаром» nav.
Output: Admin can list, read, and reply to all buyer conversations.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-realtime-chat/05-UI-SPEC.md
@.planning/phases/04-admin-operations/04-05-SUMMARY.md
@src/app/(admin)/admin/zamovlennia/page.tsx
@src/components/admin/admin-nav.tsx
@src/server/services/chat.service.ts

<interfaces>
From admin zamovlennia page:
- requireAdmin() in layout (already)
- formatDate uk-UA Intl.DateTimeFormat
- motion.div space-y-6 page shell

From chat.service (05-01):
- listConversationsForAdmin(): ConversationSummaryDto[]
- countUnreadForAdmin(): number
- markAdminRead(conversationId)
- listMessages(conversationId)

From 05-03 (reuse, do not duplicate):
- message-list.tsx, message-bubble.tsx, chat-composer.tsx, chat.actions.ts (markBuyerReadAction — extend only, do not conflict)
- D-05-16: AdminChatProvider wraps inbox and owns Pusher subscribe/unsubscribe when selectedConversationId changes
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin layout unread prop + enable Чати nav</name>
  <read_first>
    - 05-UI-SPEC.md — AdminNav update
    - 04-05-SUMMARY.md — disabled Чати replaced in Phase 5
    - src/app/(admin)/admin/layout.tsx
    - src/components/cart/cart-nav-link.tsx — Badge cap 9+
  </read_first>
  <files>src/app/(admin)/admin/layout.tsx, src/components/admin/admin-nav.tsx</files>
  <action>
    In admin layout RSC: call countUnreadForAdmin() from chat.service; pass unreadChatCount to AdminNav.
    admin-nav.tsx: Remove disabled span (lines 65-74). Add nav item { href: "/admin/chaty", label: "Чати", icon: MessageSquare, enabled: true } to navItems array (D-05-10).
    Active when pathname.startsWith("/admin/chaty").
    When unreadChatCount &gt; 0: Badge ml-auto, cap display «99+», aria-label «Чати, {n} непрочитаних» (D-05-12).
    Do NOT use client polling for badge — server count only (RESEARCH Pitfall 4).
  </action>
  <acceptance_criteria>
    - Чати link navigates to /admin/chaty
    - No «Незабаром» badge remains
    - Unread count visible when test data has unread buyer messages
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' src/components/admin/admin-nav.tsx | grep -c '/admin/chaty' | awk '{exit ($1>=1)?0:1}' && grep -v '^#' src/components/admin/admin-nav.tsx | grep -c 'Незабаром' | awk '{exit ($1==0)?0:1}'</automated>
  </verify>
  <done>Admin nav reflects chat feature (D-05-10, D-05-12)</done>
</task>

<task type="auto">
  <name>Task 2: /admin/chaty page + conversation list</name>
  <read_first>
    - 05-UI-SPEC.md — Page /admin/chaty, ConversationList
    - src/app/(admin)/admin/zamovlennia/page.tsx
  </read_first>
  <files>
    src/app/(admin)/admin/chaty/page.tsx
    src/components/chat/conversation-list.tsx
    src/components/chat/admin-chat-inbox.tsx
  </files>
  <action>
    page.tsx (RSC): metadata title «Чати»; requireAdmin(); listConversationsForAdmin(); render AdminChatInbox with conversations array.
    conversation-list.tsx ("use client"): rows min-h 14; Avatar initials; buyer name/email; last preview line-clamp-1; time meta uk-UA; unread row style border-l-2 border-primary; onSelect(conversationId); empty state copy per UI-SPEC; skeleton 6 rows loading.
    admin-chat-inbox.tsx: desktop grid grid-cols-[320px_1fr] min-h; mobile state selectedConversationId — show list OR thread; pass selected id to ChatThread.
    Sort already from service (D-05-09 lastMessageAt desc).
  </action>
  <acceptance_criteria>
    - Page renders without client-only data fetch for list
    - Empty state when no conversations
    - Desktop shows list and thread columns
  </acceptance_criteria>
  <verify>
    <automated>test -f src/app/\(admin\)/admin/chaty/page.tsx && grep -v '^#' src/app/\(admin\)/admin/chaty/page.tsx | grep -c 'listConversationsForAdmin' | awk '{exit ($1>=1)?0:1}'</automated>
  </verify>
  <done>Admin inbox list UI complete</done>
</task>

<task type="auto">
  <name>Task 3: ChatThread admin reply + mark read</name>
  <read_first>
    - 05-RESEARCH.md — admin POST with conversationId
    - 05-03 chat components — reuse composer and bubbles
  </read_first>
  <files>
    src/components/chat/admin-chat-provider.tsx
    src/components/chat/chat-thread.tsx
    src/server/actions/chat.actions.ts
  </files>
  <action>
    Extend chat.actions.ts: markAdminReadAction(conversationId) — requireAdmin(), markAdminRead, revalidatePath("/admin/chaty") and revalidatePath("/", "layout") for nav badge.
    admin-chat-provider.tsx ("use client"): wrap AdminChatInbox; when selectedConversationId changes, subscribe/unsubscribe `private-conversation-{id}` via getPusherClient singleton; expose messages state + appendMessage via context (D-05-16 — sole admin subscribe site).
    chat-thread.tsx ("use client"): consume AdminChatContext; load messages GET /api/chat/messages on id change; call markAdminReadAction on mount/focus; NO direct Pusher subscribe here; reuse MessageList + ChatComposer with conversationId in POST body for admin STORE reply (D-05-20).
    Header: buyer name + email muted; mobile back button «← До списку»; empty selection «Оберіть діалог» centered.
    Buyer cannot access this page — layout requireAdmin already (AUTH-04).
    Do NOT show individual admin user name on STORE bubbles — label «Магазин» only.
  </action>
  <acceptance_criteria>
    - Admin reply appears as STORE in DB
    - Opening thread clears unread for that conversation in nav after refresh
    - Mobile back returns to list
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/server/services/chat.service.test.ts 2>/dev/null; grep -v '^#' src/components/chat/chat-thread.tsx | grep -c 'markAdminRead' | awk '{exit ($1>=1)?0:1}'</automated>
  </verify>
  <done>CHAT-04 admin reply path complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /admin/chaty | requireAdmin server-side |
| Admin POST messages | Must include conversationId; role admin only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Elevation | /admin/chaty | mitigate | requireAdmin in layout; extend e2e admin-rbac in 05-05 |
| T-05-04-02 | Elevation | admin send as buyer | mitigate | API ignores buyer path when role admin; senderRole STORE server-side |
| T-05-04-03 | Info disclosure | inbox list | accept | Single-store policy D-05-08; all admins see all |

</threat_model>

<verification>
npm run build
grep -v '^#' src/components/admin/admin-nav.tsx | grep -c '/admin/chaty'
</verification>

<success_criteria>
Admin inbox live; nav enabled with unread badge; admin can reply; mark read updates badge
</success_criteria>

<output>
Create `.planning/phases/05-realtime-chat/05-04-SUMMARY.md` when done
</output>
