---
phase: 05-realtime-chat
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/lib/permissions.ts
  - src/app/api/chat/messages/route.ts
  - src/app/api/chat/messages/route.test.ts
  - src/app/api/chat/pusher/auth/route.ts
  - src/app/api/chat/pusher/auth/route.test.ts
autonomous: true
requirements:
  - CHAT-02
  - CHAT-03
  - AUTH-03
context_decisions:
  - "D-05-14: INSERT Message then pusher.trigger in route after chat.service.sendMessage succeeds"
  - "D-05-15: POST /api/chat/pusher/auth with session + conversation membership"
  - "D-05-18: Map ChatRateLimitError to 429 with UA copy «Забагато повідомлень. Спробуйте за хвилину.»"
  - "D-05-20: Admin POST sets senderRole STORE; buyer BUYER — never trust client senderRole"

must_haves:
  truths:
    - "Authenticated buyer can POST a message and it persists in DB"
    - "After persist, Pusher emits message:new on private-conversation-{id}"
    - "Wrong buyer cannot authorize another user's private channel"
    - "Guest receives 401 on POST /api/chat/messages"
  artifacts:
    - path: src/app/api/chat/messages/route.ts
      provides: POST send, optional GET history
      exports: ["POST", "GET"]
    - path: src/app/api/chat/pusher/auth/route.ts
      provides: POST channel authorization
      exports: ["POST"]
    - path: src/lib/permissions.ts
      provides: assertBuyerApi
      exports: ["assertBuyerApi"]
  key_links:
    - from: src/app/api/chat/messages/route.ts
      to: src/server/services/chat.service.ts
      via: sendMessage then getPusherServer().trigger
      pattern: sendMessage
    - from: src/app/api/chat/pusher/auth/route.ts
      to: chat.service assertConversationAccess
      via: channel name parse
      pattern: assertConversationAccess

---

## Phase Goal

**As a** авторизований покупець, **I want to** надсилати повідомлення через API і отримувати їх у realtime, **so that** віджет і адмінка можуть підключитися до одного контракту.

<objective>
Додати `assertBuyerApi`, Route Handlers для send/history і Pusher auth, DB-first + trigger, rate-limit mapping, route unit tests.

Purpose: CHAT-02 + CHAT-03 server API; AUTH-03 guest block on API.
Output: Working POST/GET messages and POST pusher/auth with tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-realtime-chat/05-RESEARCH.md
@.planning/phases/05-realtime-chat/05-PATTERNS.md
@src/app/api/upload/sign/route.ts
@src/server/services/chat.service.ts
@src/lib/pusher-server.ts
@src/lib/permissions.ts

<interfaces>
From src/app/api/upload/sign/route.ts:
- assertAdminApi early return Response.json 401
- try/catch JSON body; domain errors → status codes

From chat.service (05-01):
- sendMessage(...) → MessageDto
- listMessages(conversationId)
- getOrCreateConversation(userId)
- assertConversationAccess(session, conversationId)
- parseConversationChannel(name)
- ChatRateLimitError / FORBIDDEN / CONVERSATION_NOT_FOUND

Pusher trigger payload (MessageDto fields only — no internal secrets):
- id, conversationId, body, senderRole, createdAt (ISO string)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: assertBuyerApi + permissions tests</name>
  <read_first>
    - 05-PATTERNS.md — assertAdminApi mirror for assertBuyerApi
    - src/lib/permissions.ts
  </read_first>
  <files>src/lib/permissions.ts</files>
  <action>
    Add assertBuyerApi(): Promise&lt;Response | null&gt; — mirror assertAdminApi; return Response.json({ error: "UNAUTHORIZED" }, { status: 401 }) when !session?.user.
    Do NOT redirect in API guard (D-05-04 guest redirect is client-only).
    Optional: export type ChatSession = { user: { id: string; role?: string | null } } from successful session for route reuse.
  </action>
  <acceptance_criteria>
    - assertBuyerApi returns 401 Response when no session
    - assertAdminApi unchanged
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' src/lib/permissions.ts | grep -c 'export async function assertBuyerApi' | awk '{exit ($1>=1)?0:1}'</automated>
  </verify>
  <done>API auth helpers ready for chat routes</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: POST /api/chat/pusher/auth</name>
  <read_first>
    - 05-RESEARCH.md — Pattern 3 channel auth
    - 05-CONTEXT.md — D-05-15
    - Pusher docs: authorizeChannel(socket_id, channel_name)
  </read_first>
  <files>src/app/api/chat/pusher/auth/route.ts, src/app/api/chat/pusher/auth/route.test.ts</files>
  <behavior>
    - No session → 401
    - Malformed channel_name → 400
    - Buyer on another user's conversation → 403
    - Admin on any existing conversation → 200 + auth JSON
    - Buyer on own conversation → 200 + auth JSON
  </behavior>
  <action>
    Create route.ts POST handler:
    1. Parse body — support JSON and application/x-www-form-urlencoded (pusher-js may send either): socket_id, channel_name required.
    2. If no session (buyer or admin): return 401. Admin: session.user.role === "admin". Buyer: assertBuyerApi OR allow admin path — **either** logged-in buyer **or** admin may auth (D-05-08).
    3. parseConversationChannel(channel_name); invalid → 400.
    4. assertConversationAccess(session, conversationId).
    5. getPusherServer().authorizeChannel(socketId, channelName) → Response.json(auth).
    6. PusherNotConfiguredError → 503 { error: "PUSHER_NOT_CONFIGURED" }.
    route.test.ts: mock auth.getSession, prisma conversation lookup, authorizeChannel; cover buyer forbidden and admin allowed cases per 05-VALIDATION.md T-05-06.
  </action>
  <acceptance_criteria>
    - Route tests pass without real Pusher network
    - IDOR case returns 403
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/app/api/chat/pusher/auth/route.test.ts</automated>
  </verify>
  <done>Private channel auth secured</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: POST/GET /api/chat/messages + DB-first trigger</name>
  <read_first>
    - 05-RESEARCH.md — Pattern 4 DB-first send
    - 05-PATTERNS.md — upload/sign route error mapping
  </read_first>
  <files>src/app/api/chat/messages/route.ts, src/app/api/chat/messages/route.test.ts</files>
  <behavior>
    - Guest POST → 401
    - Buyer POST creates BUYER message, returns 201, triggers Pusher once
    - Admin POST with conversationId creates STORE message
    - Rate limit → 429
    - GET with conversationId returns messages for participant only
  </behavior>
  <action>
    POST:
    1. Parse JSON body with sendMessageSchema.
    2. Session required. If role admin: require conversationId in body; senderRole STORE, senderId = session.user.id. If buyer: assertBuyerApi; getOrCreateConversation(session.user.id, product context from body); senderRole BUYER. **Ignore** client senderRole for buyers (mass-assignment threat).
    3. Call chat.service.sendMessage.
    4. On success: getPusherServer().trigger(conversationChannel(id), "message:new", messageDto) **after** DB commit (service returns DTO).
    5. Map errors: ChatRateLimitError → 429 UA message; FORBIDDEN → 403; CONVERSATION_NOT_FOUND → 404; PusherNotConfigured → 503.
    GET:
    - Query conversationId required.
    - assertConversationAccess.
    - Return listMessages (limit 50).
    route.test.ts: mock prisma + pusher trigger; assert trigger called once with correct channel; guest 401.
    Do NOT implement Server Action for send (RESEARCH Pattern 4 — Route Handler for JSON composer).
  </action>
  <acceptance_criteria>
    - route.test.ts green
    - Trigger not called when sendMessage throws
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/app/api/chat/messages/route.test.ts src/app/api/chat/pusher/auth/route.test.ts</automated>
  </verify>
  <done>Chat API complete for UI integration in 05-03/04</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → POST /api/chat/messages | Untrusted body; session cookie auth |
| Client → POST /api/chat/pusher/auth | Untrusted channel_name; must match ownership |
| Server → Pusher | Server secret only in getPusherServer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Elevation | messages POST | mitigate | assertBuyerApi; admin branch checks role; ignore client senderRole |
| T-05-02-02 | Info disclosure | pusher/auth | mitigate | assertConversationAccess before authorizeChannel (D-05-15) |
| T-05-02-03 | Denial of Service | messages POST | mitigate | chat.service rate limit → 429 |
| T-05-02-04 | Spoofing | pusher trigger | mitigate | Trigger only server-side after DB insert |
| T-05-02-05 | Tampering | message body XSS | mitigate | Store plain text; UI renders text nodes only (05-03) |

</threat_model>

<verification>
npm test -- src/app/api/chat/messages/route.test.ts src/app/api/chat/pusher/auth/route.test.ts
</verification>

<success_criteria>
Messages API persists and triggers; channel auth rejects cross-tenant subscribe; guest blocked with 401
</success_criteria>

<output>
Create `.planning/phases/05-realtime-chat/05-02-SUMMARY.md` when done
</output>
