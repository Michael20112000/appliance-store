---
phase: 05-realtime-chat
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - src/lib/chat/search-params.ts
  - src/server/actions/chat.actions.ts
  - src/components/chat/chat-provider.tsx
  - src/components/chat/chat-fab.tsx
  - src/components/chat/chat-panel.tsx
  - src/components/chat/message-list.tsx
  - src/components/chat/message-bubble.tsx
  - src/components/chat/chat-composer.tsx
  - src/components/chat/product-context-banner.tsx
  - src/components/chat/open-chat-button.tsx
  - src/app/(storefront)/layout.tsx
  - src/app/(storefront)/kabinet/page.tsx
  - src/app/(storefront)/tovar/[slug]/page.tsx
autonomous: true
requirements:
  - CHAT-01
  - CHAT-02
  - CHAT-03
context_decisions:
  - "D-05-03: Global FAB in storefront layout, fixed bottom-right, z-[60]"
  - "D-05-04: Guest FAB/button → /uviity?callbackUrl=pathname+search"
  - "D-05-05: PDP OpenChatButton «Запитати про цей товар» with product context"
  - "D-05-06: /kabinet CTA «Відкрити чат» opens widget via ?chat=open"
  - "D-05-07: No /chat page — widget only"
  - "D-05-16: Single pusher-js subscribe in ChatProvider only"
  - "D-05-17: Disconnected banner + refetch on reconnect; no polling loop"
  - "D-05-21: FAB size-14; panel w-[380px] desktop, Sheet bottom mobile"

must_haves:
  truths:
    - "Authenticated buyer sees FAB on storefront and can open chat panel"
    - "Guest clicking FAB or PDP chat CTA redirects to login with callbackUrl"
    - "Sending a message appends via Pusher without full page reload"
    - "Reloading page still shows sent messages from GET /api/chat/messages"
  artifacts:
    - path: src/components/chat/chat-provider.tsx
      provides: open state, nuqs chat=open, Pusher subscribe/unsubscribe
      min_lines: 80
    - path: src/components/chat/chat-fab.tsx
      provides: FAB entry point
    - path: src/app/(storefront)/layout.tsx
      provides: ChatProvider mount sibling to main
      pattern: ChatProvider
  key_links:
    - from: src/components/chat/chat-composer.tsx
      to: /api/chat/messages
      via: fetch POST
      pattern: fetch\("/api/chat/messages"
    - from: src/components/chat/chat-provider.tsx
      to: getPusherClient
      via: subscribe private-conversation
      pattern: message:new

---

## Phase Goal

**As a** авторизований покупець, **I want to** відкрити чат з магазину з будь-якої сторінки і бачити нові відповіді без перезавантаження, **so that** можна швидко уточнити питання про товар.

<objective>
Вертикальний storefront slice: ChatProvider + FAB + panel, PDP і kabinet entry points, Pusher live append, history on open/reload.

Purpose: CHAT-01, CHAT-02, CHAT-03 buyer UX per 05-UI-SPEC.
Output: Global widget on storefront; guest gate; persistence visible after reload.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-realtime-chat/05-UI-SPEC.md
@.planning/phases/05-realtime-chat/05-PATTERNS.md
@src/app/(storefront)/layout.tsx
@src/components/cart/add-to-cart-button.tsx
@src/lib/catalog/search-params.ts
@src/components/layout/store-header.tsx

<interfaces>
From add-to-cart-button.tsx (guest redirect D-05-04):
- router.push(`/uviity?callbackUrl=${encodeURIComponent(pathname || fallback)}`)

From catalog search-params (nuqs pattern):
- createSearchParamsCache + parseAsStringEnum for chat=open

From POST /api/chat/messages (05-02):
- 201 MessageDto JSON
- GET ?conversationId= returns { messages: MessageDto[] }

From chat.service (05-01):
- markBuyerRead(conversationId) — call on panel open via chat.actions.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: nuqs + ChatProvider + Pusher subscribe lifecycle</name>
  <read_first>
    - 05-UI-SPEC.md — Architecture Overview, Provider mount
    - 05-RESEARCH.md — Pattern 5 ChatProvider responsibilities
    - src/lib/catalog/search-params.ts
    - src/components/cart/cart-pending-merge.tsx — useEffect cleanup pattern
  </read_first>
  <files>src/lib/chat/search-params.ts, src/components/chat/chat-provider.tsx, src/server/actions/chat.actions.ts</files>
  <action>
    Create src/lib/chat/search-params.ts: parsers { chat: parseAsStringLiteral(["open"]) optional, productId: parseAsString optional } + urlKeys; export chatSearchParamsCache for server if needed.
    Create chat.actions.ts: markBuyerReadAction(conversationId) — requireBuyer(), call markBuyerRead, revalidatePath("/", "layout").
    Create chat-provider.tsx ("use client"):
    - Props: optional initialConversationId, initialMessages, hasSession from RSC wrapper OR fetch on mount when session exists.
    - useQueryStates for chat=open and productId (D-05-06, D-05-07).
    - State: messages[], conversationId, connectionState, isOpen derived from nuqs.
    - On open (authenticated): fetch GET /api/chat/messages; call markBuyerReadAction; subscribe getPusherClient().subscribe(private-conversation-{id}); bind message:new with dedupe by id; cleanup unbind/unsubscribe on close/unmount (D-05-16).
    - connection.bind state_change → show disconnected banner flag (D-05-17); on connected after disconnect refetch GET (no setInterval polling).
    - Export useChat() context: openPanel({ productId?, productTitle?, productSlug? }), closePanel, isOpen, messages, send via composer callback.
    - Guest: do not instantiate Pusher; openPanel redirects to /uviity?callbackUrl=encodeURIComponent(window.location.pathname + search).
    Wrap children + render ChatFab + ChatPanel inside provider.
  </action>
  <acceptance_criteria>
    - Only one subscribe active when panel open
    - chat=open in URL opens panel on load
    - Guest openPanel navigates to login with callback
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' src/components/chat/chat-provider.tsx | grep -c 'message:new' | awk '{exit ($1>=1)?0:1}' && grep -v '^#' src/lib/chat/search-params.ts | grep -c 'chat' | awk '{exit ($1>=1)?0:1}'</automated>
  </verify>
  <done>Provider owns realtime subscription and open state</done>
</task>

<task type="auto">
  <name>Task 2: Chat UI components (FAB, panel, messages, composer)</name>
  <read_first>
    - 05-UI-SPEC.md — ChatFab, ChatPanel, MessageBubble, ChatComposer, ProductContextBanner, Copywriting Contract
    - src/components/ui/sheet.tsx — mobile panel
  </read_first>
  <files>
    src/components/chat/chat-fab.tsx
    src/components/chat/chat-panel.tsx
    src/components/chat/message-list.tsx
    src/components/chat/message-bubble.tsx
    src/components/chat/chat-composer.tsx
    src/components/chat/product-context-banner.tsx
  </files>
  <action>
    Run `npx shadcn@latest add scroll-area textarea avatar` per 05-UI-SPEC (official registry only).
    chat-fab.tsx: fixed bottom-6 right-6 z-[60] size-14 rounded-full MessageSquare; aria-label «Відкрити чат з магазином»; hide when panel open; unread dot slot (buyer: lastMessageSender STORE && unread); onClick openPanel or guest redirect.
    chat-panel.tsx: desktop fixed z-[61] w-[380px] h-[min(520px,calc(100dvh-7rem))]; mobile Sheet side=bottom h-[85dvh]; header «Чат з магазином» / «Відповімо якнайшвидше»; close X.
    message-list.tsx: ScrollArea, skeleton loading, empty state copy per UI-SPEC, aria-live=polite.
    message-bubble.tsx: buyer right primary; store left muted + label «Магазин» (D-05-20); timestamp uk-UA Intl; whitespace-pre-wrap; no dangerouslySetInnerHTML.
    chat-composer.tsx: Textarea maxLength 2000, Enter send Shift+Enter newline, fetch POST /api/chat/messages, map 429/400 errors to UA strings, optimistic bubble opacity 0.7 until ACK.
    product-context-banner.tsx: «Питання про: {title}» when product context from PDP; optional link to /tovar/{slug}.
    Match spacing/color tokens from 05-UI-SPEC (no new hex colors).
  </action>
  <acceptance_criteria>
    - Panel meets width/height/z-index spec
    - Store messages show «Магазин» label
    - Composer blocks send over 2000 chars
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Widget UI complete per design contract</done>
</task>

<task type="auto">
  <name>Task 3: Storefront integration — layout, kabinet, PDP</name>
  <read_first>
    - 05-CONTEXT.md — D-05-05, D-05-06
    - src/app/(storefront)/kabinet/page.tsx — remove stub lines 29-31
    - src/app/(storefront)/tovar/[slug]/page.tsx — AddToCartButton pattern
  </read_first>
  <files>
    src/app/(storefront)/layout.tsx
    src/app/(storefront)/kabinet/page.tsx
    src/app/(storefront)/tovar/[slug]/page.tsx
    src/components/chat/open-chat-button.tsx
  </files>
  <action>
    Create open-chat-button.tsx ("use client"): props productId?, productTitle?, productSlug?, hasSession, label default «Запитати про цей товар» or «Відкрити чат»; variant outline on PDP, default on kabinet; calls useChat().openPanel with product context (D-05-05).
    layout.tsx: After StoreFooter, add ChatProvider. Optional: small RSC ChatProviderGate that reads session via auth.api.getSession and passes hasSession + preloaded conversation to reduce client waterfall.
    kabinet/page.tsx: Remove chat stub paragraph; add section «Повідомлення» with helper text and OpenChatButton label «Відкрити чат» variant default (D-05-06).
  tovar/[slug]/page.tsx: Below AddToCartButton, add OpenChatButton variant outline full width on mobile with productId, productTitle, productSlug, hasSession={Boolean(session?.user)}.
    Do NOT create app/(storefront)/chat route (D-05-07).
  </action>
  <acceptance_criteria>
    - FAB visible on home and PDP when logged in
    - Kabinet has working «Відкрити чат» CTA
    - PDP button passes product context to banner
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' src/app/\(storefront\)/layout.tsx | grep -c 'ChatProvider' | awk '{exit ($1>=1)?0:1}' && grep -v '^#' src/app/\(storefront\)/kabinet/page.tsx | grep -c 'OpenChatButton' | awk '{exit ($1>=1)?0:1}'</automated>
  </verify>
  <done>CHAT-01 entry points wired on storefront</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser chat UI → API | Session cookie; no tokens in localStorage for auth |
| Pusher client → private channel | Only via /api/chat/pusher/auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Spoofing | Guest chat | mitigate | No Pusher client for guests; redirect AUTH-03 |
| T-05-03-02 | Tampering | message body display | mitigate | Text nodes only; no innerHTML |
| T-05-03-03 | Repudiation | optimistic UI | accept | Low risk; server is source of truth on reload |

</threat_model>

<verification>
npm run build
Manual: login → open FAB → send message → see in panel; reload → history visible
</verification>

<success_criteria>
Buyer widget works end-to-end with API + Pusher; guest redirected; kabinet and PDP entry points open widget with context
</success_criteria>

<output>
Create `.planning/phases/05-realtime-chat/05-03-SUMMARY.md` when done
</output>
