---
phase: 30-similar-products-footer-layout
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/store-footer.tsx
autonomous: true
requirements:
  - FOOT-05
must_haves:
  truths:
    - "D-14: Desktop md+ left column map iframe only; right column contacts then CallbackRequestForm"
    - "D-15: Map iframe taller on desktop (md:min-h-[280px] or equivalent)"
    - "D-16: Copyright © {year} Техніка б/у Львів centered on md+ (md:text-center)"
    - "D-17: Mobile stack order contacts → CallbackRequestForm → map"
    - "D-18: getPublicStoreContacts, store-map helpers, lazy iframe unchanged — layout-only"
    - "D-20: npm run build green; manual footer desktop columns + centered ©"
  artifacts:
    - path: src/components/layout/store-footer.tsx
      provides: FOOT-05 responsive footer grid
      contains: md:grid-cols-2
  key_links:
    - from: src/components/layout/store-footer.tsx
      to: src/components/layout/callback-request-form.tsx
      via: import CallbackRequestForm in right column
      pattern: CallbackRequestForm
    - from: src/components/layout/store-footer.tsx
      to: src/lib/catalog/store-map.ts
      via: addressMapEmbedSrc
      pattern: addressMapEmbedSrc
---

<objective>
Implement FOOT-05: restructure storefront footer to desktop map | contacts+callback and centered copyright.

Purpose: Desktop footer matches requirement layout; mobile stays action-first without regressing Phase 26 contacts/callback.
Output: Layout-only refactor in store-footer.tsx.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-similar-products-footer-layout/30-CONTEXT.md
@.planning/phases/30-similar-products-footer-layout/30-RESEARCH.md
@.planning/phases/30-similar-products-footer-layout/30-UI-SPEC.md
@.planning/phases/26-footer-mobile-contact/26-CONTEXT.md

<interfaces>
From src/components/layout/store-footer.tsx (current):
- Async server component; getPublicStoreContacts(); mapEmbedSrc from addressMapEmbedSrc(primaryAddress)
- Today: col1 contacts+map stacked; col2 CallbackRequestForm; © left-aligned

From src/components/layout/callback-request-form.tsx:
- Import only — do not edit file (D-18, out of scope)

From src/lib/catalog/store-map.ts:
- addressMapEmbedSrc, addressExternalMapUrl — unchanged

Target grid (single DOM tree, Tailwind order):
- md:grid-cols-2 md:gap-12
- Map cell: order-3 md:order-1 (left desktop, last mobile)
- Contacts: order-1 md:order-2
- CallbackRequestForm: order-2 md:order-2
- © row: mt-8 border-t pt-6 text-sm text-muted-foreground md:text-center

Do NOT modify: callback-request-form.tsx, store-map.ts, store-settings.service.ts, admin routes, PDP files.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Desktop two-column footer</name>
  <files>src/components/layout/store-footer.tsx</files>
  <read_first>
    src/components/layout/store-footer.tsx
    .planning/phases/30-similar-products-footer-layout/30-UI-SPEC.md
    .planning/phases/30-similar-products-footer-layout/30-CONTEXT.md
    .planning/phases/26-footer-mobile-contact/26-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - md+ grid has exactly two columns: left contains only map iframe when mapEmbedSrc set (D-14); no phones/emails/addresses in left column
    - Right column stacks phones, emails, addresses (existing list markup, omit empty types), then CallbackRequestForm below contacts (D-14)
    - iframe keeps title Карта магазину, loading lazy, referrerPolicy no-referrer-when-downgrade (D-18)
    - Desktop iframe classes include md:min-h-[280px] and w-full rounded-md border border-border; mobile height h-40 on map (D-15, UI-SPEC)
    - No duplicate footer markup for mobile vs desktop — one tree with order utilities
  </acceptance_criteria>
  <action>
    Refactor store-footer.tsx per D-14/D-15: split current col1 into map-only left cell and contacts+form right cell using order-3 md:order-1 on map wrapper and order-1/order-2 on right blocks. Preserve uaPhoneTelHref, formatUaPhoneDisplay, mailto links, address external links. Do not change CallbackRequestForm props or inner logic.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Desktop footer shows map left, contacts and callback right.</done>
</task>

<task type="auto">
  <name>Task 2: Mobile order and centered copyright</name>
  <files>src/components/layout/store-footer.tsx</files>
  <read_first>
    src/components/layout/store-footer.tsx
    .planning/phases/30-similar-products-footer-layout/30-UI-SPEC.md
  </read_first>
  <acceptance_criteria>
    - Viewport below md: visual order top-to-bottom is contacts block, CallbackRequestForm, map iframe (D-17)
    - Copyright paragraph has md:text-center; copy remains © {year} Техніка б/у Львів (D-16)
    - When contacts.phones/emails/addresses all empty: callback and map still render; empty contact lists not shown (Phase 26 pattern)
    - npm run build exits 0
  </acceptance_criteria>
  <action>
    Confirm Tailwind order-* classes on three blocks achieve D-17 without a second footer instance. Add md:text-center to © row per D-16; mobile © may stay left (discretion). Run npm run build.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>FOOT-05 layout complete; ROADMAP success criterion 2 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Footer → CallbackRequestForm | User-submitted phone; existing client+server validation unchanged |
| Map iframe src | From addressMapEmbedSrc(primaryAddress) — trusted server config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04 | Tampering | CallbackRequestForm | transfer | No edits to form; existing callbackRequestSchema + server action from Phase 26 |
| T-30-05 | Spoofing | Map embed URL | mitigate | Keep addressMapEmbedSrc helper; no user-controlled embed src |
| T-30-06 | Information Disclosure | Public contacts | accept | Intentional public storefront contacts from getPublicStoreContacts |
| T-30-SC | Tampering | npm installs | accept | No new dependencies |

</threat_model>

<verification>
- npm run build green
- Manual desktop (≥md): left tall map only; right contacts + callback; © centered
- Manual mobile (&lt;md): contacts → form → map; © left acceptable
- Manual: tel/mailto links and callback submit still work (Phase 26 regression)
</verification>

<success_criteria>
- FOOT-05 and ROADMAP criterion 2: footer map | contacts+form on desktop; © centered on md+
- Phase 26 callback and contact formatting unchanged
</success_criteria>

<output>
Create `.planning/phases/30-similar-products-footer-layout/30-02-SUMMARY.md` when done
</output>
