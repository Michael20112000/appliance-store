---
phase: 30-similar-products-footer-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/catalog.ts
  - src/server/services/catalog.service.ts
  - src/server/services/catalog.service.test.ts
  - src/app/(storefront)/tovar/[slug]/page.tsx
  - src/components/catalog/similar-products-section.tsx
autonomous: true
requirements:
  - PDP-07
must_haves:
  truths:
    - "D-01: «Схожі товари» section renders below md:grid-cols-2 PDP grid, full width inside max-w-6xl"
    - "D-02/D-05: Up to 4 cards with heading «Схожі товари»"
    - "D-03/D-04: ProductGrid + ProductCard with wishlist when session exists"
    - "D-06–D-11: Same category, exclude current SKU, ±20% then ±40% then category-only fallback, server shuffle"
    - "D-12: Zero candidates → section omitted (no heading)"
  artifacts:
    - path: src/server/services/catalog.service.ts
      provides: similarPriceBandKopiyky + listSimilarPublicProducts
      contains: listSimilarPublicProducts
    - path: src/server/services/catalog.service.test.ts
      provides: Vitest for band math and similar query (D-19)
    - path: src/app/(storefront)/tovar/[slug]/page.tsx
      provides: PDP similar fetch + render
      pattern: listSimilarPublicProducts
  key_links:
    - from: src/app/(storefront)/tovar/[slug]/page.tsx
      to: src/server/services/catalog.service.ts
      via: listSimilarPublicProducts after getPublicProductBySlug
      pattern: listSimilarPublicProducts
    - from: src/app/(storefront)/tovar/[slug]/page.tsx
      to: src/components/catalog/product-grid.tsx
      via: ProductGrid products prop
      pattern: ProductGrid
---

<objective>
Implement PDP-07: server-side similar products (category + price bands + fallbacks) and render «Схожі товари» below the main PDP grid.

Purpose: Buyers compare in-stock alternatives without leaving the product page.
Output: `similarPriceBandKopiyky`, `listSimilarPublicProducts`, `category.id` on public product types, PDP section wired with `ProductGrid`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-similar-products-footer-layout/30-CONTEXT.md
@.planning/phases/30-similar-products-footer-layout/30-RESEARCH.md
@.planning/phases/30-similar-products-footer-layout/30-UI-SPEC.md

<interfaces>
From src/types/catalog.ts:
- PublicProductCard.category: extend to { id: string; name: string; slug: string }
- PublicProductDetail extends PublicProductCard

From src/server/services/catalog.service.ts:
- buildPublicProductWhere(input: CatalogFilters & { categoryId?: string }): Prisma.ProductWhereInput
- mapToCard(product): PublicProductCard
- cardInclude: category + images take 5
- getPublicProductBySlug(slug): PublicProductDetail | null — today category select lacks id

From src/components/catalog/product-grid.tsx:
- ProductGrid({ products, hasSession?, wishlistedProductIds?: Set<string> })
- Grid classes: grid grid-cols-2 gap-4 md:grid-cols-4 md:gap-6

From src/app/(storefront)/katalog/[slug]/page.tsx (wishlist pattern):
- getWishlistedProductIds(session.user.id) → new Set(...) passed to ProductGrid

Do NOT modify: callback-request-form.tsx, store-footer.tsx, admin routes, PdpCartFab, ProductGallery, Phase 29 cart FAB behavior.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: category.id + similar query helpers</name>
  <files>src/types/catalog.ts, src/server/services/catalog.service.ts</files>
  <read_first>
    src/types/catalog.ts
    src/server/services/catalog.service.ts
    .planning/phases/30-similar-products-footer-layout/30-CONTEXT.md
    .planning/phases/30-similar-products-footer-layout/30-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - PublicProductCard.category includes id: string; mapToCard and getPublicProductBySlug return category.id (D-06)
    - Exported similarPriceBandKopiyky(priceKop: number, band: 20 | 40) returns minPrice/maxPrice kopiyky per D-09 (20: floor×0.8, ceil×1.2) and D-11 step 1 (40: floor×0.6, ceil×1.4)
    - Exported listSimilarPublicProducts({ productId, categoryId, price, limit?: number }) returns PublicProductCard[] length 0–4
    - Each tier uses buildPublicProductWhere with categoryId + optional minPrice/maxPrice; spreads id: { not: productId } inline (D-07, D-08) — do not change buildPublicProductWhere signature for catalog pages
    - Tier merge: pool ±20% → if unique count &lt; limit merge ±40% → if still &lt; limit merge category-only (no price keys, take: 50) → Fisher–Yates shuffle once → slice(0, limit default 4) → mapToCard (D-10, D-11)
    - Returns [] when merged pool empty (D-12)
  </acceptance_criteria>
  <action>
    Per D-13: add similarPriceBandKopiyky and listSimilarPublicProducts to catalog.service.ts. Extend category select in cardInclude and getPublicProductBySlug to { id, name, slug }. Update PublicProductCard type. Reuse cardInclude and mapToCard for all findMany results. Cap category-only findMany with take 50 before shuffle. No client-side random; no new npm packages.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Service exports similar helpers; PDP can read product.category.id and fetch up to four similar cards.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Vitest for similar bands and list</name>
  <files>src/server/services/catalog.service.test.ts</files>
  <read_first>
    src/server/services/catalog.service.test.ts
    src/server/services/catalog.service.ts
    .planning/phases/30-similar-products-footer-layout/30-RESEARCH.md
  </read_first>
  <behavior>
    - similarPriceBandKopiyky(100_00, 20) → minPrice 80_00, maxPrice 120_00
    - similarPriceBandKopiyky(100_00, 40) → minPrice 60_00, maxPrice 140_00
    - listSimilarPublicProducts: prisma.product.findMany where includes id not productId on each tier call
    - Fallback: first findMany returns 1 row, second returns 2 unique, third fills — result length ≤ 4 and excludes productId
    - All findMany return [] → result []
  </behavior>
  <acceptance_criteria>
    - Describe block similarPriceBandKopiyky with at least two band cases (D-19)
    - Describe block listSimilarPublicProducts with vi.mock prisma.product.findMany sequential returns
    - npm test -- src/server/services/catalog.service.test.ts exits 0
  </acceptance_criteria>
  <action>
    Extend catalog.service.test.ts following existing vi.mock("@/lib/db") style. Import similarPriceBandKopiyky and listSimilarPublicProducts. Assert where clauses include categoryId, price gte/lte when band applied, and id not equal exclude id. Do not fix unrelated failing tests outside this file unless this plan caused them.
  </action>
  <verify>
    <automated>npm test -- src/server/services/catalog.service.test.ts</automated>
  </verify>
  <done>D-19 automated coverage for band math and similar selection paths.</done>
</task>

<task type="auto">
  <name>Task 3: PDP wire similar section</name>
  <files>src/app/(storefront)/tovar/[slug]/page.tsx, src/components/catalog/similar-products-section.tsx</files>
  <read_first>
    src/app/(storefront)/tovar/[slug]/page.tsx
    src/components/catalog/product-grid.tsx
    src/app/(storefront)/katalog/[slug]/page.tsx
    .planning/phases/30-similar-products-footer-layout/30-UI-SPEC.md
  </read_first>
  <acceptance_criteria>
    - After getPublicProductBySlug, Promise.all listSimilarPublicProducts({ productId, categoryId: product.category.id, price: product.price, limit: 4 }) with getWishlistedProductIds when session?.user (catalog pattern)
    - Similar block is sibling after closing md:grid-cols-2 grid, still inside mx-auto max-w-6xl wrapper (D-01)
    - When similar.length === 0: no section markup (D-12)
    - When similar.length &gt; 0: section aria-labelledby="similar-products-heading", h2 text «Схожі товари» (D-05), wrapper mt-16 border-t border-border pt-12, ProductGrid with hasSession and wishlistedProductIds (D-03, D-04)
    - SimilarProductsSection server component optional but file created if markup extracted; heading text-lg font-medium per UI-SPEC
    - npm test and npm run build exit 0
  </acceptance_criteria>
  <action>
    Create similar-products-section.tsx as async or sync server wrapper (heading + ProductGrid) per 30-UI-SPEC. Update tovar page.tsx: parallel fetch, conditional render, do not nest inside right column or alter PdpCartFab placement. Do not touch gallery, AddToCartButton, or cart FAB files.
  </action>
  <verify>
    <automated>npm test && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>PDP-07 visible on /tovar/[slug] when similar products exist; hidden when none.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PDP RSC → catalog.service | productId/categoryId/price from getPublicProductBySlug only — not request params |
| Public catalog read | Same in-stock filter as /katalog |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Spoofing | listSimilarPublicProducts | mitigate | No client-supplied IDs; inputs from server-resolved PDP product only |
| T-30-02 | Information Disclosure | Similar query | accept | Public catalog data only; same exposure as listPublicProducts |
| T-30-03 | Denial of Service | Category-only take 50 | mitigate | take: 50 cap on third-tier query per RESEARCH |
| T-30-SC | Tampering | npm installs | accept | No new dependencies this plan |

</threat_model>

<verification>
- npm test -- src/server/services/catalog.service.test.ts
- npm test && npm run build green (D-20)
- Manual: PDP with stocked category shows ≤4 «Схожі товари» cards; current SKU absent; section below two-column grid
- Manual: category with no peers hides section entirely
</verification>

<success_criteria>
- PDP-07: similar section matches ROADMAP success criterion 1 (same category ±20% band, exclude current SKU, fallbacks per CONTEXT)
- All locked D-01–D-13 behaviors verifiable on /tovar/[slug]
</success_criteria>

<output>
Create `.planning/phases/30-similar-products-footer-layout/30-01-SUMMARY.md` when done
</output>
