---
phase: 32-admin-dashboard-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(admin)/admin/page.tsx
  - src/components/admin/stat-card.tsx
autonomous: true
requirements:
  - ADM-DASH-03
  - ADM-DASH-04

must_haves:
  truths:
    - "«Додати товар» button renders with blue primary (default) variant and a Plus icon"
    - "«Переглянути замовлення» button renders with outline variant and an Eye icon"
    - "Each StatCard on the dashboard shows its lucide icon in the top-right corner"
    - "StatCard with no icon prop renders identically to before (no regression)"
  artifacts:
    - path: "src/app/(admin)/admin/page.tsx"
      provides: "Dashboard page with updated buttons and icon-bearing StatCards"
      contains: "Plus, Eye, ShoppingBag, Package, PackageX"
    - path: "src/components/admin/stat-card.tsx"
      provides: "StatCard with optional icon prop"
      exports: ["StatCard"]
      contains: "icon?: React.ElementType"
  key_links:
    - from: "src/app/(admin)/admin/page.tsx"
      to: "src/components/admin/stat-card.tsx"
      via: "icon prop on each StatCard"
      pattern: "icon=\\{(ShoppingBag|Package|PackageX)\\}"
---

<objective>
Polish the admin dashboard landing page: two action buttons get correct variants and icons, three stat cards get contextual lucide icons.

Purpose: Operators orient faster when actions and metrics have recognizable visual cues.
Output: Updated admin/page.tsx and stat-card.tsx; no data model or route changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-admin-dashboard-polish/32-CONTEXT.md
</context>

<interfaces>
<!-- Extracted from source files — executor must not re-explore these. -->

From src/app/(admin)/admin/page.tsx (current state):
- "Додати товар" button: render={<Link href="/admin/tovary/novyi" />} size="sm" variant="outline"
- "Переглянути замовлення" button: render={<Link href="/admin/zamovlennia" />} size="sm" variant="outline"
- StatCard usage: label, count, href — no icon prop currently

From src/components/admin/stat-card.tsx (current state):
- StatCardProps: { label: string; count: number; href?: string; className?: string }
- content block: label in <p className="text-sm font-medium text-muted-foreground">, count in <p className="mt-2 text-3xl font-semibold tabular-nums">
- Uses cn() for cardClass; renders as Link when href provided, div otherwise

From src/app/(admin)/admin/tovary/page.tsx (reference pattern — exact model for "Додати товар"):
- Button render={<Link href="/admin/tovary/novyi" />}  ← no size prop, no variant prop = default
- <Plus className="size-4" aria-hidden /> inside button text
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update dashboard action buttons (ADM-DASH-03)</name>
  <files>src/app/(admin)/admin/page.tsx</files>
  <read_first>
    - src/app/(admin)/admin/page.tsx — current button markup to replace
    - src/app/(admin)/admin/tovary/page.tsx — canonical reference: Button with no size/variant props + Plus icon inside (lines 72–75)
    - src/components/ui/button.tsx — confirm default variant name (should be "default")
  </read_first>
  <action>
    Per D-01 and D-02, update both buttons in the flex row inside AdminDashboardPage:

    1. "Додати товар" button: remove size="sm", remove variant="outline", set variant="default" (or omit if "default" is the component default — check button.tsx). Add <Plus className="size-4" aria-hidden /> as the first child before the button text. Import Plus from "lucide-react".

    2. "Переглянути замовлення" button: remove size="sm", keep variant="outline". Add <Eye className="size-4" aria-hidden /> as the first child before the button text. Import Eye from "lucide-react".

    Per D-03, keep render={<Link href="..." />} on both buttons unchanged.

    Both buttons use default size (no size prop), matching the tovary page Add button pattern exactly.

    Add Eye and Plus to the lucide-react import line (Plus likely needs to be added alongside Eye).
  </action>
  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npx tsc --noEmit 2>&1 | grep -E "admin/page|error" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(admin)/admin/page.tsx imports Plus and Eye from "lucide-react"
    - "Додати товар" Button has no size prop and either no variant prop or variant="default"; contains <Plus className="size-4" aria-hidden />
    - "Переглянути замовлення" Button has variant="outline" and no size prop; contains <Eye className="size-4" aria-hidden />
    - Both buttons retain render={<Link href="..." />}
    - TypeScript compilation passes with no new errors in this file
  </acceptance_criteria>
  <done>"Додати товар" is blue primary with Plus icon; "Переглянути замовлення" is outline with Eye icon; no size prop on either button.</done>
</task>

<task type="auto">
  <name>Task 2: Add icon prop to StatCard and wire dashboard icons (ADM-DASH-04)</name>
  <files>
    src/components/admin/stat-card.tsx
    src/app/(admin)/admin/page.tsx
  </files>
  <read_first>
    - src/components/admin/stat-card.tsx — current props type and content render block to extend
    - src/app/(admin)/admin/page.tsx — StatCard usages to update with icon prop (after Task 1 is done)
  </read_first>
  <action>
    Per D-04, D-05, D-06:

    In src/components/admin/stat-card.tsx:
    1. Add import: import type React from "react" (or "import React" if needed for React.ElementType — check existing imports).
    2. Extend StatCardProps to add: icon?: React.ElementType
    3. Destructure icon from props.
    4. Modify the content JSX: wrap label+count in a relative container div, then conditionally render the icon in the top-right corner using absolute positioning. Concretely: wrap <> fragment in a <div className="relative">, and when icon is provided, render <Icon className="absolute top-0 right-0 size-5 text-muted-foreground" aria-hidden /> where Icon is the icon prop variable.
    5. No change to card link/div wrapper or cardClass — icon is purely inside content.

    In src/app/(admin)/admin/page.tsx:
    6. Add ShoppingBag, Package, PackageX to lucide-react imports.
    7. Pass icon prop to each StatCard per D-04:
       - "Нові замовлення" StatCard → icon={ShoppingBag}
       - "Товари в наявності" StatCard → icon={Package}
       - "Розпродано" StatCard → icon={PackageX}
  </action>
  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npx tsc --noEmit 2>&1 | grep -E "stat-card|admin/page|error" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/admin/stat-card.tsx StatCardProps includes icon?: React.ElementType
    - StatCard renders Icon with className containing "size-5" and "text-muted-foreground" and aria-hidden when icon prop is provided
    - StatCard renders identically to before when icon prop is absent (no breaking change)
    - src/app/(admin)/admin/page.tsx passes icon={ShoppingBag} to "Нові замовлення", icon={Package} to "Товари в наявності", icon={PackageX} to "Розпродано"
    - All three icon names (ShoppingBag, Package, PackageX) are imported from "lucide-react" in admin/page.tsx
    - TypeScript compilation passes with no new errors in either file
  </acceptance_criteria>
  <done>Each stat card shows its lucide icon top-right in muted color; cards without icon prop render without regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Both tasks are purely presentational — no data input, no API calls, no auth boundaries touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-SC | Tampering | npm installs | accept | No new packages installed — lucide-react already in package.json; no install step needed |
</threat_model>

<verification>
After both tasks complete:
1. npx tsc --noEmit passes with zero new errors.
2. Grep confirms: src/app/(admin)/admin/page.tsx contains "Plus", "Eye", "ShoppingBag", "Package", "PackageX" from "lucide-react".
3. Grep confirms: src/components/admin/stat-card.tsx contains "icon?: React.ElementType" and "size-5".
4. Visual check at /admin: "Додати товар" is blue with Plus icon; "Переглянути замовлення" is outlined with Eye icon; each stat card shows its icon top-right.
</verification>

<success_criteria>
- "Додати товар" renders as blue primary button with Plus icon (default variant, no size prop)
- "Переглянути замовлення" renders as outline button with Eye icon (no size prop)
- StatCard accepts optional icon?: React.ElementType prop; when provided, icon appears top-right at size-5 text-muted-foreground aria-hidden
- ShoppingBag on "Нові замовлення", Package on "Товари в наявності", PackageX on "Розпродано"
- TypeScript compiles cleanly; no regressions to existing StatCard usages without icon prop
</success_criteria>

<output>
Create `.planning/phases/32-admin-dashboard-polish/32-01-SUMMARY.md` when done.
</output>
