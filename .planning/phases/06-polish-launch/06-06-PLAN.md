---
phase: 06-polish-launch
plan: 06
type: execute
wave: 5
gap_closure: true
depends_on:
  - 06-04
  - 06-05
files_modified:
  - src/app/layout.tsx
  - src/components/chat/chat-provider.tsx
  - src/components/catalog/product-card.tsx
  - e2e/smoke-deploy.spec.ts
  - .planning/phases/06-polish-launch/06-VERIFICATION.md
  - .planning/phases/06-polish-launch/06-ENV-CHECKLIST.md
autonomous: true
requirements:
  - PERF-01
context_decisions:
  - "D-06-07: If CWV miss after preview lab, apply perf fixes — do not ship without recorded scores"
  - "D-06-08: No Lighthouse CI — local prod build check only; authoritative lab is Vercel preview (06-07)"
  - "D-06-09: Minimal targeted fixes beyond prior audit pass"
  - "D-06-17: Document PLAYWRIGHT_BASE_URL in smoke spec header"

must_haves:
  truths:
    - "Storefront loads a single webfont stack without duplicate Geist imports"
    - "Chat FAB/panel defer until after hydration to reduce main-thread and layout shift risk"
    - "06-VERIFICATION.md distinguishes dev localhost vs Vercel preview/production lab URLs"
    - "06-ENV-CHECKLIST.md has fields to record preview and production origins"
  artifacts:
    - path: src/app/layout.tsx
      provides: single font loading path
    - path: .planning/phases/06-polish-launch/06-VERIFICATION.md
      provides: preview/production lab instructions
      contains: "Vercel preview"
    - path: e2e/smoke-deploy.spec.ts
      provides: documented remote smoke contract
      contains: "PLAYWRIGHT_BASE_URL"
  key_links:
    - from: src/app/layout.tsx
      to: GeistSans
      via: className only (no second next/font)
      pattern: "GeistSans"
    - from: e2e/smoke-deploy.spec.ts
      to: playwright.config.ts
      via: baseURL from PLAYWRIGHT_BASE_URL
      pattern: "PLAYWRIGHT_BASE_URL"

---

## Phase Goal

**As a** покупець на мобільному, **I want to** швидко відкрити головну, каталог і PDP, **so that** магазин проходить CWV gate перед promote (PERF-01, D-06-07).

<objective>
Закрити кодову частину CWV gap: прибрати дубль шрифтів, зменшити CLS ризик на каталозі, оновити verification/checklist для lab на production build (не `next dev`).

Purpose: Phase verification failed lab on localhost dev; production-like bundle + targeted fixes before operator re-run (06-07).
Output: Perf commits + updated operator docs; green `npm run build`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-polish-launch/06-PHASE-VERIFICATION.md
@.planning/phases/06-polish-launch/06-VERIFICATION.md
@.planning/phases/06-polish-launch/06-04-SUMMARY.md
@src/app/layout.tsx
@src/components/chat/chat-provider.tsx
@src/components/catalog/product-card.tsx
@e2e/smoke-deploy.spec.ts

<interfaces>
Current layout (duplicate fonts — fix target):
- `GeistSans` from `geist/font/sans` applied via `GeistSans.className`
- `Geist` from `next/font/google` also applied via `geist.variable` on `<html>`

CWV targets (D-06-07): LCP ≤ 2.5s, CLS ≤ 0.1, INP ≤ 200ms on mobile lab for `/`, `/katalog`, PDP `bosch-kholodylnyky-8-available`.

Smoke remote (D-06-17):
`PLAYWRIGHT_BASE_URL=https://<origin> npx playwright test e2e/smoke-deploy.spec.ts`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CWV perf fixes — fonts, chat deferral, image stability</name>
  <read_first>
    - src/app/layout.tsx — duplicate GeistSans + next/font Geist
    - src/components/chat/chat-provider.tsx — ChatFab/ChatPanel mount
    - src/components/catalog/product-card.tsx — aspect ratio container
    - .planning/phases/06-polish-launch/06-PATTERNS.md — PERF / product-card / OptimizedImage analogs
    - .planning/phases/06-polish-launch/06-PHASE-VERIFICATION.md — CLS 0.171 on catalog/PDP
  </read_first>
  <files>
    src/app/layout.tsx
    src/components/chat/chat-provider.tsx
    src/components/catalog/product-card.tsx
  </files>
  <action>
    Per D-06-07/D-06-09, apply **minimal** fixes tied to measured CLS/LCP failures (not a refactor):

    1. **Single font stack (layout.tsx):** Remove the redundant `Geist` import from `next/font/google` and `geist.variable` on `<html>`. Keep only `GeistSans` from `geist/font/sans` with `GeistSans.className` on `<html lang="uk">`. Do not add a new font package.

    2. **Defer chat chrome (chat-provider.tsx):** Use `next/dynamic` with `{ ssr: false }` for `ChatFab` and `ChatPanel` imports so Pusher client and fixed FAB do not compete with LCP on first paint. Keep `ChatProvider` context logic unchanged; only lazy-load the visual chrome.

    3. **Catalog image stability (product-card.tsx):** Keep `fill` + `object-cover` inside the `aspect-[4/3]` container. Add `sizes="(max-width: 768px) 50vw, 25vw"` if missing. On the wrapper `div` (not the image), set `min-h-[12rem]` or equivalent fixed min-height so the grid cell reserves space before image decode — **do not** combine `fill` with explicit `width`/`height` on the same CldImage (invalid). Goal: reduce CLS on `/katalog` grid per PATTERNS product-card analog.

    Do **not** add `priority` to catalog card images (only hero + PDP main image keep priority per D-06-09).
  </action>
  <acceptance_criteria>
    - layout.tsx imports only one Geist source (no `next/font/google` Geist)
    - chat-provider uses dynamic import for Fab and Panel with ssr false
    - product-card wrapper reserves layout space (sizes and/or min-height) without fill+width+height on same CldImage
    - `npm run build` completes without TypeScript errors
  </acceptance_criteria>
  <verify>
    <automated>npm run build && ! grep -q "next/font/google" src/app/layout.tsx && grep -q "dynamic" src/components/chat/chat-provider.tsx</automated>
  </verify>
  <done>Perf code changes merged; production build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Update verification + env checklist + smoke docs for preview/prod URLs</name>
  <read_first>
    - .planning/phases/06-polish-launch/06-VERIFICATION.md — current localhost scores
    - .planning/phases/06-polish-launch/06-ENV-CHECKLIST.md — deploy runbook
    - e2e/smoke-deploy.spec.ts — 4 public route tests
    - .planning/phases/06-polish-launch/06-CONTEXT.md — D-06-06, D-06-13, D-06-16, D-06-17
  </read_first>
  <files>
    .planning/phases/06-polish-launch/06-VERIFICATION.md
    .planning/phases/06-polish-launch/06-ENV-CHECKLIST.md
    e2e/smoke-deploy.spec.ts
  </files>
  <action>
    1. **06-VERIFICATION.md** (per D-06-06/08/13):
       - Replace preview URL placeholder `http://localhost:3000` with empty field `https://<vercel-preview-host>` and add warning: **lab on `next dev` is invalid for ship gate** — use Vercel preview or `npm run build && npm run start` on port 3000.
       - Add subsection **Local production build sanity** with exact commands:
         `npm run build && npm run start` then mobile Lighthouse on `http://localhost:3000` for the three URLs (operator may run before push; authoritative gate remains preview in 06-07).
       - Reset Lighthouse table Pass/Fail to unchecked for preview re-run; keep prior localhost dev scores in a collapsed «Dev lab (invalid for ship)» note so history is not lost.
       - Add **Production origin** field (empty): `https://<production-host>` for post-promote smoke cross-reference.

    2. **06-ENV-CHECKLIST.md** — new § **Deployment record (gap closure)** with table columns: Environment | Origin URL | Deploy ID / timestamp | Operator | Notes. Rows: Preview (before promote), Production (after promote). Reference D-06-14/15/16.

    3. **e2e/smoke-deploy.spec.ts** — file-top comment block (5–8 lines) stating: requires `PLAYWRIGHT_BASE_URL` for remote smoke; when unset, `playwright.config.ts` starts `npm run dev`; ship blocker command from D-06-17 with example `https://your-app.vercel.app`. Do not change test assertions.

    Do not add Lighthouse CI or new npm dependencies (D-06-08).
  </action>
  <acceptance_criteria>
    - VERIFICATION.md warns against next dev lab for ship gate
    - ENV-CHECKLIST has deployment record table with Preview and Production rows
    - smoke-deploy.spec.ts documents PLAYWRIGHT_BASE_URL at top of file
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Vercel preview" .planning/phases/06-polish-launch/06-VERIFICATION.md && grep -q "Deployment record" .planning/phases/06-polish-launch/06-ENV-CHECKLIST.md && head -n 8 e2e/smoke-deploy.spec.ts | grep -q "PLAYWRIGHT_BASE_URL"</automated>
  </verify>
  <done>Operator artifacts ready for preview/production URL recording</done>
</task>

<task type="auto">
  <name>Task 3: Local production-build sanity + unit gate</name>
  <read_first>
    - package.json — build/start scripts
    - .planning/phases/06-polish-launch/06-VERIFICATION.md — Local production build subsection from Task 2
  </read_first>
  <files>.planning/phases/06-polish-launch/06-VERIFICATION.md</files>
  <action>
    After Task 1 changes:
    1. Run `npm test` — must pass (no regressions).
    2. Run `npm run build` — must pass.
    3. Start `npm run start` in background on port 3000; run **one** quick check: `curl -sf -o /dev/null -w "%{http_code}" http://localhost:3000/` returns 200; stop server.
    4. In `06-VERIFICATION.md` § Local production build sanity, record date and «build OK / home 200» — do **not** claim CWV pass until 06-07 preview lab.

    If build fails, fix Task 1 before marking plan complete.
  </action>
  <acceptance_criteria>
    - npm test and npm run build green
    - VERIFICATION.md local sanity row filled with build status
  </acceptance_criteria>
  <verify>
    <automated>npm test && npm run build</automated>
  </verify>
  <done>Production bundle builds; ready for Vercel preview deploy (06-07)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| layout.tsx → browser | Font/CSS delivery affects all pages |
| VERIFICATION.md → operators | May contain public preview URLs only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | Tampering | Perf gate on dev lab | mitigate | VERIFICATION.md forbids next dev as ship evidence (D-06-07) |
| T-06-06-02 | DoS | Chat dynamic import | accept | Lazy load reduces attack surface on first paint; auth unchanged |

</threat_model>

<verification>
npm test
npm run build
grep -q "Vercel preview" .planning/phases/06-polish-launch/06-VERIFICATION.md
</verification>

<success_criteria>
Duplicate font removed; chat chrome deferred; docs updated for preview/prod lab; build green — ready for 06-07 operator Lighthouse on Vercel preview
</success_criteria>

<output>
Create `.planning/phases/06-polish-launch/06-06-SUMMARY.md` when done
</output>
