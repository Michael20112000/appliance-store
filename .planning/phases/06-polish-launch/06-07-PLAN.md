---
phase: 06-polish-launch
plan: 07
type: execute
wave: 6
gap_closure: true
depends_on:
  - 06-06
files_modified:
  - .planning/phases/06-polish-launch/06-VERIFICATION.md
  - .planning/phases/06-polish-launch/06-ENV-CHECKLIST.md
autonomous: false
requirements:
  - PERF-01
  - SEO-02
user_setup:
  - service: vercel
    why: Preview deployment URL for mobile Lighthouse and Rich Results (D-06-06/11/13)
    dashboard_config:
      - task: "Confirm latest deployment includes 06-06 perf commits"
        location: "Vercel → Deployments → Preview"
context_decisions:
  - "D-06-06: Mobile lab Lighthouse on /, /katalog, seed PDP before production promote"
  - "D-06-07: LCP ≤2.5s, CLS ≤0.1 — record pass/fail; if fail return to perf fixes, do not promote"
  - "D-06-08: Manual lab only — no Lighthouse CI"
  - "D-06-11: Rich Results on public preview URLs"
  - "D-06-13: SEO/perf gate on preview deployment before promote"
  - "D-06-16: Preview green before production deploy"

must_haves:
  truths:
    - "Mobile Lighthouse on Vercel preview records LCP ≤2.5s and CLS ≤0.1 for /, /katalog, and seed PDP OR explicit blocker with scores"
    - "Google Rich Results Test passes on preview home and PDP public URLs"
    - "Preview origin URL and deploy metadata recorded in phase artifacts"
  artifacts:
    - path: .planning/phases/06-polish-launch/06-VERIFICATION.md
      provides: passing or documented-failing CWV on preview
      contains: "https://"
    - path: .planning/phases/06-polish-launch/06-ENV-CHECKLIST.md
      provides: preview deployment record row filled
  key_links:
    - from: 06-VERIFICATION.md
      to: Vercel preview origin
      via: operator Lighthouse run
      pattern: "Preview deployment URL"

---

## Phase Goal

**As a** власник магазину, **I want to** підтвердити швидкість і SEO на реальному Vercel preview, **so that** promote на production не сліпий (D-06-06/11/13).

<objective>
Закрити CWV truth gap і Rich Results deferral: operator runs mobile Lighthouse + Google Rich Results on **Vercel preview** (production build), records scores in `06-VERIFICATION.md`.

Purpose: Phase verification failed because only localhost `next dev` lab existed.
Output: Filled VERIFICATION with preview URL; CWV pass or documented remediation loop back to 06-06.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-polish-launch/06-06-SUMMARY.md
@.planning/phases/06-polish-launch/06-VERIFICATION.md
@.planning/phases/06-polish-launch/06-ENV-CHECKLIST.md
@.planning/phases/06-polish-launch/06-PHASE-VERIFICATION.md

<interfaces>
Lighthouse URLs (D-06-06):
- `{previewOrigin}/`
- `{previewOrigin}/katalog`
- `{previewOrigin}/tovar/bosch-kholodylnyky-8-available`

Targets (D-06-07): LCP ≤ 2.5s · CLS ≤ 0.1 · INP ≤ 200ms (mobile lab)

Rich Results (D-06-11): https://search.google.com/test/rich-results
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Deploy 06-06 changes to Vercel preview</name>
  <read_first>
    - .planning/phases/06-polish-launch/06-ENV-CHECKLIST.md — Deploy runbook steps 1–2
    - .planning/phases/06-polish-launch/06-06-SUMMARY.md — commits to include
  </read_first>
  <files>.planning/phases/06-polish-launch/06-ENV-CHECKLIST.md</files>
  <action>
    Human: push `main` (or open PR merged to `main`) so Vercel creates a **Preview** deployment containing 06-06 perf fixes. Copy the preview `https://` origin (no trailing slash). In `06-ENV-CHECKLIST.md` § Deployment record, fill **Preview** row: Origin URL, Deploy ID or ISO timestamp, operator name.
    Set `06-VERIFICATION.md` **Preview deployment URL** to the same origin.
    Confirm GitHub Actions CI green on the commit (D-06-23) or run locally `npm test && npm run test:e2e` if CI pending.
  </action>
  <acceptance_criteria>
    - Preview HTTPS origin accessible in browser (home loads)
    - ENV-CHECKLIST Preview row filled
    - VERIFICATION.md preview URL field is https preview host (not localhost)
  </acceptance_criteria>
  <verify>
    <automated>grep -qE 'https://[^[:space:]]+' .planning/phases/06-polish-launch/06-VERIFICATION.md && grep -q "Preview" .planning/phases/06-polish-launch/06-ENV-CHECKLIST.md</automated>
  </verify>
  <done>Preview deployment live and documented</done>
  <resume-signal>Paste preview origin URL or type "preview ready"</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Mobile Lighthouse on Vercel preview (3 URLs)</name>
  <read_first>
    - .planning/phases/06-polish-launch/06-VERIFICATION.md — Lighthouse table
    - .planning/phases/06-polish-launch/06-CONTEXT.md — D-06-06, D-06-07, D-06-08
  </read_first>
  <files>.planning/phases/06-polish-launch/06-VERIFICATION.md</files>
  <action>
    On the **Vercel preview** origin from Task 1 (production build — NOT `next dev`):

    1. Chrome DevTools → Lighthouse → **Mobile** → analyze:
       - `/`
       - `/katalog`
       - `/tovar/bosch-kholodylnyky-8-available`
    2. Record LCP, CLS, INP (or n/a) and Pass/Fail vs D-06-07 targets in `06-VERIFICATION.md` main table.
    3. Set **Дата прогону** to today (ISO).
    4. If **any** URL fails LCP or CLS: stop promote path; file issue for additional 06-06-style code fix OR document measured scores + remediation plan in § Perf remediation — do **not** check «ready for prod promote» without numeric scores (D-06-07).
    5. If all three pass: check Sign-off row «Lighthouse 3 URL» as Pass.

    Optional CLI (if installed): `npx lighthouse <preview-url>/ --preset=perf --form-factor=mobile --output=json --quiet` — not required; DevTools lab is authoritative per D-06-08.
  </action>
  <what-built>06-06 perf fixes deployed to preview; VERIFICATION template ready</what-built>
  <how-to-verify>
    1. Open preview origin in Chrome mobile emulation or real phone.
    2. Run Lighthouse mobile on each of the three URLs listed above.
    3. Confirm LCP ≤ 2.5s and CLS ≤ 0.1 on all three for Pass.
    4. Fill every cell in the Lighthouse table in 06-VERIFICATION.md.
  </how-to-verify>
  <acceptance_criteria>
    - All three Lighthouse rows have numeric LCP and CLS values
    - Preview base URL at top of VERIFICATION matches Task 1 origin
    - Pass/Fail explicitly marked per D-06-07
  </acceptance_criteria>
  <verify>
    <automated>grep -qE 'LCP.*[0-9]+(\.[0-9]+)?s' .planning/phases/06-polish-launch/06-VERIFICATION.md && grep -qE 'CLS.*0\.[0-9]+' .planning/phases/06-polish-launch/06-VERIFICATION.md && test "$(grep -cE 'LCP.*[0-9]' .planning/phases/06-polish-launch/06-VERIFICATION.md)" -ge 3 && ! grep -v 'Dev lab' .planning/phases/06-polish-launch/06-VERIFICATION.md | grep -q 'localhost:3000'</automated>
  </verify>
  <done>CWV pass on preview lab (all 3 URLs meet D-06-07) OR fail documented with promote blocked — return to 06-06 for code fixes</done>
  <resume-signal>Type "lighthouse pass" or "lighthouse fail" with scores</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Rich Results + robots on preview</name>
  <read_first>
    - .planning/phases/06-polish-launch/06-VERIFICATION.md — Rich Results table
    - e2e/catalog-seo.spec.ts — automated JSON-LD checks (already green)
  </read_first>
  <files>.planning/phases/06-polish-launch/06-VERIFICATION.md</files>
  <action>
    Per D-06-11 on **public preview URLs** (same origin as Task 1):

    1. [Google Rich Results Test](https://search.google.com/test/rich-results) — `{preview}/` → expect valid **LocalBusiness** with addressLocality Львів.
    2. Rich Results — `{preview}/tovar/bosch-kholodylnyky-8-available` → expect **Product** with UsedCondition (or RefurbishedCondition).
    3. `GET {preview}/robots.txt` — Sitemap line + Disallow /admin (curl or browser).
    4. Fill Rich Results and robots sections in `06-VERIFICATION.md`; mark Sign-off Rich Results and robots as Pass when green.

    Automated sanity (Claude may run if preview URL provided in resume): `npx playwright test e2e/catalog-seo.spec.ts` against localhost is **not** a substitute for this task.
  </action>
  <what-built>Preview storefront with JSON-LD from prior phases</what-built>
  <how-to-verify>
    1. Submit preview home URL to Rich Results Test — no critical errors for LocalBusiness.
    2. Submit preview PDP URL — Product rich result valid.
    3. Open /robots.txt on preview — confirm sitemap URL uses preview host.
  </how-to-verify>
  <acceptance_criteria>
    - Rich Results table has both URLs filled with Pass or documented Fail
    - robots.txt checks marked OK on preview
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Rich Results" .planning/phases/06-polish-launch/06-VERIFICATION.md && grep -qE 'bosch-kholodylnyky-8-available|Preview' .planning/phases/06-polish-launch/06-VERIFICATION.md</automated>
  </verify>
  <done>SEO manual preview gate complete (D-06-11/13)</done>
  <resume-signal>Type "seo pass" when Rich Results + robots are recorded</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Preview URL → Google Rich Results | Public URLs only; no admin paths |
| Operator → VERIFICATION.md | No secrets in recorded URLs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07-01 | Tampering | CWV gate bypass | mitigate | Blocking checkpoint; numeric scores required (D-06-07) |
| T-06-07-02 | Spoofing | Wrong deployment tested | mitigate | Record deploy ID in ENV-CHECKLIST Preview row |

</threat_model>

<verification>
grep -qE 'https://[^[:space:]]+' .planning/phases/06-polish-launch/06-VERIFICATION.md
grep -q "LCP" .planning/phases/06-polish-launch/06-VERIFICATION.md
</verification>

<success_criteria>
Preview Lighthouse table filled on Vercel origin with numeric scores. **If all three URLs pass D-06-07 (LCP ≤2.5s, CLS ≤0.1):** Rich Results + robots pass on preview → plan complete, **ready for 06-08** production promote. **If any URL fails:** document scores in § Perf remediation, Sign-off Lighthouse remains Fail, **do not proceed to 06-08** — re-run 06-06 perf fixes and repeat 06-07 preview lab (D-06-07).
</success_criteria>

<output>
Create `.planning/phases/06-polish-launch/06-07-SUMMARY.md` when done
</output>
