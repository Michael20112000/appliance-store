---
phase: 06-polish-launch
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/critical-journey.spec.ts
  - e2e/catalog-seo.spec.ts
autonomous: true
requirements:
  - SEO-01
  - SEO-02
context_decisions:
  - "D-06-01: Phase gate runs entire e2e/ — these specs are part of it"
  - "D-06-02: Separate spec files per domain — do not merge into monolith"
  - "D-06-03: critical-journey.spec.ts — browse → cart → checkout → ASL- confirmation with unique marker"
  - "D-06-10: Extend catalog-seo — lang uk, JSON-LD UsedCondition, sitemap sold exclusion, sample URL 200"

must_haves:
  truths:
    - "One Playwright spec proves full buyer purchase path end-to-end"
    - "Automated SEO checks cover Ukrainian lang, Product JSON-LD condition, and sitemap integrity"
    - "Sold demo SKU never appears in sitemap.xml"
  artifacts:
    - path: e2e/critical-journey.spec.ts
      provides: cross-cutting commerce journey
      min_lines: 25
    - path: e2e/catalog-seo.spec.ts
      provides: SEO-01/02 automation
  key_links:
    - from: e2e/critical-journey.spec.ts
      to: /zamovlennia/pidtverdzhennia
      via: checkout submit
      pattern: "pidtverdzhennia/ASL-"
    - from: e2e/catalog-seo.spec.ts
      to: /sitemap.xml
      via: request.get
      pattern: "samsung-kholodylnyk-sold-demo-sold"

---

## Phase Goal

**As a** покупець у Львові, **I want to** пройти шлях від каталогу до підтвердження замовлення, **so that** магазин знає, що головний revenue flow не зламаний перед launch.

<objective>
Додати `critical-journey.spec.ts` і розширити `catalog-seo.spec.ts` для автоматичного SEO gate (D-06-10).

Purpose: Cross-cutting E2E + SEO-01/02 verification без нових npm пакетів.
Output: Green specs under `npm run test:e2e`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-polish-launch/06-PATTERNS.md
@e2e/checkout.spec.ts
@e2e/orders-history.spec.ts
@e2e/product-pdp.spec.ts
@e2e/locale.spec.ts
@e2e/helpers/catalog.ts
@src/components/checkout/checkout-form.tsx
@src/app/(storefront)/zamovlennia/pidtverdzhennia/[orderNumber]/page.tsx

<interfaces>
SOLD_SLUG constant (from e2e/product-pdp.spec.ts):
`samsung-kholodylnyk-sold-demo-sold`

Checkout notes field label: `Коментар (необов'язково)` — confirmation page does NOT render notes; assert ASL- URL + optional /kabinet history (06-PATTERNS.md).

Catalog helpers:
- openFirstCatalogProduct(page)
- addCurrentProductToCart(page)

Registration pattern from e2e/checkout.spec.ts (lines 10-16).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add critical-journey.spec.ts</name>
  <read_first>
    - e2e/checkout.spec.ts — registration + checkout flow
    - e2e/orders-history.spec.ts — post-order /kabinet assertion
    - .planning/phases/06-polish-launch/06-PATTERNS.md — marker assertion rules (D-06-03)
  </read_first>
  <files>e2e/critical-journey.spec.ts</files>
  <action>
    Create `e2e/critical-journey.spec.ts` per D-06-03 (single test, no admin/chat):
    1. Register unique buyer (`critical-journey-${Date.now()}@example.com`, password `password123`) via `/reiestratsiia` — mirror checkout.spec.ts labels (Ukrainian).
    2. `openFirstCatalogProduct` → `addCurrentProductToCart` from `./helpers/catalog`.
    3. Goto `/zamovlennia`, fill name/phone, select «Самовивіз (м. Львів)».
    4. Unique marker: `const marker = \`E2E-JOURNEY-${Date.now()}\`;` fill `getByLabel(/Коментар/i)` before submit (notes stored in DB even if not on confirmation UI).
    5. Submit «Підтвердити замовлення» → expect URL `/zamovlennia/pidtverdzhennia/ASL-` and «Дякуємо за замовлення!».
    6. Capture `orderNumber` from URL or page text; goto `/kabinet` → expect «Мої замовлення» and order number visible (pattern from orders-history.spec.ts) — proves marker persisted without requiring notes on confirmation page.
    Do NOT combine admin or chat steps in this file (D-06-02/03).
  </action>
  <acceptance_criteria>
    - Spec file exists and imports catalog helpers
    - Test passes locally with seeded DB: `npx playwright test e2e/critical-journey.spec.ts`
    - No dependency on Pusher or Cloudinary API secrets
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test e2e/critical-journey.spec.ts --reporter=line</automated>
  </verify>
  <done>D-06-03 critical path automated</done>
</task>

<task type="auto">
  <name>Task 2: Extend catalog-seo.spec.ts</name>
  <read_first>
    - e2e/catalog-seo.spec.ts — existing three tests
    - e2e/locale.spec.ts — lang="uk" assertion
    - src/lib/catalog/product-json-ld.ts — CONDITION_MAP (UsedCondition / RefurbishedCondition)
    - src/server/services/catalog.service.ts — listPublicProductSlugsForSitemap AVAILABLE only
  </read_first>
  <files>e2e/catalog-seo.spec.ts</files>
  <action>
    Extend `e2e/catalog-seo.spec.ts` per D-06-10 (keep separate file, D-06-02):
    - **lang uk:** test visiting `/` → `html[lang="uk"]` (may duplicate locale.spec.ts one-liner — acceptable).
    - **UsedCondition:** in existing PDP JSON-LD test, assert content matches `/UsedCondition|RefurbishedCondition/` (SEO-02).
    - **sitemap sold exclusion:** import/redeclare `SOLD_SLUG = "samsung-kholodylnyk-sold-demo-sold"`; `request.get("/sitemap.xml")` → body must NOT contain `/tovar/${SOLD_SLUG}`.
    - **sample sitemap URL 200:** parse first `<loc>…/tovar/…</loc>` from sitemap XML, `request.get(pathname)` → `ok()`.
    - **category title (SEO-01):** keep/enhance existing category title test.
    Do not remove existing assertions; only add/strengthen.
  </action>
  <acceptance_criteria>
    - catalog-seo.spec.ts has at least 5 tests after extend
    - Sold slug absent from sitemap body
    - PDP JSON-LD includes used condition schema URL
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test e2e/catalog-seo.spec.ts --reporter=line</automated>
  </verify>
  <done>D-06-10 automated SEO gate complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright → localhost app | Tests use seeded data only; unique emails per run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Repudiation | critical-journey orders | accept | Test orders in CI DB branch; not production |
| T-06-02-02 | Tampering | sitemap E2E | mitigate | Assert sold slug excluded — catches catalog filter regressions |

</threat_model>

<verification>
npx playwright test e2e/critical-journey.spec.ts e2e/catalog-seo.spec.ts --reporter=line
</verification>

<success_criteria>
Both spec files green locally; critical journey covers catalog→cart→checkout→confirmation→kabinet; catalog-seo covers D-06-10 list
</success_criteria>

<output>
Create `.planning/phases/06-polish-launch/06-02-SUMMARY.md` when done
</output>
