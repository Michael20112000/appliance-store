---
phase: 06-polish-launch
plan: 04
type: execute
wave: 3
depends_on:
  - 06-02
  - 06-03
files_modified:
  - .planning/phases/06-polish-launch/06-VERIFICATION.md
  - src/components/media/optimized-image.tsx
  - src/components/catalog/product-gallery.tsx
  - src/components/catalog/product-card.tsx
autonomous: false
requirements:
  - PERF-01
  - SEO-02
context_decisions:
  - "D-06-06: Mobile lab Lighthouse on /, /katalog, one seed PDP before prod promote"
  - "D-06-07: LCP ≤2.5s, CLS ≤0.1, INP ≤200ms — record in 06-VERIFICATION.md; perf code task if miss"
  - "D-06-08: No Lighthouse CI — manual gate only"
  - "D-06-09: Verify OptimizedImage/CldImage and catalog Prisma queries"
  - "D-06-11: Manual Rich Results Test on preview before prod"
  - "D-06-13: SEO manual gate on preview deployment"

must_haves:
  truths:
    - "06-VERIFICATION.md exists with score tables for three URLs and Rich Results checklist"
    - "OptimizedImage consumers use format auto, quality auto, sizes, PDP priority"
    - "Launch team knows whether CWV targets pass or what perf fix was applied"
  artifacts:
    - path: .planning/phases/06-polish-launch/06-VERIFICATION.md
      provides: manual Lighthouse + Rich Results gate
      min_lines: 40
  key_links:
    - from: src/components/catalog/product-gallery.tsx
      to: OptimizedImage
      via: priority + sizes props
      pattern: "priority"

---

## Phase Goal

**As a** покупець на телефоні, **I want to** швидко відкрити каталог і картку товару, **so that** магазин не втрачає клієнтів через повільні сторінки (PERF-01).

<objective>
Створити manual verification checklist (Lighthouse + Rich Results) і виконати perf code review pass (D-06-09); зафіксувати результати або відкрити мінімальні фікси.

Purpose: PERF-01 + D-06-06–11 pre-prod gates without Lighthouse CI.
Output: Completed or explicitly deferred-with-fix `06-VERIFICATION.md`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-polish-launch/06-VALIDATION.md
@.planning/research/PITFALLS.md
@src/components/media/optimized-image.tsx
@src/components/catalog/product-gallery.tsx
@src/components/catalog/product-card.tsx
@src/server/services/catalog.service.ts
@src/app/(storefront)/page.tsx

<interfaces>
OptimizedImage defaults:
- format="auto", quality="auto"
- sizes default "(max-width: 1024px) 100vw, 50vw"

PDP gallery: priority + sizes="(max-width: 768px) 100vw, 50vw"
Product card: sizes="(max-width: 768px) 50vw, 25vw"

Lighthouse URLs (D-06-06):
- `/`
- `/katalog` (or `/katalog/kholodylnyky` — document chosen slug in VERIFICATION)
- One AVAILABLE seed PDP — pick first `/tovar/` link from catalog or document slug in VERIFICATION
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 06-VERIFICATION.md manual gate template</name>
  <read_first>
    - .planning/phases/06-polish-launch/06-CONTEXT.md — D-06-06–08, D-06-11, D-06-13
    - .planning/phases/06-polish-launch/06-VALIDATION.md — manual-only rows
  </read_first>
  <files>.planning/phases/06-polish-launch/06-VERIFICATION.md</files>
  <action>
    Create `06-VERIFICATION.md` with:
    - **Preview deployment URL** field (filled before prod promote, D-06-13)
    - **Mobile Lighthouse lab** table for 3 URLs: columns URL, LCP, CLS, INP, Pass/Fail against targets (2.5s / 0.1 / 200ms) per D-06-07
    - **Rich Results** checklist (D-06-11): home LocalBusiness Lviv + one PDP Product/UsedCondition — link to Google Rich Results Test, pass/fail checkbox
    - **robots.txt review** on preview: Sitemap line present (after 06-03)
    - **Automated SEO already green** pointer: `npx playwright test e2e/catalog-seo.spec.ts`
    - **If CWV fail:** section «Perf remediation» with checkbox linking to Task 2 fixes (D-06-07 — do not ship without recording scores)
    - Explicit: no Lighthouse CI (D-06-08); GSC post-launch (D-06-12) out of scope
    Ukrainian section headings acceptable for operator readability
  </action>
  <acceptance_criteria>
    - File exists with Lighthouse table for exactly 3 URLs
    - Rich Results and robots review sections present
    - Targets documented matching D-06-07
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/06-polish-launch/06-VERIFICATION.md && grep -q 'LCP' .planning/phases/06-polish-launch/06-VERIFICATION.md && grep -q 'Rich Results' .planning/phases/06-polish-launch/06-VERIFICATION.md</automated>
  </verify>
  <done>Manual gate template ready for preview deploy</done>
</task>

<task type="auto">
  <name>Task 2: Perf code review pass (PERF-01 / D-06-09)</name>
  <read_first>
    - src/components/media/optimized-image.tsx
    - src/components/catalog/product-gallery.tsx
    - src/components/catalog/product-card.tsx
    - src/server/services/catalog.service.ts — findMany + include (no list N+1)
    - .planning/phases/06-polish-launch/06-PATTERNS.md — OptimizedImage section
  </read_first>
  <files>
    src/components/media/optimized-image.tsx
    src/components/catalog/product-card.tsx
    src/components/catalog/product-gallery.tsx
  </files>
  <action>
    Audit PERF-01 surfaces per D-06-09 (change files **only if regression found**):
    - Confirm `OptimizedImage` keeps `format="auto"` and `quality="auto"`
    - Confirm PDP main image has `priority` and sensible `sizes`
    - Confirm catalog cards pass `sizes` (not missing on list thumbnails)
    - Skim `catalog.service.ts` list queries — single `findMany` with `include`; no per-row fetch loops
    If Lighthouse preview scores (from Task 3 checkpoint) miss LCP target, apply **minimal** fix only (e.g. add missing `sizes`, ensure hero/PDP priority) — no new npm packages, no Lighthouse CI
    Document audit outcome in `06-VERIFICATION.md` § Code review (pass / changes made)
  </action>
  <acceptance_criteria>
    - OptimizedImage still uses auto format/quality
    - PDP gallery retains priority prop
    - VERIFICATION.md records code review result
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'format="auto"' src/components/media/optimized-image.tsx && grep -q 'priority' src/components/catalog/product-gallery.tsx</automated>
  </verify>
  <done>D-06-09 perf pass documented; minimal fixes applied if needed</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Record preview Lighthouse + Rich Results scores</name>
  <read_first>
    - .planning/phases/06-polish-launch/06-VERIFICATION.md — tables to fill
    - .planning/phases/06-polish-launch/06-ENV-CHECKLIST.md — preview promote note
  </read_first>
  <files>.planning/phases/06-polish-launch/06-VERIFICATION.md</files>
  <action>
    Human fills `06-VERIFICATION.md` after running mobile Lighthouse lab and Rich Results Test on the **Vercel preview** URL (D-06-06, D-06-11, D-06-13). Record preview base URL at top of file. If any metric fails D-06-07 targets, confirm Task 2 remediation or document explicit blocker — do not promote to production without scores recorded.
  </action>
  <what-built>06-VERIFICATION.md template and PERF-01 code audit; preview deployment should be available from Vercel</what-built>
  <how-to-verify>
    1. Open Vercel **preview** URL for current `main` (not production yet — D-06-13).
    2. Chrome DevTools → Lighthouse → **Mobile** → run on `/`, `/katalog` (or documented category URL), and one seed PDP `/tovar/...`.
    3. Fill LCP/CLS/INP in `06-VERIFICATION.md`; mark Pass/Fail vs D-06-07 targets.
    4. Run [Google Rich Results Test](https://search.google.com/test/rich-results) on preview home + same PDP; check LocalBusiness (Львів) and Product/UsedCondition; record pass/fail.
    5. Open `https://<preview>/robots.txt` — confirm Sitemap line and /admin disallow.
    6. If any CWV metric fails, ensure Task 2 remediation is done or document explicit deferral with reason in VERIFICATION (D-06-07).
  </how-to-verify>
  <acceptance_criteria>
    - All Lighthouse table rows filled with numeric scores
    - Rich Results section marked pass or fail with URLs noted
    - Preview base URL recorded at top of VERIFICATION.md
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'Preview deployment URL' .planning/phases/06-polish-launch/06-VERIFICATION.md && ! grep -qE 'TODO|TBD|___' .planning/phases/06-polish-launch/06-VERIFICATION.md && grep -E 'LCP.*[0-9]+(\.[0-9]+)?s|CLS.*0\.[0-9]+|INP.*[0-9]+' .planning/phases/06-polish-launch/06-VERIFICATION.md | wc -l | awk '{exit ($1>=3)?0:1}'</automated>
  </verify>
  <done>Human approved VERIFICATION.md with preview Lighthouse + Rich Results recorded</done>
  <resume-signal>Type "approved" when VERIFICATION.md is filled, or describe failures to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Preview URL → public | No test credentials in VERIFICATION.md |
| Manual tools → Google | Only public storefront URLs submitted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Tampering | Perf gate bypass | mitigate | Block checkpoint until VERIFICATION scores recorded (D-06-07) |
| T-06-04-02 | Repudiation | SEO manual skip | mitigate | Rich Results + automated catalog-seo both required before prod (D-06-19) |

</threat_model>

<verification>
grep -q 'LCP' .planning/phases/06-polish-launch/06-VERIFICATION.md
npm test
</verification>

<success_criteria>
06-VERIFICATION.md filled on preview; PERF-01 audit documented; CWV pass or remediation recorded per D-06-07
</success_criteria>

<output>
Create `.planning/phases/06-polish-launch/06-04-SUMMARY.md` when done
</output>
