---
phase: 06-polish-launch
plan: 08
type: execute
wave: 7
gap_closure: true
depends_on:
  - 06-07
files_modified:
  - .planning/phases/06-polish-launch/06-VERIFICATION.md
  - .planning/phases/06-polish-launch/06-ENV-CHECKLIST.md
  - e2e/smoke-deploy.spec.ts
autonomous: false
requirements:
  - SEO-01
  - SEO-02
user_setup:
  - service: vercel
    why: Production promote and PLAYWRIGHT_BASE_URL smoke (D-06-16/17/19)
    env_vars:
      - name: DATABASE_URL
        source: "Vercel → Production env — Neon production branch pooled URL"
      - name: NEXT_PUBLIC_APP_URL
        source: "Vercel → Production — must equal live https origin (D-06-15)"
    dashboard_config:
      - task: "Verify all Production env vars from 06-ENV-CHECKLIST.md"
        location: "Vercel → Settings → Environment Variables → Production"
      - task: "Promote preview deployment to Production"
        location: "Vercel → Deployments → Promote to Production"
context_decisions:
  - "D-06-14/15: Production env complete; NEXT_PUBLIC_APP_URL matches BETTER_AUTH_URL"
  - "D-06-16: preview green → manual gates → production deploy → production smoke"
  - "D-06-17: PLAYWRIGHT_BASE_URL=https://<prod> npx playwright test e2e/smoke-deploy.spec.ts"
  - "D-06-19: Production smoke is ship blocker"
  - "D-06-25: No CI E2E against Vercel — smoke is manual only"

must_haves:
  truths:
    - "Production origin URL and promote timestamp recorded in 06-ENV-CHECKLIST.md"
    - "Vercel Production env vars confirmed per checklist (no ADMIN_PASSWORD)"
    - "PLAYWRIGHT_BASE_URL production smoke: 4/4 tests pass, recorded in SUMMARY"
  artifacts:
    - path: .planning/phases/06-polish-launch/06-ENV-CHECKLIST.md
      provides: production deployment proof
      contains: "Production"
    - path: .planning/phases/06-polish-launch/06-08-SUMMARY.md
      provides: smoke command output reference
  key_links:
    - from: e2e/smoke-deploy.spec.ts
      to: production https origin
      via: PLAYWRIGHT_BASE_URL
      pattern: "PLAYWRIGHT_BASE_URL=https://"

---

## Phase Goal

**As a** власник магазину, **I want to** задеплоїти на Vercel Production і прогнати smoke на живому https, **so that** фаза 6 закриває deploy gap (roadmap SC4, D-06-17/19).

<objective>
Закрити production deploy partial truth і production smoke failed truth: promote, confirm env, run remote smoke, record evidence.

Purpose: Phase verification found vercel.json only — no live origin or smoke proof.
Output: Filled Production row in ENV-CHECKLIST; green smoke-deploy against https origin in 06-08-SUMMARY.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-polish-launch/06-07-SUMMARY.md
@.planning/phases/06-polish-launch/06-ENV-CHECKLIST.md
@.planning/phases/06-polish-launch/06-VERIFICATION.md
@e2e/smoke-deploy.spec.ts
@vercel.json

<interfaces>
Production smoke (D-06-17):
```bash
PLAYWRIGHT_BASE_URL=https://<production-origin> npx playwright test e2e/smoke-deploy.spec.ts --reporter=line
```

Required Production env (D-06-14): DATABASE_URL, DIRECT_URL, BETTER_AUTH_*, NEXT_PUBLIC_APP_URL, Cloudinary trio, Pusher six vars. Forbidden: ADMIN_PASSWORD.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Confirm Vercel Production environment variables</name>
  <read_first>
    - .planning/phases/06-polish-launch/06-ENV-CHECKLIST.md — § Production (Vercel)
    - .env.example — Production section
  </read_first>
  <files>.planning/phases/06-polish-launch/06-ENV-CHECKLIST.md</files>
  <action>
    Human: In Vercel dashboard → **Production** environment, verify every variable in § Production (Vercel) of `06-ENV-CHECKLIST.md` is set with production values (D-06-14/15):

    - `NEXT_PUBLIC_APP_URL` and `BETTER_AUTH_URL` are **identical** https origins, no trailing slash.
    - `ADMIN_PASSWORD` is **absent** from Production.
    - Neon URLs are production branch (not CI/dev).

    In `06-ENV-CHECKLIST.md` § Deployment record **Production** row, add note «Env verified» with ISO date and operator initials. Do not paste secret values into markdown.
  </action>
  <acceptance_criteria>
    - Production env checklist mentally confirmed against Vercel UI
    - ENV-CHECKLIST Production row notes env verification date
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Env verified" .planning/phases/06-polish-launch/06-ENV-CHECKLIST.md</automated>
  </verify>
  <done>Production secrets confirmed (D-06-14/15)</done>
  <resume-signal>Type "env verified" when Production vars checked in Vercel</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Promote to Vercel Production</name>
  <read_first>
    - .planning/phases/06-polish-launch/06-ENV-CHECKLIST.md — Deploy runbook step 4
    - .planning/phases/06-polish-launch/06-07-SUMMARY.md — preview gate must be green
  </read_first>
  <files>
    .planning/phases/06-polish-launch/06-ENV-CHECKLIST.md
    .planning/phases/06-polish-launch/06-VERIFICATION.md
  </files>
  <action>
    Human: After 06-07 preview gates pass (Lighthouse + Rich Results), promote the verified preview deployment to **Production** in Vercel (D-06-16).

    1. Copy the live **Production** `https://` origin (custom domain or `*.vercel.app`).
    2. Fill `06-ENV-CHECKLIST.md` § Deployment record — **Production** row: Origin URL, promote timestamp (ISO), operator.
    3. Set `06-VERIFICATION.md` **Production origin** field to the same URL.
    4. Quick browser sanity: open `/`, `/katalog`, `/robots.txt` on production — Ukrainian UI, 200 responses.
  </action>
  <acceptance_criteria>
    - Production https origin documented in ENV-CHECKLIST and VERIFICATION
    - Promote timestamp recorded
    - Production home loads in browser
  </acceptance_criteria>
  <verify>
    <automated>grep -qE 'Production.*https://|https://[^[:space:]]+' .planning/phases/06-polish-launch/06-ENV-CHECKLIST.md && grep -q "Production origin" .planning/phases/06-polish-launch/06-VERIFICATION.md</automated>
  </verify>
  <done>Application live on Vercel Production</done>
  <resume-signal>Paste production origin URL or type "promoted"</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Production smoke-deploy (ship blocker)</name>
  <read_first>
    - e2e/smoke-deploy.spec.ts — 4 tests (home, catalog link, robots, sitemap)
    - playwright.config.ts — webServer disabled when PLAYWRIGHT_BASE_URL set
    - .planning/phases/06-polish-launch/06-05-SUMMARY.md — prior localhost smoke only
  </read_first>
  <files>
    e2e/smoke-deploy.spec.ts
    .planning/phases/06-polish-launch/06-ENV-CHECKLIST.md
  </files>
  <action>
    On operator machine with repo checkout, run against **Production** origin from Task 2 (D-06-17/19):

    ```bash
    PLAYWRIGHT_BASE_URL=https://<production-origin> npx playwright test e2e/smoke-deploy.spec.ts --reporter=line
    ```

    Expect **4 passed** (home h1, catalog product link, robots.txt, sitemap.xml). No `webServer` — tests hit live origin only.

    Record in plan SUMMARY (`06-08-SUMMARY.md` when created):
    - Exact `PLAYWRIGHT_BASE_URL` value used
    - Pass count (4/4)
    - ISO date/time

    Update `06-ENV-CHECKLIST.md` Deploy runbook step 5 with «Smoke green on production» note + date.

    If any test fails: do **not** mark phase complete — fix origin/env or storefront bug, re-run smoke, document failure in SUMMARY.
  </action>
  <what-built>Production deployment at https origin</what-built>
  <how-to-verify>
    1. Run the PLAYWRIGHT_BASE_URL command above with production host.
    2. Confirm terminal shows 4 passed.
    3. Optionally open production `/robots.txt` and `/sitemap.xml` in browser.
  </how-to-verify>
  <acceptance_criteria>
    - smoke-deploy 4/4 pass against https production origin
    - 06-08-SUMMARY documents command and result
    - ENV-CHECKLIST notes production smoke green
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/06-polish-launch/06-08-SUMMARY.md && grep -q "4 passed\|4/4\|passed" .planning/phases/06-polish-launch/06-08-SUMMARY.md && grep -q "PLAYWRIGHT_BASE_URL" .planning/phases/06-polish-launch/06-08-SUMMARY.md</automated>
  </verify>
  <done>Production smoke gap closed (D-06-17/19)</done>
  <resume-signal>Type "smoke pass" with pass count or paste test output summary</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → Production env | Secrets only in Vercel dashboard |
| Playwright → live prod | Read-only public routes; no destructive actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08-01 | Information disclosure | ENV-CHECKLIST | mitigate | Never write secret values; only URLs and dates |
| T-06-08-02 | Tampering | Smoke bypass | mitigate | Blocking checkpoint; SUMMARY must record 4/4 pass |

</threat_model>

<verification>
test -f .planning/phases/06-polish-launch/06-08-SUMMARY.md
grep -q "https://" .planning/phases/06-polish-launch/06-ENV-CHECKLIST.md
</verification>

<success_criteria>
Production URL + promote timestamp in artifacts; env verified; PLAYWRIGHT_BASE_URL smoke 4/4 green on live https — phase 6 deploy gaps closed
</success_criteria>

<output>
Create `.planning/phases/06-polish-launch/06-08-SUMMARY.md` when done
</output>
