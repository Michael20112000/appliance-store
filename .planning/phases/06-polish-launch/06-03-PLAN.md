---
phase: 06-polish-launch
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/app/robots.ts
  - .env.example
  - .planning/phases/06-polish-launch/06-ENV-CHECKLIST.md
autonomous: true
requirements:
  - SEO-02
context_decisions:
  - "D-06-14: Production env checklist — required vars, no ADMIN_PASSWORD on prod"
  - "D-06-15: NEXT_PUBLIC_APP_URL and BETTER_AUTH_URL same https origin, no trailing slash mismatch"
  - "D-06-18: .env.example Production section with required/forbidden comments"
  - "D-06-22: Add src/app/robots.ts with sitemap reference and disallow /admin, /api/"

must_haves:
  truths:
    - "GET /robots.txt returns rules disallowing /admin and /api/ and points to sitemap"
    - "Operators have production env checklist before Vercel promote"
    - ".env.example documents production constraints in Ukrainian comments"
  artifacts:
    - path: src/app/robots.ts
      provides: crawl rules + sitemap URL
      exports: ["default function robots"]
    - path: .env.example
      provides: Production section
      contains: "Production"
  key_links:
    - from: src/app/robots.ts
      to: getEnv().NEXT_PUBLIC_APP_URL
      via: baseUrl normalization
      pattern: "NEXT_PUBLIC_APP_URL"
    - from: src/app/robots.ts
      to: /sitemap.xml
      via: sitemap field
      pattern: "sitemap.xml"

---

## Phase Goal

**As a** власник магазину, **I want to** коректні robots.txt і production env, **so that** Google індексує каталог, а не адмінку, і auth працює на проді.

<objective>
Додати `app/robots.ts`, доповнити `.env.example` секцією Production, завершити production частину `06-ENV-CHECKLIST.md`.

Purpose: D-06-14/15/18/22 — launch infra без нових залежностей.
Output: `/robots.txt` live; production env documented.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-polish-launch/06-01-PLAN.md
@src/app/sitemap.ts
@src/lib/env.ts
@.env.example
@node_modules/next/dist/docs/app/api-reference/file-conventions/metadata/robots.md

<interfaces>
From src/app/sitemap.ts:
```typescript
const baseUrl = getEnv().NEXT_PUBLIC_APP_URL.replace(/\/$/, "");
```

Robots pattern (RESEARCH Pattern 3):
- MetadataRoute.Robots
- rules: allow `/`, disallow `/admin`, `/api/`
- sitemap: `${baseUrl}/sitemap.xml`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement app/robots.ts</name>
  <read_first>
    - src/app/sitemap.ts — getEnv + baseUrl pattern
    - node_modules/next/dist/docs/ — robots.ts MetadataRoute (AGENTS.md)
    - .planning/phases/06-polish-launch/06-RESEARCH.md — Pattern 3
  </read_first>
  <files>src/app/robots.ts</files>
  <action>
    Create `src/app/robots.ts` per D-06-22:
    - `import type { MetadataRoute } from "next"`
    - `import { getEnv } from "@/lib/env"`
    - `baseUrl = getEnv().NEXT_PUBLIC_APP_URL.replace(/\/$/, "")`
  - Return `{ rules: { userAgent: "*", allow: "/", disallow: ["/admin", "/api/"] }, sitemap: \`${baseUrl}/sitemap.xml\` }`
    - Sync with sitemap.ts host logic (D-06-15)
    - No static public/robots.txt file — use App Router metadata route only
    - Read Next 16 docs before API if types differ from training data
  </action>
  <acceptance_criteria>
    - File exports default robots() returning MetadataRoute.Robots
    - disallow includes /admin and /api/
    - sitemap URL ends with /sitemap.xml
  </acceptance_criteria>
  <verify>
    <automated>test -f src/app/robots.ts && grep -q 'disallow' src/app/robots.ts && grep -q 'sitemap.xml' src/app/robots.ts</automated>
  </verify>
  <done>D-06-22 robots route implemented</done>
</task>

<task type="auto">
  <name>Task 2: Production env docs (.env.example + checklist)</name>
  <read_first>
    - .planning/phases/06-polish-launch/06-CONTEXT.md — D-06-14, D-06-15, D-06-18
    - .planning/phases/06-polish-launch/06-ENV-CHECKLIST.md — CI section from 06-01
    - .env.example — existing dev sections
  </read_first>
  <files>
    .env.example
    .planning/phases/06-polish-launch/06-ENV-CHECKLIST.md
  </files>
  <action>
    Extend `.env.example` with **# Production (Vercel)** section per D-06-18:
    - Required on prod: `DATABASE_URL`, `DIRECT_URL`, `BETTER_AUTH_SECRET`, `BETTER_AUTH_URL`, `NEXT_PUBLIC_APP_URL`, Cloudinary trio (`NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME`, `CLOUDINARY_API_KEY`, `CLOUDINARY_API_SECRET`), all six Pusher vars for chat
    - **Forbidden on prod:** `ADMIN_PASSWORD` / seed admin credentials (D-06-14) — comment in Ukrainian
    - **D-06-15:** `NEXT_PUBLIC_APP_URL` and `BETTER_AUTH_URL` must be identical origin (https, no trailing slash)
    Complete `06-ENV-CHECKLIST.md` **§ Production (Vercel)**:
    - Table or checklist of required Vercel env vars
    - Promote prerequisite: preview deployment green before production
    - Note GSC post-launch (D-06-12) out of scope
    - Cross-link CI section from plan 06-01
  </action>
  <acceptance_criteria>
    - .env.example contains Production section header
    - Checklist documents all prod-required vars and forbids ADMIN_PASSWORD on prod
    - Checklist states BETTER_AUTH_URL === NEXT_PUBLIC_APP_URL origin rule
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'Production' .env.example && grep -q 'ADMIN_PASSWORD' .env.example && grep -q 'Production' .planning/phases/06-polish-launch/06-ENV-CHECKLIST.md</automated>
  </verify>
  <done>D-06-14/15/18 production documentation complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Crawler → /robots.txt | Public; must not leak secrets |
| Vercel env → runtime | Production secrets only in dashboard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Information disclosure | robots.ts | mitigate | disallow `/admin` and `/api/` (D-06-22) |
| T-06-03-02 | Spoofing | BETTER_AUTH_URL mismatch | mitigate | Document D-06-15 in checklist and .env.example |
| T-06-03-03 | Information disclosure | ADMIN_PASSWORD on prod | mitigate | Explicit forbid in checklist (D-06-14) |

</threat_model>

<verification>
npm run build
curl -s http://localhost:3000/robots.txt 2>/dev/null | head -5 || true
</verification>

<success_criteria>
robots.ts builds; production env documented; checklist has CI + Production sections
</success_criteria>

<output>
Create `.planning/phases/06-polish-launch/06-03-SUMMARY.md` when done
</output>
