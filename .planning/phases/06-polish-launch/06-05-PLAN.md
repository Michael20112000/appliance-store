---
phase: 06-polish-launch
plan: 05
type: execute
wave: 4
depends_on:
  - 06-03
  - 06-04
files_modified:
  - e2e/smoke-deploy.spec.ts
  - .planning/phases/06-polish-launch/06-ENV-CHECKLIST.md
autonomous: true
requirements:
  - SEO-01
  - SEO-02
  - cross-cutting production deploy
user_setup:
  - service: vercel
    why: Production deploy and PLAYWRIGHT_BASE_URL smoke against live origin"
    dashboard_config:
      - task: "Set all Production env vars from 06-ENV-CHECKLIST.md on Vercel Production"
        location: "Vercel → Project → Settings → Environment Variables → Production"
      - task: "Promote preview to production after gates green (D-06-16)"
        location: "Vercel → Deployments"
context_decisions:
  - "D-06-16: preview green → manual Lighthouse/SEO → production deploy → production smoke"
  - "D-06-17: PLAYWRIGHT_BASE_URL=prod npx playwright test e2e/smoke-deploy.spec.ts + home, /katalog, robots/sitemap"
  - "D-06-19: production smoke is ship blocker"
  - "D-06-21: public launch — smoke validates public routes only"

must_haves:
  truths:
    - "smoke-deploy.spec.ts validates home, catalog, and robots/sitemap on remote base URL"
    - "Deploy runbook documents promote order and smoke command"
    - "Production smoke passes against live https origin"
  artifacts:
    - path: e2e/smoke-deploy.spec.ts
      provides: post-deploy smoke
      min_lines: 15
    - path: .planning/phases/06-polish-launch/06-ENV-CHECKLIST.md
      provides: deploy runbook section
      contains: "smoke-deploy"
  key_links:
    - from: e2e/smoke-deploy.spec.ts
      to: PLAYWRIGHT_BASE_URL
      via: playwright.config webServer disabled
      pattern: "PLAYWRIGHT_BASE_URL"
    - from: e2e/smoke-deploy.spec.ts
      to: /robots.txt
      via: request.get
      pattern: "robots.txt"

---

## Phase Goal

**As a** власник магазину, **I want to** задеплоїти на Vercel і прогнати smoke на проді, **so that** покупці бачать живий каталог без сюрпризів після promote.

<objective>
Розширити `smoke-deploy.spec.ts` і дописати deploy runbook; виконати production smoke після deploy (D-06-17).

Purpose: Cross-cutting production deploy verification.
Output: Extended smoke spec + runbook; green prod smoke documented in SUMMARY.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-polish-launch/06-04-SUMMARY.md
@.planning/phases/06-polish-launch/06-VERIFICATION.md
@e2e/smoke-deploy.spec.ts
@e2e/public-browse.spec.ts
@e2e/catalog-seo.spec.ts
@playwright.config.ts

<interfaces>
Remote smoke (D-06-17):
```bash
PLAYWRIGHT_BASE_URL=https://<production-origin> npx playwright test e2e/smoke-deploy.spec.ts
```

When PLAYWRIGHT_BASE_URL set, webServer is undefined — tests hit remote only.

Existing smoke: home h1 «Б/у побутова техніка»
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend smoke-deploy.spec.ts</name>
  <read_first>
    - e2e/smoke-deploy.spec.ts — current single test
    - e2e/public-browse.spec.ts — catalog visibility pattern
    - .planning/phases/06-polish-launch/06-PATTERNS.md — smoke-deploy extend section
    - src/app/robots.ts (from 06-03) — Sitemap line expectation
  </read_first>
  <files>e2e/smoke-deploy.spec.ts</files>
  <action>
    Extend `e2e/smoke-deploy.spec.ts` per D-06-17 (keep file separate, D-06-02):
    - Keep existing home test (200 + h1).
    - Add: `page.goto("/katalog/kholodylnyky")` → first `main a[href^="/tovar/"]` visible (from public-browse pattern).
    - Add request tests: `GET /robots.txt` ok, body matches `/Sitemap:/i` (after robots.ts).
    - Add: `GET /sitemap.xml` ok, body contains `/tovar/` (lightweight SEO smoke).
    - Tests must pass with `PLAYWRIGHT_BASE_URL` pointing to preview **or** production — no local webServer dependency.
    - Do not add auth/checkout here — covered by critical-journey in CI (D-06-03).
  </action>
  <acceptance_criteria>
    - smoke-deploy.spec.ts has at least 4 tests
    - robots and sitemap request tests present
    - `npx playwright test e2e/smoke-deploy.spec.ts` passes locally against dev server
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test e2e/smoke-deploy.spec.ts --reporter=line</automated>
  </verify>
  <done>Extended smoke spec ready for remote BASE_URL</done>
</task>

<task type="auto">
  <name>Task 2: Deploy runbook + production smoke execution</name>
  <read_first>
    - .planning/phases/06-polish-launch/06-ENV-CHECKLIST.md — Production section
    - .planning/phases/06-polish-launch/06-VERIFICATION.md — must be green before promote
    - .planning/phases/06-polish-launch/06-CONTEXT.md — D-06-16, D-06-19
  </read_first>
  <files>.planning/phases/06-polish-launch/06-ENV-CHECKLIST.md</files>
  <action>
    Add **§ Deploy runbook** to `06-ENV-CHECKLIST.md` per D-06-16:
    1. CI green on `main` (`npm test && npm run test:e2e` locally or GitHub Actions)
    2. Preview deploy green; `06-VERIFICATION.md` Lighthouse + Rich Results filled (plan 06-04)
    3. Vercel Production env vars per checklist (D-06-14/15) — no ADMIN_PASSWORD
    4. Promote to **Production**
    5. Run: `PLAYWRIGHT_BASE_URL=https://<prod-origin> npx playwright test e2e/smoke-deploy.spec.ts`
    6. Optional sanity: open `/katalog`, `/uviity` — Ukrainian UI
    Document production origin used in `06-05-SUMMARY.md`.
    **Execute step 5** when production URL is available (Vercel CLI `vercel --prod` or dashboard promote). If production not yet provisioned, run against latest **preview** URL first, note in SUMMARY, re-run on prod after promote.
    Run full phase gate once: `npm test && npm run test:e2e` (D-06-01) and record in SUMMARY.
  </action>
  <acceptance_criteria>
    - Runbook section exists with ordered promote steps
    - smoke-deploy green against documented BASE_URL (prod preferred)
    - 06-05-SUMMARY records smoke URL and pass/fail
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'smoke-deploy' .planning/phases/06-polish-launch/06-ENV-CHECKLIST.md && npm test && test -n "$PLAYWRIGHT_BASE_URL" && npx playwright test e2e/smoke-deploy.spec.ts --reporter=line</automated>
  </verify>
  <done>D-06-16/17 deploy path documented and smoke executed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Production URL → Playwright | Smoke uses public routes only; no admin credentials in spec |
| CI → production | CI never uses production DATABASE_URL (D-06-23); smoke is separate manual step |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Spoofing | Wrong PLAYWRIGHT_BASE_URL | mitigate | Document exact https origin in runbook; verify home h1 Ukrainian copy |
| T-06-05-02 | Information disclosure | Smoke against prod | accept | Public pages only; no secrets in spec |
| T-06-05-03 | Tampering | Deploy without gates | mitigate | Runbook enforces VERIFICATION + CI before promote (D-06-19) |

</threat_model>

<verification>
npm test && npm run test:e2e
PLAYWRIGHT_BASE_URL=https://<origin> npx playwright test e2e/smoke-deploy.spec.ts
</verification>

<success_criteria>
Deploy runbook complete; smoke-deploy passes on production (or documented preview + prod re-run); full phase E2E gate green
</success_criteria>

<output>
Create `.planning/phases/06-polish-launch/06-05-SUMMARY.md` when done
</output>
