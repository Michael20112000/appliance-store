---
phase: 51-chat-badge-suggested-messages
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/components/chat/suggested-messages.test.tsx
  - src/components/chat/chat-panel.test.tsx
  - src/components/layout/storefront-fabs.test.tsx
  - src/server/services/chat.service.test.ts
autonomous: true
requirements:
  - CHAT-10
  - CHAT-11

must_haves:
  truths:
    - "All test files compile and run (exit 0 or red-on-intent for stubs)"
    - "suggested-messages.test.tsx exists with failing tests for chip render and click"
    - "chat.service.test.ts has a failing countUnreadForBuyer describe block"
    - "storefront-fabs.test.tsx mock uses unreadCount: number (not boolean) and has badge render tests"
    - "chat-panel.test.tsx baseChatContext has unreadCount/clearUnreadCount and has SuggestedMessages visibility tests"
  artifacts:
    - path: "src/components/chat/suggested-messages.test.tsx"
      provides: "CHAT-11 chip render, visibility, and click tests (failing)"
    - path: "src/server/services/chat.service.test.ts"
      provides: "countUnreadForBuyer unit tests (failing)"
    - path: "src/components/layout/storefront-fabs.test.tsx"
      provides: "Badge numeric render tests + mock updated to unreadCount"
    - path: "src/components/chat/chat-panel.test.tsx"
      provides: "SuggestedMessages visibility tests + baseChatContext renamed fields"
  key_links:
    - from: "suggested-messages.test.tsx"
      to: "src/components/chat/suggested-messages.tsx"
      via: "import (file created in Plan 04)"
      pattern: "import.*SuggestedMessages.*from.*suggested-messages"
---

<objective>
Create Wave 0 test stubs: new suggested-messages.test.tsx with failing tests for CHAT-11, plus targeted updates to three existing test files to rename the unreadFromStore boolean mock to unreadCount number and add failing tests for CHAT-10 badge rendering and countUnreadForBuyer service.

Purpose: Establish the red baseline before any production code is written. All new tests must fail (or be skipped) while the suite compiles cleanly — this proves the tests exercise real behavior, not pass vacuously.
Output: Four test files ready for execution; two have failing tests for the new behavior; two have updated mocks that will break the existing passing tests until production code is migrated in Wave 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-chat-badge-suggested-messages/51-RESEARCH.md
@.planning/phases/51-chat-badge-suggested-messages/51-PATTERNS.md
@.planning/phases/51-chat-badge-suggested-messages/51-VALIDATION.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Create suggested-messages.test.tsx (CHAT-11 failing stubs)</name>
  <files>src/components/chat/suggested-messages.test.tsx</files>
  <read_first>
    - src/components/chat/chat-panel.test.tsx (jsdom test structure: vitest-environment pragma, vi.mock hoisting, cleanup/render/screen pattern — lines 1-70)
    - src/components/chat/product-context-banner.tsx (shows ProductChatContext type shape for test fixture)
    - .planning/phases/51-chat-badge-suggested-messages/51-PATTERNS.md (Vitest component test pattern, "Note for suggested-messages.test.tsx" — no useChat mock needed)
  </read_first>
  <behavior>
    - Test: renders product chip when productContext.productTitle is "Пральна машина LG" — screen finds button with text containing "Пральна машина LG"
    - Test: renders exactly 3 general chips when productContext is null (Який у вас графік роботи? / Де ви знаходитесь? / Як оформити замовлення?)
    - Test: renders 4 chips total when productContext.productTitle is set (1 product + 3 general)
    - Test: calls onSelect with the chip text string when a chip button is clicked (fireEvent.click)
    - Test: does not render product chip when productContext.productTitle is undefined/null
  </behavior>
  <action>
    Create src/components/chat/suggested-messages.test.tsx as a new file. Use @vitest-environment jsdom pragma at top. Import afterEach, describe, expect, it, vi from "vitest"; import cleanup, fireEvent, render, screen from "@testing-library/react". Import SuggestedMessages from "@/components/chat/suggested-messages" (will not exist yet — this is intentional, the import will fail until Plan 04).

    No vi.mock of useChat is needed — SuggestedMessages receives productContext and onSelect as pure props.

    Fixture: a minimal ProductChatContext object with productTitle "Пральна машина LG", productId "prod-1", productSlug "pralna-mashyna-lg".

    Write all five behavior tests listed above. The test file should fail to import (or fail all tests) until Plan 04 creates the component — this is the intentional RED state. Do not add vi.fn() stubs that would cause tests to pass vacuously.
  </action>
  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npx vitest run src/components/chat/suggested-messages.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/chat/suggested-messages.test.tsx exists
    - File contains @vitest-environment jsdom pragma on line 1
    - File imports SuggestedMessages from "@/components/chat/suggested-messages"
    - Running `npx vitest run src/components/chat/suggested-messages.test.tsx` exits non-zero (import error or failing tests — both are acceptable RED states)
    - File does NOT import useChat (SuggestedMessages is a pure props component)
    - All five described test cases have distinct it() blocks
  </acceptance_criteria>
  <done>suggested-messages.test.tsx committed with all five failing test stubs; npx vitest run on the file exits non-zero</done>
</task>

<task type="tdd">
  <name>Task 2: Update three existing test files — rename boolean mock + add failing tests</name>
  <files>
    src/server/services/chat.service.test.ts
    src/components/layout/storefront-fabs.test.tsx
    src/components/chat/chat-panel.test.tsx
  </files>
  <read_first>
    - src/server/services/chat.service.test.ts (full file — find countUnreadForAdmin test block around line 240 to use as template; find prisma mock setup for message.count around line 44)
    - src/components/layout/storefront-fabs.test.tsx (full file — find all four occurrences of unreadFromStore in mock return values at lines 44, 173, 186, 199; find cart badge test pattern to model chat badge tests on)
    - src/components/chat/chat-panel.test.tsx (full file — find baseChatContext at lines 34-62 with unreadFromStore and clearUnreadFromStore; find existing test structure to model SuggestedMessages visibility test on)
    - .planning/phases/51-chat-badge-suggested-messages/51-PATTERNS.md (Test File Updates Required section — exact before/after for all three files)
  </read_first>
  <behavior>
    chat.service.test.ts — new describe("countUnreadForBuyer") block:
    - Test: vi.mocked(prisma.message.count).mockResolvedValueOnce(2) → count returned is 2
    - Test: prisma.message.count called with { where: { conversationId: "conv-1", senderRole: "STORE", createdAt: { gt: cutoff } } }

    storefront-fabs.test.tsx — mock updates + new tests:
    - All four vi.fn mockReturnValue blocks rename unreadFromStore: false → unreadCount: 0
    - New test: when unreadCount is 3, a Badge element with text "3" is present in the rendered output
    - New test: when unreadCount is 0, no Badge element appears in the rendered output
    - New test: when unreadCount is 10, a Badge element with text "9+" is present

    chat-panel.test.tsx — mock update only (no new tests for SuggestedMessages in this file yet):
    - baseChatContext: rename unreadFromStore: false → unreadCount: 0
    - baseChatContext: rename clearUnreadFromStore: vi.fn() → clearUnreadCount: vi.fn()
  </behavior>
  <action>
    In chat.service.test.ts: add a describe("countUnreadForBuyer") block modeled on the existing countUnreadForAdmin block. Import countUnreadForBuyer from the service (will fail until Plan 02 — intentional RED). Use vi.mocked(prisma.message.count).mockResolvedValueOnce(2); assert return value is 2 and that prisma.message.count was called with the correct where clause using expect.objectContaining.

    In storefront-fabs.test.tsx: find all occurrences of `unreadFromStore:` (there are 4) and replace with `unreadCount:`, changing the value from boolean false to number 0. Then add three new it() tests for badge rendering: use vi.mocked(useChat).mockReturnValue({ ...existing mock fields, unreadCount: 3 }) and assert screen.getByText("3") exists; for unreadCount: 0 assert screen.queryByRole on badge returns null; for unreadCount: 10 assert screen.getByText("9+") exists.

    In chat-panel.test.tsx: locate baseChatContext object (around line 34-62) and rename the two fields: unreadFromStore: false → unreadCount: 0, clearUnreadFromStore: vi.fn() → clearUnreadCount: vi.fn(). Do not add new SuggestedMessages tests here — those are deferred to Plan 04's scope.
  </action>
  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npx vitest run src/server/services/chat.service.test.ts src/components/layout/storefront-fabs.test.tsx src/components/chat/chat-panel.test.tsx 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - chat.service.test.ts contains a describe("countUnreadForBuyer") block with at least 2 it() cases
    - storefront-fabs.test.tsx contains zero occurrences of "unreadFromStore" (use grep -c to confirm: `grep -c "unreadFromStore" src/components/layout/storefront-fabs.test.tsx` returns 0)
    - storefront-fabs.test.tsx contains badge render tests for counts 3, 0, and 10 (capped to "9+")
    - chat-panel.test.tsx contains "unreadCount: 0" and "clearUnreadCount" in the baseChatContext — zero occurrences of "unreadFromStore" or "clearUnreadFromStore" (`grep -c "unreadFromStore\|clearUnreadFromStore" src/components/chat/chat-panel.test.tsx` returns 0)
    - Running `npx vitest run src/server/services/chat.service.test.ts` exits non-zero (countUnreadForBuyer import fails — RED)
    - Running `npx vitest run src/components/layout/storefront-fabs.test.tsx` exits non-zero (badge tests fail or unreadCount type mismatch — RED on new tests)
    - Running `npx vitest run src/components/chat/chat-panel.test.tsx` exits non-zero or the renamed mock fields cause TypeScript errors until Plan 02 — acceptable RED
  </acceptance_criteria>
  <done>All three existing test files updated; countUnreadForBuyer test block added; badge tests added; boolean mocks replaced with number mocks; all three files run with expected failures confirming RED baseline</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → production import | Test files import not-yet-created production modules; intentional for TDD RED phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01 | Tampering | Test stubs | accept | Test files only; no production code or user data in scope for this plan |
| T-51-SC | Tampering | npm/pip installs | accept | No new packages installed in this plan; all imports are from existing dependencies |
</threat_model>

<verification>
After both tasks complete:

```bash
npx vitest run src/components/chat/suggested-messages.test.tsx src/server/services/chat.service.test.ts src/components/layout/storefront-fabs.test.tsx src/components/chat/chat-panel.test.tsx
```

Expected: non-zero exit (RED is correct — production code does not exist yet).

Confirm mock rename is complete:
```bash
grep -c "unreadFromStore" src/components/layout/storefront-fabs.test.tsx src/components/chat/chat-panel.test.tsx
```
Expected: 0 for each file.
</verification>

<success_criteria>
- suggested-messages.test.tsx exists with 5 it() cases covering CHAT-11 behavior (RED)
- chat.service.test.ts has countUnreadForBuyer describe block (RED until Plan 02)
- storefront-fabs.test.tsx has 0 occurrences of "unreadFromStore"; has 3 badge render tests (RED until Plan 03)
- chat-panel.test.tsx has 0 occurrences of "unreadFromStore" or "clearUnreadFromStore"
</success_criteria>

<output>
Create `.planning/phases/51-chat-badge-suggested-messages/51-01-SUMMARY.md` when done
</output>
