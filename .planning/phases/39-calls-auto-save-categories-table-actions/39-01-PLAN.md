---
phase: 39-calls-auto-save-categories-table-actions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/admin/use-callback-note-auto-save.ts
  - src/hooks/admin/use-callback-note-auto-save.test.ts
  - src/components/admin/callback-note-field.tsx
autonomous: true
requirements:
  - CALL-05

must_haves:
  truths:
    - "Operator types in callback note on /admin/dzvinky active rows — text saves automatically ~400ms after last keystroke with no «Зберегти» button"
    - "Status under textarea shows «Збереження…» then «Збережено» (2s) — no success toast"
    - "Save errors show toast only; typed text stays in field (no rollback)"
    - "Archived callback rows remain read-only truncated notes — unchanged"
  artifacts:
    - path: "src/hooks/admin/use-callback-note-auto-save.ts"
      provides: "Debounced note save hook (400ms, snapshot + generation chain)"
      exports: ["useCallbackNoteAutoSave", "SaveStatus"]
    - path: "src/hooks/admin/use-callback-note-auto-save.test.ts"
      provides: "Unit tests for debounce, skip unchanged, errors"
    - path: "src/components/admin/callback-note-field.tsx"
      provides: "Controlled textarea + inline status via hook"
      contains: "aria-live"
  key_links:
    - from: "src/hooks/admin/use-callback-note-auto-save.ts"
      to: "src/server/actions/admin/callback.actions.ts"
      via: "updateCallbackNoteAction({ id, note })"
      pattern: "updateCallbackNoteAction"
    - from: "src/components/admin/callback-note-field.tsx"
      to: "src/hooks/admin/use-callback-note-auto-save.ts"
      via: "useCallbackNoteAutoSave(id, note)"
      pattern: "useCallbackNoteAutoSave"
---

<objective>
Replace manual «Зберегти» callback note save with debounced auto-save (400ms) and inline status copy matching product edit — CALL-05, locked D-01–D-07, D-19–D-21.

Purpose: Operators save notes without extra clicks; no toast spam or table refresh flicker.
Output: `useCallbackNoteAutoSave` hook + tests; refactored `CallbackNoteField`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/39-calls-auto-save-categories-table-actions/39-CONTEXT.md
@.planning/phases/39-calls-auto-save-categories-table-actions/39-RESEARCH.md
@.planning/phases/39-calls-auto-save-categories-table-actions/39-PATTERNS.md
@.planning/phases/39-calls-auto-save-categories-table-actions/39-UI-SPEC.md
@.planning/phases/39-calls-auto-save-categories-table-actions/39-VALIDATION.md
</context>

<interfaces>
From src/hooks/admin/use-product-auto-save.ts:
export type SaveStatus = "idle" | "saving" | "saved";
// Pattern: snapshotRef, generationRef, saveChainRef, createDebounce(ms), SAVED_DISPLAY_MS = 2000

From src/lib/debounce.ts:
export function createDebounce(ms: number): DebouncedInvoke;

From src/server/actions/admin/callback.actions.ts:
export async function updateCallbackNoteAction(input: unknown): Promise<CallbackActionResult>;
// Success: { ok: true }; errors: ALREADY_ARCHIVED | NOT_FOUND | VALIDATION | ...

From src/components/admin/callback-note-field.tsx (remove):
// useRouter, useTransition, Button «Зберегти», toast.success, router.refresh(), disabled={pending}

From src/components/admin/product-edit-header.tsx (status copy only):
// «Збереження…» / «Збережено»
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — failing tests for useCallbackNoteAutoSave</name>
  <files>src/hooks/admin/use-callback-note-auto-save.test.ts</files>
  <read_first>
    - src/hooks/admin/use-product-auto-save.test.ts
    - .planning/phases/39-calls-auto-save-categories-table-actions/39-PATTERNS.md
    - .planning/phases/39-calls-auto-save-categories-table-actions/39-CONTEXT.md (D-19, D-05, D-03)
  </read_first>
  <behavior>
    - Debounce 400ms: after setValue change, action not called at 399ms, called once at 400ms quiet (not 500ms)
    - Skip save when value equals initial snapshot (no network)
    - On ALREADY_ARCHIVED: toast.error with «Заявку вже в архіві»; value in hook state unchanged from user input
    - On NOT_FOUND: toast.error «Заявку не знайдено»
    - On success: status transitions saving → saved (no toast.success)
  </behavior>
  <action>
    Create src/hooks/admin/use-callback-note-auto-save.test.ts before the hook file exists (RED).

    Mirror use-product-auto-save.test.ts harness:
    - vi.mock updateCallbackNoteAction from @/server/actions/admin/callback.actions
    - vi.mock sonner toast.error (and toast.success to assert never called on save success)
    - beforeEach: vi.useFakeTimers(); mockResolvedValue({ ok: true })
    - afterEach: vi.useRealTimers()

    renderHook(() => useCallbackNoteAutoSave("id-1", "initial")) — import will fail until Task 2; that is expected RED.

    Assert DEBOUNCE_MS is 400 in tests (advanceTimersByTime 399 then 1), per D-19.

    Do not test router.refresh — hook must not call refresh per D-21.
  </action>
  <verify>
    <automated>npm test -- src/hooks/admin/use-callback-note-auto-save.test.ts -x 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists with cases above
    - Running tests fails because hook module missing or tests fail (RED state)
  </acceptance_criteria>
  <done>Wave 0 RED: test file committed; suite fails until hook implemented</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement useCallbackNoteAutoSave (GREEN)</name>
  <files>src/hooks/admin/use-callback-note-auto-save.ts</files>
  <read_first>
    - src/hooks/admin/use-product-auto-save.ts (save chain, generation, debounce effect)
    - src/components/admin/callback-note-field.tsx (current error toast strings)
    - .planning/phases/39-calls-auto-save-categories-table-actions/39-PATTERNS.md
  </read_first>
  <behavior>
    - Same as Task 1 — all tests pass
  </behavior>
  <action>
    Create src/hooks/admin/use-callback-note-auto-save.ts per D-20:

    - "use client"
    - DEBOUNCE_MS = 400 (D-19), SAVED_DISPLAY_MS = 2000
    - export type SaveStatus = "idle" | "saving" | "saved"
    - export function useCallbackNoteAutoSave(id: string, initialNote: string | null)
    - State: value/setValue from initialNote ?? ""; status; snapshotRef = initial string
    - createDebounce(400); saveChainRef + generationRef; cancel debounce on unmount
    - runSave: if value === snapshotRef return; else updateCallbackNoteAction({ id, note: value })
    - Success: snapshotRef = value; status saved → idle after 2s; NO router.refresh (D-21)
    - Error per D-03/D-05/D-07: toast.error for ALREADY_ARCHIVED, NOT_FOUND, generic; setStatus idle; do NOT revert value
    - useEffect on value → debounce(() => runSave())

    Return { value, setValue, status }.

    Do NOT extract shared primitive with useProductAutoSave in this phase (YAGNI per RESEARCH).
  </action>
  <verify>
    <automated>npm test -- src/hooks/admin/use-callback-note-auto-save.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - All hook unit tests green
    - No toast.success on successful save in hook
    - Debounce constant is 400 not 500
  </acceptance_criteria>
  <done>useCallbackNoteAutoSave implemented and fully tested</done>
</task>

<task type="auto">
  <name>Task 3: Wire CallbackNoteField to hook + status UI</name>
  <files>src/components/admin/callback-note-field.tsx</files>
  <read_first>
    - src/components/admin/callback-note-field.tsx
    - src/components/admin/product-edit-header.tsx (status strings)
    - .planning/phases/39-calls-auto-save-categories-table-actions/39-UI-SPEC.md
    - src/components/admin/callback-requests-table.tsx (confirm active-only usage — do not change archived branch)
  </read_first>
  <action>
    Refactor CallbackNoteField per D-01, D-02, D-04:

    1. Remove: useRouter, useTransition, Button import, onSave, pending/disabled on Textarea, toast.success, direct updateCallbackNoteAction import.

    2. Use: const { value, setValue, status } = useCallbackNoteAutoSave(id, note);

    3. Layout (D-02):
       - Textarea rows=3, min-w/max-w classes unchanged, placeholder «Нотатка для оператора»
       - onChange → setValue(e.target.value)
       - Do NOT disable textarea during saving (D-07 / Pitfall 4)

    4. Status line under textarea:
       - className="text-xs text-muted-foreground"
       - aria-live="polite"
       - saving: «Збереження…»; saved: «Збережено» (match ProductEditHeader)

    5. Do not add «Зберегти» button (D-04).

    Do not modify callback-requests-table.tsx archived rendering (D-06).
  </action>
  <verify>
    <automated>npm test -- src/hooks/admin/use-callback-note-auto-save.test.ts -x</automated>
    <automated>rg -v '^#' src/components/admin/callback-note-field.tsx | rg -c 'Зберегти' | test "$(rg -v '^#' src/components/admin/callback-note-field.tsx | rg -c 'Зберегти' || echo 0)" -eq 0</automated>
    <automated>rg -v '^#' src/components/admin/callback-note-field.tsx | rg -c 'router\.refresh' | test "$(rg -v '^#' src/components/admin/callback-note-field.tsx | rg -c 'router\.refresh' || echo 0)" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - CallbackNoteField uses hook only for persistence
    - No «Зберегти» button, no success toast, no router.refresh in component
    - Inline status visible under textarea
    - Textarea never disabled during save
  </acceptance_criteria>
  <done>CALL-05 UI complete on active callback rows</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Server Action | Untrusted note text from admin textarea crosses to updateCallbackNoteAction |
| Session | requireAdmin() gates all note updates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Spoofing | updateCallbackNoteAction | mitigate | requireAdmin() in action (existing) |
| T-39-02 | Tampering | Note payload | mitigate | Zod updateCallbackNoteSchema (id cuid, note max length) on server |
| T-39-03 | Repudiation | Autosave events | accept | No audit log required for L1 |
| T-39-04 | Information Disclosure | Note field | accept | Admin-only route; same as today |
| T-39-05 | Denial of Service | Rapid typing | accept | Debounce + save chain limits concurrent saves |
| T-39-SC | Tampering | npm installs | accept | No new packages this plan |

</threat_model>

<verification>
1. npm test -- src/hooks/admin/use-callback-note-auto-save.test.ts -x
2. rg «Зберегти» callback-note-field.tsx — zero matches (excluding comments)
3. Manual: /admin/dzvinky active row — type note, pause 400ms, see «Збережено» without button
</verification>

<success_criteria>
- CALL-05: Auto-save 400ms debounce after last keystroke; no manual save button; error toasts only
- Locked D-01–D-07, D-19–D-21 satisfied in hook + field
</success_criteria>

<output>
Create `.planning/phases/39-calls-auto-save-categories-table-actions/39-01-SUMMARY.md` when done
</output>
