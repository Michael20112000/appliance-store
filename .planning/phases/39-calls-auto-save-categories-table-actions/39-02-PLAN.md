---
phase: 39-calls-auto-save-categories-table-actions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/actions/admin/category.actions.ts
  - src/server/actions/admin/category.actions.test.ts
  - src/components/admin/category-table-delete-button.tsx
  - src/components/admin/category-table-delete-button.test.tsx
  - src/components/admin/admin-categories-table.tsx
  - src/components/admin/admin-categories-table.test.tsx
autonomous: true
requirements:
  - ADM-CAT-07
  - ADM-CAT-08

must_haves:
  truths:
    - "Categories table shows № column as 1-based index from localCategories order; numbers update immediately after DnD reorder"
    - "Column order: № → grip → Назва → Товари → Дії"
    - "«Додати товар» links to /admin/tovary/novyi?categoryId={id} without triggering row edit navigation"
    - "«Видалити» opens AlertDialog; confirm calls deleteCategoryFromListAction (no redirect); row removed from localCategories on success"
    - "Delete with products shows CATEGORY_HAS_PRODUCTS toast; empty category shows irreversible confirm copy"
  artifacts:
    - path: "src/server/actions/admin/category.actions.ts"
      provides: "deleteCategoryFromListAction — revalidate without redirect"
      exports: ["deleteCategoryFromListAction"]
    - path: "src/components/admin/category-table-delete-button.tsx"
      provides: "Text delete button + AlertDialog + row nav guards"
    - path: "src/components/admin/admin-categories-table.tsx"
      provides: "№ and Дії columns wired to localCategories"
  key_links:
    - from: "src/components/admin/category-table-delete-button.tsx"
      to: "deleteCategoryFromListAction"
      via: "startTransition confirm handler"
      pattern: "deleteCategoryFromListAction"
    - from: "src/components/admin/admin-categories-table.tsx"
      to: "/admin/tovary/novyi"
      via: "Link href with categoryId query"
      pattern: "novyi\\?categoryId="
    - from: "src/components/admin/admin-categories-table.tsx"
      to: "localCategories"
      via: "index + 1 rowNumber + filter on delete"
      pattern: "index \\+ 1"
---

<objective>
Add live row numbers and «Дії» column (add product + delete with confirm) to /admin/kategorii table — ADM-CAT-07, ADM-CAT-08, locked D-08–D-18.

Purpose: Operators see visual order after DnD and act on categories without leaving the list.
Output: deleteCategoryFromListAction + CategoryTableDeleteButton; extended AdminCategoriesTable + tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/39-calls-auto-save-categories-table-actions/39-CONTEXT.md
@.planning/phases/39-calls-auto-save-categories-table-actions/39-RESEARCH.md
@.planning/phases/39-calls-auto-save-categories-table-actions/39-PATTERNS.md
@.planning/phases/39-calls-auto-save-categories-table-actions/39-UI-SPEC.md
@.planning/phases/39-calls-auto-save-categories-table-actions/39-VALIDATION.md
</context>

<interfaces>
From src/server/actions/admin/product.actions.ts (mirror for list delete):
export async function deleteProductFromListAction(id: string): Promise<{ ok: true } | { ok: false; error: string }>;

From src/server/actions/admin/category.actions.ts (do NOT use for table):
export async function deleteCategoryAction(id: string); // redirects to /admin/kategorii on success

From src/components/admin/product-list-delete-button.tsx:
// AlertDialog, suppressAdminRowNavigation, stopPropagation, deleteProductFromListAction

From src/components/admin/category-form.tsx:
const errorMessages — CATEGORY_HAS_PRODUCTS, CATEGORY_NOT_FOUND, UNKNOWN (D-13)

From src/components/admin/admin-categories-table.tsx:
export type AdminCategoryRow = { id, name, sortOrder, productCount };
// localCategories state + arrayMove on drag end — keep optimistic reorder

From src/lib/admin/clickable-table-row.ts:
suppressAdminRowNavigation(); getAdminClickableRowProps; data-admin-row-interactive

From src/app/(admin)/admin/tovary/novyi/page.tsx:
// searchParams.categoryId preselect — already supported (D-16)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — deleteCategoryFromListAction + tests</name>
  <files>src/server/actions/admin/category.actions.ts, src/server/actions/admin/category.actions.test.ts</files>
  <read_first>
    - src/server/actions/admin/product.actions.ts (deleteProductFromListAction)
    - src/server/actions/admin/category.actions.ts (deleteCategoryAction redirect behavior)
    - src/server/services/admin-catalog.service.ts (CATEGORY_HAS_PRODUCTS)
    - .planning/phases/39-calls-auto-save-categories-table-actions/39-RESEARCH.md (Pattern 3)
  </read_first>
  <behavior>
    - deleteCategoryFromListAction("valid-id") returns { ok: true } and does not throw redirect
    - When deleteCategory throws CATEGORY_HAS_PRODUCTS → { ok: false, error: "CATEGORY_HAS_PRODUCTS" }
    - When deleteCategory throws CATEGORY_NOT_FOUND → { ok: false, error: "CATEGORY_NOT_FOUND" }
    - Invalid id (empty) → { ok: false, error: "UNKNOWN" }
  </behavior>
  <action>
    1. Add src/server/actions/admin/category.actions.test.ts (new — no prior admin action tests).

       Mock pattern:
       - vi.mock @/lib/permissions requireAdmin resolves
       - vi.mock @/server/services/admin-catalog.service deleteCategory
       - vi.mock next/cache revalidatePath (no-op)

       Tests call deleteCategoryFromListAction directly after export exists.

    2. In category.actions.ts add export async function deleteCategoryFromListAction(id: string) per D-14 / RESEARCH Pattern 3:
       - requireAdmin()
       - Guard invalid id like deleteCategoryAction
       - try: await deleteCategory(id); revalidateCategoryPaths(); return { ok: true }
       - catch: return mapCategoryError(error) — NO redirect

    3. Leave deleteCategoryAction unchanged (edit form + Phase 40).

    RED first: write tests, confirm fail, then implement action (GREEN).
  </action>
  <verify>
    <automated>npm test -- src/server/actions/admin/category.actions.test.ts -x</automated>
    <automated>rg -v '^#' src/server/actions/admin/category.actions.ts | rg -c 'deleteCategoryFromListAction' | test "$(rg -v '^#' src/server/actions/admin/category.actions.ts | rg -c 'deleteCategoryFromListAction' || echo 0)" -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - deleteCategoryFromListAction exported alongside existing actions
    - Tests pass; success path never calls redirect()
    - deleteCategoryAction still redirects on success (unchanged)
  </acceptance_criteria>
  <done>List-scoped category delete action tested and shipped</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CategoryTableDeleteButton + unit test</name>
  <files>src/components/admin/category-table-delete-button.tsx, src/components/admin/category-table-delete-button.test.tsx</files>
  <read_first>
    - src/components/admin/product-list-delete-button.tsx
    - src/components/admin/product-list-delete-button.test.tsx
    - src/components/admin/category-form.tsx (errorMessages)
    - .planning/phases/39-calls-auto-save-categories-table-actions/39-UI-SPEC.md (D-11, D-15, D-17)
  </read_first>
  <behavior>
    - Click and pointerDown on «Видалити» button do not bubble to parent onClick
    - On confirm success: deleteCategoryFromListAction called; onDeleted callback invoked; toast.success «Категорію видалено» (mirror product list)
    - On CATEGORY_HAS_PRODUCTS: toast.error with mapped message; dialog closes or stays per product pattern
  </behavior>
  <action>
    Create category-table-delete-button.tsx per D-11, D-12, D-15, D-17:

    Props: { categoryId: string; onDeleted?: () => void }

    - Mirror product-list-delete-button: AlertDialog, suppressAdminRowNavigation on close, startTransition on confirm
    - Call deleteCategoryFromListAction(categoryId) — NOT deleteCategoryAction (D-14)
    - errorMessages from category-form.tsx copy (D-13)
    - Text Button «Видалити» variant="outline" size="sm" with destructive text classes (not icon-only Trash2)
    - Wrapper div data-admin-row-interactive inline-flex
    - stopPropagation on click + pointerDown for open button
    - AlertDialogTitle «Видалити категорію?»; Description: irreversible delete warning (empty category) — server blocks if products exist
    - On success: toast.success «Категорію видалено»; setOpen(false); onDeleted?.() — NO router.refresh (D-14: parent filters localCategories)

    Create category-table-delete-button.test.tsx — copy product-list-delete-button stopPropagation test with name /видалити/i.
  </action>
  <verify>
    <automated>npm test -- src/components/admin/category-table-delete-button.test.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - Component uses deleteCategoryFromListAction only
    - stopPropagation test passes
    - Text «Видалити» button visible (not icon-only)
  </acceptance_criteria>
  <done>Delete button component ready for table integration</done>
</task>

<task type="auto">
  <name>Task 3: AdminCategoriesTable № + Дії columns + tests</name>
  <files>src/components/admin/admin-categories-table.tsx, src/components/admin/admin-categories-table.test.tsx</files>
  <read_first>
    - src/components/admin/admin-categories-table.tsx
    - src/components/admin/admin-categories-table.test.tsx
    - .planning/phases/39-calls-auto-save-categories-table-actions/39-PATTERNS.md
    - .planning/phases/39-calls-auto-save-categories-table-actions/39-CONTEXT.md (D-08–D-10, D-16, D-18)
  </read_first>
  <action>
    Extend AdminCategoriesTable per ADM-CAT-07/08:

    1. Column order in thead (D-09): № → grip → Назва → Товари → Дії

    2. SortableRow props: add rowNumber: number; onCategoryDeleted: (id: string) => void

    3. Map (D-08/D-10):
       localCategories.map((category, index) => (
         <SortableRow rowNumber={index + 1} onCategoryDeleted={...} />
       ))

    4. № cell: td with tabular-nums text-muted-foreground showing rowNumber — NOT category.sortOrder

    5. Дії cell (D-15/D-16/D-18):
       div data-admin-row-interactive flex flex-wrap gap-2
       - Button outline sm asChild + Link href={`/admin/tovary/novyi?categoryId=${category.id}`} label «Додати товар» + stopRowNav on click/pointerDown
       - CategoryTableDeleteButton categoryId onDeleted={() => onCategoryDeleted(category.id)}

    6. Parent handler (D-14):
       onCategoryDeleted={(id) => setLocalCategories((prev) => prev.filter((c) => c.id !== id))}

    7. Extend admin-categories-table.test.tsx:
       - Two categories render № «1» and «2» in order
       - «Додати товар» link href `/admin/tovary/novyi?categoryId=cat-1`; click does not push /admin/kategorii/cat-1
       - «Видалити» button click does not push edit URL (same guard as «Переглянути» test)
       - Optional: reorder simulation — if heavy, document manual DnD check in verification; prefer unit test rowNumber prop if DnD mock costly

    Keep existing «Переглянути» products link and DnD logic unchanged.
  </action>
  <verify>
    <automated>npm test -- src/components/admin/admin-categories-table.test.tsx -x</automated>
    <automated>npm test -- src/components/admin/category-table-delete-button.test.tsx -x</automated>
    <automated>npm test -- src/server/actions/admin/category.actions.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - № column shows index+1 and updates when localCategories order changes
    - «Додати товар» uses novyi?categoryId= URL (not adminProductsUrl list filter)
    - Delete uses list action; row removed via setLocalCategories filter
    - All three automated test paths green
  </acceptance_criteria>
  <done>ADM-CAT-07 and ADM-CAT-08 table UX complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Server Action | Category id from admin table crosses to deleteCategoryFromListAction |
| Browser → Link | categoryId query param to novyi product form |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-06 | Spoofing | deleteCategoryFromListAction | mitigate | requireAdmin() before deleteCategory service |
| T-39-07 | Tampering | Category id param | mitigate | Server validates id string; service enforces CATEGORY_HAS_PRODUCTS |
| T-39-08 | Elevation | Delete from list | mitigate | Same deleteCategory business rules as form action |
| T-39-09 | Repudiation | Delete confirm | accept | AlertDialog UX only; server is source of truth |
| T-39-10 | DoS | Mass delete clicks | accept | useTransition pending state on button |
| T-39-SC | Tampering | npm installs | accept | No new packages this plan |

</threat_model>

<verification>
1. npm test -- src/server/actions/admin/category.actions.test.ts src/components/admin/category-table-delete-button.test.tsx src/components/admin/admin-categories-table.test.tsx -x
2. rg deleteCategoryFromListAction src/components/admin — must appear in category-table-delete-button only (not table calling deleteCategoryAction)
3. Manual: /admin/kategorii — DnD reorder updates №; «Додати товар» opens novyi with category; delete empty category removes row
</verification>

<success_criteria>
- ADM-CAT-07: № column reflects localCategories order after DnD
- ADM-CAT-08: Дії column with add-product link and confirmed delete
- Locked D-08–D-18 satisfied; deleteCategoryAction not used from table
</success_criteria>

<output>
Create `.planning/phases/39-calls-auto-save-categories-table-actions/39-02-SUMMARY.md` when done
</output>
