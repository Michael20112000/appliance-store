---
phase: 03-cart-checkout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/types/cart.ts
  - src/types/order.ts
  - src/server/validators/cart.ts
  - src/server/validators/order.ts
  - src/server/validators/order.test.ts
  - src/server/services/cart.service.ts
  - src/server/services/cart.service.test.ts
  - src/server/services/order.service.ts
  - src/server/services/order.service.test.ts
autonomous: true
requirements:
  - CART-02
  - CHK-01
  - CHK-02
  - CHK-03
  - CHK-04
context_decisions:
  - "Two-tier cart: DB Cart post-auth only; localStorage pending deferred to 03-02 (03-RESEARCH SC#1 + CART-03)"
  - "CartItem.quantity always 1 for used SKUs — validators enforce max 1"
  - "Order snapshots: titleSnapshot + priceSnapshot Int kopiyky at checkout (03-04 implements transaction)"
  - "orderNumber format ASL-YYYYMMDD-#### per 03-RESEARCH A2"
  - "No payment fields or pending_payment status (CHK-03, PITFALLS #7)"

must_haves:
  truths:
    - "Postgres has Cart, CartItem, Order, OrderItem with OrderStatus and DeliveryType enums"
    - "cart.service can load, add, remove, and merge lines for a userId with AVAILABLE-only guard"
    - "order.service exposes listOrdersForUser and generateOrderNumber; createOrderFromCart exists as typed stub for 03-04"
  artifacts:
    - path: prisma/schema.prisma
      provides: Cart, CartItem, Order, OrderItem models
      contains: model Cart
    - path: src/server/services/cart.service.ts
      provides: getOrCreateCart, getCartForUser, addToCart, removeFromCart, mergePendingItems, getCartItemCount
      exports: ["getCartForUser", "addToCart", "mergePendingItems", "getCartItemCount"]
    - path: src/server/services/order.service.ts
      provides: listOrdersForUser, generateOrderNumber, createOrderFromCart (stub throws until 03-04)
      exports: ["listOrdersForUser", "generateOrderNumber", "createOrderFromCart"]
  key_links:
    - from: src/server/services/cart.service.ts
      to: prisma.product
      via: status AVAILABLE check before upsert
      pattern: 'status:\s*["\']AVAILABLE["\']'
    - from: prisma/schema.prisma
      to: Product
      via: cartItems and orderItems relations
      pattern: cartItems CartItem

---

## Phase Goal

**As a** авторизований покупець, **I want to** мати надійний кошик і замовлення в базі, **so that** наступні кроки (UI, checkout) працюють на єдиному джерелі правди (фундамент Phase 3).

<objective>
Додати Prisma Cart/Order моделі, міграцію, cart.service (повна логіка), order.service (list + orderNumber + stub checkout), Zod validators і unit-тести.

Purpose: Data + service backbone для cart UI, checkout transaction, і кабінету.
Output: Migrated schema, cart.service з AVAILABLE guard, order.service skeleton, Wave 0 unit test scaffolds.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-cart-checkout/03-RESEARCH.md
@.planning/phases/03-cart-checkout/03-PATTERNS.md
@prisma/schema.prisma
@src/server/services/catalog.service.ts
@src/lib/db.ts

<interfaces>
From prisma/schema.prisma (Product — extend):
- Add relations `cartItems CartItem[]`, `orderItems OrderItem[]` on Product

New enums and models per 03-RESEARCH Prisma block:
- OrderStatus: PENDING, CONFIRMED, READY_FOR_PICKUP, OUT_FOR_DELIVERY, COMPLETED, CANCELLED
- DeliveryType: PICKUP, LVIV_DELIVERY
- Cart: userId @unique, items CartItem[]
- CartItem: @@unique([cartId, productId]), quantity default 1
- Order: orderNumber @unique, userId, customerPhone, customerName, deliveryType, deliveryAddress?, notes?
- OrderItem: titleSnapshot, priceSnapshot Int, productId optional SetNull on delete
- Indexes: @@index([userId, createdAt]) on Order, @@index([status, createdAt])

From src/server/services/catalog.service.ts:
- PUBLIC_STATUS = "AVAILABLE" — reuse same guard semantics in cart.service

From src/lib/db.ts:
- export const prisma — PrismaNeon singleton; services import only this
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prisma Cart/Order schema + migrate</name>
  <read_first>
    - 03-RESEARCH.md — Prisma schema additions block
    - prisma/schema.prisma — Product model lines to extend
  </read_first>
  <files>prisma/schema.prisma</files>
  <action>
    Add enums OrderStatus, DeliveryType and models Cart, CartItem, Order, OrderItem exactly per 03-RESEARCH (kopiyky priceSnapshot, no payment columns).
    Extend Product with cartItems and orderItems relations.
    userId on Cart/Order is String (Better Auth User.id) — no FK to user table required.
    Do NOT add datasource url to schema.prisma (Prisma 7 — url in prisma.config.ts).
    **BLOCKING:** Run `npx prisma migrate dev --name cart_order_models` using DIRECT_URL.
    Run `npx prisma generate`.
  </action>
  <acceptance_criteria>
    - Migration applied on dev DB
    - prisma validate exit 0
    - Generated client exports Cart, Order, OrderStatus, DeliveryType
  </acceptance_criteria>
  <verify>
    <automated>npx prisma validate && npx prisma migrate status</automated>
  </verify>
  <done>Cart and Order tables exist in Neon; migrate status shows applied migration</done>
</task>

<task type="auto">
  <name>Task 2: Types, validators, cart.service</name>
  <read_first>
    - 03-PATTERNS.md — cart.service analog catalog.service
    - src/server/validators/product.ts — Zod style
    - src/types/catalog.ts — DTO pattern
  </read_first>
  <files>
    src/types/cart.ts
    src/types/order.ts
    src/server/validators/cart.ts
    src/server/validators/order.ts
    src/server/services/cart.service.ts
  </files>
  <action>
    Create src/types/cart.ts: CartLineDto (productId, slug, title, brand, priceKopiyky, imageUrl?, condition), CartViewDto (items, subtotalKopiyky).
    Create src/types/order.ts: OrderSummaryDto, OrderDetailDto for list/confirmation (serializable for RSC).
    Validators (Ukrainian error messages):
    - addToCartSchema: productId z.cuid(), quantity z.literal(1) or z.number().int().min(1).max(1)
    - mergePendingSchema: items array max 20 of { productId: z.cuid() }
    - checkoutSchema (for 03-04): customerName min 2, customerPhone UA +380 pattern, deliveryType enum, deliveryAddress min 10 when LVIV_DELIVERY, notes max 500 optional
    Implement cart.service.ts (pure TS, prisma from @/lib/db only):
    - getOrCreateCart(userId)
    - getCartForUser(userId) — include product + first image (mirror catalog card include shape), map to CartLineDto, drop lines where product.status !== AVAILABLE with removedTitles side channel or separate sanitizeStaleLines(userId)
    - addToCart(userId, productId) — reject if not AVAILABLE; upsert CartItem qty 1
    - removeFromCart(userId, productId)
    - mergePendingItems(userId, pending: { productId: string }[]) — skip unavailable; upsert qty 1
    - getCartItemCount(userId) — _count on cart items
    No React imports; no "use server".
  </action>
  <acceptance_criteria>
    - All cart.service exports typed and used only via prisma
    - mergePendingItems skips SOLD/DRAFT products
    - checkoutSchema refines address required for LVIV_DELIVERY
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>/dev/null || npm run build 2>&1 | head -5; test -f src/server/services/cart.service.ts && test -f src/server/validators/order.ts</automated>
  </verify>
  <done>Cart DTOs, validators, and full cart.service ready for actions layer</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: order.service skeleton + unit tests (Wave 0)</name>
  <read_first>
    - 03-RESEARCH.md — createOrderFromCart transaction (implement fully in 03-04)
    - src/server/services/catalog.service.test.ts — Vitest style
  </read_first>
  <files>
    src/server/services/order.service.ts
    src/server/services/cart.service.test.ts
    src/server/services/order.service.test.ts
    src/server/validators/order.test.ts
  </files>
  <behavior>
    - buildCheckoutInput / order validator: rejects invalid phone, rejects LVIV_DELIVERY without address
    - mergePendingItems pure helper or cart tests: dedupe productIds, cap 20
    - generateOrderNumber: produces ASL-YYYYMMDD-#### pattern (mock tx counter in test)
    - createOrderFromCart: exported but throws "NOT_IMPLEMENTED" until 03-04 (or returns never called in tests)
  </behavior>
  <action>
    Implement order.service.ts:
    - listOrdersForUser(userId) — findMany Order where userId, orderBy createdAt desc, include items count + total from snapshots
    - generateOrderNumber(tx) — ASL-YYYYMMDD-#### sequential per day (query max suffix for date prefix)
    - createOrderFromCart(userId, input) — export signature matching checkoutSchema; body `throw new Error("NOT_IMPLEMENTED")` with comment pointing to 03-04
    Create cart.service.test.ts: test AVAILABLE guard in add path via mocked prisma OR pure helper extract canAddProductToCart(status).
    Create order.service.test.ts: generateOrderNumber format; listOrdersForUser maps DTO (mock prisma if needed).
    Create order.test.ts for checkoutSchema phone + delivery address conditional.
    Scaffold empty describe blocks in order.service.test.ts for transaction tests (filled in 03-04) per 03-VALIDATION.md Wave 0.
  </action>
  <acceptance_criteria>
    - npm test passes new validator and cart/order unit files
    - createOrderFromCart not called in production paths until 03-04
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/server/services/cart.service.test.ts src/server/services/order.service.test.ts src/server/validators/order.test.ts</automated>
  </verify>
  <done>Order list + orderNumber helpers tested; checkout transaction deferred to 03-04 with test scaffold</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cart.service → Postgres | Cart rows keyed by userId; must not add non-AVAILABLE products |
| order.service → Postgres | Order reads scoped by userId in listOrdersForUser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Elevation | cart.service addToCart | mitigate | status AVAILABLE check before upsert; unit tests |
| T-03-01-02 | Tampering | checkoutSchema | mitigate | Zod at boundary; price never from client (snapshots at checkout in 03-04) |
| T-03-01-03 | Denial of Service | mergePendingItems | mitigate | max 20 items in mergePendingSchema |
| T-03-01-SC | Tampering | prisma migrate | mitigate | Review generated SQL; migrate dev only on dev branch |

</threat_model>

<verification>
npx prisma migrate status
npm test -- src/server/services/cart.service.test.ts src/server/services/order.service.test.ts src/server/validators/order.test.ts
</verification>

<success_criteria>
Cart/Order schema live; cart.service enforces AVAILABLE-only; order.service lists orders and generates orderNumber; unit tests green
</success_criteria>

<output>
Create `.planning/phases/03-cart-checkout/03-01-SUMMARY.md` when done
</output>
