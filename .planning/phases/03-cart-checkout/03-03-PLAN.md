---
phase: 03-cart-checkout
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - src/app/(storefront)/koszyk/page.tsx
  - src/components/cart/cart-line-item.tsx
  - src/components/cart/cart-quantity-control.tsx
  - src/components/cart/cart-summary.tsx
  - src/components/cart/cart-empty.tsx
  - src/components/cart/cart-page-content.tsx
autonomous: true
requirements:
  - CART-02
  - AUTH-03
context_decisions:
  - "Qty max 1 — CartQuantityControl hides/disables + at 1 (03-UI-SPEC)"
  - "Stale SOLD lines removed on load with UA message (03-RESEARCH Pitfall 3)"
  - "Route /koszyk per user + ARCHITECTURE; metadata title «Кошик»"

must_haves:
  truths:
    - "Authenticated user opens /koszyk and sees line items with price and thumbnails"
    - "User removes a line and sees updated subtotal"
    - "Empty cart shows Ukrainian empty state with link to catalog"
    - "Guest visiting /koszyk is redirected to /uviity with callbackUrl=/koszyk"
  artifacts:
    - path: src/app/(storefront)/koszyk/page.tsx
      provides: Cart page RSC shell
    - path: src/components/cart/cart-line-item.tsx
      provides: Single line UI with remove
    - path: src/components/cart/cart-summary.tsx
      provides: Subtotal + CTA to /zamovlennia
  key_links:
    - from: src/app/(storefront)/koszyk/page.tsx
      to: cart.service getCartForUser
      via: service call not prisma
      pattern: getCartForUser
    - from: src/components/cart/cart-line-item.tsx
      to: removeFromCartAction
      via: form action or onClick transition
      pattern: removeFromCartAction

---

## Phase Goal

**As a** авторизований покупець, **I want to** переглядати й редагувати кошик на /koszyk, **so that** я контролюю склад замовлення перед оформленням (CART-02).

<objective>
Сторінка /koszyk з лініями кошика, видаленням, підсумком і CTA на checkout; auth gate для гостей.

Purpose: Повний CART-02 UI на українських маршрутах.
Output: /koszyk page + cart components per 03-UI-SPEC.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-cart-checkout/03-UI-SPEC.md — /koszyk layout, CartLineItem, copy
@.planning/phases/03-cart-checkout/03-PATTERNS.md — koszyk/page analog kabinet
@src/app/(storefront)/kabinet/page.tsx
@src/components/catalog/product-card.tsx
@src/components/catalog/price-display.tsx
@src/server/actions/cart.actions.ts

<interfaces>
From kabinet/page.tsx:
- getSession + redirect /uviity pattern — use requireBuyer('/koszyk') instead

From cart.service (03-01):
- getCartForUser(userId) → CartViewDto

From cart.actions (03-02):
- removeFromCartAction(productId)

From 03-UI-SPEC:
- Container max-w-6xl; grid md:grid-cols-[1fr_320px]; sticky summary md:top-20
- Copy: «Кошик», «Оформити замовлення», empty state strings
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /koszyk page + auth gate</name>
  <read_first>
    - src/app/(storefront)/kabinet/page.tsx
    - 03-UI-SPEC.md — Routing /koszyk
  </read_first>
  <files>src/app/(storefront)/koszyk/page.tsx</files>
  <action>
    Create src/app/(storefront)/koszyk/page.tsx as async RSC.
    Call requireBuyer('/koszyk') at top (AUTH-03).
    metadata.title = «Кошик».
    Fetch cart via getCartForUser(session.user.id) — no direct prisma in page.
    If sanitize removed stale lines, show Alert at top listing removed titles (UA).
    Render CartPageContent or inline: H1 «Кошик», empty → CartEmpty, else grid with lines + CartSummary.
    Layout className mx-auto max-w-6xl px-4 py-12 sm:px-6 per UI-SPEC.
  </action>
  <acceptance_criteria>
    - Unauthenticated request redirects to /uviity?callbackUrl=%2Fkoszyk
    - Page imports getCartForUser from cart.service only
  </acceptance_criteria>
  <verify>
    <automated>test -f src/app/\(storefront\)/koszyk/page.tsx && grep -c requireBuyer src/app/\(storefront\)/koszyk/page.tsx | awk '{exit ($1>=1)?0:1}'</automated>
  </verify>
  <done>/koszyk route exists with buyer-only access</done>
</task>

<task type="auto">
  <name>Task 2: Cart line components + empty state</name>
  <read_first>
    - 03-UI-SPEC.md — CartLineItem, CartQuantityControl, CartEmpty
    - src/components/catalog/product-card.tsx — thumb + PriceDisplay
  </read_first>
  <files>
    src/components/cart/cart-line-item.tsx
    src/components/cart/cart-quantity-control.tsx
    src/components/cart/cart-empty.tsx
    src/components/cart/cart-page-content.tsx
  </files>
  <action>
    cart-line-item.tsx: props line CartLineDto; row with OptimizedImage thumb 80px, title Link to /tovar/[slug], brand optional, PriceDisplay, CartQuantityControl, remove Button ghost destructive «Прибрати» calling removeFromCartAction via useTransition + router.refresh().
    cart-quantity-control.tsx: display qty 1; minus disabled at 1; plus hidden or disabled (max 1 hint «Одна одиниця на позицію…» sr-only labels per UI-SPEC a11y).
    cart-empty.tsx: centered «Кошик порожній» + body copy + primary Link «Перейти до каталогу» → /katalog.
    cart-page-content.tsx: optional client wrapper for remove pending state; or keep lines as client children.
    Use Card border-b list pattern inside single Card per UI-SPEC.
  </action>
  <acceptance_criteria>
    - All monetary values use PriceDisplay / formatPriceKopiyky
    - Remove triggers server action and refresh
  </acceptance_criteria>
  <verify>
    <automated>test -f src/components/cart/cart-line-item.tsx && test -f src/components/cart/cart-empty.tsx && grep -c PriceDisplay src/components/cart/cart-line-item.tsx | awk '{exit ($1>=1)?0:1}'</automated>
  </verify>
  <done>Cart line UI matches design contract; remove works</done>
</task>

<task type="auto">
  <name>Task 3: CartSummary + checkout CTA</name>
  <read_first>
    - 03-UI-SPEC.md — CartSummary sticky, «Оформити замовлення»
  </read_first>
  <files>src/components/cart/cart-summary.tsx</files>
  <action>
    Create cart-summary.tsx: props subtotalKopiyky, itemCount, disabled when empty.
    Show «Разом» + PriceDisplay tabular-nums.
    Primary Button Link or href /zamovlennia «Оформити замовлення» — disabled if itemCount === 0.
    Muted payment notice «Оплата при отриманні…» footer text.
    Desktop: md:sticky md:top-20 in parent grid (wired in koszyk page).
    Mobile: summary below list per UI-SPEC.
  </action>
  <acceptance_criteria>
    - CTA links to /zamovlennia only when cart has items
    - Sticky positioning on md+ breakpoints
  </acceptance_criteria>
  <verify>
    <automated>grep -c zamovlennia src/components/cart/cart-summary.tsx | awk '{exit ($1>=1)?0:1}'</automated>
  </verify>
  <done>CART-02 complete: view, remove, summary, path to checkout</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /koszyk RSC | Only session owner cart via userId from session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Elevation | /koszyk | mitigate | requireBuyer redirect |
| T-03-03-02 | Tampering | removeFromCartAction | mitigate | requireBuyer; productId scoped to user cart |

</threat_model>

<verification>
test -f src/app/(storefront)/koszyk/page.tsx
grep -c getCartForUser src/app/(storefront)/koszyk/page.tsx
</verification>

<success_criteria>
Logged-in buyer manages cart at /koszyk; guest redirected; CTA to /zamovlennia when items present
</success_criteria>

<output>
Create `.planning/phases/03-cart-checkout/03-03-SUMMARY.md` when done
</output>
