---
phase: 03-cart-checkout
plan: 05
type: execute
wave: 5
depends_on:
  - 03-04
files_modified:
  - src/app/(storefront)/kabinet/page.tsx
  - src/components/account/order-history-list.tsx
  - src/components/account/order-history-card.tsx
  - src/components/cart/cart-nav-link.tsx
  - src/components/layout/store-header.tsx
  - e2e/cart-auth.spec.ts
  - e2e/cart-merge.spec.ts
  - e2e/checkout.spec.ts
  - e2e/orders-history.spec.ts
autonomous: true
requirements:
  - CHK-04
  - AUTH-03
context_decisions:
  - "CartNavLink visible when logged in only (03-UI-SPEC)"
  - "Order history in /kabinet — no separate order detail page in Phase 3"
  - "E2E extends auth.spec.ts + product-pdp patterns (03-PATTERNS)"

must_haves:
  truths:
    - "Buyer sees order history in /kabinet with order number, date, status, total"
    - "Logged-in header shows cart icon with item count badge linking to /koszyk"
    - "E2E covers guest redirect, add-to-cart, checkout, confirmation, cabinet list"
  artifacts:
    - path: src/components/account/order-history-list.tsx
      provides: CHK-04 list
    - path: src/components/cart/cart-nav-link.tsx
      provides: Header cart badge
    - path: e2e/checkout.spec.ts
      provides: End-to-end checkout verification
  key_links:
    - from: src/components/layout/store-header.tsx
      to: getCartItemCount
      via: parallel fetch with session
      pattern: getCartItemCount
    - from: src/app/(storefront)/kabinet/page.tsx
      to: listOrdersForUser
      via: order.service
      pattern: listOrdersForUser

---

## Phase Goal

**As a** авторизований покупець, **I want to** бачити історію замовлень і кількість у кошику в шапці, **so that** я контролюю покупки після checkout (CHK-04, AUTH-03 polish).

<objective>
Розширити /kabinet історією замовлень, додати CartNavLink у header, Playwright E2E для cart/checkout flows.

Purpose: Phase 3 verification gate and buyer retention UX.
Output: Order history UI, header badge, e2e specs.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-cart-checkout/03-UI-SPEC.md — OrderHistoryCard, CartNavLink
@.planning/phases/03-cart-checkout/03-RESEARCH.md — Validation Architecture test map
@e2e/auth.spec.ts
@e2e/product-pdp.spec.ts
@src/components/layout/store-header.tsx

<interfaces>
From order.service (03-01/03-04):
- listOrdersForUser(userId) → OrderSummaryDto[]

From cart.service:
- getCartItemCount(userId)

From e2e/auth.spec.ts:
- register/login flow Ukrainian button labels
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Order history in /kabinet</name>
  <read_first>
    - 03-UI-SPEC.md — OrderHistoryList, status badges
    - src/app/(storefront)/kabinet/page.tsx
  </read_first>
  <files>
    src/app/(storefront)/kabinet/page.tsx
    src/components/account/order-history-list.tsx
    src/components/account/order-history-card.tsx
  </files>
  <action>
    kabinet/page.tsx: keep welcome line; add H2 «Мої замовлення»; fetch listOrdersForUser(session.user.id); render OrderHistoryList or empty state per UI-SPEC («Замовлень ще немає» + CTA catalog).
    order-history-card.tsx: Card with orderNumber, Badge status UA labels map (PENDING → «Очікує підтвердження» per UI-SPEC table), date uk-UA «від …», item count «N поз.», PriceDisplay total.
    order-history-list.tsx: map orders createdAt desc; Alert on fetch error.
    No link to admin; no buyer order detail page in Phase 3.
  </action>
  <acceptance_criteria>
    - CHK-04: completed orders visible after checkout flow
    - Only session user orders listed
  </acceptance_criteria>
  <verify>
    <automated>grep -c listOrdersForUser src/app/\(storefront\)/kabinet/page.tsx && test -f src/components/account/order-history-list.tsx</automated>
  </verify>
  <done>Order history replaces placeholder copy in cabinet</done>
</task>

<task type="auto">
  <name>Task 2: CartNavLink in StoreHeader</name>
  <read_first>
    - 03-UI-SPEC.md — CartNavLink spec, logged-in only
    - src/components/layout/store-header.tsx
  </read_first>
  <files>
    src/components/cart/cart-nav-link.tsx
    src/components/layout/store-header.tsx
  </files>
  <action>
    cart-nav-link.tsx: client or server — prefer server wrapper passing count prop; Link /koszyk, ShoppingCart icon lucide, Badge count cap «9+», aria-label «Кошик, {n} товарів» / «Кошик, порожній», min-h-11 touch target, aria-current on /koszyk.
    store-header.tsx: when session?.user, await getCartItemCount(session.user.id) in parallel with categories; render CartNavLink before StoreHeaderAuth desktop + mobile sheet top link «Кошик» when logged in.
    Hide entirely when logged out (UI-SPEC).
    Ensure 03-02 revalidatePath('/', 'layout') refreshes count after add/remove/checkout.
  </action>
  <acceptance_criteria>
    - Badge shows 0 hidden or empty state per UI-SPEC (show pill only if n > 0)
    - Logged-out users do not see cart icon
  </acceptance_criteria>
  <verify>
    <automated>grep -c CartNavLink src/components/layout/store-header.tsx | awk '{exit ($1>=1)?0:1}' && test -f src/components/cart/cart-nav-link.tsx</automated>
  </verify>
  <done>Header cart badge links to /koszyk for authenticated buyers</done>
</task>

<task type="auto">
  <name>Task 3: Playwright E2E suite (Wave 0 files)</name>
  <read_first>
    - 03-RESEARCH.md — Phase Requirements → Test Map
    - e2e/auth.spec.ts
    - e2e/product-pdp.spec.ts
  </read_first>
  <files>
    e2e/cart-auth.spec.ts
    e2e/cart-merge.spec.ts
    e2e/checkout.spec.ts
    e2e/orders-history.spec.ts
  </files>
  <action>
    cart-auth.spec.ts: guest navigates /koszyk → expect URL /uviity with callbackUrl; guest PDP «Додати в кошик» or «Увійти» → /uviity (AUTH-03, SC#1).
    cart-merge.spec.ts: register unique email; before login set localStorage appliance-cart-pending with seeded AVAILABLE productId from env or discover via /katalog first product; login; expect merge and product in /koszyk (CART-03).
    checkout.spec.ts: login buyer; add from PDP; /koszyk → /zamovlennia; fill phone +380…; select delivery; submit; expect /zamovlennia/pidtverdzhennia/; expect copy «Оплата при отриманні» (CHK-01–03).
    orders-history.spec.ts: after checkout, /kabinet shows order number (CHK-04).
    Use getByRole Ukrainian labels from UI-SPEC. Reuse auth register pattern from auth.spec.ts.
    Document required seed product in test comments.
  </action>
  <acceptance_criteria>
    - All four spec files run with playwright
    - Tests skip gracefully if DATABASE_URL unset (match existing e2e pattern if any)
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test e2e/cart-auth.spec.ts e2e/checkout.spec.ts --reporter=line 2>&1 | tail -20</automated>
  </verify>
  <done>E2E coverage for Phase 3 requirements per 03-VALIDATION.md</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /kabinet orders | listOrdersForUser filters userId |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Elevation | Order history | mitigate | listOrdersForUser session.user.id only |
| T-03-05-02 | Information disclosure | E2E credentials | accept | test-only random emails in CI |

</threat_model>

<verification>
npx playwright test e2e/cart-auth.spec.ts e2e/cart-merge.spec.ts e2e/checkout.spec.ts e2e/orders-history.spec.ts
grep -c OrderHistoryList src/app/(storefront)/kabinet/page.tsx
</verification>

<success_criteria>
CHK-04 order list in cabinet; cart badge in header; Phase 3 e2e specs green in CI
</success_criteria>

<output>
Create `.planning/phases/03-cart-checkout/03-05-SUMMARY.md` when done
</output>
