---
phase: 03-cart-checkout
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/lib/permissions.ts
  - src/lib/cart/pending-storage.ts
  - src/components/auth/auth-form.tsx
  - src/app/(storefront)/reiestratsiia/page.tsx
  - src/server/actions/cart.actions.ts
  - src/components/cart/add-to-cart-button.tsx
  - src/components/cart/cart-pending-merge.tsx
  - src/app/(storefront)/tovar/[slug]/page.tsx
  - src/app/(storefront)/uviity/page.tsx
  - src/app/(storefront)/layout.tsx
autonomous: true
requirements:
  - AUTH-03
  - CART-01
  - CART-03
context_decisions:
  - "CART-03 via localStorage key appliance-cart-pending (03-RESEARCH) — NOT httpOnly cookie from UI-SPEC draft"
  - "Guest add-to-cart: NO server mutation; localStorage + redirect /uviity?callbackUrl= (SC#1)"
  - "requireBuyer(callbackPath?) for commerce routes with encoded callbackUrl"
  - "First use server actions in repo — thin wrappers per 03-PATTERNS"

must_haves:
  truths:
    - "Guest clicking «Додати в кошик» on PDP stores productId in localStorage and redirects to login with callbackUrl"
    - "Logged-in buyer addToCartAction persists line in DB cart and revalidates header/cart paths"
    - "After login, CartPendingMerge calls mergePendingCartAction once and clears localStorage"
    - "/uviity honors callbackUrl query after successful sign-in"
  artifacts:
    - path: src/lib/permissions.ts
      provides: requireBuyer()
      exports: ["requireBuyer"]
    - path: src/lib/cart/pending-storage.ts
      provides: readPending, addPendingProduct, clearPending
    - path: src/server/actions/cart.actions.ts
      provides: addToCartAction, mergePendingCartAction, removeFromCartAction
      exports: ["addToCartAction", "mergePendingCartAction", "removeFromCartAction"]
    - path: src/components/cart/add-to-cart-button.tsx
      provides: PDP CTA with session branch
    - path: src/components/cart/cart-pending-merge.tsx
      provides: post-auth merge client effect
  key_links:
    - from: src/components/cart/add-to-cart-button.tsx
      to: src/server/actions/cart.actions.ts
      via: addToCartAction when session
      pattern: addToCartAction
    - from: src/components/cart/cart-pending-merge.tsx
      to: mergePendingCartAction
      via: useEffect after mount
      pattern: mergePendingCartAction
    - from: src/lib/cart/pending-storage.ts
      to: localStorage
      via: KEY appliance-cart-pending JSON { v:1, items }
      pattern: appliance-cart-pending

---

## Phase Goal

**As a** покупець, **I want to** додавати товари в кошик (з логіном або з об’єднанням після входу), **so that** я не втрачаю намір купити (CART-01, CART-03, AUTH-03).

<objective>
Додати requireBuyer, перші Server Actions для кошика, localStorage pending intent, AddToCartButton на PDP, merge після авторизації, callbackUrl на /uviity.

Purpose: Закрити auth gate + add-to-cart + guest merge без UI сторінки /koszyk.
Output: Працюючий PDP CTA, merge flow, commerce actions.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-cart-checkout/03-RESEARCH.md — Coherent auth + cart flow table
@.planning/phases/03-cart-checkout/03-UI-SPEC.md — AddToCartButton, copywriting
@.planning/phases/03-cart-checkout/03-PATTERNS.md — cart.actions pattern
@src/server/services/cart.service.ts
@src/components/auth/auth-form.tsx
@src/app/(storefront)/uviity/page.tsx

<interfaces>
From src/lib/permissions.ts (extend):
- requireAdmin() existing pattern with auth.api.getSession + redirect

New requireBuyer(callbackPath?: string):
- redirect `/uviity?callbackUrl=${encodeURIComponent(callbackPath ?? '/koszyk')}` when no session
- return session when ok

From src/server/services/cart.service.ts (03-01):
- addToCart(userId, productId)
- mergePendingItems(userId, pending)
- removeFromCart(userId, productId)

From src/lib/cart/pending-storage.ts (03-RESEARCH):
- KEY = "appliance-cart-pending"
- type PendingCart = { v: 1; items: { productId: string }[] }
- addPendingProduct, readPending, clearPending — client-only ("use client" helpers)

From src/components/auth/auth-form.tsx:
- router.push + router.refresh after submit — mirror for post-login redirect with callbackUrl
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: requireBuyer + pending-storage + callbackUrl on login</name>
  <read_first>
    - src/lib/permissions.ts
    - src/app/(storefront)/uviity/page.tsx
    - 03-RESEARCH.md — Pattern 1 requireBuyer, localStorage schema
  </read_first>
  <files>
    src/lib/permissions.ts
    src/lib/cart/pending-storage.ts
    src/app/(storefront)/uviity/page.tsx
    src/app/(storefront)/reiestratsiia/page.tsx
    src/components/auth/auth-form.tsx
  </files>
  <action>
    Add requireBuyer(callbackPath?: string) to src/lib/permissions.ts parallel to requireAdmin — redirect unauthenticated buyers to `/uviity?callbackUrl=...` (default `/koszyk`).
    Create src/lib/cart/pending-storage.ts with client-safe exports only (file may be imported from client components): readPending returns { v:1, items:[] } default, addPendingProduct dedupes by productId, clearPending removes key. Max 20 items enforced client-side and again in mergePendingSchema server-side.
    Update /uviity page: read searchParams callbackUrl (string, must start with `/`, reject external URLs); pass sanitized path to AuthForm as `redirectTo` prop.
    Update AuthForm (required): add optional `redirectTo?: string`; after successful login/register call `router.push(redirectTo ?? '/kabinet')` then `router.refresh()` — replace hardcoded `/kabinet` push.
    Update /reiestratsiia page: read and sanitize `callbackUrl` from searchParams; pass same `redirectTo` to AuthForm; login link from register page preserves `callbackUrl` query.
    Document callbackUrl in page comments for AUTH-03 traceability.
  </action>
  <acceptance_criteria>
    - requireBuyer exported and used by actions in Task 2
    - Invalid callbackUrl (http://) falls back to /kabinet or /koszyk
  </acceptance_criteria>
  <verify>
    <automated>grep -n 'requireBuyer\|callbackUrl' src/lib/permissions.ts src/app/\(storefront\)/uviity/page.tsx src/components/auth/auth-form.tsx 2>/dev/null | grep -v '^#' | head -20</automated>
  </verify>
  <done>Buyer guard and login return URL ready; pending localStorage API exists</done>
</task>

<task type="auto">
  <name>Task 2: cart.actions — add, merge, remove</name>
  <read_first>
    - 03-PATTERNS.md — first "use server" files
    - src/server/validators/cart.ts
  </read_first>
  <files>src/server/actions/cart.actions.ts</files>
  <action>
    Create src/server/actions/cart.actions.ts with "use server" as first action module in repo.
    Implement:
    - addToCartAction(productId: string): requireBuyer(); parse addToCartSchema; cart.service.addToCart(session.user.id, productId); revalidatePath('/koszyk'); revalidatePath('/', 'layout') for header badge in 03-05.
    - mergePendingCartAction(items: { productId: string }[]): requireBuyer(); parse mergePendingSchema; mergePendingItems; revalidatePath('/koszyk'); revalidatePath('/', 'layout'); return { merged: number }.
    - removeFromCartAction(productId: string): requireBuyer(); removeFromCart; revalidatePath('/koszyk'); revalidatePath('/', 'layout').
    Never import prisma in actions. Return { ok: true } or throw/return Ukrainian error codes for sold product.
    Use auth.api.getSession via requireBuyer return value for userId.
  </action>
  <acceptance_criteria>
    - All three actions call requireBuyer before service
    - revalidatePath includes layout for future CartNavLink
  </acceptance_criteria>
  <verify>
    <automated>grep -c '"use server"' src/server/actions/cart.actions.ts | grep -q '^1$' && grep -c 'requireBuyer' src/server/actions/cart.actions.ts | awk '{exit ($1>=3)?0:1}'</automated>
  </verify>
  <done>Server Actions wrap cart.service with auth and cache revalidation</done>
</task>

<task type="auto">
  <name>Task 3: AddToCartButton, CartPendingMerge, PDP + layout</name>
  <read_first>
    - 03-UI-SPEC.md — AddToCartButton spec, copy «Додати в кошик»
    - src/app/(storefront)/tovar/[slug]/page.tsx
    - src/app/(storefront)/layout.tsx
  </read_first>
  <files>
    src/components/cart/add-to-cart-button.tsx
    src/components/cart/cart-pending-merge.tsx
    src/app/(storefront)/tovar/[slug]/page.tsx
    src/app/(storefront)/layout.tsx
  </files>
  <action>
    AddToCartButton (client): props productId, productTitle, disabled (when not AVAILABLE). On click: if no session (pass hasSession boolean from RSC parent OR fetch /api/auth/session — prefer hasSession from page getSession): addPendingProduct(productId); router.push(`/uviity?callbackUrl=${encodeURIComponent(pathname)}`); else call addToCartAction → loading «Додаємо…» → router.refresh(). Primary Button min-h-11, ShoppingCart icon, UA labels per UI-SPEC.
    CartPendingMerge (client): on mount if session exists, readPending; if items.length call mergePendingCartAction then clearPending; run once (useRef guard). Silent fail log only.
    PDP page: getSession at top; pass hasSession + product.id to AddToCartButton; place button after ConditionBadge before «Опис»; keep «До каталогу» as outline secondary. product.status !== AVAILABLE → disabled + Alert destructive «Цей товар уже недоступний…».
    Storefront layout: render CartPendingMerge inside NuqsAdapter when session present — fetch session in layout OR pass from wrapper Server Component child. Simplest: create small CartPendingMergeGate server wrapper that getSession and renders CartPendingMerge only if user.
  </action>
  <acceptance_criteria>
    - Guest never triggers addToCartAction without session
    - Logged-in add refreshes and persists to DB
    - Merge runs once after login when pending items exist
  </acceptance_criteria>
  <verify>
    <automated>test -f src/components/cart/add-to-cart-button.tsx && test -f src/components/cart/cart-pending-merge.tsx && grep -c AddToCartButton src/app/\(storefront\)/tovar/\[slug\]/page.tsx | awk '{exit ($1>=1)?0:1}'</automated>
  </verify>
  <done>PDP add-to-cart and post-login merge complete per CART-01 and CART-03</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser localStorage → merge action | Untrusted productIds; server re-validates AVAILABLE |
| Guest → /uviity | callbackUrl must not open redirect to external site |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Spoofing | addToCartAction | mitigate | requireBuyer on every mutation |
| T-03-02-02 | Tampering | callbackUrl | mitigate | allowlist relative paths only |
| T-03-02-03 | DoS | mergePendingCartAction | mitigate | Zod max 20 items |
| T-03-02-04 | Elevation | guest server cart | mitigate | no DB write without session (SC#1) |

</threat_model>

<verification>
npm test -- src/server/services/cart.service.test.ts
grep -l addToCartAction src/components/cart/add-to-cart-button.tsx src/server/actions/cart.actions.ts
</verification>

<success_criteria>
AUTH-03 gate on actions; CART-01 PDP add; CART-03 localStorage merge after login; callbackUrl works on /uviity
</success_criteria>

<output>
Create `.planning/phases/03-cart-checkout/03-02-SUMMARY.md` when done
</output>
