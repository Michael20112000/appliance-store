---
phase: 03-cart-checkout
plan: 04
type: execute
wave: 4
depends_on:
  - 03-01
  - 03-03
files_modified:
  - src/server/services/order.service.ts
  - src/server/services/order.service.test.ts
  - src/server/actions/order.actions.ts
  - src/app/(storefront)/zamovlennia/page.tsx
  - src/app/(storefront)/zamovlennia/pidtverdzhennia/[orderNumber]/page.tsx
  - src/components/checkout/checkout-form.tsx
  - src/components/checkout/fulfillment-picker.tsx
  - src/components/checkout/checkout-order-summary.tsx
  - src/components/checkout/order-confirmation.tsx
autonomous: true
requirements:
  - CHK-01
  - CHK-02
  - CHK-03
context_decisions:
  - "createOrderFromCart: full Prisma $transaction with updateMany AVAILABLE→SOLD (03-RESEARCH Pitfall 1)"
  - "Confirmation at /zamovlennia/pidtverdzhennia/[orderNumber] per user route contract"
  - "No online payment UI — copy «Оплата при отриманні» (CHK-03)"
  - "Empty cart on /zamovlennia redirects /koszyk"

must_haves:
  truths:
    - "Buyer submits checkout with phone, name, pickup or Lviv delivery with address when required"
    - "Successful checkout creates Order, marks products SOLD, clears cart, redirects to confirmation URL with orderNumber"
    - "Confirmation page shows thank-you and order number without payment spinner"
    - "Concurrent checkout on same product: only one succeeds (transaction)"
  artifacts:
    - path: src/server/services/order.service.ts
      provides: createOrderFromCart full transaction
      exports: ["createOrderFromCart"]
    - path: src/server/actions/order.actions.ts
      provides: submitCheckoutAction
      exports: ["submitCheckoutAction"]
    - path: src/app/(storefront)/zamovlennia/pidtverdzhennia/[orderNumber]/page.tsx
      provides: Confirmation RSC with ownership check
  key_links:
    - from: src/server/services/order.service.ts
      to: prisma.product.updateMany
      via: status AVAILABLE guard in transaction
      pattern: updateMany.*AVAILABLE
    - from: src/components/checkout/checkout-form.tsx
      to: submitCheckoutAction
      via: form submit
      pattern: submitCheckoutAction
    - from: submitCheckoutAction
      to: /zamovlennia/pidtverdzhennia
      via: redirect after order
      pattern: pidtverdzhennia

---

## Phase Goal

**As a** авторизований покупець, **I want to** оформити замовлення без онлайн-оплати, **so that** магазин передзвонить і узгодить самовивіз або доставку (CHK-01, CHK-02, CHK-03).

<objective>
Реалізувати atomic checkout transaction, форму /zamovlennia, підтвердження на /zamovlennia/pidtverdzhennia/[orderNumber].

Purpose: Revenue path completion without payment gateway.
Output: Working offline checkout + inventory lock.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-cart-checkout/03-RESEARCH.md — createOrderFromCart code example
@.planning/phases/03-cart-checkout/03-UI-SPEC.md — CheckoutForm, FulfillmentPicker, confirmation
@.planning/research/PITFALLS.md — double-sale
@src/server/validators/order.ts
@src/components/auth/auth-form.tsx

<interfaces>
From 03-RESEARCH createOrderFromCart:
- prisma.$transaction: validate cart non-empty, all AVAILABLE, create Order+OrderItems snapshots, updateMany SOLD per line, deleteMany cartItems
- throw PRODUCT_UNAVAILABLE or EMPTY_CART for action mapping to UA errors

From checkoutSchema (03-01):
- customerName, customerPhone, deliveryType, deliveryAddress?, notes?

From auth-form.tsx:
- react-hook-form + zodResolver + Alert error pattern
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: createOrderFromCart transaction + unit tests</name>
  <read_first>
    - 03-RESEARCH.md — Checkout transaction block
    - src/server/services/order.service.ts — replace stub
  </read_first>
  <files>
    src/server/services/order.service.ts
    src/server/services/order.service.test.ts
  </files>
  <behavior>
    - createOrderFromCart throws EMPTY_CART when no items
    - createOrderFromCart throws PRODUCT_UNAVAILABLE when product not AVAILABLE at commit
    - generateOrderNumber returns unique ASL-YYYYMMDD-#### per day
    - On success: order.status PENDING, items have snapshots, cart items deleted
  </behavior>
  <action>
    Replace NOT_IMPLEMENTED stub with full createOrderFromCart per 03-RESEARCH:
    Interactive $transaction: load cart+items+product status, validate all AVAILABLE, generateOrderNumber(tx), create Order with items.create snapshots (titleSnapshot, priceSnapshot, quantity 1), loop updateMany where id+AVAILABLE→SOLD count must be 1 each, deleteMany cartItems, return order.
    deliveryAddress null when PICKUP.
    Export ProductUnavailableError or string codes for actions.
    Expand order.service.test.ts: test generateOrderNumber format; integration test with prisma test DB optional; at minimum unit-test validation helpers and document transaction tests for CI with seed.
    After implementation run npm test for order.service.test.ts.
  </action>
  <acceptance_criteria>
    - No path marks SOLD outside transaction
    - revalidatePath planned in action task for PDP/catalog
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/server/services/order.service.test.ts</automated>
  </verify>
  <done>Atomic checkout service prevents double-sale at DB layer</done>
</task>

<task type="auto">
  <name>Task 2: order.actions + /zamovlennia checkout page</name>
  <read_first>
    - 03-UI-SPEC.md — shadcn add radio-group textarea; checkout layout
    - 03-PATTERNS.md — order.actions thin wrapper
  </read_first>
  <files>
    src/server/actions/order.actions.ts
    src/app/(storefront)/zamovlennia/page.tsx
    src/components/checkout/checkout-form.tsx
    src/components/checkout/fulfillment-picker.tsx
    src/components/checkout/checkout-order-summary.tsx
  </files>
  <action>
    Run `npx shadcn@latest add radio-group textarea` if not present (field optional if auth already has labels).
    Create order.actions.ts "use server": submitCheckoutAction(raw) — requireBuyer('/zamovlennia'); checkoutSchema.safeParse; createOrderFromCart(session.user.id, data); revalidatePath('/koszyk'); revalidatePath('/kabinet'); revalidatePath('/tovar/[slug]', 'page') or loop product slugs from cart; redirect(`/zamovlennia/pidtverdzhennia/${order.orderNumber}`).
    Map errors: EMPTY_CART → redirect /koszyk; PRODUCT_UNAVAILABLE → return { error: 'sold' } UA message.
    zamovlennia/page.tsx: requireBuyer; if cart empty redirect /koszyk; getCartForUser; grid checkout form + CheckoutOrderSummary sticky.
    checkout-form.tsx client: RHF + zodResolver(checkoutSchema); phone tel input; FulfillmentPicker; conditional address Textarea; comment optional; submit «Підтвердити замовлення» loading «Оформлюємо…»; prefill customerName from session.user.name display.
    fulfillment-picker.tsx: RadioGroup cards PICKUP / LVIV_DELIVERY UA copy per UI-SPEC.
    checkout-order-summary.tsx: line list + total PriceDisplay.
  </action>
  <acceptance_criteria>
    - CHK-02 address required only for LVIV_DELIVERY client+server
    - CHK-01 phone + fulfillment captured
  </acceptance_criteria>
  <verify>
    <automated>test -f src/server/actions/order.actions.ts && test -f src/app/\(storefront\)/zamovlennia/page.tsx && grep -c submitCheckoutAction src/server/actions/order.actions.ts | awk '{exit ($1>=1)?0:1}'</automated>
  </verify>
  <done>Checkout form and submit action wired to transaction</done>
</task>

<task type="auto">
  <name>Task 3: Confirmation page [orderNumber]</name>
  <read_first>
    - 03-UI-SPEC.md — OrderConfirmation, route pidtverdzhennia/[orderNumber]
  </read_first>
  <files>
    src/app/(storefront)/zamovlennia/pidtverdzhennia/[orderNumber]/page.tsx
    src/components/checkout/order-confirmation.tsx
  </files>
  <action>
    Dynamic route src/app/(storefront)/zamovlennia/pidtverdzhennia/[orderNumber]/page.tsx.
    requireBuyer(); load order by orderNumber AND userId === session.user.id — else notFound() (IDOR prevention per 03-RESEARCH Pitfall 4).
    metadata.title «Замовлення підтверджено».
    order-confirmation.tsx: CheckCircle2 icon, H1 «Дякуємо за замовлення!», order number tabular-nums, body copy with phone, payment notice «Оплата при отриманні…», fulfillment summary UA labels, CTAs «До кабінету» / «Продовжити покупки» → /katalog.
    max-w-lg centered layout per UI-SPEC. CHK-03: no card fields, no payment pending copy.
  </action>
  <acceptance_criteria>
    - Wrong orderNumber or other user order returns 404
    - Page shows orderNumber and offline payment notice
  </acceptance_criteria>
  <verify>
    <automated>test -f src/app/\(storefront\)/zamovlennia/pidtverdzhennia/\[orderNumber\]/page.tsx && grep -c userId src/app/\(storefront\)/zamovlennia/pidtverdzhennia/\[orderNumber\]/page.tsx | awk '{exit ($1>=1)?0:1}'</automated>
  </verify>
  <done>CHK-03 confirmation at /zamovlennia/pidtverdzhennia/[orderNumber]</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| submitCheckoutAction | Server owns price via snapshots; client cannot set totals |
| Confirmation page | orderNumber + userId composite auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering | Double-sale | mitigate | updateMany AVAILABLE in $transaction |
| T-03-04-02 | Elevation | Order confirmation IDOR | mitigate | where userId + orderNumber |
| T-03-04-03 | Tampering | Price manipulation | mitigate | priceSnapshot from DB product.price only |
| T-03-04-04 | Repudiation | Empty checkout | mitigate | redirect /koszyk if no lines |

</threat_model>

<verification>
npm test -- src/server/services/order.service.test.ts
test -f src/app/(storefront)/zamovlennia/pidtverdzhennia/[orderNumber]/page.tsx
</verification>

<success_criteria>
Checkout creates order, locks inventory, clears cart, shows confirmation without online payment
</success_criteria>

<output>
Create `.planning/phases/03-cart-checkout/03-04-SUMMARY.md` when done
</output>
