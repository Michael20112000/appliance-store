---
phase: 15-storefront-catalog-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - src/components/layout/store-header.tsx
  - src/components/home/category-grid.tsx
  - src/components/layout/store-mobile-nav.tsx
  - src/components/catalog/catalog-filters.tsx
autonomous: true
requirements:
  - CAT-04
  - CAT-05
context_decisions:
  - "D-15-02: header first 4 non-empty by sortOrder"
  - "D-15-03: homepage hide empty cards"
  - "D-15-04: sidebar hide productCount 0"
  - "D-15-05: mobile same as sidebar"
  - "D-15-06..D-15-09: Badge secondary inline"

must_haves:
  truths:
    - "Header shows at most 4 category links and each has available products"
    - "Homepage category section omits zero-count categories"
    - "Catalog sidebar uses Badge variant secondary not em dash format"
    - "Empty categories absent from sidebar and mobile nav lists"
  artifacts:
    - path: src/components/catalog/catalog-filters.tsx
      provides: Badge category labels
    - path: src/components/layout/store-header.tsx
      provides: filtered nav categories

---

<objective>
Hide empty categories across storefront navigation and replace count labels with shadcn Badge (CAT-04, CAT-05).

Purpose: Buyers only see categories with stock; counts match admin-style badge presentation.
Output: Updated header, homepage grid, mobile nav, catalog sidebar.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-storefront-catalog-polish/15-CONTEXT.md
@.planning/phases/15-storefront-catalog-polish/15-RESEARCH.md
@.planning/phases/15-storefront-catalog-polish/15-UI-SPEC.md
@.planning/phases/15-storefront-catalog-polish/15-PATTERNS.md
@src/components/layout/store-header.tsx
@src/components/home/category-grid.tsx
@src/components/layout/store-mobile-nav.tsx
@src/components/catalog/catalog-filters.tsx
@src/components/ui/badge.tsx
@src/server/services/catalog.service.ts
</context>

<threat_model>
| ID | Threat | Disposition | Mitigation |
|----|--------|-------------|------------|
| T-15-02-01 | Stale counts in nav | accept | Server components refetch per request |
| T-15-SC | N/A | accept | No new packages |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Filter empty categories in header, homepage, mobile (D-15-02..D-15-05)</name>
  <read_first>
    - src/components/layout/store-header.tsx — prisma.category.findMany + slice(0,4)
    - src/components/home/category-grid.tsx — all categories rendered
    - src/components/layout/store-mobile-nav.tsx — categories prop
    - src/server/services/catalog.service.ts — listCategoriesWithProductCounts, categoriesWithAvailableProducts
    - .planning/phases/15-storefront-catalog-polish/15-CONTEXT.md — D-15-02 through D-15-05
  </read_first>
  <files>src/components/layout/store-header.tsx, src/components/home/category-grid.tsx, src/components/layout/store-mobile-nav.tsx</files>
  <action>
    **store-header.tsx (D-15-02):** Replace `prisma.category.findMany` with `listCategoriesWithProductCounts()`. Apply `categoriesWithAvailableProducts` on `categories` array. Take `.slice(0, 4)` AFTER filter (no backfill). Pass filtered list to `StoreMobileNav`.

    **category-grid.tsx (D-15-03):** Use `listCategoriesWithProductCounts()` + filter helper. If filtered list length is 0, return `null` (no section). Otherwise map cards as today (preserve image/alt logic).

    **store-mobile-nav.tsx (D-15-05):** Accept only non-empty categories from parent (no separate Prisma fetch). Extend type with optional `productCount?: number` for Task 2 badges.

    Preserve `sortOrder` ordering from service (already asc via listCategories).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `store-header.tsx` does not import prisma for categories
    - Header maps at most 4 links from non-empty categories only
    - `CategoryGrid` returns null when all categories empty
    - `StoreMobileNav` lists same filtered set as header receives
    - `npm run build` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Badge labels in catalog sidebar and nav (D-15-06..D-15-09)</name>
  <read_first>
    - src/components/catalog/catalog-filters.tsx — formatCategoryLabel
    - src/components/ui/badge.tsx — variant secondary
    - .planning/phases/15-storefront-catalog-polish/15-UI-SPEC.md — label layout
    - src/components/layout/store-header.tsx — nav link markup after Task 1
  </read_first>
  <files>src/components/catalog/catalog-filters.tsx, src/components/layout/store-header.tsx, src/components/layout/store-mobile-nav.tsx</files>
  <action>
  **catalog-filters.tsx:**
  - Delete `formatCategoryLabel`.
  - Import `Badge` from `@/components/ui/badge`.
  - Filter `categories` prop with `categoriesWithAvailableProducts` before `.map` (D-15-04). Keep «Усі товари» row always.
  - Render: `span className="flex items-center gap-2"` with name + `<Badge variant="secondary">{count}</Badge>` for «Усі товари» and each category.

  **store-header.tsx + store-mobile-nav.tsx (D-15-09):** Add Badge with `productCount` next to category name in links when count is defined.

  Do not change filter reset behavior (`storinka: 1` on filter change).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -15 && rg "formatCategoryLabel" src/components/catalog/catalog-filters.tsx; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - No `formatCategoryLabel` in catalog-filters.tsx
    - Sidebar category rows with productCount 0 not rendered
    - «Усі товари» shows Badge with totalProductCount
    - Badge uses variant="secondary"
    - `npm run build` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: E2E — empty category hidden from browse surfaces</name>
  <read_first>
    - e2e/public-browse.spec.ts — homepage category links pattern
    - e2e/helpers/catalog.ts — if exists
    - .planning/phases/15-storefront-catalog-polish/15-RESEARCH.md — Validation Architecture
  </read_first>
  <files>e2e/public-browse.spec.ts</files>
  <action>
    Extend or add test in `e2e/public-browse.spec.ts`:
    - Visit `/katalog`
    - Assert sidebar does not use em dash count pattern (no `/ — \d/` in category link text)
    - Assert at least one `badge` visible in category section of filters (use accessible role or data-slot if needed)

    If seed guarantees a known empty category slug, assert its name absent from `#kategorii` on `/` and from catalog sidebar. If no empty category in seed, document skip in test comment and still assert badge pattern.
  </action>
  <verify>
    <automated>npx playwright test e2e/public-browse.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Playwright test passes locally
    - Test asserts Badge-based counts (not «— N» format) on catalog page
  </acceptance_criteria>
</task>

</tasks>

## PLAN COMPLETE
