---
phase: 15-storefront-catalog-polish
plan: 03
type: execute
wave: 3
depends_on:
  - 15-01
files_modified:
  - src/components/admin/admin-list-pagination.tsx
  - src/components/catalog/catalog-list-pagination.tsx
  - src/lib/catalog/catalog-url.ts
  - src/lib/catalog/catalog-url.test.ts
  - src/app/(storefront)/katalog/page.tsx
  - src/app/(storefront)/katalog/[slug]/page.tsx
  - e2e/catalog-pagination.spec.ts
autonomous: true
requirements:
  - CAT-06
context_decisions:
  - "D-15-10: pageSize 16"
  - "D-15-11: reuse AdminListPagination"
  - "D-15-12: pagination below grid"
  - "D-15-13: hide when totalPages <= 1"
  - "D-15-14: clamp OOB storinka via nuqs"
  - "D-15-15: keep storinka key unchanged"
  - "D-15-16: empty category 200"

must_haves:
  truths:
    - "Catalog pages request 16 products per page"
    - "Pagination UI matches admin list pattern and sits below ProductGrid"
    - "URL storinka clamps to valid range without 404"
    - "Pagination hidden when only one page"
  artifacts:
    - path: src/components/catalog/catalog-list-pagination.tsx
      provides: storefront pagination + clamp
    - path: src/lib/catalog/catalog-url.ts
      provides: href builder preserving filters

---

<objective>
Wire 16-item catalog pagination with admin-style UI and nuqs page clamp (CAT-06, D-15-10..D-15-16).

Purpose: Large catalogs are browsable; URL state stays valid.
Output: `CatalogListPagination`, catalog URL helper, updated katalog pages, e2e coverage.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-storefront-catalog-polish/15-CONTEXT.md
@.planning/phases/15-storefront-catalog-polish/15-RESEARCH.md
@.planning/phases/15-storefront-catalog-polish/15-UI-SPEC.md
@.planning/phases/15-storefront-catalog-polish/15-PATTERNS.md
@src/components/admin/admin-list-pagination.tsx
@src/components/admin/products-list-pagination.tsx
@src/lib/catalog/search-params.ts
@src/lib/pagination.ts
@src/app/(storefront)/katalog/page.tsx
@src/app/(storefront)/katalog/[slug]/page.tsx
</context>

<threat_model>
| ID | Threat | Disposition | Mitigation |
|----|--------|-------------|------------|
| T-15-03-01 | Open redirect via pagination href | mitigate | `catalogPageHref` only mutates known catalog query keys on same pathname |
| T-15-03-02 | Hydration mismatch on page | mitigate | Server uses clamped page for fetch; client syncs URL with replace |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: AdminListPagination showPageSizeSelector + catalog URL helper</name>
  <read_first>
    - src/components/admin/admin-list-pagination.tsx — full component
    - src/lib/admin/products-url.ts — hrefFor pattern
    - src/lib/catalog/search-params.ts — catalogParsers, catalogUrlKeys
    - .planning/phases/15-storefront-catalog-polish/15-CONTEXT.md — D-15-11, D-15-15
  </read_first>
  <files>src/components/admin/admin-list-pagination.tsx, src/lib/catalog/catalog-url.ts, src/lib/catalog/catalog-url.test.ts</files>
  <action>
    **admin-list-pagination.tsx (D-15-11):** Add optional prop `showPageSizeSelector?: boolean` default `true`. When `false`, do not render the «Рядків на сторінці» block; keep page indicator + Pagination controls unchanged. Verify admin `ProductsListPagination` still works (default true).

    **catalog-url.ts:** Export `catalogPageHref(pathname: string, current: CatalogUrlParams, next: { page: number }): string` using `catalogParsers` + `catalogUrlKeys` via nuqs `createSerializer` or manual encoding — preserve `q`, `brend`, `cinaVid`, `cinaDo`, `stan`, `sort`; set `storinka` to `next.page` (omit when 1 if that matches existing URL style in project).

    Add Vitest: href includes `сторінка=2` when page 2; preserves `sort` query param.
  </action>
  <verify>
    <automated>npm run test -- src/lib/catalog/catalog-url.test.ts && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `showPageSizeSelector` defaults true; admin pages unchanged visually
    - `catalogPageHref` tested for page + sort preservation
    - `npm run build` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: CatalogListPagination client component</name>
  <read_first>
    - src/components/catalog/catalog-brand-param-guard.tsx — nuqs replace pattern
    - src/components/admin/admin-list-pagination.tsx — after Task 1
    - src/lib/pagination.ts — computeTotalPages
    - src/server/services/catalog.service.ts — CATALOG_PAGE_SIZE
    - .planning/phases/15-storefront-catalog-polish/15-CONTEXT.md — D-15-12..D-15-14
  </read_first>
  <files>src/components/catalog/catalog-list-pagination.tsx</files>
  <action>
    Create `"use client"` `CatalogListPagination` props:
    - `total: number`
    - `page: number` (already clamped from server)
    - `totalPages: number`

    Internally use `usePathname()`, `useQueryStates(catalogParsers, { urlKeys: catalogUrlKeys, shallow: false })`.

    **Clamp (D-15-14):** `useEffect` — if `params.storinka !== page` (server clamped value passed as prop), `setParams({ storinka: page }, { history: 'replace' })`.

    **Render (D-15-12, D-15-13):** If `totalPages <= 1`, return `null`. Else render `AdminListPagination` with `showPageSizeSelector={false}`, `pageSize={16 as AdminPageSize}` (fixed literal matching CATALOG_PAGE_SIZE), `hrefFor` calling `catalogPageHref(pathname, params, { page })`.

    Note: `pageSize` prop type is `AdminPageSize` — use `20` only as type satisfy if needed OR widen AdminListPagination pageSize to `number` for display-only when selector hidden; prefer minimal type change documented in summary.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `catalog-list-pagination.tsx` exists and is client component
    - Returns null when totalPages <= 1
    - Uses AdminListPagination with showPageSizeSelector false
    - `npm run build` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Wire katalog pages (D-15-10, D-15-16)</name>
  <read_first>
    - src/app/(storefront)/katalog/page.tsx
    - src/app/(storefront)/katalog/[slug]/page.tsx
    - src/server/services/catalog.service.ts — CATALOG_PAGE_SIZE
    - .planning/phases/15-storefront-catalog-polish/15-CONTEXT.md — D-15-16
  </read_first>
  <files>src/app/(storefront)/katalog/page.tsx, src/app/(storefront)/katalog/[slug]/page.tsx</files>
  <action>
    Both pages:
    - Import `CATALOG_PAGE_SIZE`, `computeTotalPages` from `@/lib/pagination`
    - After parsing searchParams, compute `totalPages` from a preliminary count OR from `result.total` after fetch
    - `const safePage = Math.min(Math.max(1, parsed.storinka), totalPages)`
    - Pass `page: safePage`, `pageSize: CATALOG_PAGE_SIZE` to `listPublicProducts`
    - Render `<CatalogListPagination total={result.total} page={safePage} totalPages={totalPages} />` **below** `<ProductGrid>` inside main column (D-15-12)

    **D-15-16:** Do not add `notFound()` when `result.total === 0` for valid category — keep existing empty JSX.

    Remove hardcoded `pageSize: 24`.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Both katalog pages use CATALOG_PAGE_SIZE 16
    - CatalogListPagination rendered after ProductGrid
    - Category with zero products still returns 200 (no new notFound for empty stock)
    - `npm run build` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: E2E catalog pagination</name>
  <read_first>
    - e2e/catalog-filters-url.spec.ts — nuqs URL patterns
    - e2e/helpers/catalog.ts
    - .planning/phases/15-storefront-catalog-polish/15-VALIDATION.md
  </read_first>
  <files>e2e/catalog-pagination.spec.ts</files>
  <action>
    Create `e2e/catalog-pagination.spec.ts`:

    1. `test('catalog shows pagination when more than 16 products', ...)` — goto `/katalog`, if product count > 16 expect pagination «Наступна» visible; else test.skip with reason.

    2. `test('сторінка query updates product grid', ...)` — goto `/katalog?сторінка=2` when pagination exists; expect URL contains storinka; grid content changes or page indicator shows 2.

    3. `test('OOB storinka clamps', ...)` — goto `/katalog?сторінка=9999`; expect URL or page indicator reflects clamped page (not 9999 in active link).

    Use Ukrainian URL key `сторінка` per catalogUrlKeys.
  </action>
  <verify>
    <automated>npx playwright test e2e/catalog-pagination.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - New spec file passes or skips gracefully when seed has few products
    - At least one test validates `сторінка` param handling
  </acceptance_criteria>
</task>

</tasks>

## PLAN COMPLETE
