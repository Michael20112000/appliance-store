---
phase: 15-storefront-catalog-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/services/catalog.service.ts
  - src/server/services/catalog.service.test.ts
  - src/app/sitemap.ts
autonomous: true
requirements:
  - CAT-04
context_decisions:
  - "D-15-01: buildCatalogContextWhere single source"
  - "D-15-10: CATALOG_PAGE_SIZE 16"

must_haves:
  truths:
    - "CATALOG_PAGE_SIZE export equals 16"
    - "categoriesWithAvailableProducts filters productCount === 0"
    - "Sitemap omits category URLs with zero available products when hook applied"
  artifacts:
    - path: src/server/services/catalog.service.ts
      provides: CATALOG_PAGE_SIZE and filter helper
  key_links:
    - from: listCategoriesWithProductCounts
      to: buildCatalogContextWhere
      pattern: "getCatalogCategoryCounts"

---

<objective>
Add shared catalog constants and `categoriesWithAvailableProducts` helper (D-15-01, D-15-10 foundation).

Purpose: Unblocks navigation filtering and pagination plans without duplicating availability logic.
Output: Exported `CATALOG_PAGE_SIZE = 16`, filter helper, unit tests; optional sitemap filter per discretion.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-storefront-catalog-polish/15-CONTEXT.md
@.planning/phases/15-storefront-catalog-polish/15-RESEARCH.md
@.planning/phases/15-storefront-catalog-polish/15-UI-SPEC.md
@.planning/phases/15-storefront-catalog-polish/15-PATTERNS.md
@src/server/services/catalog.service.ts
@src/app/sitemap.ts
</context>

<threat_model>
| ID | Threat | Disposition | Mitigation |
|----|--------|-------------|------------|
| T-15-01-01 | Wrong availability filter | mitigate | Reuse `buildCatalogContextWhere` only; no new Prisma where clauses |
| T-15-SC | N/A | accept | No new dependencies |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Export CATALOG_PAGE_SIZE and filter helper</name>
  <read_first>
    - src/server/services/catalog.service.ts — DEFAULT_PAGE_SIZE, buildCatalogContextWhere, listCategoriesWithProductCounts
    - .planning/phases/15-storefront-catalog-polish/15-CONTEXT.md — D-15-01, D-15-10
    - src/server/services/catalog.service.test.ts — existing describe blocks
  </read_first>
  <files>src/server/services/catalog.service.ts, src/server/services/catalog.service.test.ts</files>
  <action>
    Per D-15-10: export `CATALOG_PAGE_SIZE = 16` as a named constant (keep `DEFAULT_PAGE_SIZE = 24` unchanged for non-storefront callers).

    Add and export:
    `categoriesWithAvailableProducts<T extends { productCount: number }>(categories: T[]): T[]`
    returning only items where `productCount > 0`.

    Add Vitest cases:
    - `CATALOG_PAGE_SIZE` is 16
    - helper removes zero-count rows and preserves order of non-zero rows
  </action>
  <verify>
    <automated>npm run test -- src/server/services/catalog.service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `catalog.service.ts` exports `CATALOG_PAGE_SIZE` with value 16
    - `categoriesWithAvailableProducts` exported and tested
    - `npm run test -- src/server/services/catalog.service.test.ts` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Sitemap non-empty categories (discretion)</name>
  <read_first>
    - src/app/sitemap.ts — listCategories usage
    - src/server/services/catalog.service.ts — listCategoriesWithProductCounts
    - .planning/phases/15-storefront-catalog-polish/15-CONTEXT.md — Claude's Discretion sitemap bullet
  </read_first>
  <files>src/app/sitemap.ts</files>
  <action>
    Replace `listCategories()` with `listCategoriesWithProductCounts()` in sitemap.ts.
    Map only categories where `productCount > 0` to `/katalog/${slug}` URLs (use `categoriesWithAvailableProducts`).

    Do not change product slug or static route logic.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `sitemap.ts` imports count-aware listing + filter helper
    - Category routes excluded when productCount is 0
    - `npm run build` exits 0
  </acceptance_criteria>
</task>

</tasks>

## PLAN COMPLETE
