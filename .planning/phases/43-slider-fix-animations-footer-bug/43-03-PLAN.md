---
phase: 43-slider-fix-animations-footer-bug
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/(storefront)/layout.tsx
autonomous: false
requirements:
  - ANIM-01

must_haves:
  truths:
    - "Navigating between storefront pages shows a 150ms opacity fade-in on the page content"
    - "The fade animation does not play on admin pages"
    - "With prefers-reduced-motion enabled, no visible animation occurs (duration is 0s)"
    - "The .page-transition CSS class exists in globals.css with 0.15s ease-out animation"
  artifacts:
    - path: "src/app/globals.css"
      provides: "@keyframes page-fade-in, .page-transition class, prefers-reduced-motion override"
      contains: "@keyframes page-fade-in"
    - path: "src/app/(storefront)/layout.tsx"
      provides: "div.page-transition wrapper around NuqsAdapter and children"
      contains: "page-transition"
  key_links:
    - from: "src/app/(storefront)/layout.tsx"
      to: "src/app/globals.css"
      via: "className=\"page-transition\" on div element"
      pattern: "page-transition"
---

<objective>
Add a subtle 150ms fade-in page transition to the storefront, scoped to the storefront layout so admin pages are unaffected. Respect prefers-reduced-motion.

Purpose: ANIM-01 — non-intrusive entrance animation on storefront navigation.
Output: globals.css updated with keyframes + class; storefront layout wraps children in div.page-transition; visual checkpoint confirms the animation and reduced-motion behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/43-slider-fix-animations-footer-bug/43-RESEARCH.md
@.planning/phases/43-slider-fix-animations-footer-bug/43-PATTERNS.md
</context>

<interfaces>
From src/app/globals.css (end of file, lines 131-143 — append after this):
  html:has(#main-content) { scroll-behavior: smooth; }
  @media (prefers-reduced-motion: reduce) { html:has(#main-content) { scroll-behavior: auto; } }
  #kategorii { scroll-margin-top: 4.5rem; }

From src/app/(storefront)/layout.tsx (current main block, lines 28-39):
  <main id="main-content" className="flex-1">
    <NuqsAdapter>
      <Suspense fallback={null}>
        <ChatProviderGate>
          <CartPendingMergeGate />
          <WishlistPendingMergeGate />
          {children}
          <Analytics />
        </ChatProviderGate>
      </Suspense>
    </NuqsAdapter>
  </main>
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add page-fade-in keyframes and .page-transition class to globals.css</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (full file — understand current end-of-file content and existing prefers-reduced-motion pattern at lines 135-139)
  </read_first>
  <action>
    Read src/app/globals.css in full before editing. Append three CSS blocks after the existing `#kategorii` block (current end of file, line 142):

    1. `@keyframes page-fade-in` with `from { opacity: 0; }` and `to { opacity: 1; }`
    2. `.page-transition { animation: page-fade-in 0.15s ease-out both; }`
    3. A `@media (prefers-reduced-motion: reduce)` block targeting `.page-transition` with `animation-duration: 0s !important;` and `animation-delay: 0s !important;`

    These three blocks must appear at the end of the file after the existing content. Do not modify any existing CSS rules. The reduced-motion block follows the same pattern as the existing `html:has(#main-content)` reduced-motion block already in the file.

    The `both` fill-mode in the `.page-transition` rule ensures the element starts at opacity 0 before the animation begins (prevents flash of content before the animation fires on navigation).
  </action>
  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && grep -c "page-fade-in" src/app/globals.css && grep -c "page-transition" src/app/globals.css && grep -c "prefers-reduced-motion" src/app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - `@keyframes page-fade-in` block is present in globals.css
    - `.page-transition` rule with `animation: page-fade-in 0.15s ease-out both` is present
    - A `@media (prefers-reduced-motion: reduce)` block targeting `.page-transition` with `animation-duration: 0s !important` is present
    - `grep -c "page-fade-in" src/app/globals.css` outputs 2 (appears in @keyframes declaration and in .page-transition rule)
    - `grep -c "page-transition" src/app/globals.css` outputs at least 2 (class definition + media query override)
    - `grep -c "prefers-reduced-motion" src/app/globals.css` outputs 2 (existing + new block)
    - All pre-existing CSS rules are unchanged (existing content at lines 1-143 is identical)
  </acceptance_criteria>
  <done>globals.css contains the page-fade-in keyframe, .page-transition class, and prefers-reduced-motion override; no existing CSS modified</done>
</task>

<task type="auto">
  <name>Task 2: Wrap storefront children with div.page-transition in layout</name>
  <files>src/app/(storefront)/layout.tsx</files>
  <read_first>
    - src/app/(storefront)/layout.tsx (full file — understand current main block structure and all children)
  </read_first>
  <action>
    Read src/app/(storefront)/layout.tsx in full before editing.

    Inside the `<main id="main-content" className="flex-1">` element, wrap the `<NuqsAdapter>` and all its descendants with a single `<div className="page-transition">`. The resulting structure must be:

      `<main id="main-content" className="flex-1">`
        `<div className="page-transition">`
          `<NuqsAdapter>`
            `<Suspense fallback={null}>`
              `<ChatProviderGate>`
                `<CartPendingMergeGate />`
                `<WishlistPendingMergeGate />`
                `{children}`
                `<Analytics />`
              `</ChatProviderGate>`
            `</Suspense>`
          `</NuqsAdapter>`
        `</div>`
      `</main>`

    No `"use client"` directive is needed — the layout is already an RSC and a plain `<div>` is legal in RSC. No new imports are required; the `page-transition` class is defined in globals.css which is already imported at the root layout level.

    The `<StoreFooter />`, `<Toaster />`, and `<StorefrontFabs />` elements outside `<main>` must remain unchanged.
  </action>
  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && grep -c "page-transition" src/app/\(storefront\)/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "page-transition" src/app/(storefront)/layout.tsx` outputs 1 (exactly one div with the class)
    - The `<div className="page-transition">` is a direct child of `<main id="main-content">`
    - `<NuqsAdapter>` is a child of `<div className="page-transition">` (not a sibling)
    - `<StoreFooter />`, `<Toaster />`, `<StorefrontFabs />` are outside the `<div className="page-transition">` (unchanged positions)
    - No `"use client"` directive added to the file
    - No new imports added to the file
  </acceptance_criteria>
  <done>layout.tsx has exactly one div.page-transition wrapping the NuqsAdapter block; all other elements in the layout are unchanged</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    CSS page-fade-in animation class added to globals.css and applied to the storefront layout wrapper div. The animation fires on every storefront page navigation (150ms opacity 0→1). Admin pages are unaffected because the wrapper div is only in (storefront)/layout.tsx. The reduced-motion CSS override sets duration to 0s.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev` — open http://localhost:3000
    2. Navigate between two catalog pages (e.g., home → /katalog → a product page → back to /katalog). Observe a subtle 150ms fade-in on the new page content.
    3. Navigate to an admin page (e.g., http://localhost:3000/admin). Confirm NO fade animation occurs on admin page navigations.
    4. In Chrome DevTools → Rendering tab → enable "Emulate CSS media feature: prefers-reduced-motion: reduce". Navigate between storefront pages. Confirm the fade animation does NOT visually play (content appears instantly).
    5. Disable the emulation and confirm the fade animation returns.
  </how-to-verify>
  <resume-signal>Type "approved" if the animation is correct, or describe any issue (e.g., "admin also fades", "animation too slow", "no animation visible")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS class application | The .page-transition class is applied by layout RSC; no user-controlled input affects it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-03 | Information Disclosure | CSS animation | accept | CSS-only animation; no data exposure; animation duration is cosmetic |
| T-43-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After Tasks 1 and 2 (before checkpoint):

  grep -c "page-fade-in" src/app/globals.css
  # Expected: 2

  grep -c "page-transition" src/app/globals.css
  # Expected: at least 2

  grep -c "page-transition" "src/app/(storefront)/layout.tsx"
  # Expected: 1

After visual checkpoint approval: ANIM-01 complete.
</verification>

<success_criteria>
- globals.css contains @keyframes page-fade-in, .page-transition rule with 0.15s ease-out both, and prefers-reduced-motion override with 0s duration
- layout.tsx has one div.page-transition wrapping NuqsAdapter block
- Visual verification: storefront pages fade in; admin pages do not; reduced-motion disables the animation
</success_criteria>

<output>
Create `.planning/phases/43-slider-fix-animations-footer-bug/43-03-SUMMARY.md` when done
</output>
