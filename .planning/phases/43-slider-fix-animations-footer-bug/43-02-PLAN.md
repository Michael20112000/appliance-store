---
phase: 43-slider-fix-animations-footer-bug
plan: 02
type: execute
wave: 2
depends_on:
  - "43-01"
files_modified:
  - src/components/catalog/catalog-filters.tsx
  - src/lib/catalog/store-map.ts
autonomous: true
requirements:
  - SLIDER-01
  - BUG-25

must_haves:
  truths:
    - "The price slider steps in 50 UAH increments regardless of whether catalog minUah/maxUah are multiples of 50"
    - "The slider handles can reach both the real catalog minimum and maximum prices"
    - "The footer address link opens a navigable Google Maps URL, not an embed URL"
    - "All normalizeSliderBounds unit tests pass (GREEN)"
    - "All addressExternalMapUrl unit tests pass (GREEN)"
  artifacts:
    - path: "src/components/catalog/catalog-filters.tsx"
      provides: "normalizeSliderBounds exported helper + Slider min/max using sliderMin/sliderMax"
      exports: ["normalizeSliderBounds", "PRICE_STEP_UAH", "PriceBounds", "CatalogFiltersPanel", "CatalogFilters"]
    - path: "src/lib/catalog/store-map.ts"
      provides: "isEmbedMapUrl guard in addressExternalMapUrl; addressMapEmbedSrc unchanged"
      exports: ["addressExternalMapUrl", "addressMapEmbedSrc"]
  key_links:
    - from: "src/components/catalog/catalog-filters.tsx"
      to: "src/components/catalog/catalog-filters.test.ts"
      via: "export normalizeSliderBounds"
      pattern: "export function normalizeSliderBounds"
    - from: "src/lib/catalog/store-map.ts"
      to: "src/lib/catalog/store-map.test.ts"
      via: "isEmbedMapUrl guard in addressExternalMapUrl"
      pattern: "isEmbedMapUrl"
---

<objective>
Turn the RED tests from Plan 01 GREEN by implementing the SLIDER-01 bounds normalization and the BUG-25 embed URL detection fixes.

Purpose: Fix the two reported bugs — slider thumbs that could never reach the real catalog max price, and footer address links that opened an embed iframe instead of navigable Google Maps.
Output: Two modified source files; both test suites green; full vitest suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/43-slider-fix-animations-footer-bug/43-RESEARCH.md
@.planning/phases/43-slider-fix-animations-footer-bug/43-PATTERNS.md
@.planning/phases/43-slider-fix-animations-footer-bug/43-01-SUMMARY.md
</context>

<interfaces>
From src/components/catalog/catalog-filters.tsx (current — read before editing):
  export const PRICE_STEP_UAH = 50;
  export type PriceBounds = { minUah: number; maxUah: number };
  // Pure helper functions at module scope (lines 43-84): snapPriceToStep, clampPrice, normalizeThumbRange, thumbValuesFromParams, toSliderValues
  // Slider props at lines 260-283:
  //   min={bounds.minUah}   ← CHANGE to sliderMin
  //   max={bounds.maxUah}   ← CHANGE to sliderMax
  // syncPriceToUrl atMin/atMax at lines 132-133 — DO NOT CHANGE:
  //   const atMin = cinaVid <= bounds.minUah;
  //   const atMax = cinaDo >= bounds.maxUah;
  // Input min/max at lines 287-297 — DO NOT CHANGE (uses bounds.minUah/bounds.maxUah for HTML validation)

From src/lib/catalog/store-map.ts (current — read before editing):
  export function addressExternalMapUrl(address: PublicStoreAddress): string {
    if (address.mapUrl) return address.mapUrl;  // ← BUG: line 4
    ...
  }
  export function addressMapEmbedSrc(address: PublicStoreAddress): string | null { ... }  // DO NOT CHANGE
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add normalizeSliderBounds and fix Slider min/max props (SLIDER-01 GREEN)</name>
  <files>src/components/catalog/catalog-filters.tsx</files>
  <read_first>
    - src/components/catalog/catalog-filters.tsx (full file — understand existing helper style at lines 43-84, Slider props at lines 260-283, syncPriceToUrl at lines 128-148)
    - src/components/catalog/catalog-filters.test.ts (tests from Plan 01 — understand exactly what normalizeSliderBounds must return)
  </read_first>
  <behavior>
    - normalizeSliderBounds({ minUah: 1273, maxUah: 7890 }) returns { sliderMin: 1250, sliderMax: 7900 }
    - normalizeSliderBounds({ minUah: 1000, maxUah: 5000 }) returns { sliderMin: 1000, sliderMax: 5000 }
    - normalizeSliderBounds({ minUah: 1000, maxUah: 1000 }) returns { sliderMin: 1000, sliderMax: 1050 }
    - Slider min prop uses sliderMin; Slider max prop uses sliderMax
    - syncPriceToUrl atMin/atMax comparisons still use bounds.minUah/bounds.maxUah (unchanged)
    - Input elements still use bounds.minUah/bounds.maxUah for HTML min/max attributes (unchanged)
  </behavior>
  <action>
    Read src/components/catalog/catalog-filters.tsx in full before making any edits.

    Add `normalizeSliderBounds` as a pure module-scope function immediately after the existing `normalizeThumbRange` function (after line 68), following the same style as the existing helpers — no class wrappers, no side effects. Mark it `export` so the test file can import it. Use PRICE_STEP_UAH (already defined) for the step constant. Include the equal-bounds guard: `if (sliderMin === sliderMax) sliderMax = sliderMin + PRICE_STEP_UAH`.

    Inside the `CatalogFiltersPanel` component function, derive `sliderMin` and `sliderMax` from `normalizeSliderBounds(bounds)` immediately before the return statement's JSX section (after the `thumbValues` useMemo block is a natural place — keep it near the Slider usage). Pass `sliderMin` to `min` prop and `sliderMax` to `max` prop of the `<Slider>` component.

    Leave untouched: `syncPriceToUrl`'s `atMin`/`atMax` comparisons (lines 132-133), both `<input type="number">` min/max/step attributes (lines 287-297), and the `normalizeThumbRange` / `thumbValuesFromParams` functions.

    After editing, run tests to confirm GREEN state.
  </action>
  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npx vitest run src/components/catalog/catalog-filters.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `export function normalizeSliderBounds` appears in src/components/catalog/catalog-filters.tsx
    - The function body contains `Math.floor(bounds.minUah / PRICE_STEP_UAH) * PRICE_STEP_UAH` for sliderMin
    - The function body contains `Math.ceil(bounds.maxUah / PRICE_STEP_UAH) * PRICE_STEP_UAH` for sliderMax
    - The function body contains the equal-bounds guard (`if (sliderMin === sliderMax)`)
    - The `<Slider>` element uses `min={sliderMin}` and `max={sliderMax}` (not `min={bounds.minUah}`)
    - `syncPriceToUrl` still references `bounds.minUah` and `bounds.maxUah` (not sliderMin/sliderMax)
    - `<input type="number">` elements still have `min={bounds.minUah}` and `max={bounds.maxUah}`
    - `npx vitest run src/components/catalog/catalog-filters.test.ts` passes all 4 tests (exit 0)
  </acceptance_criteria>
  <done>All 4 normalizeSliderBounds tests pass; catalog-filters.tsx compiles without TypeScript errors; Slider min/max now use the 50-aligned bounds</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix addressExternalMapUrl embed URL detection (BUG-25 GREEN)</name>
  <files>src/lib/catalog/store-map.ts</files>
  <read_first>
    - src/lib/catalog/store-map.ts (full file — understand both exported functions and which one to NOT change)
    - src/lib/catalog/store-map.test.ts (tests from Plan 01 — understand exactly which URL patterns must be detected as embed)
  </read_first>
  <behavior>
    - addressExternalMapUrl with mapUrl="https://maps.google.com/maps?q=Kyiv&output=embed" falls through to text-search URL (does not return the embed URL)
    - addressExternalMapUrl with mapUrl="https://www.google.com/maps/embed?pb=abc123" falls through to text-search URL
    - addressExternalMapUrl with mapUrl="https://maps.apple.com/?q=Kyiv" returns "https://maps.apple.com/?q=Kyiv" unchanged
    - addressExternalMapUrl with embed mapUrl + lat/lng returns "https://www.google.com/maps/search/?api=1&query=50.45,30.52"
    - addressExternalMapUrl with no mapUrl returns URL containing encodeURIComponent of address.text
    - addressMapEmbedSrc is completely unchanged
  </behavior>
  <action>
    Read src/lib/catalog/store-map.ts in full before making any edits.

    Add `isEmbedMapUrl(url: string): boolean` as a private (non-exported) module-scope function above `addressExternalMapUrl`. The function wraps its body in try/catch (returns false on malformed URLs). Inside the try block: parse with `new URL(url)`, return true if `u.searchParams.get("output") === "embed"`, return true if `u.pathname.startsWith("/maps/embed")`, return false otherwise. The catch block returns false.

    Change line 4 of `addressExternalMapUrl` from `if (address.mapUrl) return address.mapUrl;` to `if (address.mapUrl && !isEmbedMapUrl(address.mapUrl)) return address.mapUrl;`.

    The `addressMapEmbedSrc` function (lines 11-27) must remain byte-for-byte identical to its current form.

    After editing, run tests to confirm GREEN state.
  </action>
  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npx vitest run src/lib/catalog/store-map.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `function isEmbedMapUrl(url: string): boolean` is present in store-map.ts (NOT exported)
    - The function contains `u.searchParams.get("output") === "embed"` check
    - The function contains `u.pathname.startsWith("/maps/embed")` check
    - The function has a try/catch returning false on parse error
    - `addressExternalMapUrl` line 1 reads `if (address.mapUrl && !isEmbedMapUrl(address.mapUrl)) return address.mapUrl;`
    - `addressMapEmbedSrc` function body is unchanged (still returns `address.mapUrl` directly as embed src)
    - `npx vitest run src/lib/catalog/store-map.test.ts` passes all 5 tests (exit 0)
  </acceptance_criteria>
  <done>All 5 addressExternalMapUrl tests pass; store-map.ts compiles without TypeScript errors; footer address link will now produce a navigable Google Maps URL instead of an embed URL</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin DB → addressExternalMapUrl | Admin-entered mapUrl string crosses into URL construction; not user-submitted but worth guarding |
| URL parsing in isEmbedMapUrl | new URL(url) called on DB value; malformed URL must not throw |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Spoofing | addressExternalMapUrl | accept | mapUrl is admin-only input (not user-facing); open redirect risk is low; non-embed URLs are returned as-is which is the existing behavior. No change to trust level. |
| T-43-02 | Tampering | isEmbedMapUrl URL parsing | mitigate | try/catch in isEmbedMapUrl ensures malformed URL in DB returns false (falls through to lat/lng or text search); safe fallback |
| T-43-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After both tasks complete:

  npx vitest run src/lib/catalog/store-map.test.ts src/components/catalog/catalog-filters.test.ts

Expected: all 9 tests pass (5 + 4), exit 0.

Full suite:
  npx vitest run

Expected: all tests pass, no regressions.
</verification>

<success_criteria>
- All 4 normalizeSliderBounds tests pass
- All 5 addressExternalMapUrl tests pass
- Full vitest suite passes
- catalog-filters.tsx: `export function normalizeSliderBounds` present; Slider uses sliderMin/sliderMax; syncPriceToUrl and inputs still use bounds.*
- store-map.ts: isEmbedMapUrl private guard present; addressExternalMapUrl guards against embed URLs; addressMapEmbedSrc unchanged
</success_criteria>

<output>
Create `.planning/phases/43-slider-fix-animations-footer-bug/43-02-SUMMARY.md` when done
</output>
