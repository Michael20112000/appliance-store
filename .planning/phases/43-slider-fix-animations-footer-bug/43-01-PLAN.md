---
phase: 43-slider-fix-animations-footer-bug
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/catalog/store-map.test.ts
  - src/components/catalog/catalog-filters.test.ts
autonomous: true
requirements:
  - SLIDER-01
  - BUG-25

must_haves:
  truths:
    - "Running `npx vitest run src/lib/catalog/store-map.test.ts` exits red with 5 failing tests covering addressExternalMapUrl embed detection"
    - "Running `npx vitest run src/components/catalog/catalog-filters.test.ts` exits red with 4 failing tests covering normalizeSliderBounds"
  artifacts:
    - path: "src/lib/catalog/store-map.test.ts"
      provides: "Unit tests for addressExternalMapUrl embed detection (BUG-25)"
      exports: []
    - path: "src/components/catalog/catalog-filters.test.ts"
      provides: "Unit tests for normalizeSliderBounds (SLIDER-01)"
      exports: []
  key_links:
    - from: "src/lib/catalog/store-map.test.ts"
      to: "src/lib/catalog/store-map.ts"
      via: "relative import ./store-map"
      pattern: "from \"./store-map\""
    - from: "src/components/catalog/catalog-filters.test.ts"
      to: "src/components/catalog/catalog-filters.tsx"
      via: "relative import ./catalog-filters"
      pattern: "from \"./catalog-filters\""
---

<objective>
Create the failing test scaffolds (Wave 0 / RED phase) that will drive the BUG-25 and SLIDER-01 implementations in Plan 02.

Purpose: Establish the Nyquist contract — both implementation tasks in Plan 02 have automated verification from the moment they start. Tests must fail now because the production code is not yet fixed.
Output: Two new test files; full suite still passes on unrelated tests; targeted test files exit red.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/43-slider-fix-animations-footer-bug/43-RESEARCH.md
@.planning/phases/43-slider-fix-animations-footer-bug/43-PATTERNS.md
@.planning/phases/43-slider-fix-animations-footer-bug/43-VALIDATION.md
</context>

<interfaces>
From src/lib/catalog/catalog-labels.test.ts (analog — import pattern to replicate):
  import { describe, expect, it } from "vitest";
  import { ... } from "./catalog-labels";
  describe("catalog-labels", () => { it("...", () => { expect(...).toBe(...); }); });

From src/lib/catalog/store-map.ts (current — exports addressExternalMapUrl):
  export function addressExternalMapUrl(address: PublicStoreAddress): string

From src/components/catalog/catalog-filters.tsx (current — PRICE_STEP_UAH is exported; normalizeSliderBounds does NOT exist yet):
  export const PRICE_STEP_UAH = 50;
  export type PriceBounds = { minUah: number; maxUah: number };
  // normalizeSliderBounds is not yet exported — tests will fail on import until Plan 02 adds it
</interfaces>

<tasks>

<task type="tdd">
  <name>Task 1: Write failing tests for addressExternalMapUrl (BUG-25 RED)</name>
  <files>src/lib/catalog/store-map.test.ts</files>
  <read_first>
    - src/lib/catalog/store-map.ts (current implementation — understand addressExternalMapUrl signature and PublicStoreAddress usage)
    - src/lib/catalog/catalog-labels.test.ts (analog — import style, describe/it structure, relative import path)
  </read_first>
  <behavior>
    - Test 1: address with a normal non-embed mapUrl (e.g., "https://maps.apple.com/?q=Kyiv") returns that URL unchanged
    - Test 2: address with embed URL containing output=embed param (e.g., "https://maps.google.com/maps?q=Kyiv&output=embed") does NOT return the embed URL — falls through to text-search URL containing "google.com/maps/search" and "Kyiv"
    - Test 3: address with /maps/embed path URL (e.g., "https://www.google.com/maps/embed?pb=abc123") falls through to text-search URL containing "google.com/maps/search"
    - Test 4: address with embed URL + lat/lng coords (latitude=50.45, longitude=30.52) returns "https://www.google.com/maps/search/?api=1&query=50.45,30.52"
    - Test 5: address with no mapUrl and no coords (text="вул. Хрещатик 1, Київ") returns URL containing encodeURIComponent("вул. Хрещатик 1, Київ")
  </behavior>
  <action>
    Create src/lib/catalog/store-map.test.ts using the same import style as catalog-labels.test.ts: named imports from "vitest" (`describe`, `expect`, `it`), relative import `./store-map`, one `describe("addressExternalMapUrl")` block.

    For the PublicStoreAddress type stub, use `type Addr = Parameters<typeof addressExternalMapUrl>[0]` and cast test objects with `as Addr` — avoids importing the full service type.

    Tests 2–5 MUST FAIL on the current implementation because `addressExternalMapUrl` currently returns `address.mapUrl` unconditionally when set (the bug). Test 1 passes. After writing, run the test command to confirm the red state before committing.

    Do NOT add beforeEach/afterEach — plain it() blocks only, matching the analog pattern.
  </action>
  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npx vitest run src/lib/catalog/store-map.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File src/lib/catalog/store-map.test.ts exists
    - `import { describe, expect, it } from "vitest"` is present (exact named imports, no default)
    - `import { addressExternalMapUrl } from "./store-map"` is present (relative, no @/ alias)
    - `describe("addressExternalMapUrl"` block is present
    - Exactly 5 `it(` calls are present
    - Running `npx vitest run src/lib/catalog/store-map.test.ts` shows at least 4 failing tests (Tests 2–5 fail because current code returns embed URL unchanged)
    - No TypeScript compilation errors (tsc --noEmit passes or executor confirms no red squiggles)
  </acceptance_criteria>
  <done>store-map.test.ts exists with 5 tests; at least 4 fail under current store-map.ts implementation; test output confirms RED state</done>
</task>

<task type="tdd">
  <name>Task 2: Write failing tests for normalizeSliderBounds (SLIDER-01 RED)</name>
  <files>src/components/catalog/catalog-filters.test.ts</files>
  <read_first>
    - src/components/catalog/catalog-filters.tsx (current implementation — confirm PRICE_STEP_UAH is exported at line 25; confirm normalizeSliderBounds does NOT exist yet; confirm PriceBounds type is exported)
    - src/lib/catalog/catalog-labels.test.ts (analog — import style and describe/it structure)
  </read_first>
  <behavior>
    - Test 1: normalizeSliderBounds({ minUah: 1273, maxUah: 7890 }) returns { sliderMin: 1250, sliderMax: 7900 } (floor min, ceil max to 50 grid)
    - Test 2: normalizeSliderBounds({ minUah: 1000, maxUah: 5000 }) returns { sliderMin: 1000, sliderMax: 5000 } (already-aligned bounds unchanged)
    - Test 3: normalizeSliderBounds({ minUah: 1000, maxUah: 1000 }) returns { sliderMin: 1000, sliderMax: 1000 + PRICE_STEP_UAH } (equal bounds edge case: max gets +50)
    - Test 4: normalizeSliderBounds({ minUah: 1273, maxUah: 7890 }) — sliderMin <= 1273 and sliderMax >= 7890 (invariant assertion)
  </behavior>
  <action>
    Create src/components/catalog/catalog-filters.test.ts. Import `normalizeSliderBounds` and `PRICE_STEP_UAH` from `"./catalog-filters"` (relative import). Use same named-import pattern from "vitest" as analog. One `describe("normalizeSliderBounds")` block with 4 `it()` calls.

    All 4 tests MUST FAIL because `normalizeSliderBounds` is not yet exported from catalog-filters.tsx — the import will fail at module resolution, causing all tests to error. Run the test command after creating the file to confirm the red state before committing.

    Do NOT add beforeEach/afterEach. No default imports from vitest.
  </action>
  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npx vitest run src/components/catalog/catalog-filters.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/catalog/catalog-filters.test.ts exists
    - `import { describe, expect, it } from "vitest"` is present
    - `import { normalizeSliderBounds, PRICE_STEP_UAH } from "./catalog-filters"` is present (relative, no @/ alias)
    - `describe("normalizeSliderBounds"` block is present
    - Exactly 4 `it(` calls are present
    - Running `npx vitest run src/components/catalog/catalog-filters.test.ts` exits with non-zero status (all 4 fail — import error because normalizeSliderBounds does not yet exist in catalog-filters.tsx)
  </acceptance_criteria>
  <done>catalog-filters.test.ts exists with 4 tests; all 4 fail because normalizeSliderBounds is not yet exported; RED state confirmed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → source module | Test files import production code; no external I/O |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan — test files use only vitest (already installed) |
</threat_model>

<verification>
Both test files exist and fail in RED state before Plan 02 runs.

Run after this plan completes:
  npx vitest run src/lib/catalog/store-map.test.ts src/components/catalog/catalog-filters.test.ts

Expected: at least 4 failures in store-map.test.ts (Tests 2–5), all 4 failures in catalog-filters.test.ts.
The existing full suite (`npx vitest run`) must not gain any new failures beyond these two files.
</verification>

<success_criteria>
- src/lib/catalog/store-map.test.ts created, 5 tests, at least 4 fail
- src/components/catalog/catalog-filters.test.ts created, 4 tests, all 4 fail
- Existing tests unaffected (full suite minus these two files remains green)
- RED state confirmed by running targeted commands before committing
</success_criteria>

<output>
Create `.planning/phases/43-slider-fix-animations-footer-bug/43-01-SUMMARY.md` when done
</output>
