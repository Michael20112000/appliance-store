---
phase: 52-chat-structural-refactor
plan: 03
type: execute
wave: 1
depends_on:
  - 52-01
files_modified:
  - src/components/ui/drawer.tsx
autonomous: true
requirements:
  - CHAT-12

must_haves:
  truths:
    - "src/components/ui/drawer.tsx exists and exports DrawerRoot, DrawerPortal, DrawerBackdrop, DrawerPopup, DrawerSwipeArea"
    - "Drawer wraps @base-ui/react/drawer primitives in the same pattern as sheet.tsx wraps @base-ui/react/dialog"
    - "DrawerRoot accepts swipeDirection prop (typed via DrawerPrimitive.Root.Props)"
    - "DrawerBackdrop uses data-starting-style and data-ending-style animation classes matching sheet.tsx pattern"
    - "DrawerPopup uses data-starting-style/data-ending-style translate-y animation for bottom-slide motion"
    - "DrawerSwipeArea is positioned at top with h-8 cursor-ns-resize"
  artifacts:
    - path: "src/components/ui/drawer.tsx"
      provides: "Base UI Drawer wrapper component"
      exports:
        - DrawerRoot
        - DrawerPortal
        - DrawerBackdrop
        - DrawerPopup
        - DrawerSwipeArea
        - DrawerClose
        - DrawerTitle
        - DrawerDescription
  key_links:
    - from: "src/components/ui/drawer.tsx"
      to: "@base-ui/react/drawer"
      via: "import { Drawer as DrawerPrimitive }"
      pattern: "from.*@base-ui/react/drawer"
---

<objective>
Create `src/components/ui/drawer.tsx` — a thin wrapper around `@base-ui/react/drawer` primitives, following the exact same structural pattern as `src/components/ui/sheet.tsx`. This new file provides the mobile Drawer component that `chat-panel.tsx` (Plan 04) will import to replace the current `Sheet`-based mobile chat.

Purpose: Implements the UI primitive layer for CHAT-12 (mobile swipe-to-dismiss Drawer). No consumer wiring in this plan — that happens in Plan 04.
Output: `src/components/ui/drawer.tsx` with all exported wrapper functions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/52-chat-structural-refactor/52-RESEARCH.md
@.planning/phases/52-chat-structural-refactor/52-PATTERNS.md
</context>

<interfaces>
From src/components/ui/sheet.tsx (the exact analog — read before creating drawer.tsx):
  - Line 1: `"use client"` directive
  - Line 4: `import { Dialog as SheetPrimitive } from "@base-ui/react/dialog"`
  - Lines 26–37: SheetOverlay uses `data-ending-style:opacity-0 data-starting-style:opacity-0` classes on Backdrop
  - Lines 39–81: SheetContent (Popup) uses `data-ending-style:*` and `data-starting-style:*` Tailwind variants
  - Lines 83–90: SheetHeader as plain div with `data-slot="sheet-header"`
  - Line 129+: Named export block

From @base-ui/react/drawer type definitions (confirmed in PATTERNS.md):
  Available exports from `@base-ui/react/drawer`:
    DrawerRoot (Root), DrawerPortal (Portal), DrawerBackdrop (Backdrop),
    DrawerPopup (Popup), DrawerSwipeArea (SwipeArea), DrawerClose (Close),
    DrawerTitle (Title), DrawerDescription (Description), DrawerViewport (Viewport)
  
  DrawerRoot.Props includes `swipeDirection?: DrawerSwipeDirection`
  DrawerRoot.Props onOpenChange: `(open: boolean, eventDetails: DrawerRoot.ChangeEventDetails) => void`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create src/components/ui/drawer.tsx wrapping @base-ui/react/drawer</name>
  <files>src/components/ui/drawer.tsx</files>

  <read_first>
    - src/components/ui/sheet.tsx — read the entire file (139 lines); this is the exact structural analog; copy the wrapper pattern (data-slot, cn(), function signatures) substituting Dialog→Drawer throughout
    - node_modules/@base-ui/react/drawer/index.parts.d.ts — verify available exported types (DrawerRoot.Props, DrawerPrimitive.Popup.Props, DrawerPrimitive.SwipeArea.Props, etc.) before writing type annotations
    - .planning/phases/52-chat-structural-refactor/52-PATTERNS.md — "src/components/ui/drawer.tsx" section for exact class strings on DrawerBackdrop, DrawerPopup, and DrawerSwipeArea
  </read_first>

  <action>
    Create `src/components/ui/drawer.tsx` as a new file. The file structure mirrors `sheet.tsx` exactly with these substitutions and specifics:

    Directive and imports:
    - Line 1: `"use client"` (required — Base UI components use hooks)
    - Import React: `import * as React from "react"`
    - Import primitive: `import { Drawer as DrawerPrimitive } from "@base-ui/react/drawer"`
    - Import cn: `import { cn } from "@/lib/utils"`
    - No Button or XIcon import (Drawer does not include a built-in close button — that stays in PanelHeader)

    Wrapper functions to create (each follows the `function ComponentName({ ...props }: DrawerPrimitive.Part.Props)` pattern with `data-slot="drawer-*"`):

    DrawerRoot: wraps `DrawerPrimitive.Root`, data-slot="drawer". No className manipulation needed — pass all props through.

    DrawerPortal: wraps `DrawerPrimitive.Portal`, data-slot="drawer-portal". Pass all props through.

    DrawerBackdrop: wraps `DrawerPrimitive.Backdrop`, data-slot="drawer-backdrop". Apply these Tailwind classes via cn():
    `"fixed inset-0 z-50 bg-black/10 transition-opacity duration-150 data-ending-style:opacity-0 data-starting-style:opacity-0 supports-backdrop-filter:backdrop-blur-xs"`
    (matches SheetOverlay classes from sheet.tsx line 31–33 exactly)

    DrawerPopup: wraps `DrawerPrimitive.Popup`, data-slot="drawer-popup". Apply these Tailwind classes via cn():
    `"fixed inset-x-0 bottom-0 z-50 flex flex-col bg-background transition-transform duration-200 ease-in-out data-ending-style:translate-y-[2.5rem] data-ending-style:opacity-0 data-starting-style:translate-y-[2.5rem] data-starting-style:opacity-0"`
    (bottom-drawer animation — always slides up from bottom, no data-[side=bottom] selector needed since a Drawer is always bottom)

    DrawerSwipeArea: wraps `DrawerPrimitive.SwipeArea`, data-slot="drawer-swipe-area". Apply:
    `"absolute inset-x-0 top-0 h-8 cursor-ns-resize"`
    (no sheet.tsx analog — use RESEARCH.md pattern directly; positioned at top of popup, 32px tall drag handle)

    DrawerClose: wraps `DrawerPrimitive.Close`, data-slot="drawer-close". Pass-through only.

    DrawerTitle: wraps `DrawerPrimitive.Title`, data-slot="drawer-title". Apply cn() with font-heading text-base font-medium (matches SheetTitle).

    DrawerDescription: wraps `DrawerPrimitive.Description`, data-slot="drawer-description". Apply cn() with text-sm text-muted-foreground (matches SheetDescription).

    Named export block at the bottom of the file:
    `export { DrawerRoot, DrawerPortal, DrawerBackdrop, DrawerPopup, DrawerSwipeArea, DrawerClose, DrawerTitle, DrawerDescription }`
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - src/components/ui/drawer.tsx exists on disk
    - drawer.tsx line 1 is `"use client"`
    - drawer.tsx contains `import { Drawer as DrawerPrimitive } from "@base-ui/react/drawer"`
    - drawer.tsx contains `data-slot="drawer-popup"` (on DrawerPopup)
    - drawer.tsx contains `data-slot="drawer-swipe-area"` (on DrawerSwipeArea)
    - drawer.tsx contains `data-slot="drawer-backdrop"` (on DrawerBackdrop)
    - drawer.tsx contains `data-ending-style:translate-y-[2.5rem]` (popup animation)
    - drawer.tsx contains `data-starting-style:translate-y-[2.5rem]` (popup animation)
    - drawer.tsx exports DrawerRoot, DrawerPortal, DrawerBackdrop, DrawerPopup, DrawerSwipeArea, DrawerClose, DrawerTitle, DrawerDescription
    - `npx tsc --noEmit` exits 0 (no TypeScript errors from this new file)
    - `npm test -- --run src/components/chat/chat-panel.test.tsx` exits 0 (test stubs still pass/fail as expected)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component file only | New UI primitive wrapper; no data flow, no auth, no API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-02 | Tampering | Drawer modal focus trap | accept | @base-ui/react Drawer handles focus trapping and aria-modal automatically via modal=true default; no hand-rolled focus management needed |
| T-52-SC | Tampering | npm/pip/cargo installs | accept | No new packages; @base-ui/react already installed and pre-approved |

UI-only file. No new packages installed. No server interaction. Threat surface is minimal.
</threat_model>

<verification>
1. `npx tsc --noEmit` exits 0
2. `test -f src/components/ui/drawer.tsx` succeeds
3. `grep -c "data-slot" src/components/ui/drawer.tsx` returns 8 or more (one per wrapper function)
4. `npm test -- --run src/components/chat/chat-panel.test.tsx` exits 0
</verification>

<success_criteria>
- `src/components/ui/drawer.tsx` exists and exports all 8 named parts
- Follows sheet.tsx structural pattern exactly (data-slot, cn(), "use client", named exports)
- TypeScript compiles cleanly
- No new npm packages installed
</success_criteria>

<output>
Create `.planning/phases/52-chat-structural-refactor/52-03-SUMMARY.md` when done
</output>
