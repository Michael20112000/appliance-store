---
phase: 52-chat-structural-refactor
plan: 02
type: execute
wave: 1
depends_on:
  - 52-01
files_modified:
  - src/components/chat/chat-provider.tsx
  - src/lib/chat/search-params.ts
autonomous: true
requirements:
  - CHAT-14

must_haves:
  truths:
    - "chat-provider.tsx does not import useQueryStates, chatParsers, or chatUrlKeys"
    - "isOpen is a pure React useState(false) in ChatProvider"
    - "openPanel calls setIsOpen(true) — no setQuery call remains"
    - "closePanel calls setIsOpen(false) — no setQuery call remains"
    - "search-params.ts is deleted (no longer exists on disk)"
    - "useRouter is still present (used by router.refresh() in claim effect)"
  artifacts:
    - path: "src/components/chat/chat-provider.tsx"
      provides: "Refactored ChatProvider with useState-based isOpen"
      contains: "const [isOpen, setIsOpen] = useState(false)"
  key_links:
    - from: "src/components/chat/chat-provider.tsx"
      to: "React useState"
      via: "const [isOpen, setIsOpen]"
      pattern: "useState\\(false\\)"
---

<objective>
Remove nuqs URL-derived `isOpen` from `ChatProvider` and replace with a pure `useState(false)`. Delete `src/lib/chat/search-params.ts` which becomes orphaned after the change. Chat open state now survives page navigation because `ChatProvider` lives in the storefront layout and its React state is preserved by Next.js across client-side transitions.

Purpose: Implements CHAT-14 — chat stays open when navigating between pages. Closes only via the X button.
Output: `chat-provider.tsx` with `useState`-based `isOpen`; `search-params.ts` deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-chat-structural-refactor/52-RESEARCH.md
@.planning/phases/52-chat-structural-refactor/52-PATTERNS.md
</context>

<interfaces>
From src/components/chat/chat-provider.tsx (current, lines 114–184):
The file currently uses:
  - `useQueryStates(chatParsers, { shallow: false, urlKeys: chatUrlKeys })` → produces `[query, setQuery]`
  - `const isOpen = query.chat === "open"` (line 138)
  - `closePanel` calls `void setQuery({ chat: null, productId: null })` (line 150)
  - `openPanel` calls `void setQuery({ chat: "open", ... })` (lines 174, 181)
  - `useRouter()` is also called (line 111) and `router` is used at line 468 in claim effect → KEEP `useRouter`
  - `usePathname()` line 112 and `useSearchParams()` line 113 — verify these are not used elsewhere before removing

From src/lib/chat/search-params.ts (17 lines — full file content is just chatParsers, chatSearchParamsCache, chatUrlKeys):
  - All three exports are imported only in `chat-provider.tsx` lines 16–17
  - No server components use this file (confirmed by RESEARCH.md Pitfall 5)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remove nuqs from chat-provider.tsx and replace isOpen with useState</name>
  <files>src/components/chat/chat-provider.tsx, src/lib/chat/search-params.ts</files>

  <read_first>
    - src/components/chat/chat-provider.tsx — read the full file before editing; specifically: line 14 (next/navigation imports), line 15 (useQueryStates import), line 16 (chatParsers/chatUrlKeys import), lines 111–117 (hook calls), line 138 (isOpen derivation), lines 149–153 (closePanel), lines 158–184 (openPanel), line 468 (router.refresh usage), lines 478–532 (useMemo value and deps array)
    - src/lib/chat/search-params.ts — confirm it is 17 lines and contains only chatParsers, chatSearchParamsCache, chatUrlKeys; no other exports
    - .planning/phases/52-chat-structural-refactor/52-PATTERNS.md — "chat-provider.tsx" section for exact before/after code patterns for closePanel and openPanel rewrites
  </read_first>

  <action>
    Make the following targeted edits to `src/components/chat/chat-provider.tsx`:

    1. REMOVE the import of `useQueryStates` from "nuqs" (line 15).
    2. REMOVE the import of `chatParsers, chatUrlKeys` from "@/lib/chat/search-params" (line 16).
    3. On line 14, verify `usePathname` and `useSearchParams` are not used anywhere else in the file (search for their usage — if unused, remove them from the `next/navigation` import; keep `useRouter` as it is used for `router.refresh()` at line 468).
    4. REMOVE the `useQueryStates` hook call (lines 114–117): `const [query, setQuery] = useQueryStates(chatParsers, { shallow: false, urlKeys: chatUrlKeys });`
    5. REMOVE the line `const isOpen = query.chat === "open";` (line 138).
    6. ADD directly after the existing `useState` declarations (after line 133): `const [isOpen, setIsOpen] = useState(false);`
    7. REWRITE `closePanel` (lines 149–153) to:
       `const closePanel = useCallback(() => { setIsOpen(false); setProductContext(null); setPanelView("thread"); }, []);`
       Remove `setQuery` from the dependency array.
    8. REWRITE `openPanel` (lines 158–184):
       - Keep the guest token generation logic unchanged (the `if (!hasSession && !guestToken) { ... }` block).
       - Replace both `void setQuery({ chat: "open", ... })` calls with `setIsOpen(true)`.
       - Remove the early `return` after `setProductContext(options)` — instead fall through to `setIsOpen(true)`.
       - The new `openPanel` body: check guest token, if options has productId/title/slug call `setProductContext(options)`, then always call `setIsOpen(true)`.
       - Update dependency array: remove `setQuery`, keep `[guestToken, hasSession]`.
    9. In the `useMemo` deps array (lines 508–532), remove any reference to `setQuery` or `query` if present. The `isOpen` entry remains (now from useState).
    10. Keep the `isOpenRef` sync effect (lines 145–147) unchanged — it reads `isOpen` which now comes from `useState`.

    After editing `chat-provider.tsx`, DELETE the file `src/lib/chat/search-params.ts` entirely. Use the Bash tool: `rm src/lib/chat/search-params.ts`.
  </action>

  <verify>
    <automated>npm test -- --run src/components/chat/chat-panel.test.tsx</automated>
  </verify>

  <acceptance_criteria>
    - src/components/chat/chat-provider.tsx does not contain the string "useQueryStates"
    - src/components/chat/chat-provider.tsx does not contain the string "chatParsers"
    - src/components/chat/chat-provider.tsx does not contain the string "chatUrlKeys"
    - src/components/chat/chat-provider.tsx does not contain the string "query.chat"
    - src/components/chat/chat-provider.tsx contains `const [isOpen, setIsOpen] = useState(false)`
    - src/components/chat/chat-provider.tsx contains `setIsOpen(false)` inside the closePanel body
    - src/components/chat/chat-provider.tsx contains `setIsOpen(true)` inside the openPanel body
    - src/components/chat/chat-provider.tsx still contains `router.refresh()` (claim effect preserved)
    - src/lib/chat/search-params.ts does not exist on disk
    - `npm test -- --run src/components/chat/chat-panel.test.tsx` exits 0 (CHAT-06 tests still pass; CHAT-12 and CHAT-13 it.fails stubs still fail as expected; CHAT-14 it.todo is skipped)
    - TypeScript compiles without errors: `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ChatProvider state | isOpen moves from URL (attacker-observable) to internal React state (not in URL) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | Information Disclosure | isOpen URL param (`?chat=open`) | accept/improve | Removing URL param reduces information exposure; chat state is now private to client |
| T-52-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |

Removing the URL-based open state is a net security improvement: previously the chat state was observable and manipulable via URL. The new useState approach is internal-only.
</threat_model>

<verification>
1. `npm test -- --run src/components/chat/chat-panel.test.tsx` exits 0
2. `npx tsc --noEmit` exits 0 (TypeScript type check)
3. `grep -r "chatSearchParamsCache\|chatParsers\|chatUrlKeys" src/` returns nothing (all references removed)
4. `test ! -f src/lib/chat/search-params.ts` succeeds (file deleted)
</verification>

<success_criteria>
- nuqs is fully removed from chat open state management
- `isOpen` is a pure `useState(false)` in ChatProvider
- `search-params.ts` is deleted
- All existing tests pass
- TypeScript compiles cleanly
</success_criteria>

<output>
Create `.planning/phases/52-chat-structural-refactor/52-02-SUMMARY.md` when done
</output>
