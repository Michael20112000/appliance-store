---
phase: 52-chat-structural-refactor
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/components/chat/chat-panel.test.tsx
autonomous: true
requirements:
  - CHAT-12
  - CHAT-13
  - CHAT-14

must_haves:
  truths:
    - "Tests for CHAT-12 exist that will fail until mobile Drawer structure is implemented"
    - "Tests for CHAT-13 exist that will fail until history overlay is implemented"
    - "Tests for CHAT-14 exist that will fail until nuqs is removed from isOpen"
  artifacts:
    - path: "src/components/chat/chat-panel.test.tsx"
      provides: "Wave 0 failing test stubs for all three CHAT requirements"
      contains: "CHAT-12"
  key_links:
    - from: "chat-panel.test.tsx"
      to: "chat-panel.tsx"
      via: "import ChatPanel"
      pattern: "import.*ChatPanel.*chat-panel"
---

<objective>
Add failing test stubs to `chat-panel.test.tsx` for CHAT-12 (mobile Drawer), CHAT-13 (history overlay), and CHAT-14 (isOpen as useState). These stubs run RED now and turn GREEN after Plans 03 and 04 implement the features. No production code changes in this plan.

Purpose: Establishes the Wave 0 validation baseline required by nyquist_compliant policy. The existing test file covers CHAT-06; this plan adds three new describe blocks without disturbing existing tests.
Output: Updated `chat-panel.test.tsx` with failing stubs; all existing tests remain green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-chat-structural-refactor/52-RESEARCH.md
@.planning/phases/52-chat-structural-refactor/52-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing test stubs for CHAT-12, CHAT-13, CHAT-14 to chat-panel.test.tsx</name>
  <files>src/components/chat/chat-panel.test.tsx</files>

  <read_first>
    - src/components/chat/chat-panel.test.tsx — read the entire file; existing mock setup, `baseChatContext` shape, and the CHAT-06 describe block must be preserved exactly; new describes are appended after line 110
    - src/components/chat/chat-panel.tsx — understand current rendering so stub assertions are grounded in real DOM output; specifically line 185 (desktop content-swap) and lines 189–203 (mobile Sheet block)
    - src/components/chat/chat-provider.tsx — line 138: `const isOpen = query.chat === "open"` — the CHAT-14 stub asserts this pattern is absent after refactor (stub fails NOW because the pattern still exists; the stub must be written to test the post-refactor state)
    - .planning/phases/52-chat-structural-refactor/52-VALIDATION.md — per-task verification map rows 52-01-01 for exact behaviors required
  </read_first>

  <behavior>
    CHAT-12 stub: When `isMobile=true` and `isOpen=true`, ChatPanel renders a `[data-slot="drawer-popup"]` element.
    - Currently FAILS because ChatPanel renders `SheetContent` (data-slot="sheet-content"), not drawer-popup.
    - matchMedia mock in beforeEach must return `matches: true` for the mobile test cases.

    CHAT-13 stub: When `isOpen=true` and `panelView="thread"`, ChatPanel renders PanelBody. When `panelView="history"`, ChatPanel still renders PanelBody (not replacing it).
    - Currently FAILS for panelView="history" because chat-panel.tsx line 185/201 does a full swap: `panelView === "history" ? HistoryDrawer : PanelBody` — so PanelBody is absent when panelView="history".
    - PanelBody renders its children via mocked sub-components (MessageList etc. all mock to null). To detect PanelBody is rendered, assert presence of the panel header aria-label or the `"Чат з магазином"` text that PanelHeader renders unconditionally.

    CHAT-14 stub: ChatProvider's `isOpen` is not derived from `query.chat`. After refactor, `baseChatContext` no longer needs a `setQuery` mock. Assert that `closePanel` in the mocked context can be called without interacting with URL state (this is a source-level check in plan 02, not testable via render — so the stub for CHAT-14 is a unit test on `closePanel` behavior: calling the mocked `closePanel` does not trigger any navigation/URL side-effect from within ChatPanel's onOpenChange handler).
    - Practical stub: render ChatPanel with `isOpen=true`, `isMobile=true`; when Drawer onOpenChange fires with `open=false`, `closePanel` mock is called exactly once and no router navigation mock is called. Since DrawerRoot is mocked to null in this test plan (drawer.tsx doesn't exist yet), write this as a todo-style stub using `it.todo("CHAT-14: closePanel is called on drawer close without URL update")` so the file compiles.

    Test structure to add (three new describe blocks after the existing CHAT-06 describe, within the same file):
    - `describe("CHAT-12 — Mobile Drawer", ...)` with `it.fails(...)` for drawer-popup assertion
    - `describe("CHAT-13 — History overlay always-rendered", ...)` with `it.fails(...)` for PanelBody-present-when-panelView-history assertion
    - `describe("CHAT-14 — isOpen state", ...)` with `it.todo(...)` for the closePanel/no-URL stub
  </behavior>

  <action>
    Append three new describe blocks to `src/components/chat/chat-panel.test.tsx` after the closing brace of the existing "PanelHeader — Menu icon visibility gated on hasSession" describe block (after line 110).

    Block 1 — CHAT-12 mobile Drawer:
    Add a describe block named "CHAT-12 — Mobile Drawer". In beforeEach, override the matchMedia mock so `matches: true` (simulating a mobile viewport). Add `it.fails("renders drawer-popup when isMobile and isOpen", ...)` that renders ChatPanel with `baseChatContext` merged with `{ isOpen: true }`, then asserts `document.querySelector('[data-slot="drawer-popup"]')` is not null. Use `it.fails` because `[data-slot="drawer-popup"]` does not exist in the current implementation (it renders `[data-slot="sheet-content"]`).

    Block 2 — CHAT-13 history overlay:
    Add a describe block named "CHAT-13 — History overlay always-rendered". Use matchMedia mock with `matches: false` (desktop). Add `it.fails("renders PanelBody even when panelView=history", ...)` that renders ChatPanel with `baseChatContext` merged with `{ isOpen: true, panelView: "history" as const }`, then asserts `screen.getByText("Чат з магазином")` is present (PanelHeader renders this text). Use `it.fails` because today's desktop branch swaps PanelBody out entirely when panelView="history" — so the header text is absent.

    Block 3 — CHAT-14 isOpen state:
    Add a describe block named "CHAT-14 — isOpen as pure useState". Add `it.todo("closePanel is called on drawer close; no URL manipulation occurs")`. This is a placeholder that will be upgraded to a real test in plan 04 after DrawerRoot exists and can fire onOpenChange.

    Do NOT modify the existing "PanelHeader" describe block or its tests. Do NOT add new imports — all required imports (`describe`, `it`, `screen`, `render`, `cleanup`, `vi`, `expect`) are already present on lines 2–3.

    The `baseChatContext.panelView` is currently typed `"thread" as const`. The CHAT-13 test must supply `panelView: "history" as const` in the spread override to match the `ChatContextValue` type.
  </action>

  <verify>
    <automated>npm test -- --run src/components/chat/chat-panel.test.tsx</automated>
  </verify>

  <acceptance_criteria>
    - chat-panel.test.tsx contains the string "CHAT-12 — Mobile Drawer" in a describe block
    - chat-panel.test.tsx contains the string "CHAT-13 — History overlay always-rendered" in a describe block
    - chat-panel.test.tsx contains the string "CHAT-14 — isOpen as pure useState" in a describe block
    - chat-panel.test.tsx contains `it.fails(` (for CHAT-12 and CHAT-13 stubs)
    - chat-panel.test.tsx contains `it.todo(` (for CHAT-14 stub)
    - `npm test -- --run src/components/chat/chat-panel.test.tsx` exits 0 (it.fails counts as passing when the assertion fails; it.todo does not run)
    - The two existing CHAT-06 tests ("guest user does NOT see..." and "authenticated user DOES see...") still pass
    - No new imports are added to the test file
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test file only | No production code modified; no trust boundaries crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |

This plan modifies only a test file. No security-relevant code changes.
</threat_model>

<verification>
Run `npm test -- --run src/components/chat/chat-panel.test.tsx` after task completion.

Expected: all tests pass (it.fails passes when assertion fails; it.todo is skipped). Exit code 0.
</verification>

<success_criteria>
- `chat-panel.test.tsx` has three new describe blocks for CHAT-12, CHAT-13, CHAT-14
- All existing CHAT-06 tests still pass
- `npm test -- --run src/components/chat/chat-panel.test.tsx` exits 0
- No production code modified
</success_criteria>

<output>
Create `.planning/phases/52-chat-structural-refactor/52-01-SUMMARY.md` when done
</output>
