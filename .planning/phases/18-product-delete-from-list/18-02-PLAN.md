---
phase: 18-product-delete-from-list
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - src/components/admin/product-list-delete-button.test.tsx
  - .planning/phases/18-product-delete-from-list/18-MANUAL-CHECKLIST.md
autonomous: true
requirements:
  - ADM-PRD-04
must_haves:
  truths:
    - "Vitest proves delete control does not bubble click/pointerdown to parent row handler"
    - "Operator has manual checklist for happy path, cancel, and guard scenarios"
  artifacts:
    - path: src/components/admin/product-list-delete-button.test.tsx
      provides: stopPropagation regression test
    - path: .planning/phases/18-product-delete-from-list/18-MANUAL-CHECKLIST.md
      provides: manual verification gate
  key_links:
    - from: src/components/admin/product-list-delete-button.test.tsx
      to: src/components/admin/product-list-delete-button.tsx
      via: render + fireEvent on trash button
      pattern: "parentClick.*not.toHaveBeenCalled"
---

<objective>
Lock ADM-PRD-04 row-navigation isolation with Vitest and provide operator manual checklist for delete flows that need DB + admin session.

Purpose: Automated guard against delete-click opening edit; documented manual gate before verify-work.
Output: `product-list-delete-button.test.tsx`, `18-MANUAL-CHECKLIST.md`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-product-delete-from-list/18-RESEARCH.md
@.planning/phases/18-product-delete-from-list/18-UI-SPEC.md
@.planning/phases/18-product-delete-from-list/18-VALIDATION.md
@src/components/admin/product-list-delete-button.tsx
@src/components/admin/order-list-status-select.test.tsx
@.planning/phases/17-admin-chat-inbox-layout/17-MANUAL-CHECKLIST.md
</context>

<interfaces>
From order-list-status-select.test.tsx (pattern to mirror):

```typescript
/** @vitest-environment jsdom */
// Parent div with onClick + onPointerDown mocks
// fireEvent.click / fireEvent.pointerDown on interactive child
// expect(parent handlers).not.toHaveBeenCalled()
```

Mocks for ProductListDeleteButton test:
- next/navigation: useRouter → { refresh: vi.fn() }
- @/server/actions/admin/product.actions: { deleteProductFromListAction: vi.fn() }
- sonner: { toast: { error: vi.fn(), success: vi.fn() } }

Target element: trash Button (query by aria-label Видалити товар or role button within component).
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test environment | jsdom only; no real DB or auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02-01 | Tampering | Row navigation regression | mitigate | Vitest asserts stopPropagation on click and pointerdown |
| T-18-02-02 | Repudiation | Manual delete untested | mitigate | 18-MANUAL-CHECKLIST.md signed before phase verify |
| T-18-SC | Tampering | test deps | accept | No new packages; Vitest already in project |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Vitest stopPropagation on delete control</name>
  <read_first>
    - src/components/admin/order-list-status-select.test.tsx — full file pattern
    - src/components/admin/product-list-delete-button.tsx — Button handlers and aria-label
    - .planning/phases/18-product-delete-from-list/18-VALIDATION.md — T-18-01-02 / command product-list-delete
  </read_first>
  <files>src/components/admin/product-list-delete-button.test.tsx</files>
  <action>
    Create product-list-delete-button.test.tsx with vitest-environment jsdom header.

    Test name: stopPropagation on delete button click and pointerdown (ADM-PRD-04).

    Render:
    - Parent div with onClick and onPointerDown vi.fn()
    - ProductListDeleteButton productId="clxxxxxxxxxxxxxxxxxxxxxxxxx" (valid-looking cuid placeholder)

    Find trash control by getByRole('button', { name: 'Видалити товар' }) (preferred) or query button inside data-admin-row-interactive.

    fireEvent.click(button); fireEvent.pointerDown(button);

    Assert parent onClick and onPointerDown were NOT called.

    Optional second test (if trivial): opening dialog does not call parent — only if zero extra mock complexity.

    Do not test server action integration here (manual checklist covers DB path).
  </action>
  <verify>
    <automated>npm run test -- product-list-delete</automated>
  </verify>
  <acceptance_criteria>
    - product-list-delete-button.test.tsx exists
    - npm run test -- product-list-delete exits 0
    - Test fails if stopPropagation removed from delete button (red-green sanity)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Phase 18 manual checklist</name>
  <read_first>
    - .planning/phases/18-product-delete-from-list/18-UI-SPEC.md — Verification UI section
    - .planning/phases/18-product-delete-from-list/18-VALIDATION.md — Manual-Only table
    - .planning/phases/17-admin-chat-inbox-layout/17-MANUAL-CHECKLIST.md — format reference
    - .planning/phases/16-shadcn-select-audit-verify/16-MANUAL-CHECKLIST.md — compact table format
  </read_first>
  <files>.planning/phases/18-product-delete-from-list/18-MANUAL-CHECKLIST.md</files>
  <action>
    Create 18-MANUAL-CHECKLIST.md for Phase 18.

    Sections:
    - Prerequisites: npm run dev, admin signed in, at least one deletable product on /admin/tovary
    - Happy path: Trash → confirm Видалити → toast «Товар видалено» → row gone after refresh (or router.refresh)
    - Cancel: Trash → Скасувати → stay on list, no navigation to edit
    - Row nav: click title/photo after cancel → navigates to /admin/tovary/[id]
    - Status column: ProductListStatusSelect still works (no regression)
    - Optional guard (if seed available): product in cart → error toast, row remains
    - Sign-off line with date/initials

    Reference ROADMAP success criteria #5 and ADM-PRD-04.
  </action>
  <verify>
    <automated>test -f .planning/phases/18-product-delete-from-list/18-MANUAL-CHECKLIST.md && grep -q 'ADM-PRD-04' .planning/phases/18-product-delete-from-list/18-MANUAL-CHECKLIST.md && grep -q 'Товар видалено' .planning/phases/18-product-delete-from-list/18-MANUAL-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - 18-MANUAL-CHECKLIST.md exists with happy path, cancel, row navigation, and optional guard rows
    - Mentions /admin/tovary and Ukrainian UI strings from implementation
  </acceptance_criteria>
</task>

</tasks>

<verification>
- npm run test -- product-list-delete
- npm run build (full phase gate before verify-work)
- Manual checklist file present for operator
</verification>

<success_criteria>
- All Vitest tests green including product-list-delete
- Manual checklist ready for human sign-off on delete → row disappears
</success_criteria>

<output>
Create `.planning/phases/18-product-delete-from-list/18-02-SUMMARY.md` when done
</output>
