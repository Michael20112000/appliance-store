---
phase: 02-catalog-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - prisma/schema.prisma
  - prisma/seed.ts
  - prisma/seed-products.ts
  - prisma/seed.test.ts
  - src/server/services/catalog.service.ts
  - src/server/services/catalog.service.test.ts
  - src/server/validators/product.ts
  - src/types/catalog.ts
  - src/lib/env.ts
  - .env.example
autonomous: false
requirements:
  - CAT-07
context_decisions:
  - "ProductStatus enum AVAILABLE|SOLD|DRAFT — single source for CAT-07 (per 02-RESEARCH, not isPublished boolean)"
  - "Price stored as integer kopiyky (UAH×100) per ARCHITECTURE.md / PITFALLS.md"
  - "Seed uses pre-uploaded Cloudinary public IDs (D-16 upload deferred Phase 4)"
  - "Public catalog reads only via catalog.service — pages must not import prisma directly (02-PATTERNS)"
user_setup:
  - service: cloudinary
    why: "Demo product images in seed need valid public IDs"
    env_vars:
      - name: SEED_CLOUDINARY_PUBLIC_IDS
        source: "Comma-separated public IDs in your Cloudinary media library (document in .env.example)"
      - name: CLOUDINARY_CLOUD_NAME
        source: "Already required Phase 1 — reuse for seed docs"

must_haves:
  truths:
    - "Neon DB has Product + ProductImage tables with composite indexes prefixed by status"
    - "Seed includes AVAILABLE products across categories plus SOLD/DRAFT rows for tests"
    - "listPublicProducts always filters status AVAILABLE (sold hidden from public queries)"
  artifacts:
    - path: prisma/schema.prisma
      provides: Product, ProductImage, ProductCondition, ProductStatus enums
      contains: model Product
    - path: src/server/services/catalog.service.ts
      provides: buildPublicProductWhere, listPublicProducts, getPublicProductBySlug
      exports: ["buildPublicProductWhere", "listPublicProducts", "getPublicProductBySlug"]
    - path: prisma/seed-products.ts
      provides: Idempotent demo inventory
  key_links:
    - from: src/server/services/catalog.service.ts
      to: prisma.product
      via: "where status AVAILABLE in buildPublicProductWhere"
      pattern: 'status:\s*["\']AVAILABLE["\']'
    - from: prisma/seed.ts
      to: prisma/seed-products.ts
      via: "main() calls seedProducts()"
      pattern: seedProducts

---

## Phase Goal

**As a** покупець, **I want to** бачити лише товари в наявності в каталозі, **so that** я не витрачаю час на продані позиції (CAT-07 foundation).

<objective>
Додати Prisma Product/ProductImage, міграцію, catalog.service з єдиним public scope, seed демо-товарів і unit-тести query builder.

Purpose: Data + service backbone для всіх catalog slices.
Output: Migrated schema, seeded products, `catalog.service` з CAT-07 guard.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-catalog-discovery/02-RESEARCH.md
@.planning/phases/02-catalog-discovery/02-PATTERNS.md
@.planning/phases/01-foundation-auth-design-system/01-CONTEXT.md — D-14, D-16, D-19
@prisma/schema.prisma
@src/lib/db.ts
@prisma/seed.ts

<interfaces>
From prisma/schema.prisma (Category — extend only):
- Category gets `products Product[]` relation
- New enums: ProductCondition (LIKE_NEW, GOOD, FAIR), ProductStatus (AVAILABLE, SOLD, DRAFT)
- Product: id cuid, title, slug @unique, description?, brand, price Int (kopiyky), condition, status default DRAFT, categoryId, images relation
- ProductImage: cloudinaryPublicId, alt?, sortOrder, onDelete Cascade
- Indexes: @@index([status, categoryId]), [status, brand], [status, price], [status, condition], [status, createdAt]

From src/lib/db.ts:
- export const prisma — singleton PrismaNeon client

From prisma/seed.ts:
- slugify(name, { lower: true, strict: true, locale: "uk" }) + category.upsert pattern
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify nuqs + schema-dts packages [ASSUMED]</name>
  <files>package.json</files>
  <read_first>
    - 02-RESEARCH.md — Package Legitimacy Audit table
  </read_first>
  <action>
    Before any npm install of nuqs or schema-dts, human verifies package legitimacy on npmjs.com (slopcheck unavailable at research time).
    Check nuqs@2.8.9 (publisher 47ng, repo github.com/47ng/nuqs) and schema-dts@2.0.0 (Google). Pause until user replies "approved" or flags a concern.
    Do not run npm install for these packages in this task — installs happen in 02-04 (nuqs) and 02-06 (schema-dts).
  </action>
  <what-built>Pre-install legitimacy check for new npm dependencies</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/nuqs — confirm publisher 47ng, recent version 2.8.x, GitHub github.com/47ng/nuqs
    2. Open https://www.npmjs.com/package/schema-dts — confirm Google schema-dts, version 2.0.x
    3. Reply "approved" if both look legitimate, or describe concern
  </how-to-verify>
  <verify>
    <automated>test -f package.json</automated>
  </verify>
  <done>Human approved both packages for later install waves</done>
  <resume-signal>approved</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Prisma Product schema + [BLOCKING] migrate</name>
  <read_first>
    - 02-RESEARCH.md — Prisma Product Schema (recommended)
    - AGENTS.md — read node_modules/next/dist/docs/ only if touching Next; schema is Prisma-only here
    - prisma/schema-conventions if using Prisma plugin rules
  </read_first>
  <files>prisma/schema.prisma</files>
  <action>
    Extend Category with `products Product[]`.
    Add enums ProductCondition, ProductStatus and models Product, ProductImage exactly per 02-RESEARCH schema block (kopiyky price, status default DRAFT, composite indexes with status prefix).
    Do NOT add datasource url to schema.prisma (Prisma 7 — url stays in prisma.config.ts).
    **BLOCKING:** Run `npx prisma migrate dev --name catalog_products` using DIRECT_URL (Neon direct) — must succeed before any seed or service verification.
    Run `npx prisma generate`.
    Do not install nuqs yet (plan 02-04) — only schema work in this task.
  </action>
  <acceptance_criteria>
    - Migration applied on dev DB
    - prisma validate exit 0
    - Generated client includes Product types
  </acceptance_criteria>
  <verify>
    <automated>npx prisma validate && npx prisma migrate status</automated>
  </verify>
  <done>Product tables exist in Neon; migrate status shows applied migration</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: catalog.service + validators + seed products</name>
  <read_first>
    - 02-RESEARCH.md — Pattern 1 buildPublicProductWhere, listPublicProducts include images take 1
    - 02-PATTERNS.md — service layer; no prisma in pages
    - src/lib/env.test.ts — Vitest style
  </read_first>
  <files>
    src/server/services/catalog.service.ts
    src/server/services/catalog.service.test.ts
    src/server/validators/product.ts
    src/types/catalog.ts
    prisma/seed-products.ts
    prisma/seed.ts
    prisma/seed.test.ts
    .env.example
  </files>
  <behavior>
    - buildPublicProductWhere: always includes status AVAILABLE; omits SOLD/DRAFT
    - buildPublicProductWhere with categoryId filters by category
    - getPublicProductBySlug returns null for SOLD, DRAFT, and missing slug
    - listPublicProducts caps take at 24 server-side
    - Seed: at least 24 AVAILABLE products (3–4 per category), 2 SOLD, 1 DRAFT; upsert by slug
  </behavior>
  <action>
    Create src/types/catalog.ts with CatalogFilters, PublicProductCard, PublicProductDetail DTOs (serializable for RSC).
    Create src/server/validators/product.ts: z.enum for condition/status, filter bounds (max take 24, price min/max non-negative integers).
    Implement catalog.service.ts:
    - PUBLIC_STATUS constant AVAILABLE
    - buildPublicProductWhere(input) — status guard mandatory (CAT-07)
    - listPublicProducts({ categoryId?, filters?, page?, pageSize? }) — default pageSize 24, orderBy createdAt desc; include images orderBy sortOrder take 1 + category select name/slug
    - getPublicProductBySlug(slug) — null unless AVAILABLE
    - getDistinctBrands() for later filters (AVAILABLE only)
    Pure TS — import prisma from @/lib/db only; no React.
    prisma/seed-products.ts: seed per 02-RESEARCH (slugify title locale uk, variety brands/prices/conditions, cloudinaryPublicId from SEED_CLOUDINARY_PUBLIC_IDS split or fallback HERO_PUBLIC_ID from src/lib/demo-assets.ts for all images if env missing — document in .env.example).
    Import seedProducts() from prisma/seed.ts main() after categories.
    Extend prisma/seed.test.ts: expect product count >= 24, SOLD count >= 2.
    Unit tests for buildPublicProductWhere and getPublicProductBySlug status rules (mock-free pure where builder tests + optional integration with prisma if seed.test pattern used).
    **BLOCKING after seed code:** `npx prisma db seed` must pass.
  </action>
  <acceptance_criteria>
    - npm test passes catalog.service tests
    - Seed idempotent on second run
    - prisma.product.count where status AVAILABLE >= 24
  </acceptance_criteria>
  <verify>
    <automated>npx prisma db seed && npm test -- src/server/services/catalog.service.test.ts && npx vitest run prisma/seed.test.ts</automated>
  </verify>
  <done>Service enforces AVAILABLE-only public reads; DB seeded with demo catalog + SOLD fixtures</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prisma/seed.ts → Neon | Privileged writes; demo product data |
| catalog.service → Postgres | Public read path; must never leak SOLD in list |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Elevation | catalog.service | mitigate | status AVAILABLE enforced in one builder; unit tests assert SOLD excluded |
| T-02-01-02 | Denial of Service | listPublicProducts | mitigate | Hard cap take/pageSize at 24 in validator + service |
| T-02-01-03 | Tampering | npm install nuqs/schema-dts | mitigate | Human checkpoint Task 1 before 02-04 install; T-02-SC deferred to 02-04 |
| T-02-01-SC | Tampering | prisma migrate | mitigate | Review generated SQL; migrate dev only on dev branch |

</threat_model>

<verification>
npx prisma migrate status
npx prisma db seed
npm test -- src/server/services/catalog.service.test.ts
</verification>

<success_criteria>
Product schema live; catalog.service returns only AVAILABLE products; seed data ready for UI slices
</success_criteria>

<output>
Create `.planning/phases/02-catalog-discovery/02-01-SUMMARY.md` when done
</output>
