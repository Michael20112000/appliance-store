---
phase: 02-catalog-discovery
plan: 05
type: execute
wave: 4
depends_on:
  - 02-04
files_modified:
  - src/server/services/catalog.service.ts
  - src/server/services/catalog.service.test.ts
  - src/components/catalog/catalog-toolbar.tsx
  - src/app/(storefront)/katalog/page.tsx
  - src/app/(storefront)/katalog/[slug]/page.tsx
  - e2e/catalog-search.spec.ts
autonomous: true
requirements:
  - CAT-04
context_decisions:
  - "Text search uses Prisma contains mode insensitive on title OR description (02-RESEARCH v1 — no Meilisearch)"
  - "Minimum q length 2 characters before applying OR clause (planner discretion in RESEARCH)"
  - "q param already in UI-SPEC URL contract — wire toolbar search to existing parser"

must_haves:
  truths:
    - "Buyer types search query and sees matching products in grid"
    - "Buyer can share URL with ?q= and recipient sees same search results"
    - "Single-character q does not run expensive unbounded search"
  artifacts:
    - path: src/server/services/catalog.service.ts
      provides: q filter in buildPublicProductWhere
    - path: src/components/catalog/catalog-toolbar.tsx
      provides: Search input synced to nuqs q
    - path: e2e/catalog-search.spec.ts
      provides: CAT-04 e2e
  key_links:
    - from: catalog-toolbar
      to: nuqs q
      via: debounced set q with shallow false
      pattern: parseAsString.*\bq\b
    - from: buildPublicProductWhere
      to: Prisma OR title/description
      pattern: mode:\s*['\"]insensitive['\"]

---

## Phase Goal

**As a** покупець, **I want to** шукати товари за назвою або описом, **so that** я знаходжу потрібну модель без перегляду всього каталогу (CAT-04).

<objective>
Підключити параметр `q` до Prisma ILIKE пошуку та search input у CatalogToolbar.

Purpose: Text discovery on top of filter infrastructure.
Output: Search in service + UI + tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-catalog-discovery/02-04-SUMMARY.md
@.planning/phases/02-catalog-discovery/02-RESEARCH.md — Search Strategy v1
@.planning/phases/02-catalog-discovery/02-UI-SPEC.md — search placeholder, debounce 300ms
@src/lib/catalog/search-params.ts

<interfaces>
catalogParsers.q already defined in 02-04
buildPublicProductWhere: when q.trim().length >= 2 add OR [{ title: { contains, mode insensitive }}, { description: { contains, mode insensitive }}]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Search in catalog.service</name>
  <read_first>
    - 02-RESEARCH.md — CAT-04 ILIKE pattern
  </read_first>
  <files>
    src/server/services/catalog.service.ts
    src/server/services/catalog.service.test.ts
  </files>
  <behavior>
    - q="холод" matches title containing холод (case insensitive)
    - q="a" (length 1) does not add OR clause
    - q combined with brend filter applies AND
    - Empty q returns unfiltered (besides AVAILABLE)
  </behavior>
  <action>
    Extend buildPublicProductWhere: trim q; if length &lt; 2 skip text filter; else OR title/description contains insensitive.
    Trim q max length 100 in validator to prevent abuse.
    Update listPublicProducts tests and run npm test.
  </action>
  <acceptance_criteria>
    - Unit tests pass for q behavior
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/server/services/catalog.service.test.ts</automated>
  </verify>
  <done>Server-side text search implemented safely</done>
</task>

<task type="auto">
  <name>Task 2: CatalogToolbar search UX</name>
  <read_first>
    - 02-UI-SPEC.md — placeholder «Пошук за назвою або описом…», debounce 300ms, sr-only label
  </read_first>
  <files>
    src/components/catalog/catalog-toolbar.tsx
    src/app/(storefront)/katalog/page.tsx
    src/app/(storefront)/katalog/[slug]/page.tsx
  </files>
  <action>
    catalog-toolbar.tsx: search Input type=search with lucide Search icon; sr-only label «Пошук товарів»; debounce 300ms before setQueryState q; submit on Enter; shallow false.
    Loading state «Шукаємо…» on pending transition optional via useTransition.
    Empty search results: add hint «Спробуйте інші ключові слова» per UI-SPEC when q non-empty and total=0.
    Ensure katalog pages pass total/count to toolbar for results line «Знайдено: n товарів».
  </action>
  <acceptance_criteria>
    - Typing search updates ?q= in URL after debounce
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Search UI wired to nuqs and service</done>
</task>

<task type="auto">
  <name>Task 3: E2E catalog search</name>
  <read_first>
    - 02-VALIDATION.md — CAT-04
  </read_first>
  <files>e2e/catalog-search.spec.ts</files>
  <action>
    e2e/catalog-search.spec.ts:
    - Seed-known unique substring in product title (document in test: use brand or title fragment from seed e.g. «Samsung» or «LG»)
    - goto /katalog?q={substring}; expect URL contains q=; expect at least one product card; expect count >= 1
    - goto /katalog?q=zzzznonexistent; expect «Товарів не знайдено»
  </action>
  <acceptance_criteria>
    - Playwright search spec passes
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test e2e/catalog-search.spec.ts</automated>
  </verify>
  <done>CAT-04 e2e verified</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| q query param → Prisma | Untrusted text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Tampering | q param | mitigate | Prisma parameterized contains only; max length 100 |
| T-02-05-02 | Denial of Service | ILIKE search | mitigate | min length 2; pageSize cap 24; status index prefix |

</threat_model>

<verification>
npm test -- src/server/services/catalog.service.test.ts
npx playwright test e2e/catalog-search.spec.ts
</verification>

<success_criteria>
Buyer can search by text; URL shareable; no SQL injection via q
</success_criteria>

<output>
Create `.planning/phases/02-catalog-discovery/02-05-SUMMARY.md` when done
</output>
