---
phase: 02-catalog-discovery
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/components/catalog/product-gallery.tsx
  - src/components/catalog/product-detail-skeleton.tsx
  - src/app/(storefront)/tovar/[slug]/page.tsx
  - e2e/product-pdp.spec.ts
autonomous: true
requirements:
  - CAT-02
context_decisions:
  - "PDP route /tovar/[slug] per 02-RESEARCH (no nested /katalog/cat/product)"
  - "SOLD/DRAFT → notFound() strict CAT-07 (not «Продано» page)"
  - "No add-to-cart button — Phase 3 (02-UI-SPEC out of scope)"

must_haves:
  truths:
    - "Buyer opens /tovar/[valid-slug] and sees title, brand, price, condition, description, image gallery"
    - "Buyer gets 404 for SOLD product slug (not in listings, PDP notFound)"
  artifacts:
    - path: src/app/(storefront)/tovar/[slug]/page.tsx
      provides: Product detail RSC
    - path: src/components/catalog/product-gallery.tsx
      provides: Main image + thumbnails
    - path: e2e/product-pdp.spec.ts
      provides: CAT-02 e2e
  key_links:
    - from: src/app/(storefront)/tovar/[slug]/page.tsx
      to: getPublicProductBySlug
      via: await getPublicProductBySlug(slug)
      pattern: getPublicProductBySlug
    - from: src/components/catalog/product-gallery.tsx
      to: OptimizedImage
      via: cloudinaryPublicId from product.images
      pattern: OptimizedImage

---

## Phase Goal

**As a** покупець, **I want to** відкрити сторінку товару з описом і галереєю, **so that** я можу оцінити стан і ціну перед покупкою (CAT-02).

<objective>
Додати PDP `/tovar/[slug]` з галереєю, описом, характеристиками (brand, category, condition) українською.

Purpose: Product detail vertical slice parallel to category grid.
Output: PDP route + ProductGallery + e2e.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-catalog-discovery/02-01-SUMMARY.md
@.planning/phases/02-catalog-discovery/02-UI-SPEC.md — ProductDetail layout, gallery, copy, breadcrumb
@src/components/home/hero-section.tsx
@src/components/catalog/product-card.tsx

<interfaces>
getPublicProductBySlug(slug) → full product with images ordered sortOrder asc, category { name, slug }
Returns null for non-AVAILABLE → page calls notFound()

Breadcrumb: Каталог → {Category} → {title truncated}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProductGallery + PDP skeleton</name>
  <read_first>
    - 02-UI-SPEC.md — ProductGallery specs (4:3 main, 56px thumbs, keyboard)
    - src/components/media/optimized-image.tsx
  </read_first>
  <files>
    src/components/catalog/product-gallery.tsx
    src/components/catalog/product-detail-skeleton.tsx
  </files>
  <action>
    ProductGallery: accept images[] with cloudinaryPublicId, alt; main image priority sizes (max-width: 768px) 100vw, 50vw per UI-SPEC; thumbs row 56px rounded-md, ring-2 ring-primary on selected.
    Use "use client" only if thumb selection state needed; otherwise server with URL hash optional — prefer client for thumb switch without full navigation.
  aria-label на thumbs: «Фото {i} з {n}».
    Hide thumb row when images.length <= 1.
    product-detail-skeleton.tsx: gallery rect + title lines for loading.tsx optional.
  </action>
  <acceptance_criteria>
    - Gallery renders 1+ images without layout shift on single image
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Gallery component ready for PDP</done>
</task>

<task type="auto">
  <name>Task 2: PDP page /tovar/[slug]</name>
  <read_first>
    - 02-UI-SPEC.md — ProductDetail blocks, breadcrumb, specs dl, error copy
    - 02-RESEARCH.md — Pattern 4 notFound for SOLD
  </read_first>
  <files>src/app/(storefront)/tovar/[slug]/page.tsx</files>
  <action>
    Create route src/app/(storefront)/tovar/[slug]/page.tsx.
    await params → slug; product = await getPublicProductBySlug(slug); if !product notFound().
    Layout max-w-6xl: Breadcrumb (shadcn breadcrumb — add via npx shadcn@latest add breadcrumb if missing) Каталог → Link category → product title.
    Two-column md:grid: gallery | title Display typography, brand muted, PriceDisplay large, ConditionBadge, link to /katalog/[categorySlug].
    Description section «Опис» whitespace-pre-wrap.
    Specs dl «Характеристики»: Бренд, Категорія, Стан (UA).
    Primary CTA «До каталогу» → /katalog only (no cart).
    alt on images: `{title} — {brand}, б/у, Львів` truncate 120 chars.
  </action>
  <acceptance_criteria>
    - AVAILABLE seeded slug renders 200 with H1 title
    - SOLD seeded slug returns 404
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>PDP serves public product detail</done>
</task>

<task type="auto">
  <name>Task 3: E2E product PDP</name>
  <read_first>
    - 02-VALIDATION.md — product-pdp.spec.ts
  </read_first>
  <files>e2e/product-pdp.spec.ts</files>
  <action>
    e2e/product-pdp.spec.ts:
    - From /katalog/kholodylnyky click first /tovar/ link
    - Expect H1, price with ₴, condition badge, section «Опис»
    - Navigate to known SOLD slug from seed (document slug in test comment or read from env SEED_SOLD_SLUG optional) — expect 404/not found copy «Товар не знайдено»
  </action>
  <acceptance_criteria>
    - Playwright PDP spec green
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test e2e/product-pdp.spec.ts</automated>
  </verify>
  <done>CAT-02 automated coverage</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public PDP | No auth; only AVAILABLE products |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Information Disclosure | PDP | mitigate | getPublicProductBySlug enforces AVAILABLE; no draft leak |
| T-02-03-02 | Tampering | slug param | accept | Public slug lookup; no sensitive fields on model |

</threat_model>

<verification>
npx playwright test e2e/product-pdp.spec.ts
</verification>

<success_criteria>
PDP shows gallery, description, condition; SOLD returns 404
</success_criteria>

<output>
Create `.planning/phases/02-catalog-discovery/02-03-SUMMARY.md` when done
</output>
