---
phase: 02-catalog-discovery
plan: 06
type: execute
wave: 5
depends_on:
  - 02-02
  - 02-03
  - 02-04
files_modified:
  - package.json
  - package-lock.json
  - src/components/catalog/json-ld.tsx
  - src/lib/catalog/metadata.ts
  - src/lib/catalog/metadata.test.ts
  - src/lib/env.ts
  - .env.example
  - src/app/(storefront)/page.tsx
  - src/app/(storefront)/katalog/page.tsx
  - src/app/(storefront)/katalog/[slug]/page.tsx
  - src/app/(storefront)/tovar/[slug]/page.tsx
  - src/app/sitemap.ts
  - e2e/catalog-seo.spec.ts
autonomous: true
requirements:
  - SEO-01
  - SEO-02
context_decisions:
  - "JSON-LD server-only via json-ld.tsx helper with < escape (02-RESEARCH Pattern 5)"
  - "LocalBusiness on home with STORE_ADDRESS, STORE_PHONE env (02-RESEARCH RESOLVED)"
  - "Filtered catalog URLs: robots index false follow true when any filter param present (except empty q)"
  - "Sitemap includes only AVAILABLE products and category slugs"

must_haves:
  truths:
    - "Category and product pages have unique title and description in HTML head"
    - "PDP contains Product JSON-LD with Offer UAH and UsedCondition"
    - "Home contains LocalBusiness JSON-LD for Lviv"
    - "sitemap.xml lists categories and AVAILABLE product URLs only"
  artifacts:
    - path: src/app/sitemap.ts
      provides: Dynamic sitemap
    - path: src/components/catalog/json-ld.tsx
      provides: Safe JSON-LD script
    - path: src/app/(storefront)/tovar/[slug]/page.tsx
      provides: generateMetadata + Product schema
  key_links:
    - from: src/app/sitemap.ts
      to: catalog.service
      via: listPublicProductSlugsForSitemap() or equivalent
      pattern: listPublic
    - from: src/components/catalog/json-ld.tsx
      to: script type application/ld+json
      via: dangerouslySetInnerHTML escaped
      pattern: application/ld\+json

---

## Phase Goal

**As a** покупець (і пошуковик), **I want to** бачити коректні meta та структуровані дані для Львова, **so that** магазин знаходять у локальному пошуку (SEO-01, SEO-02).

<objective>
generateMetadata для каталогу/категорій/товарів, JSON-LD Product + LocalBusiness, sitemap.ts.

Purpose: Local SEO package for Phase 2 routes.
Output: Metadata helpers, JSON-LD, sitemap, e2e SEO checks.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-catalog-discovery/02-02-SUMMARY.md
@.planning/phases/02-catalog-discovery/02-03-SUMMARY.md
@.planning/phases/02-catalog-discovery/02-UI-SPEC.md — SEO UI Hooks
@.planning/phases/02-catalog-discovery/02-RESEARCH.md — SEO section, JSON-LD example
@src/app/layout.tsx — title template

<interfaces>
Root layout metadata template: "%s | Техніка б/у Львів"
getEnv().NEXT_PUBLIC_APP_URL for sitemap baseUrl
Product JSON-LD: price = (product.price/100).toFixed(2), priceCurrency UAH, itemCondition UsedCondition, availability InStock
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: schema-dts + JsonLd helper + env NAP</name>
  <read_first>
    - nextjs.org/docs/app/guides/json-ld (via AGENTS.md path if needed)
    - 02-RESEARCH.md — JSON-LD Product example
  </read_first>
  <files>
    package.json
    package-lock.json
    src/components/catalog/json-ld.tsx
    src/lib/env.ts
    .env.example
  </files>
  <action>
    npm install schema-dts@2.0.0.
    json-ld.tsx: export JsonLd({ data }: { data: WithContext&lt;Thing&gt; }) — script type application/ld+json, JSON.stringify replace &lt; with \\u003c.
    Extend env.ts schema with optional STORE_ADDRESS (default Lviv street placeholder), STORE_PHONE; document in .env.example.
  </action>
  <acceptance_criteria>
    - JsonLd component renders without client directive
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Safe JSON-LD primitive + store NAP config</done>
</task>

<task type="auto">
  <name>Task 2: generateMetadata on catalog routes</name>
  <read_first>
    - 02-UI-SPEC.md — metadata titles per route
    - 02-RESEARCH.md — generateMetadata async params; filtered robots
  </read_first>
  <files>
    src/lib/catalog/metadata.ts
    src/lib/catalog/metadata.test.ts
    src/app/(storefront)/katalog/page.tsx
    src/app/(storefront)/katalog/[slug]/page.tsx
    src/app/(storefront)/tovar/[slug]/page.tsx
  </files>
  <action>
    metadata.ts helpers:
    - catalogListingMetadata({ hasActiveFilters }) → title «Каталог б/у техніки у Львові», description; robots noindex when hasActiveFilters
    - categoryMetadata(category) → title `{name} у Львові — б/у техніка`
    - productMetadata(product) → title `{title} — {brand}`, description slice 155, openGraph image first Cloudinary
    Unit test: productMetadata truncates description; hasActiveFilters true when brend or q or stan present.
    Add generateMetadata to katalog/page.tsx, katalog/[slug]/page.tsx, tovar/[slug]/page.tsx using helpers + catalog.service loaders.
    Pass hasActiveFilters from parsed searchParams on list pages.
  </action>
  <acceptance_criteria>
    - Unique titles for category vs product pages in build output
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/lib/catalog/metadata.test.ts && npm run build</automated>
  </verify>
  <done>SEO-01 unique meta on catalog surfaces</done>
</task>

<task type="auto">
  <name>Task 3: JSON-LD + sitemap + e2e</name>
  <read_first>
    - 02-RESEARCH.md — sitemap scope, LocalBusiness
  </read_first>
  <files>
    src/app/(storefront)/page.tsx
    src/app/(storefront)/tovar/[slug]/page.tsx
    src/app/sitemap.ts
    e2e/catalog-seo.spec.ts
  </files>
  <action>
    Home page.tsx: add JsonLd LocalBusiness WithContext — name from PROJECT/store, address Lviv from STORE_ADDRESS, areaServed Lviv, telephone STORE_PHONE optional.
    PDP: JsonLd Product + Offer (UsedCondition, InStock) using schema-dts types; BreadcrumbList optional on PDP and category pages.
    src/app/sitemap.ts: export default async function sitemap — baseUrl from getEnv; entries /, /katalog, all category slugs, all AVAILABLE product /tovar/slug via catalog.service helper (not raw prisma); exclude SOLD/DRAFT/admin/auth.
    e2e/catalog-seo.spec.ts:
    - PDP has script[type="application/ld+json"] with @type Product
    - category page title contains category name
    - fetch /sitemap.xml — contains /tovar/ and /katalog/ and not /admin
  </action>
  <acceptance_criteria>
    - Sitemap returns XML with product URLs
    - PDP JSON-LD present in DOM
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test e2e/catalog-seo.spec.ts</automated>
  </verify>
  <done>SEO-02 structured data + sitemap complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON-LD in HTML | Reflected structured data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06-01 | Tampering | JSON-LD | mitigate | Escape &lt; in stringify; server-only product fields |
| T-02-06-02 | Information Disclosure | metadata | accept | Public catalog data only in meta |
| T-02-06-SC | Tampering | schema-dts npm | mitigate | Approved in 02-01 human verify |

</threat_model>

<verification>
npm test -- src/lib/catalog/metadata.test.ts
npx playwright test e2e/catalog-seo.spec.ts
curl -s http://localhost:3000/sitemap.xml | head -20
</verification>

<success_criteria>
Unique meta, Product + LocalBusiness JSON-LD, sitemap excludes sold products
</success_criteria>

<output>
Create `.planning/phases/02-catalog-discovery/02-06-SUMMARY.md` when done
</output>
