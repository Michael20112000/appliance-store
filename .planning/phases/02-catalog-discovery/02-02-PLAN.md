---
phase: 02-catalog-discovery
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/components/catalog/product-card.tsx
  - src/components/catalog/product-grid.tsx
  - src/components/catalog/condition-badge.tsx
  - src/components/catalog/price-display.tsx
  - src/lib/catalog/format.ts
  - src/app/(storefront)/katalog/[slug]/page.tsx
  - e2e/catalog-list.spec.ts
  - e2e/public-browse.spec.ts
autonomous: true
requirements:
  - CAT-01
  - CAT-03
  - CAT-07
context_decisions:
  - "Component path components/catalog/* per 02-UI-SPEC (not storefront/ from RESEARCH file tree)"
  - "Category page uses notFound() for unknown slug (replace Phase 1 soft inline 404)"
  - "No filter UI in this slice — grid only; filters in 02-04"
  - "Sold products never rendered — rely on service AVAILABLE filter (CAT-07)"

must_haves:
  truths:
    - "Buyer opens /katalog/kholodylnyky and sees product cards with photo, price, brand, condition"
    - "Buyer does not see SOLD products in category grid"
    - "Unknown category slug returns 404 page"
  artifacts:
    - path: src/components/catalog/product-card.tsx
      provides: Card tile linking to /tovar/[slug]
      min_lines: 40
    - path: src/app/(storefront)/katalog/[slug]/page.tsx
      provides: Category-scoped product grid
    - path: e2e/catalog-list.spec.ts
      provides: CAT-01 e2e coverage
  key_links:
    - from: src/app/(storefront)/katalog/[slug]/page.tsx
      to: src/server/services/catalog.service.ts
      via: listPublicProducts({ categoryId })
      pattern: listPublicProducts
    - from: src/components/catalog/product-card.tsx
      to: /tovar/
      via: Link href
      pattern: /tovar/\$\{

---

## Phase Goal

**As a** покупець, **I want to** переглядати товари в обраній категорії, **so that** я швидко знаходжу потрібну б/у техніку (CAT-01, CAT-03).

<objective>
Замінити stub `/katalog/[slug]` на сітку ProductCard з даними з catalog.service; прибрати «Незабаром».

Purpose: Перший видимий каталог для покупця після data slice.
Output: Category listing RSC + catalog components + e2e.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-catalog-discovery/02-01-SUMMARY.md
@.planning/phases/02-catalog-discovery/02-UI-SPEC.md — ProductCard, ProductGrid, condition badges, copy
@.planning/phases/02-catalog-discovery/02-PATTERNS.md — category-grid analog
@src/components/home/category-grid.tsx
@src/components/media/optimized-image.tsx
@src/app/(storefront)/katalog/[slug]/page.tsx

<interfaces>
From catalog.service (02-01):
- listPublicProducts({ categoryId, page?, pageSize? }) → { items, total, page, pageSize }
- getCategoryBySlug(slug) or use prisma.category.findUnique in page only for category metadata — prefer add getCategoryBySlug(slug) to catalog.service if not in 02-01

From OptimizedImage:
- src={cloudinaryPublicId}, alt required, fill, sizes prop

UA condition labels (02-UI-SPEC / RESEARCH):
- LIKE_NEW → «Як нова», GOOD → «Добрий стан», FAIR → «Задовільний»
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Catalog display primitives + format helpers</name>
  <read_first>
    - 02-UI-SPEC.md — ProductCard layout, ConditionBadge, PriceDisplay, typography
    - src/components/home/category-grid.tsx — Card + Link grid pattern
  </read_first>
  <files>
    src/lib/catalog/format.ts
    src/components/catalog/condition-badge.tsx
    src/components/catalog/price-display.tsx
    src/components/catalog/product-card.tsx
    src/components/catalog/product-grid.tsx
  </files>
  <action>
    format.ts: formatPriceKopiyky(kop: number) → "12 500 ₴" with tabular nums; conditionLabelUa(enum); pluralResultsUa(n) for later toolbar.
    ConditionBadge: shadcn Badge variants per 02-UI-SPEC table (LIKE_NEW secondary, GOOD secondary, FAIR outline).
    PriceDisplay: wraps formatted price with text-lg font-semibold tabular-nums (card) — accept size prop for PDP reuse.
    ProductCard: Server Component; Card + Link to `/tovar/${slug}`; OptimizedImage aspect-[4/3] first image; ConditionBadge absolute top-2 left-2; title line-clamp-2; brand muted; aria-label = title + price UA.
    ProductGrid: grid grid-cols-2 gap-4 md:grid-cols-4 md:gap-6; empty slot prop for empty state (category-specific copy passed from page).
    No "use client" on card/grid.
  </action>
  <acceptance_criteria>
    - Components compile without client hooks
    - Price renders kopiyky correctly (e.g. 1250000 → 12 500 ₴)
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | head -40</automated>
  </verify>
  <done>Reusable catalog UI primitives ready for pages</done>
</task>

<task type="auto">
  <name>Task 2: Category listing page (replace stub)</name>
  <read_first>
    - 02-UI-SPEC.md — /katalog/[categorySlug] layout, empty copy, H1 = category.name
    - AGENTS.md — await params Promise API
  </read_first>
  <files>src/app/(storefront)/katalog/[slug]/page.tsx</files>
  <action>
    Replace Phase 1 stub entirely. PageProps: params Promise, no searchParams yet.
    await params → slug; load category via catalog.service getCategoryBySlug (add thin helper in service if missing) — if null call notFound() from next/navigation.
    listPublicProducts({ categoryId: category.id, page: 1, pageSize: 24 }) — do not import prisma in page.
    Layout: mx-auto max-w-6xl px-4 py-12 sm:px-6; H1 category.name; optional description muted; ProductGrid with items.
    Empty state: 02-UI-SPEC category empty heading/body + Link «До каталогу» → /katalog (page exists after 02-04 — use href /katalog anyway).
    Remove Badge «Незабаром» and stub copy.
    export const metadata with static title template child: `${category.name}` deferred full generateMetadata to 02-06 — use simple metadata export with title from category in async generateMetadata minimal stub OR static until 02-06; if adding generateMetadata here keep title `${category.name}` only (SEO-01 completed in 02-06).
  </action>
  <acceptance_criteria>
    - /katalog/kholodylnyky shows at least one product card when seeded
    - Invalid slug triggers Next notFound UI
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Category route shows real product grid from DB</done>
</task>

<task type="auto">
  <name>Task 3: E2E catalog list + update public-browse</name>
  <read_first>
    - e2e/public-browse.spec.ts — existing pattern
    - 02-VALIDATION.md — CAT-01, CAT-03, CAT-07 mapping
  </read_first>
  <files>e2e/catalog-list.spec.ts, e2e/public-browse.spec.ts</files>
  <action>
    Create e2e/catalog-list.spec.ts:
    - goto /katalog/kholodylnyky — expect product card link href matches /tovar/
    - expect visible price pattern (₴) and condition badge text (one of UA labels)
    - expect getByText("Продано").toHaveCount(0) on category page
    - goto invalid slug — expect «Категорію не знайдено» or not-found copy
    Update public-browse.spec.ts: remove expectation of «Незабаром» on kholodylnyky; assert product grid instead.
  </action>
  <acceptance_criteria>
    - Playwright catalog-list spec passes against dev server baseURL
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test e2e/catalog-list.spec.ts e2e/public-browse.spec.ts</automated>
  </verify>
  <done>Automated proof of CAT-01/03/07 on category grid</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → RSC category page | Public read-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Information Disclosure | product-card | accept | Only public DTO fields passed from server |
| T-02-02-02 | Spoofing | category slug | mitigate | notFound on unknown slug; no IDOR (public data) |

</threat_model>

<verification>
npm run build
npx playwright test e2e/catalog-list.spec.ts
</verification>

<success_criteria>
Category pages list AVAILABLE products with photo, price, brand, condition; stubs removed
</success_criteria>

<output>
Create `.planning/phases/02-catalog-discovery/02-02-SUMMARY.md` when done
</output>
