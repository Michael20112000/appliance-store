---
phase: 02-catalog-discovery
plan: 04
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - package.json
  - package-lock.json
  - src/lib/catalog/search-params.ts
  - src/lib/catalog/search-params.test.ts
  - src/server/services/catalog.service.ts
  - src/server/services/catalog.service.test.ts
  - src/app/(storefront)/layout.tsx
  - src/app/(storefront)/katalog/page.tsx
  - src/components/catalog/catalog-toolbar.tsx
  - src/components/catalog/catalog-filters.tsx
  - src/components/catalog/catalog-filters-sheet.tsx
  - src/components/catalog/active-filter-chips.tsx
  - src/components/layout/store-header.tsx
  - src/app/(storefront)/katalog/[slug]/page.tsx
  - e2e/catalog-filters-url.spec.ts
autonomous: false
requirements:
  - CAT-05
  - CAT-06
context_decisions:
  - "URL params per 02-UI-SPEC: q, brend, cina-vid, cina-do, stan (multi), sort, сторінка — NOT RESEARCH brand/min/max/cursor"
  - "Pagination: page-based сторінка (1-based, pageSize 24) per UI-SPEC — overrides RESEARCH cursor pagination"
  - "nuqs urlKeys map English parser names to UA query keys (brend, cina-vid, etc.)"
  - "useQueryStates({ shallow: false }) mandatory so RSC refetches (02-RESEARCH Pitfall 2)"
  - "On category pages category comes from path only — do not duplicate kategoria in query"

must_haves:
  truths:
    - "Buyer opens /katalog and sees all AVAILABLE products with filter sidebar"
    - "Buyer changes brand/price/condition filters and URL updates; reload preserves filters"
    - "Buyer shares filter URL and recipient sees same results"
  artifacts:
    - path: src/lib/catalog/search-params.ts
      provides: catalogParsers + catalogSearchParamsCache
    - path: src/app/(storefront)/katalog/page.tsx
      provides: Global catalog with filters
    - path: src/components/catalog/catalog-filters.tsx
      provides: Client nuqs filter UI
  key_links:
    - from: src/components/catalog/catalog-filters.tsx
      to: URL query string
      via: nuqs useQueryStates shallow false
      pattern: useQueryStates
    - from: src/app/(storefront)/katalog/page.tsx
      to: catalogSearchParamsCache.parse
      via: await parse(searchParams)
      pattern: catalogSearchParamsCache\.parse

---

## Phase Goal

**As a** покупець, **I want to** фільтрувати товари за брендом, ціною і станом з посиланням, яким можна поділитися, **so that** я швидко звужу вибір (CAT-05, CAT-06).

<objective>
Встановити nuqs, глобальний `/katalog`, FilterBar/Sheet, синхронізація фільтрів з URL на `/katalog` і `/katalog/[slug]`.

Purpose: Discovery filters + shareable URLs.
Output: nuqs wired, filter UI, extended catalog.service where clauses.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-catalog-discovery/02-02-SUMMARY.md
@.planning/phases/02-catalog-discovery/02-UI-SPEC.md — Routing & URL Contract, CatalogFilters, Toolbar
@.planning/phases/02-catalog-discovery/02-RESEARCH.md — Pattern 2 nuqs, shallow false
@src/app/(storefront)/katalog/[slug]/page.tsx

<interfaces>
catalogParsers (implement with nuqs/server):
- q: parseAsString.withDefault("")
- brend: parseAsString (urlKey brend)
- cinaVid: parseAsInteger (urlKey cina-vid) — whole UAH
- cinaDo: parseAsInteger (urlKey cina-do)
- stan: parseAsArrayOf(parseAsStringEnum(["LIKE_NEW","GOOD","FAIR"])) or repeat keys per UI-SPEC
- sort: parseAsStringEnum(["novi","cina-asc","cina-desc"]).withDefault("novi")
- storinka: parseAsInteger.withDefault(1) (urlKey сторінка)

export catalogSearchParamsCache = createSearchParamsCache(catalogParsers)

buildPublicProductWhere extends: brend → brand exact; cina-vid/do → price gte/lte in kopiyky (multiply UAH×100); stan → condition in; sort → orderBy
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install nuqs + search-params cache + service filters</name>
  <read_first>
    - 02-RESEARCH.md — nuqs install 2.8.9, createSearchParamsCache
    - node_modules/nuqs or nuqs.dev server-side docs
  </read_first>
  <files>
    package.json
    package-lock.json
    src/lib/catalog/search-params.ts
    src/lib/catalog/search-params.test.ts
    src/server/services/catalog.service.ts
    src/server/services/catalog.service.test.ts
  </files>
  <action>
    npm install nuqs@2.8.9 (schema-dts deferred to 02-06).
    Implement search-params.ts with parsers and urlKeys for UA params per 02-UI-SPEC table.
    Extend buildPublicProductWhere for brend, price range (convert UAH to kopiyky), stan in[], validate max bounds in validators (e.g. cina-do <= 500000 UAH).
    listPublicProducts: accept parsed filters + sort + page (storinka); compute skip = (page-1)*pageSize for page-based pagination; return { items, total, page, pageSize, totalPages }.
    Unit tests: parser round-trip; where builder with brand+price+condition; page cap.
  </action>
  <acceptance_criteria>
    - npm test search-params + catalog.service passes
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/lib/catalog/search-params.test.ts src/server/services/catalog.service.test.ts</automated>
  </verify>
  <done>Server and client share typed filter contract</done>
</task>

<task type="auto">
  <name>Task 2: NuqsAdapter + global /katalog page shell</name>
  <read_first>
    - 02-UI-SPEC.md — /katalog layout, H1, sidebar filters
    - src/app/(storefront)/layout.tsx
  </read_first>
  <files>
    src/app/(storefront)/layout.tsx
    src/app/(storefront)/katalog/page.tsx
    src/components/layout/store-header.tsx
  </files>
  <action>
    Wrap storefront main children with NuqsAdapter from nuqs/adapters/next/app inside layout.tsx (children only, not header/footer).
    Create /katalog/page.tsx: await catalogSearchParamsCache.parse(searchParams); listPublicProducts without categoryId; H1 «Каталог б/у техніки»; subcopy «Усі товари в наявності у Львові».
    Desktop: sidebar CatalogFilters (md+) + grid; mobile: CatalogToolbar + CatalogFiltersSheet.
    Add header nav link «Каталог» → /katalog (02-UI-SPEC).
    shadcn add: breadcrumb checkbox select slider pagination separator — run npx shadcn@latest add breadcrumb checkbox select slider pagination separator.
  </action>
  <acceptance_criteria>
    - GET /katalog returns 200 with product grid when seeded
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Global catalog route live with nuqs adapter</done>
</task>

<task type="auto">
  <name>Task 3: Filter UI + category page wiring + e2e URL sync</name>
  <read_first>
    - 02-UI-SPEC.md — CatalogFilters, ActiveFilterChips, mobile sheet copy
    - src/components/auth/auth-form.tsx — client component pattern
  </read_first>
  <files>
    src/components/catalog/catalog-toolbar.tsx
    src/components/catalog/catalog-filters.tsx
    src/components/catalog/catalog-filters-sheet.tsx
    src/components/catalog/active-filter-chips.tsx
    src/app/(storefront)/katalog/[slug]/page.tsx
    e2e/catalog-filters-url.spec.ts
  </files>
  <action>
    catalog-filters.tsx (client): useQueryStates(catalogParsers, { shallow: false }); brand Select from getDistinctBrands(); price Slider step 500 + inputs; condition Checkbox group; **«Категорія» section** — list all categories as links to `/katalog/[slug]` (highlight active slug on category pages; on global `/katalog` none active); desktop live apply; mobile sheet footer «Показати результати».
    catalog-toolbar.tsx: sort Select (novi, cina-asc, cina-desc); results count pluralResultsUa; mobile filter button opens sheet.
    active-filter-chips.tsx: removable chips calling null setter per param.
    Update katalog/[slug]/page.tsx: add searchParams Promise; parse cache; pass categoryId + filters to listPublicProducts; include toolbar + filters (category list highlights active).
    **Pagination:** wire shadcn Pagination on `/katalog` and `/katalog/[slug]` bound to `storінka` parser (pageSize 24); disable prev/next at bounds.
    **Loading/error:** add `loading.tsx` with `CatalogSkeleton` per 02-UI-SPEC; error boundary or destructive Alert + «Спробувати знову» (`router.refresh()`).
    Empty filtered: «Товарів не знайдено» + «Скинути фільтри» clears parsers.
    e2e/catalog-filters-url.spec.ts: goto /katalog/pralni-mashyny?stan=GOOD; expect URL contains stan=GOOD; expect no «Продано»; change brand filter → URL updates brend=; click category link from `/katalog` sidebar → navigates to `/katalog/[slug]`.
  </action>
  <acceptance_criteria>
    - Filter change updates URL and grid without manual refresh
    - Shared URL loads filtered results
    - Category filter links navigate to category pages (CAT-05)
    - Pagination changes `сторінка` in URL and updates grid
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test e2e/catalog-filters-url.spec.ts</automated>
  </verify>
  <done>CAT-05 and CAT-06 satisfied on catalog routes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser URL → server parse | Untrusted query input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Tampering | search params | mitigate | nuqs parsers + Zod bounds on price/page; Prisma parameterized queries only |
| T-02-04-02 | Denial of Service | listPublicProducts | mitigate | pageSize max 24; reject storinka &lt; 1 or &gt; 1000 |
| T-02-04-SC | Tampering | nuqs npm | mitigate | Human verify in 02-01; install here after approval |

</threat_model>

<verification>
npm test -- src/lib/catalog/search-params.test.ts
npx playwright test e2e/catalog-filters-url.spec.ts
</verification>

<success_criteria>
Filters work on /katalog and category pages; URL is shareable; only AVAILABLE products shown
</success_criteria>

<output>
Create `.planning/phases/02-catalog-discovery/02-04-SUMMARY.md` when done
</output>
