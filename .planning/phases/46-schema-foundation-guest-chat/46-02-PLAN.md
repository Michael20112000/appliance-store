---
phase: 46-schema-foundation-guest-chat
plan: 02
type: execute
wave: 2
depends_on:
  - 46-01
files_modified:
  - src/types/chat.ts
  - src/server/validators/chat.ts
  - src/server/validators/chat.test.ts
  - src/server/services/chat.service.ts
  - src/server/services/chat.service.test.ts
autonomous: true
requirements:
  - CHAT-01
  - CHAT-03

must_haves:
  truths:
    - "ConversationSummaryDto.userId is string | null (nullable after migration)"
    - "sendMessageSchema accepts optional guestToken as z.string().uuid()"
    - "getOrCreateConversation uses findFirst({ where: { userId, isActive: true } })"
    - "getConversationForBuyer uses findFirst({ where: { userId, isActive: true } })"
    - "getOrCreateGuestConversation creates on first message with P2002 race guard"
    - "getGuestConversation looks up by guestToken uniquely"
    - "resolveConversationForSend has a guestToken branch before the userId check"
    - "listConversationsForAdmin filters null userIds, uses 'Гість' as fallback name"
    - "assertConversationAccess return type is string | null for userId"
    - "All new service tests pass"
  artifacts:
    - path: "src/types/chat.ts"
      provides: "ConversationSummaryDto with nullable userId"
      contains: "userId: string | null"
    - path: "src/server/validators/chat.ts"
      provides: "Extended sendMessageSchema with guestToken UUID field"
      contains: "guestToken"
    - path: "src/server/services/chat.service.ts"
      provides: "All guest service functions + D-07 updated existing functions"
      exports:
        - "getOrCreateGuestConversation"
        - "getGuestConversation"
    - path: "src/server/services/chat.service.test.ts"
      provides: "Test coverage for all new service behaviors"
      contains: "getOrCreateGuestConversation"
  key_links:
    - from: "src/server/validators/chat.ts"
      to: "src/server/services/chat.service.ts"
      via: "SendMessageInput type extended with guestToken"
      pattern: "guestToken"
    - from: "src/server/services/chat.service.ts"
      to: "src/generated/prisma"
      via: "Prisma client queries with new nullable fields"
      pattern: "findFirst.*isActive"
---

<objective>
Update the data layer: types, validators, and service functions to support guest conversations after the schema migration. This plan is fully autonomous and produces all the building blocks that API routes in Plan 03 depend on.

Purpose: The schema now has nullable userId, guestToken, and isActive. The TypeScript types and service functions must reflect this — otherwise the API layer cannot be written without type errors.

Output: Updated src/types/chat.ts, src/server/validators/chat.ts, src/server/services/chat.service.ts, and comprehensive test coverage in chat.service.test.ts and chat.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/46-schema-foundation-guest-chat/46-CONTEXT.md
@.planning/phases/46-schema-foundation-guest-chat/46-PATTERNS.md

<interfaces>
<!-- Current types — executor needs these to understand what must change -->

src/types/chat.ts (current, lines 15–24):
```typescript
export type ConversationSummaryDto = {
  id: string;
  userId: string;           // must become: userId: string | null
  status: ConversationStatus;
  buyerName: string;
  buyerEmail: string;
  lastMessagePreview: string | null;
  lastMessageAt: string | null;
  unreadForAdmin: boolean;
};
```

src/server/validators/chat.ts (current, full file):
```typescript
import { z } from "zod";
export const sendMessageSchema = z.object({
  body: z.string().trim().min(1, "Повідомлення не може бути порожнім").max(2000, "Повідомлення занадто довге (максимум 2000 символів)"),
  conversationId: z.string().cuid("Невірний ідентифікатор розмови").optional(),
  productId: z.string().cuid("Невірний ідентифікатор товару").optional(),
});
export type SendMessageInput = z.infer<typeof sendMessageSchema>;
```

src/server/services/chat.service.ts — key current function signatures:
- `export async function getOrCreateConversation(userId: string, context?: ProductContext)` — line 89: uses `findUnique({ where: { userId } })`
- `export async function getConversationForBuyer(userId: string)` — line 144: uses `findUnique({ where: { userId } })`
- `async function resolveConversationForSend(input: SendMessageInput)` — line 228: no guestToken branch
- `export async function listConversationsForAdmin(options: { status: ConversationStatus })` — line 303: passes null userId to user findMany
- `export async function assertConversationAccess(session, conversationId)` — line 344: return type has `userId: string`
- `type SendMessageInput` (local) — line 176: no guestToken field
- Error constants — lines 11–14: CONVERSATION_NOT_FOUND, FORBIDDEN, CHAT_RATE_LIMIT, CHAT_ARCHIVED

Error class pattern (lines 24–42):
```typescript
export class ChatServiceError extends Error {
  constructor(public readonly code: string, message: string) {
    super(message);
    this.name = "ChatServiceError";
  }
}
export class ChatRateLimitError extends ChatServiceError { ... }
```

isUniqueViolation guard (lines 80–87) — reuse as-is:
```typescript
function isUniqueViolation(error: unknown): boolean {
  return error !== null && typeof error === "object" && "code" in error && error.code === "P2002";
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update types, validators, and test coverage for guest fields</name>
  <files>src/types/chat.ts, src/server/validators/chat.ts, src/server/validators/chat.test.ts</files>
  <read_first>
    - src/types/chat.ts (current ConversationSummaryDto)
    - src/server/validators/chat.ts (current sendMessageSchema)
    - src/server/validators/chat.test.ts (existing test cases to keep + extend)
    - .planning/phases/46-schema-foundation-guest-chat/46-PATTERNS.md (sections for types/chat.ts and validators/chat.ts)
  </read_first>
  <behavior>
    - Test: `sendMessageSchema.safeParse({ body: "Привіт", guestToken: "123e4567-e89b-12d3-a456-426614174000" })` → `success: true`
    - Test: `sendMessageSchema.safeParse({ body: "Привіт", guestToken: "not-a-uuid" })` → `success: false`
    - Test: `sendMessageSchema.safeParse({ body: "Привіт" })` → `success: true` (guestToken is optional)
    - Existing tests must still pass (rejects empty body, rejects >2000 chars, accepts trimmed body, accepts optional conversationId)
  </behavior>
  <action>
    Step 1 — src/types/chat.ts: Change `userId: string` to `userId: string | null` in ConversationSummaryDto. No other changes to this file.

    Step 2 — src/server/validators/chat.ts: Add `guestToken: z.string().uuid("Невірний гостьовий токен").optional()` to sendMessageSchema, after the productId field. The exported SendMessageInput type is inferred automatically and will include the new field.

    Step 3 — src/server/validators/chat.test.ts: Add two new test cases to the existing `describe("sendMessageSchema")` block:
    - "accepts optional guestToken as UUID" — parse `{ body: "Привіт", guestToken: "123e4567-e89b-12d3-a456-426614174000" }`, expect success true
    - "rejects guestToken that is not a UUID" — parse `{ body: "Привіт", guestToken: "not-a-uuid" }`, expect success false

    Do not delete any existing test cases.
  </action>
  <verify>
    <automated>npx vitest run src/server/validators/chat.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - ConversationSummaryDto.userId is `string | null`
    - sendMessageSchema has `guestToken: z.string().uuid().optional()`
    - All existing + 2 new validator tests pass
  </done>
  <acceptance_criteria>
    - `grep "userId: string | null" src/types/chat.ts` returns a match
    - `grep "guestToken" src/server/validators/chat.ts` returns a match with `.uuid(`
    - `npx vitest run src/server/validators/chat.test.ts` exits 0 with no failures
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update chat.service.ts with guest functions and D-07 fixes</name>
  <files>src/server/services/chat.service.ts, src/server/services/chat.service.test.ts</files>
  <read_first>
    - src/server/services/chat.service.ts (full file — all function implementations)
    - src/server/services/chat.service.test.ts (all existing tests — must not break)
    - .planning/phases/46-schema-foundation-guest-chat/46-PATTERNS.md (Pattern Assignments for chat.service.ts — every code snippet is an exact target state)
    - .planning/phases/46-schema-foundation-guest-chat/46-CONTEXT.md (D-07, D-08, D-03 decisions)
  </read_first>
  <behavior>
    New service tests (add to chat.service.test.ts):

    getOrCreateGuestConversation:
    - "creates conversation on first message" — mock findUnique returns null, mock create returns `{ id: "conv-g1", guestToken: "uuid-1", isActive: true }` → result equals mock
    - "returns existing conversation on retry (P2002 race)" — mock findUnique returns null, mock create throws `{ code: "P2002" }`, mock findUniqueOrThrow returns `{ id: "conv-g1" }` → result equals mock
    - "returns existing conversation directly" — mock findUnique returns `{ id: "conv-g1", guestToken: "uuid-1" }` → result equals mock, create not called

    getGuestConversation:
    - "returns null for unknown token" — mock findUnique returns null → result is null
    - "returns conversation for known token" — mock findUnique returns `{ id: "conv-g1", guestToken: "uuid-1" }` → result matches

    listConversationsForAdmin (extend existing describe block):
    - "uses 'Гість' as buyerName when userId is null" — mock findMany returns `[{ id: "conv-g1", userId: null, status: "OPEN", lastMessagePreview: "hi", lastMessageAt: new Date(), lastMessageSender: "BUYER", adminLastReadAt: new Date(Date.now() - 1000) }]`, mock user.findMany returns `[]` → result[0].buyerName === "Гість"

    assertConversationAccess (extend existing describe block):
    - "throws FORBIDDEN for buyer accessing guest conversation (null userId)" — mock findUnique returns `{ id: "conv-g1", userId: null, status: "OPEN" }`, call with buyerSession → rejects with code FORBIDDEN
  </behavior>
  <action>
    Make these changes to src/server/services/chat.service.ts:

    1. Add two new error constants after CHAT_ARCHIVED (line 14):
       `export const GUEST_TOKEN_INVALID = "GUEST_TOKEN_INVALID";`
       `export const GUEST_NOT_FOUND = "GUEST_NOT_FOUND";`

    2. Add `guestToken?: string` to the local `type SendMessageInput` (line 176 area), after `productContext?: ProductContext`.

    3. Update `getOrCreateConversation` (D-07): change `findUnique({ where: { userId } })` to `findFirst({ where: { userId, isActive: true } })`. Change the catch block's `findUniqueOrThrow({ where: { userId } })` to `findFirstOrThrow({ where: { userId, isActive: true } })`.

    4. Update `getConversationForBuyer` (D-07): change `findUnique({ where: { userId } })` to `findFirst({ where: { userId, isActive: true } })`.

    5. Add new exported function `getGuestConversation(guestToken: string)` after `getConversationForBuyer`: uses `prisma.conversation.findUnique({ where: { guestToken } })`.

    6. Add new exported function `getOrCreateGuestConversation(guestToken: string, context?: ProductContext)` after `getGuestConversation`. Follows the same upsert-guard pattern as `getOrCreateConversation`: try findUnique first, then create with `{ guestToken, isActive: true, contextProductId, contextProductTitle }`, catch P2002 → findUniqueOrThrow({ where: { guestToken } }).

    7. Update `resolveConversationForSend` (D-08): add a `guestToken` branch before the `!input.userId` check:
       `if (input.guestToken) { return getOrCreateGuestConversation(input.guestToken, input.productContext); }`
       Update the error message for missing userId to: "Потрібен userId, guestToken або conversationId"

    8. Update `listConversationsForAdmin` (D-07, CHAT-03):
       - Replace `conversations.map((c) => c.userId)` passed to `prisma.user.findMany` with a filtered version: `const userIds = conversations.map((c) => c.userId).filter((id): id is string => id !== null);`
       - Update `prisma.user.findMany({ where: { id: { in: userIds } } })`
       - Replace `const buyer = userById.get(conversation.userId)` with `const buyer = conversation.userId ? userById.get(conversation.userId) : null`
       - Change fallback `"Покупець"` to `"Гість"` in `buyerName: buyer?.name ?? "Гість"`

    9. Update `assertConversationAccess` return type annotation from `userId: string` to `userId: string | null`. Update the ownership check from `conversation.userId !== session.user.id` to `!conversation.userId || conversation.userId !== session.user.id` (null userId = guest conversation, always forbidden for non-admin buyers).

    10. Ensure `getOrCreateGuestConversation` and `getGuestConversation` are exported.

    Update src/server/services/chat.service.test.ts:
    - Add `getOrCreateGuestConversation`, `getGuestConversation`, `GUEST_TOKEN_INVALID`, `GUEST_NOT_FOUND` to the import statement at the top.
    - Add `findFirst: vi.fn()`, `findFirstOrThrow: vi.fn()` to the `prisma.conversation` mock (the mock currently has `findUnique` and `findUniqueOrThrow` — add the `findFirst` variants alongside them).
    - Add test describe blocks as specified in the `<behavior>` section above.
    - Existing tests that use `findUnique` for `getOrCreateConversation` will now use `findFirst` — update those mocks: change `vi.mocked(prisma.conversation.findUnique)` to `vi.mocked(prisma.conversation.findFirst)` in the three existing `getOrCreateConversation` tests.
    - The `assertConversationAccess` tests still use `findUnique` (assertConversationAccess itself uses findUnique({ where: { id } })) — those mocks are correct, leave them.
    - `listConversationsForAdmin` existing test passes `userId: "buyer-1"` (non-null) — that test should still pass after the null-filter change.
  </action>
  <verify>
    <automated>npx vitest run src/server/services/chat.service.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - getOrCreateConversation uses findFirst with isActive: true
    - getConversationForBuyer uses findFirst with isActive: true
    - getOrCreateGuestConversation exported, creates conversation, handles P2002
    - getGuestConversation exported, looks up by guestToken
    - resolveConversationForSend has guestToken branch
    - listConversationsForAdmin filters nulls, uses "Гість" fallback
    - assertConversationAccess returns userId: string | null, blocks buyer on null userId
    - All existing + new service tests pass
  </done>
  <acceptance_criteria>
    - `grep "getOrCreateGuestConversation\|getGuestConversation" src/server/services/chat.service.ts | grep "export"` returns 2 matches
    - `grep "Гість" src/server/services/chat.service.ts` returns a match in listConversationsForAdmin
    - `grep "findFirst" src/server/services/chat.service.ts` returns matches for getOrCreateConversation and getConversationForBuyer
    - `grep "userId: string | null" src/server/services/chat.service.ts` returns a match in assertConversationAccess return type
    - `npx vitest run src/server/services/chat.service.test.ts` exits 0 with no failures
    - `npx vitest run src/server/validators/chat.test.ts` still exits 0 (no regression)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API input → service layer | sendMessageSchema validates guestToken before it reaches service functions |
| Guest token → DB lookup | guestToken from request body must be UUID-validated before any DB query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-03 | Tampering | sendMessageSchema guestToken field | mitigate | `z.string().uuid()` rejects non-UUID tokens at API boundary before any DB query |
| T-46-04 | Elevation of Privilege | assertConversationAccess null userId | mitigate | `!conversation.userId` check added — guests (null userId) cannot be claimed by authenticated buyers |
| T-46-05 | Denial of Service | getOrCreateGuestConversation race | mitigate | isUniqueViolation P2002 guard prevents duplicate conversation creation under concurrent requests |
| T-46-SC | Tampering | npm/pip/cargo installs | accept | Phase 46 installs zero new packages |
</threat_model>

<verification>
After both tasks complete:
1. `npx vitest run src/server/validators/chat.test.ts src/server/services/chat.service.test.ts` exits 0
2. `grep "userId: string | null" src/types/chat.ts` confirms DTO is updated
3. `grep "Гість" src/server/services/chat.service.ts` confirms CHAT-03 fallback
4. `grep "findFirst.*isActive" src/server/services/chat.service.ts` confirms D-07 applied
5. `npx tsc --noEmit 2>&1 | head -10` shows 0 new type errors
</verification>

<success_criteria>
- All types propagated: ConversationSummaryDto.userId is string | null, sendMessageSchema has guestToken UUID field
- Service layer: getOrCreateGuestConversation and getGuestConversation exported and tested
- D-07: getOrCreateConversation and getConversationForBuyer use findFirst with isActive:true
- CHAT-03: listConversationsForAdmin returns "Гість" for null userId conversations
- All service and validator tests pass (no regressions)
</success_criteria>

<output>
Create `.planning/phases/46-schema-foundation-guest-chat/46-02-SUMMARY.md` when done
</output>
