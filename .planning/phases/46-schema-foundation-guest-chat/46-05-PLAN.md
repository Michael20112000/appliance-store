---
phase: 46-schema-foundation-guest-chat
plan: 05
type: execute
wave: 5
depends_on:
  - 46-04
files_modified: []
autonomous: false
requirements:
  - CHAT-01
  - CHAT-03

must_haves:
  truths:
    - "Unauthenticated user opens the chat widget — composer shown immediately, no redirect to /uviity"
    - "Guest sends a message — message appears in admin inbox labeled 'Гість'"
    - "Guest refreshes the page — previous guest messages are still visible in the widget"
    - "Guest clears localStorage — widget treats them as a brand-new guest with no prior session"
    - "Vitest full suite passes (excluding the 2 pre-existing seed failures)"
  artifacts:
    - path: "prisma/migrations"
      provides: "Applied migration with guest_chat_schema"
    - path: "src/app/api/chat/guest/route.ts"
      provides: "Live GET endpoint returning 404/200"
  key_links:
    - from: "Browser (incognito)"
      to: "chat widget"
      via: "openPanel() no longer redirects"
      pattern: "no /uviity in address bar"
    - from: "Guest message"
      to: "admin inbox"
      via: "listConversationsForAdmin buyerName fallback"
      pattern: "Гість label"
---

<objective>
Human verification of the complete Phase 46 end-to-end guest chat flow. All automated work is done — this plan verifies the four success criteria from the ROADMAP against the live running app.

Purpose: Integration tests cannot cover real localStorage + DB + Pusher round-trips. A human must confirm the observable behaviors specified in the phase success criteria.

Output: Confirmed working guest chat flow, or a list of specific failures to fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/46-schema-foundation-guest-chat/46-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and TypeScript compile check</name>
  <files></files>
  <read_first>
    - .planning/phases/46-schema-foundation-guest-chat/46-VALIDATION.md (sampling rate and pre-existing failure count)
  </read_first>
  <action>
    Run the automated checks before asking for human verification:
    1. `npx tsc --noEmit 2>&1 | head -20` — expect 0 errors
    2. `npx vitest run 2>&1 | tail -20` — expect pass count increased by new tests; exactly 2 failures allowed (prisma/seed.test.ts pre-existing)

    If either check fails, fix the issue before proceeding to the human verification task.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -5 && npx tsc --noEmit 2>&1 | grep -c "error TS" || echo "0"</automated>
  </verify>
  <done>
    - `npx vitest run` exits with ≤2 test failures (the 2 pre-existing seed failures)
    - `npx tsc --noEmit` exits with 0 errors
  </done>
  <acceptance_criteria>
    - `npx vitest run 2>&1 | grep "Tests.*failed\|Test Files.*failed"` shows ≤2 failures
    - `npx tsc --noEmit 2>&1 | grep "error TS"` returns 0 lines
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Verify phase success criteria in the live app</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 46 Success Criteria section — four criteria to verify)
  </read_first>
  <what-built>
    Complete guest chat system:
    - Schema migrated (nullable userId, guestToken, isActive)
    - GET /api/chat/guest restore endpoint
    - POST /api/chat/messages extended for guests (guestToken path)
    - POST /api/chat/pusher/auth extended for guest Pusher auth
    - ChatProvider: no redirect for guests, localStorage token management
    - "Гість" fallback in admin inbox
  </what-built>
  <how-to-verify>
    Start the dev server: `npm run dev`

    Test 1 — Widget opens without redirect:
    - Open an incognito browser window (no session)
    - Navigate to http://localhost:3000
    - Click the chat floating action button
    - EXPECT: Chat widget opens and shows the message composer immediately. The URL does NOT change to /uviity. The address bar stays on http://localhost:3000.

    Test 2 — Guest sends a message and appears as "Гість" in admin:
    - In the incognito window, type a message and press Send
    - EXPECT: Message appears in the chat widget
    - In another window, open http://localhost:3000/admin/chaty while logged in as admin
    - EXPECT: The guest conversation appears in the inbox. The buyer name shown is "Гість" (not blank, not null, not "Покупець")

    Test 3 — Messages persist across page refresh:
    - Remaining in the incognito window, press F5 or Ctrl+R to refresh the page
    - Click the chat button to open the widget again
    - EXPECT: The message(s) sent in Test 2 are still visible in the widget

    Test 4 — Clear localStorage resets session:
    - In the incognito window, open DevTools → Application → Local Storage → http://localhost:3000
    - Delete the `chat_guest_token` key
    - Refresh the page and open the chat widget
    - EXPECT: The widget shows no previous messages (empty chat, new guest session)

    If any test fails, note exactly what happened (e.g., "redirect occurred", "inbox shows blank name", "messages not restored") and describe in your resume message.
  </how-to-verify>
  <resume-signal>Type "verified" if all 4 tests pass, or describe exactly which test failed and what was observed</resume-signal>
  <acceptance_criteria>
    <human-check>
      - Test 1: Widget opens without redirect to /uviity
      - Test 2: Admin inbox shows "Гість" for the guest conversation (not blank, null, or "Покупець")
      - Test 3: After page refresh, previously sent messages are still visible in the widget
      - Test 4: After clearing localStorage, widget shows no previous messages
    </human-check>
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human verifier → dev app | Incognito window ensures no stale auth session interferes with guest flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-13 | Spoofing | Dev server auth state bleeds into incognito test | mitigate | Use incognito window with fresh profile; verify no session cookie exists before starting Test 1 |
| T-46-SC | Tampering | npm/pip/cargo installs | accept | Phase 46 installs zero new packages |
</threat_model>

<verification>
Phase complete when:
1. `npx vitest run` exits with ≤2 failures (only pre-existing seed failures)
2. `npx tsc --noEmit` exits with 0 errors
3. Human confirms all 4 success criteria from ROADMAP Phase 46 are met
</verification>

<success_criteria>
All four ROADMAP Phase 46 success criteria verified by a human in the live app:
1. Unauthenticated user opens chat widget — composer shown immediately, no redirect to /uviity
2. Guest sends message — admin inbox shows "Гість" (not blank/null)
3. Guest refreshes page — previous messages still visible
4. Guest clears localStorage — widget resets to new guest session
</success_criteria>

<output>
Create `.planning/phases/46-schema-foundation-guest-chat/46-05-SUMMARY.md` when done
</output>
