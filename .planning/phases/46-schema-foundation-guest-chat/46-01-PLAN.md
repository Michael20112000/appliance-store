---
phase: 46-schema-foundation-guest-chat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
autonomous: false
requirements:
  - CHAT-01
  - CHAT-03

must_haves:
  truths:
    - "Conversation model has nullable userId (String?) with no @unique constraint"
    - "Conversation model has guestToken String? @unique"
    - "Conversation model has isActive Boolean @default(true)"
    - "Composite index @@index([userId, isActive]) exists on Conversation"
    - "Generated Prisma client reflects all new fields before any API code is written"
    - "Migration SQL contains DROP INDEX for Conversation_userId_key"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Updated Conversation model per D-05"
      contains: "guestToken"
    - path: "prisma/migrations"
      provides: "Migration SQL with correct DDL"
      contains: "guest_chat_schema"
  key_links:
    - from: "prisma/schema.prisma"
      to: "src/generated/prisma"
      via: "npx prisma generate"
      pattern: "guestToken.*unique"
---

<objective>
Migrate the Prisma schema to support multiple conversations per user and anonymous guest sessions. This is the hard prerequisite for all subsequent work in Phase 46 — the generated Prisma client must reflect the new schema before any service or API code is written.

Purpose: The current Conversation model has `userId String @unique` — a hard constraint preventing multiple conversations per user and blocking guest conversations entirely. D-05 removes this constraint and adds guestToken + isActive fields.

Output: Updated prisma/schema.prisma committed, migration applied, generated client reflects new types.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/46-schema-foundation-guest-chat/46-CONTEXT.md
@.planning/phases/46-schema-foundation-guest-chat/46-RESEARCH.md
@.planning/phases/46-schema-foundation-guest-chat/46-PATTERNS.md

<interfaces>
<!-- Current Conversation model — executor MUST know this before editing -->
<!-- Source: prisma/schema.prisma lines 223–241 -->

Current model (lines 223–241):
```prisma
model Conversation {
  id                  String             @id @default(cuid())
  userId              String             @unique
  status              ConversationStatus @default(OPEN)
  contextProductId    String?
  contextProductTitle String?
  buyerLastReadAt     DateTime           @default(now())
  adminLastReadAt     DateTime           @default(now())
  lastMessageAt       DateTime?
  lastMessagePreview  String?
  lastMessageSender   MessageSender?
  messages            Message[]
  createdAt           DateTime           @default(now())
  updatedAt           DateTime           @updatedAt

  @@index([updatedAt])
  @@index([lastMessageAt])
  @@index([status, lastMessageAt])
}
```

Current generator block (lines 4–7):
```prisma
generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Conversation model in schema.prisma per D-05</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma (full file — understand current Conversation model lines 223–241 and generator block lines 4–7)
    - .planning/phases/46-schema-foundation-guest-chat/46-PATTERNS.md (Pattern Assignments section for schema.prisma — exact target state)
    - .planning/phases/46-schema-foundation-guest-chat/46-CONTEXT.md (D-05, D-06 for exact migration steps)
  </read_first>
  <action>
    Apply D-05 changes to the Conversation model in this exact order, following PATTERNS.md "Target state":

    1. Change `userId String @unique` to `userId String?` — remove @unique, make nullable.
    2. Add `guestToken String? @unique` as a new field after userId.
    3. Add `isActive Boolean @default(true)` as a new field after guestToken.
    4. Add `@@index([userId, isActive])` to the index block.

    D-06 constraint: Do NOT add `@@unique([userId], where: { isActive: true })` filtered index in Phase 46 — use service-layer enforcement instead (RESEARCH.md open question 1 recommends this). Do not add `previewFeatures = ["partialIndexes"]` to the generator block.

    Field ordering in the model: id, userId, guestToken, isActive, status, contextProductId, contextProductTitle, buyerLastReadAt, adminLastReadAt, lastMessageAt, lastMessagePreview, lastMessageSender, messages, createdAt, updatedAt.

    Index ordering: @@index([userId, isActive]), @@index([updatedAt]), @@index([lastMessageAt]), @@index([status, lastMessageAt]).

    Do not change any other models (Message, User, Order, etc.).
  </action>
  <verify>
    <automated>grep -n "guestToken\|isActive\|userId" /Users/michael_ivashko/WebStormProjects/web/appliance-store/prisma/schema.prisma | grep -v "^#"</automated>
  </verify>
  <done>
    - `userId String?` (no @unique) appears in Conversation model
    - `guestToken String? @unique` appears in Conversation model
    - `isActive Boolean @default(true)` appears in Conversation model
    - `@@index([userId, isActive])` appears in Conversation model index block
    - `userId String @unique` (old form) is gone
  </done>
  <acceptance_criteria>
    - `grep "userId.*String\?" prisma/schema.prisma` matches a line without @unique
    - `grep "guestToken.*String.*@unique" prisma/schema.prisma` returns a result
    - `grep "isActive.*Boolean.*@default" prisma/schema.prisma` returns a result
    - `grep '@@index(\[userId, isActive\])' prisma/schema.prisma` returns a result
    - `grep "userId.*@unique" prisma/schema.prisma | grep -v "guestToken"` returns nothing
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action">
  <name>Task 2 [BLOCKING]: Run Prisma migration (requires interactive TTY)</name>
  <read_first>
    - prisma/schema.prisma (verify Task 1 edits are correct before migrating)
  </read_first>
  <what-built>Schema file edited with nullable userId, guestToken, isActive fields. Migration must now be applied to the live database and the generated client regenerated.</what-built>
  <how-to-verify>
    Run these commands in your terminal (requires interactive TTY — cannot be automated):

    ```
    npx prisma migrate dev --name guest_chat_schema
    npx prisma generate
    ```

    After migration succeeds:
    1. Check that a new migration directory exists: `ls prisma/migrations/` — should show a directory ending in `_guest_chat_schema`
    2. Inspect the generated SQL: `cat prisma/migrations/*guest_chat_schema/migration.sql` — MUST contain `DROP INDEX "Conversation_userId_key"` (Pitfall 1 from PATTERNS.md)
    3. If `DROP INDEX` is absent, the migration is wrong — stop and check the schema diff

    If migration fails due to existing data constraint (P2002 on existing userId values), it means the DB already has rows — run `npx prisma migrate dev --name guest_chat_schema --create-only` to generate SQL only, then manually adjust if needed.

    After successful migration AND generate, confirm TypeScript compile is clean:
    ```
    npx tsc --noEmit 2>&1 | head -20
    ```
    Expect 0 errors on files untouched by Phase 46.
  </how-to-verify>
  <resume-signal>Type "migration done" after both commands succeed and DROP INDEX is confirmed in migration.sql</resume-signal>
  <acceptance_criteria>
    - `ls prisma/migrations/ | grep guest_chat_schema` returns a directory
    - `grep "DROP INDEX" prisma/migrations/*guest_chat_schema/migration.sql` returns a match
    - `grep "guestToken" prisma/migrations/*guest_chat_schema/migration.sql` returns a match confirming column was added
    - `npx prisma generate` ran without error (src/generated/prisma is updated)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema → Database | DDL change alters production DB structure; migration must be reviewed before apply |
| Generated client → Application | App code must recompile against new nullable types; stale generated client causes runtime mismatches |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | Tampering | prisma/schema.prisma migration SQL | mitigate | Inspect migration.sql before applying — confirm DROP INDEX "Conversation_userId_key" is present; if absent, do not apply |
| T-46-02 | Denial of Service | Migration on production DB | accept | Neon PostgreSQL handles ALTER TABLE online for small table; existing rows get isActive=true default — no downtime |
| T-46-SC | Tampering | npm/pip/cargo installs | accept | Phase 46 installs zero new packages; no slopcheck needed |
</threat_model>

<verification>
After Task 2 completes:
1. `grep "guestToken\|isActive" prisma/schema.prisma` shows both fields in Conversation model
2. `grep "DROP INDEX" prisma/migrations/*guest_chat_schema/migration.sql` confirms old unique constraint removed
3. `ls src/generated/prisma/` shows generated client files updated after `prisma generate`
4. `npx tsc --noEmit 2>&1 | head -5` shows 0 errors on unrelated files
</verification>

<success_criteria>
- Conversation model has userId String? (no @unique), guestToken String? @unique, isActive Boolean @default(true), @@index([userId, isActive])
- Migration SQL has been applied to the live DB and contains DROP INDEX for the old unique constraint
- Generated Prisma client reflects all new nullable types (src/generated/prisma updated)
- TypeScript compile is clean on files not touched by Phase 46
</success_criteria>

<output>
Create `.planning/phases/46-schema-foundation-guest-chat/46-01-SUMMARY.md` when done
</output>
