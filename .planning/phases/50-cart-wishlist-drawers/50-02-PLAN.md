---
phase: 50-cart-wishlist-drawers
plan: 02
type: execute
wave: 2
depends_on:
  - 50-01
files_modified:
  - src/lib/drawers/drawer-context.tsx
  - src/server/actions/cart.actions.ts
  - src/server/actions/wishlist.actions.ts
autonomous: true
requirements:
  - DRWR-01
  - DRWR-02

must_haves:
  truths:
    - "DrawerProvider exports cartOpen, wishlistOpen, openCart, closeCart, openWishlist, closeWishlist via useDrawers()"
    - "useDrawers() throws 'useDrawers must be used within DrawerProvider' when called outside the provider"
    - "getCartAction() returns CartViewDto for authenticated users and throws/redirects for guests"
    - "getWishlistAction() returns WishlistViewDto for authenticated users"
    - "npx vitest run src/lib/drawers/drawer-context.test.tsx exits 0 (GREEN)"
  artifacts:
    - path: "src/lib/drawers/drawer-context.tsx"
      provides: "DrawerProvider + useDrawers hook"
      exports: ["DrawerProvider", "useDrawers"]
    - path: "src/server/actions/cart.actions.ts"
      provides: "getCartAction server action"
      contains: "getCartAction"
    - path: "src/server/actions/wishlist.actions.ts"
      provides: "getWishlistAction server action"
      contains: "getWishlistAction"
  key_links:
    - from: "src/components/cart/cart-drawer-content.tsx"
      to: "src/lib/drawers/drawer-context"
      via: "useDrawers() hook"
      pattern: "useDrawers"
    - from: "src/server/actions/cart.actions.ts"
      to: "src/server/services/cart.service"
      via: "getCartForUser"
      pattern: "getCartForUser"
---

<objective>
Create the DrawerProvider React context and the two thin server actions that the drawer content components need. These are pure additions — no existing component is modified yet. After this plan, the drawer-context tests go GREEN and server actions are callable by plans 03 and 04.

Purpose: Foundational layer for the entire phase. All drawer components in plans 03 and 04 depend on DrawerProvider and the server actions created here.
Output: src/lib/drawers/drawer-context.tsx (new), getCartAction added to cart.actions.ts (modified), getWishlistAction added to wishlist.actions.ts (modified).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-cart-wishlist-drawers/50-RESEARCH.md
@.planning/phases/50-cart-wishlist-drawers/50-PATTERNS.md

<interfaces>
<!-- ChatProvider context shape (analog for DrawerProvider) -->
<!-- From src/components/chat/chat-provider.tsx lines 46-75 and 552-558 -->

Context type pattern:
```typescript
type ChatContextValue = { hasSession: boolean; isOpen: boolean; openPanel: (options?: ProductChatContext) => void; closePanel: () => void; ... };
const ChatContext = createContext<ChatContextValue | null>(null);
```

useMemo value object:
```typescript
const value = useMemo<ChatContextValue>(() => ({ ... all fields }), [ /* all deps */ ]);
return <ChatContext.Provider value={value}>{children}</ChatContext.Provider>;
```

Null-guard hook:
```typescript
export function useChat() {
  const context = useContext(ChatContext);
  if (!context) { throw new Error("useChat must be used within ChatProvider"); }
  return context;
}
```

<!-- Cart service signature (from src/server/services/cart.service.ts) -->
getCartForUser(userId: string): Promise<CartViewDto>

<!-- Wishlist service signature (from src/server/services/wishlist.service.ts line 69) -->
listWishlistForUser(userId: string): Promise<WishlistViewDto>

<!-- Existing cart.actions.ts requireBuyer pattern (lines 47-53) -->
export async function removeFromCartAction(productId: string) {
  const session = await requireBuyer();
  ...
  return { ok: true as const };
}

<!-- Types needed -->
CartViewDto: src/types/cart (already imported in guest-cart-view.tsx)
WishlistViewDto: src/types/wishlist (already used in wishlist service)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create DrawerProvider context</name>
  <files>src/lib/drawers/drawer-context.tsx</files>
  <read_first>
    - src/components/chat/chat-provider.tsx lines 1-12 (imports pattern), lines 46-75 (context type + createContext), lines 482-550 (useMemo value + return JSX), lines 552-558 (null-guard hook)
    - src/lib/drawers/drawer-context.test.tsx (the failing tests this task must make green — read the test file to understand exact API expected)
  </read_first>
  <behavior>
    - DrawerProvider exports: cartOpen (boolean, default false), wishlistOpen (boolean, default false), openCart (() => void), closeCart (() => void), openWishlist (() => void), closeWishlist (() => void)
    - useDrawers() returns the context value when called inside DrawerProvider
    - useDrawers() throws Error with message containing "DrawerProvider" when called outside DrawerProvider
    - openCart() sets cartOpen to true; closeCart() sets it to false
    - openWishlist() sets wishlistOpen to true; closeWishlist() sets it to false
    - cartOpen and wishlistOpen are independent — opening one does not affect the other
  </behavior>
  <action>
    Create src/lib/drawers/drawer-context.tsx. The directory src/lib/drawers/ does not exist — create it.

    Structure mirrors ChatProvider pattern exactly:
    - "use client" directive at top
    - Imports: createContext, useContext, useMemo, useState, type ReactNode from "react"
    - Type DrawerContextValue with 6 fields: cartOpen: boolean, wishlistOpen: boolean, openCart: () => void, closeCart: () => void, openWishlist: () => void, closeWishlist: () => void
    - const DrawerContext = createContext<DrawerContextValue | null>(null)
    - export function DrawerProvider({ children }: { children: ReactNode }): uses two useState(false) booleans, builds value with useMemo (deps: [cartOpen, wishlistOpen]), returns DrawerContext.Provider wrapping children (children only — CartDrawer/WishlistDrawer are added in plan 05)
    - export function useDrawers(): null-guard throws new Error("useDrawers must be used within DrawerProvider"), returns context

    Do NOT add useCallback — simple inline arrow functions in useMemo are sufficient for this context shape.
    Do NOT add useQueryStates — plain useState only per research decision (no URL-reflected state).
    Do NOT render CartDrawer or WishlistDrawer yet — that wiring happens in plan 05 (chat-provider.tsx).
  </action>
  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npx vitest run src/lib/drawers/drawer-context.test.tsx 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File src/lib/drawers/drawer-context.tsx exists with "use client" directive
    - Exports DrawerProvider (function component accepting children: ReactNode)
    - Exports useDrawers (function returning DrawerContextValue)
    - DrawerContextValue type has exactly 6 fields: cartOpen, wishlistOpen, openCart, closeCart, openWishlist, closeWishlist
    - npx vitest run src/lib/drawers/drawer-context.test.tsx exits 0 (all 6 tests GREEN)
  </acceptance_criteria>
  <done>drawer-context.tsx created; all DRWR-CTX-01 through DRWR-CTX-06 tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Add getCartAction and getWishlistAction server actions</name>
  <files>
    src/server/actions/cart.actions.ts
    src/server/actions/wishlist.actions.ts
  </files>
  <read_first>
    - src/server/actions/cart.actions.ts (full file — existing "use server" directive, requireBuyer import, existing function pattern, current import block from cart.service)
    - src/server/actions/wishlist.actions.ts (full file — existing "use server" directive, requireBuyer import, existing function pattern)
    - src/server/services/cart.service.ts lines 1-20 and grep for getCartForUser (verify export signature)
    - src/server/services/wishlist.service.ts lines 69-79 (listWishlistForUser signature)
  </read_first>
  <action>
    cart.actions.ts — add getCartAction after clearCartAction:
    1. Extend the existing destructured import from "@/server/services/cart.service" to include getCartForUser (add it to the import block alongside addToCart, clearCart, etc.)
    2. Add import for CartViewDto: import type { CartViewDto } from "@/types/cart" (check if already imported; if not, add it)
    3. Add the action function:
       export async function getCartAction(): Promise<CartViewDto> {
         const session = await requireBuyer();
         return getCartForUser(session.user.id);
       }
    No revalidatePath call — this is a read action. No Zod validation — no user input.

    wishlist.actions.ts — add getWishlistAction after clearWishlistAction:
    1. Extend the existing import from "@/server/services/wishlist.service" to include listWishlistForUser
    2. Add import for WishlistViewDto: import type { WishlistViewDto } from "@/types/wishlist" (check if already imported)
    3. Add the action function:
       export async function getWishlistAction(): Promise<WishlistViewDto> {
         const session = await requireBuyer();
         return listWishlistForUser(session.user.id);
       }
    No revalidatePath — read action. No Zod — no input.
  </action>
  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npx tsc --noEmit 2>&1 | grep -E "cart\.actions|wishlist\.actions" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - src/server/actions/cart.actions.ts contains `export async function getCartAction(): Promise<CartViewDto>`
    - src/server/actions/cart.actions.ts contains `return getCartForUser(session.user.id)` inside getCartAction
    - src/server/actions/wishlist.actions.ts contains `export async function getWishlistAction(): Promise<WishlistViewDto>`
    - src/server/actions/wishlist.actions.ts contains `return listWishlistForUser(session.user.id)` inside getWishlistAction
    - npx tsc --noEmit produces zero TypeScript errors for cart.actions.ts and wishlist.actions.ts
  </acceptance_criteria>
  <done>getCartAction and getWishlistAction added to their respective action files; TypeScript compiles cleanly</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → getCartAction | Auth user calls server action; session validated by requireBuyer() |
| client → getWishlistAction | Auth user calls server action; session validated by requireBuyer() |
| DrawerProvider → children | Client-side React context; no sensitive data crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-02-01 | Elevation of Privilege | getCartAction | mitigate | requireBuyer() enforced — unauthenticated callers get session error before service is called |
| T-50-02-02 | Elevation of Privilege | getWishlistAction | mitigate | requireBuyer() enforced — same guard as all other auth mutations |
| T-50-02-03 | Information Disclosure | getCartAction returning full CartViewDto | accept | CartViewDto contains only the authenticated user's own cart data; requireBuyer() ensures userId matches session |
| T-50-02-04 | Information Disclosure | DrawerContext client state | accept | cartOpen/wishlistOpen are UI booleans — no PII, no sensitive data |
| T-50-02-SC | Tampering | npm install | accept | No new packages installed in this plan |
</threat_model>

<verification>
npx vitest run src/lib/drawers/drawer-context.test.tsx — exits 0 (all 6 context tests green)
npx tsc --noEmit — no errors in cart.actions.ts or wishlist.actions.ts
grep "getCartAction" src/server/actions/cart.actions.ts — returns a match
grep "getWishlistAction" src/server/actions/wishlist.actions.ts — returns a match
</verification>

<success_criteria>
- DrawerProvider and useDrawers exported from src/lib/drawers/drawer-context.tsx
- drawer-context tests (6 cases) all GREEN
- getCartAction added to cart.actions.ts — requireBuyer() + getCartForUser pattern
- getWishlistAction added to wishlist.actions.ts — requireBuyer() + listWishlistForUser pattern
- TypeScript compiles cleanly for modified files
</success_criteria>

<output>
Create `.planning/phases/50-cart-wishlist-drawers/50-02-SUMMARY.md` when done
</output>
