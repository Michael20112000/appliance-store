---
phase: 50-cart-wishlist-drawers
plan: 03
type: execute
wave: 3
depends_on:
  - 50-02
files_modified:
  - src/components/cart/cart-drawer.tsx
  - src/components/cart/cart-drawer-content.tsx
autonomous: true
requirements:
  - DRWR-01

must_haves:
  truths:
    - "CartDrawer renders a Sheet with side='right' when cartOpen is true"
    - "CartDrawer renders nothing (Sheet is closed) when cartOpen is false"
    - "CartDrawer passes hasSession to CartDrawerContent"
    - "CartDrawerContent loads auth cart via getCartAction on drawer open when hasSession is true"
    - "CartDrawerContent loads guest cart via resolveGuestCartAction on drawer open when hasSession is false"
    - "CartDrawerContent re-fetches on CART_CHANGED_EVENT when guest"
    - "npx vitest run src/components/cart/cart-drawer.test.tsx exits 0 (GREEN)"
  artifacts:
    - path: "src/components/cart/cart-drawer.tsx"
      provides: "Sheet wrapper for cart — controlled by DrawerProvider"
      exports: ["CartDrawer"]
    - path: "src/components/cart/cart-drawer-content.tsx"
      provides: "Auth + guest data loading + cart item list rendering"
      exports: ["CartDrawerContent"]
  key_links:
    - from: "src/components/cart/cart-drawer.tsx"
      to: "src/lib/drawers/drawer-context"
      via: "useDrawers().cartOpen + closeCart"
      pattern: "useDrawers"
    - from: "src/components/cart/cart-drawer-content.tsx"
      to: "src/server/actions/cart.actions"
      via: "getCartAction() on drawer open"
      pattern: "getCartAction"
---

<objective>
Create CartDrawer and CartDrawerContent — the full-height right Sheet for the cart. CartDrawer is the Sheet shell; CartDrawerContent handles auth/guest data loading and renders the item list using existing CartLineItem, GuestCartLineItem, CartEmpty, and CartSummary components.

Purpose: Delivers DRWR-01 — cart opens as a right Sheet instead of navigating to /koszyk.
Output: Two new files. CartDrawer is renderable and Sheet-open-/close-responsive. CartDrawerContent loads and displays real cart data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-cart-wishlist-drawers/50-RESEARCH.md
@.planning/phases/50-cart-wishlist-drawers/50-PATTERNS.md

<interfaces>
<!-- Sheet API (from src/components/ui/sheet.tsx — verified) -->
Sheet: SheetPrimitive.Root.Props (open, onOpenChange)
SheetContent: side?: "top"|"right"|"bottom"|"left"; showCloseButton?: boolean; className
SheetClose: SheetPrimitive.Close.Props — use render={<button type="button" aria-label="..." />}
SheetHeader, SheetTitle, SheetFooter: plain div wrappers

Controlled open/close (from catalog-filters-sheet.tsx analog):
<Sheet open={cartOpen} onOpenChange={(open) => { if (!open) closeCart(); }}>

<!-- DrawerProvider API (from src/lib/drawers/drawer-context.tsx — created in plan 02) -->
useDrawers(): { cartOpen: boolean; wishlistOpen: boolean; openCart: () => void; closeCart: () => void; openWishlist: () => void; closeWishlist: () => void }

<!-- getCartAction (from src/server/actions/cart.actions.ts — added in plan 02) -->
getCartAction(): Promise<CartViewDto>

<!-- GuestCartView data loading pattern (from src/components/cart/guest-cart-view.tsx lines 1-98) -->
const load = useCallback(async () => {
  setLoading(true); setError(null);
  const productIds = getPendingProductIds();
  if (productIds.length === 0) { setCart({ items: [], subtotalKopiyky: 0, removedTitles: [] }); setLoading(false); return; }
  try { const result = await resolveGuestCartAction(productIds); setCart(result.cart); }
  catch { setError("Не вдалося завантажити кошик. Спробуйте оновити сторінку."); }
  finally { setLoading(false); }
}, []);

useEffect(() => {
  void load();
  window.addEventListener(CART_CHANGED_EVENT, load);
  return () => window.removeEventListener(CART_CHANGED_EVENT, load);
}, [load]);

<!-- CartViewDto shape (from src/types/cart) -->
{ items: CartLineItemDto[]; subtotalKopiyky: number; removedTitles: string[] }

<!-- Existing components reused unchanged -->
CartLineItem: src/components/cart/cart-line-item.tsx (auth user item row)
GuestCartLineItem: src/components/cart/guest-cart-line-item.tsx
GuestClearCartButton: src/components/cart/guest-clear-cart-button.tsx
CartEmpty: src/components/cart/cart-empty.tsx
CartSummary: src/components/cart/cart-summary.tsx (the checkout Link to /zamowlennia is fine — leave as-is)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create CartDrawer shell component</name>
  <files>src/components/cart/cart-drawer.tsx</files>
  <read_first>
    - src/components/catalog/catalog-filters-sheet.tsx (full file — exact Sheet usage pattern, onOpenChange, SheetContent className, SheetClose render prop)
    - src/components/ui/sheet.tsx (full file — verify SheetClose render prop API, SheetContent side prop, showCloseButton prop)
    - src/components/cart/cart-drawer.test.tsx (the failing tests this task must make green — read to understand exact assertions)
    - src/lib/drawers/drawer-context.tsx (verify useDrawers export shape)
  </read_first>
  <behavior>
    - CartDrawer renders null-equivalent (no sheet in DOM) when cartOpen is false
    - CartDrawer renders Sheet with data-testid="sheet" when cartOpen is true (per sheet mock in tests)
    - SheetTitle text is "Кошик"
    - Calling onOpenChange(false) on the Sheet calls closeCart from useDrawers
    - CartDrawerContent is rendered inside the sheet (mocked in tests as data-testid="cart-drawer-content")
  </behavior>
  <action>
    Create src/components/cart/cart-drawer.tsx as a "use client" component.

    Props: CartDrawerProps = { hasSession: boolean }
    Export: export function CartDrawer({ hasSession }: CartDrawerProps)

    Implementation:
    - const { cartOpen, closeCart } = useDrawers()
    - Return: Sheet open={cartOpen} onOpenChange={(open) => { if (!open) closeCart(); }}
    - SheetContent: side="right", showCloseButton={false}, className="flex flex-col gap-0 p-0 sm:max-w-md"
    - SheetHeader: className="flex-row items-center justify-between border-b px-4 py-3" containing SheetTitle ("Кошик") and SheetClose render={<button type="button" aria-label="Закрити кошик" />}
    - Body div: className="min-h-0 flex-1 overflow-y-auto" containing CartDrawerContent hasSession={hasSession}
    - No SheetFooter — CartSummary is rendered inside CartDrawerContent

    Imports needed: useDrawers from @/lib/drawers/drawer-context; Sheet/SheetContent/SheetHeader/SheetTitle/SheetClose from @/components/ui/sheet; CartDrawerContent from ./cart-drawer-content (forward reference — this file is created in Task 2, but the import is declared now).
  </action>
  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npx vitest run src/components/cart/cart-drawer.test.tsx 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/cart/cart-drawer.tsx exists with "use client" directive
    - Exports CartDrawer function with hasSession: boolean prop
    - Contains useDrawers() call destructuring cartOpen and closeCart
    - SheetContent has side="right" and showCloseButton={false}
    - SheetTitle text is "Кошик"
    - npx vitest run src/components/cart/cart-drawer.test.tsx exits 0 (all 4 CART-DRW tests GREEN — CartDrawerContent import will resolve once Task 2 creates it; if tests still fail at this point due to missing cart-drawer-content, proceed to Task 2 and then re-verify)
  </acceptance_criteria>
  <done>cart-drawer.tsx created; CartDrawer renders Sheet controlled by DrawerProvider state</done>
</task>

<task type="auto">
  <name>Task 2: Create CartDrawerContent — auth and guest data loading</name>
  <files>src/components/cart/cart-drawer-content.tsx</files>
  <read_first>
    - src/components/cart/guest-cart-view.tsx (full file — exact load pattern, useCallback, CART_CHANGED_EVENT listener, loading/error/empty state rendering)
    - src/components/cart/cart-empty.tsx (verify props interface)
    - src/components/cart/cart-line-item.tsx (verify props interface — especially if it accepts an onRemoved callback)
    - src/components/cart/guest-cart-line-item.tsx (verify props interface)
    - src/components/cart/cart-summary.tsx (verify props interface — subtotalKopiyky)
    - src/components/cart/guest-clear-cart-button.tsx (verify props interface)
    - src/server/actions/cart.actions.ts (verify getCartAction export and resolveGuestCartAction export)
  </read_first>
  <action>
    Create src/components/cart/cart-drawer-content.tsx as a "use client" component.

    Props: CartDrawerContentProps = { hasSession: boolean }
    Export: export function CartDrawerContent({ hasSession }: CartDrawerContentProps)

    State: cart (CartViewDto | null), loading (boolean, starts true), error (string | null)

    The load function (useCallback): mirrors GuestCartView.load exactly:
    - setLoading(true); setError(null)
    - if hasSession: result = await getCartAction(); setCart(result)
    - if !hasSession: productIds = getPendingProductIds(); if empty setCart({ items: [], subtotalKopiyky: 0, removedTitles: [] }); else result = await resolveGuestCartAction(productIds); setCart(result.cart)
    - catch: setError("Не вдалося завантажити кошик. Спробуйте оновити сторінку.")
    - finally: setLoading(false)
    Dependencies: [hasSession]

    useEffect gated on cartOpen from useDrawers():
    - if (!cartOpen) return
    - void load()
    - if (!hasSession): attach CART_CHANGED_EVENT listener → load; return cleanup
    Dependencies: [cartOpen, hasSession, load]

    Render:
    - loading: return <div className="flex-1 flex items-center justify-center text-muted-foreground text-sm">Завантаження...</div>
    - error: return <Alert><AlertDescription>{error}</AlertDescription></Alert>
    - cart === null: return null (loading not yet started)
    - cart.items.length === 0: return <CartEmpty />
    - items present (auth): render ordered list of CartLineItem per item, then CartSummary subtotalKopiyky={cart.subtotalKopiyky}
    - items present (guest): render ordered list of GuestCartLineItem per item, then GuestClearCartButton, then CartSummary subtotalKopiyky={cart.subtotalKopiyky}

    Do NOT import router or call router.refresh() inside this component — CartLineItem already handles router.refresh() internally.
    Do NOT strip CartSummary — its checkout Link to /zamowlennia navigates normally (closes drawer via page change, which is correct behavior per research pitfall notes).

    Imports: useCallback, useEffect, useState from react; useDrawers from @/lib/drawers/drawer-context; getCartAction, resolveGuestCartAction from @/server/actions/cart.actions; CartEmpty from ./cart-empty; CartLineItem from ./cart-line-item; GuestCartLineItem from ./guest-cart-line-item; CartSummary from ./cart-summary; GuestClearCartButton from ./guest-clear-cart-button; CART_CHANGED_EVENT from @/lib/cart/cart-events; getPendingProductIds from @/lib/cart/pending-storage; Alert/AlertDescription from @/components/ui/alert; CartViewDto from @/types/cart.
  </action>
  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npx vitest run src/components/cart/cart-drawer.test.tsx 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/cart/cart-drawer-content.tsx exists with "use client" directive
    - Exports CartDrawerContent with hasSession: boolean prop
    - Contains useDrawers() call destructuring cartOpen
    - useEffect has [cartOpen, hasSession, load] dependency array
    - Auth branch calls getCartAction() (not a direct service call)
    - Guest branch calls resolveGuestCartAction(productIds) and attaches CART_CHANGED_EVENT listener
    - Renders CartEmpty when cart.items.length === 0
    - Renders CartSummary with subtotalKopiyky prop
    - npx vitest run src/components/cart/cart-drawer.test.tsx exits 0 (all 4 CART-DRW tests GREEN with CartDrawerContent now resolved)
    - npx tsc --noEmit produces no errors for cart-drawer.tsx or cart-drawer-content.tsx
  </acceptance_criteria>
  <done>cart-drawer-content.tsx created; auth and guest data loading implemented; cart-drawer tests all GREEN</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CartDrawerContent → getCartAction | Auth user's browser calls server action; requireBuyer() guards the session |
| CartDrawerContent → resolveGuestCartAction | Guest's productIds (from localStorage) passed to server; validated by resolveGuestCartSchema |
| CartDrawer → DrawerContext | Client-side state crossing; cartOpen/closeCart are UI booleans only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-03-01 | Elevation of Privilege | CartDrawerContent → getCartAction | mitigate | requireBuyer() in getCartAction prevents unauthenticated data access; added in plan 02 |
| T-50-03-02 | Information Disclosure | Cart items rendered in client drawer | accept | Data is the user's own cart; requireBuyer() ensures userId matches session; no cross-user data risk |
| T-50-03-03 | Input Validation | resolveGuestCartAction productIds from localStorage | mitigate | resolveGuestCartSchema (Zod) already validates productId array in the action — unchanged from prior phase |
| T-50-03-04 | Denial of Service | Repeated open/close triggers multiple getCartAction calls | accept | Low-frequency user action; no rate limiting needed at this scale |
| T-50-03-SC | Tampering | npm install | accept | No new packages; all imports from existing validated dependencies |
</threat_model>

<verification>
npx vitest run src/components/cart/cart-drawer.test.tsx — exits 0 (all 4 CART-DRW tests GREEN)
npx tsc --noEmit — no errors in cart-drawer.tsx or cart-drawer-content.tsx
grep "getCartAction" src/components/cart/cart-drawer-content.tsx — returns a match (auth branch)
grep "CART_CHANGED_EVENT" src/components/cart/cart-drawer-content.tsx — returns a match (guest branch listener)
</verification>

<success_criteria>
- CartDrawer renders Sheet with side="right" controlled by useDrawers().cartOpen
- CartDrawerContent handles auth (getCartAction) and guest (resolveGuestCartAction) data loading
- CART_CHANGED_EVENT listener attached for guest users when drawer is open
- cart-drawer tests all GREEN
- TypeScript compiles cleanly
</success_criteria>

<output>
Create `.planning/phases/50-cart-wishlist-drawers/50-03-SUMMARY.md` when done
</output>
