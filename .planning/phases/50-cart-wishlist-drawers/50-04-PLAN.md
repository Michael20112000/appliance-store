---
phase: 50-cart-wishlist-drawers
plan: 04
type: execute
wave: 3
depends_on:
  - 50-02
files_modified:
  - src/components/wishlist/wishlist-drawer.tsx
  - src/components/wishlist/wishlist-drawer-content.tsx
autonomous: true
requirements:
  - DRWR-02

must_haves:
  truths:
    - "WishlistDrawer renders a Sheet with side='right' when wishlistOpen is true"
    - "WishlistDrawer renders nothing when wishlistOpen is false"
    - "WishlistDrawerContent loads auth wishlist via getWishlistAction on drawer open when hasSession is true"
    - "WishlistDrawerContent loads guest wishlist via resolveGuestWishlistProductsAction on drawer open when hasSession is false"
    - "WishlistDrawerContent re-fetches on WISHLIST_CHANGED_EVENT and storage events when guest"
    - "npx vitest run src/components/wishlist/wishlist-drawer.test.tsx exits 0 (GREEN)"
  artifacts:
    - path: "src/components/wishlist/wishlist-drawer.tsx"
      provides: "Sheet wrapper for wishlist — controlled by DrawerProvider"
      exports: ["WishlistDrawer"]
    - path: "src/components/wishlist/wishlist-drawer-content.tsx"
      provides: "Auth + guest data loading + wishlist item grid rendering"
      exports: ["WishlistDrawerContent"]
  key_links:
    - from: "src/components/wishlist/wishlist-drawer.tsx"
      to: "src/lib/drawers/drawer-context"
      via: "useDrawers().wishlistOpen + closeWishlist"
      pattern: "useDrawers"
    - from: "src/components/wishlist/wishlist-drawer-content.tsx"
      to: "src/server/actions/wishlist.actions"
      via: "getWishlistAction() on drawer open"
      pattern: "getWishlistAction"
---

<objective>
Create WishlistDrawer and WishlistDrawerContent — the full-height right Sheet for the wishlist. Parallel to plan 03 (no file overlap). WishlistDrawer is the Sheet shell; WishlistDrawerContent handles auth/guest data loading and renders the item grid using existing WishlistGrid and ClearWishlistButton components.

Purpose: Delivers DRWR-02 — wishlist opens as a right Sheet instead of navigating to /obrane.
Output: Two new files. WishlistDrawer is Sheet-open-/close-responsive. WishlistDrawerContent loads and displays real wishlist data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-cart-wishlist-drawers/50-RESEARCH.md
@.planning/phases/50-cart-wishlist-drawers/50-PATTERNS.md

<interfaces>
<!-- Sheet API — same as plan 03 -->
Sheet: open, onOpenChange; SheetContent: side, showCloseButton, className; SheetClose: render prop; SheetHeader, SheetTitle

<!-- DrawerProvider API (from src/lib/drawers/drawer-context.tsx — created in plan 02) -->
useDrawers(): { wishlistOpen: boolean; closeWishlist: () => void; ... }

<!-- getWishlistAction (from src/server/actions/wishlist.actions.ts — added in plan 02) -->
getWishlistAction(): Promise<WishlistViewDto>
WishlistViewDto: { lines: WishlistLineDto[] }

<!-- GuestWishlistView data loading pattern (from src/components/wishlist/guest-wishlist-view.tsx lines 1-81) -->
useEffect with cancelled flag pattern:
let cancelled = false;
async function load() {
  setLoading(true); setError(null);
  const productIds = getGuestWishlistProductIds();
  if (productIds.length === 0) { if (!cancelled) { setLines([]); setLoading(false); } return; }
  try {
    const result = await resolveGuestWishlistProductsAction(productIds);
    if (!cancelled) { setLines(result.lines); setLoading(false); }
  } catch { if (!cancelled) { setError("..."); setLoading(false); } }
}
void load();
const onChanged = () => { void load(); };
window.addEventListener(WISHLIST_CHANGED_EVENT, onChanged);
window.addEventListener("storage", onStorage); // for GUEST_WISHLIST_KEY cross-tab sync
return () => { cancelled = true; window.removeEventListener(WISHLIST_CHANGED_EVENT, onChanged); window.removeEventListener("storage", onStorage); };

<!-- Existing components reused unchanged -->
WishlistGrid: src/components/wishlist/wishlist-grid.tsx — accepts lines: WishlistLineDto[], hasSession: boolean
ClearWishlistButton: src/components/wishlist/clear-wishlist-button.tsx — accepts hasSession: boolean
(Do NOT use WishlistPageContent — it has page-level layout wrappers mx-auto max-w-6xl px-4 py-12)

<!-- Guest wishlist storage (from src/lib/wishlist/guest-storage.ts) -->
getGuestWishlistProductIds(): string[]
GUEST_WISHLIST_KEY: string (storage event key)

<!-- WISHLIST_CHANGED_EVENT (from src/lib/wishlist/wishlist-events.ts) -->
WISHLIST_CHANGED_EVENT: string
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create WishlistDrawer shell component</name>
  <files>src/components/wishlist/wishlist-drawer.tsx</files>
  <read_first>
    - src/components/catalog/catalog-filters-sheet.tsx (full file — Sheet usage pattern)
    - src/components/ui/sheet.tsx (SheetClose render prop API, SheetContent side prop)
    - src/components/wishlist/wishlist-drawer.test.tsx (failing tests to make green)
    - src/lib/drawers/drawer-context.tsx (verify useDrawers export, wishlistOpen/closeWishlist fields)
  </read_first>
  <behavior>
    - WishlistDrawer renders null-equivalent (no sheet in DOM) when wishlistOpen is false
    - WishlistDrawer renders Sheet with data-testid="sheet" when wishlistOpen is true (per sheet mock)
    - SheetTitle text is "Обране"
    - onOpenChange(false) on the Sheet calls closeWishlist from useDrawers
    - WishlistDrawerContent rendered inside the sheet (mocked as data-testid="wishlist-drawer-content")
  </behavior>
  <action>
    Create src/components/wishlist/wishlist-drawer.tsx as a "use client" component.

    Props: WishlistDrawerProps = { hasSession: boolean }
    Export: export function WishlistDrawer({ hasSession }: WishlistDrawerProps)

    Implementation:
    - const { wishlistOpen, closeWishlist } = useDrawers()
    - Return: Sheet open={wishlistOpen} onOpenChange={(open) => { if (!open) closeWishlist(); }}
    - SheetContent: side="right", showCloseButton={false}, className="flex flex-col gap-0 p-0 sm:max-w-md"
    - SheetHeader: className="flex-row items-center justify-between border-b px-4 py-3" containing SheetTitle ("Обране") and SheetClose render={<button type="button" aria-label="Закрити обране" />}
    - Body div: className="min-h-0 flex-1 overflow-y-auto" containing WishlistDrawerContent hasSession={hasSession}
    - No SheetFooter — wishlist has no checkout action

    Imports: useDrawers from @/lib/drawers/drawer-context; Sheet/SheetContent/SheetHeader/SheetTitle/SheetClose from @/components/ui/sheet; WishlistDrawerContent from ./wishlist-drawer-content.
  </action>
  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npx vitest run src/components/wishlist/wishlist-drawer.test.tsx 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/wishlist/wishlist-drawer.tsx exists with "use client" directive
    - Exports WishlistDrawer with hasSession: boolean prop
    - Contains useDrawers() call destructuring wishlistOpen and closeWishlist
    - SheetContent has side="right" and showCloseButton={false}
    - SheetTitle text is "Обране"
    - npx vitest run src/components/wishlist/wishlist-drawer.test.tsx exits 0 once WishlistDrawerContent is also created (proceed to Task 2)
  </acceptance_criteria>
  <done>wishlist-drawer.tsx created; WishlistDrawer renders Sheet controlled by DrawerProvider wishlistOpen state</done>
</task>

<task type="auto">
  <name>Task 2: Create WishlistDrawerContent — auth and guest data loading</name>
  <files>src/components/wishlist/wishlist-drawer-content.tsx</files>
  <read_first>
    - src/components/wishlist/guest-wishlist-view.tsx (full file — cancelled flag pattern, WISHLIST_CHANGED_EVENT, storage event listener, loading/error states)
    - src/components/wishlist/wishlist-grid.tsx (verify props: lines: WishlistLineDto[], hasSession: boolean)
    - src/components/wishlist/clear-wishlist-button.tsx (verify props: hasSession: boolean)
    - src/server/actions/wishlist.actions.ts (verify getWishlistAction export and resolveGuestWishlistProductsAction export)
    - src/lib/wishlist/guest-storage.ts (verify getGuestWishlistProductIds signature and GUEST_WISHLIST_KEY export)
    - src/lib/wishlist/wishlist-events.ts (verify WISHLIST_CHANGED_EVENT export)
    - src/types/wishlist.ts (verify WishlistViewDto and WishlistLineDto shapes)
  </read_first>
  <action>
    Create src/components/wishlist/wishlist-drawer-content.tsx as a "use client" component.

    Props: WishlistDrawerContentProps = { hasSession: boolean }
    Export: export function WishlistDrawerContent({ hasSession }: WishlistDrawerContentProps)

    State: lines (WishlistLineDto[], starts []), loading (boolean, starts true), error (string | null)

    useEffect gated on wishlistOpen from useDrawers():
    - Dependencies: [wishlistOpen, hasSession]
    - if (!wishlistOpen) return
    - let cancelled = false
    - async function load():
        setLoading(true); setError(null)
        if hasSession: const result = await getWishlistAction(); if (!cancelled) { setLines(result.lines); setLoading(false); }
        if !hasSession: productIds = getGuestWishlistProductIds(); if empty → setLines([]), setLoading(false); else result = await resolveGuestWishlistProductsAction(productIds); if (!cancelled) setLines(result.lines, setLoading(false))
        catch: if (!cancelled) setError("Не вдалося завантажити обране. Спробуйте оновити сторінку."), setLoading(false)
    - void load()
    - if !hasSession: attach WISHLIST_CHANGED_EVENT listener (onChanged = () => void load()); attach storage listener for GUEST_WISHLIST_KEY (onStorage = (e) => { if (e.key === GUEST_WISHLIST_KEY) void load(); })
    - return cleanup: cancelled = true; removeEventListeners

    Render:
    - loading: <div className="flex-1 flex items-center justify-center text-muted-foreground text-sm">Завантаження...</div>
    - error: <Alert><AlertDescription>{error}</AlertDescription></Alert>
    - lines.length === 0 (and !loading): empty state div with text "Обране порожнє" and a Link to /katalog
    - lines.length > 0: render ClearWishlistButton hasSession={hasSession}; then WishlistGrid lines={lines} hasSession={hasSession}
    Wrap content in a flex flex-col gap-4 div. Do NOT wrap in WishlistPageContent (it has page-level mx-auto max-w-6xl padding).

    Imports: useEffect, useState from react; useDrawers from @/lib/drawers/drawer-context; getWishlistAction, resolveGuestWishlistProductsAction from @/server/actions/wishlist.actions; WishlistGrid from ./wishlist-grid; ClearWishlistButton from ./clear-wishlist-button; WISHLIST_CHANGED_EVENT from @/lib/wishlist/wishlist-events; getGuestWishlistProductIds, GUEST_WISHLIST_KEY from @/lib/wishlist/guest-storage; Alert/AlertDescription from @/components/ui/alert; WishlistLineDto from @/types/wishlist; Link from next/link; buttonVariants from @/components/ui/button; cn from @/lib/utils.
  </action>
  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npx vitest run src/components/wishlist/wishlist-drawer.test.tsx 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/wishlist/wishlist-drawer-content.tsx exists with "use client" directive
    - Exports WishlistDrawerContent with hasSession: boolean prop
    - Contains useDrawers() call destructuring wishlistOpen
    - Contains cancelled flag pattern (let cancelled = false inside useEffect)
    - Auth branch calls getWishlistAction() (not direct service call)
    - Guest branch calls resolveGuestWishlistProductsAction and attaches WISHLIST_CHANGED_EVENT + storage listeners
    - Does NOT import or use WishlistPageContent
    - Renders WishlistGrid with lines and hasSession props when lines.length > 0
    - npx vitest run src/components/wishlist/wishlist-drawer.test.tsx exits 0 (all 3 WLIST-DRW tests GREEN)
    - npx tsc --noEmit produces no errors for wishlist-drawer.tsx or wishlist-drawer-content.tsx
  </acceptance_criteria>
  <done>wishlist-drawer-content.tsx created; auth and guest data loading implemented; wishlist-drawer tests all GREEN</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WishlistDrawerContent → getWishlistAction | Auth user's browser calls server action; requireBuyer() guards the session |
| WishlistDrawerContent → resolveGuestWishlistProductsAction | Guest productIds from localStorage passed to server; validated by resolveGuestWishlistSchema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-04-01 | Elevation of Privilege | WishlistDrawerContent → getWishlistAction | mitigate | requireBuyer() in getWishlistAction prevents unauthenticated data access |
| T-50-04-02 | Information Disclosure | Wishlist items rendered in client drawer | accept | Data is the user's own wishlist; requireBuyer() ensures userId matches session |
| T-50-04-03 | Input Validation | resolveGuestWishlistProductsAction productIds from localStorage | mitigate | resolveGuestWishlistSchema (Zod) validates productId array — unchanged from prior implementation |
| T-50-04-04 | Spoofing | storage event from another tab/window | accept | Triggers a re-read of localStorage which is same-origin; no auth escalation possible |
| T-50-04-SC | Tampering | npm install | accept | No new packages; all imports from existing validated dependencies |
</threat_model>

<verification>
npx vitest run src/components/wishlist/wishlist-drawer.test.tsx — exits 0 (all 3 WLIST-DRW tests GREEN)
npx tsc --noEmit — no errors in wishlist-drawer.tsx or wishlist-drawer-content.tsx
grep "getWishlistAction" src/components/wishlist/wishlist-drawer-content.tsx — returns a match
grep "WISHLIST_CHANGED_EVENT" src/components/wishlist/wishlist-drawer-content.tsx — returns a match
grep -v "WishlistPageContent" src/components/wishlist/wishlist-drawer-content.tsx — no import of WishlistPageContent
</verification>

<success_criteria>
- WishlistDrawer renders Sheet with side="right" controlled by useDrawers().wishlistOpen
- WishlistDrawerContent handles auth (getWishlistAction) and guest (resolveGuestWishlistProductsAction) data loading
- Cancelled flag pattern prevents state updates after drawer closes (prevents memory leak)
- WISHLIST_CHANGED_EVENT + storage event listeners attached for guest users when drawer is open
- wishlist-drawer tests all GREEN
- TypeScript compiles cleanly
</success_criteria>

<output>
Create `.planning/phases/50-cart-wishlist-drawers/50-04-SUMMARY.md` when done
</output>
