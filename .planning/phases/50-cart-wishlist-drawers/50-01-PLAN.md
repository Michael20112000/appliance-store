---
phase: 50-cart-wishlist-drawers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/drawers/drawer-context.test.tsx
  - src/components/cart/cart-nav-button.test.tsx
  - src/components/cart/cart-drawer.test.tsx
  - src/components/wishlist/wishlist-nav-link.test.tsx
  - src/components/wishlist/wishlist-drawer.test.tsx
  - src/components/layout/storefront-fabs.test.tsx
autonomous: true
requirements:
  - DRWR-01
  - DRWR-02

must_haves:
  truths:
    - "npx vitest run src/lib/drawers/drawer-context.test.tsx exits with non-zero (failing tests — RED)"
    - "npx vitest run src/components/cart/cart-nav-button.test.tsx exits with non-zero (RED)"
    - "npx vitest run src/components/cart/cart-drawer.test.tsx exits with non-zero (RED)"
    - "npx vitest run src/components/wishlist/wishlist-nav-link.test.tsx exits with non-zero (RED)"
    - "npx vitest run src/components/wishlist/wishlist-drawer.test.tsx exits with non-zero (RED)"
    - "npx vitest run src/components/layout/storefront-fabs.test.tsx exits with non-zero (RED — FAB-01-a and FAB-01-b now fail because storefront-fabs still has the Link)"
  artifacts:
    - path: "src/lib/drawers/drawer-context.test.tsx"
      provides: "DrawerProvider context unit tests"
    - path: "src/components/cart/cart-nav-button.test.tsx"
      provides: "CartNavButton unit tests"
    - path: "src/components/cart/cart-drawer.test.tsx"
      provides: "CartDrawer sheet unit tests"
    - path: "src/components/wishlist/wishlist-nav-link.test.tsx"
      provides: "WishlistNavLink button unit tests"
    - path: "src/components/wishlist/wishlist-drawer.test.tsx"
      provides: "WishlistDrawer sheet unit tests"
  key_links:
    - from: "src/components/cart/cart-drawer.test.tsx"
      to: "src/lib/drawers/drawer-context"
      via: "vi.mock useDrawers"
      pattern: "vi\\.mock.*drawer-context"
    - from: "src/components/wishlist/wishlist-drawer.test.tsx"
      to: "src/lib/drawers/drawer-context"
      via: "vi.mock useDrawers"
      pattern: "vi\\.mock.*drawer-context"
---

<objective>
Create Wave 0 test stubs: failing unit tests for all new components and the updated storefront-fabs test. These tests drive the RED phase of the TDD cycle — they import components that do not yet exist and assert behaviors that have not been implemented. Plan 02+ will make them green.

Purpose: Nyquist compliance — every implementation task in plans 02–05 has a pre-existing automated verification command.
Output: 5 new test files + 2 updated assertions in storefront-fabs.test.tsx. All 5 new files fail. storefront-fabs.test.tsx FAB-01-a and FAB-01-b fail (they now assert button behavior but the component still has a Link).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-cart-wishlist-drawers/50-RESEARCH.md
@.planning/phases/50-cart-wishlist-drawers/50-PATTERNS.md
@.planning/phases/50-cart-wishlist-drawers/50-VALIDATION.md

<interfaces>
<!-- Test pattern from src/components/layout/storefront-fabs.test.tsx (existing) -->
<!-- Apply this boilerplate to ALL new test files in this phase -->

jsdom pragma and imports block:
```
/** @vitest-environment jsdom */
import { afterEach, beforeEach, describe, expect, it, vi } from "vitest";
import { cleanup, fireEvent, render, screen } from "@testing-library/react";
```

useDrawers mock (apply wherever useDrawers is consumed):
```
vi.mock("@/lib/drawers/drawer-context", () => ({
  useDrawers: vi.fn().mockReturnValue({
    cartOpen: false,
    wishlistOpen: false,
    openCart: vi.fn(),
    closeCart: vi.fn(),
    openWishlist: vi.fn(),
    closeWishlist: vi.fn(),
  }),
}));
```

DrawerProvider (for tests that render the provider directly):
```
import { DrawerProvider } from "@/lib/drawers/drawer-context";
// wrap component under test in <DrawerProvider>
```

Sheet mock (for cart-drawer.test and wishlist-drawer.test — Sheet uses Base UI Dialog which requires DOM portal):
```
vi.mock("@/components/ui/sheet", () => ({
  Sheet: ({ open, children }: { open: boolean; children: React.ReactNode }) =>
    open ? React.createElement("div", { "data-testid": "sheet" }, children) : null,
  SheetContent: ({ children }: { children: React.ReactNode }) =>
    React.createElement("div", { "data-testid": "sheet-content" }, children),
  SheetHeader: ({ children }: { children: React.ReactNode }) =>
    React.createElement("div", null, children),
  SheetTitle: ({ children }: { children: React.ReactNode }) =>
    React.createElement("h2", null, children),
  SheetClose: ({ render: r }: { render: React.ReactElement }) => r,
  SheetFooter: ({ children }: { children: React.ReactNode }) =>
    React.createElement("div", null, children),
}));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DrawerProvider context test stub</name>
  <files>src/lib/drawers/drawer-context.test.tsx</files>
  <read_first>
    - src/components/layout/storefront-fabs.test.tsx (test boilerplate: jsdom pragma, vi.mock, beforeEach/afterEach pattern)
    - src/components/chat/chat-provider.tsx lines 552-558 (useChat null-guard pattern — drawer-context.tsx will mirror this)
    - .planning/phases/50-cart-wishlist-drawers/50-RESEARCH.md (Pattern 1: DrawerProvider context shape)
  </read_first>
  <action>
    Create src/lib/drawers/drawer-context.test.tsx. The file does NOT yet exist; the test will import DrawerProvider and useDrawers from @/lib/drawers/drawer-context, which also does not yet exist — this causes an import-level failure (RED state is acceptable).

    Tests to write (describe "DrawerProvider"):
    - "DRWR-CTX-01: cartOpen defaults to false" — render a consumer component inside DrawerProvider, assert useDrawers().cartOpen is false on mount
    - "DRWR-CTX-02: openCart sets cartOpen to true" — call openCart(), assert cartOpen is true
    - "DRWR-CTX-03: closeCart sets cartOpen to false after open" — open then close, assert false
    - "DRWR-CTX-04: wishlistOpen defaults to false" — same pattern for wishlist
    - "DRWR-CTX-05: openWishlist sets wishlistOpen to true"
    - "DRWR-CTX-06: useDrawers throws outside DrawerProvider" — call useDrawers outside provider, expect thrown Error containing "DrawerProvider"

    Test helper pattern: render a small TestConsumer component that calls useDrawers() and exposes state via data-testid attributes, wrapped in DrawerProvider. Use fireEvent.click on buttons that call openCart/openWishlist/closeCart/closeWishlist.

    Do NOT mock @/lib/drawers/drawer-context in this file — this test tests the real implementation.
    Use the jsdom pragma and standard before/afterEach cleanup pattern from storefront-fabs.test.tsx.
  </action>
  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npx vitest run src/lib/drawers/drawer-context.test.tsx 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File src/lib/drawers/drawer-context.test.tsx exists
    - File starts with `/** @vitest-environment jsdom */`
    - File imports DrawerProvider and useDrawers from "@/lib/drawers/drawer-context"
    - At least 6 it() blocks present covering the behaviors listed above
    - Running the test exits non-zero (RED — drawer-context.tsx does not exist yet)
  </acceptance_criteria>
  <done>Test file created; vitest run returns non-zero exit code (import failure or test failure is expected)</done>
</task>

<task type="auto">
  <name>Task 2: Create CartNavButton, CartDrawer, WishlistNavLink, and WishlistDrawer test stubs</name>
  <files>
    src/components/cart/cart-nav-button.test.tsx
    src/components/cart/cart-drawer.test.tsx
    src/components/wishlist/wishlist-nav-link.test.tsx
    src/components/wishlist/wishlist-drawer.test.tsx
  </files>
  <read_first>
    - src/components/layout/storefront-fabs.test.tsx (full file — mock structure, render pattern, fireEvent pattern)
    - src/components/cart/guest-cart-nav-link.tsx (the component CartNavButton will mirror — className, badge structure)
    - src/components/wishlist/wishlist-nav-link.tsx (the file being converted — current Link structure, badge logic, ariaLabel pattern)
    - .planning/phases/50-cart-wishlist-drawers/50-PATTERNS.md (Sheet mock pattern for cart-drawer.test and wishlist-drawer.test)
  </read_first>
  <action>
    Create four test stub files. Each imports the component under test (which does not yet exist). Apply the useDrawers mock and Sheet mock from the context section above to each file that consumes them.

    cart-nav-button.test.tsx (imports CartNavButton from @/components/cart/cart-nav-button):
    - "CART-NAV-01: renders a button element (not an anchor/link)"
    - "CART-NAV-02: button has aria-label 'Кошик' when initialCount is 0"
    - "CART-NAV-03: button has aria-label 'Кошик, 2 товарів' when initialCount is 2"
    - "CART-NAV-04: clicking button calls openCart from useDrawers"
    - "CART-NAV-05: does not render badge when initialCount is 0"
    - "CART-NAV-06: renders badge with '3' when initialCount is 3"
    Mocks needed: @/lib/drawers/drawer-context (useDrawers), @/lib/cart/cart-events (CART_CHANGED_EVENT), @/lib/cart/pending-storage (getPendingItemCount returning 0)

    cart-drawer.test.tsx (imports CartDrawer from @/components/cart/cart-drawer):
    - "CART-DRW-01: renders nothing when cartOpen is false" — useDrawers mock with cartOpen: false; assert screen.queryByTestId("sheet") is null
    - "CART-DRW-02: renders sheet when cartOpen is true" — useDrawers mock with cartOpen: true; assert screen.getByTestId("sheet") exists
    - "CART-DRW-03: calls closeCart when onOpenChange fires false" — cartOpen: true, trigger sheet onOpenChange(false), assert closeCart called
    - "CART-DRW-04: renders SheetTitle with text 'Кошик'"
    Mocks needed: @/lib/drawers/drawer-context (useDrawers), @/components/ui/sheet (Sheet mock from context), @/components/cart/cart-drawer-content (stub: CartDrawerContent → div with data-testid="cart-drawer-content")

    wishlist-nav-link.test.tsx (imports WishlistNavLink from @/components/wishlist/wishlist-nav-link):
    - "WLIST-NAV-01: renders a button element (not an anchor/link)"
    - "WLIST-NAV-02: button has aria-label 'Обране' when count is 0"
    - "WLIST-NAV-03: button has aria-label 'Обране, 2 товарів' when hasSession=true initialCount=2"
    - "WLIST-NAV-04: clicking button calls openWishlist from useDrawers"
    Mocks needed: @/lib/drawers/drawer-context (useDrawers), @/lib/wishlist/guest-storage, @/lib/wishlist/wishlist-events
    Note: The EXISTING wishlist-nav-link.test.tsx does not exist yet (per VALIDATION.md Wave 0 gaps). Write it fresh — do not update.

    wishlist-drawer.test.tsx (imports WishlistDrawer from @/components/wishlist/wishlist-drawer):
    - "WLIST-DRW-01: renders nothing when wishlistOpen is false"
    - "WLIST-DRW-02: renders sheet when wishlistOpen is true"
    - "WLIST-DRW-03: renders SheetTitle with text 'Обране'"
    Mocks needed: @/lib/drawers/drawer-context (useDrawers), @/components/ui/sheet (Sheet mock), @/components/wishlist/wishlist-drawer-content (stub: WishlistDrawerContent → div with data-testid="wishlist-drawer-content")

    All four files: jsdom pragma, standard beforeEach(vi.clearAllMocks), afterEach(cleanup).
  </action>
  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npx vitest run src/components/cart/cart-nav-button.test.tsx src/components/cart/cart-drawer.test.tsx src/components/wishlist/wishlist-nav-link.test.tsx src/components/wishlist/wishlist-drawer.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - All four files exist with jsdom pragma
    - cart-nav-button.test.tsx imports CartNavButton from "@/components/cart/cart-nav-button"
    - cart-drawer.test.tsx imports CartDrawer from "@/components/cart/cart-drawer"
    - cart-drawer.test.tsx contains vi.mock("@/components/ui/sheet", ...) and vi.mock("@/lib/drawers/drawer-context", ...)
    - wishlist-nav-link.test.tsx imports WishlistNavLink from "@/components/wishlist/wishlist-nav-link"
    - wishlist-drawer.test.tsx imports WishlistDrawer from "@/components/wishlist/wishlist-drawer"
    - All four test runs exit non-zero (RED — component files do not exist yet)
  </acceptance_criteria>
  <done>Four test stub files created; all fail with import-level or assertion errors (RED state)</done>
</task>

<task type="auto">
  <name>Task 3: Update storefront-fabs.test.tsx FAB-01-a and FAB-01-b assertions</name>
  <files>src/components/layout/storefront-fabs.test.tsx</files>
  <read_first>
    - src/components/layout/storefront-fabs.test.tsx (full file — current FAB-01-a and FAB-01-b test cases on lines 62-76)
    - src/components/layout/storefront-fabs.tsx (full file — current cart FAB is a Link; these tests will fail after this update since the component is not yet changed)
  </read_first>
  <action>
    Update two existing test cases in src/components/layout/storefront-fabs.test.tsx and add the useDrawers mock.

    Changes:
    1. Add vi.mock for @/lib/drawers/drawer-context at the top of the file (after the existing vi.mock blocks, before the React import). Use the standard mock shape: useDrawers returns { cartOpen: false, wishlistOpen: false, openCart: vi.fn(), closeCart: vi.fn(), openWishlist: vi.fn(), closeWishlist: vi.fn() }.

    2. Add import: import { useDrawers } from "@/lib/drawers/drawer-context"; (after the existing `import { useChat }` line).

    3. Replace FAB-01-a (lines 62-68):
       Old: getByRole("link", { name: "Кошик" }) — asserts element is a link
       New: getByRole("button", { name: "Кошик" }) — asserts element is a button

    4. Replace FAB-01-b (lines 70-76):
       Old: asserts href="/koszyk" on the cart link
       New: "FAB-01-b: clicking cart FAB calls openCart from useDrawers" — click the button with name "Кошик", assert vi.mocked(useDrawers().openCart) was called once. To assert on mock, store the openCart spy: const openCartMock = vi.fn(); vi.mocked(useDrawers).mockReturnValue({ ...defaultValue, openCart: openCartMock }); then after click assert openCartMock.mock.calls.length === 1.

    Do not change any other test cases. Leave FAB-01-c through FAB-04-d untouched.
  </action>
  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npx vitest run src/components/layout/storefront-fabs.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - storefront-fabs.test.tsx contains vi.mock("@/lib/drawers/drawer-context", ...) block
    - FAB-01-a uses getByRole("button", { name: "Кошик" }) — not getByRole("link")
    - FAB-01-b asserts openCart mock was called on click — does not assert href="/koszyk"
    - Test run exits non-zero: FAB-01-a and FAB-01-b fail because storefront-fabs.tsx still has a Link (RED — expected)
    - Tests FAB-01-c through FAB-04-d are unmodified and still pass
  </acceptance_criteria>
  <done>storefront-fabs.test.tsx updated; FAB-01-a and FAB-01-b now fail (RED) against the unmodified component; all other tests remain green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → source modules | Test files import source modules; no user data flows here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-01-SC | Tampering | npm/pip test infrastructure | accept | No new packages introduced; vitest and @testing-library/react already validated in prior phases |

Note: This plan creates only test stubs — no production code, no auth surface, no user data. Security surface is zero. Threat register is minimal by design.
</threat_model>

<verification>
All 5 new test files exist and return non-zero exit (RED).
storefront-fabs.test.tsx FAB-01-a and FAB-01-b return non-zero (RED).
All other storefront-fabs tests (FAB-01-c through FAB-04-d) remain green.
Combined: `npx vitest run src/lib/drawers/drawer-context.test.tsx src/components/cart/cart-nav-button.test.tsx src/components/cart/cart-drawer.test.tsx src/components/wishlist/wishlist-nav-link.test.tsx src/components/wishlist/wishlist-drawer.test.tsx` — all non-zero.
</verification>

<success_criteria>
- 5 new test files created with jsdom pragma and appropriate it() blocks
- storefront-fabs.test.tsx updated with useDrawers mock + revised FAB-01-a/FAB-01-b assertions
- All 5 new test files fail (RED — correct TDD state before implementation)
- FAB-01-a and FAB-01-b fail (RED); FAB-01-c through FAB-04-d pass (GREEN)
</success_criteria>

<output>
Create `.planning/phases/50-cart-wishlist-drawers/50-01-SUMMARY.md` when done
</output>
