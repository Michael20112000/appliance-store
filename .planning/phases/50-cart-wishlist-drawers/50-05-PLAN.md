---
phase: 50-cart-wishlist-drawers
plan: 05
type: execute
wave: 4
depends_on:
  - 50-03
  - 50-04
files_modified:
  - src/components/chat/chat-provider.tsx
  - src/components/layout/storefront-fabs.tsx
  - src/components/cart/cart-nav-link.tsx
  - src/components/cart/cart-nav-button.tsx
  - src/components/cart/guest-cart-nav-link.tsx
  - src/components/wishlist/wishlist-nav-link.tsx
  - src/components/cart/pdp-cart-fab.tsx
autonomous: true
requirements:
  - DRWR-01
  - DRWR-02

must_haves:
  truths:
    - "Clicking the cart FAB in StorefrontFabs calls openCart() — the browser does NOT navigate to /koszyk"
    - "Clicking the cart icon in the header (CartNavLink / CartNavButton / GuestCartNavLink) opens the cart drawer"
    - "Clicking the wishlist heart icon in the header (WishlistNavLink) opens the wishlist drawer"
    - "Clicking the PDP cart FAB (PdpCartFab) opens the cart drawer — does NOT navigate to /koszyk"
    - "CartDrawer and WishlistDrawer are rendered inside ChatProvider's tree (DrawerProvider wraps ChatContext.Provider)"
    - "npx vitest run src/components/layout/storefront-fabs.test.tsx exits 0 (FAB-01-a and FAB-01-b now GREEN)"
    - "npx vitest run src/components/cart/cart-nav-button.test.tsx exits 0 (GREEN)"
    - "npx vitest run src/components/wishlist/wishlist-nav-link.test.tsx exits 0 (GREEN)"
  artifacts:
    - path: "src/components/chat/chat-provider.tsx"
      provides: "DrawerProvider wrapping ChatContext.Provider + CartDrawer + WishlistDrawer rendered as siblings"
      contains: "DrawerProvider"
    - path: "src/components/cart/cart-nav-button.tsx"
      provides: "Client cart nav button with initialCount prop and openCart onClick"
      exports: ["CartNavButton"]
    - path: "src/components/cart/cart-nav-link.tsx"
      provides: "RSC wrapper that fetches count and renders CartNavButton"
  key_links:
    - from: "src/components/chat/chat-provider.tsx"
      to: "src/lib/drawers/drawer-context"
      via: "DrawerProvider wrapping ChatContext.Provider"
      pattern: "DrawerProvider"
    - from: "src/components/layout/storefront-fabs.tsx"
      to: "src/lib/drawers/drawer-context"
      via: "useDrawers().openCart"
      pattern: "openCart"
    - from: "src/components/cart/cart-nav-link.tsx"
      to: "src/components/cart/cart-nav-button"
      via: "renders CartNavButton with initialCount"
      pattern: "CartNavButton"
---

<objective>
Wire the entire phase together: (1) Place DrawerProvider inside ChatProvider, rendering CartDrawer and WishlistDrawer as siblings. (2) Convert all five Link-to-/koszyk and Link-to-/obrane entry points to buttons that call openCart/openWishlist. (3) Create CartNavButton as the new client component for the header cart icon.

Purpose: Delivers the final user-visible behavior of DRWR-01 and DRWR-02 — no navigation on cart/wishlist icon click, drawers open instead.
Output: ChatProvider wired with DrawerProvider; StorefrontFabs, GuestCartNavLink, WishlistNavLink, PdpCartFab converted; CartNavButton created; CartNavLink updated to RSC wrapper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-cart-wishlist-drawers/50-RESEARCH.md
@.planning/phases/50-cart-wishlist-drawers/50-PATTERNS.md

<interfaces>
<!-- ChatProvider current render return (lines 539-549 of chat-provider.tsx) -->
return (
  <ChatContext.Provider value={value}>
    {children}
    <StorefrontFabs phones={phones} initialCartCount={initialCartCount} hasSession={hasSession} />
    <ChatPanel />
  </ChatContext.Provider>
);

<!-- Target render return (after this plan) -->
return (
  <DrawerProvider>
    <ChatContext.Provider value={value}>
      {children}
      <StorefrontFabs phones={phones} initialCartCount={initialCartCount} hasSession={hasSession} />
      <ChatPanel />
      <CartDrawer hasSession={hasSession} />
      <WishlistDrawer hasSession={hasSession} />
    </ChatContext.Provider>
  </DrawerProvider>
);

<!-- StorefrontFabs current cart FAB (lines 68-86 of storefront-fabs.tsx) -->
<Link href="/koszyk" aria-label={...} className={cn(...)}>
  <ShoppingCart className="size-6" aria-hidden />
  {cartCount > 0 && <Badge ...>{badgeLabel}</Badge>}
</Link>

<!-- Target cart FAB in StorefrontFabs (after conversion) -->
const { openCart } = useDrawers();
<button type="button" onClick={openCart} aria-label={cartCount > 0 ? `Кошик, ${cartCount} товарів` : "Кошик"} className={cn("flex size-14 items-center justify-center rounded-full bg-primary text-primary-foreground shadow-lg", "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring relative")}>
  <ShoppingCart className="size-6" aria-hidden />
  {cartCount > 0 && <Badge className="absolute -right-0.5 -top-0.5 min-w-5 justify-center px-1 text-[10px]" aria-hidden>{badgeLabel}</Badge>}
</button>

<!-- CartNavLink current (async RSC) (cart-nav-link.tsx) -->
export async function CartNavLink({ userId }: CartNavLinkProps) {
  const count = await getCartItemCount(userId);
  return <Link href="/koszyk" ...>...</Link>;
}

<!-- CartNavButton target (new client component, cart-nav-button.tsx) -->
"use client"
CartNavButtonProps = { initialCount: number }
export function CartNavButton({ initialCount }: CartNavButtonProps) {
  const { openCart } = useDrawers();
  return <button type="button" onClick={openCart} className="relative inline-flex min-h-11 min-w-11 items-center justify-center rounded-md px-3 text-sm font-medium hover:bg-muted" aria-label={`Кошик${initialCount > 0 ? `, ${initialCount} товарів` : ""}`}>
    <ShoppingCart className="size-5" />
    {initialCount > 0 ? <Badge className="absolute -right-0.5 -top-0.5 min-w-5 justify-center px-1 text-[10px]">{initialCount > 9 ? "9+" : initialCount}</Badge> : null}
  </button>
}

<!-- CartNavLink target (RSC wrapper) -->
export async function CartNavLink({ userId }: CartNavLinkProps) {
  const count = await getCartItemCount(userId);
  return <CartNavButton initialCount={count} />;
}

<!-- GuestCartNavLink current: Link href="/koszyk" — convert to button onClick={openCart} -->
<!-- WishlistNavLink current: Link href="/obrane" — convert to button onClick={openWishlist} -->
<!-- PdpCartFab current: Link href="/koszyk" — convert to button onClick={openCart} -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire DrawerProvider into ChatProvider and render CartDrawer + WishlistDrawer</name>
  <files>src/components/chat/chat-provider.tsx</files>
  <read_first>
    - src/components/chat/chat-provider.tsx lines 530-558 (current render return + useChat hook — these are the only lines being modified)
    - src/lib/drawers/drawer-context.tsx (verify DrawerProvider export name)
    - src/components/cart/cart-drawer.tsx (verify CartDrawer props: hasSession: boolean)
    - src/components/wishlist/wishlist-drawer.tsx (verify WishlistDrawer props: hasSession: boolean)
  </read_first>
  <action>
    Modify src/components/chat/chat-provider.tsx. Only the render return block and the import section change — all other code is untouched.

    1. Add three imports (place after the existing import for StorefrontFabs):
       - import { DrawerProvider } from "@/lib/drawers/drawer-context"
       - import { CartDrawer } from "@/components/cart/cart-drawer"
       - import { WishlistDrawer } from "@/components/wishlist/wishlist-drawer"

    2. Wrap the existing return statement: replace the current return that starts with <ChatContext.Provider with:
       <DrawerProvider>
         <ChatContext.Provider value={value}>
           {children}
           <StorefrontFabs phones={phones} initialCartCount={initialCartCount} hasSession={hasSession} />
           <ChatPanel />
           <CartDrawer hasSession={hasSession} />
           <WishlistDrawer hasSession={hasSession} />
         </ChatContext.Provider>
       </DrawerProvider>

    Do NOT change: ChatContextValue type, useState, useCallback, useMemo, useEffect, Pusher wiring, ChatProvider props — only the return JSX and import section.

    Why this placement: DrawerProvider wraps ChatContext.Provider, making DrawerContext accessible to StorefrontFabs (inside ChatContext.Provider) AND to page {children} (which includes PdpCartFab). Both consumer components can call useDrawers() because DrawerProvider is their ancestor.
  </action>
  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npx tsc --noEmit 2>&1 | grep "chat-provider" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/chat/chat-provider.tsx contains `import { DrawerProvider } from "@/lib/drawers/drawer-context"`
    - src/components/chat/chat-provider.tsx contains `import { CartDrawer } from "@/components/cart/cart-drawer"`
    - src/components/chat/chat-provider.tsx contains `import { WishlistDrawer } from "@/components/wishlist/wishlist-drawer"`
    - The return block contains DrawerProvider as the outermost JSX element
    - CartDrawer hasSession={hasSession} and WishlistDrawer hasSession={hasSession} are siblings to ChatPanel
    - npx tsc --noEmit produces zero errors for chat-provider.tsx
  </acceptance_criteria>
  <done>DrawerProvider wraps ChatContext.Provider; CartDrawer and WishlistDrawer are rendered; TypeScript clean</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert StorefrontFabs cart FAB + create CartNavButton + update CartNavLink</name>
  <files>
    src/components/layout/storefront-fabs.tsx
    src/components/cart/cart-nav-button.tsx
    src/components/cart/cart-nav-link.tsx
  </files>
  <read_first>
    - src/components/layout/storefront-fabs.tsx (full file — current cart Link on lines 68-86, existing useChat import and usage pattern)
    - src/components/layout/storefront-fabs.test.tsx (full file — the updated FAB-01-a and FAB-01-b assertions from plan 01 that must now pass)
    - src/components/cart/cart-nav-link.tsx (full file — current async RSC with getCartItemCount, to be converted to RSC wrapper)
    - src/components/cart/guest-cart-nav-link.tsx (full file — badge/className pattern to replicate in CartNavButton)
    - src/components/cart/cart-nav-button.test.tsx (the failing CART-NAV tests that must now pass)
  </read_first>
  <behavior>
    - StorefrontFabs cart FAB is a button element (not an anchor) with onClick={openCart}
    - StorefrontFabs cart FAB has aria-label "Кошик" when cartCount is 0
    - StorefrontFabs cart FAB has aria-label "Кошик, N товарів" when cartCount is N > 0
    - CartNavButton renders a button (not anchor) with onClick calling openCart from useDrawers
    - CartNavButton aria-label is "Кошик" when initialCount=0, "Кошик, 2 товарів" when initialCount=2
    - CartNavButton shows badge "3" when initialCount=3, "9+" when initialCount>9
    - CartNavLink (RSC) fetches count and renders CartNavButton with initialCount={count}
  </behavior>
  <action>
    storefront-fabs.tsx changes:
    1. Add import: import { useDrawers } from "@/lib/drawers/drawer-context"
    2. Add destructuring in component body (alongside the useChat destructuring): const { openCart } = useDrawers()
    3. Replace the cart <Link href="/koszyk" ...> block (lines 68-86) with an equivalent <button type="button" onClick={openCart} ...> using identical className, aria-label, and badge child
    4. Remove `import Link from "next/link"` (no other Link remains in storefront-fabs.tsx — verify before removing)

    cart-nav-button.tsx (NEW file):
    - "use client" directive
    - Props: CartNavButtonProps = { initialCount: number }
    - export function CartNavButton({ initialCount }: CartNavButtonProps)
    - const { openCart } = useDrawers()
    - return <button type="button" onClick={openCart} className="relative inline-flex min-h-11 min-w-11 items-center justify-center rounded-md px-3 text-sm font-medium hover:bg-muted" aria-label={`Кошик${initialCount > 0 ? `, ${initialCount} товарів` : ""}`}>
        <ShoppingCart className="size-5" />
        {initialCount > 0 ? <Badge className="absolute -right-0.5 -top-0.5 min-w-5 justify-center px-1 text-[10px]">{initialCount > 9 ? "9+" : initialCount}</Badge> : null}
      </button>
    - Imports: useDrawers from @/lib/drawers/drawer-context; ShoppingCart from lucide-react; Badge from @/components/ui/badge

    cart-nav-link.tsx changes (RSC wrapper):
    - Remove `import Link from "next/link"` (no longer renders Link directly)
    - Add import: import { CartNavButton } from "./cart-nav-button"
    - Keep: `import { getCartItemCount } from "@/server/services/cart.service"` and the async function signature
    - Replace the return: was <Link href="/koszyk" ...>...</Link>; now return <CartNavButton initialCount={count} />
    - The function remains async and keeps `await getCartItemCount(userId)`
  </action>
  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npx vitest run src/components/layout/storefront-fabs.test.tsx src/components/cart/cart-nav-button.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - storefront-fabs.tsx contains `const { openCart } = useDrawers()` in component body
    - storefront-fabs.tsx cart element is a `<button` not `<Link` — grep for `href="/koszyk"` in storefront-fabs.tsx returns no match
    - src/components/cart/cart-nav-button.tsx exists with "use client" directive and exports CartNavButton
    - CartNavButton renders button not anchor: grep for `<button` in cart-nav-button.tsx returns a match
    - cart-nav-link.tsx contains `import { CartNavButton }` and renders `<CartNavButton initialCount={count} />`
    - cart-nav-link.tsx does NOT contain `href="/koszyk"`
    - npx vitest run src/components/layout/storefront-fabs.test.tsx exits 0 (FAB-01-a and FAB-01-b now GREEN)
    - npx vitest run src/components/cart/cart-nav-button.test.tsx exits 0 (all CART-NAV tests GREEN)
  </acceptance_criteria>
  <done>StorefrontFabs cart FAB converted to button; CartNavButton created; CartNavLink updated to RSC wrapper; all tests GREEN</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Convert GuestCartNavLink, WishlistNavLink, and PdpCartFab</name>
  <files>
    src/components/cart/guest-cart-nav-link.tsx
    src/components/wishlist/wishlist-nav-link.tsx
    src/components/cart/pdp-cart-fab.tsx
  </files>
  <read_first>
    - src/components/cart/guest-cart-nav-link.tsx (full file — current Link, useEffect, badge logic)
    - src/components/wishlist/wishlist-nav-link.tsx (full file — current Link, pathname, badge logic, storage listeners)
    - src/components/cart/pdp-cart-fab.tsx (full file — current Link, FAB styling)
    - src/components/wishlist/wishlist-nav-link.test.tsx (the failing WLIST-NAV tests that must now pass)
    - .planning/phases/50-cart-wishlist-drawers/50-PATTERNS.md sections for guest-cart-nav-link, wishlist-nav-link, and pdp-cart-fab (exact modification instructions)
  </read_first>
  <behavior>
    - GuestCartNavLink renders a button (not anchor) with onClick calling openCart from useDrawers
    - WishlistNavLink renders a button (not anchor) with onClick calling openWishlist from useDrawers
    - WishlistNavLink aria-label is "Обране" when count=0, "Обране, 2 товарів" when count=2 with hasSession=true, initialCount=2
    - PdpCartFab renders a button (not anchor) with onClick calling openCart from useDrawers
    - PdpCartFab aria-label changes from "Перейти до кошика" to "Відкрити кошик"
    - All three components retain their existing badge rendering and count state logic
  </behavior>
  <action>
    guest-cart-nav-link.tsx minimal changes:
    1. Add import: import { useDrawers } from "@/lib/drawers/drawer-context"
    2. Add in component body: const { openCart } = useDrawers()
    3. Replace <Link href="/koszyk" className="..." aria-label={...}> with <button type="button" className="..." aria-label={...} onClick={openCart}>
    4. Replace closing </Link> with </button>
    5. Remove import Link from "next/link"
    Keep: useEffect, CART_CHANGED_EVENT listener, useState, badge logic — all unchanged.

    wishlist-nav-link.tsx changes:
    1. Add import: import { useDrawers } from "@/lib/drawers/drawer-context"
    2. Add in component body: const { openWishlist } = useDrawers()
    3. Remove: const pathname = usePathname() (no longer needed — aria-current based on pathname is removed)
    4. Remove: import { usePathname } from "next/navigation" (check whether usePathname is used anywhere else in the file; if not, remove the import)
    5. Replace <Link href="/obrane" className="..." aria-label={ariaLabel} aria-current={pathname === "/obrane" ? "page" : undefined}> with <button type="button" className="..." aria-label={ariaLabel} onClick={openWishlist}>
    6. Replace closing </Link> with </button>
    7. Remove import Link from "next/link"
    Keep: useEffect, WISHLIST_CHANGED_EVENT listener, storage listener, GUEST_WISHLIST_KEY, getGuestWishlistCount, badge logic — all unchanged.

    pdp-cart-fab.tsx changes:
    1. Add import: import { useDrawers } from "@/lib/drawers/drawer-context"
    2. Add in component body: const { openCart } = useDrawers()
    3. Replace <Link href="/koszyk" className={cn(...)} aria-label="Перейти до кошика"> with <button type="button" className={cn(...)} aria-label="Відкрити кошик" onClick={openCart}>
    4. Replace closing </Link> with </button>
    5. Remove import Link from "next/link"
    Keep: if (count < 1) return null guard, useEffect, CART_CHANGED_EVENT listener, badge logic, initialCount/hasSession props — all unchanged.
  </action>
  <verify>
    <automated>cd /Users/michael_ivashko/WebStormProjects/web/appliance-store && npx vitest run src/components/wishlist/wishlist-nav-link.test.tsx 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - guest-cart-nav-link.tsx contains `const { openCart } = useDrawers()` and renders `<button` not `<Link`
    - guest-cart-nav-link.tsx does NOT contain `href="/koszyk"` — grep returns no match
    - wishlist-nav-link.tsx contains `const { openWishlist } = useDrawers()` and renders `<button` not `<Link`
    - wishlist-nav-link.tsx does NOT contain `href="/obrane"` — grep returns no match
    - wishlist-nav-link.tsx does NOT contain `usePathname` (removed with aria-current)
    - pdp-cart-fab.tsx contains `const { openCart } = useDrawers()` and renders `<button` not `<Link`
    - pdp-cart-fab.tsx does NOT contain `href="/koszyk"` — grep returns no match
    - pdp-cart-fab.tsx aria-label is "Відкрити кошик" (not "Перейти до кошика")
    - npx vitest run src/components/wishlist/wishlist-nav-link.test.tsx exits 0 (all WLIST-NAV tests GREEN)
    - npx tsc --noEmit produces no errors for the three modified files
  </acceptance_criteria>
  <done>GuestCartNavLink, WishlistNavLink, and PdpCartFab all converted to buttons; wishlist-nav-link tests GREEN; TypeScript clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User click → openCart/openWishlist | Client-side event; calls context function; no data crosses trust boundary |
| CartDrawer/WishlistDrawer → server actions | Covered in plans 03/04 threat models |
| DrawerProvider → ChatProvider tree | Client-side React context hierarchy; no sensitive data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-05-01 | Tampering | chat-provider.tsx DrawerProvider placement | accept | DrawerProvider wraps ChatContext.Provider; wrong placement would cause runtime errors (useDrawers throws) caught immediately in testing |
| T-50-05-02 | Information Disclosure | CartDrawer hasSession prop passed from ChatProvider | accept | hasSession is a boolean (not the session object); no sensitive data leaked via prop |
| T-50-05-03 | Spoofing | Button elements without type="button" can submit forms | mitigate | All converted buttons explicitly set type="button" — no accidental form submission |
| T-50-05-04 | Denial of Service | Multiple simultaneous openCart calls (double-click) | accept | useState setter is idempotent; setCartOpen(true) twice is harmless |
| T-50-05-SC | Tampering | npm install | accept | No new packages installed |
</threat_model>

<verification>
npx vitest run src/components/layout/storefront-fabs.test.tsx — exits 0 (all tests GREEN including FAB-01-a/FAB-01-b)
npx vitest run src/components/cart/cart-nav-button.test.tsx — exits 0 (all CART-NAV tests GREEN)
npx vitest run src/components/wishlist/wishlist-nav-link.test.tsx — exits 0 (all WLIST-NAV tests GREEN)
npx vitest run — full suite green (no regressions in other components)
npx tsc --noEmit — zero errors
grep -r "href=\"/koszyk\"" src/components/layout/storefront-fabs.tsx src/components/cart/cart-nav-link.tsx src/components/cart/guest-cart-nav-link.tsx src/components/cart/pdp-cart-fab.tsx — returns no matches
grep -r "href=\"/obrane\"" src/components/wishlist/wishlist-nav-link.tsx — returns no match
grep "DrawerProvider" src/components/chat/chat-provider.tsx — returns a match
</verification>

<success_criteria>
- DrawerProvider wraps ChatContext.Provider in chat-provider.tsx; CartDrawer and WishlistDrawer rendered as siblings to ChatPanel
- All five Link → button conversions complete: StorefrontFabs cart FAB, CartNavButton (new), CartNavLink (RSC wrapper), GuestCartNavLink, WishlistNavLink, PdpCartFab
- No href="/koszyk" or href="/obrane" remains in any of the five converted files
- All test suites GREEN: storefront-fabs, cart-nav-button, wishlist-nav-link
- Full Vitest suite passes (no regressions)
- TypeScript compiles cleanly across all modified files
</success_criteria>

<output>
Create `.planning/phases/50-cart-wishlist-drawers/50-05-SUMMARY.md` when done
</output>
