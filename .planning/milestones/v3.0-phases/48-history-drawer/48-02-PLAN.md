---
phase: 48-history-drawer
plan: 02
type: execute
wave: 2
depends_on:
  - 48-01
files_modified:
  - src/server/services/chat.service.ts
  - src/app/api/chat/conversations/route.ts
autonomous: true
requirements:
  - CHAT-07

must_haves:
  truths:
    - "listConversationsForBuyer returns all conversations for a userId (no isActive filter)"
    - "listConversationsForBuyer returns items ordered lastMessageAt desc, capped at 50"
    - "Each item has buyerName: 'Ви', buyerEmail: '', unreadForAdmin: false placeholders"
    - "GET /api/chat/conversations returns 401 for unauthenticated requests"
    - "GET /api/chat/conversations returns { conversations: [...] } for authenticated users"
  artifacts:
    - path: "src/server/services/chat.service.ts"
      provides: "listConversationsForBuyer exported function"
      contains: "export async function listConversationsForBuyer"
    - path: "src/app/api/chat/conversations/route.ts"
      provides: "GET handler for buyer conversation history"
      exports: ["GET"]
  key_links:
    - from: "src/app/api/chat/conversations/route.ts"
      to: "src/server/services/chat.service.ts"
      via: "import { listConversationsForBuyer }"
      pattern: "listConversationsForBuyer"
    - from: "src/app/api/chat/conversations/route.ts"
      to: "@/lib/auth"
      via: "auth.api.getSession"
      pattern: "auth\\.api\\.getSession"
---

<objective>
Implement the backend for the history drawer: the service function that queries all buyer
conversations and the auth-gated API route that exposes it.

Purpose: Provides the data layer the HistoryDrawer component (Plan 03) will fetch from.
The RED stubs from Plan 01 turn GREEN when this plan's code ships.

Output: listConversationsForBuyer in chat.service.ts, GET /api/chat/conversations route.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-history-drawer/48-RESEARCH.md
@.planning/phases/48-history-drawer/48-PATTERNS.md
@.planning/phases/48-history-drawer/48-01-SUMMARY.md

<interfaces>
<!-- Existing type from src/types/chat.ts — listConversationsForBuyer returns this shape -->
<!-- No new type needed — use ConversationSummaryDto with buyerName/buyerEmail placeholders -->

From src/types/chat.ts:
```typescript
export type ConversationSummaryDto = {
  id: string;
  userId: string | null;
  status: ConversationStatus;
  buyerName: string;
  buyerEmail: string;
  lastMessagePreview: string | null;
  lastMessageAt: string | null;
  unreadForAdmin: boolean;
};
```

From src/server/services/chat.service.ts (auth guard pattern, existing route):
```typescript
// Analog: auth.api.getSession pattern used in GET /api/chat/messages
const session = await auth.api.getSession({ headers: await headers() });
if (!session?.user) {
  return Response.json({ error: "UNAUTHORIZED" }, { status: 401 });
}
```

From src/server/services/chat.service.ts (analog: listConversationsForAdmin):
```typescript
export async function listConversationsForAdmin(options: {
  status: ConversationStatus;
}): Promise<ConversationSummaryDto[]> {
  const conversations = await prisma.conversation.findMany({
    where: { status: options.status },
    orderBy: { lastMessageAt: "desc" },
  });
  // ...maps to ConversationSummaryDto shape
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add listConversationsForBuyer to chat.service.ts</name>
  <files>src/server/services/chat.service.ts</files>
  <read_first>
    - src/server/services/chat.service.ts (read ENTIRE file — find the end of listConversationsForAdmin to place the new function after it; confirm existing imports cover ConversationSummaryDto and prisma)
    - src/server/services/chat.service.test.ts (review the RED stub to confirm the exact assertions the implementation must satisfy)
    - src/types/chat.ts (confirm ConversationSummaryDto shape — specifically that userId field exists on the type)
  </read_first>
  <behavior>
    - listConversationsForBuyer("buyer-1") calls prisma.conversation.findMany with { where: { userId: "buyer-1" }, orderBy: { lastMessageAt: "desc" }, take: 50, select: { id, userId, status, lastMessagePreview, lastMessageAt } }
    - The where clause contains ONLY { userId } — no isActive field, no status field
    - Each result row maps to ConversationSummaryDto with buyerName: "Ви", buyerEmail: "", unreadForAdmin: false
    - lastMessageAt is toISOString() if non-null, null otherwise
    - Returns [] when findMany returns []
  </behavior>
  <action>
    Place the new function after the closing brace of listConversationsForAdmin (search for the end of that function by its return statement).

    Function signature: export async function listConversationsForBuyer(userId: string): Promise<ConversationSummaryDto[]>

    Prisma query: prisma.conversation.findMany with where: { userId }, orderBy: { lastMessageAt: "desc" }, take: 50, select: { id: true, userId: true, status: true, lastMessagePreview: true, lastMessageAt: true }.

    Map each row: { id: row.id, userId: row.userId, status: row.status, buyerName: "Ви", buyerEmail: "", lastMessagePreview: row.lastMessagePreview, lastMessageAt: row.lastMessageAt?.toISOString() ?? null, unreadForAdmin: false }.

    No imports need to be added — prisma and ConversationSummaryDto are already imported at the top of the file.
  </action>
  <verify>
    <automated>npx vitest run src/server/services/chat.service.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    All 3 listConversationsForBuyer tests pass GREEN. The @ts-expect-error comment on the import in the test file now causes a TS error — if vitest/tsc complains about a redundant @ts-expect-error, remove it from the test file import. Total chat.service.test.ts failures do not increase beyond pre-phase baseline.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create GET /api/chat/conversations route</name>
  <files>src/app/api/chat/conversations/route.ts</files>
  <read_first>
    - src/app/api/chat/messages/route.ts (lines 171-198 — exact auth guard pattern to copy)
    - src/app/api/chat/conversations/route.test.ts (review RED stubs — confirm GET takes no parameters, confirm response shape { conversations: [...] })
    - src/app/api/chat/claim/route.ts (import pattern for auth + headers)
  </read_first>
  <behavior>
    - GET() with no session → Response.json({ error: "UNAUTHORIZED" }, { status: 401 })
    - GET() with valid session → calls listConversationsForBuyer(session.user.id) → Response.json({ conversations: [...] }, status 200)
    - Route handler function is named GET (not get) and exported as a named export
    - Route takes no request parameter (unlike messages route which takes Request for query params)
  </behavior>
  <action>
    Create the directory src/app/api/chat/conversations/ if it does not exist, then create route.ts.

    Imports (three lines only):
    - import { headers } from "next/headers"
    - import { auth } from "@/lib/auth"
    - import { listConversationsForBuyer } from "@/server/services/chat.service"

    Export async function GET() (no parameters):
    1. const session = await auth.api.getSession({ headers: await headers() })
    2. if (!session?.user) return Response.json({ error: "UNAUTHORIZED" }, { status: 401 })
    3. const conversations = await listConversationsForBuyer(session.user.id)
    4. return Response.json({ conversations })

    No try/catch needed — listConversationsForBuyer does not throw ChatServiceError.
    No runtime config export needed — plain route handler.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/chat/conversations/route.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    Both route.test.ts tests pass GREEN: 401 for no session, 200 with { conversations } for auth user. TypeScript compiles without errors on the new file (npx tsc --noEmit src/app/api/chat/conversations/route.ts exits 0 or reports no new errors).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /api/chat/conversations | Unauthenticated clients must be rejected; authenticated clients receive only their own conversations |
| route → service | Service receives session.user.id from trusted session object, not from client request body/params |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-02-01 | Information Disclosure | GET /api/chat/conversations | mitigate | auth.api.getSession check returns 401 before service is called; listConversationsForBuyer filters by session.user.id (not a client-supplied param) |
| T-48-02-02 | Information Disclosure | listConversationsForBuyer | mitigate | Prisma where clause is { userId } from server session — no client-controlled input in the query predicate |
| T-48-02-03 | Elevation of Privilege | GET /api/chat/conversations | mitigate | Route is auth-only (401 for guests); no admin-only data (buyerEmail, unreadForAdmin are placeholders) leaks |
| T-48-02-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this plan |
</threat_model>

<verification>
After both tasks:

```
npx vitest run src/server/services/chat.service.test.ts src/app/api/chat/conversations/route.test.ts 2>&1 | grep -E "(FAIL|PASS|Tests)"
```

Expected: all previously RED stubs for listConversationsForBuyer and GET /api/chat/conversations are now GREEN.

Full suite:
```
npx vitest run 2>&1 | grep -E "^(Tests|Test Files)" | tail -5
```

Failure count must not exceed pre-phase baseline (5 pre-existing failures). The 3 RED stubs from plan 01 for chat.service.test.ts and route.test.ts are now GREEN; chat-panel.test.tsx stubs remain RED (plan 03).
</verification>

<success_criteria>
- listConversationsForBuyer is exported from chat.service.ts, queries { userId } with no isActive filter, maps to ConversationSummaryDto with buyerName: "Ви"
- GET /api/chat/conversations exists, returns 401 for guests, returns { conversations } for auth users
- All 5 tests from plan 01 (3 service + 2 route) are GREEN
- TypeScript finds no errors in the two new/modified files
</success_criteria>

<output>
Create `.planning/phases/48-history-drawer/48-02-SUMMARY.md` when done
</output>
